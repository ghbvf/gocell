// Code generated by gocell generate assembly. DO NOT EDIT.
package main

import (
	"context"
	"log/slog"
	"os"
	"os/signal"
	"syscall"

	"github.com/ghbvf/gocell/kernel/assembly"
	// Cell imports
	// TODO: import access-core cell package
	// TODO: import audit-core cell package
	// TODO: import config-core cell package
)

func main() {
	app := assembly.New(assembly.Config{ID: "core-bundle"})

	// Register cells
	// TODO: app.Register(access-core.NewCore(...))
	// TODO: app.Register(audit-core.NewCore(...))
	// TODO: app.Register(config-core.NewCore(...))

	ctx, cancel := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer cancel()

	if err := app.Start(ctx); err != nil {
		slog.Error("failed to start assembly", "error", err)
		os.Exit(1)
	}

	<-ctx.Done()
	slog.Info("shutting down")

	if err := app.Stop(context.Background()); err != nil {
		slog.Error("failed to stop assembly", "error", err)
		os.Exit(1)
	}
}
