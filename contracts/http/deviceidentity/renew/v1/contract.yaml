id: http.deviceidentity.renew.v1
kind: http
ownerCell: _framework
consistencyLevel: L1
lifecycle: draft
description: |
  Device identity certificate renewal — reissues a certificate for an existing device.
  ref: RFC 7030 §4.2 simplereenroll
  ref: docs/architecture/202606130635-1939-adr-framework-owned-contract.md
  注：同步签发前端；cert-issued outbox emit（升 L2）随 PR-9 #1905 落地。
endpoints:
  server: _framework
  clients: []
  http:
    method: POST
    path: /api/v1/deviceidentity/renew
    successStatus: 200
    auth:
      public: true
      reason: "Certificate renewal authenticates by the device's EXISTING client certificate via mutual TLS on the dedicated device-mTLS listener (kernel/auth.AuthMTLS + middleware.MTLS PeerIdentity); transport-layer peer authentication replaces the JWT/ABAC route gate (the ABAC PDP Authorizer is injected on the primary listener only). The device identity (tenant + device) is recovered from the presented certificate's SPIFFE URI SAN, and authorization (device:enroll) is enforced inside the handler via the certsigning Authorizer (PDP)."
    noContent: false
    responses:
      400:
        description: "Bad request — malformed JSON body, missing required fields, or field validation error"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      401:
        description: "Unauthorized — missing or invalid bearer token"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      403:
        description: "Forbidden — authenticated but lacks required permission"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      404:
        description: "Not Found — device or prior certificate not found"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      413:
        description: "Payload Too Large — request body exceeds the size limit"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      422:
        description: "Unprocessable Entity — request body is structurally valid JSON but contains semantic validation errors"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
      503:
        description: "Service Unavailable — CA backend is not reachable"
        schemaRef: "../../../../shared/errors/error-response-v1.schema.json"
schemaRefs:
  request: request.schema.json
  response: response.schema.json
