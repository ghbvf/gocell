{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "http.registry.contract.reject.v1.request",
  "description": "Reject a probing or pending-approval contract registration. The id is the path parameter; the body carries an optional free-form rejection reason (recorded on the registration's append-only migration event as the transition Reason).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "reason": {
      "type": "string",
      "maxLength": 1024,
      "description": "Optional free-form rejection reason recorded as the transition Reason."
    }
  }
}
