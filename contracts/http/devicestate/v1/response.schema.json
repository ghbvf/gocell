{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "http.devicestate.v1.response",
  "type": "object",
  "required": [
    "data"
  ],
  "properties": {
    "data": {
      "type": "object",
      "required": [
        "deviceId",
        "state",
        "observedAt",
        "lastSeenAt",
        "tenantId"
      ],
      "properties": {
        "deviceId": {
          "type": "string",
          "maxLength": 256
        },
        "state": {
          "type": "string",
          "enum": [
            "online",
            "offline",
            "unknown"
          ]
        },
        "observedAt": {
          "type": "string",
          "format": "date-time",
          "maxLength": 64,
          "description": "Time this presence reading was determined (freshness anchor); always present even when state is unknown."
        },
        "lastSeenAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "maxLength": 64,
          "description": "Last actual device contact time. null when the device has never been seen; the key is always present (stable column set, #1875)."
        },
        "tenantId": {
          "type": "string",
          "maxLength": 256
        }
      }
    }
  }
}
