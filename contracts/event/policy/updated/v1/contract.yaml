id: event.policy.updated.v1
description: "Metadata-only notification: subscribers MUST refetch current policy via GET /api/v1/access/policies/{id}. Producer-only and lifecycle: draft until the first real consumer lands — promote to active when MDM (#1051) and zerotrust (#1052) continuous-eval consumers subscribe. draft (not active) keeps this off the ADV-05 dead-event check while it has no subscriber."
kind: event
ownerCell: accesscore
consistencyLevel: L2
lifecycle: draft
endpoints:
  publisher: accesscore
schemaRefs:
  headers: headers.schema.json
  payload: payload.schema.json
replayable: false
idempotencyKey: eventId
deliverySemantics: at-least-once
