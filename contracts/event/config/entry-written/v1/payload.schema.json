{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event.config.entry-written.v1.payload",
  "type": "object",
  "description": "Emitted by configwrite on Create/Update/Delete. value and version are present on created/updated and omitted on deleted (consumer only needs key to evict its cache).",
  "properties": {
    "action": { "type": "string", "enum": ["created", "updated", "deleted"] },
    "key": { "type": "string" },
    "value": { "type": "string" },
    "version": { "type": "integer" }
  },
  "required": ["action", "key"],
  "additionalProperties": false
}
