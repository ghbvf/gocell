{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event.config.rollback.v1.payload",
  "type": "object",
  "description": "Emitted by configpublish.Rollback when a config entry is reverted to a prior version. Topic name is the discriminator — no action field.",
  "properties": {
    "key": { "type": "string" },
    "targetVersion": { "type": "integer" },
    "newVersion": { "type": "integer" },
    "actorId": { "type": "string" }
  },
  "required": ["key", "targetVersion", "newVersion", "actorId"],
  "additionalProperties": false
}
