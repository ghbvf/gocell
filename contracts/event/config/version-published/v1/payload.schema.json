{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event.config.version-published.v1.payload",
  "type": "object",
  "description": "Emitted by configpublish.Publish when a new ConfigVersion snapshot is created. Topic name is the discriminator — no action field.",
  "properties": {
    "key": {
      "type": "string"
    },
    "configId": {
      "type": "string"
    },
    "version": {
      "type": "integer"
    },
    "actorId": {
      "type": "string"
    }
  },
  "required": [
    "key",
    "configId",
    "version",
    "actorId"
  ],
  "unevaluatedProperties": false
}
