{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event.user.locked.v1.payload",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "actorId": {
      "type": "string",
      "description": "Identity of the principal that triggered the lock. UUID for admin-initiated locks; the sentinel 'system' for auto-locks triggered by accountlockout policy (cells/accesscore/internal/accountlockout)."
    }
  },
  "required": [
    "userId",
    "actorId"
  ]
}
