// Package metadata provides a parser that loads all GoCell YAML descriptors
// (cell.yaml, slice.yaml, contract.yaml, assembly.yaml, journey.yaml) from a
// project root and produces a validated ProjectMeta model.
package metadata
