// Code generated by `gocell scaffold slice`. DO NOT EDIT THE TODO MARKERS BLINDLY.
// Replace TODO comments with real wiring before activating this slice in production.
package {{.ID}}

import (
	"net/http"

	"github.com/ghbvf/gocell/pkg/httputil"
)

// Handler is the HTTP handler for the {{.ID}} slice.
//
// TODO: inject the slice's domain Service via NewHandler.
type Handler struct{}

// NewHandler constructs a Handler. Add Service / repository deps as parameters
// when wiring real business logic.
func NewHandler() *Handler { return &Handler{} }

// HandleGet is a placeholder GET /api/v1/TODO/{id} handler that demonstrates
// the canonical UUID path-param validation pattern enforced by the CH-05
// governance rule. Replace the body with real business logic.
//
// The contract.yaml MUST declare:
//
//	pathParams:
//	  id:
//	    type: string
//	    format: uuid     # → CH-05 requires httputil.ParseUUIDPathParam call below
//	responses:
//	  400: { schemaRef: ... }   # ERR_VALIDATION_INVALID_UUID, written by the helper
//	  404: { schemaRef: ... }   # ErrXxxNotFound from your domain layer
func (h *Handler) HandleGet(w http.ResponseWriter, r *http.Request) {
	id, ok := httputil.ParseUUIDPathParam(w, r, "id")
	if !ok {
		return
	}
	_ = id // TODO: pass to service / repository
	w.WriteHeader(http.StatusNotImplemented)
}
