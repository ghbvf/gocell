{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "event.devicecert-rotation-resolved.v1.headers",
  "description": "Event envelope headers. eventId is the idempotency key declared in contract.yaml; it is carried in outbox.Entry.ID at the transport level.",
  "type": "object",
  "properties": {
    "eventId": {
      "type": "string",
      "description": "Prefixed event identifier (evt-{uuid}), used as idempotency key"
    }
  },
  "required": [
    "eventId"
  ]
}
