# commandType made required in-place on active v1 (#1694 F9) — permitted by the
# Pre-GA wire-break window in .claude/rules/gocell/api-versioning.md §兼容窗口
# (until 2026-12-31; no external wire consumers). After the window, breaking wire
# changes rev the version dir.
id: command.remotecommand.v1
kind: command
ownerCell: devicecell
consistencyLevel: L4
lifecycle: active
endpoints:
  handler: devicecell
  invokers: [devicecell]
schemaRefs:
  request: request.schema.json
  response: response.schema.json
