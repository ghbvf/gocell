---
name: git-worktree
description: Git Worktree 项目约定（编号、基准分支、权限兼容、删除安全）。涉及 worktree 创建、编号、删除、Per-PR worktree 等操作时自动激活。
alwaysApply: true
---

# Git Worktree 项目约定

执行 `.claude/skills/git-worktree/SKILL.md` 中定义的 Git Worktree 约定。

涉及 worktree 创建、编号、删除、Per-PR worktree 等操作时，**必须**遵守该技能中的所有约束。

详见 [git-worktree SKILL.md](../../.claude/skills/git-worktree/SKILL.md)
