id: configsubscribe
belongsToCell: configcore
contractUsages:
  - contract: event.config.entry-written.v1
    role: subscribe
  - contract: event.config.version-published.v1
    role: subscribe
verify:
  unit:
    - unit.configsubscribe.service
  contract:
    - contract.event.config.entry-written.v1.subscribe
    - contract.event.config.version-published.v1.subscribe
  waivers: []

allowedFiles:
  - cells/configcore/slices/configsubscribe/**
