{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "http.registry.contract.submit.v1.response",
  "description": "The created contract registration, projected from the kernel ContractRegistration (#2233).",
  "type": "object",
  "required": [
    "data"
  ],
  "properties": {
    "data": {
      "type": "object",
      "required": [
        "id",
        "kind",
        "state",
        "submitter",
        "createdAt",
        "updatedAt"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "state": {
          "type": "string",
          "description": "Lifecycle state; the sealed RegistrationState wire spelling (a new submission is 'submitted')."
        },
        "submitter": {
          "type": "string",
          "description": "Authenticated principal subject that submitted the registration."
        },
        "payloadSchema": {
          "type": "string",
          "description": "Opaque payload-schema reference recorded at submit; absent when not supplied."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  }
}
