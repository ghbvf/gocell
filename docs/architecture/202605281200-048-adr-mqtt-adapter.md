# ADR 048 — MQTT Adapter: v5-only, autopaho, DLT app-level, sealed-struct funnels

| 字段 | 值 |
|------|---|
| ADR ID | 048 |
| 状态 | Accepted |
| 日期 | 2026-05-28 |
| 实施 PR 系列 | PR-1 (`073-mqtt-adapter-pr1`) … PR-5 |
| 关联 Epic | gh issue #1138 |
| 关联 Spec | `docs/plans/specs/202605262101-047-mqtt-adapter/spec.md` |
| 关联 Plan | `docs/plans/specs/202605262101-047-mqtt-adapter/plan.md` |

---

## 1. 背景

GoCell 消息层目前只有 RabbitMQ（AMQP 0-9-1）一个 broker adapter。MDM 推送通道、IoT 标准设备通道和零信任端点心跳均需要 MQTT。本 ADR 记录 adapters/mqtt 系列 PR 的关键决策，供后续 PR 审查与未来重构参考。

---

## 2. 决策记录

### 2.1 MQTT v5 only — 不提供 v3.1.1 兼容

**决策**：仅支持 MQTT v5，不引入 v3.1.1 兼容抽象层或双协议 seam。

**理由**：
- v5 新增 User Properties（`eventId` 幂等键传递）、Session Expiry Interval、Reason Codes（精确错误分类）、Flow Control。这些特性与 GoCell 的幂等/一致性/错误分类机制深度耦合；v3 缺少 User Properties，幂等键只能嵌 payload，需要额外协议层。
- v3 双协议抽象将增加约 25% 额外代码 + 测试矩阵翻倍，且 CI broker（Mosquitto 2.0）对 v5 的支持已完整。
- 业务方（IoT MDM）当前部署目标均支持 v5。

**影响**：调用方只能使用支持 v5 的 broker（Mosquitto 2.0+、EMQX 5.x、HiveMQ 4.x）。

### 2.2 客户端库：autopaho v0.23.0

**决策**：使用 `github.com/eclipse/paho.golang/autopaho` v0.23.0 作为唯一客户端库。

**理由**：
- autopaho 是 Eclipse Paho 官方 Go v5 客户端，内置 `ConnectionManager`（自动重连、会话恢复、backoff 注入接口），与 adapters/rabbitmq 自实现 reconnect goroutine 相比节省约 400 行代码。
- `ConnackError.ReasonCode` 提供 typed reason code 枚举，支持精确 bootstrap fail-fast / 瞬态 / 永久 三分类（见 §3 威胁矩阵 CONNACK 行）。
- `ReconnectBackoff` 注入点直接对接 `adapters/adapterutil.ExponentialBackoffWithJitter`（共享 helper），无需引入第三方 backoff 库。

**版本选择**：v0.23.0（2025 最新稳定版）相比 spec 草稿中的 v0.12 增加了 `AwaitConnection`、`WaitConnected` 等接口，更适合测试场景。

ref: `eclipse/paho.golang/autopaho/auto.go` — `NewConnection`, `ClientConfig`, `ReconnectBackoff`

### 2.3 DLT（Dead Letter）app-level 路由：`$dead/<topic>`

**决策**：Disposition.Reject 在 adapter 应用层 publish 到 `$dead/<originalTopic>`，不依赖 broker 特定 DLT 插件。

**理由**：
- MQTT v5 规范不含原生 DLT 机制；EMQX/HiveMQ 的 DLT 插件是非标准扩展，绑定特定 broker。
- app-level publish 到 `$dead/<topic>` 保持 broker 无关性，独立 subscriber 可以 subscribe `$dead/#` 做死信监控，topic hierarchy 与 GoCell outbox 语义一致。
- 与 adapters/rabbitmq 的 broker-native DLX 语义不同，此处是 adapter 负责路由；PR-4 实施。

### 2.4 clientId 唯一性：双 typed function（Ephemeral / Stable）+ sealed ClientID

**决策**：clientId 格式 `{cellID}-{role}-{instanceID}`，封装在 sealed struct `ClientID` 中。提供两个 typed function（typed function choice Hard 范本）：

- `ParseEphemeralClientID(cellID, role string)` — 内部生成 uuid 作为 instanceID。`SessionExpiry == 0`（clean session）时使用，避免节点重启 session 残留。
- `ParseStableClientID(cellID, role, instanceID string)` — caller 提供稳定的 instanceID（pod name / node ID 等）。`SessionExpiry > 0`（persistent session）时使用，broker 凭 stable ClientID 恢复未确认消息。

构造器集合内部全部委托私有 `assembleClientID` 完成 `ClientID{value: value}` 字面量构造——`MQTT-CLIENT-ID-NAMESPACE-01/A2` 的 allowedFuncs 集合 = `{assembleClientID}`，是包内唯一合法构造点。

**理由**：
- 单一 `ParseClientID` 强制内嵌 uuid 锁死了持久会话语义（C6 round-1 finding F10）。typed function choice 让"选错语义 = 选错 API 名"在 caller 命名空间编译期可见，符合 ai-robust.md「typed function choice」Hard 范本。
- MQTT broker 会对 clientId 重复的新连接强制断开旧连接。ephemeral path 用 uuid 后缀避免 k8s 滚动更新场景的踢人；stable path 用 caller-supplied identity 服务持久会话。
- cellID+role 前缀保留了可读性，便于 broker 侧 ACL 配置（基于 clientId 前缀的 Topic ACL）。
- 格式约束（lowercase segment regex + length ≤ 128）由 `validateCellRole` / `validateInstance` / `assembleClientID` 在构造期强制。

### 2.5 sealed-struct funnels vs string-newtype（ClientID / TopicNamespace）

**决策**：ClientID 和 TopicNamespace 均使用 `struct{ value string }` sealed struct，而非 `type X string` string newtype。

**理由（为何 sealed struct 优于 string newtype）**：

| 属性 | `type X string` | `struct{ value string }` |
|------|----------------|--------------------------|
| 包外 literal 构造 | `X("raw")` 合法 | 编译期错误（unexported field） |
| AI-robust 上游 Hard | 仅 archtest | Go 编译器 |
| 类型转换绕过 | `X(s)` 合法 | 编译期错误（no underlying string） |
| 零值语义 | `X("")` 合法 | `X{}` 合法（.value == ""，String() 返回 ""） |

sealed struct 的上游 Hard 来自 Go 可见性规则：包外无法构造 unexported 字段的 struct literal。string newtype 在包外可以通过 `ClientID(rawString)` 类型转换绕过，必须依赖 archtest（Medium 上游）拦截。

参照：`pkg/errcode.PublicDetail` sealed-value pattern（同源设计，DETAILS-SEALED-FIELD-FROZEN-01）。

### 2.6 backoff 提升到 adapterutil 共享 helper

**决策**：指数退避 + jitter 逻辑提取到 `adapters/adapterutil/backoff.go`，rabbitmq adapter 同步重构使用同一实现。

**理由**：
- adapters/rabbitmq 原有 `rabbitmq/backoff.go` 与本 adapter 需要的逻辑完全相同。
- 单源治理（DRY），避免 backoff 参数语义在两个 adapter 间悄悄漂移。

---

## 3. 威胁矩阵 / Threat Model

| 威胁 | 缓解 | 状态 |
|------|------|------|
| connect URL 中的 credentials 泄漏到 slog/span | `redact.go::redactConnectURL` 走 `pkg/redaction.RedactString` | ✅ PR-1 |
| payload PII 泄漏到 slog/span | publish/subscribe 路径 payload 走 `pkg/redaction.RedactPayload`；span attribute 走 `safeStringAttr`（`SPAN-SETATTR-REDACT-01` 覆盖） | ✅ PR-2/3 实施 |
| CONNACK 0x81/0x82 (protocol/identifier error) | bootstrap fail-fast：onConnectError → classBootstrapFatal → bootstrapErrCh → Open 返回错误 | ✅ PR-1 |
| CONNACK 0x84/0x85/0x8A (unsupported protocol version / invalid client ID / banned) | bootstrap fail-fast（protocol/identity 配置错误不可自愈） | ✅ PR-1 |
| CONNACK 0x87/0x86/0x8C (operator-fixable not-authorized / bad-creds / bad-auth-method) | classPermanentRetain：readyz 503，autopaho 继续重连等待 operator 修复；首次失败也经 outcomeCh 透传给 Open，errcode Details 含 reasonCode + reasonName | ✅ PR-1 round-1 |
| CONNACK 0x97 (rate-limited) | classTransient：autopaho 内置重连 | ✅ PR-1 |
| Open 调用方传入 unvalidated Config | Open 函数体先调 `cfg.Validate()` 再进入 side-effect 路径；`MQTT-CONFIG-VALIDATE-FIRST-01` archtest 锁定该 form | ✅ PR-1 round-1 |
| 远程明文 broker（tcp/mqtt/ws 非 loopback host） | `Config.validateBrokers` 走 `pkg/secutil.ValidateTLSEndpoint`（单源 TLS endpoint 治理） | ✅ PR-1 round-1 |
| KeepAlive / SessionExpiry uint 截断 | Config.Validate 校验 KeepAlive ≤ 65535s（uint16）、SessionExpiry ∈ [0, uint32 max s] | ✅ PR-1 round-1 |
| MaximumPacketSize 死字段（Config 暴露但 wire 不下发） | `connectPacketBuilder` 把 cfg.MaximumPacketSize 注入 paho.Connect.Properties | ✅ PR-1 round-1 |
| bootstrap-fatal CONNACK 信号被 select race 吞掉 | outcomeCh 单 channel 表达 connected/permErr，删 connectedCh+bootstrapErrCh 双轨设计 | ✅ PR-1 round-1 |
| WaitConnected 在 Close 后悄悄阻塞 ctx | 循环顶部检查 `c.closed` 返回 ErrAdapterMQTTClosed；ctx 取消走 errcode.WrapInfra(ErrAdapterMQTTConnectTimeout, …) | ✅ PR-1 round-1 |
| publish 到 topic 含 `+/#` wildcard（MQTT v5 §3.3.2.1 禁止） | TopicNamespace.PublishOK 顶部 `strings.ContainsAny(topic, "+#")` 拒绝 → ErrAdapterMQTTInvalidSubscribeFilter | ✅ PR-1 round-1 |
| connect URL query/fragment 中的 secret（redactConnectURL userinfo-only 早返回） | `redactConnectURL` 始终 compose `url.URL.Redacted()` 与 `pkg/redaction.RedactString` 两层 | ✅ PR-1 round-1 |
| TLS handshake fail (x509: certificate signed by unknown authority) | bootstrap fail-fast：classifyConnackReason x509 err → classBootstrapFatal | ✅ PR-1 |
| 网络瞬断 / broker 重启 | classTransient → autopaho 内置重连 + ExponentialBackoffWithJitter | ✅ PR-1 |
| MQTT clientId 碰撞（broker 踢旧连接） | ParseEphemeralClientID 嵌入 uuid 保证跨实例唯一；ParseStableClientID 由 caller 控制 instanceID 服务持久会话 | ✅ PR-1 round-1 |
| publish 到 cell namespace 外的 topic | TopicNamespace.PublishOK 在 Publisher 调用前拦截 | ⏳ PR-2 (#1225) |
| subscribe 到 cell namespace 外的 filter | TopicNamespace.SubscribeOK 在 Subscriber 调用前拦截 | ⏳ PR-3 (#1225) |
| QoS 1 PUBACK 超时（broker 慢） | publisher retry backoff (Requeue)；超时后 errcode.KindUnavailable | ⏳ PR-2 |
| Reject 消息堆积在 DLT topic | `$dead/<topic>` 独立 subscriber + metric dlx_total 可观测 | ⏳ PR-4 |
| mTLS 证书过期 | bootstrap fail-fast（x509 handshake fail）；cert rotation 通过 Vault PKI（nightly TLS test） | ⏳ PR-4 nightly |

---

## 4. archtest 新增（AI-robust）

| Archtest ID | 文件 | 评级 | 说明 |
|-------------|------|------|------|
| `MQTT-CLIENT-ID-NAMESPACE-01` | `tools/archtest/mqtt_funnel_test.go` | Hard 上游（包外 compile gate）+ Medium 上游（包内 archtest A2）| sealed-struct field freeze (A1) + construction allowlist (A2, allowedFuncs = {assembleClientID}) + no alias (A3) + A2 scanner red-fixture self-check (round-1) |
| `MQTT-TOPIC-NAMESPACE-01` | `tools/archtest/mqtt_funnel_test.go` | 同上 | 同上，下游 callsite funnel 延迟 PR-2/3 (#1225) |
| `MQTT-CONFIG-VALIDATE-FIRST-01` (round-1) | `tools/archtest/mqtt_config_validate_first_test.go` | Medium 下游（form-lock）| Open 函数体 cfg.Validate 必须在 autopaho.NewConnection 之前调用；Hard 升级（sealed Config + NewConfig 唯一构造）追踪 #1231 |
| `PROBENAME-SEALED-FUNNEL-01` (既有，扩 inventory) | `tools/archtest/probename_sealed_funnel_test.go` | Hard downstream (A2) + Medium upstream (A1) | `mqtt_ready` 加入 probeNameSanctionedPkgs + adapterSanctionedPkgs + goldenProbeNames() |

ClientID Funnel 评级表（round-1 amended）：

| Funnel | 上游构造 | 下游 callsite | PR-1 round-1 状态 |
|--------|---------|--------------|-------------------|
| `ClientID` typed function choice | Hard 包外（sealed struct）+ Medium 包内（A2 + red-fixture self-check）| Hard `Config.ClientID` 类型化 | ✅ {ParseEphemeralClientID, ParseStableClientID} → assembleClientID 单 callsite |
| `TopicNamespace` 类型 | 同上 | — | ✅ 类型/构造 seal |
| `TopicNamespace` publish/subscribe callsite | — | Hard 延迟 PR-2/3 (#1225) | ⏳ |
| `Config` validate-first | — | Medium archtest form-lock | ✅ 升 Hard 追踪 #1231 |

下游 callsite funnel（publish/subscribe topic 参数必须经 PublishOK/SubscribeOK）：gh issue #1225 跟踪，PR-2/3 实施，现在写是 vacuous-pass。

延续 Soft 模式同步登记的升级 backlog（round-1）：

- gh issue #1231 — `Config` sealed 化 + `NewConfig` 唯一构造（C1 F1 Hard 升级路径）
- gh issue #1232 — adapter-level remote endpoint funnel：所有 adapter 的 Config remote URL 字段必须自动接入 `secutil.ValidateTLSEndpoint`（C1 F2 Hard 升级路径）
- gh issue #1233 — Config exported field 必须被 Open 引用（C1 F12 Hard 升级路径）
- gh issue #1234 — CONNACK reason code switch 必须覆盖 spec §3.2.2.2 全集（C5 F9 Hard 升级路径）

> 上述 issue 在 PR-1 round-1 commit 完成后由 `gh issue create` 实际创建（4 个 issue 已存在于 backlog，pri-p2 / pri-p3），避免 silent carryover。

---

## 5. 遗留与不在本 ADR 决议

- MQTT v3.1.1 兼容：OUT（spec §3），无计划。
- 端到端 payload 加密：OUT（spec §3），TLS 覆盖 in-transit。
- EMQX 专属 conformance：留 backlog，Mosquitto 是 CI 基线。
- codegen `transports: [amqp, mqtt]` 多 transport 派生：不在本 PR 系列。
