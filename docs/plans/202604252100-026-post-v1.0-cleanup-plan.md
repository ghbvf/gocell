# 026 · 后 v1.0 清债与加固实施计划（剩余）

> 日期: 2026-04-25 21:00（精简版: 2026-04-29，按最近 PR #321/#323/#325/#326/#316 再同步）
> 基线: `origin/develop @ ad986cad`（最近合入 PR#316；026 主链最近合入 #326 A53 / #325 A63）
> 范围:
> - 主线 2 PR / **~16h（~2 工作日）**：A48 / A64
> - 触发条件 5 PR：Wave 9（A56-A58）+ Wave 10（A59-A60）
> - 决策记录：§六 Deferred Decision Notes（4 项，未关闭）+ §七 触发条件（7 项）+ §八 长期债占位（指向 025 plan）

---

## 轮次更新 — 2026-04-29（latest pull 同步）

> 按 §进度跟踪规则追加；以 `origin/develop @ ad986cad`（PR#316）为准。

- **PR-CFG-M**：✅ #321 已合；outbox fail-open drop counter、OBS-01 typed gate、generator fingerprint、metrics schema 与 generated verify gate 均落地。
- **PR-A53 BOOTSTRAP-LISTENER-SLICE-POLISH**：✅ #326 已合；phase7 listener runtime 已拆到 `runtime/bootstrap/bootstrap_phase7.go`，绑定/启动按 listenerConfigs 迭代。
- **PR-A63 TEST-INFRA-HARDEN**：✅ #325 已合；`examples/ssobff` 抽出 `NewSSOBFFApp`，walkthrough 复用生产 bootstrap 组装路径；root journey harness 与 account lockout 业务能力拆入 backlog。
- **READYZ taxonomy**：✅ #323 已合；`/readyz` 503 public code 统一 `ERR_SERVICE_UNAVAILABLE`，不再占 backlog。
- **剩余主链**：A48（config event consumer metrics）+ A64（lint modernization / websocket / dead var），文件域不冲突但 A64 仍建议最后 solo。

---

## 并行执行计划（剩余主链 2 lane）

> Wave 维度只表达主题；实际执行按"文件域不冲突 + 同域串行"切 lane。各 PR 详细 scope 见下方 Wave 6/7/8/11 章节 + L4 batch2.4 plan。

### 首轮 lane 状态（A46 / A47 / A53 / A55 / A61 / A62 / A63 已合）

| Lane | PR | 状态 | 关闭内容 | 落地 |
|---|---|---|---|---|
| 1 Runtime/Auth | **A46** | ✅ | internal listener service-token guard fail-closed + AuthPlan 校验补强 | #318 |
| 2 Governance/Outbox（L4 batch2.4） | **PR-CFG-M** | ✅ | L4 batch2.4 独立项，不计入 026 主链剩余工时 | #321 |
| 3 HTTP-5xx | **A47** | ✅ | public 5xx errcode taxonomy + 原始 code 仅留 slog | #317 |
| 4 HTTP-Pagination | **A62** | ✅ | pagination/list helper + rbaccheck 真实分页 + list 4xx sampling + parser status-board 测试去漂移 | #319 |
| 5 Adapter | **A55** | ✅ | adapter/testcontainers false-green skip 收口 + OTel collector smoke + RabbitMQ helper 统一 | #320 |
| 6 Accesscore | **A61** | ✅ | initialadmin credpath facade 删除 + ErrCellInvalidConfig + LifecycleHook 慢启动 warn | #322 |
| 8 Bootstrap follow-up | **A53** | ✅ | bootstrap phase7 listener runtime 物理拆分 + shutdown hardening | #326 |
| 9 Test infra | **A63** | ✅ | ssobff walkthrough 复用 `NewSSOBFFApp` + root journey residual 登记 | #325 |

### 后续 lane（首轮合入后启动）

| Lane | 串行队列 | 工时 | 状态 | 说明 |
|---|---|---:|---|---|
| 7 Event/Config | **A48** | ~10h | 🟡 待启 | 独立长项；CFG-M 已合，启动时只需基于 #321 后 metrics provider / tools baseline 重扫 config consumer residual |
| Solo（最后） | **A64** | ~6h | 🟡 待启 | 全仓 lint modernization / dead var / websocket 迁移；主要代码 PR 合入后单独做 |

### 跨 batch 依赖链（必须串行）

- **A46 → A53**：A46 ✅ #318；A53 ✅ #326，依赖链关闭。
- **A47 / A62**：均已 ✅（#317 / #319）；后续只保留 PR#319 新增的 route-level error policy 条件触发项在 backlog。
- **A55**：已 ✅ #320；仅新增 `PR320-FU-CONFIGCORE-CI-NOOP` 作为 Cx1 follow-up，不阻塞主链。
- **A61 → A63**：A61 ✅ #322；A63 ✅ #325，依赖链关闭；剩余 root journey harness / account lockout 已拆入 backlog。
- **A48 / CFG-M**：CFG-M ✅ #321 已先合；A48 后跟即可。
- **A64 最后 solo**：全仓 lint modernization + deprecated websocket 迁移放在主要代码 PR 之后，减少 rebase churn。

### 触发条件 batch（独立流程，不进主链）

| Trigger | PRs | 触发条件 |
|---|---|---|
| **PG accesscore 接入** | A56（5h）| adminprovision 接 PG TxRunner 时 |
| **公网开放 setup** | A57（4h）| setup 端点暴露 0.0.0.0 时 |
| **v1.0 GA** | A58（4h）| 🔴 必做 |
| **第二个 durable command adapter** | A59 → A60 串行（14h）| PG command queue 接入；A60 含 `PR-A12-SWEEPER-WIRE` 搭车 |

---

## 已完成进度（截至 2026-04-29）

主链 26 项中已完成 **18 PR / 19 项**（按原估算工时约 ~115h，剩余主链 ~16h）：

| 完成 PR | 关闭项 | 落地 PR# |
|---|---|---|
| PR-A39 HOUSEKEEPING-SWEEP | A12/A13/A18/V-A10/V-A14/V-A16/V-A17/V-A18/V-A19/L9/PR240-DOC1/PR237-A5/F13/lifecycle-test 包归属/PR246-ADR-SYNC | #283 |
| PR-A40 GOVERNANCE-CI-POLISH | L11/PR220-4/PR239-O1/L7-FMT15/L7-EXAMPLES-STARTUP-SMOKE | #282 |
| PR-A41 CLI-PRINTER-FOLLOWUP | VERIFY-JSON-OUTPUT/CHECK-DIAGNOSTIC-LOCATOR/SARIF-URIBASEID | #270 |
| PR-A42 SETUP-POLISH | PR247-N3/N4/N5/N7 + MULTI-REVIEW-RES-1 SETUP-410-MIGRATION-DOC | #284 |
| PR-A43 LISTENER-TEST-GAPS | PR237-T1/F06 + PR225-N1 + phase0 fail-fast/TLS sanity + T-08 archtest | #285 |
| PR-A44 AUTH-RESIDUAL-POLISH | PR244-F8/F9/F1 + V-A9/V-A15 | #289 |
| PR-A45 PATH-PARAM-VALIDATION | PR239-S1/S3/T1/T2 | #288 |
| PR-A49 OUTBOX-ARCHTEST-EXPAND | PR245-F5/F6 + MULTI-REVIEW-RES-2（落地 `packages.Load + go/types` 范式） | #290 |
| PR-A50+A51 CTX-CANCEL CONSOLIDATE & HTTP 499 | PR238-FU1/FU3/FU6/FU7 + STALECIPHER-OPFIELD + FLAGREPO-CTXCANCEL + CTXCANCEL-HELPER + CTXCANCEL-HTTP-499 + OTEL-SPAN-CTXCANCEL | #271 |
| PR-A52 LISTENER-DX-UNIFY | PR237-DX1/A2/A4 + PR-CFG-G1-FU3 + PR258-RES-4 + LISTENER-DX-A52 anti-regression guard | #306 |
| PR-A54 VAULT-RMQ-DIAGNOSTIC | PR240-A1/A2/OB1/DX1 + LATER-AL-R + PR-A8 Kubernetes auth fixture/wiring coverage（真实 Vault K8s auth e2e residual 仍留 backlog） + V-A12 + A8 CI-DIGEST + A9 CI-LINT-PIN | #305 |
| PR-A47 ERRCODE-5XX-MASK | S-5xx-code-mask + `pkg/errcode.PublicCode/PublicCodeForStatus` taxonomy | #317 |
| PR-A46 INTERNAL-GUARD-REQUIRED | PR239-S2 + internal listener service-token guard fail-closed + AuthPlan invalid wiring rejection | #318 |
| PR-A62 PAGINATION-HELPER + SLICE-RESIDUAL | L8 + S13-follow(list-boundary) + FEAT-1-残余 + role repo pagination snapshots + parser status-board drift cleanup | #319 |
| PR-A55 ADAPTER-T-SKIP-BACKFILL | LATER-T-1 + A10 + testcontainers guard/RequireDocker strict semantics | #320 |
| PR-A61 ACCESSCORE-FACADE-POLISH | A5a-R9/R10/R11 | #322 |
| PR-A53 BOOTSTRAP-LISTENER-SLICE-POLISH | PR237-A6 phase7 listener runtime 物理拆分；A53 明确排除 ListenerSpec public API 重塑 | #326 |
| PR-A63 TEST-INFRA-HARDEN | S23 walkthrough 装配复用；F10 root harness residual 复核并拆入 backlog | #325 |

完成 PR 详细章节已归档：见 git history（commits `283/282/270/284/285/289/288/290/271/306/305/317/318/319/320/322/326/325`）与 `docs/plans/archive/`。

---

## Wave 6 — Auth / Security 加固（已完成）

### PR-A46 INTERNAL-GUARD-REQUIRED（Cx3，~4h）✅ 完成 @ #318

**主题**：bootstrap fail-fast 校验 internal listener guard 必须注册。

**搭车 backlog 子项**：
- `PR239-S2` INTERNAL-GUARD-REQUIRED-01（`FinalizeAuth` 阶段 delegated 路径存在时强制 guard 注册；或 bootstrap layer 默认 InMemoryGuard 类似 S-nonce）

**落地**：#318 选择 fail-closed service-token guard 方案；拒绝 `NonceStoreKindNoop`、nil/typed-nil store/keyring、短 keyring、混合/重复 AuthPlan 等错误配置；corebundle runtime wiring 始终要求 internal addr + guard。

**验收**：
- composition root 漏配 internal guard 时 startup fail-fast，错误信息含修复 hint
- bootstrap phase 单测覆盖 missing guard / multiple guard 场景

---

### PR-A47 ERRCODE-5XX-MASK（Cx2，~3h）✅ 完成 @ #317

**主题**：5xx 响应的 `error.code` 字段统一遮蔽，避免基础设施故障类型枚举泄漏。

**搭车 backlog 子项**：
- `S-5xx-code-mask` 5XX-ERRCODE-RESPONSE-MASK-01（`pkg/httputil/WriteError` 在 `status>=500` 时把 `code` 改为 `ERR_INTERNAL` / 503 改为 `ERR_SERVICE_UNAVAILABLE`，原 code 保留到 slog）
- 顺扫 `ErrKeyProviderTransient` / `ErrVaultAuthFailed` / `ErrCircuitOpen` 503 是否统一 `ERR_SERVICE_UNAVAILABLE`

**落地**：#317 将 public 5xx taxonomy 上提到 `pkg/errcode.PublicCode/PublicCodeForStatus`；500 默认 `ERR_INTERNAL`，503 `ERR_SERVICE_UNAVAILABLE`，504 保留 `ERR_SERVER_TIMEOUT` 公开机器语义，原始内部 code 保留在 slog。

---

## Wave 7 — Outbox / Event / Config 收口（剩 1 PR，~3h）

### PR-A48 CONFIG-EVENT-CONSUMER-METRICS（Cx2，~3h）

**主题**：仅保留 config event consumer metrics residual。user / flag events camelCase sweep 已由 #250/#292 baseline 关闭，不再作为本 PR 主题。

**搭车 backlog 子项**：
- `PR266-METRICS-CONFIG-EVENT` CONFIG-EVENT-CONSUMER-METRICS-01（Cx3）：拆成 `gocell_config_event_process_total{cell,slice,reason}` 与 `gocell_config_event_settlement_total{cell,slice,disposition,result}`；handler 记录 ack/stale/permanent_error process reason，subscriber/adapter 最终 settlement 记录 ack/requeue/reject。owner metadata 由 `AddContractHandler(..., cell.WithSubscriptionSliceID(...))` 写入 `outbox.Subscription`，不再维护 bundle 影子 target 表。**对标**：Watermill subscriber/handler metrics 分层；OpenTelemetry messaging process/settle 分段。

**注意**：启动前需在 #266/#267/#268/#269/#271 后的新 baseline 上重测 backlog 子项 PR238-FU1/FU3/FU4/FU6/FU7/FU8 是否仍然成立。

**验收**：
- 两个 config consumer 成功 / stale / permanent_error 路径均记录 `gocell_config_event_process_total{cell,slice,reason}`
- 各 cell producer 全部走 `outbox.Emit[T]`
- `gocell_config_event_settlement_total{cell, slice, disposition, result}` 在 demo 部署可观测；config consumer 与 middleware test 覆盖 process reason、retry-exhausted reject、commit/nack settlement 边界

---

## Wave 8 — Listener / Adapter 收尾（完成）

### PR-A53 BOOTSTRAP-LISTENER-SLICE-POLISH（Cx3，~4h）✅ 完成 @ #326

**主题**：bootstrap phase7 listener slice 重构。Health checker ctx budget / readyz verbose fail-closed / relay probe forwarding 已由后续 baseline 吸收（#297 / #304 / #271）。

**前置**：A46 ✅ #318（service-token guard fail-closed baseline 已合）。

**搭车 backlog 子项**：
- `PR237-A6` BOOTSTRAP-PHASE7-SLICE-POLISH-01（phase7 slice 物理拆分，新建 `bootstrap_phase7.go` 承载 listener 装配代码，避免与 cell wiring / lifecycle 阶段混淆）

**落地**：#326 新增 `runtime/bootstrap/bootstrap_phase7.go` 承载 phase7 listener runtime；`phase7BindListeners` 按 `listenerConfigs` 收集并排序 `ListenerRef`，同步预绑定所有 listener，失败时释放 bootstrap-owned sockets；`phase7ServeAll` 统一启动 N 个 server。既有 dual listener bind / rollback / shutdown 行为由 `dual_listener_test.go` 继续锁定。

**保留非目标**：ListenerSpec public API 重塑未做，已登记到 backlog `LISTENER-API-SPEC-01`。

---

### PR-A55 ADAPTER-T-SKIP-BACKFILL（Cx3，~3h）✅ 完成 @ #320

**主题**：adapter `t.Skip` testcontainer 配置补齐。Redis/RabbitMQ/OTel 主干 skip 已不在计划范围内，剩余集中在 Postgres env-gated integration 与 Vault container-unavailable 条件 skip。

**搭车 backlog 子项**：
- `LATER-T-1` ADAPTER-TSKIP-BACKFILL-01（剩余：`adapters/postgres/pool_test.go` 的 `PG_INTEGRATION` gate + `adapters/vault/integration_test.go` 的 Vault container unavailable 条件 skip；启动前再跑一次 `git grep -n "t.Skip" origin/develop -- adapters/...`）
- `A10` OBS-LGTM-INTEGRATION-01（`//go:build integration` 夜间 OTel collector 真实 OTLP 协议兼容性测试）

**落地**：#320 收敛 Postgres/Vault/Corebundle/RabbitMQ/OTel integration false-green skip，新增 `tests/testutil.RequireDocker` strict 语义、RabbitMQ container helper、OTel collector PR smoke 与 nightly/manual workflow。仅保留 `PR320-FU-CONFIGCORE-CI-NOOP` 为独立 Cx1 follow-up。

---

## Wave 11 — 长尾清扫 + 测试基础设施（剩 1 PR，~6h）

不阻塞 v1.0、可在主链 Wave 6-8 任意空档插入。

### PR-A61 ACCESSCORE-FACADE-POLISH（Cx2，~4h）✅ 完成 @ #322

**主题**：accesscore initialadmin facade 修边 — credpath 命名链 + errcode 一致性 + LifecycleHook 慢启动 warn。

**搭车 backlog 子项**：
- `A5a-R9` INITIALADMIN-CREDPATH-NAMING-CHAIN-01（删 `accesscore.ResolveBootstrapCredentialPath` 外层 wrapper，调用方直接用 `initialadmin.ResolveCredentialPath`）
- `A5a-R10` INITIALADMIN-ERRCODE-CONSISTENCY-01（`bootstrap.go:79-90` 三处 `fmt.Errorf` → `errcode.New(errcode.ErrCellInvalidConfig, ...)`）
- `A5a-R11` LIFECYCLECONTRIBUTOR-BLOCKING-SEMANTICS-WARN-01（phase3b runHook 在 OnStart 耗时 ≥ defaultStartTimeout × 0.8 时 slog.Warn）

**落地**：#322 删除 `accesscore.ResolveBootstrapCredentialPath` thin facade，调用方直用 `initialadmin.ResolveCredentialPath`；`newBootstrapper` 配置校验统一 `ErrCellInvalidConfig`；`runtime/bootstrap` 对接近 timeout 的 OnStart hook 追加 warn 覆盖。

---

### PR-A62 PAGINATION-HELPER + SLICE-RESIDUAL（Cx2，~5h）✅ 完成 @ #319

**主题**：分页 helper 抽取 + slice 残余的真实分页 + list-boundary 4xx 采样 + status-board 测试。

**搭车 backlog 子项**：
- `L8` PAGINATION-HELPER-EXTRACT-01（`pkg/httputil/pagination.go` 抽 `ParsePageRequest` + `slog.Warn` + `WriteDomainError` 三件套）
- `S13-follow` 4XX-LOG-SAMPLING-01（高频端点 4xx 日志 1% 采样 / rate-limiter 触发后限速）
- `FEAT-1-残余` RBACCHECK-PAGINATION-REAL-01（`cells/accesscore/slices/rbaccheck/handler.go:79` `handleListRoles` 迁 `query.PageResult[RoleResponse]` + 真实游标分页）
- `PR220-e3` STATUS-BOARD-J-ORDERCREATE-01（J-ordercreate journey status board 校验）

**落地**：#319 抽共享 pagination/list response helper，拒绝非正 page limit；把 list 4xx 采样移到 request-boundary helper；rbaccheck role repo 加分页快照测试；parser integration test 不再 pin mutable status-board 值。更大的 route-level error/log policy 已登记为 `ROUTE-ERROR-POLICY-01`。
`PR220-e2` 已拆分：generated boundary 文件存在，剩余 drift hard gate 留在 PR-CFG-I.X2 / PR-CI-1；`F3` audit e2e 未由 #319 落地，仍保留在 backlog。

---

### PR-A63 TEST-INFRA-HARDEN（Cx3，~3h）✅ 完成 @ #325

**主题**：ssobff walkthrough 装配复用 + journey harness 残余复核。Journey active auto-check governance 已由 #295 落地，旧 `tests/integration/journey_test.go` 28 条 `t.Skip` 已不在当前 baseline。

**前置**：A61 ✅ #322（accesscore initialadmin facade polish 已合，ssobff 重构基线稳定）。

**搭车 backlog 子项**：
- `PR220-3` JOURNEY-VERIFY-FAIL-CLOSED-01 — ✅ #295 已落地 strict-only active journey auto-check；#325 复核确认 root journey 缺真实 harness 时 fail-closed。
- `S23` AUTH-WALKTHROUGH-COMPOSE-01 — ✅ #325 已抽 `examples/ssobff/app.go::NewSSOBFFApp`，`main.go` 与 walkthrough test 共用同一 bootstrap + Start/Stop 装配。
- `F10` TEST-JOURNEY-ASSEMBLY-HARNESS-01 — 🔄 旧目标已重写并拆入 backlog：`TEST-JOURNEY-ROOT-HARNESS-01` / `ACCESSCORE-ACCOUNT-LOCKOUT-AUTO-LOCK-01`。

**落地**：#325 新增 `SSOBFFApp` / `SSOBFFAppOption` 工厂，walkthrough 通过 injected listener 复用生产 composition root；同时登记 root journey harness 和 account lockout 业务语义残余，避免把业务实现混入测试基础设施 PR。

---

### PR-A64 LINT-MODERN-DEAD-VAR（Cx2，~6h）

**主题**：一次性 lint baseline 清理 — modernization + dead variable。

**搭车 backlog 子项**：
- `X9` LINT-MODERN-01（全仓 modernization baseline：rangeint / stringsseq / forvar / inline / testingcontext / any / nhooyr.io→coder/websocket）
- `DEAD-VARIABLE-01`（unused variables / parameters 全仓清扫）
- `V-A6` WEBSOCKET-DEPRECATED-01（`go.mod` `nhooyr.io/websocket` → `github.com/coder/websocket`，与 X9 合 PR）
- `V-A7` DEPRECATED-ADAPTER-METHODS-01（`Connection.Statter()` / `Subscriber.InitializeSubscription()` 移除 Deprecated 标注或先迁测试再删）
- `V-A8` CMD-THICK-ENTRY-REDUCE-01（`cmd/corebundle/main.go` 继续缩减 + `internalGuard` 提升为 `corebundle.InternalGuard` 或抽接口）
- `V-A5` VALIDATION-HELPER-EXTRACT-01（`pkg/validation.RequireNotBlank` 抽取 helper 消除 6+ 处 `if token == ""`）

**验收**：
- `golangci-lint run --enable-all` 0 issues
- `go.mod` `nhooyr.io/websocket` 完全替换
- `cmd/corebundle/main.go` < 100 行

---

## 触发条件 PR（不进主链）

### Wave 9 — 多 pod / 发布触发项（3 PR，~13h，🟠 触发）

| PR | 工时 | 触发条件 | 内容 |
|---|---:|---|---|
| **PR-A56** SETUP-MULTIPOD-CLOSURE | ~5h | adminprovision 接 PG TxRunner 时 | A26-R1 ADMINPROVISION-DIST-LOCK + A26-R4 SETUP-ORPHAN-E2E |
| **PR-A57** SETUP-PUBLIC-EXPOSURE-HARDEN | ~4h | setup 端点暴露 0.0.0.0 时 | A26-R2 SETUP-ADMIN-RATE-LIMIT（per-IP rate-limit + 失败 lockout 或一次性 bootstrap token）|
| **PR-A58** RELEASE-DOCS-MULTIPOD | ~4h | 🔴 v1.0 GA 前必做 | PR237-PM5 dual-listener 部署指南 + PR237-PM7 examples 注释 |

### Wave 10 — Command 生产治理（2 PR，~14h，🟠 触发）

| PR | 工时 | 触发条件 | 内容 |
|---|---:|---|---|
| **PR-A59** COMMAND-QUEUE-REGISTRAR-FAILFAST | ~6h | 第二个 durable command adapter 前 | PR252-F1 QueueRegistrar bootstrap fail-fast + bootstrap phase 单测 |
| **PR-A60** COMMAND-SWEEPER-PRODUCTION-GOVERNANCE | ~8h | 多副本 command consumer / PG command queue 前 | PR252-F2 leader/分片 ownership + 失败阈值 → /readyz 降级 + 多实例 ownership 测试；含 `PR-A12-SWEEPER-WIRE` 搭车（`InMemQueue.ListAllPending` + Sweeper） |

---

## §六 · Deferred Decision Notes（4 项，未关闭）

> 这些项不由本 plan 关闭，仍以 `docs/backlog.md` 的触发条件为准；如触发条件成立，需重新评估。

| 条目 | 名称 | 决策依据 |
|---|---|---|
| **A5a-R3** | ACCESSCORE-INITIALADMIN-THIN-WRAPPER-01 | PM + architect review 一致倾向保持现状；thin wrapper 增 facade 维护成本，且 `LifecycleOption` 切片语义天然暴露底层结构。**复查触发**：任意第二个 cell 公开"subpackage + functional options"模式时。 |
| **A5a-R12** | INITIALADMIN-TESTING-ENTROPY-UNEXPORT-01 | accesscore cell-level test 需注入确定性熵源；`withPasswordSource` unexport 后外包不可见。**复查触发**：v1.0 前 security audit 标红。 |
| **PR250-F3** | EVENT-WIRE-BYTE-PINNING-TEST-EVAL-01 | schema validation + `additionalProperties: false` 已覆盖；golden-string 对字段顺序 / `omitempty` 边界脆弱。**复查触发**：出现真实 camelCase regression 漏网。 |
| **V-A13** | GENTPL-LIFECYCLE-PATTERN-01 | 2026-04-28 source audit 确认 `kernel/assembly/gentpl/main.go.tpl` 仍直连 `assembly.New/Start/Stop`，没有走 `runtime/bootstrap.Run`；本 plan 不把它归档。**复查触发**：决定 gentpl 是最小骨架还是开箱即用模板时。 |

---

## §七 · 触发条件项（7 项，不排期）

> 不进 Wave 计划，触发条件成立时单独评估并新建 backlog → plan 流程。

| 条目 | 名称 | 触发条件 |
|---|---|---|
| **T1** | AUTH-PROVIDER-EXPORT-01 | 第二个 auth provider cell 出现（`runtime/bootstrap.authProvider` 接口需移出 bootstrap 包） |
| **T2** | AUTH-ISSUE-OPTIONS-01 | `JWTIssuer.Issue()` 添加第 5 个参数（重构为 `IssueOptions` struct） |
| **T3** | DEVICE-ENQUEUE-RBAC | 多租户 operator（HandleEnqueue 加设备维度鉴权） |
| **T4** | CB-RESILIENCE-PACKAGE-01 | 第二个非 HTTP 的 CB 消费方（`Allower` / `CircuitBreakerRetryAfter` 迁 `runtime/resilience/circuitbreaker/`） |
| **T5** | AUTH-SIGNER-01 | golang-jwt v6 发布（`SigningKeyProvider` 返回 `crypto.Signer` 替代 `*rsa.PrivateKey`） |
| **T6** | CONTRACT-EVENT-PAYLOAD-CODEGEN-01 | event subscriber 扩散到 ≥ 5 cell（当前 2 cell：accesscore/configreceive + configcore/configsubscribe） |
| **S14a** | CONFIG-VALUE-KMS-AWS-PROVIDER-01 | 明确部署目标云平台 + 通过 KMS 安全评审 |

---

## §八 · 长期债占位（不在本 plan 详写）

以下条目仍归 025 plan §Wave 4，本 plan **不重写、不重新切 PR**；触发条件成立或排期到达时回 025 plan 推进：

- **025 plan PR-A21** AUTH-JWT-LIB-EVAL（~0.5-1d）
- **025 plan PR-A22** Cell ISP 拆分（~1.5d）
- **025 plan PR-A23** ER-ARCH-01 Subscriber Setup/Run 双阶段（~2d）
- **025 plan PR-A24** DURABLE-TYPE-01 + G-6 + kernel/replay + rollback（~2d）
- **025 plan PR-A33** REFRESH-OPAQUE-POLISH（X12 + X13 + X14，~8h；PR-A29 已合可解锁）

---

## 进度跟踪规则

每批 PR 落地后在本 plan 头部追加"轮次更新"段落；PR 编号 + PR# + 落地日期 + 主要 backlog 子项关闭情况。完成后在已完成进度表加行，Wave 全部完成时归档到 `docs/plans/archive/`。

PR 拆分允许向下细化（一个 PR 拆成 a/b 子 PR），但**不允许新增 PR-A65+ 编号**——超出 26 个 PR 的项必须证明属于本 plan 漏网，否则归 027 plan 或 backlog deferred decision。
