# 026 · 后 v1.0 清债与加固实施计划

> 日期: 2026-04-25 21:00（最近更新: 2026-04-26）
> 基线: `develop @ c7ea8db7`（PR#262 / #263 / #264 / #265 / #266 / #267 / #268 / #269 / #270 / #272 已落地；当前 0 个 OPEN PR）
> 来源:
> - `docs/backlog.md`（2026-04-25 复盘后约 80 条开放条目，按文件亲缘 + 主题去重分组）
> - `docs/plans/202604232330-025-architecture-pr-implementation-plan.md`（架构 plan v1.0 路径，**本 plan 不重复其内容**，只续编号）
> - 2026-04-25 多 reviewer 复盘（PR#258 / PR#257 round-2 + 架构席位 + 合并复核）
> 目标: 把 backlog 中 26 条主线项打包成 26 个内聚 PR（PR-A39 ~ PR-A64），明确 Wave 顺序、搭车关系、触发条件；同时把 5 条 Won't-do 与 7 条触发条件项独立成章，避免持续滚动到下一份计划
> **工期**: 净编码 **~131h** → 已完成 PR-A41（~5h）→ 剩余 **~126h（~15.7 工作日）**；双人并行剩余 **~10 工作日**
>
> **范围说明**:
> - 025 plan 中 PR-A1..A38 已完工（或长期占位 PR-A21/A22/A23/A24/A33）一律不再列出；PR-A30 AUTH-TEST-COVERAGE 因为 PR-A29 已合可启动，025 已留位、本 plan 不重复
> - 本 plan 全部使用 PR-A39 起始的新编号；backlog 旧标识（如 PR244-Fxx / V-A* / L*）只作为子项追溯链
> - 最后两章（Won't-do / 触发条件）是显式不排期的项，禁止滚动到下一份 plan，需要做时再单独评估

---

## 轮次更新 — 2026-04-26

> 按本 plan §进度跟踪规则追加。

### 本轮落地 PR

| PR | 类型 | 主题 | 对 026 plan 的影响 |
|---|---|---|---|
| **#262** | 计划内 follow-up | PR-A14b round-3 reviewer findings (#1-#11) | bootstrap/router 收尾，不影响 plan 项 |
| **#263** | PR-A30（025 占位） | AUTH-TEST-COVERAGE — table-driven jwt aud + login/refresh drift integ + middleware e2e | 025 plan 占位项关闭 |
| **#264** | 计划外 fix | accesscore first admin setup | 不影响 plan 项 |
| **#265** | 计划外 fix | distlock TestManager_HeapOrder flaky race | 不影响 plan 项 |
| **#266 PR-CFG-B** | 计划外重构 | CELL-GRAPH-INTEGRITY — LAYER-09 + ADV-05 + entry-upserted metadata-only + flag.changed retire | **PR-A48** 涉及的 `event.flag.changed.v1` payload schema 已被 #266 大改；**PR-A50** configcore 全 slices/repos 已被 #266 重写，需 rebase 后才能压抽 helper |
| **#267 PR-CFG-C** | 计划外重构 | contract-as-auth-truth — `cells/configcore/cell_routes.go` + `runtime/auth/{guard,middleware,authtest}` + examples | **PR-A39** examples/ssobff/walkthrough_test.go 已被 #267 改写；**PR-A45** 落地时 contract-4xx 完整性需对齐 #267 已新增的 `runtime/auth/authtest`；**PR-A63** walkthrough 复用模式已部分实现 |
| **#268 PR-CFG-A** | 计划外重构 | LIFECYCLE-OBSERVABILITY-AUTO-AGGREGATE — outbox/bootstrap/cells cell.go/adapters | **PR-A39** lifecycle_integration_test 包归属已与 #268 测试改动叠加；**PR-A43** listener bind 测试需在 #268 之上扩展；**PR-A53** Checker 签名升级要建立在 #268 ManagedResource 改造上；**PR-A55** adapters/postgres pool_resource 已被 #268 改过；**PR-A61** accesscore cell.go 与 LifecycleHook phase3b 路径已被 #268 移动 |
| **#269 PR262-AUTH-POLICY-PLAN-01** | 计划外重构 | typed AuthPlan/ListenerAuth/GroupAuth replaces cell.Policy | **PR-A44** runtime/auth/nonce.go 已被 #269 触动；**PR-A46** FinalizeAuth 路径在 #269 之上；**PR-A52** Delegated → Internal rename ⚠️ 仍需做（#269 引入 AuthPlan 但保留了 `cell.AuthRouteMeta.Delegated` / `runtime/auth/Route.Delegated` 字段），可继续按计划执行；**PR-A53** bootstrap phase 文件已被 #269 删除 policy_*.go 多个文件，phase7 拆分有重新约束 |
| **#270 PR-A41** | ✅ **计划内完成** | CLI printer follow-up — SARIF uriBaseId + verify --format + contract-health locator | **PR-A41 关闭** |
| **#272** | 计划外 docs | drop V-A11b row to unblock Governance Strict CI | **PR-A40** 子项 V-A11b 已废，本 PR 落地时跳过该子项 |

### Plan 状态变化

- **完成**：PR-A41 ✅（#270，2026-04-26）
- **剩余主链**：20 PR（PR-A39 / A40 / A42 / A43 / A44 / A45 / A46 / A47 / A48 / A49 / A50 / A51 / A52 / A53 / A54 / A55 / A61 / A62 / A63 / A64），净编码 ~126h
- **触发条件 Wave**：Wave 9（A56-A58）+ Wave 10（A59-A60）维持原状

### 已观测到的 baseline 漂移（rebase 必读）

1. **configcore 重写（#266）**：PR-A50 ctx-cancel 收口要面对的 repo 文件已被 #266 大量改动；启动前需 ✅ 在新 baseline 上重测 backlog 子项 PR238-FU1/FU3/FU4/FU6/FU7/FU8 是否仍然成立
2. **AuthPlan typed model（#269）**：PR-A52 LISTENER-DX-UNIFY 中 `auth.Route.Delegated` / `kernel/cell.AuthRouteMeta.Delegated` 字段仍在（PR#269 未触此 rename），16 处调用站点估算需重新核对（grep 后实际命中点可能减少）
3. **examples lifecycle（#267 + #268）**：PR-A63 子项 S23 AUTH-WALKTHROUGH-COMPOSE-01 部分已由 #267 落地，启动前要重新评估 28 条 t.Skip 列表是否仍全数存在
4. **bootstrap policy_*.go 删除（#269）**：PR-A46 INTERNAL-GUARD-REQUIRED 不再需要"保留 policy_*.go 兼容路径"考量，可直接基于 AuthPlan 接口实施
5. **kernel/governance（#266）**：PR-A40 GOVERNANCE-CI-POLISH 中 V-A11b 子项已被 #272 主动放弃，本 PR 范围缩小

---

## 设计原则

1. **延续 025 plan 的文件亲缘 + 语义内聚**：同 backlog 主题 + 同目录的多条小修打包成一个 PR
2. **新编号单调**：PR-A39 起严格递增，避免与 025 的 PR-A21/A22/A23/A24/A33 长期占位冲突
3. **触发条件项单独 Wave**：Wave 9（多 pod / 发布前）+ Wave 10（command 生产治理）不进开发主线，触发后再启动
4. **Won't-do 显式登记**：5 条「评估后不做」必须写入 §六，附决策依据；下一份 plan 不再回看
5. **共享子项合并**：跨 backlog 项的同一文件改动（如 PR250-F2 + PR247-N3 都需要 `user_events.go` 抽 typed struct）合并到一个 PR，避免冲突

---

## PR 切分总览

| Wave | 目标 | PR 数 | 已完成 | 净编码（剩余） | 双人并行 |
|---|---|---:|---:|---:|---:|
| **Wave 5** — 卫生 + CI + 短链路 | housekeeping + CI 门禁 + CLI / setup 收尾 + listener 测试补齐 | 5 | **1（A41 ✅）** | ~12h | ~1d |
| **Wave 6** — Auth / Security 加固 | nonce 容量 + path-param 校验 + internal guard + 5xx code 遮蔽 | 4 | 0 | ~17h | ~1.5d |
| **Wave 7** — Outbox / Event / Config 收口 | event camelCase 残余 + outbox archtest + ctx-cancel + HTTP 499 | 4 | 0 | ~24h | ~2d |
| **Wave 8** — Listener / Adapter 收尾 | listener DX + Vault/RMQ 诊断 + adapter t.Skip 回填 | 4 | 0 | ~24h | ~2d |
| **Wave 9** — 多 pod / 发布触发项（🟠 触发） | setup multipod + 公网加固 + 发布文档 | 3 | 0 | ~13h | 触发后 ~1d |
| **Wave 10** — Command 生产治理（🟠 触发） | queue registrar fail-fast + sweeper ownership / readiness | 2 | 0 | ~14h | 触发后 ~1d |
| **Wave 11** — 长尾清扫 + 测试基础设施 | accesscore facade + 分页 helper + journey assembly harness + lint baseline | 4 | 0 | ~22h | ~2d |
| **合计** | | **26** | **1** | **~126h（~15.7d）** | **~10d** |

> Wave 9/10 不计入主链，触发条件成立时再纳入 Sprint。
> 长期占位（PR-A21 / A22 / A23 / A24 / A33）仍归 025 plan §Wave 4，本 plan 不重写。

---

## Wave 5 — 卫生 + CI + 短链路（5 PR，~17h）

低风险一次性卫生扫，先行清掉漂移文档 / 风格不一致 / 治理缺口，给后续 Wave 腾出干净 baseline。

### PR-A39 HOUSEKEEPING-SWEEP（Cx2，~5h）

**主题**：一次性卫生扫，删 deprecated / 补 godoc / 统一日志风格 / Windows 路径 / 文档同步。

**搭车 backlog 子项**：
- `A12 deprecated 删除`（kernel 层 deprecated 字段清零）
- `A13 kernel godoc 补齐`（kernel/* 公共导出符号 godoc 完整性）
- `A18 slog 风格归一`（adapter / runtime 层结构化字段顺序与 key 命名）
- `V-A10` GOVERNANCE-WINDOWS-PATH-01（`examples/ssobff/main.go:150,168` 用 `filepath.Join` 替换字面 `/`）
- `V-A14` SHUTDOWN-WAIT-READY-CHANNEL-01（消除 50ms 硬等 flake）
- `V-A16` SHUTDOWN-INTERRUPT-WINDOWS-TEST-01（补 `//go:build windows` 测试）
- `V-A17` SHUTDOWN-BUILD-TAG-MUTUAL-EXCLUSION-01（编译期守卫 `signalsToWatch` 唯一性）
- `V-A18` DOCS-MACOS-TROUBLESHOOTING-CLARIFY-01（`docs/operations/first-run-setup.md:244`）
- `V-A19` DEMO-KEY-WARN-COMMENT-01（`demo_keys.go` + `examples/ssobff/main.go` 加 `// DO NOT COPY TO PRODUCTION`，与 PR-A44 V-A9 同 PR 时合并）
- `L9` EXAMPLES-CONTEXT-NOOP-01（删 examples 自定义 noop tx runner，改用 `persistence.NoopTxRunner`）
- `PR240-DOC1`（PR#240 文档残余）
- `PR237-A5` plan 同步（PR#237 plan 描述与实际实现对齐）
- `PR237-F13` 端口 rename（命名收尾）
- `BOOTSTRAP-LIFECYCLE-INTEG-TEST-PACKAGE`（`runtime/bootstrap/lifecycle_integration_test.go` 包归属修复）
- `PR246-ADR-SYNC`（PR#246 ADR §9 与 cell-development-guide event-spec 示例同步）

**验收**：
- `golangci-lint run ./...` 0 issues
- `go test -tags=integration ./...` 跑通（含 `runtime/bootstrap/`）
- `gocell validate --strict` 0 violations
- 所有 deprecated 字段 grep 后确认无残留（白名单除外）

---

### PR-A40 GOVERNANCE-CI-POLISH（Cx2，~4h）

**主题**：CI 治理门禁完整性 — main 分支扫描 / push 全量 lint / `--strict` flag / examples 字符串扫描。

**搭车 backlog 子项**：
- `L11` GOVERNANCE-CI-MAINBRANCH-01（`.github/workflows/governance.yml` 扩展 `branches: [develop, main, 'release/**']`）
- `PR220-4` CI-LINT-EVENT-SEMANTIC-SPLIT-01（`pull_request` 用 diff、`push` 全量 lint）
- `PR239-O1` `--strict` flag（`gocell validate --strict` 显式 flag 化）
- `V-A11b` EXAMPLES-HARDCODE-STRING-SCAN-01（kernel/governance 加 string-scan rule 防 kebab 命名复活）
- `L7` FMT15-NEXTCURSOR-ENFORCE-01（响应含 `hasMore` 必含 `nextCursor`）
- `L7` EXAMPLES-STARTUP-SMOKE-01（CI examples-smoke job 跑 `examples/ssobff` 5s ready）

**验收**：
- governance workflow `branches` 列表覆盖 main / release/\*
- push 到 develop 触发的 lint job 跑全量（不带 `--new-from-merge-base`）
- `gocell validate --strict` 与 default 行为差异有 e2e 测试
- examples-smoke job 在 CI 5s 内进入 ready 状态退出

---

### PR-A41 CLI-PRINTER-FOLLOWUP（Cx2，~5h）— ✅ 完成（PR#270，2026-04-26）

**主题**：PR-A10 OUTPUT-JSON-SARIF 落地后的 CLI 打印细节收尾。

**搭车 backlog 子项**（已全部关闭）：
- `VERIFY-JSON-OUTPUT-01` ✅（`gocell verify --format` 落地）
- `CHECK-DIAGNOSTIC-LOCATOR-01` ✅（contract-health locator 字段统一）
- `SARIF-URIBASEID-01` ✅（SARIF `originalUriBaseIds` 路径 anchor 校验）

**验收**（已通过）：
- `gocell verify --output json` schema 与 SARIF spec 1.0 一致
- `gocell check --output sarif` 加载到 GitHub Code Scanning 无 schema warn
- `cmd/gocell/*_test.go` golden file 覆盖 JSON / SARIF 两路输出

---

### PR-A42 SETUP-POLISH（Cx2，~3h）

**主题**：PR-A26 AUTH-SETUP-ENDPOINT 落地后的 reviewer round-1 N 类残余 + 410 文档收口。

**搭车 backlog 子项**：
- `PR247-N3` CELL-PATTERNS-EVENT-DTO-01（user.created.v1 typed struct，本 PR 占坑、实际抽取下放到 PR-A48 与 PR250-F2 一起做；本 PR 仅修 `setup/service.go` 调用点）
- `PR247-N4`（`ErrSetupAlreadyInitialized` message 路径泄漏修复，next-action 移到 details）
- `PR247-N5`（setup 端点 contract.yaml `responses[410]` 声明完整性）
- `PR247-N7`（setup metric label 与 PR-A36 metrics realign 对齐）
- `MULTI-REVIEW-RES-1` SETUP-410-MIGRATION-DOC-CONSOLIDATE-01（`docs/operations/first-run-setup.md` 或新 `docs/guides/admin-bootstrap-paths.md` 写两路径选型矩阵 + 410 → 200 客户端迁移示例）

**验收**：
- 410 响应 body 不含 `next_action: "POST /api/v1/access/sessions"` 路径字面量
- `docs/guides/admin-bootstrap-paths.md` 包含选型表 + 410 vs 409 客户端兼容示例
- setup contract test 覆盖 410 响应 schema

---

### PR-A43 LISTENER-TEST-GAPS（Cx2，~4h）

**主题**：PR-A14a / A14b dual-listener 落地后的测试 Cx1 gap 一次性补齐。

**搭车 backlog 子项**：
- `PR237-T1` LISTENER-TEST-GAPS-01（10 条 Cx1 测试 gap 列表，含 listener bind / route registration / metrics label 顺序）
- `PR237-F06`（PR#237 round-1 reviewer F6 残余测试）
- `PR225-N1`（pkg/validation 测试覆盖率补齐）

**验收**：
- `cells/.../**` listener 相关 _test.go 覆盖率 ≥ 80%
- `runtime/bootstrap/` 三 listener bind 失败路径有显式 fail-fast 测试
- PR237-T1 列表 10 条 100% 关闭

---

## Wave 6 — Auth / Security 加固（4 PR，~17h）

PR-A29 AUTH-REFRESH-MAIN ✅ + PR-A25 AUTH-PROD-HARDENING ✅ 之后的安全清债，覆盖 nonce 容量 / path-param 校验 / internal guard / 5xx errcode 遮蔽。

### PR-A44 AUTH-NONCE-CAPACITY-POLISH（Cx2，~5h）

**主题**：nonce store 容量治理 + replay errcode 颗粒度 + demo key 拒绝路径。

**搭车 backlog 子项**：
- `PR244-F8` NONCE-CAPACITY-CONTROL-01（`InMemoryNonceStore` 加 `WithMaxEntries` option + `gocell_auth_nonce_store_size` gauge + LRU evict）
- `PR244-F9` AUTH-REPLAY-ERRCODE-GRANULARITY-01（新增 `ErrAuthReplayDetected` 子码，跨 audit log / SIEM 同步评估）
- `V-A9` DEMO-KEY-REJECT-LOADKEYSET-01（`runtime/auth/keys.go:328 LoadKeySetFromEnv()` 加 `rejectDemoKey` + 模板黑名单）
- `V-A15` INITIALADMIN-OPTION-FAILFAST-UNSUPPORTED-01（`accesscore.WithInitialAdminBootstrap()` 在 `!unix && !windows` 平台 cell.Init 时 platform-check 返回 errcode）

**验收**：
- nonce store 注入容量 100k 时撑满后 LRU evict + gauge 单调
- replay 攻击场景 HTTP 响应可区分 `ERR_AUTH_REPLAY_DETECTED` vs `ERR_AUTH_UNAUTHORIZED`
- demo key 模板字面量在 `LoadKeySetFromEnv` 路径 startup fail-fast
- audit / SIEM 文档同步 errcode 表

---

### PR-A45 PATH-PARAM-VALIDATION（Cx2，~5h）

**主题**：UUID path-param 入口校验 + 4xx 完整性。

**搭车 backlog 子项**：
- `PR239-S1` PATH-PARAM-UUID-RUNTIME-VALIDATION-01（identitymanage / sessionlogout / rbaccheck 等 handler 入口加 `uuid.Parse(id)`，非法值 400）
- `PR239-S3` CONTRACT-4XX-COMPLETENESS-01（`http.auth.login.v1` 等 Public 端点补 `responses[400]`，扫 handler 实际返回的 4xx 全部声明）
- `PR239-T1` scaffold smoke（`gocell scaffold` 生成的 handler 包含 path-param parse 模板）
- `PR239-T2` placeholder whitebox（contract test placeholder 字符串校验）

**验收**：
- 任意 UUID path-param handler 接受非法字符串返回 400 + `ERR_VALIDATION_INVALID_UUID`
- contract test 全 Public 端点 4xx 响应声明完备性
- `gocell scaffold` 新生成 slice 默认含 uuid parse boilerplate

---

### PR-A46 INTERNAL-GUARD-REQUIRED（Cx3，~4h）

**主题**：bootstrap fail-fast 校验 internal listener guard 必须注册。

**搭车 backlog 子项**：
- `PR239-S2` INTERNAL-GUARD-REQUIRED-01（`FinalizeAuth` 阶段 delegated 路径存在时强制 guard 注册；或 bootstrap layer 默认 InMemoryGuard 类似 S-nonce）
- `PR244-F1` MULTIPOD-INMEMORY-NONCE-WARN-01 (a) 选项（`internalGuardFromEnv` 装配后 kind==InMemory 追加 multipod warn slog）

**前置**：需 architect 决策 — `Topology.SinglePodReplayProtection` 显式 opt-in vs 默认 InMemory + warn。

**验收**：
- composition root 漏配 internal guard 时 startup fail-fast，错误信息含修复 hint
- 多 pod warn 路径有测试覆盖（kind == InMemory + topology 多 pod）
- bootstrap phase 单测覆盖 missing guard / multiple guard 场景

---

### PR-A47 ERRCODE-5XX-MASK（Cx2，~3h）

**主题**：5xx 响应的 `error.code` 字段统一遮蔽，避免基础设施故障类型枚举泄漏。

**搭车 backlog 子项**：
- `S-5xx-code-mask` 5XX-ERRCODE-RESPONSE-MASK-01（`pkg/httputil/WriteError` 在 `status>=500` 时把 `code` 改为 `ERR_INTERNAL` / 503 改为 `ERR_SERVICE_UNAVAILABLE`，原 code 保留到 slog）
- 顺扫 `ErrKeyProviderTransient` / `ErrVaultAuthFailed` / `ErrCircuitOpen` 503 是否统一 `ERR_SERVICE_UNAVAILABLE`

**验收**：
- 任意 500 响应 body 的 `code` 字段为 `ERR_INTERNAL`，slog 仍记录原 code
- `pkg/httputil/response_test.go` 翻新 5xx + code 断言用例
- 503 响应统一为 `ERR_SERVICE_UNAVAILABLE`

---

## Wave 7 — Outbox / Event / Config 收口（4 PR，~24h）

PR-A6 typed event payload 落地 + PR-A5c outbox emitter 收口之后的 trailing sweep + ctx-cancel 一次性彻底分类。

### PR-A48 EVENT-CAMELCASE-TRAILING-SWEEP（Cx2，~7h）

**主题**：user / flag events camelCase trailing sweep（PR-A6 留的最后一批），合并 PR247-N3 typed DTO struct。

**搭车 backlog 子项**：
- `PR250-F2` EVENT-USER-FLAG-CAMELCASE-FOLLOWUP-01（`event.user.created.v1` / `event.user.locked.v1` / `event.flag.changed.v1` payload schema + DTO + producer 调用点全部迁 camelCase）
- `PR247-N3` CELL-PATTERNS-EVENT-DTO-01（共享 `cells/accesscore/internal/dto/UserCreatedEvent` typed struct，setup + identitymanage 共用）

**验收**：
- 三类事件的 payload schema `additionalProperties: false` + 字段全 camelCase
- auditappend 删除 `UserIDSnake` 降级 fallback
- contract test 覆盖三类事件 round-trip
- 各 cell producer 全部走 `outbox.Emit[T]`

---

### PR-A49 OUTBOX-ARCHTEST-EXPAND（Cx3，~6h）

**主题**：outbox archtest 静态守卫升级 + fail-open 路径可观测。

**搭车 backlog 子项**：
- `PR245-F5` envelope test audit（确认 envelope wire 字段 round-trip 测试覆盖率）
- `PR245-F6` archtest scan scope expand（archtest 用 AST 解析 topic 常量传播链路，参考 PR246-FU1 FMT-19 模式）
- `MULTI-REVIEW-RES-2` OUTBOX-FAILOPEN-SECURITY-EVENT-ALERT-01（`gocell_outbox_emit_failopen_dropped_total{cell,topic}` counter + `HealthContributor` /readyz `degraded` 状态联动）

**验收**：
- archtest AST 模式可扫到 `const xxxTopic = "user.created.v1"` 这类常量定义点
- fail-open drop 计数器在三 cell demo 拓扑下可观测
- /readyz 在 fail-open drop 频率超阈值时返回 `degraded`
- archtest `OUTBOX-TOPIC-FAILOPEN-01` rule 通过 PR-A6 涉及的所有事件 topic

---

### PR-A50 CONFIGCORE-CTX-CANCEL-CONSOLIDATE（Cx3，~6h）

**主题**：configcore ctx-cancel 错误分类一次性彻底，抽 pkg 级 helper。

**搭车 backlog 子项**：
- `PR238-FU1` CONFIGCORE-DECRYPT-CATEGORY-AUTH-EVAL-01（`decryptValue` / `decryptVersionValue` 错误改 `Category: CategoryAuth`）
- `PR238-FU3` / `FU4` / `FU6` / `FU7` / `FU8`（configcore 各 ctx-cancel 处错误归类与文档对齐）
- `STALECIPHER-OPFIELD`（stale cipher 错误 op 字段统一）
- `FLAGREPO-CTXCANCEL`（flagrepo ctx-cancel 路径统一）
- `CTXCANCEL-HELPER-PKGLEVEL`（决策抽 `pkg/persistence/ctxcancel.go` 公共 helper）
- `PR238-FU2`（infra bucket category 漂移识别）+ `FU2-GOVERNANCE-STATIC` OBS-IS-INFRA-BUCKET-GOVERNANCE-STATIC-01（`kernel/governance/rules_obs.go` 扫 Prometheus counter 注册的 label 来源）

**前置决策**：是否抽 `pkg/persistence/ctxcancel.go`（影响所有 cell repo），需 architect 评审。

**验收**：
- configcore 所有 repo 的 ctx-cancel 路径统一 `Category: CategoryInfra` 或 `CategoryAuth` 分类决策
- `pkg/persistence/ctxcancel` helper 接口稳定，三 cell（accesscore/configcore/auditcore）全部迁移
- `kernel/governance/rules_obs.go` archtest 扫到 Prometheus counter label 来源不在 errcode bucket 时 PR fail
- bucket-migration 表写入 PR description

---

### PR-A51 CTX-CANCEL-HTTP-499（Cx2，~5h）

**主题**：HTTP 客户端断开统一映射 499 + Span status 同步。

**搭车 backlog 子项**：
- `CTXCANCEL-HTTP-499` CTX-CANCEL-HTTP-499-MAPPING-01（`pkg/errcode` + `pkg/httputil/codeToStatus` 加 `ErrConfigReqCanceled` → 499，停发 log5xx Error）
- `OTEL-SPAN-CTXCANCEL` OTEL-SPAN-CTX-CANCEL-STATUS-01（`runtime/http/middleware/tracing.go` 在 ctx cancel 时强制 `span.SetStatus(codes.Error, "client_canceled")`）

**验收**：
- 客户端 ctx cancel 路径 HTTP 响应 499（参考 nginx 语义），slog 仅 Warn 不 Error
- OTel span 状态在 ctx cancel 时 Error + `error.type=client_canceled`
- 现有 5xx Error rate SLO 不再被 client cancel 污染

---

## Wave 8 — Listener / Adapter 收尾（4 PR，~24h）

PR-A14a/A14b dual-listener + PR-A18 Vault/RMQ 落地之后的 DX 与诊断收尾。

### PR-A52 LISTENER-DX-UNIFY（Cx2，~5h）

**主题**：dual-listener API 命名统一 + Delegated → Internal rename + prefix 常量提升。

**搭车 backlog 子项**：
- `PR237-DX1` LISTENER-OPTION-NAMING-UNIFY-01（`WithHTTPPrimaryAddr` / `WithHTTPInternalAddr` 与 `WithPrimaryListener` / `WithInternalListener` 命名对称）
- `PR237-A2` ROUTEDECL-DELEGATED-RENAME-01（`auth.Route.Delegated` / `kernel/cell.AuthRouteMeta.Delegated` 字段名 rename 为 `Internal bool`，16 处调用站点）
- `PR237-A4` LISTENER-PREFIX-CONST-LIFT-01（`/internal/v1/` prefix 常量从 router 内部提升到 `kernel/cell` 公共导出）

**验收**：
- 全仓 grep 无 `Delegated` 残留（含 godoc）
- listener option 命名可在 IDE 补全列表中相邻
- prefix 常量从 cell.go 直接引用，无 magic string

---

### PR-A53 BOOTSTRAP-LISTENER-SLICE-POLISH（Cx3，~4h）

**主题**：bootstrap phase7 listener slice 重构 + health checker ctx budget 收尾。

**搭车 backlog 子项**：
- `PR237-A6` BOOTSTRAP-PHASE7-SLICE-POLISH-01（phase7 slice 物理拆分，避免 listener 装配代码与 cell wiring 混淆）
- `A21` HEALTH-CHECKER-CTX-BUDGET-01（`runtime/http/health.Checker` 升级为 `func(ctx.Context) error`，`ReadyzHandler` 注入 2s 统一 deadline + 并发执行；前置 `kernel/lifecycle.ManagedResource.Checkers()` 签名同步升级，已部分由 PR-A35 覆盖，剩余收尾）

**前置**：PR-A35 ✅ READYZ-POLISH 已合，本 PR 处理 Checker 签名升级残余。

**验收**：
- `Checker` 签名统一 `func(context.Context) error`
- `ReadyzHandler` 在 2s deadline 内并发执行所有 checker，超时返回各 checker 耗时
- bootstrap phase7 文件 ≤ 200 行
- 所有 `Checkers()` 实现迁移完成

---

### PR-A54 VAULT-RMQ-DIAGNOSTIC（Cx3，~9h）

**主题**：adapter 诊断字段形式化 + Vault cache version metric + RMQ structured diagnostic + CI 固定。

**搭车 backlog 子项**：
- `PR240-A1` MANAGEDRESOURCE-CONTRACT-FORMALIZE-01（`kernel/lifecycle/doc.go` 写实施清单 + archtest 强制 `adapters/*` 顶层 exported 类型实现 `ManagedResource` 或显式 opt out）
- `PR240-A2` PROBE-NAMING-CONVENTION-01（probe naming 统一约定）
- `PR240-OB1` VAULT-CACHE-VERSION-METRIC-01（Vault cache version metric 暴露）
- `PR240-DX1` MANAGEDRESOURCE-WIRING-EXAMPLE-01（wiring example 文档）
- `LATER-AL-R` RMQ-STRUCTURED-DIAGNOSTIC-01（RabbitMQ 关闭路径 structured diagnostic 字段）
- `PR-A8-RESIDUAL` VAULT-K8S-AUTH-E2E-01（K8s auth e2e 测试，4h，🟡 可延后）
- `V-A12` SYMLINK-TOCTOU-CREDFILE-AUDIT-01（全仓 `os.OpenFile(...O_CREATE...)` TOCTOU 路径审计）
- `A8` CI-DIGEST-01（testcontainers 镜像 tag+digest 双固定）
- `A9` CI-LINT-PIN-01（golangci-lint patch 级固定 + dependabot）

**验收**：
- archtest LAYER-07 扫 `adapters/*` 顶层 exported 类型实现 `ManagedResource` 或写在白名单
- Vault cache version metric 在 demo 部署可见
- RMQ Close 路径 slog 含结构化关闭原因
- 全仓 `os.OpenFile(...O_CREATE...)` 审计 PR description 含表格
- CI workflow 镜像 digest 固定，dependabot config 含 golangci-lint

---

### PR-A55 ADAPTER-T-SKIP-BACKFILL（Cx3，~6h）

**主题**：6 个主干 adapter 共 15 处 `t.Skip` testcontainer 配置补齐。

**搭车 backlog 子项**：
- `LATER-T-1` ADAPTER-TSKIP-BACKFILL-01（`adapters/postgres` / `redis` / `rabbitmq` / `vault` / `otel` 各 t.Skip 来源审计 + Testcontainer 配置补齐）
- `A10` OBS-LGTM-INTEGRATION-01（`//go:build integration` 夜间 OTel collector 真实 OTLP 协议兼容性测试）

**验收**：
- 优先补"重连 / 错误恢复 / TLS 拒收 / 死信路由"等运行时关键路径
- `go test -tags=integration ./adapters/...` 跑通含原 t.Skip 测试
- CI nightly job 含 `adapters/otel` integration

---

## Wave 9 — 多 pod / 发布触发项（3 PR，~13h，🟠 触发）

不进开发主链，触发条件成立时再启动；触发条件未到时只在 backlog 留位。

### PR-A56 SETUP-MULTIPOD-CLOSURE（Cx3，~5h，🟠 PG accesscore adapter 接入前）

- `A26-R1` ADMINPROVISION-DIST-LOCK-01（`adminprovision.Provisioner.Ensure` 在 PG TxRunner 内 `pg_advisory_xact_lock(<const>)` 跨进程唯一 admin 创建）
- `A26-R4` SETUP-ORPHAN-E2E-01（PG path E2E：DB 写 orphan 行 → 重启 → setup POST 验证 RequireReset=false）

---

### PR-A57 SETUP-PUBLIC-EXPOSURE-HARDEN（Cx2，~4h，🟠 setup 端点对公网开放前）

- `A26-R2` SETUP-ADMIN-RATE-LIMIT-01（per-IP rate-limit middleware + 失败计数 lockout 5min/5次→429；或一次性 bootstrap token 模式）

---

### PR-A58 RELEASE-DOCS-MULTIPOD（Cx2，~4h，🔴 v1.0 GA 前必做）

- `PR237-PM5` DUAL-LISTENER-DEPLOYMENT-DOC-01（部署指南：dual-listener 网络拓扑 + ingress 配置 + Service 拆分模式）
- `PR237-PM7` EXAMPLE-INTERNAL-LISTENER-COMMENT-01（examples 内部 listener wiring 注释加 production hint）
- `PR250-F1` CONFIG-VERSION-PUBLISHED-SENSITIVE-FIELD-EVAL-01（首次外部 cell 订阅 `event.config.version-published.v1` 时评估，本 PR 写文档触发条件而不实施）

---

## Wave 10 — Command 生产治理（2 PR，~14h，🟠 触发）

PR-A12b kernel/command 生命周期收口之后的生产治理项；触发条件成立时再启动。

### PR-A59 COMMAND-QUEUE-REGISTRAR-FAILFAST（Cx3，~6h，🟠 第二个 durable command adapter 前）

- `PR252-F1` COMMAND-QUEUE-REGISTRAR-BOOTSTRAP-FAILFAST-01（QueueRegistrar 发现 / queue+scanner 注入 / capability 校验纳入唯一 bootstrap phase；缺 queue 或 scanner 时 fail-fast；补 bootstrap phase 单测覆盖 missing queue / missing scanner / duplicate registrar / devicecell 成功路径）

---

### PR-A60 COMMAND-SWEEPER-PRODUCTION-GOVERNANCE（Cx4，~8h，🟠 多副本 command consumer 或 PG command queue 前）

- `PR252-F2` COMMAND-SWEEPER-PRODUCTION-GOVERNANCE-01（leader / 分片 ownership + 连续失败阈值 → /readyz 降级 + scan/ack timeout/failure/consecutive failure 指标 + fake scanner/queue 失败预算测试 + 多实例 ownership 测试）
- `PR-A12-SWEEPER-WIRE` PR-A12-KERNEL-COMMAND-SWEEPER-WIRE-01（`InMemQueue` 长出 `ListAllPending(ctx, since time.Time)` → cell-level `Sweeper` 后台定时回收，超时 entry 按 `TimeoutPhase` 分流；与 F2 同 PR）

---

## Wave 11 — 长尾清扫 + 测试基础设施（4 PR，~22h）

不阻塞 v1.0、可在主链 Wave 5-8 任意空档插入。

### PR-A61 ACCESSCORE-FACADE-POLISH（Cx2，~4h）

**主题**：accesscore initialadmin facade 修边 — credpath 命名链 + errcode 一致性 + LifecycleHook 慢启动 warn。

**搭车 backlog 子项**：
- `A5a-R9` INITIALADMIN-CREDPATH-NAMING-CHAIN-01（删 `accesscore.ResolveBootstrapCredentialPath` 外层 wrapper，调用方直接用 `initialadmin.ResolveCredentialPath`）
- `A5a-R10` INITIALADMIN-ERRCODE-CONSISTENCY-01（`bootstrap.go:79-90` 三处 `fmt.Errorf` → `errcode.New(errcode.ErrCellInvalidConfig, ...)`）
- `A5a-R11` LIFECYCLECONTRIBUTOR-BLOCKING-SEMANTICS-WARN-01（phase3b runHook 在 OnStart 耗时 ≥ defaultStartTimeout × 0.8 时 slog.Warn）

**验收**：
- `cmd/corebundle` + `examples/*` 直接 import `initialadmin` 解析 credential path
- 所有 errcode 路径统一 `ErrCellInvalidConfig` 包装
- LifecycleHook 慢启动 warn 在 phase3b 单测覆盖

---

### PR-A62 PAGINATION-HELPER + SLICE-RESIDUAL（Cx2，~5h）

**主题**：分页 helper 抽取 + slice 残余的真实分页 + 4xx 采样 + boundary / status-board 测试。

**搭车 backlog 子项**：
- `L8` PAGINATION-HELPER-EXTRACT-01（`pkg/httputil/pagination.go` 抽 `ParsePageRequest` + `slog.Warn` + `WriteDomainError` 三件套）
- `S13-follow` 4XX-LOG-SAMPLING-01（高频端点 4xx 日志 1% 采样 / rate-limiter 触发后限速）
- `FEAT-1-残余` RBACCHECK-PAGINATION-REAL-01（`cells/accesscore/slices/rbaccheck/handler.go:79` `handleListRoles` 迁 `query.PageResult[RoleResponse]` + 真实游标分页）
- `PR220-e2` PAGINATION-BOUNDARY-TEST-01（boundary 分页参数测试）
- `PR220-e3` STATUS-BOARD-J-ORDERCREATE-01（J-ordercreate journey status board 校验）
- `F3` audit e2e（journey 级验收）

**验收**：
- 各 cell handler 统一引用 `pkg/httputil/pagination` helper
- rbaccheck `handleListRoles` 真实游标分页 + contract test `MustRejectResponse` 负向断言
- 4xx 采样在高频端点测试可观测

---

### PR-A63 TEST-INFRA-HARDEN（Cx3，~7h）

**主题**：journey 验证 fail-closed + walkthrough compose + journey assembly harness。

**搭车 backlog 子项**：
- `PR220-3` JOURNEY-VERIFY-FAIL-CLOSED-01（journey verify 默认 fail-closed，缺 fixture / 缺 assembly 时 PR fail 而非 skip）
- `S23` AUTH-WALKTHROUGH-COMPOSE-01（`examples/ssobff/walkthrough_test.go` 与 `main.go` 共用 `NewSSOBFFApp(opts...)` 装配函数，走同一 bootstrap + Start/Stop）
- `F10` TEST-JOURNEY-ASSEMBLY-HARNESS-01（`tests/integration/journey_test.go` 28 条 t.Skip 全部恢复 — J-sessionrefresh / J-sessionlogout / J-accountlockout / J-auditlogintrail / J-config-* / J-ssologin / J-useronboarding）

**验收**：
- `gocell run-journey` 默认 fail-closed，缺 fixture 时 exit 1
- `examples/ssobff/walkthrough_test.go` 复用 `NewSSOBFFApp` 装配
- 28 条 journey integration test 100% 恢复（不再 t.Skip）

---

### PR-A64 LINT-MODERN-DEAD-VAR（Cx2，~6h）

**主题**：一次性 lint baseline 清理 — modernization + dead variable。

**搭车 backlog 子项**：
- `X9` LINT-MODERN-01（全仓 modernization baseline：rangeint / stringsseq / forvar / inline / testingcontext / any / nhooyr.io→coder/websocket）
- `DEAD-VARIABLE-01`（unused variables / parameters 全仓清扫）
- `V-A6` WEBSOCKET-DEPRECATED-01（`go.mod` `nhooyr.io/websocket` → `github.com/coder/websocket`，与 X9 合 PR）
- `V-A7` DEPRECATED-ADAPTER-METHODS-01（`Connection.Statter()` / `Subscriber.InitializeSubscription()` 移除 Deprecated 标注或先迁测试再删）
- `V-A8` CMD-THICK-ENTRY-REDUCE-01（`cmd/corebundle/main.go` 继续缩减 + `internalGuard` 提升为 `corebundle.InternalGuard` 或抽接口）
- `V-A5` VALIDATION-HELPER-EXTRACT-01（`pkg/validation.RequireNotBlank` 抽取 helper 消除 6+ 处 `if token == ""`）

**验收**：
- `golangci-lint run --enable-all` 0 issues
- `go.mod` `nhooyr.io/websocket` 完全替换
- `cmd/corebundle/main.go` < 100 行

---

## §六 · Won't-do（5 项，决策已结）

> 显式登记不做的 backlog 项 + 决策依据。下一份 plan 不再回看；如需重启评估，需附新触发条件。

| 条目 | 名称 | 决策依据 |
|---|---|---|
| **A5a-R3** | ACCESSCORE-INITIALADMIN-THIN-WRAPPER-01 | PM + architect review 一致倾向保持现状；thin wrapper 增 facade 维护成本，且 `LifecycleOption` 切片语义天然暴露底层结构；省一次 import 的 DX 收益有限。**复查触发**：任意第二个 cell 公开"subpackage + functional options"模式时。 |
| **A5a-R12** | INITIALADMIN-TESTING-ENTROPY-UNEXPORT-01 | accesscore cell-level test（`package accesscore`）需注入确定性熵源；`withPasswordSource` unexport 后外包不可见。替代方案 `accesscore.NewLifecycleForTest(...)` 更侵入。**复查触发**：v1.0 前 security audit 标红，或 accesscore 包内更多 test-only entropy 注入需求。 |
| **PR250-F1** | CONFIG-VERSION-PUBLISHED-SENSITIVE-FIELD-EVAL-01 | 当前 `event.config.version-published.v1` 订阅者均为同 repo 内三 cell（accesscore/configcore/auditcore），攻击面有限；从 wire 移除 `sensitive` 字段会破坏既有 audit 路径。**复查触发**：首次外部 cell 订阅此 topic（已纳入 PR-A58 文档触发条件）。 |
| **PR250-F3** | EVENT-WIRE-BYTE-PINNING-TEST-EVAL-01 | schema validation + `additionalProperties: false` 已在 contract_test 覆盖字段名漂移；golden-string 对字段顺序 / `omitempty` 边界 / 生成器差异脆弱，净增维护成本。**复查触发**：出现真实 camelCase regression 漏网。 |
| **V-A13** | GENTPL-LIFECYCLE-PATTERN-01 | `kernel/assembly/gentpl/main.go.tpl` 已用 `app.Start/Stop` 走正确路径，仅 `doc.go` 文档示例旧。模板不需要结构性改造，仅文档同步即可（已在 PR-A39 HOUSEKEEPING 覆盖）。**复查触发**：第二个用户报告 `gentpl` 生成的 assembly 缺生命周期。 |

---

## §七 · 触发条件项（7 项，不排期）

> 不进 Wave 计划，触发条件成立时单独评估并新建 backlog → plan 流程。

| 条目 | 名称 | 触发条件 |
|---|---|---|
| **T1** | AUTH-PROVIDER-EXPORT-01 | 第二个 auth provider cell 出现（`runtime/bootstrap.authProvider` 接口需移出 bootstrap 包） |
| **T2** | AUTH-ISSUE-OPTIONS-01 | `JWTIssuer.Issue()` 添加第 5 个参数（重构为 `IssueOptions` struct） |
| **T3** | DEVICE-ENQUEUE-RBAC | 多租户 operator（HandleEnqueue 加设备维度鉴权） |
| **T4** | CB-RESILIENCE-PACKAGE-01 | 第二个非 HTTP 的 CB 消费方（`Allower` / `CircuitBreakerRetryAfter` 迁 `runtime/resilience/circuitbreaker/`） |
| **T5** | AUTH-SIGNER-01 | golang-jwt v6 发布（`SigningKeyProvider` 返回 `crypto.Signer` 替代 `*rsa.PrivateKey`） |
| **T6** | CONTRACT-EVENT-PAYLOAD-CODEGEN-01 | event subscriber 扩散到 ≥ 5 cell（当前 2 cell：accesscore/configreceive + configcore/configsubscribe） |
| **S14a** | CONFIG-VALUE-KMS-AWS-PROVIDER-01 | 明确部署目标云平台 + 通过 KMS 安全评审 |

---

## §八 · 长期债占位（不在本 plan 详写）

以下条目仍归 025 plan §Wave 4，本 plan **不重写、不重新切 PR**；触发条件成立或排期到达时回 025 plan 推进：

- **025 plan PR-A21** AUTH-JWT-LIB-EVAL（~0.5-1d）
- **025 plan PR-A22** Cell ISP 拆分（~1.5d）
- **025 plan PR-A23** ER-ARCH-01 Subscriber Setup/Run 双阶段（~2d）
- **025 plan PR-A24** DURABLE-TYPE-01 + G-6 + kernel/replay + rollback（~2d）
- **025 plan PR-A33** REFRESH-OPAQUE-POLISH（X12 + X13 + X14，~8h；PR-A29 已合可解锁）
- **backlog P3 章节**：X1 PG-DOMAIN-REPO / X2 / X3 / X4 / X5 / X12 / X13 / X14（已映射到 025 PR-A33）
- **backlog 历史发布项**：F2 SYSTEM-TOPOLOGY-API / F4-F8 / P1-5 METADATA-PERF-BENCH / DISTLOCK-WORKER（已早期完工，不再列）

---

## 工期与并行策略

### Wave 主链（Wave 5-8 + Wave 11）

| Wave | PR 数 | 已完成 | 剩余编码 | 推荐并行 |
|---|---:|---:|---:|---|
| Wave 5 卫生 + CI | 5 | 1（A41 ✅） | ~12h | 双路 worktree（A39 / A40+A42+A43） |
| Wave 6 Auth/Security | 4 | 0 | ~17h | 双路（A44+A45 / A46+A47） |
| Wave 7 Outbox/Event/Config | 4 | 0 | ~24h | 双路（A48+A49 / A50+A51） |
| Wave 8 Listener/Adapter | 4 | 0 | ~24h | 双路（A52+A53 / A54+A55） |
| Wave 11 长尾 | 4 | 0 | ~22h | 双路（A61+A62 / A63+A64） |
| **主链合计** | **21** | **1** | **~99h（~12.4d）** | **双人 ~7.5d** |

### 触发条件 Wave（Wave 9-10）

| Wave | PR 数 | 净编码 | 触发后并行 |
|---|---:|---:|---|
| Wave 9 多 pod / 发布 | 3 | ~13h | A56 / A57 / A58 三路并行 ~1d |
| Wave 10 Command 生产 | 2 | ~14h | A59 → A60 串行 ~2d |

### 推荐执行顺序（主链）

1. **Sprint 1（~5d 双人）**：Wave 5 + Wave 6 — 卫生 + Auth 加固一气呵成
2. **Sprint 2（~4d 双人）**：Wave 7 + Wave 8 — Outbox/Event/Config + Listener/Adapter 收尾
3. **Sprint 3（~2d 双人）**：Wave 11 — 长尾清扫，可与 Sprint 1/2 任意空档插入
4. **触发后**：Wave 9（v1.0 GA 前）+ Wave 10（生产前）

---

## 依赖与风险

### 跨 Wave 依赖

- **PR-A47 ERRCODE-5XX-MASK** 需要 PR-A50 / PR-A51 ctx-cancel 收口前完成，避免 503 → 499 重映射时 errcode 字典再次翻新
- **PR-A49 OUTBOX-ARCHTEST-EXPAND** 的 AST 升级路径参考 PR246-FU1 FMT-19 已落地实现，无前置阻塞
- **PR-A50 CONFIGCORE-CTX-CANCEL-CONSOLIDATE** 抽 pkg 级 helper 需 architect 决策（`pkg/persistence/ctxcancel.go`）
- **PR-A53 BOOTSTRAP-LISTENER-SLICE-POLISH** 的 Checker 签名升级需要 `kernel/lifecycle.ManagedResource.Checkers()` 同步升级，影响所有 adapter — 与 PR-A54 同 Sprint 执行

### 触发条件项的依赖

- **PR-A56 / PR-A57** 触发条件互不依赖
- **PR-A60** 包含 `PR-A12-SWEEPER-WIRE` 搭车，依赖 `InMemQueue.ListAllPending` 长出
- **PR-A58** 文档项无前置依赖，可在 v1.0 GA 前任意时间启动

### 已识别风险

1. **PR-A50 + PR-A47 errcode 改动重叠**：建议 PR-A50 → PR-A47 顺序，避免双向回滚
2. **PR-A54 V-A12 全仓 TOCTOU 审计** 可能扩大范围（发现新路径需新增 PR）
3. **PR-A55 testcontainer 补齐** 受 CI runner 资源限制，可能需要 nightly job 隔离
4. **PR-A63 28 条 journey 恢复** 任何一条 fixture 缺失会阻塞整组；建议先扫 fixture 完整性再开 PR
5. **配置/认证 baseline 漂移**（2026-04-26 新增）：PR#266/#267/#268/#269 集中改写了 configcore / runtime/auth / runtime/bootstrap / cells 多个区域。启动 PR-A48 / A50 / A52 / A53 / A61 前必须基于新 baseline 重新核销 backlog 子项的实际可见点，避免针对已不存在的代码做修改

---

## 进度跟踪规则

延续 025 plan 习惯：每批 PR 落地后在本 plan 头部追加"轮次更新"段落；PR 编号 + PR# + 落地日期 + 主要 backlog 子项关闭情况。Wave 完成时更新顶部 `## PR 切分总览` 表的"已完成"列。

PR 拆分允许向下细化（一个 PR-A39 拆成 A39a / A39b 子 PR），但**不允许新增 PR-A65+ 编号**——超出 26 个 PR 的项必须证明属于本 plan 漏网，否则归 027 plan 或 backlog Won't-do。
