# PR 473
• 结论：先别直接合，建议小修后合并。

  唯一需要处理的是 SonarCloud 报的 2 个新 Critical code smell，都是 worktrees/570-contractspec-framework-funnel/kernel/
  contractspec/framework_test.go:14 的测试函数认知复杂度超标：

  - TestNewFrameworkHTTP: 26 > 15
  - TestNewEventDerivation: 29 > 15

  这违反项目 CLAUDE.md 里的“函数认知复杂度 ≤ 15”。拆几个 assertion helper 或分成更小的子测试就能解决。

  功能和架构上我没看到阻塞问题：typed funnel 方向合理，runtime 迁移无明显行为回归，cells 侧 funnel bypass 也补了
  archtest。验证已过：
worktrees/570-contractspec-framework-funnel
worktrees/570-contractspec-framework-funnel


# PR 474 
• 结论：条件通过。
  代码实现和测试结果都 OK，我没看到阻断合并的代码问题；但文档同步还有残留矛盾，建议修完再 merge。

  发现的问题：

  - docs/plans/202605112000-036-archtest-governance-rollout-plan.md:268 仍把 Phase 3.2 PR-SH1 算进“当前剩余”，但上
    面 :256 已标 ✅ PR #474。
  - 同文件 :276 仍把 PR-SH1 排为 next-up 主线，:282 的总剩余工时也还包含 PR-SH1。
  - docs/backlog.md:421 状态列仍写“剩余 SCANNER-FRAMEWORK-HARDENING + CALL-MATCHER caller 迁移 → Phase 3.2 PR-SH1”，和前
    面子项已 closed by PR-SH1 矛盾。

  验证：

  - 本地 go test ./tools/archtest/... -count=1 通过。
  - PR checks 全绿：make verify、race、integration、16 个 verify-archtest shard、CodeQL、Semgrep、govulncheck 都 pass。

  我的建议：修掉上述文档残留后合并；代码面可以给 approve。

    完成 Phase 3.2 PR-SH1 全流程 (L4)：

  实施 (commit 1f6e8637)
  - inspector 方法 ban 移到 forbiddenMethodSymbols（path A' via ResolveMethodCall），forbiddenWalkSymbols[inspector]
  缩为真实顶级集 {New, All}
  - 新增 tools/archtest/internal/inspectorredfixture/ 真实包 + TestScannerFrameworkUsage01_InspectorMethodBanLive
  end-to-end 锁 4 method coverage（Hard：删除条目 → 测试红，已验证）
  - svctoken / role_admin 迁移到 typeseval.ResolvePackageRef，统一 path A.2 + A.3，删除 isAuthFuncCall helper 和
  go/types 冗余 import
  - 迁移暴露 runtime/auth/servicetoken_test.go 内部负向 test，加 same-package exemption
  - 文档同步：rollout plan §3.2 / §5 / §6 + backlog bundle 两子项标 closed

  L4 6-reviewer review (commit 761a46a8 - fix pass)
  - Cx2 (3 reviewers): svctoken 豁免改 pkg.PkgPath == authRuntimeImportPath + _test.go 后缀（不再用 file-rel 前缀）
  - Cx1 × 6: 失败诊断 pretty-print、cross-file const 注释、SharedResolver 参数说明、var _ = callBannedMethods lockstep
  说明、fixture 计数 32→39、plan §5 标 ✅ PR #474
  - 架构师 A6: outbox_invariants_test.go::scanForHandleResultLiterals 同主题修复：dot-import path A.3 类型引用
  gap，迁移到 info.Uses[id].(*types.TypeName) 统一三种形态（qualified / same-package / dot-imported）

  验证
  - go test ./tools/archtest/... 247s 全绿
  - verify-archtest.sh 16-shard / 315 tests PASS
  - golangci-lint run ./... 0 issues
  - go build -tags=integration ./... 清洁
  - CI 仍 pending（推 fix 后重跑）

  未处理 (人工确认)

  #: Sec F1
  Finding: inspector 数据行评级 Medium-with-strong-test
  Cx: Cx2
  处置: 接受
  原因: Hard 需类型化常量，超出本 PR 范围
  ────────────────────────────────────────
  #: Cov F4
  Finding: 重命名 inspector import (import inspr) 未单独覆盖
  Cx: Cx1
  处置: 不修
  原因: ResolveMethodCall 走 info.Selections.Obj()，与语法 import 名无关；reviewer 自评 risk 极低
  ────────────────────────────────────────
  #: Cov F5
  Finding: inspector MethodExpr 形式 (*Inspector).Preorder(insp,...) 未在 live test 覆盖
  Cx: Cx1
  处置: 不修
  原因: ResolveMethodCall 的 MethodExpr 路径已由 method_expr_qualified_interface / method_expr_pointer_type (io/fs/os)
    锁定
  ────────────────────────────────────────
  #: Obs F3
  Finding: plan §0 b30afc52 commit hash
  Cx: Cx1
  处置: 不修
  原因: 非本 PR 引入，是之前 PR #440 commit hash，未验证但属现有文档状态
  ────────────────────────────────────────
  #: Arch A1a
  Finding: inspectorredfixture 缺 //go:build archtest_fixture 标签
  Cx: Low
  处置: 不修
  原因: architect 自标 OUT_OF_SCOPE follow-up
  ────────────────────────────────────────
  #: DX F2
  Finding: 诊断消息硬编码 auth. 前缀（path A.3 调用无该前缀）
  Cx: Cx1
  处置: 不修
  原因: 规范名 auth.GenerateServiceToken 仍是消息中正确的语义引用；rule ID + 文件 path 已足以定位

  merge 后清理：在主 repo git worktree remove worktrees/569-archtest-inspector-method-ban && git branch -d
  refactor/569-archtest-inspector-method-ban。

# PR 475

• 阻塞发现：无。结论：PR 475 可以合并，我会给 approve 结论。

  我检查了 diff、PR 元数据和评论：PR 可合并、非 draft，GitHub checks 全绿，评论里只有 Sonar quality gate 通过。改动范围
  也合理：把 fileHasIntegrationTag 合并到既有 fileHasExclusivelyTag(path, "integration")，并新增 TYPESEVAL-EVAL-
  PREDICATE-CENTRALIZED-01 archtest，当前 Expr.Eval 调用点都符合新规则。

  验证已跑：
  go -C worktrees/005-archtest-eval-predicate-central test ./tools/archtest/... 通过。
  focused test TestEvalPredicateCentralization|TestArchtest_BuildConstraint... 也通过。

  残余风险只有一个很窄的非阻塞点：worktrees/005-archtest-eval-predicate-central/tools/archtest/
  eval_predicate_centralization_test.go:301 对 return false 是 AST 名字检查，理论上外层作用域可 shadow false。这属于刻意
  绕过型边界，不影响现有路径；要追求更硬可后续用 types.Info 校验该 ident 指向 universe false。


    Ship 流程完成 — 等待人工确认

  PR: #475 — feat(archtest): TYPESEVAL-EVAL-PREDICATE-CENTRALIZED-01 Hard funnel (Phase 3.5 PR-EP1)

  Branch: feat/005-archtest-eval-predicate-central
  Worktree: worktrees/005-archtest-eval-predicate-central/
  Commits: 2（主实现 e1ffc0c2 + reviewer fix 93862c24）

  已完成

  - Phase 1-2：3 explorer 并行探索 + plan 设计 + 4 项原则三层自审
  - Phase 3：worktree 005 基于 origin/develop 创建
  - Phase 4：TDD RED — archtest 准确捕获 line 138 1 处违规
  - Phase 5：合并删 fileHasIntegrationTag + 改调用 fileHasExclusivelyTag(path, "integration") → GREEN
  - Phase 5.x：go build/-tags=integration/go test ./tools/archtest/.../golangci-lint（0
  issues）/hack/verify-archtest.sh（316 tests, 16 shards 全绿）
  - Phase 6：commit + push + 开 PR #475
  - Phase 7：1 reviewer agent 六维度（6 findings, 5 Cx1 + 1 Cx2, 0 Cx3/Cx4 阻塞）
  - Phase 8：
    - P1 Cx2：添加 TestEvalPredicateCentralizationFixtures + 5 RED/GREEN testdata fixture（form_a_good / form_b_good /
  inline_predicate_red / var_binding_red / funclit_multi_stmt_red）
    - P1 Cx1：isBuildContextPredicateCallee return false 解释注释（防 fallback 误改）
    - P1 Cx1：isAllFalseSentinelFuncLit shadowing 不可达推理 godoc
    - P1 Cx1：findIntegrationTagViolations godoc 同步 PR-BT1 BuildContextPredicate 语义说明

  未处理问题（需人工确认）

  reviewer 6 findings 中，3 处 P2 reviewer 自己建议接受现状（不算违规）：

  #: 1
  Finding: nil-TypesInfo 静默 skip 处补 t.Logf
  Cx: P2 Cx1
  建议方案: 接受现状
  原因: reviewer：与 scanner_framework_usage_test.go:111-112 同惯例，非阻塞
  ────────────────────────────────────────
  #: 2
  Finding: formatEvalCallee 与 panic_invariants_test.go::formatCallee 合并至 helpers_test.go
  Cx: P2 Cx1
  建议方案: 接受现状（follow-up 触发型）
  原因: reviewer：3 文件 refactor，优化方向，不破坏规则
  ────────────────────────────────────────
  #: 3
  Finding: violation msg 在 <unknown> callee 时附加 go/printer 表示
  Cx: P2 Cx1
  建议方案: 接受现状
  原因: reviewer：file:line 已足够定位，非阻塞

  CI 状态

  刚 push 完，CI 全 pending。按 ship skill 约定不自动轮询：

  gh pr checks 475

  请人工跟进 CI 结果，merge 后处理 backlog sync（plan §3.5 标 ✅ done + backlog 条目 closed + 删 worktree）。

# PR 477

• 结论：PR 477 可以合并。未发现需要阻断的 review 问题。

  我重点看了 worktrees/004-cellgen-reflect-literal-printer/tools/codegen/cellgen/literal_printer.go、cell.tmpl、
  CellGenSpec 改动和 AssemblyMeta DTO archtest 迁移。当前实现能覆盖本 PR 目标：CellMeta 顶层字段改动会进 reflect
  renderer，codegen drift gate 能兜住；catalog 的 AssemblyMeta 字段覆盖检查也已迁到 tools/archtest。

  验证结果：

  - GitHub checks 全绿。
  - 本地通过：go build ./...
  - 本地通过：go test ./tools/codegen/cellgen/... ./tools/archtest/... ./runtime/devtools/catalog/...
  - 本地通过：go test ./cells/...
  - 本地通过：go test ./kernel/metadata/...
  - 本地通过：gocell validate --strict
  - 本地通过：gocell verify codegen-cell --local=true

  非阻断残留：CELLGEN-LITERAL-FUNNEL-02 仍有必要，防止未来模板被改回手写字段枚举；但这个已在 PR 里登记为 follow-up，不影
  响本 PR 合并。

⏺ PR 已 push。CI pending（不轮询）。整理最终报告。

  ---
  PR-MD1 完工报告

  PR: #477 feat(cellgen): reflect-driven CellMeta literal printer + AssemblyMeta DTO coverage (PR-MD1)

  Branch: feat/004-cellgen-reflect-literal-printer (worktree worktrees/004-cellgen-reflect-literal-printer)

  Commits:
  - b900f660 feat: PR-MD1 主体（reflect renderer + 任务二迁移 + 文档同步）
  - 35645ccb fix: PR-MD1 reviewer findings (P1-①②③ + P2-①②③)

  完成阶段

  ┌───────────────────────────────────────────────────────────────────┬─────────────────────────────────────────┐
  │                               阶段                                │                  状态                   │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ 探索 (L3 三 explorer 并行)                                        │ ✅                                      │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ Hard 方案研究 + 用户确认（保留 task 2 Medium，funnel guard 延后） │ ✅                                      │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ Worktree + TDD (literal_printer GREEN baseline 先写)              │ ✅                                      │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ 实施 (2 个 developer agent 并行)                                  │ ✅                                      │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ 验证 (build/test/lint/K#04/archtest-verify-process-isolated)      │ ✅                                      │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ PR 创建                                                           │ ✅ #477                                 │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ 六维度 review (1 reviewer agent, L3)                              │ ✅ 4 P1/Cx1 + 3 P2/Cx1 + 1 P2/Cx2       │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ Cx1 fix (developer agent)                                         │ ✅ 6 条 IN_SCOPE 全修                   │
  ├───────────────────────────────────────────────────────────────────┼─────────────────────────────────────────┤
  │ CI snapshot                                                       │ pending (push 完成不轮询，按 ship 约定) │
  └───────────────────────────────────────────────────────────────────┴─────────────────────────────────────────┘

  未处理问题（需人工确认）

  #: 1
  Finding: P1-④ CELLGEN-LITERAL-FUNNEL-02 (cell.tmpl type-system funnel guard)
  Cx: Cx1
  建议方案: 立即开 next-up PR (1.5-2h)
  原因: 按用户决定本 PR 不做；backlog 已登记 P1/Cx1 🟠 next-up，type-system Hard funnel 关闭"AI 改回手写 cell.tmpl
    字段枚举"silent drift 漏洞窗口
  ────────────────────────────────────────
  #: 2
  Finding: P2-④ archtest 不递归嵌套 struct 字段比对
  Cx: Cx2
  建议方案: godoc 注释明确测试范围；不改代码
  原因: OUT_OF_SCOPE per plan；测试精度上限问题，不影响当前正确性

  Worktree 清理

  按 git-worktree skill 约定：PR merge 后手动在主仓库目录执行 git worktree remove
  worktrees/004-cellgen-reflect-literal-printer。本会话不自动删除。

  CI 监控

  gh pr checks 477 可随时查看；CI 完成后请人工核实通过情况。

# PR 476

⏺ 阶段 9：人工确认

  PR: #476 https://github.com/ghbvf/gocell/pull/476
  分支: 806-effective-last-admin
  Commits:
  - 23f38b9e feat(accesscore): effective last-admin invariant (S4.0)
  - 70b347a5 fix(accesscore): S4.0 review Cx1/Cx2 — sentinel, fixtures, update guard test
  - bf5da662 fix(adapters/postgres): bump expected migration version to 24

  已完成：
  - TDD: domain / mem / service / outbox / contract 多层测试
  - 实施: domain sealed interface、ports + CountEffectiveAdmins、PG SQL 重写、mem 单 Store 共享 mutex、migration 024 +
  schema_guard、Lock/Delete/Update guard、4 个 contract YAML
  - PR + 六维 review（找出 6 项 Cx1 + 1 项 Cx2）
  - 已 fix: pgerrors.go sentinel（关键 P0：DB safety-net 之前实际失效）、测试夹具、断言文本、auth.RoleAdmin 常量、Update
  guard 单元测试、advisory-lock godoc 契约、migration 版本号
  - 全部 verification: go build / go test ./... / golangci-lint 0 issues / hack/verify-archtest.sh 314/314 pass

  未处理问题（需人工确认）：

  #: 1
  Finding: PG integration: effective_admin_invariant_on_users BEFORE UPDATE 触发器独立测试
  Cx: Cx3
  建议方案: 新增 TestEffectiveAdminTrigger_RawStatusUpdate_Rejected_PG，直接 SQL 修改 status='locked' 验证 trigger 拦截
  原因: 需 testcontainer，建议在后续 PG-specific PR 中补充；不阻塞 S4.0 合并
  ────────────────────────────────────────
  #: 2
  Finding: PG integration: CountEffectiveAdmins + locked peer 语义
  Cx: Cx3
  建议方案: 新增 PG 层 TestCountEffectiveAdmins_LockedAdminExcluded_PG +
  TestRemoveFromUserIfNotLast_LockedPeerDoesNotCount_PG
  原因: 已在 mem 层覆盖；PG 层 SQL 的 JOIN users + status='active' 过滤未独立验证
  ────────────────────────────────────────
  #: 3
  Finding: WithLastAdminProtection(nil) 忘传时静默跳过 guard
  Cx: Cx1 P1 (Soft)
  建议方案: 长期: archtest 覆盖 lastAdminProtectionRequested-without-guard；短期: backlog 登记
  原因: 当前 cell_init.go 唯一调用方已正确传入，archtest 兜底防未来回归
  ────────────────────────────────────────
  #: 4
  Finding: PGUserRepo.Delete trigger 路径错误消息 "delete blocked: last admin" 与其他路径不一致
  Cx: Cx1 P2 (部分 pre-existing)
  建议方案: 统一为 "cannot remove the last effective admin" 与 domain 层对齐
  原因: OUT_OF_SCOPE 的 pre-existing 字符串，单独 cleanup PR 处理

  CI 状态: no checks reported — 该仓库未配置 GitHub Actions CI，依赖本地 verify-archtest.sh / go test，均已本地通过。

  worktree 路径 worktrees/806-effective-last-admin 合并后可手动 git worktree remove worktrees/806-effective-last-admin 清理。

• ## Preflight

  - repo: ghbvf/gocell
  - PR: #476 feat(accesscore): effective last-admin invariant (S4.0)
  - base...head: develop(09df55c)...806-effective-last-admin(bf5da66)
  - changedFiles: 49
  - evidenceSource: GitHub API + local git diff
  - consistencyCheck: PASS (sha-match=PASS, api-files=49, local-files=49, fileSetMatch=PASS)

  ## Findings

  P1 - suspended 没有真正禁用账号，且用户可通过 self PATCH 改回 active
  worktrees/806-effective-last-admin/cells/accesscore/slices/identitymanage/handler.go:133 把 status 暴露给 PATCH，
  worktrees/806-effective-last-admin/cells/accesscore/slices/identitymanage/handler.go:237 对 PATCH 使用
  selfOrAdminPolicy。worktrees/806-effective-last-admin/cells/accesscore/slices/identitymanage/service.go:283 接受
  active/suspended。但登录只拒绝 locked：worktrees/806-effective-last-admin/cells/accesscore/slices/sessionlogin/
  service.go:158，refresh/validate 也没有检查用户状态。
  影响：被 suspended 的 admin 仍可登录、刷新、继续使用会话，甚至 self PATCH 回 active，直接破坏 “effective admin =
  active admin” 的安全语义。建议把 status/requirePasswordReset 做字段级 admin-only 或拆 endpoint，并让 login/refresh/
  validate 对 status != active fail closed，状态降级时像 lock 一样撤销会话。

  P1 - migration/setup 可把已有坏数据升级成“零 effective admin 且 setup 已退休”
  migration 024 只安装未来 DML 触发器，没有预检已有数据：worktrees/806-effective-last-admin/adapters/postgres/
  migrations/024_effective_admin_invariant.sql:33。setup retirement 仍按任意 admin role holder 判断：worktrees/806-
  effective-last-admin/cells/accesscore/internal/adminprovision/provisioner.go:117，worktrees/806-effective-last-admin/
  cells/accesscore/slices/setup/service.go:171。
  影响：如果升级前只剩 locked/suspended admin，升级后没有 effective admin，但 setup 仍关闭，HTTP 路径无法恢复。建议
  migration 在 admin_assignments > 0 && effective_admins = 0 时 fail-fast，fresh install 的零 admin 仍允许；setup/
  provisioner 改用 effective admin 判断。

  P1 - PG 应用层 last-admin lock CTE 未被引用，且 users UPDATE 触发器错误映射成 500
  lock_acquired CTE 在 revoke/count 查询里没有被后续查询引用：worktrees/806-effective-last-admin/cells/accesscore/
  internal/adapters/postgres/role_repo.go:102，worktrees/806-effective-last-admin/cells/accesscore/internal/adapters/
  postgres/role_repo.go:140。同时 PGUserRepo.Update 只映射唯一约束错误，未映射 ERR_AUTH_LAST_ADMIN_PROTECTED：
  worktrees/806-effective-last-admin/cells/accesscore/internal/adapters/postgres/user_repo.go:204。
  影响：并发保护可能实际只依赖 DB trigger fallback；当 status demotion 命中 trigger 时 API 返回 internal 500，而不是
  contract 里的 403。建议显式执行 advisory lock 或 CROSS JOIN lock_acquired，并在 PGUserRepo.Update 复用 delete 的 last-
  admin 错误映射。

  P2 - revoke 对 locked/suspended admin 的语义和 DB trigger 不一致
  PG/mem revoke 只检查“是否有其他 active admin”，没有先判断目标用户是否 active：worktrees/806-effective-last-admin/
  cells/accesscore/internal/adapters/postgres/role_repo.go:106，worktrees/806-effective-last-admin/cells/accesscore/
  internal/mem/role_repo.go:119。但 DB trigger 对非 active 目标允许删除 admin role：worktrees/806-effective-last-admin/
  adapters/postgres/migrations/024_effective_admin_invariant.sql:52。
  影响：service revoke locked admin 可能 403，而 raw SQL trigger/identity delete 允许，恢复和清理路径不一致。建议 revoke
  先读目标 status；目标非 active 时直接允许删除。

  P2 - contract 把 403 last-admin 写在错误的 PUT surface 上
  PUT update request schema 只有 email，不能改 status；但 contract 加了 status demotion 的 last-admin 403：
  worktrees/806-effective-last-admin/contracts/http/auth/user/update/v1/contract.yaml:28。真实 status surface 是 PATCH
  schema：worktrees/806-effective-last-admin/contracts/http/auth/user/patch/v1/request.schema.json:16，PATCH contract 却
  缺少该 403。
