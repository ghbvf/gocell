# Wave 2.5 Batch2 — 剩余开放项（精简版）

> 基线：`develop @ 34499ef5`（2026-04-28）
> **完整历史版本（D/E/F/H/J/K'/G1/G2 完工 PR 的全部子任务描述、文件域、对标参考、合并后 review 全集）已归档**：[`docs/plans/archive/202604260058-l4-virtual-taco.md`](archive/202604260058-l4-virtual-taco.md)
>
> 本文件仅保留 batch2.4 待执行项 + batch3 重构。

---

## 进度速览

| 状态 | PR | 主题 | 合并 |
|---|---|---|---|
| ✅ | PR-CFG-D | TEST-VERIFY-REALITY | #276 |
| ✅ | PR-CFG-E | GOVERNANCE-SELF-HARDEN-AND-COMPLETE（6 角色 review） | #278 |
| ✅ | PR-CFG-F | EXAMPLES-SECURITY-AND-CONSISTENCY-BASELINE | #281 |
| ✅ | PR-CFG-H | IDENTITY-RACE-AND-VALIDATE | #280 |
| ✅ | PR-CFG-J | E2E-CI-TRUE-EXECUTION | #286 |
| ✅ | PR-CFG-K' | CTXCANCEL-LOCK-AND-REDACT | #287 |
| ✅ | PR-CFG-G1 | DATAFLOW-AND-PG（含 G.9 自闭环 FU1/FU2/FU5） | #292 |
| ✅ | PR-CFG-G2 | GOVERNANCE-AND-RESIDUE | #291 |
| ⬜ | **PR-CFG-L** | **CONFIG-READ-ADMIN-GATE-AND-SCHEMA**（来自 025 CFG-4，🔴 P1 安全） | ~1d |
| ⬜ | **PR-CFG-M** | **GOVERNANCE-ARCHTEST-AND-OUTBOX-METRICS**（原 I.X2 + 025 CFG-6） | ~16h |
| ⬜ | **PR-CFG-I** | **AUTH-FAIL-CLOSED-AND-OPS-RESIDUE**（原 I.X1 + I.X3） | ~14h |
| ⬜ | **PR-A66** | BOOTSTRAP-STRUCT-DECOMPOSE | ~1d |

> **025 plan Wave 2.5 残余迁入**（2026-04-28）：CFG-4 → PR-CFG-L 独立；CFG-6 → PR-CFG-M 内 OUTBOX-EMIT-FAILOPEN-DROP-COUNTER 段落。025 plan Wave 2.5 板块已同步清零（见该文档 Wave 2.5 节）。
>
> **PR-CFG-I 拆分原因**（2026-04-28）：原打包 X1+X2+X3 累计 ~25-30h 单 reviewer 负担过重；按主题拆为「治理/archtest/可观测（M）」+「auth/ops 业务残尾（I）」两个 PR，size 控制在 14-16h，可并行 review。

---

## PR-CFG-L `CONFIG-READ-ADMIN-GATE-AND-SCHEMA`（~1d, 1 reviewer，🔴 P1 安全独占审）

**抽象**：configcore 读路径 metadata 管控收口。`GET /api/v1/config/`（list）与 `GET /api/v1/config/{key}`（get）当前挂在默认 listener auth chain 下（`auth.Authenticated()` 等价语义），任何已登录用户都能枚举 key 列表 + 读 sensitive 元数据；HTTP response schema 未声明 `sensitive: bool` 但真实 DTO 已写——contract-first 调用方误判线上响应非法。本 PR admin gate + schema 显式声明 sensitive + contract_test 负向断言三处同 PR 校齐。

**清零**：025 PR-CFG-4 全条目（🔴 P1 安全数据泄漏）

**前置**：#267 PR-CFG-C 已铺好 `runtime/auth/authtest` 基底，本 PR 直接复用。

**改动**：

- `cells/configcore/slices/configread/handler.go::RegisterRoutes` — 两条 GET 在 `auth.Mount` 处加 `Policy: auth.AnyRole(auth.RoleAdmin)`；同步 `RegisterInternalRoutes`（internal listener service-token chain 已是 admin 等价，但显式 Policy 保留 PR-A14a 一致性）
- `contracts/http/config/get/v1/response.schema.json` — 顶层 `properties.data.properties.sensitive: {"type": "boolean"}` + `required: [..., "sensitive"]`
- `contracts/http/config/list/v1/response.schema.json` — `items.properties.sensitive` 同款声明
- `cells/configcore/slices/configread/service.go` — DTO converter 必填 `sensitive`，删任何 omitempty
- `cells/configcore/slices/configread/contract_test.go` — `MustRejectResponse` 负向断言（缺 sensitive 字段的响应应 schema 校验失败 + 普通用户调用应 403）

**对标**：Vault KV v2 `data` vs `metadata` 路径分离授权；K8s RBAC ConfigMap/Secret 默认不进 `view`；Consul KV keys 枚举与 metadata/value 读取分离。

**回归断言**：
- 普通 user JWT 调 `GET /api/v1/config/` → 403 `ERR_AUTH_INSUFFICIENT_ROLE`
- admin JWT 调同端点 → 200 + `data[].sensitive` 必现
- contract_test 删响应中 `sensitive` 字段 → schema 校验失败

**不做**：不动 `/internal/v1/config/{key}` refetch 端点 auth（service-token + RoleInternalAdmin 已是更强守卫，G1 G.3 已落地）；不动 write/publish 端点 auth；不引入新 errcode。

**文件域**：`cells/configcore/slices/configread/{handler,service,contract_test}.go` + `contracts/http/config/{get,list}/v1/response.schema.json`。**不触** `cells/configcore/cell_routes.go`（auth.Mount Policy 在 readHandler 内部；现核 develop @ 34499ef5）。

---

## PR-CFG-M `GOVERNANCE-ARCHTEST-AND-OUTBOX-METRICS`（~16h, 1 reviewer）

**抽象**：治理 walker / boundary 漏项 + archtest 加固批 + outbox fail-open 可观测性补强。**主题：治理/archtest/可观测**，文件域不触业务 cell 代码。

**清零**：合并后 review C.3 第 2-5 条 + backlog `PR220-e2 GENERATED-BOUNDARY-STRATEGY-01` + G1/G2 review 新登记 `PR-CFG-G1-FU6` / `PR-CFG-G2-FU1` + 老条目 `PR238-FU2` / `FU2-GOVERNANCE-STATIC` / `PR-CFG-A-DEFER-3` + **025 PR-CFG-6**

### M.1 治理 walker / boundary（~3h）

- `kernel/governance/rules_strict_extra.go:141-155` FMT-20 — schema walker 加 `allOf` / `oneOf` / `anyOf` / `if-then-else` 路径遍历 + 回归测试
- `assemblies/corebundle/generated/boundary.yaml:12-15` + `kernel/assembly/generator.go:198-244` — role assign/revoke audience 与 InternalListener 入口对齐；REF-16 + generator fingerprint 覆盖 schema drift（**关闭 backlog `PR220-e2`**）
- **PR-CFG-G2-FU1**：`contracts/http/auth/role/{assign,revoke,list}/v1/contract.yaml::endpoints.clients` 当前 `[edge-bff]` 但 path 是 `/internal/v1/access/roles/*`，与 InternalListener service-token 拓扑不一致；修正为正确的 internal caller cell-id（`accesscore` 自身或 corebundle 控制面 actor），同步 `actors.yaml` + `boundary.yaml` REF-16 与 `audience` 字段

### M.2 Archtest 加固（~9h，全部复用 PR-A49 #290 落地的 `packages.Load + go/types` 常量评估范式）

- **PR-CFG-G1-FU6 CONTRACT-PATH-ID-MAPPING-ARCHTEST-01**（~2h）：新规则 `kernel/governance/rules_fmt.go::FMT-CONTRACT-PATH-ID-MAPPING-01` 或 `tools/archtest/contract_path_id_test.go`，扫 `contracts/{kind}/.../v1/contract.yaml`，机械验证 `path-segments-joined-by-dot == id`。FMT-08 仅校验 ID 第一段等于 kind，命名错位（如 `id: http.config.internal-get.v1` 落在 `contracts/http/config/internal/get/v1/`）现状不会拦截。
- **PR238-FU2 + FU2-GOVERNANCE-STATIC**（~2.5h）：`kernel/governance/rules_obs.go`（新）静态守卫 — 扫 `runtime/observability/metrics/` + `adapters/*/` 下任何 Prometheus counter 注册，若 label 值来自 `errcode.Category` / `IsInfraError(...)` 输出 Warning 要求 PR 附 bucket-migration 表；防止未来引入 Category-labeled counter 时 SLO 告警漂移失控。
- **PR-CFG-A-DEFER-3 HEALTH-AGG-ARCHTEST-GOPACKAGES-UPGRADE-01**（~4-5h）：`tools/archtest/health_aggregation_test.go` 从 `go/parser` 升级到 `golang.org/x/tools/go/packages` + `go/types`，覆盖嵌入字段（`type X struct { *PGResource }`）的 Checkers() 实现追踪；**复用 #290 已落地代码**降低 6h → 4-5h。
- 新建 `tools/archtest/internal/typeseval/`（~30min）— 提取 `evaluateConstString(types.Info, ast.Expr) (string, bool)` 共用 helper，本 PR 三处 archtest + M.3 outbox_topic_test 一并复用

### M.3 025 CFG-6 OUTBOX-EMIT-FAILOPEN-DROP-COUNTER（~4h）

- (a) `kernel/outbox/emitter.go:101-108` `DirectPublishFailOpen` 分支当前仅 `slog.Warn` + `return nil`，无任何 metrics counter — fail-open 静默丢消息线上无信号。**修复**：`DirectEmitter` 构造时注入 metrics provider（避免在 `kernel/` 引入全局注册表），fail-open 分支自增 `gocell_outbox_emit_failopen_dropped_total{cell, topic}`。Provider 接口在 `runtime/observability/metrics/` 暴露 `OutboxMetrics interface { ObserveFailOpenDrop(cell, topic string) }`，`kernel/outbox` 持有此接口，nil-receiver safe（demo / 测试不强制注入）
- (b) `tools/archtest/outbox_topic_test.go:217-237` `extractStringField` 当前仅检查 `*ast.BasicLit`，遇 `*ast.Ident`（常量引用）/`*ast.SelectorExpr`（包级常量）时直接 `ok=false` 跳过——常量/变量构造的 topic 漏检。**修复**：升级到 `packages.Load + go/types TypesInfo` 常量评估，调用 M.2 提取的 `typeseval.evaluateConstString` helper
- 关联：原 backlog `MULTI-REVIEW-RES-2` (1)+(2) 与本节同 commit 落地

**对标**：Watermill `metrics.NewPrometheusMetricsBuilder` outbox failure counter；Kafka producer `record-error-rate` per-topic 标签；PR-A49 #290 `packages.Load + go/types TypesInfo` 常量评估范式

**文件域**：`kernel/governance/rules_{strict_extra,fmt,obs}.go` + `kernel/assembly/generator.go` + `assemblies/corebundle/generated/boundary.yaml` + `contracts/http/auth/role/{assign,revoke,list}/v1/contract.yaml` + `actors.yaml` + `tools/archtest/{contract_path_id,health_aggregation,outbox_topic}_test.go` + `tools/archtest/internal/typeseval/` + `kernel/outbox/emitter.go` + `runtime/observability/metrics/`

---

## PR-CFG-I `AUTH-FAIL-CLOSED-AND-OPS-RESIDUE`（~14h, 1 reviewer）

**抽象**：runtime/auth fail-closed 默认 + cell 业务侧数据泄漏 / ops 残尾。**主题：auth/业务/ops**，与 PR-CFG-M 文件域完全不重叠（M = kernel + tools；I = runtime/auth + cells + tests/e2e）。

**清零**：合并后 review C.3 第 1 / 6-9 条 + G1/G2 review 新登记 `PR-CFG-G1-FU4`（FU7 已 ✅ done @ G1 #292）

### I.1 runtime/auth fail-closed by default（~4h）

- `runtime/auth/servicetoken.go:202-207` — 默认 NonceStore 从 `NoopNonceStore` 改 fail-closed；构造期校验非 Noop 或显式 dev opt-in（**P1 安全**）
- `runtime/auth/authenticator.go` — `NewServiceTokenAuthenticator` 对 nil key ring 构造期 fail-fast，与 middleware 路径不变量对齐
- `runtime/auth/servicetoken.go:304-311` + `pkg/errcode/errcode.go` — 新增 `ErrAuthNonceStoreFull`（503），从 `ErrNonceStoreFull` 映射替换 `ERR_INTERNAL`

### I.2 数据泄漏 + ops 残尾（~10h，第 1 条已升级 PR-CFG-K'.2）

- ~~`cells/configcore/internal/adapters/postgres/config_repo.go` Warn stale-cipher key_id 移除~~ — **已 ✅ K'.2** 关闭
- `cells/configcore/cell_init.go::ensureCursorCodec` — `GOCELL_ADAPTER_MODE=real` 或缺显式 dev opt-in 时禁用固定密钥兜底；启动期注入随机密钥（**P2 安全**）
- `tests/e2e/config_pilot_e2e_test.go` + `tests/e2e/healthcheck-verify.sh`（如存在）— e2e 等待改 `/readyz`；脚本失败 trap 清理
- `cells/configcore/cell_routes.go` + `cells/configcore/slices/configsubscribe/service.go` — config 自发布自消费两 topic 拆分 consumerGroup；明确链路价值（in-memory cache / version tracking），否则降级或移除
- **PR-CFG-G1-FU4 SESSIONLOGIN-CLEANUP-DUAL-CALL-01**（~30min）：`cells/accesscore/slices/sessionlogin/service.go::persistSessionWithRefresh` emit 失败路径在 durable tx 模式下产生双清理（rollback + 显式 cleanup）→ not-found 噪音日志。`cleanupIssuedSession` 内 `errors.Is(err, ErrNotFound)` 时降级 Debug；或仅在 `txRunner` 是 NoopTxRunner 时执行
- **PR-CFG-G1-FU7 E2E-COMPOSE-SECRETS-CI-INJECTION-01**：✅ done @ G1 #292（保留条目仅作历史标记）

**对标**：vault server fail-closed default；OpenTelemetry semantic conventions sensitive attribute redaction

**文件域**：`runtime/auth/{servicetoken,authenticator}.go` + `pkg/errcode/errcode.go` + `cells/configcore/{cell_init,cell_routes}.go` + `cells/configcore/slices/configsubscribe/...` + `cells/accesscore/slices/sessionlogin/service.go` + `tests/e2e/...`

---

## PR-A66 `BOOTSTRAP-STRUCT-DECOMPOSE`（**batch3 独立**, ~1d, 1 reviewer）

**抽象**：`Bootstrap` struct 36 字段、864 行单文件、10 phase 拆为 sub-struct 与 phase 分文件。架构债重构，**不与治理批次混合**。

**清零**：audit A-5

**改动**：
- 拆 `runtime/bootstrap/bootstrap.go` 为 5 sub-struct：`bootstrapAssembly` / `bootstrapHTTP` / `bootstrapHealth` / `bootstrapLifecycle` / `bootstrapMetrics`，`Bootstrap` 嵌入它们
- phase0..N 拆到独立文件 `phase0_validate.go` / `phase1_managed.go` / ...（已部分拆出，本 PR 完成）
- 所有 With* Option 函数保留 API 不变，仅内部分发到对应 sub-struct
- 测试不动（外部 API 无变化）

**对标**：fx `App` 结构化分组；k8s `apiserver/pkg/server.Config` 嵌套配置组

**编号说明**：原计划编号 PR-A41 已被 PR#270 CLI printer follow-up 占用，本 PR 改为 PR-A66（git log 最高 A52 + 026 plan 预留 A53-A65 之后，下一个安全可用编号）。

**文件域**：`runtime/bootstrap/...`

---

## 执行依赖

```
G1 + G2 已合（develop @ 34499ef5）
   ↓
batch2.4（四路并行可行，文件域全不重叠）：
  ├─ PR-CFG-L  CONFIG-READ-ADMIN-GATE-AND-SCHEMA          (~1d,   1 reviewer) 🔴 P1 安全独占审
  │     文件域：cells/configcore/slices/configread/ + contracts/http/config/{get,list}
  │     建议先合（最短链 + 安全优先）
  ├─ PR-CFG-M  GOVERNANCE-ARCHTEST-AND-OUTBOX-METRICS     (~16h,  1 reviewer)
  │     文件域：kernel/governance + kernel/outbox + tools/archtest + runtime/observability/metrics
  │     吸收：G1-FU6 / G2-FU1 / PR238-FU2 / FU2-static / DEFER-3 / 025 CFG-6
  ├─ PR-CFG-I  AUTH-FAIL-CLOSED-AND-OPS-RESIDUE           (~14h,  1 reviewer)
  │     文件域：runtime/auth + pkg/errcode + cells/{configcore,accesscore} + tests/e2e
  │     吸收：G1-FU4
  └─ PR-A66    BOOTSTRAP-STRUCT-DECOMPOSE                  (~1d,   1 reviewer)
        文件域：runtime/bootstrap/
        编号需重选（建议 PR-A54）
        ↓ 全部 merge 后
batch2 终点 retrospective（6 角色并行，D+E+F+G1+G2+H+I+J+K'+L+M 累计 diff）
```

### 文件交集核对（develop @ 34499ef5）

| PR | kernel/ | runtime/ | cells/ | tools/ | tests/e2e | contracts/ |
|---|---|---|---|---|---|---|
| L | — | — | configcore/slices/configread/ | — | — | http/config/{get,list} |
| M | governance + outbox + assembly | observability/metrics | — | archtest + archtest/internal/typeseval | — | http/auth/role + actors.yaml |
| I | — | auth + (pkg/errcode) | configcore/{cell_init,cell_routes,slices/configsubscribe} + accesscore/slices/sessionlogin | — | tests/e2e/* | — |
| A66 | — | bootstrap/ | — | — | — | — |

**冲突点**：无文件级冲突。L vs I 在 cells/configcore/ 下不同子目录（configread vs cell_init/cell_routes/configsubscribe），可真四路并行。

---

## 6 角色 review 投放（剩余）

- **PR-CFG-L, M, I, A66（各 1 reviewer）**：六维度全覆盖单 reviewer
  - L 必须包含 security 维度（🔴 P1 安全）
  - M 必须包含 architect + observability 维度（治理 + metrics）
- **batch2 终点 retrospective**：6 角色并行（D+E+F+G1+G2+H+I+J+K'+L+M 累计 diff）

剩余调用：4 PR × 1 reviewer + retrospective 6 角色 = **10 调用**。

---

## 验证矩阵（每 PR 必跑）

```bash
go build ./... && go test ./...
golangci-lint run ./...                                            # 0 issues
go run ./cmd/gocell validate --strict                              # 0 errors
go test ./tools/archtest/...

# PR-CFG-L
go test ./cells/configcore/slices/configread/...                   # contract_test 负向断言
# 普通 user JWT → 403；admin JWT → 200 + sensitive 必现

# PR-CFG-M
go test ./kernel/governance/... ./kernel/outbox/... ./tools/archtest/...
# outbox_topic_test 覆盖 *ast.Ident / *ast.SelectorExpr 包级常量
# /metrics 端点出 gocell_outbox_emit_failopen_dropped_total{cell, topic}

# PR-CFG-I
go test -tags=integration ./runtime/auth/... ./cells/configcore/... ./cells/accesscore/...
# real 模式 NonceStore=Noop → fail-fast；nil key ring → 构造期 fail
docker compose -f tests/e2e/docker-compose.e2e.yaml up -d \
  && go test -tags=e2e ./tests/e2e/... \
  && docker compose -f tests/e2e/docker-compose.e2e.yaml down -v

# PR-A66
go build ./runtime/bootstrap/... && go test ./runtime/bootstrap/...
# 外部 With* API 签名 0 改动
```

---

## 保留 backlog（仅 2 条，真 YAGNI 理由）

| Finding | 保留理由 |
|---|---|
| audit KG-F7 (LAYER-05 L0 Cell 例外) | 当前零 L0 Cell（cells/ + examples/ 全仓 `consistencyLevel: L0` = 0 命中），宪法允许但无活体违规；首个 L0 Cell 入场时再实施 archtest 例外 |
| audit RA-F5 (configreceive 仅 log) | 已登记 `PR266-METADATA-ONLY-CONSUMER-BUSINESS`；属业务接入（JWT TTL refresh / key rotation）超 audit 范畴；G1 G.3 已建立 refetch 闭环框架，业务 reload 由后续业务 PR 实施 |

---

## ExitPlanMode 后执行（batch2.4，2026-04-28 起）

1. `git fetch && git pull --rebase`（已 @ 34499ef5；G1 #292 / G2 #291 已合）
2. **四路并行**起 PR-CFG-L + PR-CFG-M + PR-CFG-I + PR-A66 worktree（文件域全不重叠，已逐一核对）
3. 顺序约束：
   - L 建议**先合**（🔴 安全 + 最短链 ~1d）
   - M / I / A66 文件域全互不相交，可真并行
4. 每 worktree push 前 `golangci-lint 0 issues` + 对应 scope 真跑
5. CI 状态用 `gh pr checks <num>` 单次查询，不循环
6. Cx1/Cx2 IN_SCOPE findings 派 developer `/fix`
7. 4 PR 全 merge 后启 batch2 终点 retrospective 6 角色（累计 diff）
8. 完成后人工确认：
   - backlog 仅剩 2 条 YAGNI 项（KG-F7 / RA-F5）
   - 025 plan Wave 2.5 板块清零（CFG-1 ✅ / CFG-4 → L ✅ / CFG-5 won't-do / CFG-6 → M ✅）
   - 025 plan 此后只剩 Wave 3（A15/A16/A17/A36/A37/A38）+ Wave 4（A21/A22/A24/A33）新模块路线图
