# Wave 2.5 Batch2 — 彻底版

## 进度（2026-04-26）

- ✅ **PR-CFG-D** TEST-VERIFY-REALITY — PR #276 merged 2026-04-25
- ✅ **PR-CFG-E** GOVERNANCE-SELF-HARDEN-AND-COMPLETE — PR #278 merged 2026-04-25
- ✅ **PR-CFG-F** EXAMPLES-SECURITY-AND-CONSISTENCY-BASELINE — PR #281 merged 2026-04-25
- ✅ **PR-CFG-H** IDENTITY-RACE-AND-VALIDATE — PR #280 merged 2026-04-25
- ✅ **PR-CFG-J** E2E-CI-TRUE-EXECUTION — PR #286 merged 2026-04-26
- ✅ **PR-CFG-K'** CTXCANCEL-LOCK-AND-REDACT — PR #287 merged 2026-04-26
- ⬜ **PR-CFG-G1** DATAFLOW-AND-PG — 立即可启动（batch2.3 与 G2 并行，~14h；G.1 写路径事件 + G.2 actor 注入 + G.3 refetch 闭环 + G.6 camelCase + G.8 真 PG 装配）
- ⬜ **PR-CFG-G2** GOVERNANCE-AND-RESIDUE — 立即可启动（batch2.3 与 G1 并行，~6.5h；G.4 contract consistencyLevel + governance 规则 + G.5 UX + G.7 D/E 残余清扫）
- ⬜ **PR-CFG-I** BACKLOG-CFG-DRAIN — 待启动（batch2.4 G 后并行，~13h；X1 auth fail-closed + X2 治理 walker/boundary + X3 数据泄漏/ops；I.X3 第 1 条 key_id redact 已升级 K'）
- ⬜ **PR-A41** BOOTSTRAP-STRUCT-DECOMPOSE — 待启动（batch3 独立，G 后并行；编号需重选，PR#270 已用 PR-A41）

## Context

develop @ 43b2c1ec 之上累计 3 组工作：plan 文档 batch2 (PR-CFG-D)、5 路 audit ~40 条 finding、PR#269 已合（消 architect F2/F3/F4/F8 四条）。

本计划组织成 **5 PR (batch2) + 1 micro-batch + 1 batch3 重构**，主题单一、文件交集 ∅、不向后兼容、不留软性补丁。原 9 条 backlog 项中 7 条已纳入或彻底处理，仅 2 条保留 backlog（真 YAGNI 理由）。

---

## batch0 — 前置（10 min）

```bash
git fetch origin && git pull --rebase     # 整合 PR#269
```

---

## PR-CFG-D `TEST-VERIFY-REALITY`（~1d, 1 reviewer） ✅ DONE — PR #276 (2026-04-25)

**抽象**：测试必须真跑或显式条件 skip——新建 `tools/nogo/unconditionalskip` analyzer + 删除所有 stub 占位测试。

**清零**：plan-D 全范围 + audit T-1 + audit O-1 + backlog F10（28 处 stub 全删）

**改动**：
- 新建 `tools/nogo/unconditionalskip/{analyzer,analyzer_test}.go` — go/analysis Analyzer，**唯一规则**：测试函数体首句 `t.Skip` 且无前置 if 一律 fail，无豁免
- 新建 `tools/nogo/unconditionalskip/cmd/gocell-vet/main.go`
- `cmd/gocell/app/check.go` — `unconditional-skip` 子命令实现（替换 1 个 placeholder；其余 4 个见 PR-CFG-E）
- 新建 `tests/e2e/internal/require/{docker,pg,rmq}.go`（参考 `tests/testutil/docker.go::RequireDocker` 扩展）
- `tests/e2e/config_pilot_e2e_test.go` — 3 处 `t.Skip` 改 `require.Docker/PG/RMQ(t)`
- **删** `tests/integration/assembly_test.go` 整个文件（3 处 stub，0 真实断言）
- **删** `tests/integration/journey_test.go` 整个文件（26 处 stub，0 真实断言）
- backlog F10 同步关闭（journey_test 28 处 stub 已删，full-assembly harness 后重新加测试）
- `runtime/bootstrap/managed_resource_test.go` — 加 3 个反向断言 case：nil-worker skip / 重名 checker phase0 fail / Close 失败 teardown 链继续（已存在文件，扩展而非新建）
- `kernel/outbox/emitter.go::DirectEmitter.Emit` — 加 `entry = entry.InjectObservabilityFromContext(ctx)`（参考 `adapters/postgres/outbox_writer.go:60-61`）
- `kernel/outbox/emitter_test.go` — 加 `TestDirectEmitter_InjectsObservability`
- `.github/workflows/_build-lint.yml` — integration-test job scope 加 `./tests/e2e/...` + 新 e2e job (`-tags=e2e`)

**对标**：`golang.org/x/tools/go/analysis/passes/nilness` Analyzer 模板；`gotest.tools/v3/skip` API 风格

---

## PR-CFG-E `GOVERNANCE-SELF-HARDEN-AND-COMPLETE`（~2.5d, **6 角色 review**） ✅ DONE — PR #278 (2026-04-25)

**抽象**：治理基线本身的可靠性 + HTTP schema 严格化 + 治理能力补全（4 placeholder check 实现）+ 接口冗余清理。

**清零**：audit P0-1 / P0-2 / G-1 / G-2 / G-3 / KG-F6 / KG-F8 / KG-F9 / KG-F10 / **A-1**（4 placeholder check）/ **A-6**（Delegated 字段冗余）

### E.1 治理 self-harden（~1d）

- `docs/architecture/naming-guard.yaml` — exclude 直接列 `docs/bak/**` + `docs/backlog.md`（不做反引号边界 patch-on-patch）
- `tools/archtest/{archtest,auth_authtest_boundary,health_aggregation}_test.go` + `kernel/governance/rules_wrapper.go` — 4 处 `filepath.WalkDir` 入口加 `case "vendor", "worktrees", "testdata":` SkipDir
- `tools/archtest/archtest_test.go::loadPackages` — 改用 `golang.org/x/tools/go/packages` `Config.BuildFlags=["-tags=integration"]` 含全部 build-tag 文件
- `kernel/governance/helpers.go` — 5 个 `xxxFile()` 函数签名改接受 `*XxxMeta`，直接返 `m.File`，删原 ID-重构回退路径

### E.2 HTTP schema 严格化（~0.5d）

- `contracts/http/config/flags/{get,evaluate,list}/v1/response.schema.json` + `contracts/http/auth/user/{lock,get,unlock}/v1/request.schema.json` — 6 schema 顶层 + items 加 `additionalProperties: false`
- 新规则 `kernel/governance/rules_strict.go::FMT-RESPONSE-STRICT-01` — 扫所有 HTTP kind contract 的 schema，顶层缺 `additionalProperties:false` 即 fail

### E.3 治理能力补全：4 placeholder check 实现（~1d）

- `cmd/gocell/app/check.go::checkSliceCoverage` — 扫 `cells/<id>/cell.yaml::ownedSlices` 与 `cells/<id>/slices/*/slice.yaml` 一致性，缺失/孤儿 slice 报 fail
- `cmd/gocell/app/check.go::checkAssemblyCompleteness` — 扫 `assembly.yaml::cells` 引用 vs `cells/<id>/cell.yaml` 存在性 + `journeys/<id>` 引用对齐
- `cmd/gocell/app/check.go::checkJourneyReadiness` — `journeys/<id>.yaml` 与 `journeys/status-board.yaml` 状态一致性 + 引用 contract 存在
- `cmd/gocell/app/check.go::checkL0Imports` — `cells/<id>/cell.yaml::consistencyLevel == L0` 时 `l0Dependencies` 必填 + 实际 import 与声明一致（go/packages 静态扫描）
- 复用 `kernel/governance/helpers.go` + `kernel/registry.NewContractRegistry` 已有基础设施
- `cmd/gocell/app/dispatch.go` — Usage 完整列出 5 个 check 子命令

### E.4 接口冗余清理 + 治理元数据完整（~30min）

- **删** `kernel/cell/registrar.go::AuthRouteMeta.Delegated` 字段（注释自承"declarative marker only"）；`Delegated` 由 path 前缀 `/internal/v1/` 自动派生
- 跑 `gocell generate assembly --id=corebundle` 一次，提交生成的 `assemblies/corebundle/generated/boundary.yaml`（解决 KG-F6）
- 新规则 `FMT-CONTRACT-DIR-ID-MATCH-01`（KG-F9）— `c.Dir` 与 `c.ID` 推导路径一致性 ASSERT
- 新规则 `STATUSBOARD-STATE-ENUM-01`（KG-F8）— `StatusBoardEntry.State` 枚举为 `{todo, doing, done}`
- 新规则 `CONTRACT-DEPRECATED-CLEANUP-01`（KG-F10）— `lifecycle: deprecated` 资产标注 `deprecatedAt`，超 90d 报 warn

**对标**：`golang.org/x/tools/go/packages` BuildTags；k8s admission policy 严格性

---

## PR-CFG-F `EXAMPLES-SECURITY-AND-CONSISTENCY-BASELINE`（~1.5d, 1 reviewer） ✅ DONE — PR #281 (2026-04-25)

**详细计划**：`docs/plans/202604260156-pr-cfg-f-examples-security-consistency-baseline.md`

**抽象**：examples 必须用 production-quality auth；删除固定 bearer token 和 unauth internal listener；real-mode multi-pod 同时使用 Redis-backed service-token nonce store 与 Redis-backed outbox claimer；service-token timestamp 语义修正为过去按 `ServiceTokenMaxAge` 过期、未来只允许 `ServiceTokenClockSkew`。

**清零**：audit P0-3 / P0-4 / S-1 / S-2 / **A-3**（Claimer multi-pod）/ **AR-F7**（assemblyID doc）/ RB-F4 / RB-F5 / RB-F8 / RB-F9 / RB-F10 / RB-F12

### F.1 service-token 协议修正（~1h）

- `runtime/auth` 新增 `ServiceTokenClockSkew = 30 * time.Second` 和 `ServiceTokenNonceTTL = ServiceTokenMaxAge + ServiceTokenClockSkew`
- timestamp 校验删除 `abs(age)`：未来 token 只接受 30s 内 skew，过去 token 仍按 `ServiceTokenMaxAge` 过期
- 所有 `auth.NewInMemoryNonceStore(...)` service-token 路径统一传 `auth.ServiceTokenNonceTTL`

### F.2 Redis distributed nonce + corebundle wiring（~3h）

- `adapters/redis` 新增 `NonceStore` 实现 `auth.NonceStore`：`SET key value NX EX ttl`；重复返回 `auth.ErrNonceReused`；Redis 错误按 `ERR_ADAPTER_REDIS_*` 包装；`Kind()==distributed`
- `cmd/corebundle` 新增 Redis env 装配：`GOCELL_REDIS_ADDR`、`GOCELL_REDIS_PASSWORD`、`GOCELL_REDIS_DB`
- real + multi-pod 必须配置 Redis；single-pod 可继续使用 in-memory
- 同一个 Redis client 复用于 service-token nonce store 和 outbox `redis.IdempotencyClaimer`
- `buildConsumerBase(deps *SharedDeps)` 替换旧无参签名
- `SharedDeps.Validate` 同时校验 distributed nonce 与 distributed claimer；新增 `ERR_CONTROLPLANE_CLAIMER_NOT_DISTRIBUTED`

### F.3 examples auth（~3h）

- `examples/{ssobff,iotdevice,todoorder}/main.go` InternalListener 全部启用 `cell.NewAuthServiceToken`
- 缺 `GOCELL_SSOBFF_SERVICE_SECRET` / `GOCELL_IOTDEVICE_SERVICE_SECRET` / `GOCELL_TODOORDER_SERVICE_SECRET` 直接 fail-fast
- 不新增 `examples/internal/exampleauth`；各 `main.go` 直接调用 `auth.NewHMACKeyRing`、`auth.NewInMemoryNonceStore`、`cell.NewAuthServiceToken`
- `iotdevice`、`todoorder` 删除 `demoAdminToken` / `demoCustomerToken` / `demoTokenVerifier`，改用现有 RS256 JWT verifier
- README 改为生成真实 RS256 测试 token 的命令或 example-local 小工具，不再记录固定 bearer token

### F.4 运维细节（~3h）

- `runtime/auth/middleware.go:83-93` — slog 改全 typed (`slog.Any/String`)，无裸字符串 key
- `runtime/http/health/health.go:328-329` — `adapters` 在 RLock 内 shallow copy
- `runtime/http/health/health.go:585-590::truncateErrMsg` — 按 rune 截断（`[]rune(msg)[:max]`）
- `adapters/postgres/pool.go:185::Pool.Stats()` — 加 nil receiver / nil inner 防御
- `runtime/bootstrap/bootstrap_phases.go` — pub/sub identity 抽 helper，避免不可比较动态类型 panic
- `runtime/bootstrap/managed_resource.go` — teardown 错误带资源类型，复用现有 `td.name`
- `runtime/bootstrap/bootstrap.go:60-73` — `WithAssemblyID` 注释收窄到 HTTP metrics collector 的 `cell_id` label，不改 runtime 行为

**不做**：不加 `examples/internal/exampleauth`；不加 HS256 JWT verifier；不把 corebundle demo-key/topology 策略下沉到 runtime；不新增 Redis username/sentinel 配置面。

---

## PR-CFG-J `E2E-CI-TRUE-EXECUTION`（前置 G，~1d, 1 reviewer） ✅ DONE — PR #286 (2026-04-26)

**抽象**：CI 集成 e2e 真执行 gate — 配齐依赖装配 + executed/skipped 解析门禁，杜绝"全 skip 仍绿"。原 backlog `PR-CFG-D-FU` E2E-CI-TRUE-EXECUTION-01 升级前置 plan 项。

**清零**：合并后 review C.4；G.3 e2e refetch 闭环验收前置依赖

**实际改动**（PR #286，commits `0f5aeb75 → 67aeb5ec`）：
- 新建 `tests/e2e/docker-compose.e2e.yaml` — **单 corebundle 服务，demo + in-memory 模式**（见下"PR-CFG-J 范围收敛"）
- 新建 `tests/e2e/Dockerfile.corebundle` — multi-stage build；JWT key 在 image build 期生成；USER gocell 非 root；repo 根 `.dockerignore` 收敛 build context
- 新建 `tests/e2e/smoke_e2e_test.go` — 2 个无条件 e2e 测试（`/readyz` 200 + primary listener 404 internal path）维持 e2egate gate 信号
- 新建 `tools/e2egate/{parser,parser_test}.go` + `cmd/e2egate/main.go` — `go test -json` 解析，executed=0 / 任何包"declared 测试但全 skip" / build-fail / 全包 no-test-files 即 fail
- `.github/workflows/_build-lint.yml` 新增独立 e2e job（不动 integration-test）— `docker compose up --wait` + `go test -tags=e2e -json | tee | e2egate` + 失败时上传 `e2e.json + compose.log` artifact
- `tests/e2e/config_pilot_e2e_test.go` 文档注释对齐（Vault → Redis → demo+memory），`waitForReady` 改 `e2eHealthURL()+/readyz`（9091）

**PR-CFG-J 范围收敛（vs 计划字面）**：
原 plan 字面"PG + Vault + corebundle 三服务" 实施期发现 corebundle 在 `StorageBackend=postgres → DurabilityDurable` 触发 accesscore `ERR_CELL_MISSING_OUTBOX`（`access_module.go:99` 给 outboxWriter=nil），corebundle 启动失败 / `/readyz` 永不通过。**accesscore 真 outbox wiring 跨 cmd/corebundle 装配 + SharedDeps + archtest，超出 PR-CFG-J "建 e2e 闸门" 的核心边界，已转入 G.8**。本 PR 通过切 demo+memory 路径让 e2egate gate 真起作用（2 个 smoke 测试 + 3 个加密测试 require.PG skip）。

**未处理项（已转入 PR-CFG-G G.8）**：
- accesscore 真 outbox wiring（共享 configcore PG pool 或独立装配）
- e2e 真 PG harness（postgres + redis + migrate + corebundle，恢复 `tools/pg-migrate/` + `tests/e2e/scripts/bootstrap-admin.sh`）
- 3 个 `TestE2E_ConfigEncryption_*` unskip（require.PG=1 切换即生效）

**依赖**：先于 PR-CFG-G 合入。G.3 的 `cmd/corebundle/outbox_e2e_integration_test.go` 验收依赖本 PR 的 e2egate parser；G.8 才负责 e2e 真 PG harness 完整启用。

**对标**：Kubernetes test/e2e/framework/skipper；Kratos examples docker-compose；testcontainers-go examples/postgres；cmd/internal/test2json event schema

---

## PR-CFG-K' `CTXCANCEL-LOCK-AND-REDACT`（与 J 并行，~3h, 1 reviewer） ✅ DONE — PR #287 (2026-04-26)

**抽象**：PR#271 把 ctx-cancel 收口到 `pkg/ctxcancel`、双语义分流（`Canceled→499+Warn` / `DeadlineExceeded→504+Error`）、stale-cipher op PascalCase、decrypt Category=Auth 全部落地。本 PR 收尾 3 件 PR#271 未做的：configcore PG repo 本地 thin wrapper 残留、stale-cipher Warn 中 key_id 字段泄漏、archtest 双向锁未来回退。**不向后兼容**：直接删除残留代码与日志字段，不留过渡层。

**清零**：合并后 review C.3 第 8 条 LOG-KEY-ID-REDACT；PR#271 的"已落地实现"未来回退保护

### K'.1 删 configcore 本地 thin wrapper（~30min）

`audit_repo.go` 已直接调 `ctxcancel.Wrap` 是标准答案；configcore 两个 repo 还套了一层 thin method：

- `cells/configcore/internal/adapters/postgres/config_repo.go:23-46` — 整段删除 `wrapCtxCancel` method 与文档块；约 13 处 callsite（line 303 / 365 / 477 / 632 / 648 / 659 / 700 / 729 等）改为 `ctxcancel.Wrap(err, op, "key="+key)` 等直接调用
- `cells/configcore/internal/adapters/postgres/flag_repo.go:65-83` — 同款删除；约 5 处 callsite 同改

### K'.2 stale-cipher Warn 移除 key_id（~30min）

- `config_repo.go:432-444::observeStaleCipher` — `slog.Warn` 字段从 3 收成 1：保留 `key`（业务键名公开），删 `stored_key_id` + `current_key_id`
- `onStaleCipher` 回调签名 `func(key, storedKeyID, currentKeyID string)` 不动 — 指标维度合法（不进日志面，进 Prometheus label cardinality 控制）
- `config_repo_encrypt_test.go::M3 Stale key observability` 收紧断言到只查 `key` 字段

### K'.3 archtest 双向锁（~2h）

新建 `tools/archtest/repoerr_test.go`：

- **CTXCANCEL-LOCAL-IMPL-BAN-01** — 扫 `cells/*/internal/adapters/**/*.go`：
  - 本地 `type ctxCanceledError struct` / `type ctxCancelError struct` 等同义类型 → fail
  - 本地 `func wrapCtxCancel(...)` / `func wrapCtxCanceled(...)` thin method → fail
  - `errors.Is(err, context.Canceled)` 后构造 `*errcode.Error` 而不调 `ctxcancel.Wrap` → fail
- **REPO-LOG-KEY-ID-REDACT-01** — 扫 `cells/*/**/*.go` 中 `slog.{Warn,Error,Info}*` 调用，attrs 字面量含 `key_id` / `keyID` / `key-id` / `stored_key_id` / `current_key_id` → fail
- 测试覆盖：fixture 文件演证 ban / 演证 redact 双向

**不向后兼容**：
- 旧 `wrapCtxCancel` method 直接消失，无 deprecation
- 日志面 `stored_key_id` / `current_key_id` 字段直接消失；外部 dashboard / 告警规则需自行更新

**不做**（PR#271 已彻底）：
- 不抽 `pkg/repoerr/` 二级层（pkg/ctxcancel 一层够）
- 不改 OTel span attribute（4xx Unset 是 OTel HTTP semconv 规范要求，PR#271 正确）
- 不补 accesscore PG repo ctx-cancel（PG repo 不存在，X1 PG-DOMAIN-REPO 触发再做）

**对标**：archtest pattern 参考 `tools/archtest/outbox_topic_test.go`（AST 同包常量传播）+ `tools/archtest/auth_authtest_boundary_test.go`（cells 路径扫描）

---

## PR-CFG-G1 `DATAFLOW-AND-PG`（~14h ≈ 1.75d, 1 reviewer, 与 G2 并行） ⬜ TODO — 立即可启动

**抽象**：accesscore/configcore/auditcore 数据流端到端打通——identity 写路径事件全覆盖 + 所有事件 payload 必带 actor + Role 常量单一来源 + auditappend 严格 unmarshal + metadata-only refetch 闭环 + **e2e 真 PG 模式启用**（accesscore 真 outbox wiring + 3 个加密测试 unskip）+ camelCase 收尾。

**清零**：audit S-5 / DF-F2 / DF-F5 / DF-F6 / DF-F7 / DF-F8 / **DF-F3**（refetch design gap）/ RA-F3 / RA-F6 / RA-F7 / **PR-CFG-J-FU H+I**（accesscore 真 PG outbox + 3 个 ConfigEncryption e2e 测试 unskip）+ backlog PR250-F2

**子任务集**：G.1 + G.2 + G.3 + G.6 + G.8（硬约束：G.3 refetch 闭环验收依赖 G.8 真 PG harness，必须同 PR；G.6 camelCase 与 G.1 共用 `cells/accesscore/internal/dto/user_events.go`，避免合并冲突一并处理）

### G.1 identity 事件全覆盖（~3h）

- `contracts/event/user/{deleted,updated,unlocked}/v1/{contract,payload.schema,headers.schema}.yaml/json` — payload 必含 `userId, actorId`
- `cells/accesscore/internal/dto/user_events.go` — 新增 `UserDeletedEvent / UserUpdatedEvent / UserUnlockedEvent / UserLockedEvent`（替 `map[string]any`）
- `cells/accesscore/slices/identitymanage/service.go` — 5 个写路径全 emit typed event（Update/Patch/Delete/Lock typed/Unlock）
- `cells/accesscore/slices/sessionlogin/service.go::IssueForUser` — 删 `emitCreated=false` 分支，所有新 session 必发 `event.session.created.v1`

### G.2 Config 事件 actor + auditquery Role 常量（~2h）

- `cells/configcore/internal/events/config_events.go` — `EntryUpserted / EntryDeleted / ConfigRollback / VersionPublished` 4 event 加 `ActorID string`
- `contracts/event/config/{entry-upserted,entry-deleted,rollback,version-published}/v1/payload.schema.json` — 加 `actorId` required
- `cells/configcore/slices/{configwrite,configrollback,configpublish}/service.go` — emit 取 `auth.FromContext(ctx).Subject` 注入 actorId
- `cells/auditcore/slices/auditquery/handler.go:74` — 字面量 `"admin"` → `dto.RoleAdmin` 常量
- `cells/auditcore/slices/auditappend/service.go` — 直接换 strict `json.NewDecoder + DisallowUnknownFields`，删 lenient 路径；UserIDSnake fallback 删除

### G.3 metadata-only refetch 闭环（~3h，audit DF-F3，验收依赖 G.8）

- `cells/configcore/cell.go::RouteGroups()` — 把 `GET /api/v1/config/{key}` 同款挂到 `cell.InternalListener`，路径 `/internal/v1/config/{key}`，policy 由 `cell.NewAuthServiceToken` 提供（与 internal listener auth chain 一致）
- `cells/accesscore/internal/ports/configport.go`（新）— 定义 `ConfigClient` 接口（`GetEntry(ctx, key) (Entry, error)`）
- `cells/accesscore/slices/configreceive/service.go` — 注入 `ConfigClient`，`HandleEntryUpserted` 内回源 GET 拿到当前值，触发 reload 业务（首批仅日志记录闭环成功，真业务 reload 在后续业务 PR）
- `cells/accesscore/internal/adapters/http/configclient.go`（新）— HTTP 实现，使用 service-token 调用
- 同步契约 `contracts/http/config/get/v1/contract.yaml` `clients: [accesscore]` + 新增 internal version `contracts/internal/config/get/v1/...`
- e2e 集成测试 `cmd/corebundle/outbox_e2e_integration_test.go` 加断言：触发 entry-upserted → accesscore.configreceive 确实回源调用 GET

### G.6 snake_case → camelCase 收尾（~2h，吸收 backlog PR250-F2）

- `event.user.{created,locked}.v1` payload schema + DTO 改 camelCase（与 G.1 共用 `cells/accesscore/internal/dto/user_events.go`，必须同 PR）
- `cells/auditcore` actor 提取删除 snake_case fallback

### G.8 PR-CFG-J 残尾：accesscore 真 outbox + e2e 真 PG 模式启用（~4h，PR#286 转入）

PR-CFG-J 实施期发现 corebundle 在 `GOCELL_CELL_ADAPTER_MODE=postgres` 触发的 `DurabilityDurable` 模式下 accesscore cell 启动失败：`access_module.go:99` 的 `WithOutboxDeps(eb, nil)` 给 outboxWriter=nil，`kernel/cell.ResolveEmitter` 在 durable 路径上要求 `OutboxWriter+TxRunner` 都非 nil，触发 `ERR_CELL_MISSING_OUTBOX`。PR-CFG-J 已通过切 demo+memory + 2 个无条件 smoke 测试维持 e2egate gate 信号；本节负责把真 PG 模式打开。

- **G.8.1 accesscore 真 outbox wiring**：`cmd/corebundle/access_module.go::AccessCoreModule.Provide` 在 `shared.Topology.StorageBackend == "postgres"` 路径上：
  - 复用 configcore 的 PG pool（最干净，避免双 PG 装配）— 通过 `SharedDeps` 暴露 `ConfigCorePool *adapterpg.Pool` 字段，`ConfigCoreModule` 写入、`AccessCoreModule` 读取
  - 用 `adapterpg.NewOutboxWriter()` + `adapterpg.NewTxManager(pool)` 装配 `accesscore.WithOutboxDeps(shared.EventBus, writer)` + `accesscore.WithTxRunner(txMgr)`
  - 同步加 archtest：`tools/archtest` 验证 accesscore PG 装配只在 `cell_adapter_mode=postgres` 路径上启用，避免 in-memory 路径误连 PG
- **G.8.2 e2e 真 PG harness 重新启用**：
  - `tests/e2e/docker-compose.e2e.yaml` — 4 服务回归（postgres + redis + migrate + corebundle），改 `GOCELL_ADAPTER_MODE=real` + `GOCELL_CELL_ADAPTER_MODE=postgres` + 完整生产 secrets（service-secret / metrics-token / readyz-verbose-token）
  - 新建 `tools/pg-migrate/main.go`（PR-CFG-J 已设计、为收敛范围删除，这里恢复 + 集成 archtest 已 migration 引用）
  - `tests/e2e/scripts/bootstrap-admin.sh` 恢复（PR-CFG-J 已设计 + bounded retry + jq -n）
  - workflow `.github/workflows/_build-lint.yml::e2e` job：`GOCELL_E2E_PG_AVAILABLE=1`，分两阶段拉服务（先 postgres+redis+migrate `--wait`，再 corebundle `--wait`），加 `Bootstrap admin` step 把 token 写入 `$GITHUB_ENV`
- **G.8.3 加密测试 unskip**：`tests/e2e/config_pilot_e2e_test.go` 3 个 `TestE2E_ConfigEncryption_*` 保留 `e2erequire.PG(t)`（env 切换即生效），不动测试代码

**Why this lives in G1**：accesscore 真 outbox wiring 触及 `cmd/corebundle/access_module.go`、`SharedDeps`、archtest，与 G.1/G.3 的 accesscore 改动同包同 PR；e2e 真 PG harness 是 G.3 metadata-only refetch 闭环验收的硬前置（refetch 测试调 internal `/internal/v1/config/{key}` 必须真 corebundle 跑起来 + 真 PG 写入 + accesscore consumer 真消费）。拆分到独立 PR 会让 G.3 没法验收。

**对标**：k8s audit-policy v1 actor 字段；vault audit/file backend；contract-first refetch 模式；fx Module fx.Provide cross-module pool 共享模式（accesscore 共享 configcore PG pool）

---

## PR-CFG-G2 `GOVERNANCE-AND-RESIDUE`（~6.5h, 1 reviewer, 与 G1 并行） ⬜ TODO — 立即可启动

**抽象**：HTTP contract consistencyLevel 一致性收尾 + governance 规则补全 + UX/产品微调 + D/E 合并后残余清扫。纯 contract / governance / doc 层，不触 service.go 业务逻辑，与 G1 文件域无交集。

**清零**：合并后 review C.1（5 条 D/E 残余清扫）+ C.2（2 条 G.4 漏项）

**子任务集**：G.4 + G.5 + G.7

### G.4 contract.yaml consistencyLevel 修正 + governance 规则 + 漏项补齐（~2.5h）

- 6 个 HTTP contract 的 `consistencyLevel` L1→L2：`auth/user/{create,lock}` + `auth/session/{login,delete}` + `auth/role/{assign,revoke}`
- 新规则 `kernel/governance/rules_consistency.go::CONTRACT-CONSISTENCY-EMIT-01` — HTTP contract 在 service 触发 `outbox.Emit` 时，contract.consistencyLevel ≥ L2；通过 contract.yaml 新字段 `triggers: [event.x.v1]` 声明 + 静态扫描 service.go 调用点验证
- **CONFIG-UPDATE-SENSITIVE-CONTRACT-IMPL**（合并后 review C.2-1）：`contracts/http/config/update/v1/request.schema.json` 当前声明 `sensitive` 字段，但 `cells/configcore/slices/configwrite/{handler,service}.go` + `config_repo.go` 全链路不接收 — contract-impl 断流。**决策**：从 schema 删 `sensitive`（与现实现对齐）；后续如需支持改 sensitive 单独走 `PATCH /config/{key}/sensitive` 端点
- **FMT-13-HTTP-ENDPOINTS-REQUIRED**（合并后 review C.2-2）：`kernel/governance/rules_fmt.go:392-398` HTTP contract 缺 `endpoints.http` 当前直接放过 — 改为 fail，与 contract-health 显示 "-" 的实际语义对齐

### G.5 UX/产品（~1h）

- `cells/configcore/slices/featureflag/handler.go` — `FeatureFlagResponse` 加 description / version / createdAt / updatedAt
- `contracts/http/config/flags/list/v1/response.schema.json` — 同步加字段
- `cells/configcore/slices/configpublish/handler.go:93` — `http.StatusOK` → `http.StatusCreated`

### G.7 D/E 合并后残余清扫（~3h，合并后 review C.1）

- `README.md`（顶层 quickstart）— 同步到现行 API；按"go run ./examples/todoorder"操作会因 #281 fail-fast 启动失败，需要补 service secret / JWT env 与 token mint 流程
- `examples/iotdevice/README.md:107` — 命令响应示例补 `commandType`，enqueue 示例补 `attempt`，与 handler/schema required 对齐
- `docs/guides/integration-testing.md:56-60` — 删除"无条件 t.Skip stub"建议，改写为"必须条件 skip 或反向断言"，与 PR-CFG-D unconditional-skip gate 对齐
- `cmd/gocell/app/check.go::checkSliceCoverage`（~210-218）— `--cell=<id>` 在 `project.Cells` 中找不到时 fail（unknown cell 假绿）
- `cmd/gocell/app/check.go::runUnconditionalSkip`（~808-812）— `packages.Load` 加 `BuildFlags=["-tags=integration e2e examples_smoke"]`，覆盖 build-tag 屏蔽测试漏检

**对标**：k8s admission policy 严格性；JSON Schema spec keyword；contract-first endpoints required

---

## PR-CFG-I `BACKLOG-CFG-DRAIN`（~13h, 1 reviewer, **G1+G2 merge 后并行**） ⬜ TODO — 等 PR-CFG-G1+G2 完成

**抽象**：D/E/F/H 合并后 review 发现的跨子系统残余，按"模块/语义"分三批合批，单 PR 三个 commit。

**清零**：合并后 review C.3 全部 9 条 + 合并已登记 backlog `PR220-e2 GENERATED-BOUNDARY-STRATEGY-01`

### I.X1 — auth fail-closed by default（~4h）

- `runtime/auth/servicetoken.go:202-207` — 默认 NonceStore 从 `NoopNonceStore` 改 fail-closed；构造期校验非 Noop 或显式 dev opt-in（**P1 安全**）
- `runtime/auth/authenticator.go` — `NewServiceTokenAuthenticator` 对 nil key ring 构造期 fail-fast，与 middleware 路径不变量对齐
- `runtime/auth/servicetoken.go:304-311` + `pkg/errcode/errcode.go` — 新增 `ErrAuthNonceStoreFull`（503），从 `ErrNonceStoreFull` 映射替换 `ERR_INTERNAL`

### I.X2 — 治理 walker / boundary 一致性（~4h）

- `kernel/governance/rules_strict_extra.go:141-155` FMT-20 — schema walker 加 `allOf` / `oneOf` / `anyOf` / `if-then-else` 路径遍历 + 回归测试
- `assemblies/corebundle/generated/boundary.yaml:12-15` + `kernel/assembly/generator.go:198-244` — role assign/revoke audience 与 InternalListener 入口对齐；REF-16 + generator fingerprint 覆盖 schema drift（**合并已登记 backlog `PR220-e2`，本批执行后关闭**）

### I.X3 — 数据泄漏 + ops 残尾（~5h，第 1 条已升级 PR-CFG-K'.2）

- ~~`cells/configcore/internal/adapters/postgres/config_repo.go` Warn stale-cipher key_id 移除~~ — **已升级 PR-CFG-K'.2**（与 archtest REPO-LOG-KEY-ID-REDACT-01 同 PR 落地）
- `cells/configcore/cell_init.go::ensureCursorCodec` — `GOCELL_ADAPTER_MODE=real` 或缺显式 dev opt-in 时禁用固定密钥兜底；启动期注入随机密钥（**P2 安全**）
- `tests/e2e/config_pilot_e2e_test.go` + `tests/e2e/healthcheck-verify.sh`（如存在）— e2e 等待改 `/readyz`；脚本失败 trap 清理
- `cells/configcore/cell_routes.go` + `cells/configcore/slices/configsubscribe/service.go` — config 自发布自消费两 topic 拆分 consumerGroup；明确链路价值（in-memory cache / version tracking），否则降级或移除

**对标**：vault server fail-closed default；JSON Schema spec keyword combinator；OpenTelemetry semantic conventions sensitive attribute redaction

---

## PR-CFG-H `IDENTITY-RACE-AND-VALIDATE`（independent, ~6h, 1 reviewer） ✅ DONE — PR #280 (2026-04-25)

**抽象**：identity 数据竞态 + 输入校验补全。

**清零**：audit S-3 / S-4

**改动**：
- `cells/accesscore/slices/identitymanage/service.go::Lock+Unlock` — `GetByID` 移入 `txRunner.RunInTx` 闭包内
- `cells/accesscore/slices/identitymanage/service.go::Create` — `validation.RequireNotBlank` 补 `username` + `email`，与 `setup.CreateAdmin::validateCreateAdminInput` 对齐

---

## PR-A41 `BOOTSTRAP-STRUCT-DECOMPOSE`（**batch3 独立**, ~1d, 1 reviewer） ⬜ TODO — 待启动（注：PR-A41 编号已被 PR#270 CLI printer follow-up 占用，本 PR 应另选编号）

**抽象**：`Bootstrap` struct 36 字段、864 行单文件、10 phase 拆为 sub-struct 与 phase 分文件。架构债重构，**不与治理批次混合**。

**清零**：audit A-5

**改动**：
- 拆 `runtime/bootstrap/bootstrap.go` 为 5 sub-struct：`bootstrapAssembly` / `bootstrapHTTP` / `bootstrapHealth` / `bootstrapLifecycle` / `bootstrapMetrics`，`Bootstrap` 嵌入它们
- phase0..N 拆到独立文件 `phase0_validate.go` / `phase1_managed.go` / ...（已部分拆出，本 PR 完成）
- 所有 With* Option 函数保留 API 不变，仅内部分发到对应 sub-struct
- 测试不动（外部 API 无变化）

**对标**：fx `App` 结构化分组；k8s `apiserver/pkg/server.Config` 嵌套配置组

---

## 执行依赖

```
batch0 (前置, 10min)：git pull --rebase                        ✅ DONE

batch2.1 (✅ 已合)：
  ├─ PR-CFG-D                                                  ✅ PR #276
  ├─ PR-CFG-E                                                  ✅ PR #278
  ├─ PR-CFG-F                                                  ✅ PR #281
  └─ PR-CFG-H                                                  ✅ PR #280

batch2.2 (✅ 已合)：
  ├─ PR-CFG-J  E2E-CI-TRUE-EXECUTION                            ✅ PR #286
  └─ PR-CFG-K' CTXCANCEL-LOCK-AND-REDACT                        ✅ PR #287
        ↓ (J + K' 全 merge)
batch2.3 (并行, ~1.75d 完工时长 — G1 是瓶颈)：
  ├─ PR-CFG-G1 DATAFLOW-AND-PG (G.1+G.2+G.3+G.6+G.8) (~14h, 1 reviewer) ⬜ 立即启动
  └─ PR-CFG-G2 GOVERNANCE-AND-RESIDUE (G.4+G.5+G.7)  (~6.5h, 1 reviewer) ⬜ 立即启动
        ↓ (G1 + G2 全 merge)
batch2.4 (并行可行，~13h + 6 角色 retrospective)：
  ├─ PR-CFG-I BACKLOG-CFG-DRAIN (X1+X2+X3)   (~13h,  1 reviewer) ⬜ 等 G1+G2
  └─ batch2 终点 retrospective (D+E+F+G1+G2+H+I+J+K' 累计 diff, 6 角色) ⬜ 等 I

batch3 (G1+G2 merge 后启，与 I 并行)：
  └─ PR-A41 Bootstrap 拆分                   (~1d,   1 reviewer) ⬜ 等 G1+G2（编号需重选，PR#270 已用 PR-A41）
```

文件交集（J / K' / G1 / G2 / I / A41 全无写冲突）：
- J  改 `tests/e2e/*` + `tools/e2egate/*` + `.github/workflows/_build-lint.yml`
- K' 改 `cells/configcore/internal/adapters/postgres/{config,flag}_repo*.go` + `tools/archtest/repoerr_test.go`
- G1 改 `cells/{accesscore,configcore,auditcore}/{slices/identitymanage,sessionlogin,configreceive,configwrite,configrollback,configpublish,auditquery,auditappend}` + `cells/accesscore/internal/{dto/user_events.go,ports/configport.go,adapters/http/configclient.go}` + `cells/configcore/internal/events/config_events.go` + `cells/configcore/cell.go`（internal route 挂载）+ `contracts/event/{user,config}/...` + `contracts/http/config/get/v1/contract.yaml` + `contracts/internal/config/get/v1/...` + **G.8**: `cmd/corebundle/{access_module,shared_deps}.go` + `tests/e2e/{docker-compose.e2e.yaml,Dockerfile.corebundle,scripts/bootstrap-admin.sh}` + `tools/pg-migrate/main.go`（重建）+ `.github/workflows/_build-lint.yml::e2e` job env 切到 PG=1
- G2 改 `contracts/http/auth/{user,session,role}/...` + `contracts/http/config/{update,flags/list}/v1/...` + `kernel/governance/rules_{consistency,fmt}.go` + `cells/configcore/slices/{featureflag,configpublish}/handler.go` + `README.md` + `examples/iotdevice/README.md` + `docs/guides/integration-testing.md` + `cmd/gocell/app/check.go`
- I.X1 改 `runtime/auth/{servicetoken,authenticator}.go` + `pkg/errcode/errcode.go`
- I.X2 改 `kernel/governance/rules_strict_extra.go` + `kernel/assembly/generator.go` + `assemblies/corebundle/generated/boundary.yaml`
- I.X3 改 `cells/configcore/cell_init.go` + `cells/configcore/cell_routes.go` + `cells/configcore/slices/configsubscribe/...` + `tests/e2e/...`
- A41 改 `runtime/bootstrap/...`
- G1 vs G2 潜在交集：均触 `cells/configcore/`，但路径不重叠（G1 = `slices/configwrite|configrollback|configpublish/service.go` + `cell.go::RouteGroups` + `internal/events`；G2 = `slices/featureflag/handler.go` + `slices/configpublish/handler.go` 行级 status code），rebase 平滑
- K' / G1 / I.X3 文件路径完全不同（K' = PG adapter；G1 = slices + cell.go；I.X3 = cell_init + slices/configsubscribe + cell_routes）

总剩余工时：max(G1 1.75d, G2 6.5h) + max(I 13h, A41 1d) = ~3d（G1/G2 并行净省 ~0.75d；K'/J 已合）

---

## 6 角色 review 投放

- **PR-CFG-E（6 角色并行）**：✅ 已完成
- **PR-CFG-D, F, H（各 1 reviewer）**：✅ 已完成
- **PR-CFG-J, K'（各 1 reviewer）**：✅ 已完成
- **PR-CFG-G1, G2, I, A41（各 1 reviewer）**：六维度全覆盖单 reviewer
- **batch2 终点 retrospective（6 角色并行）**：D+E+F+G1+G2+H+I+J+K' 累计 diff 整体审

剩余：G1/G2/I/A41 各 1 reviewer + retrospective 6 角色 = 10 调用。

---

## 验证矩阵（每 PR 必跑）

```bash
go build ./... && go test ./...
golangci-lint run ./...                                            # 0 issues
go run ./cmd/gocell validate --strict                              # 0 errors
go test ./tools/archtest/...                                       # 含 worktrees 排除验证
go run ./cmd/gocell check unconditional-skip ./...                 # PR-CFG-D 后
go run ./cmd/gocell check slice-coverage --cell=accesscore         # PR-CFG-E 后
go run ./cmd/gocell check assembly-completeness --id=corebundle    # PR-CFG-E 后
go run ./cmd/gocell check journey-readiness --journey=J-sessionlogin  # PR-CFG-E 后
go run ./cmd/gocell check l0-imports --cell=accesscore             # PR-CFG-E 后
go test -tags=integration -covermode=atomic \                      # 照抄 CI scope
  ./adapters/... ./tests/integration/... ./cmd/corebundle/... \
  ./examples/ssobff/... ./examples/iotdevice/... ./examples/todoorder/... \
  ./cells/accesscore/slices/identitymanage/... \
  ./runtime/bootstrap/... -timeout 15m
go test -tags=e2e ./tests/e2e/...                                  # PR-CFG-D 后

# PR-CFG-J 后真执行 gate（取代 D 之后的"假绿"风险）
docker compose -f tests/e2e/docker-compose.e2e.yaml up -d \
  && bash tests/e2e/scripts/bootstrap-admin.sh \
  && go test -tags=e2e -json ./tests/e2e/... \
       | go run ./tools/e2egate/cmd/...                       # executed=0 / all-skipped 即 fail
docker compose -f tests/e2e/docker-compose.e2e.yaml down -v

# 生产 gate（PR-CFG-F 后）
GOCELL_ADAPTER_MODE=real go run ./cmd/corebundle                              # fail (multi-pod 缺 Redis / control-plane env)
GOCELL_ADAPTER_MODE=real GOCELL_SINGLE_POD=1 go run ./cmd/corebundle          # only pass when all single-pod secrets are set
GOCELL_ADAPTER_MODE=real go run ./examples/iotdevice                          # fail (缺 service secret/JWT verifier env)
GOCELL_ADAPTER_MODE=real go run ./examples/todoorder                          # fail (缺 service secret/JWT verifier env)
```

回归断言：
- 普通用户 GET `/api/v1/config/` → 403（PR-CFG-E 层）
- examples real 模式启动失败（PR-CFG-F）
- accesscore.configreceive 收到 entry-upserted 后真调用 internal GET（PR-CFG-G1）
- `event.user.{deleted,updated,unlocked}.v1` 在 audit 表可查；任何 config 写操作 audit 记录含 actorId（PR-CFG-G1）

---

## 保留 backlog（仅 2 条，真 YAGNI 理由）

| Finding | 保留理由 |
|---|---|
| audit KG-F7 (LAYER-05 L0 Cell 例外) | 当前零 L0 Cell（cells/ + examples/ 全仓 `consistencyLevel: L0` = 0 命中），宪法允许但无活体违规；首个 L0 Cell 入场时再实施 archtest 例外 |
| audit RA-F5 (configreceive 仅 log) | 已登记 `PR266-METADATA-ONLY-CONSUMER-BUSINESS`；属业务接入（JWT TTL refresh / key rotation）超 audit 范畴；本批 PR-CFG-G1 G.3 已建立 refetch 闭环框架，业务 reload 由后续业务 PR 实施 |

---

## 已彻底处理（前述 backlog 项 + 合并后 review 16 条全部纳入本计划）

| Finding | 之前 backlog 理由 | 真处理 |
|---|---|---|
| A-1 (4 placeholder check) | "需独立 ADR" | E.3 全实现 ✅ #278 |
| A-3 (Claimer multi-pod) | "需 architect 决策" | F.2 Redis nonce + claimer 装配 ✅ #281 |
| A-5 (Bootstrap 拆分) | "无紧急性" | PR-A41 batch3 独立 PR，~1d |
| A-6 (Delegated 字段冗余) | "Cx2 优化" | E.4 删字段 ✅ #278 |
| AR-F7 (assemblyID doc) | "doc 修正" | F.4 doc/代码对齐 ✅ #281 |
| DF-F3 (refetch design gap) | "需 ADR" | G1.G.3 改路由镜像 + 真实施（待） |
| KG-F6 (boundary.yaml 缺) | "需决定 generate 何时跑" | E.4 跑 `gocell generate` ✅ #278 |
| **PR-CFG-D-FU** (e2e CI 真执行 gate) | "1.5d 测试基建独立 PR" | **PR-CFG-J 前置 G**，~1.5d |
| **PR220-e2** (boundary 治理策略) | "工时 1h，未决 generate 节奏" | **I.X2 合并实施**，本批关闭 |
| 合并后 review C.1（5 条 D/E 残余清扫） | review 直接产出 | **PR-CFG-G2 G.7**（README × 2 + integration-testing.md + slice-coverage failfast + analyzer build-tag），~3h |
| 合并后 review C.2（2 条 G.4 漏项） | review 直接产出 | **PR-CFG-G2 G.4 增补**（update.sensitive 断流 + endpoints.http required），~1.5h |
| 合并后 review C.3（9 条跨子系统） | review 直接产出 | **PR-CFG-I**（X1 auth fail-closed + X2 治理 walker/boundary + X3 数据泄漏/ops 4 项；第 1 条 key_id redact 升级 K'），~13h |
| 合并后 review C.4（CI 真执行 gate） | review 直接产出 | **PR-CFG-J 前置**，~1.5d |
| **PR#271 ✅ 已落地** (`pkg/ctxcancel` + 双语义分流 499/504 + tracing attr + configcore/auditcore PG 迁移 + decrypt Category=Auth + STALECIPHER PascalCase) | bd2381cd / 2026-04-26 | 关闭 backlog 5 条详细行（CTXCANCEL-HTTP-499 / OTEL-SPAN-CTXCANCEL / PR238-FU3 / PR238-FU6 / PR238-FU7）+ 4 条孤儿汇总行引用（PR238-FU1 / STALECIPHER-OPFIELD / FLAGREPO-CTXCANCEL / CTXCANCEL-HELPER-PKGLEVEL）+ 026 plan PR-A50/A51 |
| **PR#271 残尾** (configcore PG thin wrapper 残留 + stale-cipher Warn key_id 泄漏 + archtest 未锁回退) | 实际状态发现 | **PR-CFG-K'**（删 thin wrapper + Warn 移除 key_id + archtest 双向锁），~3h，与 J 并行 |

---

## ExitPlanMode 后执行（剩余批次，2026-04-26 起）

1. `git fetch && git pull --rebase`（已 @ 523b7fc0；J #286 / K' #287 已合）
2. **并行**起 PR-CFG-G1 + PR-CFG-G2 worktree（文件路径不重叠）
   - G1 → identity 事件全覆盖 + config event actor + auditquery Role 常量 + auditappend strict + refetch 闭环 + accesscore 真 PG outbox + e2e 真 PG harness + camelCase 收尾
   - G2 → contract consistencyLevel L1→L2 + governance 规则补全 + UX/产品 + D/E 残余清扫
3. G1 + G2 全 merge 后 **并行**起 PR-CFG-I + PR-A41 worktree（文件路径完全不重叠）
4. PR-CFG-I 三批分独立 commit（X1 auth fail-closed → X2 治理 walker/boundary → X3 数据泄漏/ops）；PR-A41 phase 拆分独立 commit
5. 每 worktree push 前 `golangci-lint 0 issues` + 对应 scope 真跑
6. CI 状态用 `gh pr checks <num>` 单次查询，不循环
7. Cx1/Cx2 IN_SCOPE findings 派 developer `/fix`
8. I + A41 merge 后启 batch2 终点 retrospective 6 角色（D/E/F/G1/G2/H/I/J/K' 累计 diff）
9. 完成后人工确认 backlog 仅剩 2 条 YAGNI 项（KG-F7 / RA-F5）+ 合并后 review C.3 全部已通过 PR-CFG-I + PR-CFG-K' 关闭
