# L3 计划：K8s 风格 CI 治理 第二批（make verify 入口 + generated drift done + 5 条 hard gate，单 PR 彻底消化）

> 基线：`origin/develop @ 0f8f737e`（2026-04-30，最新合入 PR#337）
>
> **轮次更新 2026-04-28**：阶段 0 ✅ — PR #308 落地 `make verify` + 7 个 verify gate（超出原 plan 描述的 2 个 govalidate+archtest，预消化了 contract-health / examples-import / journey / scaffold-reject / unconditional-skip 五个 gate 的创建动作）。
>
> **补同步 2026-04-29**：#321 已落地 PR-CI-1 的 generated artifact gate：`hack/verify-generated.sh` + `make generate` metrics/boundary regenerate + CI diff/untracked gate。
>
> **补同步 2026-04-30 (round 1)**：#336 ✅ PR-CI-6 PROD-DURATION-CONST-01 落地；#335 ✅ PR-CI-5 PANIC-REGISTERED-01 落地（ADR 永久白名单 4 条）；#333 PR-A66 BOOTSTRAP-STRUCT-DECOMPOSE 落地。
>
> **补同步 2026-04-30 (round 2)**：#334 ✅ PR-A48（026 plan main lane，本 plan 不计核心）；#337 ✅ Qodana CI trust boundary 修复（移除 `pull_request` 触发）+ consolidate #327-#330 dependabot bumps + macOS symlink pivot fix。本 plan 剩余 PR-CI 项不受这两个 PR 影响。
>
> **剩余工作**：阶段 1（PR-CI-2/3，~14h，可并行）+ 阶段 2（PR-CI-4，~28h）= 3 PR / ~42h（PR-CI-1 ✅ #321 / PR-CI-5 ✅ #335 / PR-CI-6 ✅ #336 已合）。

## Context

第一批 plan（`docs/plans/202604270020-1-2-ci-3-claude-ship-reactive-bachman.md`）已合并 PR-MODE-1~8 八条 archtest，**PR #307 PR-MODE-6.1 nil error-first 收尾已合（2026-04-28）**。第一批仍有 Phase 2（Claude 分层骨架）/ Phase 4（6 角色 retrospective）未启动；Phase 3（v1 schema ADR）已迁入本 plan 的 PR-CI-3。

本 plan 对标 Kubernetes `hack/verify-*` 治理体系，把散落的 archtest+governance+脚本统一到 `make verify`，并推进用户列出的 6 条优先 hard gate（GENERATED-DRIFT / TEST-NO-SLEEP / PANIC-REGISTERED / V1-RESPONSE-EVOLVE / SHELL-SAFETY / PROD-DURATION-CONST）。其中 V1-RESPONSE-EVOLVE 把第一批 Phase 3 收掉，PROD-DURATION-CONST 把 production 硬编码 duration 治理也纳入 CI。

**核心节奏**：保留第一批「1 模式 1 PR + 同 PR 业务消化 + CI 始终绿」**不引入 baseline / NEW-CLEAR-HARD 分批**。每条规则一次性彻底消化全仓存量（当前包含 97 处测试 `time.Sleep` 与 35 处 production panic），开 PR 前在 worktree 跑通 archtest 0 violations。

**勘察实测（2026-04-27 / 2026-04-28 develop）**：
- `make verify` **已由 #308 落地**；workflow `governance.yml` / `_build-lint.yml` 已收敛到 `make verify` 入口，后续 gate 只增 `hack/verify-*.sh`
- `tools/archtest/` 37 文件（已含 LAYER-01~10、SEC-FAIL-CLOSED-01~06、ERROR-FIRST-API-01、META-QUERYPARAM-DRIFT、ADV-06、integration guard、accesscore facade guard 等）
- `kernel/governance/rules_strict_extra.go` 含 FMT-20（response strict）
- `gocell generate assembly --id=corebundle` 写 2 文件（`cmd/corebundle/main.go` + `assemblies/corebundle/generated/boundary.yaml`），均 git 跟踪，当前无漂移
- 测试代码 `time.Sleep` 实测 **97 调用 / 38 测试文件**（#320/#322 新增 integration/lifecycle 覆盖后，PR-CI-4 启动前需重扫具体分布）
- production panic **35 处**；ADR `docs/architecture/202604270030-architectural-panic-whitelist.md` 当前白名单 1 项；剩余集中在 kernel Must*/wrapper、runtime auth/http、postgres tx recover、router 注册期等路径
- `contracts/http/*/v1/response.schema.json` **30 文件**，100% 顶层 `additionalProperties:false`
- `scripts/seed-test-data.sh` 缺 `set -euo pipefail`（其余 2 个脚本合规）
- **production 硬编码 duration 实测（2026-04-28）**：`context.WithTimeout(_, 字面量)` 6 处 — `runtime/eventbus/eventbus.go:540,554`（2 × 5s release）/ `adapters/rabbitmq/subscriber.go:453`（30s wait）/ `adapters/rabbitmq/subscriber.go:804,839`（2 × 5s cleanup）/ `adapters/postgres/pool_resource.go:76`（5s probe）；production `time.Sleep` 字面量 1 处 `tests/e2e/internal/clients/clients.go:72`（500ms）；`time.After/NewTimer/NewTicker` 0 处真生产代码（`outboxtest`/`locktest` test helper 包内剩余但 _testpkg_ 豁免）

**对标 K8s（已勘明）**：
| 元素 | K8s 实现 | 本 plan 借鉴 |
|---|---|---|
| 入口 | `hack/make-rules/verify.sh` glob `hack/verify-*.sh` | `make verify` 自动发现，新增 gate 只增文件 |
| 隔离 | `git worktree add` 临时工作树 | GENERATED-DRIFT 实现路径 |
| 累积 | fail-accumulate（不 fail-fast） | 所有 verify-* 跑完再统一 exit |
| 测试质量 | `verify-test-code.sh` 直接 grep / 零容忍 | TEST-NO-SLEEP / PANIC archtest 直接零容忍 |

---

## 用户硬约束（彻底 / 不向后兼容 / 优雅简洁）

1. **每个 CI 模式 1 个 PR**，archtest + 同 PR 业务消化全仓存量，CI 始终绿
2. **不留** waiver / lenient / opt-in flag / `//nolint:archtest-XXX` / **baseline 文件锁存量** / **NEW-CLEAR-HARD 三阶段**——任何「先豁免后清零」都是向后兼容 shim
3. **不留** TODO / placeholder / 决策 follow-up——所有路径选择本 plan 内拍板
4. **不新建辅助包/迁移文档**当现有库够用——`testify.Eventually` 替代新写 `pkg/synctest`；不写 `no-sleep-migration.md`
5. **不保留旧规则名**——FMT-20 直接改语义，不拆「FMT-20-REQUEST + 删响应」造别名
6. 中文输出；路径/命令保留原样

---

## 范围与依赖

### 范围内（7 个 PR）
| 阶段 | PR | 状态 | 内容 |
|---|---|---|---|
| 0 | PR-CI-0 VERIFY-ENTRYPOINT-01 | ✅ #308 (2026-04-28) | `make verify` 入口 + glob 自动发现（实际落地 7 个 gate） |
| 1 | PR-CI-1 GOV-GENERATED-DRIFT-01 | ✅ #321 (2026-04-28/29) | generated boundary + metrics schema regenerate-and-diff gate |
| 1 | PR-CI-2 SHELL-SAFETY-01 | ⬜ | grep `set -euo pipefail` |
| 1 | PR-CI-3 V1-RESPONSE-EVOLVE | ⬜ | ADR + FMT-20 改语义 + 30 schema 顶层放宽 |
| 1 | PR-CI-6 PROD-DURATION-CONST-01 | ✅ #336 (2026-04-30) | production 硬编码 duration 抽 const + archtest 零容忍 |
| 2 | PR-CI-4 TEST-NO-SLEEP-01 | ⬜ | 一次性消化 97 处 sleep + archtest 零容忍 |
| 2 | PR-CI-5 PANIC-REGISTERED-01 | ✅ #335 (2026-04-30) | 一次性消化 35 处 panic + ADR 永久白名单 4 条 + archtest 零容忍 |

### 范围外
- 第一批 plan Phase 2（Claude 分层骨架）/ Phase 4（retrospective）—— 留原 plan
- `import-restrictions.yaml` 外置化 —— LAYER-01~10 已稳定
- GOV-CONTRACT-GROUP-01 / CONTRACT-COMPAT-01 / SECRET-SINK-01 —— 用户标记后批
- `pkg/synctest/` 新包 —— 用现有 `testify.Eventually` 与 stdlib `time.After + select`
- `no-sleep-migration.md` —— 不写文档；范式直接体现在 97 处修改 diff 中

---

## 实施切分

### 阶段 0 — PR-CI-0 VERIFY-ENTRYPOINT-01（~4h，无依赖）✅ 完成 @ #308 (2026-04-28)

**抽象**：`make verify` 入口 + glob 自动发现，后续 gate 只增 `hack/verify-X.sh`。

**实际落地**（与原计划差异）：原 plan 仅要求落 `verify-govalidate` + `verify-archtest` 两个 gate，实际 #308 一并落地 **7 个** verify gate（一次性把分散在 archtest 子目录中的 govalidate / archtest / contract-health / examples-import / journey / scaffold-reject / unconditional-skip 全部暴露为独立 verify 入口）。这是"未来 gate 的创建路径已统一"的预消化，不增加 PR-CI-1/2/4/5/6 工时但缩短其落地路径。

**已落地文件清单**：
- `hack/make-rules/verify.sh`（fail-accumulate 入口）
- `hack/lib/util.sh`（共享 logging）
- `hack/verify-archtest.sh` / `hack/verify-govalidate.sh`
- `hack/verify-contract-health.sh` / `hack/verify-examples-import.sh` / `hack/verify-journey.sh` / `hack/verify-scaffold-reject.sh` / `hack/verify-unconditional-skip.sh`
- `hack/README.md`
- `Makefile`：新增 `verify:` target
- `.github/workflows/governance.yml`：3 个 inline step 收敛为 1 个 `make verify` job
- `.github/workflows/_build-lint.yml`：drop 4 个 governance step + 1 个 layering grep（archtest layer/cell-internal guard 仍由 kernel shard 的 Test step 与 `make verify` 双跑覆盖）

**业务消化**：无（行为等价迁移）；governance.yml 和 _build-lint.yml 已收敛**不保留旧 step 双路径**

**验收**：✅ 已通过

---

### 阶段 1 — 单 PR 一次消化（PR-CI-2/3/6，文件域 ∅，可并行，~18h，依赖 0）

#### PR-CI-1 GOV-GENERATED-DRIFT-01（~3h）✅ 完成 @ #321

**改动**：
- 新 `hack/verify-generated.sh`：
  ```bash
  set -euo pipefail
  WT="$(mktemp -d)/wt"
  git worktree add --detach -q "$WT" HEAD
  trap "git worktree remove -f '$WT' 2>/dev/null || true" EXIT
  ( cd "$WT" && go run ./cmd/gocell generate assembly --id=corebundle )
  ( cd "$WT" && git diff --exit-code ) || {
    echo "FAIL: generated drift; run 'make generate' and commit"
    exit 1
  }
  ```

**业务消化**：#321 实测并提交 generated baseline，新增 `assemblies/corebundle/generated/metrics-schema.yaml`；不加 generated header（项目仅自身消费，README 即足够）。

**落地**：#321 新增 `hack/verify-generated.sh`，`make generate` 同步 regenerates boundary + metrics schema；CI 中 boundary / metrics-schema regenerate-and-diff gate 同时检查 tracked diff 与首次生成但未提交的 untracked artifacts。

**验收**：worktree 跑后 `git status --porcelain` 空；故意改 `boundary.yaml` 一行后 verify 失败

---

#### PR-CI-2 SHELL-SAFETY-01（~1h）

**改动**：
- 新 `hack/verify-shell-safety.sh`：
  ```bash
  set -euo pipefail
  fail=0
  while IFS= read -r f; do
    grep -q '^set -euo pipefail' "$f" || { echo "FAIL: $f missing 'set -euo pipefail'"; fail=1; }
  done < <(find scripts hack -name '*.sh' -type f)
  exit $fail
  ```
- 改 `scripts/seed-test-data.sh`：shebang 后加 `set -euo pipefail`

**验收**：`make verify` 通过；故意删一行后失败

---

#### PR-CI-3 V1-RESPONSE-EVOLVE + FMT-20 改语义（~13h）

**改动（governance）**：
- 新 ADR `docs/architecture/202604280000-v1-schema-evolution.md`：方向 A（响应放宽）+ 与第一批 PR-CFG-E #278 关系说明（策略修正非回退）
- 改 `kernel/governance/rules_strict_extra.go::FMT-20`：**保留 ID FMT-20，改语义为「仅 request schema 强制 `additionalProperties:false`」**——不拆双 ID、不留别名
- 改 `kernel/governance/rules_strict_extra_test.go` 同步
- 改 `.claude/rules/gocell/api-versioning.md`：明确「响应可加新字段，请求保持严格」

**业务消化（30 文件）**：
- 全仓 `contracts/http/*/v1/response.schema.json` 移除**所有层级**的 `additionalProperties:false`（顶层 + nested）—— 彻底放宽不留半截，与 ADR 方向 A 一致
- `pkg/contracttest/` 加 1 条 case：response 多 1 字段 PASS

**验收**：
```bash
go run ./cmd/gocell validate --strict   # 0 errors
grep -rn '"additionalProperties": false' contracts/http/*/v1/response.schema.json | wc -l   # = 0
go test ./pkg/contracttest/... -count=1   # 容忍用例 PASS
```

---

#### PR-CI-6 PROD-DURATION-CONST-01（~4h）✅ 完成 @ #336 (2026-04-30)

**唯一不变量**：在所有可发到产品的 `.go` 文件中，任何静态类型为 `time.Duration` 且子树含 BasicLit 的表达式，必须直接出现在 **package-level** `const` 声明的初始化表达式中。其他任何位置（var 初值、赋值 RHS、struct/map 字面量字段、函数返回、CallExpr 参数、函数局部 const、switch case、for 初始化、TypeAssert/Conversion 内部、闭包内部）出现 literal duration 一律视为违规。例外：BasicLit 字面值为 `"0"` 时不计违规（零值惯用法）。

**文件宇宙**：`PatternsExtended(root)`（`Patterns + ./tests/... + ./tools/...`），build tags `{e2e,integration,pg}` 联合加载（`LoadPackagesWithTags`）。

**豁免清单**：`_test.go` / `tools/archtest/` / `examples/` / `vendor/` / `generated/` / `testdata/` / `**/locktest/` / `**/outboxtest/` / `**/storetest/` / `**/healthtest/` / `**/conformance.go`。

**改动（invariant-driven rewrite，一次性闭合全部已知绕过路径）**：
- `tools/archtest/prod_duration_const_test.go`：完全重写为 universal walk + type-based 判定，删除 5-node / API 白名单 / unwrapNowAddCall / context.AfterFunc 等机制式代码，fail-closed
- `tools/archtest/internal/typeseval/typeseval.go`：新增 `LoadPackagesWithTags` + `SharedResolverWithTags`
- `tools/internal/prodscan/patterns.go`：新增 `PatternsExtended(root)`
- `tools/archtest/prod_duration_const_internal_test.go`：重写 4 个测试函数
- `tools/archtest/prod_duration_fixtures_test.go`：新增 23 fixture（5 正向 + 18 反向）+ fail-closed 测试
- `tools/archtest/testdata/prod_duration_fixtures/`：23 个独立 go.mod 子包
- `hack/verify-prod-duration.sh`：header 缩为 plan 单引用

**业务消化（全仓 ~17+ 处，universal walk 一次性全部消化）**：包括 `runtime/eventbus/eventbus.go`, `adapters/rabbitmq/subscriber.go`, `adapters/postgres/pool_resource.go`, `tests/e2e/internal/clients/clients.go` 以及 universal walk 二次清扫暴露的其余文件，各抽 `default*` 风格 const。

**与 PR-CI-4 边界**：PR-CI-4 = `*_test.go` 中 `time.Sleep`；PR-CI-6 = production `*.go` 中所有 literal duration（不含 `_test.go`）。`locktest/` / `outboxtest/` / `conformance.go` 整体豁免。

**验收**：
```bash
make verify   # PR-CI-6 archtest 0 violations
go test ./tools/archtest/ -run 'TestProdDurationConst$|TestProdDurationConstFixtures$|TestProdDurationConstFailsClosedOnLoadError$' -race -count=1
go build -tags="e2e integration pg" ./...
```

---

### 阶段 2 — 大规模一次性彻底消化（PR-CI-4/5，~50h，依赖 0；文件域有交集，串行）

#### PR-CI-4 TEST-NO-SLEEP-01（~28h）

**抽象**：97 处 sleep / 38 文件**单 PR 全部修完** + archtest 零容忍。不引入 baseline、不分批、不新建 `pkg/synctest`。

**替代范式（直接用现有库）**：
- 等待条件：`testify/assert.Eventually(t, fn, timeout, interval, msg)`
- 等待 channel：`select { case <-ch: case <-time.After(timeout): t.Fatal("timeout") }`
- 让协程让出：`runtime.Gosched()`

**改动**：
- 新 `tools/archtest/test_no_sleep_test.go`：用 `topic_const_resolver.go` 的 `packages.Load` 范式扫所有 `*_test.go` 中 `time.Sleep` CallExpr，**全仓零容忍**（无白名单参数）
- 新 `hack/verify-test-no-sleep.sh`：调 archtest

**业务消化（当前实测 38 文件 / 97 处）**：
- 以 `git grep -n "time.Sleep" origin/develop -- "*_test.go"` 为启动前唯一事实源；#320/#322 已新增 integration/lifecycle 覆盖，旧 35 文件 / 87 处目录表作废。
- 每处按用途选 `Eventually` / `time.After+select` / `runtime.Gosched`；rabbitmq / testcontainers 健康轮询优先用 `Eventually(timeout=5s, interval=50ms)`。

**验收**：
```bash
grep -rn "time.Sleep" --include="*_test.go" | grep -v "/testdata/" | wc -l   # = 0
go test ./... -count=1 -race   # 全绿
go test ./tools/archtest/... -run TestNoSleep -count=1
```

**风险/接受**：单 PR diff 大（38 文件 + 约 180 行修改），但符合「彻底」「不向后兼容」原则；review 用文件分组逐目录看；CI 始终绿（开 PR 前 worktree 跑通）

---

#### PR-CI-5 PANIC-REGISTERED-01（~22h）✅ 完成 @ #335 (2026-04-30)

**实际落地**（与原计划差异）：ADR 永久白名单落地 4 条（非计划的 mux 注册期，而是 recover re-panic 四处：`kernel/wrapper/lifecycle.go` / `runtime/http/middleware/circuit_breaker.go` / `adapters/postgres/tx_manager.go` ×2）；archtest 强制 `len(goKeys) == 4` 与 ADR 表行数一致。

**抽象**：35 处 production panic 单 PR 全部处理 + ADR 永久白名单 4 条（recover re-panic 语义） + archtest 零容忍。不留分批清扫、不留白名单缓冲清单。

**改动（archtest + ADR）**：
- 新 `tools/archtest/panic_registered_test.go`：扫 production `*.go`（排除 `_test.go` / `tools/archtest/` / `testdata/` / `vendor/`），自动豁免 `Must*` 前缀函数 / `func init()` / `recover()` 后 re-panic；白名单从 ADR 永久表硬编码 + 校验「白名单条目数 == ADR 永久表行数」
- 新 `hack/verify-panic-registered.sh`：调 archtest
- 改 `docs/architecture/202604270030-architectural-panic-whitelist.md`：定稿**永久白名单（≤5 条）**，仅保留「mux 注册期 panic」「init 期 panic」类，理由 ADR 锁定

**业务消化（35 处一次性彻底，按 `origin/develop @ ad986cad` 复核仍为 35 处）**：
| 路径 | 处置 | 估 panic |
|---|---|---:|
| `runtime/http/router/router.go` | **永久白名单候选**（mux 注册期 panic 等同 init 类，编程错误 fail-fast） | 1 |
| `kernel/{cell,outbox,wrapper,observability}/` | `Must*` / 构造器 guard / error-first 三选一逐处定，wrapper recover re-panic 单独评估 | 10 |
| `runtime/auth/` + `runtime/http/{health,middleware}/` + `runtime/distlock/` | error-first 或永久白名单候选（test-key / recover re-panic / principal context）逐处定 | 13 |
| `adapters/{postgres,websocket}/` | init/embed panic、tx recover re-panic、handler constructor error-first 分流 | 5 |
| `cells/accesscore/slices/{sessionlogin,sessionlogout,sessionrefresh}/` | error-first | 3 |
| `pkg/{contracttest,errcode,idutil}/` | test helper / invariant panic / entropy fail-fast 分类，必要时 ADR 白名单 | 3 |

**验收**：
```bash
make verify   # 全绿
go test ./tools/archtest/... -run TestPanicRegistered -count=1
# 白名单条目数与 ADR 表一致由 archtest 强制
```

**风险**：router.go 5 处永久豁免与「kernel/runtime/adapters 禁 panic」原则需 ADR 论证——本 plan 论证：mux 注册期 panic 仅在配置错误（重复路由 / 路径冲突）触发，**进程启动期 fail-fast**而非运行期错误，与 `init()` panic 同性质，纳入永久白名单优于把所有路由注册改 error-first（侵入面过大，违背「优雅简洁」）

---

## 关键文件路径速查

**PR-CI-0**：新 `hack/make-rules/verify.sh`、`hack/lib/util.sh`、`hack/verify-{govalidate,archtest}.sh`、`hack/README.md`；改 `Makefile`、`.github/workflows/{governance,_build-lint}.yml`

**PR-CI-1**：新 `hack/verify-generated.sh`

**PR-CI-2**：新 `hack/verify-shell-safety.sh`；改 `scripts/seed-test-data.sh`

**PR-CI-3**：新 `docs/architecture/202604280000-v1-schema-evolution.md`；改 `kernel/governance/rules_strict_extra.go`、`kernel/governance/rules_strict_extra_test.go`、`.claude/rules/gocell/api-versioning.md`、`contracts/http/*/v1/response.schema.json`（30 文件）、`pkg/contracttest/`

**PR-CI-6**：新 `tools/archtest/prod_duration_const_test.go`、`hack/verify-prod-duration.sh`；改 `runtime/eventbus/eventbus.go`、`adapters/rabbitmq/subscriber.go`、`adapters/postgres/pool_resource.go`、`tests/e2e/internal/clients/clients.go`（5 文件抽 const）

**PR-CI-4**：新 `tools/archtest/test_no_sleep_test.go`、`hack/verify-test-no-sleep.sh`；改 35 个测试文件（adapters/rabbitmq/* 7、runtime/* 14、cells/cmd/tests 5、adapters/redis|vault|postgres|adapterutil 5、kernel|pkg 4）

**PR-CI-5**：新 `tools/archtest/panic_registered_test.go`、`hack/verify-panic-registered.sh`；改 `docs/architecture/202604270030-architectural-panic-whitelist.md`、`runtime/distlock/locker.go`、`cells/accesscore/slices/{sessionlogin,sessionrefresh}/service.go`、`runtime/auth/refresh/memstore/store.go` + 散落 ~33 处

---

## 复用 Pattern

```go
// tools/archtest/topic_const_resolver.go::ResolveString — packages.Load + go/types
// PR-CI-4 / PR-CI-5 共用范式扫 AST CallExpr
cfg := &packages.Config{Mode: packages.NeedTypes | packages.NeedTypesInfo | packages.NeedSyntax | packages.NeedFiles}
pkgs, err := packages.Load(cfg, "./...")
```

```bash
# K8s fail-accumulate（hack/make-rules/verify.sh 核心）
fails=()
for f in hack/verify-*.sh; do bash "$f" || fails+=("$f"); done
[[ ${#fails[@]} -eq 0 ]] || { printf 'FAIL: %s\n' "${fails[@]}"; exit 1; }
```

```bash
# K8s worktree 隔离（PR-CI-1）
WT="$(mktemp -d)/wt"; git worktree add --detach -q "$WT" HEAD
trap "git worktree remove -f '$WT' 2>/dev/null || true" EXIT
( cd "$WT" && make generate && git diff --exit-code )
```

---

## 工时与排期

| 阶段 | PR | 工时 | 累计 | 状态 | 并行性 |
|---|---|---:|---:|---|---|
| 0 | PR-CI-0 VERIFY-ENTRYPOINT | 4h | 4h | ✅ #308 | 已先合 |
| 1 | PR-CI-1 GENERATED-DRIFT | 3h | 7h | ✅ #321 | 已合 |
| 1 | PR-CI-2 SHELL-SAFETY | 1h | 8h | ⬜ | 与 3/6 并行（文件域 ∅） |
| 1 | PR-CI-3 V1-RESPONSE-EVOLVE | 13h | 21h | ⬜ | 同上 |
| 1 | PR-CI-6 PROD-DURATION-CONST | 4h | 25h | ✅ #336 | 已合 |
| 2 | PR-CI-4 TEST-NO-SLEEP | 28h | 53h | ⬜ | 与 PR-CI-5 串行（archtest 共用 packages.Load 范式 + 测试文件可能交集）|
| 2 | PR-CI-5 PANIC-REGISTERED | 22h | 75h | ✅ #335 | 已合 |

**剩余工时 ~42h**（CI-0/CI-1/CI-5/CI-6 已合，已消化 33h）；**单人单 PR 28h（PR-CI-4）是最大单**

---

## 决策点（已采用方案）

| 决策 | 已选方案 | 理由 |
|---|---|---|
| baseline 三阶段 vs 单 PR 一次消化 | **单 PR 一次消化** | 用户硬约束「不向后兼容 / 彻底」；baseline 文件本质是 backwards-compat shim |
| FMT-20 拆分 vs 改语义 | **改语义保留 ID** | 不留别名；语义改动在 ADR + test 中说明 |
| 响应顶层放宽 vs 顶层+nested 全放宽 | **全放宽** | 响应 schema 既然按方向 A 演进自由就该彻底；保留 nested 严格是半截兼容 |
| `pkg/synctest` 新包 vs 用现有库 | **`testify.Eventually` + stdlib** | 优雅简洁；不引入新抽象 |
| `no-sleep-migration.md` 文档 | **不写** | 直接体现在 diff；项目无外部消费方 |
| router.go 5 panic：refactor vs 永久豁免 | **永久 ADR 白名单** | mux 注册期 = init 语义；refactor 侵入过大违背简洁 |
| PR-CI-6 抽 const vs 强制 option 化 | **抽 const 即合规** | 项目无运行时调参需求；强制 7 个新 `WithXxxTimeout` API 增加 surface 面违背简洁；点状有需求时再加 option |
| PR-CI-6 测试 helper 包（locktest/outboxtest/conformance.go）处置 | **archtest 路径白名单豁免** | 这些是 driver/conformance 测试 helper，物理 sleep 是测试语义本身；与 PR-CI-4 `_test.go` 范围互补但不重叠 |
| `update-generated.sh` 配对脚本 | **不建** | `make generate` 已存在，verify-generated 内部直接调，无需配对脚本 |
| Generated header 加 `// Code generated` | **不加** | 项目仅自身消费；README 注明 + verify 强制即足够 |
| `_build-lint.yml` 是否保留 archtest step | **替换为 `make verify`** | 不留双路径 |
| 6 角色 retrospective 并入本 plan | **不并入** | 应在 Claude 分层骨架（第一批 Phase 2）合并后再跑 |

---

## 验证矩阵

```bash
# 阶段 0 后
make verify   # 累积 exit code，所有 verify-* 全跑
ls hack/verify-*.sh | wc -l   # ≥ 2
# 验证 governance.yml / _build-lint.yml 已替换为 make verify（grep 旧 step 无残留）
grep -rn "go run ./cmd/gocell validate --strict\|go test ./tools/archtest" .github/workflows/   # 仅 make verify 调用

# 阶段 1 / 2 每个 PR 开 PR 前
make verify
go test ./... -count=1 -race
golangci-lint run ./...
go test -tags=integration ./adapters/... ./tests/integration/... -timeout 15m

# PR-CI-1 专项
( cd "$(mktemp -d)" && git clone /Users/shengming/Documents/code/gocell src && cd src && bash hack/verify-generated.sh )

# PR-CI-3 专项
grep -rn '"additionalProperties": false' contracts/http/*/v1/response.schema.json | wc -l   # = 0

# PR-CI-4 专项
grep -rn "time.Sleep" --include="*_test.go" | grep -v "/testdata/" | wc -l   # = 0

# PR-CI-5 专项
go test ./tools/archtest/... -run TestPanicRegistered -count=1
# 白名单条目数 ≤ 5 由 archtest 强制

# PR-CI-6 专项
go test ./tools/archtest/... -run TestProdDurationConst -count=1
grep -rnE '(time\.Sleep|time\.After|time\.NewTimer|time\.NewTicker)\([0-9]+[[:space:]]*\*[[:space:]]*time\.' --include="*.go" \
  | grep -v "_test.go" | grep -v "tools/archtest/" | grep -v "/locktest/\|/outboxtest/\|/conformance.go" | grep -v "vendor/" | wc -l   # = 0
grep -rnE 'context\.With(Timeout|Deadline)\([^,]+,[[:space:]]*[0-9]+[[:space:]]*\*' --include="*.go" \
  | grep -v "_test.go" | grep -v "vendor/" | wc -l   # = 0
```

---

## Out of Scope

- 第一批 plan Phase 2（Claude 分层骨架）/ Phase 4（retrospective）—— 留原 plan
- `import-restrictions.yaml` 外置化 —— LAYER-01~10 已稳定
- GOV-CONTRACT-GROUP-01 / CONTRACT-COMPAT-01 / SECRET-SINK-01 —— 用户标记后批
- `pkg/synctest/` 新包 / `no-sleep-migration.md` 文档 —— 简洁优先
- `update-generated.sh` 配对脚本 —— `make generate` 已存在
