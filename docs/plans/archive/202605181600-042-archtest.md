# [SUPERSEDED] Arch test

> **2026-05-25 归档**：全部任务已 ship。
>
> | 项 | 终态 |
> |---|---|
> | 1.1 TEST-POLLING-DETERMINISM testwait | ✅ PR1 framework（`pkg/testutil/testwait/`）+ PR2 #840 迁移 + PR3 refactor/653 enforcement（`TEST-EVENTUALLY-FUNNEL-01`）全 DONE;全仓裸 `Eventually` 实际调用=0 |
> | 1.1 上游 Hard funnel | → #739（flag-cond，下游已 ship + 上游 Hard 追踪，funnel 双向锁） |
> | 1.2 FindFirstChild typed API | ✅ DONE（`scanner/eachnode.go:148`） |
> | 1.2 残留 FindFirstInSubtree | → #721（API+迁移已 ship 16 callsites / ~50+→28;剩 USAGE-02 enforcement 扩展 trigger-gated，保持 OPEN） |
>
> 新工作走 GitHub Issues（#739 / #721）。以下为归档前原文。

# Arch test

## 0. Template-PR-Split

framework + bulk migration + archtest enforcement 三维以上必拆 N 个 PR：第一个 PR = framework，最后一个 PR = archtest enforcement（enforcement 上线时所有站点已合规）。单 PR 多维捆绑会让 type-aware 边界多维 fail-open 缝隙同时暴露。

- PR1（framework）：新建 typed API + 单点 dogfood
- PR2（bulk migration）：已有站点批量迁移
- PR3（archtest enforcement）：拦裸调用形态 + 删旧 allowlist

---

## 1. Wave 1

### 1.1 TEST-POLLING-DETERMINISM typed marker

裸 `(require|assert).Eventually` + closure+done sentinel 轮询散落 ~339 站点 / ~70 文件，race CI flake（D500ms 踩穿为首例事故）。

**形态固定**：新建 `pkg/testutil/testwait/` —— `External(reason)` typed marker（保留真 wall-clock 等待）+ `Deterministic` 收纳 channel-as-condvar；一锅改造 339 站点；同 PR archtest 拦裸 `(require|assert).Eventually`；不 ratchet。

按 Template-Wave4-3PR 拆：

- **PR1 framework**（4-6h）：`pkg/testutil/testwait/` typed API + 单点 dogfood — **DONE**
- **PR2 bulk migration**（12-16h）：393 站点批量迁移 — **DONE (#840)**
- **PR3 enforcement**（2-3h）：archtest `TEST-EVENTUALLY-FUNNEL-01` 拦裸 `(require|assert).Eventually` / `*WithT` 并清 MIGRATION-NOTE 残留；ai-robust.md 晋升 Hard 范本 — **DONE (refactor/653)**

合计 ~18-25h dev（一锅推估 24-32h）。每 PR review 面积 ≤ 单锅推一半，fail-open 缝隙按维度暴露。

> **PR3 范围说明**：原文 "拦 closure+done sentinel + 删旧 allowlist" 在 backlog `TEST-EVENTUALLY-FUNNEL-01` 中已收敛为 "ban bare `(require|assert).Eventually(WithT)?`"。closure+done sentinel 在测试中与合法 `<-ctx.Done()`/`<-done` 通道等待无可靠结构差，无具体 callsite 计数，未纳入；改由触发型 backlog `TEST-CLOSURE-DONE-SENTINEL-FUNNEL-01`（`docs/backlog/20260520/cap-14-tooling.md`）追踪，触发条件 = 首次出现具体绕过实例 / 下次 race CI flake 根因追溯到该形态。旧 allowlist 在 PR2 后即已不存在，本 PR 仅清理 4 处 `MIGRATION-NOTE` 注释残留。

**触发**：flake-density gate —— 第 2 次 race CI flake / 进入下一 AI-robust 升级批 / 339 站点任一新增违反，任一即立项。

### 1.2 FINDFIRSTCHILD-TYPED-API-01

**形态固定**：`tools/archtest/internal/scanner` 加 `FindFirstChild[S any, N interface{ *S; ast.Node }](root ast.Node, pred func(N) bool) (N, bool)` typed find-first API（语义 = depth=1 + early-stop = typed function choice）；迁移 6 处 closure+done helper（`outbox_invariants_test.go` ×4 / `security_defaults_test.go::hasKey` / `contract_spec_clients_test.go` ×2）；删 sentinel 内联注释 + 删 ai-robust.md "closure+done family" 提及。

depth-1 部分已 ship。**单 PR + commit-level 分维度例外**（6 处 helper 总改动 < 200 LOC，review 面积可控）：framework + 一个 dogfood 先行 commit → 5 处迁移 + archtest 同 commit；3-4h dev / 1h review。

**触发**：第 7 处 closure+done sentinel helper 出现 / 进入下一 AI-robust 升级批。

**残留（subtree 版，正交轴）**：`EachInSubtree` + 早返 sentinel（递归 find-first）仍散落约 50+ 站点，depth-1 的 `FindFirstChild` 不覆盖。立 `scanner.FindFirstInSubtree[N]` + 扩 USAGE-02 覆盖 EachInSubtree 早返子集。详 backlog `FINDFIRSTINSUBTREE-API-01`（cap-14-tooling.md，P2/Cx2，触发：第 N 处 EachInSubtree 早返 sentinel）。

---

## 2. 引用

- backlog 锚点：`FINDFIRSTINSUBTREE-API-01`（`docs/backlog/cap-14-tooling.md`）
- AI-robust 治理章程：`.claude/rules/gocell/ai-robust.md`
