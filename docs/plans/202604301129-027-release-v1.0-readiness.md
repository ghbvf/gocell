# 027 · v1.0 发布前 PR 待办清单

> 日期: 2026-04-30 11:29
> 基线: `origin/develop @ 0f8f737e`（最新合入 PR#337）
> 来源: 全量盘点 `docs/backlog.md` + `backlog1.md` + `backlog2.md` + `docs/plans/202604252100-026-post-v1.0-cleanup-plan.md` + `docs/plans/202604260058-l4-virtual-taco.md` + `docs/plans/202604272358-2-2-ci-batch2-k8s-verify.md`，按"是否阻塞 v1.0 GA + PG 接入纳入"两个维度合并去重。
>
> 决策记录:
> - **PG 接入纳入 v1.0**（用户 2026-04-30 决策）—— X1 PG-DOMAIN-REPO 主线 + 触发条件项 + backlog2 §5.1 PG adapter 12 条硬伤一并打包
> - **Qodana trust boundary**（PR332-QODANA）✅ 已由 #337 关闭（移除 `pull_request` 触发）—— 不再阻塞
>
> 范围:
> - **32 个 PR** / 实际 ~38-50 工作日（含 review 轮次 + bug fix buffer）
> - P0 阻塞 25 PR（A-I 段，~25-32d） + CI/工程基线 7 PR（J 段，~13-19d）
> - 排除明确 v1.1+ 的项（详见 §四 排除清单）

---

## §一 · v1.0 发布前 PR 待办（合并 32 PR：P0 阻塞 25 + CI/工程基线 7）

### A. 安全 / fail-closed（4 PR / ~5d）

| # | PR 名 | 关闭项 | 工时 | 文件域 |
|---|---|---|---:|---|
| 1 | **PR-V1-SEC-WS-AUTH-ACL** | B2-W-01 Upgrade auth + B2-W-02 Broadcast ACL + B2-W-06 集成文档（合一 PR，hub 改造同时） | 2.5d | `adapters/websocket/handler.go` + `runtime/websocket/hub.go` + `docs/guides/websocket-integration.md`（新） |
| 2 | **PR-V1-SEC-SETUP-CLOSURE** | B2-C-02 setup 端点迁 internal listener 或 bootstrap token（A26-R2 升级，rate-limit 不根治） | 1d | `cells/accesscore/cell_routes.go` + `slices/setup/handler.go` + `contracts/http/auth/setup/admin/v1/contract.yaml` |
| 3 | **PR-V1-SEC-WRAPPER-REDACTOR** | PR-SEC-1 残尾 `SEC-WRAPPER-REDACTOR-DEFAULT-01`（kernel/wrapper 默认最小脱敏） | 3h | `kernel/wrapper/consumer.go` + `lifecycle.go` |
| 4 | **PR-V1-RMQ-TERMINAL** | B2-A-02 RMQ permanent error 分类（403/404/530 → close → readyz 503，不再无限重连） | 1d | `adapters/rabbitmq/connection.go::reconnectLoop` |

### B. 数据完整性 / 幂等（2 PR / ~2.5d）

| # | PR 名 | 关闭项 | 工时 | 文件域 |
|---|---|---|---:|---|
| 5 | **PR-V1-DATA-OUTBOX-FENCING** | B2-A-01 PG outbox claim 加 `lease_id`/fencing token + ADR | 1d | `adapters/postgres/outbox_store.go` + ADR |
| 6 | **PR-V1-DATA-HASHCHAIN-RECOVERY** | B2-C-01 auditcore 启动从 repo 恢复尾节点 + B2-C-14 跨重启 continuity 测试 | 1.5d | `cells/auditcore/internal/domain/hashchain.go` + `cell.go` + integration test |

### C. 业务语义补齐（2 PR / ~2.5d）

| # | PR 名 | 关闭项 | 工时 | 文件域 |
|---|---|---|---:|---|
| 7 | **PR-V1-BIZ-LOCKOUT** | `ACCESSCORE-ACCOUNT-LOCKOUT-AUTO-LOCK-01`（含 PG users.failed_attempts schema + migration + outbox + lock 事件） | 1d | `cells/accesscore/slices/sessionlogin/` + `internal/domain/user.go` + user PG repo + migration + tests/integration/ |
| 8 | **PR-V1-TEST-JOURNEY-ROOT** | `TEST-JOURNEY-ROOT-HARNESS-01`（J-useronboarding 优先；J-accountlockout 由 #7 补 harness） | 1-1.5d | `tests/integration/` + `journeys/J-*.yaml` |

### D. 契约执行闭环（1 PR / ~1.5d）

| # | PR 名 | 关闭项 | 工时 | 文件域 |
|---|---|---|---:|---|
| 9 | **PR-V1-CONTRACT-INTEGRITY** | backlog1 §2.2 残 3 子项打包：`CONTRACTTEST-SCHEMAREF-FAILFAST-01` + `CONTRACT-ENDPOINT-TEST-MAPPING-01` + `CONTRACT-PATH-QUERY-EXECUTABLE-01` | 1.5d | `pkg/contracttest/` + `kernel/governance/` + 各 active HTTP contract test |

### E. PG 接入主线（**8 PR / ~12-15d**，用户决策已纳入）

| # | PR 名 | 关闭项 | 工时 | 依赖 |
|---|---|---|---:|---|
| 10 | **PR-V1-PG-DOMAIN-REPO**（X1 主线） | 5 域 Repository（User/Session/Role/Device/Command）+ 4 migration DDL + `CONFIG-VERSIONS-MIGRATION-01` + 联动 RBAC-ASSIGN-LEVEL-UPGRADE / SEED-ROLE-IFACE / ACCESS-LEVEL-AUDIT / AUTH-CACHE 激活 | 3-5d | — |
| 11 | **PR-V1-PG-MULTIPOD-SETUP**（026 PR-A56） | A26-R1 ADMINPROVISION-DIST-LOCK + A26-R4 SETUP-ORPHAN-E2E | 5h | #10 |
| 12 | **PR-V1-PG-MIGRATOR-HARDEN** | `POSTGRES-MIGRATOR-LOCK-ORDER-REGRESSION-01`（goose advisory locker + 并发 Up + 9/10 顺序 fixture） | 3-4h | — |
| 13 | **PR-V1-PG-REFRESH-CROSS-STORE** | `AUTH-REFRESH-CROSS-STORE-ATOMICITY-01`（session repo + refresh store 共 PG TxRunner，整个 rotate 链 ACID） | 4-6h | #10 |
| 14 | **PR-V1-PG-OUTBOX-RELAY-HARDEN**（B2-C1） | B2-A-04（observability 注入顺序）+ B2-A-05（RowsAffected==0 处理）+ B2-A-06（reclaim LIMIT）+ B2-A-07（metadata 字节上限）+ B2-A-12（schema_guard qualified name） | 8h | — |
| 15 | **PR-V1-PG-REFRESH-READYZ**（B2-C2） | B2-A-08（refresh tx 拆双接口）+ B2-A-09（timing sidechannel 收口）+ B2-A-10（readyz 聚合 schema_guard）+ B2-A-11（constructor error-first）+ B2-A-13（tx rollback 日志脱敏） | 12h | #10 |
| 16 | **PR-V1-PG-CONNECT-STRICT** | B2-A-18 ADAPTER-CONNECT-TIMEOUT（PG + RMQ）+ B2-X-03 invalid index → strict fail-fast / readyz 联动 | 4h | — |
| 17 | **PR-V1-REFRESH-IDLE-GRACE** | X12 REFRESH-IDLE-EXPIRE + X14 REFRESH-GRACE-COUNTER（X13 分区延 v1.1） | 5h | #10 |

### F. Redis Cluster + 多 cell metrics（2 PR / ~1.5d）

| # | PR 名 | 关闭项 | 工时 | 触发 |
|---|---|---|---:|---|
| 18 | **PR-V1-REDIS-CLUSTER** | B2-A-03 ModeCluster + DistLock/Idempotency 算法 cluster slot 评估（hashtag） | 8h | 生产用 ElastiCache/Azure Cache Cluster 时强制 |
| 19 | **PR-V1-METRICS-CELL-LABEL** | R2-FOLLOW HTTP-METRICS-LABEL-REALIGN（多 cell assembly label 误归因）+ T6 GOCELL-PER-CELL-ADAPTER（按 cell 拆 env） | 6h | 多 cell assembly 部署前 |

### G. 文档 / 部署（1 PR / ~6h）

| # | PR 名 | 关闭项 | 工时 |
|---|---|---|---:|
| 20 | **PR-A58 / PR-V1-DOCS-MULTIPOD** | dual-listener 部署指南（PR237-PM5）+ examples 注释（PR237-PM7）+ Docker compose healthcheck（PR284-FU-COMPOSE-HEALTH）— WS 文档已合到 #1 | 6h |

### H. 治理 / CI 必收（4 PR / ~12-13d，与 PG 主线并行）

| # | PR 名 | 关闭项 | 工时 |
|---|---|---|---:|
| 21 | **PR-CFG-I** AUTH-FAIL-CLOSED-AND-OPS-RESIDUE | I.X1 NonceStore fail-closed + nil ring fail-fast + ErrAuthNonceStoreFull(503) + I.X3 ensureCursorCodec real 模式禁固定密钥 + e2e 等待 readyz + sessionlogin double-cleanup | 14h |
| 22 | **PR-CI-2** SHELL-SAFETY-01 | `seed-test-data.sh` 加 `set -euo pipefail` + verify gate | 1h |
| 23 | **PR-CI-3** V1-RESPONSE-EVOLVE | ADR + FMT-20 改语义 + 30 schema 顶层放宽 `additionalProperties` | 13h |
| 24 | **PR-CI-4** TEST-NO-SLEEP-01 | 97 处测试 sleep 一次性消化 + archtest 零容忍 | 28h |

### I. 最后 solo（1 PR / ~6h）

| # | PR 名 | 关闭项 | 工时 |
|---|---|---|---:|
| 25 | **PR-A64** LINT-MODERN-DEAD-VAR | X9 modernization + DEAD-VARIABLE-01 + V-A6 `nhooyr.io→coder/websocket` + V-A7/V-A8/V-A5 | 6h |

### J. CI / 工程实践基线（**7 PR / ~13-19 工作日**，SemVer 边界 + 外部认知锚定）

> 来源：`docs/plans/ci-governance/202604300430-golangci-tier12-priority-and-projection.md` Batch 1+2 + `docs/plans/engineering-baseline/202604300600-radical-lightweight-revision.md` E1/E2/E5/E14 + 原 L1。
>
> **入选准则**：(a) 破坏 public API 的减重必须 v1.0 前做（否则要等 v2）；(b) 0 代码的认知锚定立即做；(c) GA 必须有的供应链扫描。E3/E4/E6/E8/E9/E10 codegen 工具链 + E7-3/4/5 + CI Batch 3/4 推 v1.0 后（增量不破 SemVer）。

| # | PR 名 | 关闭项 | 工时 | 性质 |
|---|---|---|---:|---|
| 26 | **PR-V1-POSITIONING-AND-LIBS** | engineering-baseline E5 `docs/architecture/positioning.md`（300-500 字，校正"重框架，强结构化，不引 IoC 反射"）+ README elevator pitch + E14 README 加「GoCell 自带通用库清单」（Tier A 5 包 + Tier B 20 包，共 25 个独立可用包） | 4-6h | 0 代码，立即做 |
| 27 | **PR-V1-CI-LINT-BATCH-1** | ci-governance Batch 1：`.golangci.yml` 启 `goimports + misspell + unconvert` + 修光全仓 finding（预期 < 100 条） | 4-8h | 零风险开局 |
| 28 | **PR-V1-CI-LINT-BATCH-2A** | ci-governance Batch 2.1：启 `depguard`（编码 CLAUDE.md 五层依赖规则 kernel/cells/runtime/adapters/pkg → 静态守）+ `forbidigo`（把 CLAUDE.md 文字约束变静态门：`fmt.Println` / `log.Printf` / `errors.New` 对外 / `_ = func()` / `time.Sleep` 在测试 / `panic` 业务路径）+ 同步 CLAUDE.md「依赖规则」 + 检查 archtest 是否能简化（移除已被 depguard 覆盖的包级黑名单） | 1-2d | GoCell 最大结构性收益 |
| 29 | **PR-V1-CI-LINT-BATCH-2B** | ci-governance Batch 2.2：启 `errorlint`（强制 `%w` wrap + `errors.Is/As`）+ `bodyclose`（防 OIDC/S3/webhook HTTP body 泄漏）+ `durationcheck`（量纲错误） | 1-2d | 行为/资源/时间收口 |
| 30 | **PR-V1-SUPPLY-CHAIN** | engineering-baseline L1 / E7-1：新增 4 个 workflow —— `security-vuln.yml`（govulncheck 全仓）+ `security-static.yml`（Semgrep `p/golang` + CodeQL SARIF 上传）+ `test-race.yml`（独立 race job）+ CLAUDE.md 加「供应链安全」章节列已启用 scanner | 1-2d | GA 必须 |
| 31 | **PR-V1-API-CONTRIB-COLLAPSE** | engineering-baseline E2：删 `RouteGroupContributor / EventRegistrar / HealthContributor / LifecycleContributor / ConfigReloader` 5 接口 → 单一 `cell.Registry` API（`Routes / Subscribe / Health / OnStart / OnStop / OnConfigReload`）；删 `kernel/cell/registrar.go`（500+ 行）+ bootstrap phase3b/5/6 type assertion 自动发现循环；新增 `kernel/cell/registry.go` + bootstrap phase3 调 `cell.Init(ctx, reg)`；全 cell 迁移（accesscore / auditcore / configcore / examples）；IoC 比例 25-30% → 10-15% | 5-7d | **breaking，最大 IoC 杠杆，必须 v1.0 前** |
| 32 | **PR-V1-API-VOCABULARY-COLLAPSE** | engineering-baseline E1：二级词汇收口（HandleResult 三态 `Ack/Requeue/Reject` 替代 5 词幂等模型；藏 `Claimer/Receipt/ClaimState/SubscriberIntakeStopper` 入 kernel internal；删 `NoopTxRunner/RunnerOrNoop/WrapLegacyHandler` 命名）；全 L2 consumer 切签名；`kernel/outbox` 公开 API 简化 ~30% | 3-5d | **breaking，必须 v1.0 前** |

**Batch J 推荐顺序**：

```
Sprint 起步（与 P0 主线并行）：
  PR #26 POSITIONING-AND-LIBS    (4-6h, 0 代码 0 阻塞，立即做)
  PR #27 CI-LINT-BATCH-1         (4-8h, 零风险开局)
  PR #30 SUPPLY-CHAIN            (1-2d, workflow 独立)

中段（depguard 守 layering 是后续 cell 重构的护栏）：
  PR #28 CI-LINT-BATCH-2A        (1-2d, depguard + forbidigo)
  PR #29 CI-LINT-BATCH-2B        (1-2d, errorlint + bodyclose + durationcheck)

后段（API 减重，与 PG 主线 #10 串行避免 rebase 冲突）：
  PR #31 API-CONTRIB-COLLAPSE    (5-7d, breaking)
  PR #32 API-VOCABULARY-COLLAPSE (3-5d, breaking)
```

**关键耦合**：
- PR #28 depguard 必须先于 PR #31/#32 合并（守 layering 防 contributor 重构期间引入分层漂移）
- PR #31 必须先于 PR #32（Registry API 形状决定 HandleResult 在哪暴露）
- PR #31/#32 与 PG 主线 #10 PR-V1-PG-DOMAIN-REPO **强烈建议串行**（两者都改 cell.go，并行 rebase 痛苦）

---

## §二 · 工时与并行建议

| 批次 | PR 数 | 工时合计 | 并行性 |
|---|---:|---:|---|
| A 安全 + B 数据 + C 业务 + D 契约 | 9 | ~12d | **可 4 路并行**（文件域不重叠：runtime/auth-ws / kernel-outbox / cells/auditcore / cells/accesscore） |
| E PG 主线 | 8 | ~12-15d | **#10 串行前置**；#11/#13/#14/#15/#17 在 #10 合后多路并行；#12/#16 独立可全程并行 |
| F Redis + Metrics | 2 | ~1.5d | 与 E 并行 |
| G 文档 | 1 | ~6h | 与 H 一起放最后 |
| H 治理 / CI | 4 | ~12-13d | **PR-CI-4 单 PR 28h 是最大单点**；可与 E 并行 |
| I lint solo | 1 | ~6h | 主线 PR 全合后做 |
| **J CI / 工程基线** | 7 | ~13-19d | **#26/#27/#30 立即起**；#28 depguard 必须先于 #31/#32；#31/#32 与 PG 主线 #10 强烈建议**串行**（同改 cell.go） |

**关键路径**：`#26/#27/#30 立即起 → #28 depguard → #10 PG-DOMAIN-REPO (3-5d) → #11/#13/#14/#15 → #31 API-CONTRIB-COLLAPSE (5-7d) → #32 API-VOCABULARY-COLLAPSE (3-5d) → #25 lint solo`，约 **30-38 工作日**（理论极限）。

**实际工期**：含 review 轮次 + bug fix buffer + 跨 PR rebase 成本（尤其 #31/#32 改全 cell.go，与 #7 BIZ-LOCKOUT 同时改 accesscore），建议按 **38-50 工作日** 排期。

---

## §三 · 推进顺序（推荐 sprint 切分）

### Sprint 0（认知锚定 + 守护建立，~3d，立即起）

```
0 阻塞 / 立即可起的 3 路：
  Lane A: PR #26 POSITIONING-AND-LIBS         (4-6h, 0 代码)
  Lane B: PR #27 CI-LINT-BATCH-1              (4-8h, 零风险)
  Lane C: PR #30 SUPPLY-CHAIN                 (1-2d, workflow 独立)
```

### Sprint 1（解锁 + 起 PG 主线 + 分层守护，~5-6d）

```
并行起 5 路：
  Lane A: PR #1 PR-V1-SEC-WS-AUTH-ACL          (2.5d)
  Lane B: PR #5 PR-V1-DATA-OUTBOX-FENCING      (1d) → PR #6 HASHCHAIN-RECOVERY (1.5d)
  Lane C: PR #10 PR-V1-PG-DOMAIN-REPO          (3-5d, 关键路径前置)
  Lane D: PR #21 PR-CFG-I + PR #22 CI-2        (1.5d)
  Lane E: PR #28 CI-LINT-BATCH-2A depguard     (1-2d, 守 layering 防后续重构期漂移)
       → PR #29 CI-LINT-BATCH-2B              (1-2d, 行为/资源/时间)
```

### Sprint 2（PG 主线发散 + 业务/安全收口，~5d）

```
依赖 #10 合并后起：
  Lane A: PR #11 MULTIPOD-SETUP + PR #12 MIGRATOR        (1d)
  Lane B: PR #13 REFRESH-CROSS-STORE + PR #15 REFRESH-READYZ  (2.5d)
  Lane C: PR #14 OUTBOX-RELAY-HARDEN + PR #16 CONNECT-STRICT  (1.5d)
  Lane D: PR #2 SETUP-CLOSURE + PR #3 WRAPPER-REDACTOR        (1.5d)
  Lane E: PR #4 RMQ-TERMINAL + PR #7 BIZ-LOCKOUT              (2d)
```

### Sprint 3（业务收尾 + CI 大件，~5d）

```
  Lane A: PR #8 TEST-JOURNEY-ROOT          (1.5d)
  Lane B: PR #9 CONTRACT-INTEGRITY         (1.5d)
  Lane C: PR #17 REFRESH-IDLE-GRACE + PR #18 REDIS-CLUSTER + PR #19 METRICS-CELL-LABEL  (2.5d)
  Lane D: PR #23 CI-3 V1-RESPONSE-EVOLVE   (2d)
  Lane E: PR #24 CI-4 TEST-NO-SLEEP        (4d, 最大单点)
```

### Sprint 4（API 减重 + 发布收尾，~10-13d）

```
  Lane A: PR #31 API-CONTRIB-COLLAPSE   (5-7d, breaking 5→1 contributor，必须 v1.0 前)
       → PR #32 API-VOCABULARY-COLLAPSE (3-5d, breaking HandleResult 收口)
  Lane B: PR #20 PR-A58 RELEASE-DOCS-MULTIPOD  (1d)
  Lane C: 6 角色 retrospective + bug fix buffer
  Lane D: PR #25 PR-A64 LINT solo（最后做）    (1d)
  Tag v1.0
```

> **Sprint 4 风险点**：PR #31/#32 是 v1.0 内最大单 PR（5-7d + 3-5d），改全 cell.go + bootstrap phase 重写 + outbox 公开 API 简化。建议在所有其他 PR 合入后再起，避免 rebase 灾难。

---

## §四 · 排除清单（明确推 v1.1+ 或永久 won't-do）

| 编号 | 原因 |
|---|---|
| **X13** REFRESH-PARTITION-01 | PG range 分区，等生产流量达阈值后做 |
| **B2-C-13** L2-CROSS-LAYER-E2E-REGRESSION | 由 #8 TEST-JOURNEY-ROOT harness 顺带覆盖 |
| **B2-T-06 / B2-T-07** SERVICE-TOKEN-CALLER-IDENTITY | Wave G 决策延 v1.1（设计需 service mesh 配合） |
| **PR280-FU1** CHANGEPASSWORD-CONCURRENT-SEMANTICS | 客户反馈触发，当前 OUT_OF_SCOPE 共识 |
| **PR-A8-RESIDUAL** Vault K8s auth 真实 e2e | 部署目标平台明确后做 |
| **OBS-SSA-ANALYZER-01** | 当前 typed archtest 已够；SSA 留给跨包 taint 真实需求出现时 |
| **S14a** CONFIG-VALUE-KMS-AWS-PROVIDER | 明确部署目标云平台后触发 |
| **T1-T9** 触发条件项（backlog §触发条件项） | 触发条件未达成 |
| **025 plan Wave 4** 长期债（PR-A21/A22/A23/A24/A33） | 不在 v1.0 范围 |
| **engineering-baseline E3 / E4** Codegen 全栈 + marker 真相源迁移 | 不破坏代码 API（仅破坏 yaml 写法），可在 v1.0 后增量切（保留旧 yaml 兼容路径） |
| **engineering-baseline E6** `gocell visualize` 工具 | 增量工具，不破 API |
| **engineering-baseline E8** Bootstrap 显式化（phase 拆 8 函数） | 增量（`bootstrap.Run()` 保留），可推 |
| **engineering-baseline E9** 一键 scaffold + 项目模板 | 增量工具，不破 API |
| **engineering-baseline E10** contract → DTO/iface 全自动 codegen | 增量 codegen，不破 API |
| **engineering-baseline E7-3 / E7-4 / E7-5** 错误库 PII / API 治理 / 文档自动化 | 增量装备期，可推 |
| **engineering-baseline E11 / E12 / E13** errcode 收敛 / Actor 合并 / assembly.yaml 极简 | 评估候选（E12 不建议做） |
| **ci-governance Batch 3** gosec + revive | 存量大（百条级 finding），多 PR 切片分散 review 资源 |
| **ci-governance Batch 4** gofumpt + gocritic | 美化项 |

---

## §五 · 验证矩阵（每批 PR 合并后必跑）

```bash
# 每个 PR 合并前
make verify
go build ./... && go test ./... -count=1 -race
golangci-lint run ./...
go run ./cmd/gocell validate --strict

# Sprint 末尾（PG 主线就绪后）
docker compose -f tests/e2e/docker-compose.e2e.yaml up -d
go test -tags=e2e ./tests/e2e/... -count=1
docker compose -f tests/e2e/docker-compose.e2e.yaml down -v
go test -tags=integration ./tests/integration/l2_atomicity/... -count=1
go test -tags=integration ./adapters/postgres/... -count=1

# v1.0 tag 前
gocell verify journey --id=J-useronboarding --strict
gocell verify journey --id=J-accountlockout --strict
gocell verify cell --strict   # 全 cell
gocell verify slice --strict  # 全 slice
```

---

## §六 · 进度跟踪规则

每个 PR 合并后在本文件 §一 表中对应行追加 `✅ #PR# (日期)` 标记 + 简短落地说明；累计 5 PR 合并后在头部追加"轮次更新"段落。

**所有 25 PR 合并 + v1.0 tag 完成后**，本 plan 归档到 `docs/plans/archive/`，并在 `docs/backlog.md` 头部更新 v1.0 GA 节点。

---

## §七 · 与既有 plan 的关系

| 既有 plan | 本 plan 影响 |
|---|---|
| `docs/plans/202604252100-026-post-v1.0-cleanup-plan.md` | 主线 PR-A48 ✅ #334 / PR-A64 ⬜（本 plan #25）；Wave 9 PR-A56-58 → 本 plan #11 / #20；Wave 10 PR-A59-60 不在 v1.0 范围（command queue PG 触发条件未达成） |
| `docs/plans/202604260058-l4-virtual-taco.md` | 仅剩 PR-CFG-I（本 plan #21）；其余已全合 |
| `docs/plans/202604272358-2-2-ci-batch2-k8s-verify.md` | 剩余 PR-CI-2/3/4（本 plan #22/#23/#24） |
| `docs/plans/ci-governance/` | Batch 1+2（goimports/misspell/unconvert + depguard/forbidigo/errorlint/bodyclose/durationcheck）→ 本 plan #27/#28/#29；Batch 3 gosec/revive + Batch 4 gofumpt/gocritic 推 v1.0 后 |
| `docs/plans/engineering-baseline/` | E1 / E2 / E5 / E14 / E7-1（L1）→ 本 plan #26/#30/#31/#32；E3/E4/E6/E8/E9/E10 + E7-3/4/5 + E11/E13 推 v1.0 后；E12 不建议做 |
| `docs/backlog.md` | 本 plan 覆盖的 backlog 行在 PR 合并时同步追加 ✅ 标记；其余 P2/P3 按原优先级在本 plan 之外推进 |
| `docs/backlog1.md` | PR-SEC-1（本 plan #3 关尾） + PR-CONTRACT-INTEGRITY（本 plan #9 关尾） |
| `docs/backlog2.md` | §1 P0 全部覆盖（B2-A-01/02/03 + B2-W-01/02 + B2-C-01/02 + B2-W-06）；§5.1 PG 12 条由 #14/#15/#16 吸收；§6 WS 由 #1 吸收；§3 OTel 加固延 v1.1 |
