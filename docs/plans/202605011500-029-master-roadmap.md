# 029 · GoCell Master Roadmap（替换 027 + 028）

> 日期: 2026-05-01 15:00（last update: 2026-05-08 — **PR#385/#386/#387 + ADR M0 全合规**：(a) **PR#385 PR-V1-CIRCUITBREAKER-INHOUSE-ERRCODE ✅**（df6b5448）— W1 A-05 errcode 收口 + clock 构造期注入（D6 PROD-CLOCK-INJECTION-01）+ **删 sony/gobreaker/v2 依赖换自研 generation+expiry 状态机** + 7 branch tests + FIND-002/004/005/007 闭口；(b) **PR#387 [M0-FOUNDATION] poolstats descend to kernel ✅**（dc306199）— `runtime/observability/poolstats` 整包 mv 到 `kernel/observability/poolstats` + 11 处 import 替换 + archtest `KERNEL-POOLSTATS-LOCATION-01a/01b`；ADR §M0-FOUNDATION 4 项全合规（poolstats=PR#387 / Noop=`Nooper`+`CheckNotNoop` superseded / CellMetadata=单源 + `CELLMETA-SINGLE-SOURCE-01` + K#04/K#05 / adapters API 包装=`AMQPConnection` interface + pgx via cell-side interface 实质完成）；ADR 残余边界点 `adapters/websocket/NewConn(*websocket.Conn)` 改 unexported 折入 D7（8h+4h → 9h+4.5h）；(c) **PR#386 06.FU3 PR-V1-ARCHTEST-AST-FULL-AUDIT ✅**（f6edea47）— Wave 1 RED 6 fixtures + 7 negative tests / Wave 2 GREEN: F6 param-scoped block-binding + 全量审查 `metadata.NewParser(root)` 单根扫 + 6 archtest 全迁；(d) `674496d1` fix(test) — auth invalid-MAC + websocket fake-clock 稳定化 + RabbitMQ conformance readiness FU 登记。上轮 — **Track B 加 RMQ 三 PR**：B12 PR-V1-RMQ-LIFECYCLE-HARDEN（B2-A-14/15/16，10h+5h）+ B13 PR-V1-RMQ-CONFORMANCE-AND-CLOSURE（B2-A-17 + B2-A-18 RMQ 半 + PR#379 review FU×2，7h+3.5h）+ B14 PR-V1-RMQ-TEST-CLEANUP（PR225-N1 + PR333-RMQ-CLOSE-FLAKE，2.5h+1.5h）；配套：B8 范围收窄为纯 PG（4h+2h → 3h+1.5h，B2-A-18 RMQ 半剥离），B2-A-18 拆双归宿，backlog2 §6 `PR-B2-C3` 聚合行废弃；新增 backlog2 `B2-A-29 RMQ-PR379-FU-DOC-DRIFT` + `B2-A-30 RMQ-SUBSCRIBER-TERMINAL-PROPAGATION-TEST` 两条 PR#379 review FU；Track B 总数 11 PR → 14 PR / ~111h+57h → ~131h+67h。上轮 — **PR#384 N8 PR-V1-OUTBOX-FU-CLOSURE ✅** 已合入 origin/develop（4674e214）：(a) DB CHECK `outbox_claiming_requires_lease` (migration 015) + 删 `VerifyOutboxLeaseInvariant` probe + errcode + corebundle 调用（DB 单源化 lease 不变量）+ ADR cutover 段重写；(b) `kernel/outbox.ConsumerBase` 加 unexported `built` sentinel + `IsConstructed()` 接口，`outbox.WithConsumerBase` / phase6 wiring fail-fast 拒 `&ConsumerBase{}` 字面量；(c) `Subscription.Validate` 加 ContractID/Kind/Transport 三字段非空校验单源，`NewContractTracingSubscriber` 改返 error，删 `wrapper.MustWrapSubscriber` 整函数；(d) `adapters/rabbitmq/subscriber.go` commit_failed 路径 release-first（先 Release 再 Nack(requeue=true)），与 eventbus 顺序对齐 + testcontainers 集成测试；(e) `tools/archtest/migration_no_transaction_rerun_safe_test.go` 强制 NO TRANSACTION migration DDL 必带 IF NOT EXISTS / IF EXISTS / DO 块；新增 P2 #2 phase6 wiring 拒裸字面量、P2 #3 014/015 negative integration tests；关闭 K#12 + B1 全部残余 FU + won't-do `B2-X-09` 触发条件等待中。上轮 — **§B'' PR#376 06.PR4 review 闭口分发** — origin/pr/376@1b7585a 独立审查 6 finding（2 P1 + 4 P2）按文件域分发到既有 5 PR：A2 升 P0 吸收 F1+F2（10h+5h）/ 06.FU 范围扩吸收 F4+F3(doc.go)（17h+8.5h）/ 06.FU3 升格已调度吸收 F6（16h+8h）/ F1 微扩吸收 F3(README+cell-guide)（6h+2.5h）/ F4 微扩吸收 F5（9h+4.5h）；工时增量 10h+5.5h，无独立 PR；bootstrap admin 路线决议 = bootstrap credential via env（Keycloak/Grafana/MinIO 三方对标），不走 internal listener，不需 B3 配套；详见 §B''。上轮 — **PR#376 06.PR4 PR-V1-CODEGEN-FULL-MIGRATION ✅**（方案 D 末段 W1-W4：W1 contractgen 给 event contract 多产 spec_gen.go + subscription_gen.go / W2 64 contract 加 codegen:true 全量 regen + CH-04/05 修 + 删 dead `event.flag.changed.v1` / W3 4 archtest gates GREEN with empty allowlist (CELLS-NO-WRAPPER-CONTRACTSPEC-IMPORT-01 + NO-MANUAL-CONTRACTSPEC-LITERAL-01 + EVENT-SUBSCRIPTION-CONTRACTGEN-COVERAGE-01 + CONTRACT-KINDS-CLOSED-SET-01) + 删 `wrapper.EventSpec` + FMT-18 / W4 K-06 残余 configcore consistencyLevel:L2 + scaffold golden test；同时 06.FU3 PR-V1-ARCHTEST-AST-FULL-AUDIT 触发条件项登记；实测 44h+21h vs 原估 30h+15h；ref: controller-tools genall + oapi-codegen strict + go-zero genroutes）+ **PR#383 N3 PR-V1-030-G02-ROLLBACK-CTX-DECOUPLE ✅** — `rollbackCells` 派生 fresh ctx with `HookTimeout` + `phase0ValidateOptions` warn `terminationGracePeriodSeconds >= shutdownTimeout + preShutdownDelay + 10s`，关闭 030 G-02 / backlog2 B2-R-03 / backlog1 §2.4 STARTUP-ROLLBACK-ERR-JOIN（部分相关 — G-02 解决 ctx 派生问题；errors.Join 仍待）。上轮 — **PR#382 B10 PR-V1-REDIS-CLUSTER ✅** — `ModeCluster` + `ClusterAddrs` + `buildClusterOptions`（URL/plain mix + TLSConfig + 凭据合并 + fail-fast）+ `idempotency` `{key}:lease`/`{key}:done` hashtag（CRC16 colocate 单 slot 避 EVAL CROSSSLOT）+ `corebundle` `GOCELL_REDIS_CLUSTER_ADDRS` env + cluster build tag integration + archtest `IDEMPOTENCY-LUA-HASHTAG-01`，关闭 B2-A-03 AWS/Azure Cluster 接入阻塞。同轮 **PR#381 A1 PR-V1-SEC-WS-AUTH-ACL ✅** — WebSocket Authenticator + BroadcastFilter/BroadcastToSubject + token expiry (ExpiresAt) + slow-client evict + subjectIdx O(1) fanout + writeLoop select-default-drop + websocket-integration.md，关闭 B2-W-01/02/06；ref: olahol/melody + centrifugal/centrifuge + gorilla/websocket + coder/websocket；新增 SEC-07/08/09 archtest；3 文件 +2430/-208 LOC；round-3 review fix：401 vs 403 RFC 9110 status code 派生 + token expiry RFC 7519 `!After(now)` 边界。上轮 2026-05-05 深夜 — 追加 030 融合 § B' N8 `PR-V1-OUTBOX-FU-CLOSURE` 登记：PR#373/#374 二轮 review 6 条 finding（4 P1 + 2 P2，**0 现网阻塞**，v1.0 release gate）彻底版闭口；删 `VerifyOutboxLeaseInvariant` + `MustWrapSubscriber` 两公开 API，DB CHECK 单源化 lease 不变量；净 LOC 负；10h+4.5h，文件域与 N1-N5/06.PR4/R-METRIC-PACK 0 重叠；won't-do `B2-X-09` 已登记 backlog2。上轮回灌（baseline e8f6a820 → 0c06a484，新增 9 PR）：**PR#380 B6 PG-OUTBOX-RELAY-HARDEN payload cap + lost metric + reclaim cfg loop ✅** + **PR#379 A4 RMQ runtime reconnect permanent classification ✅** + **PR#378 N4 G-03 VALIDATE-CTX-PROPAGATION ✅** + **PR#377 G8 (b) gofumpt producer pipeline + verify gate ✅**（关闭 G8 全部 a/b 子项）+ **PR#375 N1 follow-up snapshots visibility started state ✅** + **PR#374 K#12 follow-up close delivery settlement lifecycle ✅** + **PR#373 B1 PG outbox claim fencing token + B6 relay harden（B6 一并 absorb）✅** + **PR#372 N2 G-01 HOOK-DISPATCHER-LIFECYCLE drain observer sinks ✅** + **PR#371 G8 (a) consumer-side gofumpt repo-wide reformat ✅**；上轮已记: PR#369 K#12 / PR#370 N1 first cut / PR#368 pkg breaking cleanup（⚠️ #08 + ⚠️ C5 partial）/ PR#367 D8 / PR#366 A3 / PR#365 K#05 PR-A2+B / PR#364 G4 FU / PR#363 K#05 PR-A1 / PR#362 A5 / PR#361 K#06 PR-2 / PR#360 K#04 PR-1 / PR#359 D1 / PR#358 K#03 / PR#357 J1 / PR#356 K#02 / PR#353 G5 / PR#352 K#01 / PR#347/350/351/346/345/344 lint/shellcheck/G6/F3 / 030 review 融合方案登记）
> 基线: `origin/develop @ 4674e214`（最新合入 PR#384 N8 PR-V1-OUTBOX-FU-CLOSURE — DB CHECK + ConsumerBase sentinel + Subscription.Validate 单源 + RMQ release-first + 014/015 archtest + negative integration tests；上轮 PR#376 06.PR4 + PR#383 N3 G-02）
> 形态: 关键路径串行 + 并行轨道并发；无 v1.0 GA 时间约束

---

## errcode 主题实施计划（2026-05-06 增补）

> **背景**：PR#368 完成 errcode pkg 重构（删 6 Category-ctor 收敛 `New(kind,...)+Option` / `Kind.IsClient/Status` 单源派生 / 治理表 reflect-built），关闭 `ERRCODE-CLASSIFY-SINGLE-SOURCE-01` / `B2-R-04` / `B2-K-04` / `PR-B2-E2`。残余主题分布在 K#08 残余 + 06.FU typed envelope + 各 adapter + lint，按下表 4 wave 推进；W4 与 W1-W3 全程解耦可独立 lane。

### Wave 编排

| Wave | 项 | 目标 | 文件域 | 工时 | 依赖 |
|:-:|---|---|---|---|---|
| **W1** | K#08 残余 | (a) `Assertion` ctor + (b) `WithDetails(map[string]any)` → `slog.Attr` + (c) PII-safe 默认（Option 显式 opt-in 改 default）| `pkg/errcode/*` + 全仓 caller | 3h+2h | — |
| **W1** | ✅ A-05 CIRCUITBREAKER-INHOUSE-ERRCODE — PR#385（df6b5448）| `circuitbreaker.New` `fmt.Errorf` → `errcode.ErrAdapterCircuitBreakerConfig` + `clock.Clock` 构造期注入（D6 PROD-CLOCK-INJECTION-01）+ **删 sony/gobreaker/v2 依赖换自研 generation+expiry 状态机** + 7 branch tests（generation/interval/half-open edges）+ FIND-002 StateOpen expiry.IsZero guard + FIND-004 MaxRequests=1 严格 Equal 断言 + FIND-005/007 doc 修正 | `adapters/circuitbreaker/*` + `go.mod` 删 sony/gobreaker/v2 | ✅ done（实测 ≈ 2h+1h）|
| **W1** | C5 残余 B2-K-03（与 errcode 无关但同原 PR 残余）| `asm.(assemblyWithCell)` 隐式 cast → 显式 `AssemblyRef.Cell(id)` 接口 | `kernel/cell/*` + `runtime/bootstrap/*` | 4h+2h | — |
| **W1** | G3 CI-LINT-BATCH-2B | 启 errorlint（`%w` wrap 漂移）+ bodyclose + durationcheck，全仓收口 | `.golangci.yml` + 全仓 reformat | 12h+4h | repo-wide 独占窗口 |
| **W2** | **06.FU-A** typed envelope contractgen + 1 cell 验证 | contractgen 扩响应 union/envelope 模板；1 cell（建议 accesscore）验证范式 | `tools/codegen/contractgen/*` + 1 cell + 1-2 contract.yaml | 8h+4h | **W1 K#08（PII-safe 默认是 typed envelope 语义底座）**|
| **W3-B** | 06.FU-B 全 cell 迁移 + 顺路收 4 新 errcode | 28 HTTP handler service `(*Response, error)` → typed response；同 PR 落地 B8 `ERR_ADAPTER_CONNECT_TIMEOUT` + B2-T-01 `ERR_CONFIG_VERSION_MISMATCH` + B2-T-08 publish 失败码补全 + B2-A-13 driver error 边界 | `cells/**` + `contracts/**` + `runtime/http/middleware/*` | 12h+6h | W2 |
| **W3-C** | 06.FU-C contract-health 改造 + archtest + 删旧路径 | CH-04/CH-05 治理规则改读 declaration table；archtest 守 service 不能裸返 ad-hoc error；删 `httputil.WriteError` 旧路径或限定边界 | `kernel/governance/rules_http_*.go` + `tools/archtest/*` + `pkg/httputil/*` | 6h+3h | W3-B |
| **W4** | A-03 ADAPTER-ERROR-CLASSIFICATION-TRANSIENT | postgres `40001/40P01/08*` + redis `i/o timeout` + s3 5xx/429 标 transient；复用 vault `errcode.WrapInfra+IsTransient` 范式 | `adapters/{postgres,redis,s3}/errors.go` + `pkg/errcode/*` | 16h+8h | — |
| **W4** | NOLINT-AUDIT-01 | 全仓 101 处 `//nolint` 含 errcheck 类豁免审查 | 全仓 `*.go` | 4h+0h | G3（更经济）|
| **W4** | PR238-FU8 InternalMessage op label 测试 | configrepo `UpdateForRollback` op 测试覆盖（防硬编码回退）| `cells/configcore/internal/adapters/postgres/config_repo_test.go` | 0.5h | — |

### 顺序与并行

```
W1 (3 worktree 并行 + G3 独占窗口)：
  ┌── K#08 残余 ──────────┐
  ├── A-05 ────────────── │ 三者文件域 0 重叠，3 worktree 并发
  └── C5 残余 ───────────  │
                          
  G3 errorlint  ──── repo-wide reformat 独占窗口（W1 末段或 W1→W2 切换跑）

W2: 06.FU-A typed envelope + 1 cell  ←── 必须等 W1 K#08 ship
                  │
                  ▼
W3-B: 全 cell 迁移 + 4 新 errcode 顺路收
                  │
                  ▼
W3-C: contract-health 改造 + archtest + 删旧路径

W4 (独立 lane，与 W1-W3 全程可并行)：
  ┌── A-03 transient 分类 ────┐
  ├── NOLINT-AUDIT ──────────┤  3 项文件域互不重叠 + 与 W1-W3 全解耦
  └── PR238-FU8 ─────────────┘
```

### 工时与解决问题面

| 类别 | dev | review |
|---|---|---|
| W1 errcode 基础（K#08 + A-05 + G3 + C5 残余）| 21h | 9h |
| W2 typed envelope 范式（06.FU-A）| 8h | 4h |
| W3 全迁移 + 新 errcode 顺路（06.FU-B/C）| 18h | 9h |
| W4 transient + lint 审 + 测试小项 | 20.5h | 8h |
| **errcode 主题合计** | **67.5h** | **30h** |

| 解决问题 | Wave | 方式 |
|---|:-:|---|
| K#08 (a)(b)(c) 残余 | W1 | 直接 |
| A-05 circuitbreaker 规范 | W1 | 直接 |
| G3 `%w` wrap 漂移 | W1 | 静态守卫 |
| handler/runtime/contract 三方漂移最后一处 | W3-C | declaration table 单源 + archtest |
| B2-T-08 publish 失败码不全 | W3-B | typed envelope 强制 contract 声明 |
| B2-T-01 `ERR_CONFIG_VERSION_MISMATCH` 409 | W3-B | typed envelope 自动派生，service 编译期被迫实现 |
| B8 `ERR_ADAPTER_CONNECT_TIMEOUT` | W3-B | 顺路收（contract 加 declaration 即可）|
| B2-A-13 driver error 泄漏 | W3-B | typed response 强制边界 |
| PR238-FU8 InternalMessage op label | W4 | declaration table 加字段或独立小项 |
| A-03 transient/permanent 分类 | W4 | adapter 层独立（与 06.FU 解耦）|
| NOLINT-AUDIT errcheck 类豁免 | W4 | grep + 人审 |
| OBS-SSA-ANALYZER-01 跨包 taint | — | 触发条件型，不主动做 |
| PR283-OTEL-SLOG-ERROR-ATTR | — | 触发条件型，OTEL bridge 接入时再做 |

### 关键决策

1. **W4 完全独立 lane**：A-03 / NOLINT / PR238-FU8 与 06.FU 文件域 0 重叠，全程可并发，**不阻塞 W1-W3 关键路径**
2. **W1 4 项 3+1 并行**：K#08 / A-05 / C5 残余 3 worktree 并发；G3 errorlint repo-wide reformat 必须独占窗口（与所有在飞 PR rebase 冲突），建议在 W1→W2 切换的窄窗口跑
3. **W2/W3 必须等 W1 K#08**：PII-safe 默认是 typed envelope 的语义底座，先 ship K#08 再起 typed envelope 才能编译期强制 PII-safe
4. **新 errcode 落地标准化**：W3-B 后续新 errcode 落地从「pkg/errcode + contract.yaml + handler + 治理表 4 处人工对齐」→「contract.yaml 一行 + service 编译期错误指引」

---

## K + G 剩余实施路径

> Phase 0-3 全部 ship 含 06.PR4 末段（K#01-#06 / G1/2/4/5/6/7/8 / D1 / J1 / K#12 / **06.PR4 ✅ PR#376** 等）。剩 Phase 4 装备类 + G 残余 lint 批 + F v1.1 docs。详细行见下文「关键路径」与 Track G/F 表。

| 剩余项 | 内容 | 状态 | 工时 |
|---|---|---|---|
| **K#07** | EVENTROUTER-SUBSCRIPTION-FIELDS（拆 `Subscription.CellID` ↔ `ConsumerGroup` 字段）| 待办 | 4h+4h |
| **K#08 残余** | ERRCODE-CONVERGE：Assertion ctor + `WithDetails(map[string]any)` → `slog.Attr` + PII-safe 默认 | ⚠️ partial（PR#368 已收 Kind-based ctor + IsClient/Status 单源派生）| 残余 ~3h+2h |
| **K#09** | SCAFFOLD-ONE-CMD（`gocell new-cell/new-slice/new-contract/scaffold assembly`，吸收 K-08(b)）| 待办 | 22h+10h |
| **K#10** | ASSEMBLY-YAML-MINIMAL（必填只剩 `cells:`，吸收 K-08(a) schema 扩 + (c) `modules_gen.go` 派生）| 待办 | 4h+4h（+10h+5h K-08 吸收）|
| **G3** | CI-LINT-BATCH-2B（启 errorlint / bodyclose / durationcheck）| 待办 | 12h+4h |
| **G9** | SLOW-TEST-BUDGET（`tools/slowgate` 二进制 + 2s 阈值 + allowlist；D6 已 ship 解锁触发条件）| 待办 | 4h+2h |
| **G11** | TEST-GATE-CLOSURE（`adapters/postgres` 包级覆盖率 ≥60% + Migrator.Down v=0 幂等回归）| 待办 | 4h+2h |
| **F4** | API-VERSIONING-POLICY（storageVersion + deprecation 窗口 + `lifecycle:deprecated` + `removeBy:vN.M`）| 待办 | 8h+4h |
| **F5** | DOCS-AUTOMATION（`gen-crd-api-reference-docs` 范式，从 contract.yaml schema 自动生成 + KEP frontmatter ADR 模板）| 待办 | 8h+4h |

---

## 关键路径（12 项 / ~228h dev + 137h review = ~365h 单线）

> 单线串行，每项依赖上一项合入 develop。按依赖 + 杠杆分排序。

| # | PR | 来源 | 问题 | 方案 | ship | 工时 | 依赖 |
|---|---|---|---|---|:-:|---|---|
| 01 | ✅ PR#352（165f69b7）PR-V1-DEPGRAPH-TYPED-ARCHTEST | PR-A38（11分） | archtest 当前用文件级 string scan，类型级 LAYER 违规漏检；codegen 产物的 LAYER 校验需类型分析才不误报 | 新模块 `tools/depgraph/`（基于 `golang.org/x/tools/go/packages`）+ Layer/CellID 推断 + JSON/DOT 输出；archtest 升级为传递闭包级类型分析；**`Graph()` JSON wire 与 Track J J1 (PR-A37 扩) 同窗口设计**（J1 现可立即接入）；借鉴 `loov/goda internal/pkggraph` 数据模型不引依赖（commit `ref: loov/goda internal/pkggraph`） | ✅ done | 16h + 8h | - |
| 02 | ✅ PR#356（2b8a86c6）PR-V1-API-CONTRIB-COLLAPSE | E2（15分）/ 027#31 | Contributor 5 接口（RouteGroup/EventRegistrar/Health/Lifecycle/ConfigReloader）散在 type assertion 自动发现循环里；新 cell 入场 4-8h；IoC 比例 25-30% | 删 5 接口 → 单一 `cell.Registry` builder API（`Routes/Subscribe/Health/OnStart/OnStop/OnConfigReload`）；删 `kernel/cell/registrar.go` 399 行 + `routegroup.go` + `celltest/eventrouter.go`；新建 `kernel/cell/registry.go`；bootstrap phase3 读 RegistrySnapshot drain；cells/{configcore,auditcore,accesscore} + examples/{iotdevice,todoorder} 全迁移；archtest `cell_init_test.go` 锁 kernel/cell 不引 runtime/adapters | ✅ done | 32h + 24h | #01 |
| 03 | ✅ PR#358（4829f9e5）PR-V1-API-VOCABULARY-COLLAPSE | E1（13分）/ 027#32 | HandleResult 5 词幂等模型（Claimer/Receipt/ClaimState/Stopper/WrapLegacy）混用，handler 签名不一致 | 收口三态 `Ack/Requeue/Reject`；删 `NoopTxRunner/RunnerOrNoop/WrapLegacyHandler` + isPermanentRejection 隐式升级；全 L2 consumer 切签名；`kernel/outbox` 公开 API 简化 ~30%；service 构造器 nil TxRunner fail-fast；ADR 见 `docs/architecture/202605031900-adr-handler-vocabulary-collapse.md`；K#12 OUTBOX-RECEIPT-EXTRACT 跟进（#03 合入后可并行）| ✅ done | 24h + 16h | #02 |
| 04 | ✅ **PR#360**（842a8d69）PR-V1-CODEGEN-CELL-GEN — 方案 D PR-1 | E3（15分）/ 028 B1-1 | hello world 7 文件 / 220 行手写 / 11+ 概念；`reg.X()` 必须人工对齐 cell.yaml（ADV-06/VERIFY-01 只检测漂移不消除手工成本）；marker 4-5 段重复样板；对标 net/http 18× 重 | ✅ shipped: tools/codegen 通用 framework（render/writer/verify with K8s git worktree sandbox）+ tools/codegen/cellgen 子包（CellGenSpec → cell.tmpl + slice.tmpl）+ `gocell generate cell` CLI + `hack/verify-codegen-cell.sh` 双模式 + 4 archtest gates（CODEGEN-CELL-GEN-01/02 + USER-FILE-OVERLAP-01 + INIT-INTERNAL-01 + MARKER-NONE-01）+ examples/todoorder/cells/ordercell 首例迁移；framework 前瞻支持 K#06 contractgen + K#05 markergen 子包扩展（`tools/codegen/templates/header.tmpl` SharedTemplates）。**4 cell（accesscore/auditcore/configcore/iotdevice）留 PR-4 一次到位** — 跳过 yaml-driven 中间态以避免 PR-3 marker 翻转时的 ~200 行 yaml 二次返工 | ✅ done | 方案 D PR-1 25h + 12h（实际 ~30h dev 含 6 角色 review fix）| #01 #02 |
| 05 | ✅ PR-V1-CODEGEN-MARKER-MIGRATE — 方案 D PR-3 **3 层双源消除完成：A1 (类型层) ✅ + A2+B 合并 (内容层 + wire 层) ✅** | E4（15分）/ 028 B1-2 | yaml 与 Go marker 双源不一致历史问题已 K#05 彻底清理 | **PR-A1 ✅** (PR#363 9ccef27a merged) 类型层物理合一：删 5 个 kernel/cell 类型，BaseCell 持 `*metadata.CellMeta`，27 caller 全迁；archtest CELLMETA-SINGLE-SOURCE-01..03；ADR 202605051300。**PR-A2+B 合并 ✅** (PR#365 b829b8d7 merged) 内容层 + wire 层一次到位：新建 `tools/codegen/markergen` 子包（闭集 switch dispatch 对标 controller-tools splitMarker；不采纳 Registry/Definition 抽象，节省 ~350 LOC）；cellgen 模板扩展 metadata block + WireBundle 注入 ProjectMeta bridge；4 cell K#04 opt-in（accesscore/auditcore/configcore/devicecell）+ ordercell wire 迁移；5 cell.go 删 metadata literal + 加 markers (`// +cell:listener` / `// +slice:route` / `// +slice:subscribe`)；cell.yaml/slice.yaml 全删 wire 字段；新增 5 archtest gates (NO-METADATA-LITERAL-IN-CELLGO-01 / MARKER-WIRE-SINGLE-SOURCE-01 / NO-WIRE-FIELDS-IN-YAML-01 / MARKERGEN-DRIFT-VERIFY-01 / MARKER-MISSING-FOR-WIRE-CALL-01) + 删 CODEGEN-MARKER-NONE-01；ADR 202605051500 `k05-markergen-cellgen-unified.md`（决定 1: 合并 PR-A2+B；决定 2: drop cellmetagen；决定 3: 闭集 switch；决定 4: CellMeta.Listeners 过渡保留；决定 6: catalog augment + 决定 8: CLI flag default + scaffold stub 拆 follow-up backlog） | PR-A1 ✅ / PR-A2+B ✅ | 实际 A1: ~18h ✅ + A2+B 合并: ~38h dev + ~14h review = ~52h（vs 原估 80h+36h，合并节省 ~26h dev/差异） | #04 (K#06 实际无依赖，但顺序留 PR-2 后) |
| 06 | ✅ **PR-V1-CONTRACT-DTO-CODEGEN** — 方案 D PR-2（feat/166-codegen-contract-gen）| E10（15分）/ 028 B1-3 | contract → DTO/iface/handler 全手写（83 LOC handler.go × 3 slice），模板与 contract.yaml 漂移；errcode 常量手抄治理表 | ✅ shipped: `tools/codegen/contractgen` 子包（spec/builder/templates/generator + 内置 JSON Schema subset parser ~200 LOC，零第三方依赖）复用 K#04 framework；CLI `gocell generate contract` + verify gate `hack/verify-codegen-contract.sh`（双模式 sandbox + --local）+ **3 archtest gates**（CODEGEN-CONTRACT-GEN-01/02 + USER-OVERLAP-01）+ contract.yaml `codegen: true` opt-in 字段（PR-4 整字段清账）+ examples/todoorder 4 contract 全量迁移（3 HTTP + 1 event，3 slice handler.go/handler_test.go 整文件删共 6 文件）。**errors_gen.go 推到后续**：当时中央状态表治理仍未完成，PR-2 单生成新 errcode constants 会引入 fallback 500 回归；PR#368 后状态已改由 errcode.Kind 单源驱动，typed response envelope 后续见 06.FU；ADR 见 `docs/architecture/202605041700-adr-contractgen-errors-defer-to-c5.md` | ✅ done | 24h + 12h | #04 (PR-1 framework 已 ship) |
| 06.PR4 | ✅ PR#376（24e37fd6）PR-V1-CODEGEN-FULL-MIGRATION — 方案 D PR-4 末段 | 029 plan-D | K#04 PR-1 仅 ordercell opt-in；archtest 凭 `cell.GoStructName != ""` 判定，PR-4 只需在 4 cell 的 cell.yaml 加 GoStructName + listeners/routeMounts/subscribes，cell.go 写 initInternal hook | ✅ shipped: 64 contract opt-in (5 kind=command deferred), event spec/subscription codegen, 22 slice migrated, wrapper.EventSpec deleted, FMT-18 deleted, 4 archtest gates GREEN with empty allowlist (CELLS-NO-WRAPPER-CONTRACTSPEC-IMPORT-01 + NO-MANUAL-CONTRACTSPEC-LITERAL-01 + EVENT-SUBSCRIPTION-CONTRACTGEN-COVERAGE-01 + CONTRACT-KINDS-CLOSED-SET-01); scaffold golden test added (K-06 residual configcore.cell.yaml `consistencyLevel: L2`); CH-04/05 修 + 删 dead `event.flag.changed.v1`; ref: controller-tools genall + oapi-codegen strict + go-zero genroutes; 029/030 roadmap reconciled。**⚠️ FU 待（origin/pr/376@1b7585a 独立 review 二轮发现 → §B'' PR#376 06.PR4 review 闭口分发）**：6 条 finding（2 P1 + 4 P2）全部并入既有 5 个 PR（A2 升 P0 / 06.FU / 06.FU3 升格已调度 / F1 / F4），工时增量 10h+5.5h，无独立 PR。详见 §B'' 分发表。 | ✅ done（FU 残余 §B'' 分发待）| **实测 44h+21h**（W1 8h+4h, W2 6h+3h, W3 22h+10h, W4 8h+4h） | #04 #05 #06 |
| 06.FU | PR-V1-CONTRACT-TYPED-RESPONSE-ENVELOPE | PR#368 review follow-up + **PR#376 review F4 + F3(doc.go) absorb（2026-05-06）** | generated HTTP handler 现在仍接收 `(*Response, error)` 并把 service error 交给 `httputil.WriteError`，contract-health 只能用 AST + errcode.Kind 猜测错误响应；长期仍可能出现 handler/runtime/contract 三方漂移；同 generator 在 pagination shape 启发式（cursor+limit 严格 2 元）外 inline limit 解析 → 同一 PR#376 generated 面产出两套 limit 错误 envelope（user-visible API 不一致） | contractgen 改为生成 typed response union/envelope（类似 oapi-codegen strict server）：service 返回生成的 response object，handler 只 switch 已声明 response 类型并拒绝未知响应；contract-health 和 handler 模板共用同一 response declaration table；届时 CH-04 不再从 helper 名或实现细节反推 status。**追加范围（2026-05-06，吸收 06.PR4 review F4 + F3 doc.go）**：(d) F4 contractgen IR 加 `paginationShape` 字段，pagination 检测从「query=cursor+limit 严格 2 元」改为「含 cursor+limit、其余自由」，模板统一走 `pkg/httputil.ParsePageParams`，删 `handler_gen.go` 内联 limit 解析分支，全量 regen；archtest `HANDLER-NO-INLINE-LIMIT-PARSE-01` 守不再有内联；(e) F3 `tools/codegen/contractgen/doc.go` 重写为完整 artifact matrix（dto_gen + iface_gen + spec_gen + subscription_gen + handler_gen + errors_gen 待）—— typed envelope 本来就要重写 doc.go，顺路收 | L4 / Phase 4 | 17h + 8.5h（原 16h+8h + 1h+0.5h pagination IR） | #06 #08 C5 |
| 06.FU2 | PR-V1-CONTRACT-RESPONSE-CONVERTER-CODEGEN | PR#376 二轮 SonarCloud 重复率 36.2% follow-up | 同 cell 多个 contract 共享同形 ResponseData 时业务方手写 N 份 `to{X}ResponseData(domainEntry) → *xxxcontract.ResponseData` converter（如 `examples/iotdevice/cells/devicecell/slices/devicecommand/service.go` 6 个 converter ~120 行 36% 重复，2026-05-05 PR#376 已用同包 `entryToFields` helper 减半至 ~60 行作为过渡）；问题模式：generated DTO 类型隔离（每 contract 独立 ResponseData 类型）+ 业务方写跨类型映射，N 份相似代码无法消除 | contractgen 扩展 `response.schema.json` + 用户在 `contract.yaml` 声明 `responseSourceType: command.Entry`（或 `internal/domain.Entry`）→ 生成 `converter_gen.go` 含 `ToResponseData(src CommandEntryFields) *ResponseData` 函数；source type 必须实现公共 `Fields()` 接口（或用 reflect 安全提取）；service 直接调 `xxxcontract.ToResponseData(entry.Fields())`，删手写 converter；archtest 守 cell 内不再有手写 `to{X}ResponseData` 函数 | L4 / Phase 4 | 16h + 8h | #06 #06.FU |
| 06.FU3 | ✅ PR#386（f6edea47）PR-V1-ARCHTEST-AST-FULL-AUDIT | PR#376 二轮 review follow-up + **PR#376 review F6 触发条件达成（2026-05-06）** | PR#376 二轮已把若干字符串扫升级为 AST/`metadata.LoadProject`（W1 F8 / W3 PATCH-OPTIONAL-BOOL-POINTER-01 等），但仍有部分 archtest 偏存在性 / 字符串匹配，未绑定真实数据流；**触发条件已达**：F6 实证发现 `tools/archtest/handler_path_query_length_validation_test.go` 文件级 `hasLenCheck && hasParamRef` 启发式可在多 param handler 上 false-pass（一个 param 有效 length check + 另一个 param 仅 reference 即满足，单 param 退化无法捕获） | ✅ shipped: **Wave 1 RED** — 6 个 negative fixtures（path-query length 双 param 漏检 / string-literal-only / query missing max / event-subscription var-shadows-func / handler-no-schema GET-with-validator / spec-gen wrong-id-in-comment / marker listener-in-string-literal / security-defaults missing-tls-validate）+ 7 个 negative tests，全部 RED 锁定 false-pass / false-positive / false-negative 路径；**Wave 2 GREEN** — (a) F6 `handler_path_query_length_validation_test.go` 改 `scanHandlerLengthCheck(f, name, isPath, requireMin, requireMax)`：定位含 `r.PathValue("name")` 或 `req.X = r.URL.Query().Get("name")` 赋值的 `*ast.BlockStmt` + 同 block 内 `len(target) <\|> N` IfStmt 必须分别绑定（requireMin/requireMax 独立 gate）；(b) 全量审查 — 扫描根统一 `metadata.NewParser(root).Parse()` 单根（cells/ + examples/*/cells/ 通过 path-pattern 一并覆盖），`findCellProductionGoFiles` / `findCellFiles` / `findSliceServiceFiles` 全经 ProjectMeta；硬编码多根 archtest 全迁（`cells_no_wrapper_contractspec_import` / `no_manual_contractspec_literal` / `svctoken_caller_cell` / `no_test_service_context_in_production` / `no_deleted_auth_symbols` / `queryparam_drift`）；负向 fixture 全经 owning gate scanner 入口验证 | ✅ done | 16h + 8h（F6 在原范围内，0 增量） | #06 |
| 07 | PR-V1-EVENTROUTER-SUBSCRIPTION-FIELDS | B2-K-06（9分）/ 028 B2-3 | `Subscription.CellID` 实为 consumerGroup，metric label / 日志属性自相矛盾 | 显式拆 `CellID` + `ConsumerGroup` 两字段；codegen 之后做最便宜（改模板 5 LOC + go generate） | L2 | 4h + 4h | #04 |
| 08 | ⚠️ **PR#368 部分推进** PR-V1-ERRCODE-CONVERGE | E11（9分）/ 028 B2-2 | errcode 5 构造器冗余；PII-safe 默认错配（默认 unsafe）；`map[string]any` 与 logtag 不对齐 | **PR#368 (refactor/514-pkg-breaking-cleanup) 重构走 Kind-based 路线（非原 5→3）**：(a) 删除 `NewInfra/NewDomain/NewAuth + WrapInfra/WrapDomain/WrapAuth` 6 个 Category-ctor，收敛为 `New(kind, code, message, opts...)` + `Wrap(kind, code, message, cause, opts...)` 双入口 + `Option` 模式（`WithCategory/WithInternal/WithDetails`）；(b) `expected4xxCodes` whitelist 删除，改 `Kind.IsClient()` 单源派生（关闭 backlog `ERRCODE-CLASSIFY-SINGLE-SOURCE-01` / `B2-R-04` / `PR-B2-E2`）；(c) `Error.Status()` / `Error.PublicCode()` 同 Kind 派生。**残余 follow-up**：`Assertion` ctor + `WithDetails(map[string]any)` → `slog.Attr` 迁移 + PII-safe 默认（当前 Option 显式 opt-in，不是 default）—— 拆 follow-up backlog 或并入 C5；roadmap 描述需修订（5→3 路线已被 Kind-based 替代）| ⚠️ partial | 残余 ~3h+2h | #04 |
| 09 | PR-V1-SCAFFOLD-ONE-CMD | E9（8分）/ 028 B2-1 + 030 K-08(b) | 新 cell 入场 4-8h（codegen 后 1-2h）；无对标 kubebuilder/`rails generate scaffold` 一键脚手架；scaffold 缺 `assembly` 子命令，加第 2 个 assembly 时无工具生成初始 yaml + `cmd/{id}/run.go` 占位 | `gocell new-cell <id> --type=<core\|edge\|support> --level=<L0-L4> [--with-http] [--with-events]` 一键产出可编译可运行最小 cell + 1 example slice + 1 通过测试；附 `new-slice` + `new-contract` 子命令；**吸收 K-08(b)**：加 `gocell scaffold assembly --id=<id> --cells=<a,b> --deploy=k8s`，同步生成 `assemblies/{id}/assembly.yaml` + `cmd/{id}/run.go` 占位 | L3 | 22h + 10h | #04 #05 #06 |
| 10 | PR-V1-ASSEMBLY-YAML-MINIMAL | E13（6分）/ 028 B3-4 | assembly.yaml 13 字段必填，但 entrypoint/binary/deployTemplate 可由 cell type 推 | 必填只剩 `cells: [...]` 一行；entrypoint/binary/deployTemplate 从 cell.yaml type 自动推导；复杂部署才显式声明 | L1 | 4h + 4h | - |
| ~~11~~ | ~~PR-V1-VISUALIZE-CMD~~ | ~~E6（5分）/ 028 B3-3~~ | **2026-05-04 砍掉**：原痛点"boundary 无可视化"已被 K#01 PR#352（`gocell graph` JSON+DOT 输出）+ J1 PR#357（unified catalog 三维查询）覆盖；SVG/mermaid 可由 `gocell graph --format=dot \| dot -Tsvg` / 在线 DOT→mermaid 工具完成，不值得 CLI 内置 | — | — | — | — |
| 12 | ✅ PR#369 + PR#374 PR-V1-OUTBOX-RECEIPT-EXTRACT（branch refactor/513-outbox-receipt-extract）| 029 #03 ADR Q1 follow-up（2026-05-03）| `outbox.HandleResult.Receipt` 字段是 ConsumerBase → Subscriber delivery loop 内部回传通道（业务 handler 不读写，但 `idempotency.Receipt` 类型仍 leak 到公开 handler API）；#03 ADR Decision 4 后这是最后一处 idempotency 残留 | ✅ DONE：`HandleResult.Receipt` 字段删除；新增 `outbox.Settlement` 接口（Commit/Release）；`SubscriberHandler = func(ctx, Entry) (HandleResult, Settlement)` 携带 settlement 作为第二返回值；`ConsumerBase.Wrap` 返回 `SubscriberHandler`；`EntryToSubscriberHandler` 升格辅助（adapter-layer 测试保留）；adapters/rabbitmq + runtime/eventbus 适配；idempotency.Receipt 满足 Settlement duck typing；ADR Q1 closed。**二轮深度修复（Codex 独立审查）**：删 `ConsumerBase.AsMiddleware`；`SubscriptionMiddleware` 回退为 `func(sub, next EntryHandler) EntryHandler`（业务签名，不接触 Settlement）；`*ConsumerBase` 字段化注入 `SubscriberWithMiddleware`；lift→discard 根因消除；对齐 Watermill/Kratos/sarama 业界共识。**PR#369 follow-up closure**：commit-failed Ack→Requeue 路径先 `Release` 再重试/通知；`ContractTracingMiddleware` 替换为 subscriber 层 `NewContractTracingSubscriber` + `wrapper.WrapSubscriber`，span 在最终 broker settlement 后结束并覆盖 `commit_failed`；订阅存在但缺 `ConsumerBase` 时 phase6 fail-fast；ssobff demo 显式接入 in-memory ConsumerBase。**PR#374 (5d5835bb) close delivery settlement lifecycle** — 收口 settlement 双 Commit/Release 路径生命周期防漏。**✅ FU 闭口 PR#384 (4674e214) N8 PR-V1-OUTBOX-FU-CLOSURE**：(a) `kernel/outbox/consumer_base.go` 加 unexported `built` sentinel + `IsConstructed()` 接口，`NewConsumerBase` 唯一写入点；`outbox.WithConsumerBase` / phase6 wiring 拒 `&ConsumerBase{}` 字面量（运行时 ClaimRetryCount=0 → 静默 Reject/DLX 攻击面消除）；(b) `Subscription.Validate` 加 ContractID/Kind/Transport 三字段非空校验作为单源真相；`NewContractTracingSubscriber` 签名改 `(*ContractTracingSubscriber, error)` 内部走 Validate；**删 `wrapper.MustWrapSubscriber` 整函数** + 全 caller 迁 `WrapSubscriber` error-returning；(c) `adapters/rabbitmq/subscriber.go` commit_failed 路径 release-first（先 Release 再 Nack(requeue=true)），与 `runtime/eventbus/eventbus.go` 顺序对齐；新增 testcontainers `subscriber_release_before_redelivery_integration_test.go` 锁真实 broker redelivery 在 lease TTL 之前发生。| ✅ done after PR#369 + PR#374 + PR#384 N8 closure | 12h + 6h + 4h + N8 FU 5h+2.5h | #03 |
| 13 | PR-V1-CELL-IFACE-ISP-SPLIT（PR-A22）| backlog_later_detail.md §4 + 历史 025 plan PR-A22（2026-05-08 上 029）| `kernel/cell/interfaces.go:34-72` 当前 12 方法基础接口混合 identity(3) + lifecycle(3) + status(2) + metadata accessor(4)；K#04/K#05 已让 `metadata.CellMeta` 数据层单源化（`*metadata.CellMeta` 指针经 `Metadata()` 暴露），但接口形态层仍未做 ISP 切分，CELLMETA-SINGLE-SOURCE-03 archtest 现守在主 Cell 接口而非子接口；同时 PR-A5c `OUTBOX-CELL-01` 仅 ban `WithPublisher`/`WithOutboxWriter` 两个 raw-dep Option，`WithTxManager(persistence.TxRunner)` 等 raw dep 在 cells/{accesscore,auditcore,configcore}/cell.go 仍可被新 PR 重新打开口子（PR245-F10 / 030 G-17 触发） | (a) `kernel/cell/interfaces.go` 12 方法 → 三接口：`CellIdentity{ID/Type/ConsistencyLevel}` + `CellLifecycle{Init/Start/Stop/Health/Ready}` + `CellMetadata{Metadata/OwnedSlices/ProducedContracts/ConsumedContracts}`；`Cell` 改写为 `interface { CellIdentity; CellLifecycle; CellMetadata }` 复合形保持调用方零变化；(b) `base.go:13-17` compile-time check 三段式 `_ CellIdentity / _ CellLifecycle / _ CellMetadata = (*BaseCell)(nil)`；BaseCell 实现不变（已实现全部方法）；(c) **吸收 PR245-F10 / 030 G-17**：archtest `CELL-RAW-DEPS-01` 扩到禁所有 `cells/*/cell.go` 公开 `With*` Option 接受 raw infra 类型（`persistence.TxRunner` / `outbox.Publisher` / `outbox.OutboxWriter` / `eventbus.Bus` / `kvstore.Store`），保留现有 `WithTxManager` 调用形态仅锁未来不退化；(d) archtest `CELLMETA-SINGLE-SOURCE-03` 升级：`Metadata()` 必须落在 `CellMetadata` 子接口而非顶层 `Cell`；(e) **ADR**：写入三接口切分决议 + `Cell` 复合形保留原因 + **Slice 接口同步评估决议（默认不拆）**——Slice 7 方法形态相似但 cells/* 全嵌 BaseSlice 实现疲劳为 0、字段为简单值无 metadata 单源化数据层驱动，等首次第三方 Slice 实现需 ISP 时再触发，登记 backlog_later_detail.md §4 同槽位 + `CellMetadata` 接口名与 `kernel/metadata` 包共存导入约定。**风险**：高（接口破坏性变更）；强制 `/ultrareview`；调用方零代码变更但 `var _ Cell = (*BaseCell)(nil)` 升级为三段式 | L4 / Phase 4（v1.1+ 长期债清账） | 16h + 7h（主体 10h+5h + G-17 archtest 2h+1h + CELLMETA-03 升级 1h+0.5h + ADR 含 Slice 决议 2h+0h + buffer 1h+0.5h）| #02 #04 #05（已全 done）|

**为什么是这个顺序**：
- `#01` 必先：codegen 产物的 LAYER 校验需类型分析（否则 verify-codegen gate 误报淹没）
- `#02 → #03`：E2 改 Registry 形状决定 E1 HandleResult 在哪暴露（027 已论证）
- `#04 → #05/#06`：E3 codegen 引擎建好后，E4 翻转真相源 / E10 复用引擎
- `#07/#08` 在 codegen 后：模板 5 LOC 改 + go generate 自动跟，比 codegen 前手工改全仓便宜
- `#09` 在 #07/#08 后：scaffold 模板编码 codegen 后的干净 API
- `#10` 装备类，独立可放最后（原 #11 VISUALIZE-CMD 已 2026-05-04 砍掉，被 K#01 + J1 吸收）
- `#13` 放最末：长期债清账，K#04/K#05 已完成 metadata 数据层单源化作为前置；接口形态切分零调用方变更（BaseCell 已实现满），但属协议级破坏性变更（强制 `/ultrareview`），与其他关键路径项文件域 0 重叠可独立排

---

## 并行轨道（无关键路径依赖，按 reviewer 容量穿插）

每条轨道按 backlog 顺序独立推进；所有 PR 文件域已注明，可与关键路径并行。

### Track A · 安全闭口（5 PR / 51h + 26h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| A1 | ✅ PR#381（4a1335fe）PR-V1-SEC-WS-AUTH-ACL | B2-W-01/02/06 / 027#1 | WebSocket Upgrade 无 authn / Broadcast 无 ACL，多租户信息越界（P0） | ✅ shipped: (1) `runtime/auth.Principal.ExpiresAt` 字段（zero = no expiry，从 JWT `Claims.ExpiresAt` 透传）+ `NewContextAuthenticator`（authenticated listener 后挂载读 ctx principal）+ `NewAnonymousAuthenticator`（显式无认证通道返回 `PrincipalAnonymous`）+ 5 errcode constants；(2) `adapters/websocket/handler.go` Upgrade 期注入 Authenticator，认证失败 fail-closed 返回派生 status（401 KindUnauthenticated / 403 KindPermissionDenied，RFC 9110 单源 `errcode.Kind.Status()`，不再硬编 401）；(3) `runtime/websocket/Hub` 重构：删 `Broadcast(ctx, data)` 加 `BroadcastFilter`（filter 必填）+ `BroadcastToSubject`（centrifuge-style subjectIdx O(1) fanout，5 个 write point lockstep 维护）；(4) per-conn `writeLoop` `select-default-drop`（gorilla chat hub.go 范式）+ slow client（SendBufferSize chan 满）evict；(5) `pingLoop` token expiry 检测（centrifuge `checkExpired`，`!After(now)` RFC 7519 §4.1.4 边界）；(6) `Conn.Principal()` interface 暴露；(7) `docs/guides/websocket-integration.md` 439 行集成指南；(8) 新增 SEC-07/08/09 archtest entries；ref: olahol/melody + centrifugal/centrifuge + gorilla/websocket + coder/websocket | ✅ done | 16h + 8h |
| A2 | **🔴 升 P0** PR-V1-SEC-SETUP-CLOSURE | B2-C-02 / 027#2 + **PR#376 review F1+F2 absorb（2026-05-06）** | setup admin 端点常驻 anonymous business plane（`contracts/accesscore/setup-admin.yaml` `auth: public` + generated handler 挂 public + service 仅 gate `current status` + `cmd/corebundle/access_module.go` empty config 默认 interactive mode）—— 即使 multi-pod 已 410，**默认不安全**仍是产品级风险；同时 `.github/workflows/_build-lint.yml` `Verify generated artifacts` / `Verify cell codegen` / `Verify contract codegen` 跟 kernel `Test` shard 同 `if: matrix.static_checks`，红 Test 静默掩盖 codegen drift（CI trust 漏洞）| **走 bootstrap credential via env/secret 路线**（不走 internal listener，与 Keycloak `KC_BOOTSTRAP_ADMIN_USERNAME` / Grafana default+force-change / MinIO `MINIO_ROOT_USER` 三方对标 + 与 `cmd/corebundle` 现有配置范式一致；不需 B3 PG 分布式锁配套）：(a) F1 — `contracts/accesscore/setup-admin.yaml` 删 anonymous public 暴露，改 `auth: bootstrap` 新值（contract.schema 闭集扩）；regen `handler_gen.go` 走 bootstrap credential authn；`access_module.go` empty config → fail-fast（不再 default interactive），interactive 模式必须显式 `GOCELL_SETUP_MODE=interactive` opt-in；新增 `GOCELL_BOOTSTRAP_ADMIN_USERNAME` / `GOCELL_BOOTSTRAP_ADMIN_PASSWORD` env（一次性，admin 已存在后忽略）；ADR `docs/architecture/2026050X-adr-bootstrap-admin-boundary.md`；archtest `SETUP-ADMIN-NOT-PUBLIC-01` 守 contract 不能为 `auth.public`。(b) F2 — `_build-lint.yml` 把 verify 三步拆出独立 `verify-codegen` job，与 `test-and-build` matrix 解耦，K8s 范式（参考 `kubernetes/kubernetes` `verify-*.sh` 单独 workflow）；CI gate 必须双 job 同时 GREEN | **L1 P0** | **10h + 5h**（A2 原 8h+4h + F2 verify gate 拆 job 2h+1h；F1 在 A2 原范围内 0 增量） |
| A3 | ✅ PR#366（95bbc1b3）PR-V1-SEC-WRAPPER-REDACTOR | PR-SEC-1 / 027#3 | kernel/wrapper 默认 redactor=identity，敏感字段泄漏 span/metric label | ✅ shipped: `kernel/wrapper.WrapConsumer` + `runtime/http/middleware.Recovery` 强制 `pkg/redaction.RedactError` fail-closed，**删除全部 caller-side opt-out 接线**（`ErrorRedactor` type / `WithConsumerErrorRedactor` / `bootstrap.WithErrorRedactor` / `middleware.WithErrorRedactor` / `Bootstrap.errorRedactor` 全删）；默认 mask password/passwd/pwd/secret/token/api_key/authorization/bearer/private_key/signing_key/dsn/connection_string；`runtime/outbox.SanitizeError` 收口到 `pkg/redaction` 单源；net delta -198/+189 LOC（10 改 + 2 新增）；ADR `docs/architecture/202604242030-adr-kernel-wrapper-contract-observability.md` §8 + `.claude/rules/gocell/observability.md` Span Error Redaction 节同步 | ✅ done | 3h + 2h |
| A4 | ✅ PR#379（8c7e7ebb）PR-V1-RMQ-TERMINAL | B2-A-02 / 027#4 | RMQ 永久错误（403/404/530）无限重试，凭证撤销后 Pod 永不自愈（P0） | ✅ shipped: **soft permanent classification** — runtime reconnect 复用 `classifyDialError`（双层 sentinel：inferred `ErrCredentials/ErrVhost` 需 N=2 确认 + definitive 协议级 sentinel + broker `Server=true && !Recover` + url.Error/net.Error/字符串 fallback），命中即 `markPermanent` 设 `permanentErr` 唤醒 WaitConnected，**reconnect goroutine 持续 retry 不退**；下次 dial 成功 → `markRecovered` 自动清 `permanentErr` → `/readyz` 自愈 200。范式对齐 watermill-amqp 上游。删整套 hard-terminal 脚手架（`StateTerminal` phase + `terminalCh` chan + WaitConnected `<-terminalCh` 分支 + Health StateTerminal 分支），删 dead code（`Config.MaxReconnectAttempts` 字段 + `ErrAdapterAMQPReconnectExhausted` errcode + `isTerminalConnectionError` 简化只检 ConnectPermanent + 5 处 A.1 NOTE + WaitConnected/Health godoc）；同步 `sanitizeURL` fail-closed 丢 RawQuery+Fragment（RabbitMQ URI query 含 password / TLS 文件路径）；吸收 030 A-06 `RMQ-WAITCONNECTED-DOC-FIX`；ADR `docs/architecture/202605051700-adr-rmq-runtime-permanent-classification.md` + `docs/guides/adapter-config-reference.md` migration note | ✅ done | 8h + 4h |
| A5 | ✅ PR#362（04b63b1f）PR-V1-SVCTOKEN-CALLER-IDENTITY | B2-T-07（13分）/ 027#33 | service token 共享 `RoleInternalAdmin`，缺 caller_cell 最小权限 | ✅ shipped: claims 加 `caller_cell` + AuthMiddleware 强制 `caller_cell ∈ contract.clients`；`BuiltinServiceRoles` 派生 scope；同时关闭 PR266-CONTRACT-CLIENTS-RUNTIME-ENFORCE-01 + B2-T-06 CONTRACT-CLIENTS-NOT-COMPILED-INTO-RUNTIME 一并落地 | ✅ done | 16h + 8h |

### Track B · PG/RMQ/Redis 加固（14 PR / ~131h + ~67h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| B1 | ✅ PR-V1-DATA-OUTBOX-FENCING-AND-RELAY-HARDEN（fix/221-pg-outbox-fencing，**absorbs B6**）| B2-A-01 / 027#5 + B6 子项 B2-A-04/05/06/07/12 | PG outbox claim 无 fencing token，旧 worker 可覆盖新 worker 结果（P0）+ 顺路清 relay 5 项加固 | ✅ shipped: `lease_id UUID` 列 + 五个 CAS SQL 全部 `AND lease_id = $N`；ClaimedEntry 加 LeaseID；Mark*(...,leaseID) 签名；relay handleFailedEntry 观察 stale-lease bool；reclaimStale 加 LIMIT 1000；observability 注入移到 Validate 之前；MaxMetadataBytes=64KiB writer 守门；schema_guard SQL JOIN pg_namespace 返回 qualified name；3 个 archtest gate（OUTBOX-LEASE-ID-CAS-01 / OUTBOX-MARK-RETURNS-BOOL-01 / OUTBOX-METADATA-MAX-BYTES-01）；conformance suite 加 fencing race + stale-lease 子用例；ADR `docs/architecture/202605051600-adr-pg-outbox-fencing.md`；ref: graphile/worker complete_job + jackc/pgxjob claim CTE。**✅ FU 闭口 PR#384 (4674e214) N8 PR-V1-OUTBOX-FU-CLOSURE**：(a) migration 015 `ADD CONSTRAINT outbox_claiming_requires_lease CHECK (status <> 'claiming' OR lease_id IS NOT NULL)` 把 lease 不变量从 startup probe 升为 DB 级硬约束（旧二进制 INSERT/UPDATE 直接 23514 失败，reclaim CAS NULL=NULL 路径自然消解）；**删** `VerifyOutboxLeaseInvariant` + `ErrAdapterPGOutboxLeaseInvariant` errcode + `cmd/corebundle/bundle.go:430` 调用 + 对应 schema_guard 用例（删平行防御层，DB CHECK 单源）；新增 014/015 negative integration tests（`OutboxMigration014_AbortsOnClaimingResidue` + `OutboxMigration015_RejectsExistingClaimingNullLeaseRow` + `OutboxMigration015_RejectsUpdateIntoClaimingNullLease`）锁 INSERT/UPDATE 双写路径；(b) 014 加 `IF NOT EXISTS`；archtest `migration_no_transaction_rerun_safe_test.go` 扫所有 `-- +goose NO TRANSACTION` 文件强制每条 DDL 用 `IF NOT EXISTS` / `IF EXISTS` / `DO` 块（守 015 之后所有同类 migration）；(c) ADR cutover 段同步改写。won't-do `B2-X-09` corebundle 负向集成测试（DB CHECK 已使该残留不可达分支）登记 backlog2 触发条件：N3 顺路或新 wiring 路径出现。| ✅ done | 16h + 8h + N8 FU 3h+1.5h |
| B2 | PR-V1-PG-DOMAIN-REPO | X1 主线 / 027#10 | 5 域 Repository（User/Session/Role/Device/Command）+ 4 migration DDL 全缺，PG 接入主线 | 5 域 Repo + DDL + `CONFIG-VERSIONS-MIGRATION-01` + RBAC-ASSIGN-LEVEL-UPGRADE / SEED-ROLE-IFACE / ACCESS-LEVEL-AUDIT / AUTH-CACHE 联动激活 | L4 | 32h + 24h |
| B3 | PR-V1-PG-MULTIPOD-SETUP | A26-R1/R4 / 027#11 | 多 pod admin provision 无分布式锁；setup orphan e2e 缺 | ADMINPROVISION-DIST-LOCK + SETUP-ORPHAN-E2E | L2 | 5h + 3h |
| B4 | PR-V1-PG-MIGRATOR-HARDEN | POSTGRES-MIGRATOR / 027#12 | `goose advisory locker` 缺 + 9/10 顺序 fixture 缺，并发 Up 可冲突 | advisory locker + 并发 Up + 9/10 顺序 fixture | L2 | 4h + 2h |
| B5 | ✅ PR-V1-PG-REFRESH-CROSS-STORE（refactor/532-pg-refresh-cross-store）| AUTH-REFRESH / 027#13 | session repo + refresh store 跨 PG 事务非 ACID，rotate 链可断 | ✅ shipped: `sessionrefresh.Service` 加 `txRunner persistence.TxRunner` 字段 + `WithTxManager` option（fail-fast on nil，对齐 sessionlogin/sessionlogout）+ `Refresh()` 整体包入单个 `txRunner.RunInTx`：Peek → verifySession → fetchPasswordResetRequired → persistRefreshedSession → Rotate 共用一个 commit boundary，cascade-revoke 仍走 `RevokeSessionDetached` 保留 PR#395 detached-context 不变量；新增 archtest `REFRESH-CROSS-STORE-TX-01`（AST 静态：Refresh 必须恰含 1 个 RunInTx 调用 + 闭包非空 + 禁止 `s.refreshStore.Peek/Rotate` / `s.sessionRepo.Update/GetByID` / `s.userRepo.GetByID` 出现在闭包外）+ 单元测试 `TestNewService_RequiresTxRunner` 守缺/nil rejection；新增 `adapters/postgres/refresh_outer_tx_atomicity_integration_test.go`（testcontainers PG 三场景：Issue rolled back / Rotate rolled back / RevokeSessionDetached 跨 outer rollback 存活）。**Honest test-scope boundary**：今日已断 refresh 侧 rollback；session 侧 row rollback 待 B2 落地 `postgres.PGSessionRepository` 时由该集成测试自动加强（mem session repo 不支持 TX rollback 是 mem 固有属性，B5 wrap 不引入 regression）。`cell_init.go` 同步把 `sessionrefresh.WithTxManager(c.txRunner)` 注入构造 | ✅ done | 6h + 4h |
| B6 | ✅ PR#373（0c70a329）首批 absorbed by B1 + ✅ PR#380（0c06a484）payload cap + lost metric + reclaim cfg loop | B2-A-04..12 / 027#14 | observability 注入顺序 / RowsAffected==0 / reclaim LIMIT / metadata 字节上限 / schema_guard qualified name 5 处加固；后续追加 payload size 守卫 + relay lost metric + reclaim 配置化循环 | **首批 absorbed by B1（PR#373）**：五子项与 B1 文件域 100% 重叠（同 outbox_store.go / 同 relay.go），fencing 落地后 MarkRetry/MarkDead 因 stale lease 返回 false 是 B2-A-05 硬依赖。**PR#380 (0c06a484) 第二批加固**：(a) payload size 上限守卫 + `tools/archtest/outbox_payload_size_test.go`；(b) `kernel/outbox/relay_metrics.go` 新增 `outbox_relay_lost_total` counter + `tools/archtest/outbox_relay_lost_metric_test.go` 守卫；(c) reclaim 循环改 cfg 驱动（loop interval/batch size 可注入）+ `runtime/outbox/relay.go` 配置项扩展 | ✅ done | 16h+8h（首批 absorbed B1）+ 6h+3h（PR#380 增量） |
| B7 | PR-V1-PG-REFRESH-READYZ | B2-A-08..13 / 027#15 | refresh tx 拆双接口 / timing sidechannel / readyz 聚合 / constructor error-first / tx rollback 日志脱敏 5 项加固 | 5 项一并打包 | L3 | 12h + 6h |
| B8 | PR-V1-PG-CONNECT-STRICT | B2-A-18(PG 半) / B2-X-03 / 027#16 | PG `pool.go` 仅 ctx.Ping 无 adapter 级 connect 预算；invalid index 未 strict fail-fast | PG `WithConnectTimeout` 默认 5s 构造期 fail-fast + invalid index strict / readyz 联动（**RMQ 半已剥离至 B13**，本 PR 范围纯 PG） | L1 | 3h + 1.5h |
| B9 | PR-V1-REFRESH-IDLE-GRACE | X12/X14 / 027#17 | REFRESH-IDLE-EXPIRE + REFRESH-GRACE-COUNTER 缺（X13 分区延后） | IDLE-EXPIRE + GRACE-COUNTER（不含分区） | L2 | 5h + 3h |
| B10 | ✅ PR#382（8b6da59a）PR-V1-REDIS-CLUSTER | B2-A-03 / 027#18 | Redis 缺 Cluster 模式支持，AWS/Azure Cluster 无法接入（P0） | ✅ shipped: (1) `adapters/redis` 加 `ModeCluster` + `Config.ClusterAddrs` + `buildClusterOptions`（URL/plain mix 检测、TLSConfig 抽取空 ServerName、per-URL 凭据合并冲突检测）；`validateConfig` fail-fast (a) ClusterAddrs 空 (b) DB!=0 (c) Addr 与 cluster mode 共存；`validateEndpointTLS` for-loop ClusterAddrs；`Config.LogValue` 按 Mode 切换（cluster 模式 cluster_addrs）；`defaults()` PoolSize cluster 模式 5*GOMAXPROCS（vs standalone/sentinel 10*GOMAXPROCS）per-node sizing；(2) `adapters/redis/idempotency.go` `leaseKey`/`doneKey` 包 `{key}:lease` / `{key}:done` hashtag — CRC16 只 hash 业务 key 让两 KEY colocate same slot，避免 EVAL CROSSSLOT 拒绝；6 处 Lua 脚本 doc 同步；(3) `cmd/corebundle` 加 `GOCELL_REDIS_CLUSTER_ADDRS` env（逗号分隔 + trim/dedupe/空条目拒绝 + 与 `GOCELL_REDIS_ADDR` 互斥 + cluster 模式 DB=0 强制）；(4) integration_cluster build tag + `GOCELL_TEST_REDIS_CLUSTER_ADDRS` 环境驱动发现；archtest `IDEMPOTENCY-LUA-HASHTAG-01`（AST 校验 leaseKey/doneKey 遵守 `{key}:role` hashtag 模式 + mock dispatch 锁 suffix 匹配）| ✅ done | 8h + 4h |
| B11 | PR-V1-REDIS-KEYNS | B2-A-27（10分）/ 027#34 | Redis Idempotency / Nonce / Cache / DistLock 缺 cell 级 namespace prefix，多租户冲突 | 构造期注入 `KeyNamespace`（cell ID）自动 prefix；archtest 守 cell 级隔离 | L2 | 8h + 4h |
| B12 | PR-V1-RMQ-LIFECYCLE-HARDEN | B2-A-14 + B2-A-15 + B2-A-16 / backlog2 §5.2 | RMQ subscriber/connection/publisher 三处生产硬化缺位：(a) StopIntake 与 runCtx cancel 耦合，prefetch 缓冲未排空可能丢消息；(b) 每个 publisher/subscriber 创建 channel 无上限，可触发 broker `channel_max` 死锁；(c) publisher NACK / confirm timeout 静默失败，下游基于"成功发布"假设状态分叉 | (a) `subscriber.go` StopIntake 三段流程：先停拉新 → 排空 prefetch → 等 inflight ack；超时 fail-closed；(b) `connection.go` 加 `MaxChannelsPerConn`（默认 256）+ channel pool 复用；超限 fail-fast；(c) `publisher.go` NACK/confirm timeout 路径统一 `slog.Warn` + emit `publish_failed` metric，与 outbox relay 协调失败语义；TDD 红→绿（archtest + unit + testcontainers 三层），先 RED commit 再 GREEN 实施 | L2 | 10h + 5h |
| B13 | PR-V1-RMQ-CONFORMANCE-AND-CLOSURE | B2-A-17 + B2-A-18(RMQ 半) + **PR#379 review FU×2** / backlog2 §5.2 | (a) 缺 EventBus 真实语义集成测试（Ack / Requeue / Reject Permanent → broker 端 DLX/redeliver/ack 三分支断言）；(b) RMQ `DefaultDial` 直接调 `amqp.Dial` 无内置超时；(c) **PR#379 FU#1**：`docs/guides/adapter-config-reference.md` SubscriberConfig 表与真实 struct 漂移（写了不存在的 `ShutdownTimeout`，漏 `StopIntakePerCallTimeout` + 必填 `Clock`）；(d) **PR#379 FU#2**：subscriber 层 terminal 传播无回归 — `awaitReconnect` → `ErrAdapterAMQPConnectPermanent` 这条路径决定 EventRouter/Bootstrap 能否收到永久错误，但只在 connection 层有测试，subscriber 公开边界（`Subscribe` 返回值）无锁 | (a) testcontainers RMQ 三 fixture：handler 返回 Ack/Requeue/Reject Permanent，断言 broker DLX 入队 / 重新投递 / consumer ack 计数；(b) RMQ `WithConnectTimeout` 默认 5s 构造期 fail-fast；(c) 重写 SubscriberConfig 表对齐 `adapters/rabbitmq/subscriber.go:95-119` 真实 struct（删 ShutdownTimeout / 加 StopIntakePerCallTimeout 默认 2s / 加必填 Clock 注 fail-fast）+ 验证 Health/WaitConnected godoc 已正确（PR#379 已对齐）；(d) 新增 `subscriber_terminal_propagation_test.go` — mock connection markPermanent → WaitConnected 返回 permanentErr → 断言 `Subscriber.Subscribe` 返回 `ErrAdapterAMQPConnectPermanent`；TDD 红→绿 | L2 | 7h + 3.5h |

### Track C · cells 业务（6 PR / 56h + 27h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| C1 | PR-V1-DATA-HASHCHAIN-RECOVERY | B2-C-01/14 / 027#6 | auditcore hashchain 重启未恢复，链完整性无法验证（P0） | auditcore 启动从 repo 恢复尾节点 + 跨重启 continuity 测试 | L3 | 12h + 6h |
| C2 | PR-V1-BIZ-LOCKOUT | ACCESSCORE-LOCKOUT / 027#7 | 缺自动账户锁定业务实现（含 PG users.failed_attempts schema + migration + outbox + lock 事件） | sessionlogin slice + user domain + PG repo + migration + integration tests | L3 | 8h + 4h |
| C3 | PR-V1-TEST-JOURNEY-ROOT | TEST-JOURNEY-ROOT / 027#8 | 缺 root journey integration harness（J-useronboarding / J-accountlockout） | tests/integration root harness + 2 个 J yaml；顺带覆盖 B2-C-13 L2-CROSS-LAYER-E2E-REGRESSION | L2 | 12h + 4h |
| C4 | PR-V1-CONTRACT-INTEGRITY | backlog1 §2.2 / 027#9 | contracttest schema ref / endpoint→test mapping / path-query executable 三处闭环缺 | 3 子项打包：CONTRACTTEST-SCHEMAREF-FAILFAST + CONTRACT-ENDPOINT-TEST-MAPPING + CONTRACT-PATH-QUERY-EXECUTABLE | L3 | 12h + 6h |
| C5 | ⚠️ **PR#368 部分推进** PR-V1-KERNEL-IFACE-EXPLICIT | B2-K-03+04（21分）/ 027#35 | `asm.(assemblyWithCell)` 隐式 cast；`errcodeNameToStatus` 治理表手抄 errcode 漂移 | **PR#368 已闭合 errcode 治理表（B2-K-04）**：`kernel/governance/rules_http_response_alignment.go` 删除 70+ 行 `errcodeNameToStatus` 手抄 pairs，换 `errcodeKindNameToStatus` 由 `pkg/errcode.Kind.Status()` 单源派生（10 行 reflect-built 表）；`expected4xxCodes` 同侧删除走 `Kind.IsClient()`。**残余**：B2-K-03 `asm.(assemblyWithCell)` 隐式 cast → 显式 `AssemblyRef.Cell(id)` 接口仍需做 | ⚠️ partial | 残余 ~4h+2h |
| C6 | PR-V1-JOURNEY-AUDIT-E2E | P2-T-02（tech-debt 核验）| J-auditlogintrail journey 已在 yaml 定义，但 smoke_e2e_test.go 未锁该链路，auditcore → accesscore → sessionmint 事件链 e2e 闭环缺口 | 基于 C3 root harness 补 J-auditlogintrail 端到端断言；断言审计链写入 → 登录事件发布 → accesscore 投影三段完整性；依赖 C3 先合 | L2 | 6h + 3h |

### Track D · runtime / observability（11 项，多数推 v1.1 后）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| D1 | ✅ PR#359（4ffd972d）PR-V1-METRICS-CELL-LABEL | R2/T6 / 027#19 | 多 cell assembly 部署 HTTP metrics label 误归因；adapter env 未按 cell 拆 | ✅ HTTP-METRICS-LABEL-REALIGN + RouteGroup.CellID 注入；per-cell label archtest 守卫 | ✅ done | 6h + 4h |
| D2 | PR-V1-SHARED-DEPS-SPLIT | B2-X-02（10分）/ 027#36 | `cmd/corebundle/shared_deps.go` 单结构 ~20 字段，composition root 维护成本高 | 拆 4 sub-struct（coreDeps/eventDeps/httpDeps/metricsDeps），照 PR-A66 #333 BootstrapDecompose 范式 | L1 | 6h + 4h |
| D3 | PR-V11-OBS-OTEL-HARDENING（**拆 3 PR，2026-05-08**）| B2-R-02..09（9 项）+ B2-A-19/20/21 + PR237-OB2 + WS-HUB-READYZ-PROBE-01 + PR-CI-5-FU-HEALTH-LATE-WATCHER | configcore/auditcore readyz 缺 repo 健康探针 / bootstrap rollback 错误未并入返回值 / OTel provider/tracer/callback/cache 6 大生产缺陷 / Prometheus middleware 缺 internal listener / WebSocket Hub readyz / health late-watcher 泄漏 | **拆为 3 PR + 散落项吸收**：(D3a) `PR-V11-D3-R-METRIC-PACK` = R-01+R-02+G-05+PR237-OB2（kernel Provider GaugeVec + bootstrap auto-wire + outbox/event collector + internal mux metrics，文件域 `kernel/observability/metrics/*` + `runtime/observability/metrics/*` + `runtime/http/middleware/metrics.go` + bootstrap phase 5/6）= **26h+12h**；(D3b) `PR-B2-B3 OTEL-PROVIDER-LIFECYCLE-AND-GLOBAL` = B2-R-05/06/07/08/09 + B2-A-19/20/21（adapters/otel/* 8 条）= **14h+7h**；(D3c) `PR-B2-D4 CONFIGCORE-READYZ-REPO-PROBE` = B2-R-02（cells/configcore/auditcore HealthCheckers 加 repo Pingable）= **3h+1.5h**。其余散落 2 项：WS-HUB-READYZ-PROBE-01 → 并入 **D7 PR-V1-WS-OBS-AND-SHUTDOWN-HARDEN**（2026-05-08，原 PR-B2-F1 整体合并到 D7）/ PR-CI-5-FU-HEALTH-LATE-WATCHER → 并入 `PR-B2-E1 BOOTSTRAP-ROLLBACK-AND-HEALTH` | L3 | **43h+20.5h**（D3a 26h+12h + D3b 14h+7h + D3c 3h+1.5h；散落项工时计入 F1/E1）|
| D4 | PR-V11-AUDITAPPEND-MUTEX-DECOMPOSE | B2-C-10 | auditappend 全局 mutex 串行化 13 个 topic consumer，吞吐受限 | 按 topic 拆细粒度锁 / partition-aware claim | L3 | 8h + 4h |
| D5 | PR-V11-CONFIG-ROLLBACK-OPTLOCK | B2-T-01 | config rollback 缺 optimistic lock，并发回滚双写 | 加 version 字段 + CAS 回滚 | L2 | 6h + 4h |
| D6 | ✅ PR-V1-PROD-CLOCK-INJECTION（refactor/503-prod-clock-injection）| G6 调研衍生（2026-05-01）| 生产代码 97 处 `time.Now()` 直调，TTL/lease/expiry/审计窗口测试必须烧 wall-clock；3 个本地 `Clock` interface 互不收敛 | ✅ 新建 `kernel/clock/`（`Clock`+`Timer`+`Real()`）+ `kernel/clock/clockmock`（`FakeClock` w/ `Advance`/`Set`/`PendingTimers`）；3 个本地 Clock 全删，调用方直 import；生产 ~95 处 `time.Now/Since/Until` + 4 处 `time.NewTimer` 全部注入式；archtest `PROD-CLOCK-INJECTION-01` 落地（白名单 `kernel/clock/` + `pkg/securecookie/`）。**2026-05-02 follow-up（witty-dazzling-balloon）closure**：(a) gate 函数集扩到 9 项（+ `NewTicker / After / AfterFunc / Tick / Sleep`）+ 改 type-aware 解析（`info.ObjectOf(sel).(*types.Func)` + `obj.Pkg().Path() == "time"`），import alias / dot-import / 函数值引用 / 结构字段赋值四类绕过路径全覆盖；(b) `kernel/cell.Dependencies.Clock` 必填字段 + assembly typed-nil 防线（reflect-based）+ `clock.MustHaveClock` 公共守卫（PANIC-REGISTERED §5 auto-exempt）；(c) bootstrap/health/eventrouter/relay/distlock/watcher/refresh.gc/periodic/websocket/eventbus/consumer_base/hook_dispatcher/sweeper/auth/idempotency/governance + 全部 cells（accesscore + configcore + auditcore + sessionmint + initialadmin）+ adapters（postgres/rabbitmq/ratelimit/vault）+ tests/e2e 全部 `nil → clock.Real()` 兜底删除、`func() time.Time` pattern 与 `WithKeySetClock/WithVerifierClock/WithIssuerClock/WithServiceTokenClock/WithEnvClock/WithNonceClock` Option 删除，构造改 `clk clock.Clock` 必填位置参数；(d) Clock 接口扩 `NewTicker / AfterFunc / Sleep`，Real + FakeClock 同步实现（FakeClock 加 PendingTickers + 多间隔 catch-up + AfterFunc callback queue）；fixture 10 个（alias / dot_import / after / newticker / afterfunc / tick / sleep / func_value_ref / struct_field_assign + injected_clock_passes）锁 gate 行为；ADR 与本 roadmap 同步更新；`runtime/auth/keys.go` 与 worktree 201-wm2-key-rotation 含协调注释，KeySetClock Option 签名变更需 201 rebase。ADR 见 `docs/architecture/202605021500-adr-kernel-clock-injection.md`「Closure status」节；**修正**：roadmap 原写「11 处 `runtime.Gosched()` poll 屏障」实际全在 `_test.go`（FakeClock pending-timer 等待），生产代码 0 处，本 PR 不处理 | done | 24h + 12h（+ 8h closure）|
| D7 | **PR-V1-WS-OBS-AND-SHUTDOWN-HARDEN**（**2026-05-08 重命名 + 合并**：原 D7 PR-V1-WS-SHUTDOWN-OPS 吸收 `PR-B2-F1 WEBSOCKET-OBSERVABILITY-AND-LIMITS` + backlog `PR-CI-5-FU-WEBSOCKET-ORIGIN-CONTRACT`）| **shutdown 域**：WS-OPS-01/02 + WS-T-01/02 + WS-DX-02（tech-debt 核验）+ ADR M0-FOUNDATION adapters API 包装残余边界点；**observability 域**（吸收 PR-B2-F1）：B2-W-03 + B2-W-05 + WS-HUB-READYZ-PROBE-01；**配置契约**（吸收散落 backlog）：PR-CI-5-FU-WEBSOCKET-ORIGIN-CONTRACT | (a) **shutdown 超时硬编码 10s** + 逐连接串行关闭（千连接 Pod terminationGracePeriod 易超）+ Stop×external-cancel 并发竞争同一 shutdown CAS 的测试缺 + Conn 缺 RemoteAddr 诊断；(b) **`NewConn(id, principal, conn *websocket.Conn) *Conn`** 第三参暴露 SDK 具体类型，违反 P-D3「边界类型自有」（无外部调用方，唯一外部引用是 archtest 字符串）；(c) **Hub 无 Prometheus 指标** — 连接数/消息率/错误/慢 client 全黑盒；(d) **Hub 不实现 ManagedResource** — `/readyz` 在 stateIdle/stateStopping 时仍 200，K8s 无法阻断流量；(e) **Stop 同步 close all conns** 与 (a) 同根（B2-W-05 ≈ WS-OPS-02，去重一次到位）；(f) **Origin 契约三方面漂移** — `handler.go:35` Validate trim 后未写回 `cfg.AllowedOrigins`，handler_test 用裸 host (`example.com`) vs godoc 示例 `https://example.com` vs integration_test 不发 Origin 头 | (1) `ShutdownTimeout` 构造期配置化注入；(2) 有界并发 close（`errgroup` + semaphore）+ 超时强制收敛；(3) 补 WS-T-01 Stop×cancel 竞争测试 + WS-T-02 stopped 态 Broadcast/Send 行为测试；(4) `Conn` 加 `RemoteAddr() string`（借鉴 coder/websocket CloseNow 两阶段语义）；(5) `NewConn` 改 unexported `newConn`（仅本包 handler.go upgrade 调用），同步 archtest `tools/archtest/security_defaults_test.go` import 检测字符串；(6) 注入 metrics provider，暴露 `ws_connections{cell}` / `ws_messages_total{direction,outcome}` / `ws_send_duration_seconds`；(7) `Hub` 实现 `lifecycle.ManagedResource.Checkers()` 返回 `{Name:"websocket_hub_ready", Check: state==stateRunning ? nil : err}`，bootstrap 注册到 ready aggregator；同步 `adapters/websocket.UpgradeHandler` 加 hub-not-running 503 path；(8) integration_test.go 加真实 `Origin: https://example.com` 头握手测试锁死行为，再决定 (1) Validate 写回规范化值 / (2) 统一裸 host vs 完整 origin 文档示例 | L3 | **17h + 8h**（D7 9+4.5 + B2-W-03 metrics 4+1.5 + WS-HUB-READYZ 2+1 + Origin 1.5+1 - B2-W-05↔WS-OPS-02 去重 0+0；B2-W-05 仅在 D7 计工时）|
| D8 | ✅ PR#367（e8f6a820）PR-V1-HTTP-CHI-TO-STDLIB | Go 1.25 modernization（2026-05-05）| `runtime/http/router` 依赖 `go-chi/chi/v5` 历史决定，Go 1.25 stdlib `*http.ServeMux` 已支持模板化路由 + pattern 抽取，第三方依赖可下；保留 chi 阻碍未来 stdlib middleware/observability ecosystem 复用 | ✅ shipped: `Router` 内核换 `*http.ServeMux` + `patternRecordingMux` dispatch wrapper；`nativeMuxAdapter` 替换 `chiRouterAdapter`；`enumerateRoutes` 由 `recordRoutePattern` 直接维护；archtest `ROUTER-ATTRIBUTION-01` 同步；`go.mod` 删 `github.com/go-chi/chi/v5` | ✅ done | 029 范围外 |

> Track D 还有 5+ 项 backlog2 §3 OTel 残余加固，由 `D3` 内部分批，详情见 `docs/backlog2.md` §3。
>
> **D6 状态**：✅ 已完成（refactor/503-prod-clock-injection）。G9 触发条件随之解锁——D6 后 TTL 类 sleep 已可由 `clockmock.Advance` 消除，G9 可设激进 threshold（建议 2s）+ allowlist 兜底 OS-sync/sleep-即-SUT 类残余。

### Track G · CI / lint / governance（10 PR / 残余 16h dev + 8h review；G1/G2/G4/G5/G6/G7/G8 ✅；G3/G9/G11 待）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| G1 | ✅ PR#347（588376d5）PR-V1-CI-LINT-BATCH-1 全收 | ci-governance / 027#27 | ✅ misspell/unconvert PR#342 + goimports PR#347（并入 G2 PR 一并 ship） | — | ✅ done |
| G2 | ✅ PR#347（588376d5）PR-V1-CI-LINT-BATCH-2A | ci-governance / 027#28 | ✅ depguard（5 层规则）+ forbidigo（含 `time.Sleep in *_test.go`）+ revive + goimports + EXPORTED-ERROR-NEW archtest gate 全部 ship PR#347 | — | ✅ done（bonus: EXPORTED-ERROR-NEW 新 archtest gate）|
| G3 | PR-V1-CI-LINT-BATCH-2B | ci-governance / 027#29 | errorlint/bodyclose/durationcheck 未启，`%w` wrap 漂移 / HTTP body 泄漏 / 量纲错可滑过 review | 启 3 lint + 全仓收口 | L2 | 12h + 4h |
| G4 | ✅ PR#350（335ac1d3）+ ✅ PR#351 follow-up + ✅ PR#364（38234bff）pre-push hook | SHELL-SAFETY-01 / 027#22 | ✅ PR#350 加 `set -euo pipefail` + regex verify gate + CodeQL manual build；PR#351 follow-up 把 regex gate 换成 K8s-style `verify-shellcheck.sh`（pinned shellcheck v0.9.0 + docker fallback，exclude SC1090/1091/2230，对齐 kubernetes/kubernetes），CodeQL 改走 `make check-build` 单一编译入口；**PR#364 follow-up**：opt-in `.githooks/pre-push` 跑 `hack/verify-generated.sh`（~3s 本地 vs ~3min CI 回环），enable=`git config core.hooksPath .githooks` / bypass=`VERIFY_SKIP=generated git push`；CI 仍是 source of truth | — | ✅ done |
| G5 | ✅ PR#353（5b08d41b）PR-CI-3-V1-RESPONSE-EVOLVE | FMT-20 / 027#23 | v1 响应 schema 30 处顶层 `additionalProperties` 不放宽，新增字段需 v2 | ADR `202605031600-adr-v1-schema-evolution.md` + FMT-20 收窄到 request-only（lenient response/event, strict request）+ 30 response/event schema 放宽 + `B2-C-08` cell event decoder `DisallowUnknownFields` 同步关闭 | ✅ done | 13h + 6h |
| G6 | ✅ PR#346（cd0f8b30）PR-CI-4-TEST-TIME-LITERAL-01 | TEST-NO-SLEEP-01 / 027#24 + 2026-05-01 explore 调研 | ✅ 一级 6 family + Eventually 字面量全部收口；`tools/archtest/test_time_literal_test.go` + `hack/verify-test-time-literal.sh` + CI gate 已 ship | — | ✅ done；衍生 G9/D6 见对应行 |
| G7 | ✅ PR#347（588376d5）PR-V11-CI-LINT-BATCH-3 全收 | ci-governance B3 / 028 B3-5 | ✅ gosec PR#342 + revive PR#347 全部 ship；gosec 残余 G401/G104 视生产 finding 量评估，当前 unexclude 状态（finding=0 则不必解封）| — | ✅ done |
| G8 | ✅ PR#371（db493d7b）+ PR#377（8af79a9a）PR-V11-CI-LINT-BATCH-4（refactor/516-ci-lint-batch4-gofumpt + refactor/518-codegen-gofumpt-formatter）| ci-governance B4 / 028 B3-6 | ✅ (a) consumer (PR#371)：gocritic PR#342 + nolintlint PR#344 + gofumpt 全仓 reformat（`.golangci.yml` `formatters.enable += gofumpt` + `golangci-lint fmt` 一次性 reformat 98 文件，4 处合并后超 lll 140 改用多行 struct literal，CI `golangci-lint run` v2 自动校验 formatter）；(b) producer (PR#377)：`tools/codegen/render.go` 导出 `FormatGoSource`（goimports → gofumpt LangVersion `go1.25`），`tools/codegen/cellgen/scaffold.go` + `tools/generatedcatalog/catalog.go` 收敛到统一出口；`go.mod` 加 `mvdan.cc/gofumpt v0.9.2`（**必须**等于 golangci-lint v2.11.4 vendored 版本，否则 producer / consumer 漂移；二轮 review 把 v0.10.0 降到 v0.9.2 就是为了关闭这个分裂）；3 处 producer round-trip fence 单测 + `make fmt` 入口 + `hack/verify-gofumpt.sh` formatter-only gate | — | ✅ done |
| G9 | PR-V11-SLOW-TEST-BUDGET | G6 衍生（2026-05-01）| D6 已 ship，TTL/lease/expiry 类 sleep 已可由 `clockmock.Advance` 消除；残余 ~50 处 sleep 是 OS-sync（fsnotify/goroutine 阻塞观察）+ sleep-即-SUT（debounce 窗口/负测）两类不可消除；无单测 wall-time 阈值则慢测漂移无感知 | `tools/slowgate/` 二进制 + CI step `go test -json \| slowgate --threshold 2s --allowlist <list>`；allowlist 从残余 `//archtest:allow:test-sleep` 反推 OS-sync/sleep-即-SUT 测试名 | L2 | 4h + 2h | 触发条件：D6 合入后即可起 |
| G11 | PR-V1-TEST-GATE-CLOSURE | P3-TD-02/P4-TD-08/P4-TD-11（tech-debt 核验）| `adapters/postgres` 包无覆盖率门禁阈值，CI 仅采集不阻断；Migrator.Down v=0 重复调用回归锁不足 | `adapters/postgres` 包级覆盖率 gate ≥ 60%（渐进，对齐 kernel/ 90% 路线）+ workflow 阻断步骤；补 Migrator.Down v=0 幂等回归测试（多次调用不报错、RowsAffected==0 正确处理）| L1 | 4h + 2h |

### Track F · 供应链 / 文档 / 治理（6 PR / 49h + 18h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| F1 | PR-V1-POSITIONING-AND-LIBS | E5/E14 / 027#26 + **PR#376 review F3 absorb（2026-05-06）** | 外部认知锚定缺 — README 无 elevator pitch，无对标定位（重框架/强结构化），无通用库清单；**追加**：F3 入门文档与 generated artifact 矩阵漂移 — README tutorial 缺 cell 元数据、build 指令陈旧 + `docs/guides/cell-development-guide.md` 教手写 `wrapper.ContractSpec{}` 范例 + 引用已删除 FMT-18 + 多处与 PR#376 后的真相源不一致 | `docs/architecture/positioning.md` + README pitch + 通用库清单（Tier A 5 + Tier B 20 = 25 包）；**追加范围（吸收 F3 README + cell-guide 部分）**：(a) README tutorial 补必填 cell 元数据 + 修 build 指令 + cell metadata 章节走 contractgen workflow；(b) `cell-development-guide.md` 删手写 `wrapper.ContractSpec{}` / `wrapper.EventSpec{}` 范例改为 contractgen workflow + 删 FMT-18 引用；(c) 顺路 grep `docs/**` 扫 `wrapper.ContractSpec{` / `wrapper.EventSpec{` / `FMT-18` 残留全量清理。**doc.go 部分由 06.FU 收**（typed envelope 本来就要重写 contractgen/doc.go） | L1 | **6h + 2.5h**（原 5h+2h + 1h+0.5h F3 docs drift） |
| F2 | PR-V1-DOCS-MULTIPOD | PR237-PM5/PR-A58 / 027#20 | dual-listener 部署指南缺；examples 注释缺；compose 缺 healthcheck | 部署指南 + examples 注释 + compose healthcheck | L1 | 6h + 2h |
| F3 | ✅ PR#345（4b680792）PR-V1-SUPPLY-CHAIN | E7-1 / 027#30 | ✅ govulncheck + Semgrep + CodeQL + 独立 race workflow 全部 ship | — | ✅ done |
| F4 | PR-V11-API-VERSIONING-POLICY | E7-4 / 028 B3-1 + **PR#376 review F5 absorb（2026-05-06）** | contract 缺 deprecation 机制，`storageVersion` 概念缺；**追加**：F5 governance 不拒绝 `/internal/v1/*` 与 `auth.public` 组合（`kernel/governance/rules_fmt.go` + `contracts/contract.schema.json` 仅编码 `public` vs `passwordResetExempt`，runtime 拒绝在 route.go/router.go，contract 层仍允许语义破损组合，当下无 live 违规但属预防性漏洞） | storageVersion + 弃用窗口策略 + kube-api-linter 评估 + `lifecycle: deprecated` + `removeBy: vN.M` 字段 + 治理强制 4 周 deprecation；**追加范围（吸收 F5 internal+public 互斥）**：(a) `rules_fmt.go` 加 cross-rule（占位 FMT-21）：`path` 以 `/internal/v1/` 前缀 ⊕ `auth: public` 互斥；(b) `contracts/contract.schema.json` 加 conditional require（`if path startswith /internal/v1/ then auth ∈ {service, bootstrap}`）；(c) 顺路加 `auth ∈ {public, service, bootstrap, passwordResetExempt}` 闭集守卫；(d) 负向 fixture 喂入 governance test | L2 | **9h + 4.5h**（原 8h+4h + 1h+0.5h F5 governance rule） |
| F5 | PR-V11-DOCS-AUTOMATION | E7-5 / 028 B3-2 | API reference HTML 全手写，与 contract.yaml schema 漂移；ADR 模板不统一 | gen-crd-api-reference-docs（K8s 范式）从 contract.yaml schema 自动生成 + KEP frontmatter 风格 ADR 模板 | L2 | 8h + 4h |
| F6 | ✅ #338 (1958a5a8) PR-CFG-I-AUTH-FAIL-CLOSED | I.X1/X3 / 027#21 | NonceStore 非 fail-closed / nil ring fail-fast / sessionlogin double-cleanup 等 6 处加固 | 6 项一并打包 | L3 | 14h + 6h |

### Track J · DevTools Platform（✅ 全部 done；admin-gated 默认）

> J1 + J2（absorbed）已随 PR#357（867c136b）全部 ship。callgraph (P3) / runtime topology (P4) 触发条件未达 — runtime topology 不自建，走 OTel + Jaeger/Tempo 现成生态（D3 Track 承接）。

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| J1 | ✅ PR#357（867c136b）DEVTOOLS-CATALOG-UNIFIED-01 | backlog PR-A37 升级 / 2026-05-02 三档暴露讨论 / J2 absorb / 2026-05-04 review 后架构修正 | gocell-web 单独 clone 后 DevTools 元数据不可用（前端 Vite glob 读 sibling repo）；缺生产可访问的统一 metadata 入口；+ 包级 typed depgraph 一并暴露（合并 J2 范围） | ✅ 双形态 (a) `gocell export catalog` CLI 输出静态 JSON；(b) `GET /api/v1/devtools/catalog` HTTP handler admin-gated；query 参数 kinds/layers/cells/include 任意组合；HTTP packageDeps 由 `cmd/corebundle/catalog_gen.go` build-time generated graph 提供；wire format 落在 `runtime/devtools/catalog/`（archtest `KERNEL-METADATA-NO-WIRE-01` 守卫）；借鉴 Backstage Catalog Entity model wire format（不引依赖） | ✅ done | 20h + 6h |

**Track J 依赖关系**：
- J1 → ✅ PR#357 done（J2 范围已合并，包级 typed depgraph 通过 `?include=packageDeps` / `--include=packageDeps` 暴露）
- J2 → absorbed into J1（独立 PR 不再需要）
- callgraph (P3) won't-do until 用户反馈"package 粒度不够"
- runtime topology (P4) won't-do — 用 OTel + Jaeger/Tempo

---

## Won't-do / 触发条件待达

| 来源 | 项 | 不做理由 / 触发条件 |
|---|---|---|
| baseline | **E8** bootstrap 显式化（phase 拆 8 函数） | 与 E5 positioning「重框架，强结构化」张力；`bootstrap.Run()` 单入口是卖点 |
| baseline | **E12** Actor 合并入 Cell（`type=external`） | 破坏 Cell vs Actor 语义清晰度（Cell = 你的代码，Actor = 外部依赖）；词汇收敛 5% 体感改进 << 语义清晰度代价 |
| backlog | **OBS-SSA-ANALYZER-01** | 当前 typed archtest（#01 后）已够；SSA 留给跨包 taint 真实需求出现时 |
| backlog | **CHANGEPASSWORD-CONCURRENT-SEMANTICS** | 客户反馈触发；当前 OUT_OF_SCOPE 共识 |
| backlog | **X13** REFRESH-PARTITION-01 | PG 分区，等生产流量阈值 |
| backlog | **PR-A8-RESIDUAL** Vault K8s auth 真实 e2e | 部署目标平台明确后做 |
| backlog | **S14a** CONFIG-VALUE-KMS-AWS-PROVIDER | 部署目标云平台明确后触发 |
| backlog | **T5** AUTH-SIGNER-01（`SigningKeyProvider` → `crypto.Signer`）| 2-3h，无外部依赖（stdlib + jwt/v5 已支持），caller 需 HSM/KMS/EC 私钥时优先 ship；2026-05-01 解除 golang-jwt v6 gating |
| backlog | **T1-T4 / T6-T10** 其余触发条件项 | 触发条件未达成 |
| 025 | Wave 4 PR-A22/A23/A24/A33 | 部分被 #02/#03 间接吸收，残余触发条件未达成（PR-A21 已移除：JWT 抽象 won't-do、T5 不再 gating，2026-05-01） |
| Track G | **G10** PR-V11-MULTI-OS-GATE（archtest multi-GOOS scan）| 当前 `os-smoke` workflow 兜底已够；平台 tag 文件 sleep/literal 逃逸概率极低；引入 OS matrix 必跑成本 > 收益（2026-05-02）|
| Track J | **J3** DEVTOOLS-CALLGRAPH-API（function 粒度静态 callgraph，`x/tools/go/callgraph` cha/rta）| J1 unified catalog (含 packageDeps) 落地后看用户反馈；package 粒度若已够则不必下钻到 function；触发条件 = J1 ship 后用户明确提出"package 粒度看不出问题" |
| Track J | **J4** DEVTOOLS-RUNTIME-TOPOLOGY-API（实际请求 trace 拓扑）| 不自建 — runtime topology 由 OTel + Jaeger/Tempo 生态承接（D3 已在做 OTel 加固）；自建 API 与 Jaeger UI 重叠且生态成熟度不及，永久 won't-do（2026-05-02）|
| K#12 review | **PR-V11-OUTBOX-MIDDLEWARE-DUAL-LIST**（双 middleware 列表分离业务/框架 + 框架层列表化扩展点）| **重方案**：分离 `EntryMiddleware`（业务签名 EntryHandler→EntryHandler）+ `SubscriberMiddleware`（框架签名 SubscriberHandler→SubscriberHandler）双列表；新增 `bootstrap.WithEntryMiddleware` + `WithSubscriberMiddleware` option（取代 `WithConsumerMiddleware`）；~400-500 LOC（含测试）。**Won't-do 理由**：违反 YAGNI — 当前框架 middleware 仅 observability ctx restore + ConsumerBase 转换 + contract tracing 三件内置 wiring，具体装配完全够用；框架层列表化扩展是为假设性未来需求加抽象。PR#369 follow-up 采用 concrete `NewContractTracingSubscriber`，不新增公共 framework middleware list。**触发条件**（任一达成才起 PR）：(a) 框架 middleware ≥ 3 且需要用户自定义排序/替换（如细粒度 audit / dead-letter routing / 多租户 throttle）；(b) 用户/外部贡献者明确要求自定义框架 middleware 注入点；(c) 测试覆盖需要按 framework middleware 维度切片解耦且 concrete decorator 已不足。**中方案已于 Wave 4 全量实施**（commit 98632e02 + K#12 Wave 4 二轮 review 修复 PR：删 `AsMiddleware` + `SubscriptionMiddleware` 回退为 `func(sub, next EntryHandler) EntryHandler` + `*ConsumerBase` 字段化注入 `SubscriberWithMiddleware` + 删 `EntryToSubscriberHandler` lift 函数 + 删 `SubscriberWithMiddleware.Subscribe(SubscriberHandler)` 防 lift→discard 回归），lift→discard 根因彻底消除。**重方案仍 won't-do**（YAGNI 未变）：永久 won't-do 状态由 reviewer 在 (a)-(c) 任一触发时升格为 PR-V11-OUTBOX-MIDDLEWARE-DUAL-LIST 重新评估（2026-05-05）|

---

## 参考

- **真理源 backlog**：`docs/backlog.md`（主，Cx3/Cx4）+ `docs/backlog2.md` §2-§8（B2-K/T/A/X/W/R/C P0/P1/P2 区域），不归档，只追加状态
- **archive plan**（追溯历史决策时打开）：027 `docs/plans/archive/202604301129-027-release-v1.0-readiness.md`（36 PR 全文 + 5 维评分体系定义）+ 028 `docs/plans/archive/202604301204-028-post-v1.0-leverage-and-v1.1-roadmap.md`（13 PR + Batch + won't-do）
- **engineering-baseline**：`docs/plans/engineering-baseline/202604300600-radical-lightweight-revision.md` + `202604300700-extension-leverages.md`（E1-E14 杠杆评分原文）
- **ci-governance**：`docs/plans/ci-governance/202604300430-golangci-tier12-priority-and-projection.md`（Batch 1/2/3/4）
- **ship skill**：`.claude/skills/ship/SKILL.md`（L1-L4 + 工时区间）
- **git baseline**：`origin/develop @ 674496d1`（2026-05-08 update：**PR#387 [M0-FOUNDATION] poolstats descend to kernel ✅**（dc306199）— `runtime/observability/poolstats` 整包 mv 到 `kernel/observability/poolstats`（zero-dep 契约：1 Snapshot struct + 1 Statter interface），11 处 import 替换（adapters/{postgres,redis,rabbitmq,otel} + self-import），新增 archtest `KERNEL-POOLSTATS-LOCATION-01a/01b`（01a 禁旧 `runtime/observability/poolstats` 路径防退化 / 01b 强制 `kernel/observability/poolstats` 生产 .go 仅 stdlib import）；**ADR `docs/architecture/202605041430-adr-architecture-optimization-via-engineering-thinking.md` §M0-FOUNDATION 4 项标全部合规**（poolstats=PR#387 / Noop=`Nooper`+`CheckNotNoop` 等价异形 superseded / CellMetadata=`kernel/metadata/types.go::CellMeta` 单源 + `CELLMETA-SINGLE-SOURCE-01` archtest + codegen K#04/K#05 守卫 / adapters API 包装=`AMQPConnection`/`AMQPChannel` interface + pgx via `kernel/persistence/txctx.go::TxCtxKey` + cell-side `tx interface` 实质完成；`*sql.DB` 不适用 — 项目用 pgx）；ADR 残余边界点 `adapters/websocket/conn.go::NewConn(*websocket.Conn)` 改 unexported 折入 D7 PR-V1-WS-SHUTDOWN-OPS（工时 8h+4h → 9h+4.5h）。**PR#386 06.FU3 PR-V1-ARCHTEST-AST-FULL-AUDIT ✅**（f6edea47）— Wave 1 RED 6 negative fixtures + 7 negative tests / Wave 2 GREEN: F6 改 param-scoped block-binding（BlockStmt 定位 PathValue/Query 调用域 + 同 block 内 IfStmt len 检测，requireMin/requireMax 独立 gate）+ 全量审查统一 `metadata.NewParser(root).Parse()` 单根扫（cells/ + examples/*/cells/ 一并覆盖）+ 6 个硬编码多根 archtest 全迁。**PR#385 PR-V1-CIRCUITBREAKER-INHOUSE-ERRCODE ✅**（df6b5448）— W1 A-05 errcode 收口（`fmt.Errorf` → `errcode.ErrAdapterCircuitBreakerConfig`）+ `clock.Clock` 构造期注入（D6 PROD-CLOCK-INJECTION-01）+ **删 sony/gobreaker/v2 依赖换自研 generation+expiry 状态机** + 7 branch tests + FIND-002/004/005/007 review 闭口；ref: sony/gobreaker v2/gobreaker.go（generation+expiry model adopted, no runtime dependency）。**`674496d1` fix(test) stabilize flaky CI tests** — auth invalid-MAC 测试 + websocket fake-clock token-expiry 测试稳定化 + RabbitMQ conformance readiness FU 登记。上轮 — **PR#384 N8 PR-V1-OUTBOX-FU-CLOSURE ✅** — DB CHECK `outbox_claiming_requires_lease` (migration 015) + 删 `VerifyOutboxLeaseInvariant` probe + ConsumerBase `built` sentinel + `IsConstructed()` 接口 + `Subscription.Validate` 三字段单源 + 删 `wrapper.MustWrapSubscriber` + `NewContractTracingSubscriber` 改返 error + RMQ commit_failed 路径 release-first + testcontainers redelivery 集成测试 + `migration_no_transaction_rerun_safe_test.go` archtest + 014/015 PG migration negative integration tests，关闭 K#12 + B1 全部残余 FU；上轮 **PR#376 06.PR4 PR-V1-CODEGEN-FULL-MIGRATION ✅** — 方案 D 末段 W1-W4：W1 contractgen 给 event contract 多产 spec_gen.go + subscription_gen.go / W2 64 contract codegen:true + 全量 regen + CH-04/05 修 + 删 dead `event.flag.changed.v1` / W3 4 archtest gates GREEN（CELLS-NO-WRAPPER-CONTRACTSPEC-IMPORT-01 + NO-MANUAL-CONTRACTSPEC-LITERAL-01 + EVENT-SUBSCRIPTION-CONTRACTGEN-COVERAGE-01 + CONTRACT-KINDS-CLOSED-SET-01）+ 删 `wrapper.EventSpec` + FMT-18 / W4 K-06 残余 configcore consistencyLevel:L2 + scaffold golden test；同轮 **PR#383 N3 PR-V1-030-G02-ROLLBACK-CTX-DECOUPLE ✅** — `rollbackCells` 派生 fresh ctx + `phase0ValidateOptions` warn 部署窗口安全余量；上轮 **PR#382 B10 PR-V1-REDIS-CLUSTER ✅** — ModeCluster + hashtag idempotency keys + corebundle env + integration_cluster + archtest，关闭 B2-A-03；同轮 **PR#381 A1 PR-V1-SEC-WS-AUTH-ACL ✅** — Authenticator + BroadcastFilter/BroadcastToSubject + token expiry + slow-client evict + websocket-integration.md，关闭 B2-W-01/02/06；上轮 2026-05-05 晚：**PR#380 K B6 PG-OUTBOX-RELAY-HARDEN payload cap + lost metric + reclaim cfg loop ✅** + **PR#379 A4 RMQ runtime reconnect permanent classification ✅** + **PR#378 N4 G-03 VALIDATE-CTX-PROPAGATION ✅** + **PR#377 G8 (b) gofumpt producer pipeline + verify gate ✅** + **PR#375 N1 follow-up snapshots visibility started state ✅** + **PR#374 K#12 follow-up close delivery settlement lifecycle ✅** + **PR#373 B1 PG outbox claim fencing token + B6 relay harden ✅** + **PR#372 N2 G-01 HOOK-DISPATCHER-LIFECYCLE ✅** + **PR#371 G8 (a) consumer-side gofumpt repo-wide reformat ✅**；上轮：**PR#369 K#12 OUTBOX-RECEIPT-EXTRACT ✅** + **PR#368 pkg breaking cleanup**（⚠️ #08 partial Kind-based ctor + ⚠️ C5 partial errcode 治理表收敛 + ✅ closes ERRCODE-CLASSIFY-SINGLE-SOURCE-01/B2-R-04/PR-B2-E2/B2-K-04）+ **PR#370 030 N1 assembly snapshots race fix ✅** + PR#367 D8 chi→stdlib net/http ✅ + PR#366 A3 wrapper-redactor fail-closed ✅ + PR#365 K#05 PR-A2+B markergen+cellgen unified ✅ + PR#364 G4-followup pre-push verify-generated ✅ + PR#363 K#05 PR-A1 CellMeta single source ✅ + PR#362 A5 svctoken-caller-identity ✅ + PR#361 K#06 PR-2 contractgen ✅）

---

## 030 review 融合方案（2026-05-05）

> 来源：`docs/plans/202605051600-030-review-0504-implementation.md`（16 份 0504 review 报告 ~80 条 finding 去重）
> 裁剪范围：K-02 / K-07 / 030 Track A/C/F/G(残余)/J 全部押后；本轮只融合 kernel + runtime 稳定性高杠杆项

### A. 并入既有 029 PR（A 档）

| 030 项 | 并入目标 | 范围扩展 | 增量工时 |
|---|---|---|---|
| **K-06** CELL-METADATA-SINGLE-SOURCE（残余）| **06.PR4** | ~~archtest `CELL-YAML-GO-PARITY-01` 双向校验~~ — 已被 K#05 PR-A2+B 物理消除 Go literal 取代（守卫由 `NO-METADATA-LITERAL-IN-CELLGO-01` + `MARKERGEN-DRIFT-VERIFY-01` 承担）；残余 = configcore 顶层补 `consistencyLevel` + scaffold cell 模板同步 `loadCellMetadata()` pattern | +2h/+1h（原 6h/3h，K#05 吸收节省 4h/2h）|
| **K-05a** KIND-PROJECTION-ENUM-RETAIN（决议）| **06.PR4** archtest follow-up | N7 决议保留 4-kind 不变（projection 0 实例但 v1.1 CQRS 端点需要，YAGNI 反向不成立 — 删了 1.1 重做）；archtest `CONTRACT-KIND-ENUM-CLOSED-01` 守卫 `kind ∈ {http,event,command,projection}` 闭集 | +1h/+0h |
| **K-05c** NO-MANUAL-CONTRACTSPEC-LITERAL → **强化为三条 archtest + event 派生** | **06.PR4** 末段 archtest | N7 决议第 (c) 项由 06.PR4 完成时自动闭环 — contractgen 已让 ContractSpec 派生自 ContractMeta（生成 `iface_gen.go::Contract var`）；**升级范围（2026-05-05）**：(a) contractgen 给 event contract 多产 `spec_gen.go` + `subscription_gen.go`（消除 cell 写 `wrapper.EventSpec(...)` 字面量的最后不对称缺口）；(b) 删 `kernel/wrapper.EventSpec` 便利函数；(c) `ContractSpec` godoc 警示「cell 不直接构造」；archtest 由单条 `NO-MANUAL-CONTRACTSPEC-LITERAL-01` 扩为三条：+ `CELLS-NO-WRAPPER-CONTRACTSPEC-IMPORT-01`（cells/** + examples/**/cells/** 禁 import `wrapper.{ContractSpec,EventSpec}`）+ `EVENT-SUBSCRIPTION-CONTRACTGEN-COVERAGE-01`（每个 `kind:event` contract 必须有对应 `subscription_gen.go`）；FMT-17 governance 单向校验降级为冗余可清退 | ~~+2h/+1h~~ → **+8h/+4h** |
| **K-08(a)** AssemblyMeta schema 扩 + **K-08(c)** modules_gen.go 派生 | **K#10** ASSEMBLY-YAML-MINIMAL | (a) schema 加 `owner`(必填) / `maxConsistencyLevel`(派生校验) / `validDeployTemplates={k8s,compose,binary}` enum；(c) `gocell generate assembly` 派生 `cmd/{id}/modules_gen.go`（cell ID → CellModule 工厂），`cmd/corebundle/run.go` 手工 switch 删除；复用 K#04 已 ship 的 codegen render/writer/verify pipeline | +10h/+5h |
| **K-08(b)** scaffold assembly | **K#09** SCAFFOLD-ONE-CMD | 第 4 个子命令 `gocell scaffold assembly --id=... --cells=... --deploy=k8s` 一键产出可编译 assembly | +6h/+2h |
| **R-01** EVENT-OBSERVABILITY-METRIC-PACK + **R-02** EVENTBUS-DROP-CONTEXTUAL-LOG + **G-05** OUTBOX-CONSUMER-COLLECTOR + **PR237-OB2** INTERNAL-LISTENER-METRICS（吸收 2026-05-08）| **D3a**（命名 `PR-V11-D3-R-METRIC-PACK`，是 D3 第一批）| (R-01) `kernel/observability/metrics.Provider` 加 `GaugeVec` + NopProvider；shutdown/outbox/event 三套 collector 工厂收口到 `runtime/observability/metrics/{shutdown,outbox,event}.go`；bootstrap phase 5/6 自动 wire `metricsProvider`；新增 `event_router_{subscriptions_active,setup_errors_total,ready_wait_seconds}` + `outbox_pending_depth` Gauge + `outbox_consumer_rejected_total{cell,topic,reason}` + `eventbus_dropped_total{reason}`；consumer reject 日志升 Error 级。(R-02) `InMemoryEventBus.broadcast/roundRobin` drop 路径升 Error 级 + 补 entry_id/aggregate_id/event_type 三字段。(G-05) retry budget 耗尽 reject counter 由 R-01 `ConsumerCollector` 接口注入。**(PR237-OB2 顺路收)** internal mux 挂 `middleware.Metrics`：`Provider` 加 `listener` label（primary/internal）或新增 `WithInternalMetricsCollector` option，让 `/internal/v1/*` 流量进入 `http_requests_total` / `http_request_duration_seconds`；文件域与 R-01 phase 5/6 auto-wire 100% 重叠 | 22h+11h + **+4h**（PR237-OB2 吸收）= **26h+12h** |
| **G-04** KERNEL-INTERNAL-DAG-GUARD | **K#01 follow-up archtest**（独立小 PR）| `tools/archtest/` 新增 `KERNEL-INTERNAL-DAG-01`：固化 22 个 kernel 子模块合法上下游（assembly/wrapper 顶层、crypto/clock/ctxkeys 叶子）；与 030 K-03 doc.go 双护栏 | 8h+4h |

### A'. 独立 small PR（A 档不便并入）

| 030 项 | PR 名 | 范围 | 工时 |
|---|---|---|---|
| **R-03** BOOTSTRAP-NIL-OPTION-CONSISTENCY | `PR-V1-030-R03-BOOTSTRAP-NIL-OPTION` | `WithManagedCloser(nil)` 与 `WithManagedResource(nil)` 行为统一 fail-fast；option 函数记录 nil 标志，phase0 拒绝 | 2h+1h |

### B. Standalone 优先（kernel/runtime P0/P1，本周并发 + 下周 ADR 串行）

| 顺序 | PR 名 | 030 项 | 类型 | 范围 | 工时 |
|:-:|---|---|---|---|---|
| **N1** | ✅ PR#370（5795e9eb）+ PR#375（bb4ff753）`PR-V1-030-K01-ASSEMBLY-SNAPSHOTS-RACE-FIX` | K-01 | P0 race | ✅ shipped: PR#370 `assembly.startInternal` Phase1 改局部 `localSnaps`，全部成功后 `a.mu` 锁内一次赋值；`TestAssembly_StartConcurrentSnapshots_RaceDetector` + `tools/archtest/assembly_snapshots_locked_test.go` archtest 守卫。**PR#375 follow-up** — bind snapshots visibility to started state（snapshots 仅在 started=true 后对 reader 可见，规避半启动 race window）| ✅ done | 4h+2h |
| **N2** | ✅ PR#372（7a510460）`PR-V1-030-G01-HOOK-DISPATCHER-LIFECYCLE` | G-01 | P1 泄露 | ✅ shipped: drain hook dispatcher observer sinks — (a) `d.sinkWg` 追踪超时遗弃 goroutine + `stop()` drain 后 `sinkWg.Wait()` 兜底；(b) panic value `fmt.Sprintf("%v", r)` + 截断 256 字节；(c) `queue_full` drop 分支补 `slog.Warn` 兜底 | ✅ done | 8h+4h |
| **N3** | ✅ PR#383（738217a5）`PR-V1-030-G02-ROLLBACK-CTX-DECOUPLE` | G-02 | P1 错误传导 | ✅ shipped: (a) `rollbackCells` 派生 fresh ctx with `cfg.HookTimeout`，BeforeStop/Stop/AfterStop hooks 拿到非 cancelled ctx；`TestRollbackCells_DerivedCtx` 三 row 覆盖 HookTimeout=0 / >0 / <0；(b) `phase0ValidateOptions` warn `terminationGracePeriodSeconds >= shutdownTimeout + preShutdownDelay + 10s` 部署窗口安全余量；ADR/部署文档同步；关闭 backlog2 B2-R-03 BOOTSTRAP-ROLLBACK-ERROR-NOT-PROPAGATED 部分相关（errors.Join 仍待，独立项）| ✅ done | 8h+4h |
| **N4** | ✅ PR#378（cbda4e10）`PR-V1-030-G03-VALIDATE-CTX-PROPAGATION` | G-03 | P1 CI 阻塞 | ✅ shipped: (a) `Validator.Validate(ctx context.Context)` 全链路透传；(b) `runGit(ctx, args...)`；(c) 新增 `TestValidateFailFast_ShortCircuitsOnFirstError` + `kernel/governance/ctx_test.go` + `pkg/cmdrun/` 跨平台 ctx-aware exec helper（含 cmdrun_unix.go / cmdrun_windows.go）；ef3edc6b 已登记 G-03 follow-up backlog 条目 | ✅ done | 12h+6h |
| **N5** | `PR-V1-030-K03-OBSERVABILITY-PKGDOC` | K-03 | 编辑前提 | `kernel/observability/doc.go` 30-50 行明确"kernel/observability 只定义 provider-neutral 抽象，导出器在 adapters/runtime"；与产品定位 ADR 配套 | 2h+1h |
| ~~N6~~ | ~~ADR-030-K04-PLATFORM-CELLS-BOUNDARY~~ | K-04 | **决议 won't-do**（2026-05-05）| 决议：accesscore / auditcore / configcore **留 framework 仓**（不迁 `examples/platform-cells/`）；产品定位 = 框架自带认证/配置/审计能力（与 v1.1 product roadmap `202605042330-001-v1.1能力路线-零信任mdm基础.md` 对齐）。落 ADR 锁定决策（1-2h，不需 architect 仲裁会议） | ~~12h+6h~~ → 2h+1h |
| ~~N7~~ | ~~ADR-030-K05-CONTRACT-CONCEPT-COLLAPSE~~ | K-05 | **决议 won't-do**（2026-05-05）| 决议：(a) 保留 4-kind（含 projection）— v1.1 CQRS 后端会用 projection 端点，删了 1.1 重做；(b) ContractMeta `EndpointsMeta` 10 字段森林**不拆 sealed oneof** — Cell 业务代码不直接构造 EndpointsMeta，K#06 contractgen 已生成 typed iface 类型隔离消费侧；(c) ContractSpec 双定义由 06.PR4 完成时自动闭环（见 A 档 K-05c 行）| ~~16h+8h~~ → 0h（拆解为 K-05a +1h / K-05c +2h，并入 06.PR4）|

**文件域 0 重叠（N1-N5 可 5 worktree 并发）**：
- N1 → `kernel/assembly/assembly.go`
- N2 → `kernel/assembly/hook_dispatcher.go`
- N3 → `kernel/assembly/rollback.go` + `runtime/bootstrap/options.go`（与 R-03 文件域有重叠，N3 先行 R-03 后行）
- N4 → `kernel/governance/validator.go` + `kernel/governance/git.go`
- N5 → `kernel/observability/doc.go`

**N6/N7 won't-do 后 06.PR4 立即解阻塞**（2026-05-05）：N6 决「不迁移」+ N7 决「不做」+ K-05a/K-05c 已并入 06.PR4 范围。06.PR4 文件域（`cells/*/slice_gen.go` + 76 contract opt-in + `generated/contracts/*`）与 N1-N5 文件域（`kernel/{assembly,governance,observability}/*` + `runtime/bootstrap/options.go`）**0 重叠**，可完全并发，不存在「等 N1-N5 落 develop」前置。

### B'. PR#373/#374 review 闭口（独立 standalone，2026-05-05；✅ shipped 2026-05-06 PR#384）

> 来源：PR#373（B1 PG outbox fencing）+ PR#374（K#12 settlement lifecycle）二轮独立审查共 6 条 finding（4 P1 + 2 P2）。**严重度回到当前部署现状**：6 条全部为「未来部署/外部消费者落地前必须收口」级别，当前 develop 不阻塞任何 PR；按 v1.0 release gate 处理。
>
> 设计原则：**彻底版**（删平行防御层 + 删 panic 便利函数 + DB 单源不变量），净 LOC 负，删 2 个公开 API。文件域与 N1-N5 / 06.PR4 / R-METRIC-PACK / K#01 FU 完全 0 重叠，可立即并发。
>
> **✅ shipped PR#384 (4674e214)**：5 子项全部按彻底版闭口 + 2 项 review-driven 加固（P2 #2 phase6 wiring 拒裸字面量 + P2 #3 014/015 PG migration negative integration tests），SonarCloud 78.6%→≥80% on new code（contract_tracing_subscriber 5 新用例覆盖 Setup/Ready/Subscribe/Close/StopIntake 100%）。

| 顺序 | PR 名 | 来源 | 类型 | 范围 | 工时 |
|:-:|---|---|---|---|---|
| **N8** | ✅ PR#384（4674e214）`PR-V1-OUTBOX-FU-CLOSURE` | PR#373 + PR#374 review 二轮 | P1 闭口（无现网阻塞，v1.0 gate） | ✅ shipped: (a) **B1 闭口**：Migration 015 `ADD CONSTRAINT outbox_claiming_requires_lease CHECK (status <> 'claiming' OR lease_id IS NOT NULL)` 把 lease 不变量从 startup probe 升为 DB 级硬约束（旧二进制 INSERT/UPDATE 直接 23514 失败，reclaim CAS NULL=NULL 路径自然消解）；**删** `VerifyOutboxLeaseInvariant` + `ErrAdapterPGOutboxLeaseInvariant` errcode + `cmd/corebundle/bundle.go:430` 调用 + `schema_guard_test.go` 对应用例（删平行防御层，DB CHECK 单源）；ADR `202605051600-adr-pg-outbox-fencing.md` cutover 段同步改写；014 加 `IF NOT EXISTS`。(b) **K#12 闭口·ConsumerBase**：`kernel/outbox/consumer_base.go` 加 unexported `built bool` sentinel + `IsConstructed() bool` 接口，`NewConsumerBase` 唯一写入点；`outbox.WithConsumerBase` / `SubscribeEntry` / phase6 wiring（**review-driven P2 #2 升格**）改 `IsConstructed()` 校验取代裸 nil 检查；单测锁 `&ConsumerBase{}` 字面量必失败、`NewConsumerBase` 通过、nil 接收方 `IsConstructed()==false`。(c) **K#12 闭口·tracing manual path**：`Subscription.Validate` 加 ContractID/Kind/Transport 三字段非空校验作为单源真相（替换 NewContractTracingSubscriber 平台降级"二次校验"）；`NewContractTracingSubscriber` 签名改 `(*ContractTracingSubscriber, error)` 内部走 Validate；**删** `wrapper.MustWrapSubscriber` 整函数 + grep 全 caller 迁 `WrapSubscriber` error-returning（删 panic 便利函数）；README.md manual 用法改新签名。(d) **K#12 闭口·RMQ 顺序**：`adapters/rabbitmq/subscriber.go` commit_failed 路径 release-first（先 Release 再 Nack(requeue=true)），与 `runtime/eventbus/eventbus.go` 顺序对齐；新增 `subscriber_test.go` callOrderRecorder 锁单元时序 + `subscriber_release_before_redelivery_integration_test.go` testcontainers 实测 ≤10s 内 redelivery（vs Nack-first ordering 会 gate 在 5m lease TTL）。(e) **archtest 防同类复发**：`tools/archtest/migration_no_transaction_rerun_safe_test.go` 扫所有 `-- +goose NO TRANSACTION` 文件，强制每条 DDL 用 `IF NOT EXISTS` / `IF EXISTS` / `DO` 块（014 已修，015 之后所有同类 migration 受守）。**review-driven 加固 P2 #3**：`schema_guard_integration_test.go` 新增 3 PG integration tests — `OutboxMigration014_AbortsOnClaimingResidue` + `OutboxMigration015_RejectsExistingClaimingNullLeaseRow` + `OutboxMigration015_RejectsUpdateIntoClaimingNullLease`，把 SQL/ADR rollout 防线变 CI 契约；INSERT + UPDATE 双写路径全锁。**净 LOC**：删 probe ~17 + errcode ~5 + 测试 ~50 + MustWrapSubscriber ~30 ≈ −100 / 加 migration ~5 + sentinel ~3 + tracing 校验 ~5 + tests ~40 + archtest ~80 + integration ~165 ≈ +298；总 31 文件 +1298/-153 LOC（含 review-driven 增量）；**去 2 个公开 API**（`VerifyOutboxLeaseInvariant` + `MustWrapSubscriber`）。**Won't-do（按 `feedback_pr_scope_carveouts_must_backlog` 登记）**：corebundle 负向集成测试（NULL lease residue 在真实 wiring 阻断启动）—— DB CHECK 已使该残留无法存在；登记 `B2-X-09 OUTBOX-FU-COREBUNDLE-NEGATIVE-INTEGRATION` 触发条件 = N3 顺路 absorb 或新 wiring 路径出现（已登记 backlog2）。**验证**：`go build ./...` / `go test ./...` / `-tags=integration` 三档全通过；`hack/make-rules/verify.sh` 18/18 gates pass；`golangci-lint --new-from-rev=develop` 0 issues。 | ✅ done | 10h+4.5h |

**N8 文件域**（与 N1-N5 / 06.PR4 / R-03 / R-METRIC-PACK / K#01 FU 0 重叠）：
- 015 migration → `adapters/postgres/migrations/015_add_outbox_claiming_lease_check.sql`（新增）
- probe 删 → `adapters/postgres/schema_guard.go` + `adapters/postgres/errcode.go`（删 `ErrAdapterPGOutboxLeaseInvariant`）+ `cmd/corebundle/bundle.go` + `adapters/postgres/schema_guard_test.go`
- ConsumerBase sentinel → `kernel/outbox/{consumer_base.go,outbox.go,consumer_base_test.go}`
- MustWrap 删 + tracing → `kernel/wrapper/wrapper.go`（删 `MustWrapSubscriber`）+ 全 callers + `runtime/eventrouter/contract_tracing_subscriber.go` + `runtime/eventrouter/contract_tracing_subscriber_test.go` + `kernel/eventrouter/subscription.go` + `README.md`
- RMQ 顺序 → `adapters/rabbitmq/subscriber.go` + `adapters/rabbitmq/subscriber_test.go` + `adapters/rabbitmq/subscriber_release_before_redelivery_integration_test.go`（新增）
- archtest → `tools/archtest/migration_no_transaction_rerun_safe_test.go`（新增）
- ADR → `docs/architecture/202605051600-adr-pg-outbox-fencing.md`

**N8 拆 commit（按 TDD 红→绿 + memory `feedback_tdd_strict_red_green`）**：
- Wave RED：6 条测试 + 1 条 archtest 一次 commit（断言失败状态）
- Wave GREEN：5 条实施（migration / probe 删 / sentinel / MustWrap 删 / 顺序换）+ ADR + README

**N8 严重度备注**（按 memory `feedback_radical_self_audit`）：6 条 finding 的"P1 阻塞合并"是 review 形式口径；落到当前 gocell 部署现状（无外部消费方、无生产滚动升级、无字面量绕构造器）**0 条现网阻塞**。但本 PR 仍按彻底原则做（删 vs 留），不留软兼容/双路径。

### 落地序列

```
**Day 1 独占（G8 solo 窗口）**：✅ done — PR#371 + PR#377 已完成 (a)+(b) 全量 gofumpt baseline，06.PR4 76 contract opt-in 不再受 reformat 淹没风险阻塞
**Day 2+ 并发（最多 6 worktree，0 文件域重叠）**：~~N2 + N3 + N4 + N5 + B1~~ → **N1 ✅ / N2 ✅ (PR#372) / N3 ✅ (PR#383) / N4 ✅ (PR#378) / N8 ✅ (PR#384) / A1 ✅ (PR#381) / B1 ✅ (PR#373) / 06.PR4 ✅ (PR#376) 完成；剩 N5** 可继续推进
ADR / 决议备忘（异步）：N6 决「不迁移」ADR（1-2h）+ N5 配套产品定位 ADR；N7 决议备忘录入 docs/architecture/
后续并发（按 reviewer 容量）：K#10（吸收 K-08(a)+(c)）/ K#09（吸收 K-08(b)）/ D3 第一批 R-METRIC-PACK（吸收 R-01+R-02+G-05）/ K#01 FU G-04 archtest / R-03 small
029 原序接续：K#07 → K#08（残余 ~3h+2h 详 #08 行）→ K#12 ✅ done（PR#369 + PR#374 + PR#384 N8 closure）→ B1 ✅ done（PR#373 + PR#384 N8 closure）
```

### B''. PR#376 06.PR4 review 闭口分发（2026-05-06）

> 来源：PR#376 06.PR4 ship 后 origin/pr/376@1b7585a 独立 review 共 6 条 finding（2 P1 + 4 P2）。**设计原则**：不新起独立 PR，按文件域映射回既有 5 个已规划 PR 顺路收，工时增量 10h+5.5h（vs 拆 6 PR 的 29h+14.5h，省 ~19h+9h）。无 §B'' 段自身工时，全部分发到既有 PR 行。
>
> Bootstrap admin boundary 路线决议：**走 bootstrap credential via env/secret**（与 Keycloak `KC_BOOTSTRAP_ADMIN_USERNAME` / Grafana default+force-change / MinIO `MINIO_ROOT_USER` 三方 upstream pattern 一致），不走 internal listener，不需 B3 PG 分布式锁配套。

| Finding | 严重度 | 自审 | 归宿 PR | 增量 | 关闭项 |
|---|:-:|:-:|---|---|---|
| F1 setup admin 公开面 | P1 | P1 confirmed | **A2 升 P0** | 0（A2 原范围）| F1 + 027#2 + B2-C-02 |
| F2 verify gate 跟 kernel shard 一起跳 | P1 | P1 confirmed | **A2 同 PR** | +2h+1h | F2（CI trust）|
| F4 pagination shape 启发式产出双 error envelope | P2 | **升 P1.5** | **06.FU** typed envelope 同源 | +1h+0.5h | F4 + B2-X-? |
| F6 length-validation archtest false-pass | P2 | P2 | **06.FU3 升格已调度** | 0（原范围）| F6 + 06.FU3 触发条件项 |
| F3 doc/README/cell-guide 三处与 generated 矩阵漂移 | P2 | P2 | doc.go → **06.FU** + README+cell-guide → **F1** | 0(06.FU) + 1h+0.5h(F1)| F3 |
| F5 `/internal/v1/* + auth.public` governance 缺拦截 | P2 | **降 P3** | **F4** contract 治理同源 | +1h+0.5h | F5 |

**为什么不拆独立 PR**：
1. **A2+F2 必须同 PR**：A2 改 contract 类型 + regen handler 后，没有可信的 verify gate 等于改完无法验证；F2 单飞 PR 与 A2 互锁
2. **F4 与 06.FU 文件域 100% 重叠**：同改 contractgen IR + 模板 + 全量 regen 窗口，拆成独立 PR 等于做两次 regen
3. **F6 是 06.FU3 触发条件首个实证**：原本「触发条件 ≥3 次 false-positive」改判「F6 性质恶劣不必等齐」，升格已调度
4. **F3 doc.go 由 06.FU typed envelope 改写**：典型 generated artifact 描述跟着 generator 改，README+cell-guide 顺路 F1
5. **F5 与 F4 contract 治理同源**：governance rule + schema 改动文件域重叠

**优先级序列**：

```
Now (P0)：A2 升格（吸收 F1 + F2）—— 本周必合
  ├── 安全：bootstrap credential via env，删 anonymous public setup-admin
  └── CI trust：verify-codegen 拆独立 job，与 test-and-build 解耦
       │
       ▼
Next 1-2 周 (P1，2 worktree 并发)：
  ├── 06.FU 范围扩展 → 吸收 F4 pagination + F3 doc.go     contractgen + regen
  └── 06.FU3 升格已调度 → 吸收 F6 length param-scoped     tools/archtest
       │ (二者文件域 0 重叠)
       ▼
顺路 (P2，按 reviewer 容量)：
  ├── F1 微扩 → 吸收 F3 README+cell-guide                  docs only
  └── F4 微扩 → 吸收 F5 governance auth-plane              kernel/governance + schema
```

**与拆 6 PR 方案对比**：

| 维度 | 拆 6 PR | 合并到原 PR（采用）|
|---|---|---|
| 工时 | 29h + 14.5h | **10h + 5.5h 增量**（落到 5 个原 PR 上）|
| reviewer 切片 | 6 次 | 0 次（沿用原 PR 节奏）|
| 优先级清晰度 | 弱（6 个并列）| 强（P0 单线 + P1 双轨 + P2 顺路）|
| roadmap 改动 | 新增 §B'' 段 + 6 PR 行 | 5 行 in-place 扩 + §B'' 仅分发表 |

### Won't-do（030 范围内本轮裁剪）

| 030 项 | 不做理由 |
|---|---|
| **K-02** JOURNEY-LIFECYCLE-CI-CLOSE | 本轮优先 kernel/runtime 稳定性，journey lifecycle 治理收敛押后；J-confighotreload 引用未声明 contract 由 ADV-06 已能拦截 |
| **K-07** CELLS-SLICE-MULTI-VERB-DECOMPOSE | 永久 won't-do — 06.PR4 已吸收 K#04 PR-4 + K#05 PR-A2 + PR-B + 030 K-06，再加 K-07 单 PR 触 50+ 文件 review 不可行；slice 拆分非 kernel/runtime 稳定性必需 |
| **K-04** PLATFORM-CELLS-BOUNDARY（N6）| **决议 不迁移**（2026-05-05）— accesscore / auditcore / configcore 留 framework 仓；产品定位 = 框架自带认证/配置/审计能力，不属于 `examples/`；与 v1.1 product roadmap 路线对齐；落 ADR 锁定即可（1-2h），不需 architect 仲裁会议 |
| **K-05** CONTRACT-CONCEPT-COLLAPSE（N7）| **决议 不做**（2026-05-05）— (a) 保留 4-kind 含 projection（v1.1 CQRS 后端会用，YAGNI 反向不成立 — 删了 1.1 重做）；(b) `EndpointsMeta` 10 字段森林**不拆 sealed oneof**（Cell 业务代码不直接构造，K#06 contractgen 已让消费侧类型隔离，重构投入 16h 换 governance 内部清爽 cost > benefit）；(c) ContractSpec 双定义由 06.PR4 完成时自动闭环（contractgen 生成 `iface_gen.go::Contract var`，76 contract opt-in 后手写字面量降为 0 + archtest 守卫，FMT-17 governance 单向校验降级为冗余）；拆解出的 K-05a +1h / K-05c +2h 并入 06.PR4 |
| **030 Track A** (A-01..A-08) | adapters 加固押后 |
| **030 Track C** (C-01..C-07) | cells 业务收敛押后 |
| **030 Track G** P2/P3（G-06..G-15 除 G-04/G-05）| governance/kernel 防腐 P2/P3 押后 |
| **030 Track J** (J-01..J-04) | journeys/contracts 收敛押后 |
| **030 Track F** (F-01..F-10) | 工程基线押后 |

原 124h+61h → 103h+50h；N6/N7 won't-do 节省 27h+14h，K-05c 强化（event 派生 + 三条 archtest）追加 +6h+3h；2026-05-05 晚追加 B' 档 N8（PR#373/#374 二轮 review 6 条 finding 闭口）+10h+4.5h；2026-05-06 追加 B'' 档分发（PR#376 06.PR4 review 6 条 finding 闭口，全分发到既有 5 PR）+5h+2.5h。**N1+N2+N3+N4+N8 + 06.PR4 + A1 + B1 + B10 已 ship 后**，剩余 ~38h+18h（030 + B'' 合计范围内：N5 + 06.FU/06.FU2/06.FU3 + A2(P0) + F1/F4 微扩 + K#07-#10 等）。

单线 ~4 周；本周 5 并发 + 下周 06.PR4 解阻塞并发 5 PR → 实测 ~2.5 周（不计 029 原序剩余 K#07/K#08/K#09/K#10）。
