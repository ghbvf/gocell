# Wave 4 主动推进 Round 2 — B 类 scope-已知项收口计划

**生成日期**：2026-05-16
**关系**：本计划是 [`202605112000-036-archtest-governance-rollout-plan.md`](202605112000-036-archtest-governance-rollout-plan.md) §4 Wave 4 清单的**第二轮分流方案**，是 [`202605121750-037-wave4-advance-plan.md`](archive/202605121750-037-wave4-advance-plan.md)（Round 1，PR-A1/A2/A3 ✅ 全 ship，已归档）的后继波次。036 保持决策原则视角不动；037 R1 已完结。

## 核心论点修正（R1 → R2）

037 R1 的筛选轴是「**trigger 距离 = 1**（下一个就触发）→ 主动做 ROI > 等触发」。R2 纠正并外延这一轴：

> 真正的判据不是「trigger 是否触发」，而是「**scope 是否已知**」。把「gate 未满足」当「不可行 blocker」是 lazy deferral 的包装。Wave 4 剩余项据此二分：
>
> - **A 类 — 真 scope-blocker**：触发信号本身定义正确 scope（误报案例 / 事故形态）。信号未现 = 连规则要拦什么都不确定，先做 = 凭猜 = PR #445 反模式。**保持休眠**。
> - **B 类 — 优先级/决策 gate**：scope 已在 backlog 写死，「未做」是优先级/ROI 选择或一次内部决策，**不是外部阻塞**。"方案未定" 对决策 gate 而言是任务第一步，非 blocker。**全部可主动推进**，挡它们的唯一诚实理由是机会成本 + 单 PR=单维度的 scope 纪律。

**适用范围**：仅 036 §4 Wave 4 B 类。A 类 + 大体量 Template-Wave4-3PR 项不在本波。

---

## 0. 分类盘点（2026-05-16）

| 项 | 原 trigger | 分类 | scope 已知证据 | 去向 |
|---|---|---|---|---|
| PR-TS1-FU-VALIDATIONRESULT-EMITTER-SEALED-MARKER-01 | 同包非-`*locator` emitter 真 fp / 他处建 sealed marker | **B** | cap-02:26 写死（unexported marker iface + `types.Implements` predicate） | **R2-P1** |
| TYPESEVAL-IFACE-IMPL-HELPER-CONSOLIDATE-01 | 第 4 处 `implements*` runtime guard 重复 | **B** | cap-02 写死（`tools/typesutil/ImplementsInterface` + 10-case 单测模式） | **R2-P2** |
| POSTGRES-NOTFOUND-TEST-OTHER-ERROR-MIXUP-ARCHTEST-01 | 第 2 次同类漂移 | **B** | cap-14 写死（扫 `_NotFound` 测试断言 typed `errcode.Error.Code`） | **R2-P3** |
| ~~PR408-FU-GOVERNANCE-OWNER-AST-EXTRACTION-01~~ | 第二次主题归属错误 | **B** | — | ❌ subsumed by PR #435（前提已结构性消除，详见 R2-P4 段） |
| CELLGEN-ERRCODE-FUNNEL-HARDEN | depguard method-level 方案确定 OR typed wrapper 抽出 | **B（决策 gate）** | 目标已知（cellgen errcode Hard），机制二选一是任务首步 | **R2-P5** |
| TEST-POLLING-DETERMINISM typed marker | 第 2 次 race flake / 进入下一治理批 / 站点新增违反 | B 但大件 | scope 已知，体量 18-25h 三维 = PR #445 风险 | **不进波**，留 036 §4 Template-Wave4-3PR，flake-density gate |
| HANDLER-POLICY-TYPEAWARE-SCANNER-01 | scanner 误报/漏报触发 | **A 真 blocker** | 真值形态 = 修哪个误报，信号定义 scope | **休眠** |
| AUTH-COMBO-ARCHTEST-DOUBLE-DEFENSE | `hasFMT27AuthModeConflict` 被重新 inline 化 | **A 真 blocker** | 第二道防线仅在第一道侵蚀后有意义，先做纯思辨 | **休眠** |
| B-FLOOR-FOLLOWUP §2.5/§4 | contract status↔adapter typed return 漂移事故首现 | A 反应型 | 反向触发（事故=已晚），40h 大件 | **休眠**，留 036 §4 |

> ~~PRODUCTION-LOADER-API-PRIVATE-HARD-UPGRADE-01~~ ⊃ superseded by plan 040（不计入 Wave 4）。

---

## 1. R2 波次（按"关 Soft/latent 面 + 小 + 单维 + 独立 > enabler/ROI > 需 spike 多维"排序）

### 进度状态

| 阶段 | 维度 | 体量 | 状态 | 前置 | 并行 |
|------|------|------|------|------|------|
| **R2-P1** EMITTER-SEALED-MARKER | 单维 framework | ~2-3h | ✅ ship | 无 | P1∥P2 |
| **R2-P2** IFACE-IMPL-HELPER | framework+微迁移（commit-split 单 PR） | ~3-4h | ⏳ 起 | 无 | P1∥P2 |
| **R2-P3** POSTGRES-NOTFOUND-MIXUP | archtest enforcement（±migration） | spike 1h + 1-3h | ⏳ spike | spike 结论 | 独立 |
| ~~**R2-P4** OWNER-AST-EXTRACTION~~ | 单维 tooling refactor | — | ❌ subsumed by PR #435 | — | — |
| **R2-P5** CELLGEN-ERRCODE-FUNNEL-HARDEN | 决策→多维 | spike 2h + TBD | ⏳ spike | P5.0 决策 | 串行其后 |

### R2-P1：PR-TS1-FU-VALIDATIONRESULT-EMITTER-SEALED-MARKER-01 ✅ shipped 2026-05-16

> **Ship 报告（含 reviewer F-1 闭环）**：`kernel/governance/emitter_invariant.go` 声明 BFS owner invariant + 3 行 compile-time witnesses；`signatureMatchesValidationResultEmitter` predicate body 三处全 `types.Identical`（含 owner gate `types.Identical(recvNamed, locatorType)`），**零字符串锚点 + 结构性免疫 method promotion**；fixture loader fail-fast on missing scope identifier；新增 2 个 RED fixture（embedded_locator outer receiver + unrelated receiver）覆盖完整威胁面。**Hard 防线四层**：type-identity owner gate + compile-time witnesses + predicate body type-system + loader fail-fast。OSS 对标超过 staticcheck/govet/gopls 两档。
>
> **设计演化**：首版（PR #521 commit baba2a6f2）用 sealed marker iface (`validationResultEmitter` + `*locator.isValidationResultEmitter()`) + `types.Implements` 做 owner gate。Reviewer F-1 (Cx1) 发现 Go embedding 自动 promote marker method 到 *Validator / *DependencyChecker（二者均 `struct { locator }` 值嵌入），导致 `types.Implements(*Validator, iface)=true`，owner gate 失效。改为 `types.Identical(recvNamed, locatorType)`——named type 身份不参与 promotion，结构性免疫此攻击面。Sealed marker 是 cross-package unimplementability 范本（kernel/outbox / kernel/persistence），不适合 same-package owner allowlist。

- **现状**：PR-TS1 把 BFS emitter 升至 signature-based 三谓词（return ValidationResult + arg0 string + receiver/result 同包），AI-rebust Medium-偏-Hard；owner 约束「同包」比原 `*locator` 单一允许集略宽 → 同包新加非-`*locator` 的 `(string,...) ValidationResult` 方法会被误纳（真 Soft false-positive 面）。
- **修复**：`kernel/governance` 加 unexported sealed marker `validationResultEmitter interface { isValidationResultEmitter() }`，`*locator` 实现；BFS predicate 改 `types.Implements(recvNamed, emitterIface)`。
- **维度**：单维 framework（predicate 改写 + 既有 `rule_inventory_test.go` conformance 同一 surface，无独立 enforcement PR）。
- **AI-rebust**：Medium-偏-Hard → **真 Hard**（违反不可表达——非 emitter 类型编译期不实现 marker）。
- **文件**：`kernel/governance/locator.go` + `kernel/governance/rule_inventory_test.go`。
- **前置**：无。**关闭 backlog** `cap-02-metadata-governance.md` PR-TS1-FU-… 行（🟠 → ✅）。

### R2-P2：TYPESEVAL-IFACE-IMPL-HELPER-CONSOLIDATE-01

- **现状**：archtest 内多处手写 `func implementsXxx(t types.Type, iface *types.Interface) bool`（实测 2-3 处：`implementsManagedResource` / `implementsAMQPChannel`），各自重复 `types.Implements` 包裹；charter §5 L4 承认 interface-impl 枚举是 typed helper 真实需求。
- **修复**：`tools/typesutil/` 抽 `ImplementsInterface(typesInfo, expr, ifaceObj) bool`，10-case 单测（promoted/embedded/ptr-vs-value/generic/nil，同 ResolveReceiverType 模式）；2-3 站点机械委托。
- **维度**：framework + 微迁移；总改动 < 200 LOC → 援引 037 §4 template 例外，**单 PR commit-split**（commit-1 helper+单测，commit-2 站点委托）。
- **AI-rebust**：helper Medium（type-aware，复用 SharedResolver 同层）；消除重复 runtime guard。后续 iface-impl 类规则 enabler。
- **文件**：`tools/typesutil/` + `tools/archtest/{managed_resource_contract,rmq_invariants}_test.go`。
- **前置**：无。**关闭 backlog** cap-02 新登记行（🟢 → ✅）。

### R2-P3：POSTGRES-NOTFOUND-TEST-OTHER-ERROR-MIXUP-ARCHTEST-01

- **修复**：archtest 静态扫 `*_test.go`，`_NotFound` 后缀测试必须断言 typed `errcode.Error.Code == Err*NotFound`，禁裸 `assert.AnError`。
- **维度**：archtest enforcement；**前置 spike**：扫现存 `_NotFound` 测试合规性 → 0 不合规 = 单 PR；有不合规 = 拆 PR-a（修站点 migration）→ PR-b（enforcement），遵 036 单 PR=单维度。
- **AI-rebust**：**Hard**（违反不可表达）。
- **文件**：`tools/archtest/` 新规则 + （视 spike）若干 `*_test.go`。
- **前置**：合规性 spike 结论。**关闭 backlog** cap-14 对应行。

### ~~R2-P4：PR408-FU-GOVERNANCE-OWNER-AST-EXTRACTION-01~~ ❌ subsumed by PR #435 (2026-05-16 核销)

> **结论**：两个代码前提均被 PR #435（`ab4b93a70` — `INVENTORY-ANCHOR-SINGLE-SOURCE 彻底版 — drop persistent inventory + drift gate`）结构性消除，**不实施、不新建任何 tool**。
>
> 1. **持久化 inventory 已删除（非改造）** — `tools/archtest/inventory_anchor_required_test.go` godoc 明示 *"Replaces the deleted `docs/audit/archtest-inventory.md` + drift gate"*。R2-P4 提案的「inventory 加 `referenced_by` 列」无载体可加。
> 2. **list-archtests.sh 不再抽 ID** — 现为纯 stdout raw 视图，godoc *"intentionally does NOT re-implement parsing — there is no second grammar to drift"*。finding 根因「引用关系被算成归属」不可能再发生（脚本不再计算 owner）。
> 3. **canonical owner 改由更强方案承载** — owner = 文件 header `// INVARIANT: <ID>` 源码锚点，两条 Hard archtest 守卫（`INVENTORY-ANCHOR-REQUIRED-01` + `INVENTORY-ANCHOR-VALID-ID-01`）。比 R2-P4 提案的 go/ast Medium 档位更高（锚点与测试 co-located + single source）。
>
> **否决「新建独立 tool 加 referenced_by 索引」**：等于重建 PR #435 刻意铲除的「持久化 artifact + drift gate」，是对该架构决策的回归，违反 single-source 原则。原事故类（照 inventory 改错文件）已随 inventory 删除消失。cap-02 对应行同步关闭。

### R2-P5：CELLGEN-ERRCODE-FUNNEL-HARDEN（决策 gate）

- **P5.0 决策 spike**（任务首步，非外部依赖）：评估 (a) depguard method-level rule vs (b) cellgen typed errcode wrapper 抽出，产出选型结论 + 理由（ADR-lite 段落）。
- **P5.1+**：按选定机制拆 framework / migration / enforcement N PR（036 单维度规则）。
- **维度**：决策→多维。排 P1-P3 之后（唯一多维非平凡项，需专注，且决策结论影响后续拆分）。
- **文件**：`tools/codegen/cellgen/` + （视方案）`.golangci.yml` 或新 typed wrapper。
- **前置**：P5.0 决策。**关闭 backlog** cap-14 对应行。

---

## 2. 依赖图与并行度

```
R2-P1 (kernel/governance) ─┐
R2-P2 (tools/typesutil)    ─┘ 完全并行（零文件交叉）
R2-P3 spike ── (条件分支) ── P3 单 PR / P3a→P3b   [独立，可与 P1/P2 并行]
R2-P5.0 决策 spike ────────── P5.1+ 按维度拆       [spike 可并行起跑，实施串行其后]

（R2-P4 已 ❌ subsumed by PR #435，退出依赖图）
```

- **并行峰值**：P1 + P2 + P3(spike) 三线并行（kernel/governance / tools/typesutil / 各自独立测试，零交叉）。
- **与 040 关系**：零依赖。本波文件面与 040 Stage 3 的 `tools/archtest/*_test.go` 迁移面不重叠，可与 040 整体并行。
- **与 A 类**：HANDLER-POLICY / AUTH-COMBO / B-FLOOR 保持休眠，触发信号出现才立项；不在本波。

## 3. 036 / 037 / backlog 同步

- 037 R1 plan：§0 表 PR-A2 🟠→✅ #503、PR-A3 🟠→✅ #508；037 整体标完结。
- 036 §4：触发表诚实重构为 **A 真 scope-blocker** / **B 优先级-决策 gate（→ 本 037-R2）** 两类，消除「拿 gate 当 blocker」复发。
- backlog：R2 每阶段 ship 时同 PR 关闭对应 cap-02/cap-14 行（不 silent carryover）。
- 036 §0：backlog ✅/WONTFIX 条目手动归档至 `docs/backlog/archive/202605121700-backlog-completed-2026q2.md`（事务性，与本波正交）。

## 4. 验收

每阶段遵 036 验收：`go build ./... + -tags=integration`、`hack/verify-archtest.sh` 16 shard、`golangci-lint` 0 issues、对应 backlog 行同 PR 关闭、AI-rebust 评级写入 PR 描述。
