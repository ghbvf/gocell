# 029 · GoCell Master Roadmap（残余版，归档自 `archive/202605011500-029-master-roadmap.md`）

> 日期: 2026-05-10 18:39
> 基线: `origin/develop @ a54d0c77`
> 形态: 关键路径串行 + 并行轨道并发；无 v1.0 GA 时间约束
> 来源: 上一版 029 master roadmap（2026-05-01 起，2026-05-10 a54d0c77 完成 D2/N5 后整体归档）；本残余版只保留 OPEN 项
> 归档基线: 关键路径 9/12 ✅ / Track A 5/5 ✅ / Track G 11/11 ✅ / Track J 1/1 ✅ / Track B 11/14 ✅ / Track C 1/6 ✅ / Track D 4/8 ✅ / Track F 1/6 ✅ / §B/§B' 全 ✅；详细历史见归档版

---

## errcode 主题剩余项

> W1-W3 全部 ✅（K#08 残余 / A-05 / C5 残余 / G3 / 06.FU 三批 typed envelope）。W4 与 W1-W3 全程解耦可独立 lane。

| Wave | 项 | 目标 | 文件域 | 工时 | 依赖 |
|:-:|---|---|---|---|---|
| **W4** | A-03 ADAPTER-ERROR-CLASSIFICATION-TRANSIENT | postgres `40001/40P01/08*` + redis `i/o timeout` + s3 5xx/429 标 transient；复用 vault `errcode.WrapInfra+IsTransient` 范式 | `adapters/{postgres,redis,s3}/errors.go` + `pkg/errcode/*` | 16h+8h | — |
| **W4** | PR238-FU8 InternalMessage op label 测试（残余）| ~~configrepo `UpdateForRollback` op 测试覆盖（防硬编码回退）~~ — ✅ 已由 PR#446 PR-CFG-TEST-RESIDUALS 闭合（`opUpdate` / `opUpdateForRollback` 提为 const + Update/UpdateForRollback NotFound 双向 op 锁）| — | — | — |

---

## K + G 剩余实施路径

| 剩余项 | 内容 | 状态 | 工时 |
|---|---|---|---|
| **K#07** | EVENTROUTER-SUBSCRIPTION-FIELDS（拆 `Subscription.CellID` ↔ `ConsumerGroup` 字段）| 待办 | 4h+4h |
| **K#09** | SCAFFOLD-ONE-CMD（`gocell new-cell/new-slice/new-contract/scaffold assembly`，吸收 K-08(b)）| 待办 | 22h+10h |
| **F4** | API-VERSIONING-POLICY（storageVersion + deprecation 窗口 + `lifecycle:deprecated` + `removeBy:vN.M`）| 待办 | 9h+4.5h（含 §B'' F5 吸收）|
| **F5** | DOCS-AUTOMATION（`gen-crd-api-reference-docs` 范式，从 contract.yaml schema 自动生成 + KEP frontmatter ADR 模板）| 待办 | 8h+4h |

---

## 关键路径残余（3 项 / ~42h dev + ~21h review）

> 单线串行，每项依赖上一项合入 develop。完整 12 项历史见归档版；本表只列 OPEN。

| # | PR | 来源 | 问题 | 方案 | ship | 工时 | 依赖 |
|---|---|---|---|---|:-:|---|---|
| 07 | PR-V1-EVENTROUTER-SUBSCRIPTION-FIELDS | B2-K-06（9分）/ 028 B2-3 | `Subscription.CellID` 实为 consumerGroup，metric label / 日志属性自相矛盾 | 显式拆 `CellID` + `ConsumerGroup` 两字段；codegen 之后做最便宜（改模板 5 LOC + go generate） | L2 | 4h+4h | #04 ✅ |
| 09 | PR-V1-SCAFFOLD-ONE-CMD | E9（8分）/ 028 B2-1 + 030 K-08(b) | 新 cell 入场 4-8h（codegen 后 1-2h）；无对标 kubebuilder/`rails generate scaffold` 一键脚手架；scaffold 缺 `assembly` 子命令，加第 2 个 assembly 时无工具生成初始 yaml + `cmd/{id}/run.go` 占位 | `gocell new-cell <id> --type=<core\|edge\|support> --level=<L0-L4> [--with-http] [--with-events]` 一键产出可编译可运行最小 cell + 1 example slice + 1 通过测试；附 `new-slice` + `new-contract` 子命令；**吸收 K-08(b)**：加 `gocell scaffold assembly --id=<id> --cells=<a,b> --deploy=k8s`，同步生成 `assemblies/{id}/assembly.yaml` + `cmd/{id}/run.go` 占位 | L3 | 22h+10h | #04 #05 #06 ✅ |
| 13 | PR-V1-CELL-IFACE-ISP-SPLIT（PR-A22）| backlog_later_detail.md §4 + 历史 025 plan PR-A22（2026-05-08 上 029）| `kernel/cell/interfaces.go:34-72` 当前 12 方法基础接口混合 identity(3) + lifecycle(3) + status(2) + metadata accessor(4)；K#04/K#05 已让 `metadata.CellMeta` 数据层单源化（`*metadata.CellMeta` 指针经 `Metadata()` 暴露），但接口形态层仍未做 ISP 切分，CELLMETA-SINGLE-SOURCE-03 archtest 现守在主 Cell 接口而非子接口；同时 PR-A5c `OUTBOX-CELL-01` 仅 ban `WithPublisher`/`WithOutboxWriter` 两个 raw-dep Option，`WithTxManager(persistence.TxRunner)` 等 raw dep 在 cells/{accesscore,auditcore,configcore}/cell.go 仍可被新 PR 重新打开口子（PR245-F10 / 030 G-17 触发） | (a) `kernel/cell/interfaces.go` 12 方法 → 三接口：`CellIdentity{ID/Type/ConsistencyLevel}` + `CellLifecycle{Init/Start/Stop/Health/Ready}` + `CellMetadata{Metadata/OwnedSlices/ProducedContracts/ConsumedContracts}`；`Cell` 改写为 `interface { CellIdentity; CellLifecycle; CellMetadata }` 复合形保持调用方零变化；(b) `base.go:13-17` compile-time check 三段式 `_ CellIdentity / _ CellLifecycle / _ CellMetadata = (*BaseCell)(nil)`；BaseCell 实现不变（已实现全部方法）；(c) **吸收 PR245-F10 / 030 G-17**：archtest `CELL-RAW-DEPS-01` 扩到禁所有 `cells/*/cell.go` 公开 `With*` Option 接受 raw infra 类型（`persistence.TxRunner` / `outbox.Publisher` / `outbox.OutboxWriter` / `eventbus.Bus` / `kvstore.Store`），保留现有 `WithTxManager` 调用形态仅锁未来不退化；(d) archtest `CELLMETA-SINGLE-SOURCE-03` 升级：`Metadata()` 必须落在 `CellMetadata` 子接口而非顶层 `Cell`；(e) **ADR**：写入三接口切分决议 + `Cell` 复合形保留原因 + **Slice 接口同步评估决议（默认不拆）**——Slice 7 方法形态相似但 cells/* 全嵌 BaseSlice 实现疲劳为 0、字段为简单值无 metadata 单源化数据层驱动，等首次第三方 Slice 实现需 ISP 时再触发，登记 backlog_later_detail.md §4 同槽位 + `CellMetadata` 接口名与 `kernel/metadata` 包共存导入约定。**风险**：高（接口破坏性变更）；强制 `/ultrareview`；调用方零代码变更但 `var _ Cell = (*BaseCell)(nil)` 升级为三段式 | L4 / Phase 4（v1.1+ 长期债清账） | 16h+7h | #02 #04 #05 ✅ |

**为什么是这个顺序**：
- `#07/#08` 在 codegen 后：模板 5 LOC 改 + go generate 自动跟，比 codegen 前手工改全仓便宜（K#08 ✅，#07 仍 OPEN）
- `#09` 在 #07 后：scaffold 模板编码 codegen 后的干净 API
- `#13` 放最末：长期债清账，K#04/K#05 已完成 metadata 数据层单源化作为前置；接口形态切分零调用方变更（BaseCell 已实现满），但属协议级破坏性变更（强制 `/ultrareview`），与其他关键路径项文件域 0 重叠可独立排

---

## 并行轨道残余（无关键路径依赖，按 reviewer 容量穿插）

每条轨道按 backlog 顺序独立推进；所有 PR 文件域已注明，可与关键路径并行。

### Track A · 安全闭口

✅ 全 5 项 ship（A1-A5）。详见归档版。

### Track B · PG/RMQ/Redis 加固（残余 3 PR / ~44h + ~29h；含 B2 拆 3）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| B2 | PR-V1-PG-DOMAIN-REPO（**拆 3 并行 PR**，2026-05-07）| X1 主线 / 027#10 | 5 域 Repository（User/Session/Role/Device/Command）+ 4 migration DDL 全缺，PG 接入主线 | **拆 3 并行 PR（文件域按 cell 边界 0 重叠，L4 整体降为 L2/L3 散）**：**(B2.A) PR-V1-PG-ACCESSCORE-REPO**（16h+12h，L3，**关键路径** — B5.FU 硬卡 `postgres.PGSessionRepository`）— User+Session+Role 3 域 + migration `017_users.sql`（**吸收 B3** `UNIQUE(role=admin)` 让 multi-pod setup POST 由 DB 保证首个 INSERT 胜出）+ `018_sessions.sql` + `019_roles.sql` + `adapters/postgres/{user,session,role}_repo*.go` + `cells/accesscore/internal/mem/{user_repo,session_repo,role_repo}.go` 接口对齐 + `cmd/corebundle/access_module.go` 仅声明 PG provider（**不切 `WithInMemoryDefaults`**，runtime wiring 切换归 B5.FU）+ RBAC-ASSIGN-LEVEL-UPGRADE / SEED-ROLE-IFACE / ACCESS-LEVEL-AUDIT / AUTH-CACHE 联动激活。**(B2.B) PR-V1-PG-DEVICECELL-REPO**（8h+6h，L2）— Device+Command 2 域 + migration `020_devices.sql`[+`021_commands.sql`] + `adapters/postgres/{device,command}_repo*.go` + `examples/iotdevice/cells/devicecell/internal/mem/repository*.go` + iotdevice cmd wiring；与 B2.A 文件域 0 重叠。**(B2.C) PR-V1-PG-CONFIGCORE-VERSIONS-WIRING**（6h+4h，L2，需 explorer 先核实范围）— `CONFIG-VERSIONS-MIGRATION-01` 联动激活（migration 004 已建表，激活 cell wiring + 联动 repo 测试）+ `adapters/postgres/config_*` + `cells/configcore/internal/mem/*`；若仅 wiring 切换可缩到 4h+2h。**协调点（PR 开启前商定）**：(1) migration 编号预分配 B2.A `017-019` / B2.B `020-021` / B2.C `022+`（如需）防同选；(2) `adapters/postgres/` 共享 archtest 主题（PG-REPO-CONSTRUCTOR-MUST-FREE-01 类似 PR#388 范式）走 funnel-first 单文件 `tools/archtest/pg_repo_invariants_test.go`，三 PR `// INVARIANT: {ID}` 锚点 append（不准每 PR 各建一份）；(3) `schema_guard.go` 主体由 B2.A 先落，B2.B/B2.C 只 append 表清单；(4) `adapters/postgres/errcode.go` append-only，三 PR 各加各的不冲突。**ship 顺序**：3 PR 同时开 worktree，**B2.A 先 merge**（解锁 B5.FU）→ B2.B/B2.C 任意顺序 merge → B5.FU 接力 `postgres.PGSessionRepository` 端到端 wiring | L3+L2+L2（散）| 30h+22h（B2.A 16+12 / B2.B 8+6 / B2.C 6+4）|
| B3 | ⚠️ scope revised by PR#392 — ACCESSCORE-PG-USERS-MIGRATION-01 | A26-R1/R4 / 027#11 + PR#392 D5 | 旧方案"admin provision 分布式锁"不再成立：PR#392 已删除 bootstrap admin provision mode 与 credfile 路径；当前 in-memory mode 不支持 multi-pod（无共享存储），单进程幂等由 `adminprovision.Provisioner` mutex + admin role 检查保证 | PG adapter 落地时在 users migration 加 `UNIQUE(role=admin)`（或等价唯一约束），让 multi-pod setup POST 由 DB 保证首个 INSERT 胜出、后续 409 / 已存在 410；不做分布式锁；orphan recovery 仍保留在 `adminprovision` 语义内 | L2 | 2h+1h（**随 B2.A `017_users.sql` 吸收更经济**，B2 拆分后落入 accesscore PR） |
| B5.FU | PR-V1-PG-REFRESH-CROSS-STORE-RUNTIME-WIRING | PR#399 L2 single-reviewer P1+P2 follow-up（2026-05-06）| **P1 runtime wiring 与 service-level integration**：B5 (PR#399) 落地了 `sessionrefresh.Service` 单 `txRunner.RunInTx` 包装 + 三场景 `PGRefreshStore` 集成测试，但 **shipped 的 corebundle wiring 仍走 `WithInMemoryDefaults`**（`cmd/corebundle/access_module.go:77` 无条件追加 in-memory defaults，postgres 模式仅在其后追加 `WithTxManager(txMgr)`；`cells/accesscore/cell_init.go:309-315` 看到 session repo / refresh store 缺位时实例化 `mem.SessionRepository` + `refreshmem.New`）。即便 PG `TxRunner` 注入到位，**真实参与外层事务的仍是内存实现** —— "cross-store ACID" 仅在 refresh-store 侧由 `PGRefreshStore` integration test 证明，service-level 与 session-repo 侧在目标 runtime 路径上未被端到端验证。同时新增 `REFRESH-CROSS-STORE-TX-01` 仅基于 AST 字面 selector matching（`s.txRunner.RunInTx` / `s.refreshStore.Rotate`），锁不住 alias 重构（`rs := s.refreshStore; rs.Rotate(...)`）或把真实调用搬到 closure 外 helper 的绕过路径 | **(a) P1 runtime wiring 切换（依赖 B2.A 落地 `postgres.PGSessionRepository`）**：`cmd/corebundle/access_module.go` postgres 分支删除/绕过 `WithInMemoryDefaults`，改为显式注入 `accesscore.WithSessionRepository(adapterpg.NewSessionRepository(pool, ...))` + `WithRefreshStore(adapterpg.NewRefreshStore(pool, txMgr, policy, clk, randReader))`；in-memory defaults 仅 memory backend 模式启用；`cell_init.go` mem fallback 在 postgres 模式 fail-fast（不允许混用）。**(b) P1 service-level PG integration test**：新增 `cells/accesscore/slices/sessionrefresh/service_pg_integration_test.go`（`//go:build integration`），把 `sessionrefresh.Service` + `postgres.PGSessionRepository` + `postgres.PGRefreshStore` 接入同一 `postgres.TxManager`，端到端验证：commit-failure 路径 session row 与 refresh row 同时回滚；Rotate 失败时 session.Update 也回滚；仅当三者均成功才一起 commit。**(c) P2 archtest 类型化升级**：`tools/archtest/refresh_cross_store_tx_test.go` 升级到 `golang.org/x/tools/go/packages` + `types.Info` 别名传播——跟踪 `s.txRunner` / `s.refreshStore` / `s.sessionRepo` / `s.userRepo` 局部别名 + helper 调用图；补三类 negative fixture：(c1) 局部别名重定向（`rs := s.refreshStore; rs.Rotate(...)`）；(c2) helper 间接调用（`func helper(s *Service) { s.refreshStore.Rotate(...) }; in Refresh: helper(s)`）；(c3) 空 closure + 真实调用搬出（`runInTxWithEmptyClosure(); s.refreshStore.Rotate(...)`）。**Honest scope**：P1#5 已记入 B2.A 的 PR description "Honest test-scope boundary"；本行明确登记为 B5 follow-up 而非合入 B2.A 主线 | L4 / B2.A 后接力 | 12h+6h（依赖 B2.A `postgres.PGSessionRepository` 落地）|

> Track B 已 ship 11 PR（B1/B4/B5/B6/B7/B8/B9/B10/B11/B12/B13/B14）详见归档版。

### Track C · cells 业务（残余 5 PR / ~50h + ~23h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| C1 | PR-V1-DATA-HASHCHAIN-RECOVERY | B2-C-01/14 / 027#6 | auditcore hashchain 重启未恢复，链完整性无法验证（**P0**） | auditcore 启动从 repo 恢复尾节点 + 跨重启 continuity 测试 | L3 | 12h+6h |
| C2 | PR-V1-BIZ-LOCKOUT | ACCESSCORE-LOCKOUT / 027#7 | 缺自动账户锁定业务实现（含 PG users.failed_attempts schema + migration + outbox + lock 事件） | sessionlogin slice + user domain + PG repo + migration + integration tests | L3 | 8h+4h |
| C3 | PR-V1-TEST-JOURNEY-ROOT | TEST-JOURNEY-ROOT / 027#8 | 缺 root journey integration harness（J-useronboarding / J-accountlockout） | tests/integration root harness + 2 个 J yaml；顺带覆盖 B2-C-13 L2-CROSS-LAYER-E2E-REGRESSION | L2 | 12h+4h |
| C4 | PR-V1-CONTRACT-INTEGRITY | backlog1 §2.2 / 027#9 | contracttest schema ref / endpoint→test mapping / path-query executable 三处闭环缺 | 3 子项打包：CONTRACTTEST-SCHEMAREF-FAILFAST + CONTRACT-ENDPOINT-TEST-MAPPING + CONTRACT-PATH-QUERY-EXECUTABLE | L3 | 12h+6h |
| C6 | PR-V1-JOURNEY-AUDIT-E2E | P2-T-02（tech-debt 核验）| J-auditlogintrail journey 已在 yaml 定义，但 smoke_e2e_test.go 未锁该链路，auditcore → accesscore → sessionmint 事件链 e2e 闭环缺口 | 基于 C3 root harness 补 J-auditlogintrail 端到端断言；断言审计链写入 → 登录事件发布 → accesscore 投影三段完整性；依赖 C3 先合 | L2 | 6h+3h |

> C5 ✅ PR#368 + PR#394 详见归档版。

### Track D · runtime / observability（残余 3 项 / ~57h + ~28.5h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| D3 | PR-V11-OBS-OTEL-HARDENING（**拆 3 PR，2026-05-08**）| B2-R-02..09（9 项）+ B2-A-19/20/21 + PR237-OB2 + WS-HUB-READYZ-PROBE-01 + PR-CI-5-FU-HEALTH-LATE-WATCHER | configcore/auditcore readyz 缺 repo 健康探针 / bootstrap rollback 错误未并入返回值 / OTel provider/tracer/callback/cache 6 大生产缺陷 / Prometheus middleware 缺 internal listener / WebSocket Hub readyz / health late-watcher 泄漏 | **拆为 3 PR + 散落项吸收**：(D3a) `PR-V11-D3-R-METRIC-PACK` = R-01+R-02+G-05+PR237-OB2（kernel Provider GaugeVec + bootstrap auto-wire + outbox/event collector + internal mux metrics，文件域 `kernel/observability/metrics/*` + `runtime/observability/metrics/*` + `runtime/http/middleware/metrics.go` + bootstrap phase 5/6）= **26h+12h**；(D3b) `PR-B2-B3 OTEL-PROVIDER-LIFECYCLE-AND-GLOBAL` = B2-R-05/06/07/08/09 + B2-A-19/20/21（adapters/otel/* 8 条）= **14h+7h**；(D3c) `PR-B2-D4 CONFIGCORE-READYZ-REPO-PROBE` = B2-R-02（cells/configcore/auditcore HealthCheckers 加 repo Pingable）= **3h+1.5h**。其余散落 2 项：WS-HUB-READYZ-PROBE-01 → 已并入 D7 PR#393 ✅ / PR-CI-5-FU-HEALTH-LATE-WATCHER → 并入 `PR-B2-E1 BOOTSTRAP-ROLLBACK-AND-HEALTH` | L3 | **43h+20.5h**（D3a 26h+12h + D3b 14h+7h + D3c 3h+1.5h；散落项工时计入 F1/E1）|
| D4 | PR-V11-AUDITAPPEND-MUTEX-DECOMPOSE | B2-C-10 | auditappend 全局 mutex 串行化 13 个 topic consumer，吞吐受限 | 按 topic 拆细粒度锁 / partition-aware claim | L3 | 8h+4h |
| D5 | PR-V11-CONFIG-ROLLBACK-OPTLOCK | B2-T-01 | config rollback 缺 optimistic lock，并发回滚双写 | 加 version 字段 + CAS 回滚 | L2 | 6h+4h |

> D1 ✅ PR#359 / D2 ✅ PR#443 / D6 ✅ refactor/503-prod-clock-injection / D7 ✅ PR#393 / D8 ✅ PR#367 详见归档版。

### Track G · CI / lint / governance

✅ 全 11 项 ship（G1-G12，除 G10 永久 won't-do）。详见归档版。

### Track F · 供应链 / 文档 / 治理（残余 4 PR / ~29h + ~13h）

| # | PR | 来源 | 问题 | 方案 | ship | 工时 |
|---|---|---|---|---|:-:|---|
| F1 | PR-V1-POSITIONING-AND-LIBS | E5/E14 / 027#26 + **PR#376 review F3 absorb（2026-05-06）** | 外部认知锚定缺 — README 无 elevator pitch，无对标定位（重框架/强结构化），无通用库清单；**追加**：F3 入门文档与 generated artifact 矩阵漂移 — README tutorial 缺 cell 元数据、build 指令陈旧 + `docs/guides/cell-development-guide.md` 教手写 `wrapper.ContractSpec{}` 范例 + 引用已删除 FMT-18 + 多处与 PR#376 后的真相源不一致 | `docs/architecture/positioning.md` + README pitch + 通用库清单（Tier A 5 + Tier B 20 = 25 包）；**追加范围（吸收 F3 README + cell-guide 部分）**：(a) README tutorial 补必填 cell 元数据 + 修 build 指令 + cell metadata 章节走 contractgen workflow；(b) `cell-development-guide.md` 删手写 `wrapper.ContractSpec{}` / `wrapper.EventSpec{}` 范例改为 contractgen workflow + 删 FMT-18 引用；(c) 顺路 grep `docs/**` 扫 `wrapper.ContractSpec{` / `wrapper.EventSpec{` / `FMT-18` 残留全量清理。**doc.go 部分由 06.FU ✅ 收**（typed envelope 本来就要重写 contractgen/doc.go） | L1 | **6h+2.5h**（原 5h+2h + 1h+0.5h F3 docs drift）|
| F2 | PR-V1-DOCS-MULTIPOD | PR237-PM5/PR-A58 / 027#20 | dual-listener 部署指南缺；examples 注释缺；compose 缺 healthcheck | 部署指南 + examples 注释 + compose healthcheck | L1 | 6h+2h |
| F4 | PR-V11-API-VERSIONING-POLICY | E7-4 / 028 B3-1 + **PR#376 review F5 absorb（2026-05-06）** | contract 缺 deprecation 机制，`storageVersion` 概念缺；**追加**：F5 governance 不拒绝 `/internal/v1/*` 与 `auth.public` 组合（`kernel/governance/rules_fmt.go` + `contracts/contract.schema.json` 仅编码 `public` vs `passwordResetExempt`，runtime 拒绝在 route.go/router.go，contract 层仍允许语义破损组合，当下无 live 违规但属预防性漏洞） | storageVersion + 弃用窗口策略 + kube-api-linter 评估 + `lifecycle: deprecated` + `removeBy: vN.M` 字段 + 治理强制 4 周 deprecation；**追加范围（吸收 F5 internal+public 互斥）**：(a) `rules_fmt.go` 加 cross-rule（占位 FMT-21）：`path` 以 `/internal/v1/` 前缀 ⊕ `auth: public` 互斥；(b) `contracts/contract.schema.json` 加 conditional require（`if path startswith /internal/v1/ then auth ∈ {service, bootstrap}`）；(c) 顺路加 `auth ∈ {public, service, bootstrap, passwordResetExempt}` 闭集守卫；(d) 负向 fixture 喂入 governance test | L2 | **9h+4.5h**（原 8h+4h + 1h+0.5h F5 governance rule）|
| F5 | PR-V11-DOCS-AUTOMATION | E7-5 / 028 B3-2 | API reference HTML 全手写，与 contract.yaml schema 漂移；ADR 模板不统一 | gen-crd-api-reference-docs（K8s 范式）从 contract.yaml schema 自动生成 + KEP frontmatter 风格 ADR 模板 | L2 | 8h+4h |

> F3 ✅ PR#345（supply-chain）/ F6 ✅ #338 详见归档版。

### Track J · DevTools Platform

✅ 全 1 项 ship（J1 PR#357，J2 absorbed）。详见归档版。

---

## Won't-do / 触发条件待达

| 来源 | 项 | 不做理由 / 触发条件 |
|---|---|---|
| baseline | **E8** bootstrap 显式化（phase 拆 8 函数）| 与 E5 positioning「重框架，强结构化」张力；`bootstrap.Run()` 单入口是卖点 |
| baseline | **E12** Actor 合并入 Cell（`type=external`）| 破坏 Cell vs Actor 语义清晰度（Cell = 你的代码，Actor = 外部依赖）；词汇收敛 5% 体感改进 << 语义清晰度代价 |
| backlog | **OBS-SSA-ANALYZER-01** | 当前 typed archtest（#01 后）已够；SSA 留给跨包 taint 真实需求出现时 |
| backlog | **CHANGEPASSWORD-CONCURRENT-SEMANTICS** | 客户反馈触发；当前 OUT_OF_SCOPE 共识 |
| backlog | **X13** REFRESH-PARTITION-01 | PG 分区，等生产流量阈值 |
| backlog | **PR-A8-RESIDUAL** Vault K8s auth 真实 e2e | 部署目标平台明确后做 |
| backlog | **S14a** CONFIG-VALUE-KMS-AWS-PROVIDER | 部署目标云平台明确后触发 |
| backlog | **T5** AUTH-SIGNER-01（`SigningKeyProvider` → `crypto.Signer`）| 2-3h，无外部依赖（stdlib + jwt/v5 已支持），caller 需 HSM/KMS/EC 私钥时优先 ship；2026-05-01 解除 golang-jwt v6 gating |
| backlog | **T1-T4 / T6-T10** 其余触发条件项 | 触发条件未达成 |
| 025 | Wave 4 PR-A22/A23/A24/A33 | 部分被 #02/#03 间接吸收，残余触发条件未达成 |
| Track G | **G10** PR-V11-MULTI-OS-GATE（archtest multi-GOOS scan）| 当前 `os-smoke` workflow 兜底已够；平台 tag 文件 sleep/literal 逃逸概率极低；引入 OS matrix 必跑成本 > 收益（2026-05-02）|
| Track J | **J3** DEVTOOLS-CALLGRAPH-API（function 粒度静态 callgraph）| J1 unified catalog (含 packageDeps) 落地后看用户反馈；package 粒度若已够则不必下钻到 function；触发条件 = J1 ship 后用户明确提出"package 粒度看不出问题" |
| Track J | **J4** DEVTOOLS-RUNTIME-TOPOLOGY-API（实际请求 trace 拓扑）| 不自建 — runtime topology 由 OTel + Jaeger/Tempo 生态承接（D3 已在做 OTel 加固）；自建 API 与 Jaeger UI 重叠且生态成熟度不及，永久 won't-do（2026-05-02）|
| K#12 review | **PR-V11-OUTBOX-MIDDLEWARE-DUAL-LIST**（双 middleware 列表分离业务/框架 + 框架层列表化扩展点）| **重方案**：分离 `EntryMiddleware` + `SubscriberMiddleware` 双列表 ~400-500 LOC（含测试）。**Won't-do 理由**：违反 YAGNI；**触发条件**（任一达成才起 PR）：(a) 框架 middleware ≥ 3 且需要用户自定义排序/替换；(b) 用户/外部贡献者明确要求自定义框架 middleware 注入点；(c) 测试覆盖需要按 framework middleware 维度切片解耦且 concrete decorator 已不足 |

---

## 030 review 融合方案残余（2026-05-05 起）

> §A/§A'/§B/§B' 全 ✅；本节只列残余项

### A. 并入既有 PR 残余

| 030 项 | 并入目标 | 范围 | 增量工时 |
|---|---|---|---|
| **K-08(b)** scaffold assembly | **K#09** SCAFFOLD-ONE-CMD（待）| 第 4 个子命令 `gocell scaffold assembly --id=... --cells=... --deploy=k8s` 一键产出可编译 assembly | +6h/+2h |
| **R-01** EVENT-OBSERVABILITY-METRIC-PACK + **R-02** EVENTBUS-DROP-CONTEXTUAL-LOG + **G-05** OUTBOX-CONSUMER-COLLECTOR + **PR237-OB2** INTERNAL-LISTENER-METRICS | **D3a**（待，命名 `PR-V11-D3-R-METRIC-PACK`，是 D3 第一批）| (R-01) `kernel/observability/metrics.Provider` 加 `GaugeVec` + NopProvider；shutdown/outbox/event 三套 collector 工厂收口；bootstrap phase 5/6 自动 wire `metricsProvider`。(R-02) `InMemoryEventBus.broadcast/roundRobin` drop 路径升 Error 级 + 补 entry_id/aggregate_id/event_type 三字段。(G-05) retry budget 耗尽 reject counter 由 R-01 `ConsumerCollector` 接口注入。**(PR237-OB2 顺路收)** internal mux 挂 `middleware.Metrics`：`Provider` 加 `listener` label（primary/internal）让 `/internal/v1/*` 流量进入 `http_requests_total` / `http_request_duration_seconds` | 22h+11h + **+4h**（PR237-OB2 吸收）= **26h+12h** |
| **G-04** KERNEL-INTERNAL-DAG-GUARD | **K#01 follow-up archtest**（独立小 PR，待）| `tools/archtest/` 新增 `KERNEL-INTERNAL-DAG-01`：固化 22 个 kernel 子模块合法上下游（assembly/wrapper 顶层、crypto/clock/ctxkeys 叶子）；与 030 K-03 doc.go 双护栏 | 8h+4h |

### B. Standalone 残余（决议 ADR 待写）

| 顺序 | PR 名 | 030 项 | 类型 | 范围 | 工时 |
|:-:|---|---|---|---|---|
| ~~N6~~ | ~~ADR-030-K04-PLATFORM-CELLS-BOUNDARY~~ | K-04 | **决议 won't-do**（2026-05-05），独立 ADR 待写 | 决议：accesscore / auditcore / configcore **留 framework 仓**（不迁 `examples/platform-cells/`）；产品定位 = 框架自带认证/配置/审计能力。落 ADR 锁定决策（1-2h，不需 architect 仲裁会议） | 2h+1h |
| ~~N7~~ | ~~ADR-030-K05-CONTRACT-CONCEPT-COLLAPSE~~ | K-05 | **决议 won't-do**（2026-05-05），独立 ADR 待写 | 决议：(a) 保留 4-kind（含 projection）；(b) ContractMeta `EndpointsMeta` 10 字段森林**不拆 sealed oneof**；(c) ContractSpec 双定义已由 06.PR4 ✅ 闭环 | 2h+1h |

### B''. PR#376 06.PR4 review 闭口分发残余（2026-05-06）

| Finding | 严重度 | 自审 | 归宿 PR | 增量 | 关闭项 |
|---|:-:|:-:|---|---|---|
| F3 doc/README/cell-guide 三处与 generated 矩阵漂移（README+cell-guide 部分）| P2 | P2 | **F1** README+cell-guide | +1h+0.5h（已计入 F1 工时）| F3 残余（doc.go 部分 ✅ 由 06.FU 收）|
| F5 `/internal/v1/* + auth.public` governance 缺拦截 | P2 | **降 P3** | **F4** contract 治理同源 | +1h+0.5h（已计入 F4 工时）| F5 |

> F1 / F2 / F4 / F6 已 ✅；详见归档版。

### Won't-do（030 范围内本轮裁剪）

| 030 项 | 不做理由 |
|---|---|
| **K-02** JOURNEY-LIFECYCLE-CI-CLOSE | 本轮优先 kernel/runtime 稳定性，journey lifecycle 治理收敛押后；J-confighotreload 引用未声明 contract 由 ADV-06 已能拦截 |
| **K-07** CELLS-SLICE-MULTI-VERB-DECOMPOSE | 永久 won't-do — 06.PR4 已吸收 K#04 PR-4 + K#05 PR-A2 + PR-B + 030 K-06，再加 K-07 单 PR 触 50+ 文件 review 不可行；slice 拆分非 kernel/runtime 稳定性必需 |
| **K-04** PLATFORM-CELLS-BOUNDARY（N6）| **决议 不迁移**（2026-05-05）— accesscore / auditcore / configcore 留 framework 仓；落 ADR 锁定即可（1-2h），见 §B 残余 |
| **K-05** CONTRACT-CONCEPT-COLLAPSE（N7）| **决议 不做**（2026-05-05）— 三段决议详见 §B 残余；K-05a/K-05c 已 ✅ 并入 06.PR4 |
| **030 Track A** (A-01..A-08) | adapters 加固押后 |
| **030 Track C** (C-01..C-07) | cells 业务收敛押后 |
| **030 Track G** P2/P3（G-06..G-15 除 G-04/G-05）| governance/kernel 防腐 P2/P3 押后 |
| **030 Track J** (J-01..J-04) | journeys/contracts 收敛押后 |
| **030 Track F** (F-01..F-10) | 工程基线押后 |

---

## 残余总览

| 维度 | 数量 | 工时（dev+review）|
|---|:-:|---|
| 关键路径（#07 #09 #13）| 3 | 42h+21h |
| Track B 残余（B2 拆 3 / B3 / B5.FU）| 3 PR（B2 含 3 子 PR）| 44h+29h |
| Track C 残余（C1 P0 / C2 / C3 / C4 / C6）| 5 | 50h+23h |
| Track D 残余（D3 拆 3 / D4 / D5）| 3 PR（D3 含 3 子 PR）| 57h+28.5h |
| Track F 残余（F1 / F2 / F4 / F5）| 4 | 29h+13h |
| 030 §A 残余（K-08(b)→K#09 / D3a R-METRIC-PACK / G-04 archtest）| 3 项（已并入 K#09 / D3a 或独立小 PR）| 含上 |
| 030 §B 决议 ADR（N6 / N7）| 2 | 4h+2h |
| 030 §B'' 残余（F3 docs → F1 / F5 governance → F4）| 0 独立 PR（已并入 F1/F4 工时）| 含上 |
| **合计** | **~17 PR**（含 B2/D3 拆分子 PR）| **~226h dev + ~117h review ≈ 343h** |

> 不含 Won't-do / 触发条件待达项；不含 backlog.md 233 条 OPEN 项中由 032 plan 接管的 V1.0 GA 后期 + V1.1 前期收口（详见 `archive/202605071100-032-backlog-029-residual-roadmap.md`，16 PR / ~475h）。

---

## 下一步建议优先级

1. **Track C C1 hashchain 恢复**（P0，12h+6h，独立可起）
2. **B2.A PG-ACCESSCORE-REPO**（关键路径，解锁 B5.FU；16h+12h）
3. **D3a R-METRIC-PACK**（reviewer 容量充足时；26h+12h）
4. B2.B / B2.C 与 B2.A 并发 worktree（14h+10h 散）
5. C2-C6 串行（依赖 C3）
6. F1 / F2 / F4 / F5 docs 顺路收（按 reviewer 容量穿插）
7. K#09 scaffold-one-cmd（吸收 K-08(b)）
8. K#07 EventRouter 字段拆分（codegen 后最便宜）
9. ADR N6 / N7 决议落地
10. #13 PR-A22 cell-iface ISP 拆分（L4 长期债，最末）

---

## 参考

- **归档源**：`docs/plans/archive/202605011500-029-master-roadmap.md`（完整 12 项关键路径 / 7 Track 全文 / errcode 主题 W1-W4 / 030 §A-§B'' 全段）
- **真理源 backlog**：`docs/backlog.md`（主，269 条 entry，233 OPEN）+ `docs/backlog2.md` §2-§8（B2-K/T/A/X/W/R/C P0/P1/P2 区域），不归档，只追加状态
- **032 V1.0 后期 + V1.1 前期 plan**：`docs/plans/archive/202605071100-032-backlog-029-residual-roadmap.md`（029 范围外 backlog 收口，16 PR / ~475h，0 ✅ shipped）
- **archive plan**（追溯历史决策时打开）：027 `docs/plans/archive/202604301129-027-release-v1.0-readiness.md`（36 PR 全文 + 5 维评分体系定义）+ 028 `docs/plans/archive/202604301204-028-post-v1.0-leverage-and-v1.1-roadmap.md`（13 PR + Batch + won't-do）+ 029 中段快照 `docs/archive/202605011500-029-master-roadmap-bak202605060248.md`（2026-05-06 02:48 备份，PR#384 N8 闭口前的 §A/§B/§B' 状态）
- **engineering-baseline**：`docs/plans/engineering-baseline/202604300600-radical-lightweight-revision.md` + `202604300700-extension-leverages.md`（E1-E14 杠杆评分原文）
- **ci-governance**：`docs/plans/ci-governance/202604300430-golangci-tier12-priority-and-projection.md`（Batch 1/2/3/4）
- **ship skill**：`.claude/skills/ship/SKILL.md`（L1-L4 + 工时区间）
- **git baseline**：`origin/develop @ a54d0c77`（最新：PR#447 cells/{configcore,accesscore}/cell_routes.go 占位删除 + configreceive 撤回主方案改业务触发 / PR#446 PR-CFG-TEST-RESIDUALS 3 Cx1 follow-ups + Soft→Medium const lift / PR#444 runtime/auth/session Store interface + MemStore + Protocol-driven storetest（plan 038 S2）/ PR#443 corebundle shared_deps.go + bundle.go per-concern split（**D2 闭口**）/ PR#440 PR-Δ1 scanner bug + 安全收口 3 follow-up 合并 / PR#439 runtime/auth/session typed Protocol primitive skeleton + ADR / PR#423 kernel/observability/doc.go umbrella 包级文档（**N5 闭口**）；归档前历史 PR 详见 archived 029）
