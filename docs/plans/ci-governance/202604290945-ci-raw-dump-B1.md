# CI 原始配置转储 B1 — 6 个工业项目

> 日期：2026-04-30
> 任务：直接从大项目 .golangci.yml / CI 配置 raw 内容提取规则全集，**不以 GoCell 内部问题为筛选标准**
> 调研者：explorer agent
> 关联文件：`202604290945-ci-baseline-raw-extraction.md`（aggregate）

---

## §1 caddyserver/caddy

### 1.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/caddyserver/caddy/master/.golangci.yml`
- CI workflow：`https://raw.githubusercontent.com/caddyserver/caddy/master/.github/workflows/ci.yml`

源码位置：
- https://github.com/caddyserver/caddy/blob/master/.golangci.yml
- https://github.com/caddyserver/caddy/blob/master/.github/workflows/ci.yml

### 1.2 完整 linters: enable 列表（共 29 个）

```
asasalint
asciicheck
bidichk
bodyclose
decorder
dogsled
dupl
dupword
durationcheck
errcheck
errname
exhaustive
gosec
govet
importas
ineffassign
misspell
modernize
prealloc
promlinter
sloglint
sqlclosecheck
staticcheck
testableexamples
testifylint
tparallel
unconvert
unused
wastedassign
whitespace
zerologlint
```

### 1.3 完整 linters-settings 展开

**run:**
```yaml
issues-exit-code: 1
tests: false
build-tags:
  - nobadger
  - nomysql
  - nopgx
```

**staticcheck:**
```yaml
checks: ["all", "-ST1000", "-ST1003", "-ST1016", "-ST1020", "-ST1021", "-ST1022", "-QF1006", "-QF1008"]
```

**errcheck:**
```yaml
exclude-functions:
  - fmt.*
  - (go.uber.org/zap/zapcore.ObjectEncoder).AddObject
  - (go.uber.org/zap/zapcore.ObjectEncoder).AddArray
```

**exhaustive:**
```yaml
ignore-enum-types: reflect.Kind|svc.Cmd
```

**formatters:**
```yaml
enable:
  - gci
  - gofmt
  - gofumpt
  - goimports
settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/caddyserver/caddy/v2/cmd)
      - prefix(github.com/caddyserver/caddy)
    custom-order: true
```

### 1.4 完整 exclude-rules

```yaml
exclusions:
  generated: lax
  presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
  rules:
    - linters: [gosec]
      text: G115
    - linters: [gosec]
      text: G107
    - linters: [gosec]
      text: G203
    - linters: [gosec]
      text: G204
    - linters: [gosec]
      path: modules/caddyhttp/reverseproxy/selectionpolicies.go
      text: G404
    - linters: [gosec]
      path: modules/caddyhttp/reverseproxy/streaming.go
      text: G404
    - linters: [dupl]
      path: modules/logging/filters.go
    - linters: [dupl]
      path: modules/caddyhttp/matchers.go
    - linters: [dupl]
      path: modules/caddyhttp/vars.go
    - linters: [errcheck]
      path: _test\.go
  paths:
    - third_party$
    - builtin$
    - examples$
```

### 1.5 CI / Makefile target 摘录

CI workflow (`ci.yml`) test job:
```yaml
env:
  GOFLAGS: '-tags=nobadger,nomysql,nopgx'
  GOTOOLCHAIN: local
steps:
  - go get -v -t -d ./...
  - go build -trimpath -ldflags="-w -s" -v   # CGO_ENABLED: 0
  - go test -v -coverprofile="cover-profile.out" -short -race ./...
```

lint 通过 `golangci-lint run --timeout 10m` 在单独 job 中运行（`golangci-lint` action）。

### 1.6 lint 风格特征

**gosec 精选豁免（G107/G115/G203/G204/G404）+ dupl 文件级白名单 + 4 个格式化器共用（gci 分段导入 + gofumpt + goimports）+ sloglint/zerologlint 强制结构化日志 + tests:false（lint 不扫测试文件）**

---

## §2 pulumi/pulumi

### 2.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/pulumi/pulumi/master/.golangci.yml`
- Makefile：`https://raw.githubusercontent.com/pulumi/pulumi/master/Makefile`

源码位置：
- https://github.com/pulumi/pulumi/blob/master/.golangci.yml
- https://github.com/pulumi/pulumi/blob/master/Makefile

### 2.2 完整 linters: enable 列表（共 21 个）

```
depguard
durationcheck
exhaustive
forbidigo
goheader
goprintffuncname
gosec
importas
lll
misspell
nakedret
nolintlint
paralleltest
perfsprint
prealloc
revive
unconvert
usetesting
wastedassign
whitespace
copyloopvar
gocritic
```

### 2.3 完整 linters-settings 展开

**run:**
```yaml
version: "2"
build-tags:
  - all
```

**usetesting:**
```yaml
os-temp-dir: true
context-background: true
context-todo: true
```

**gocritic:**
```yaml
disabled-checks:
  - appendAssign
  - argOrder
  - badCall
  - badCond
  - caseOrder
  - codegenComment
  - deprecatedComment
  - dupArg
  - dupCase
  - dupSubExpr
  - exitAfterDefer
  - flagDeref
  - flagName
  - mapKey
  - offBy1
  - sloppyLen
  - sloppyTypeAssert
  - assignOp
  - captLocal
  - commentFormatting
  - defaultCaseOrder
  - elseif
  - ifElseChain
  - newDeref
  - regexpMust
  - singleCaseSwitch
  - switchTrue
  - typeSwitchVar
  - underef
  - unlambda
  - unslice
  - valSwap
  - wrapperFunc
enabled-checks:
  - ruleguard
settings:
  ruleguard:
    rules: '${config-path}/.golangci/rules.go'
    failOn: dsl,import
```

**depguard:**
```yaml
rules:
  protobuf:
    deny:
      - pkg: github.com/golang/protobuf
        desc: Use google.golang.org/protobuf instead
```

**goheader:**
```yaml
values:
  regexp:
    COPYRIGHT_YEARS: \d{4}
    WHITESPACE: '[ ]{4}'
template: |-
  Copyright {{ COPYRIGHT_YEARS }}, Pulumi Corporation.

  Licensed under the Apache License, Version 2.0 (the "License");
  ...
```

**govet:**
```yaml
enable:
  - nilness
  - reflectvaluecompare
  - sortslice
  - unusedwrite
```

**importas:**
```yaml
alias:
  - pkg: github.com/pulumi/pulumi/sdk/v3/proto/go
    alias: pulumirpc
  - pkg: github.com/pulumi/pulumi/sdk/v3/proto/go/testing
    alias: testingrpc
  - pkg: github.com/deckarep/golang-set/v2
    alias: mapset
  - pkg: github.com/pulumi/pulumi/sdk/v3/go/common/testing
    alias: ptesting
```

**nakedret:**
```yaml
max-func-lines: 60
```

**nolintlint:**
```yaml
allow-unused: true
```

**forbidigo:**
```yaml
forbid:
  - pattern: ^assert.NoError$
    msg: Use require.NoError instead
  - pattern: ^assert.NotNil$
    msg: Use require.NotNil instead
  - pattern: ^assert.Len$
    msg: Use require.Len instead
  - pattern: ^resource.New(String|Bool|Number|Array|Asset|Archive|Object|Computed|Output|Secret|ResourceReference)Property$
    msg: Use resource.NewProperty instead
  - pattern: ^os.Rename$
    msg: "`os.Rename` does not work across file systems and is not atomic for windows. Use with caution"
```

**revive:**
```yaml
rules:
  - name: use-any
```

**formatters:**
```yaml
enable:
  - gofumpt
```

### 2.4 完整 exclude-rules

```yaml
exclusions:
  generated: lax
  presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
  rules:
    - linters: [revive]
      text: 'unused-parameter: parameter \S+ seems to be unused, consider removing or renaming it as _'
    - linters: [revive]
      text: 'empty-block: this block is empty, you can remove it'
    - linters: [revive]
      text: 'redefines-builtin-id: redefinition of the built-in function new'
    - path: (.+)\.go$
      text: 'Name is deprecated: Name returns the variable or declaration name of the resource'
    - path: (.+)\.go$
      text: '"github.com/golang/protobuf/[\w/]+" is deprecated'
    - path: (.+)\.go$
      text: strings.Title has been deprecated
    - path: (.+)\.go$
      text: 'deprecated: Please use types in:? cloud.google.com/go/logging/apiv2/loggingpb'
    - linters: [staticcheck]
      text: 'error strings should not end with punctuation or newlines'
    - linters: [staticcheck]
      text: 'error strings should not be capitalized'
    - linters: [staticcheck]
      text: 'is being imported more than once'
    - linters: [staticcheck]
      text: 'other import of'
  paths:
    - Godeps$
    - builtin$
    - node_modules
    - testdata$
    - third_party$
    - vendor$
```

### 2.5 CI / Makefile target 摘录

**Makefile lint targets:**
```makefile
GOLANGCI_LINT_CONFIG=$(shell pwd)/.golangci.yml

lint::              # 运行 golangci-lint + pulumi_json + golang lint
lint_golang:        # golangci-lint $(LINT_GOLANG_PKGS) --timeout 5m --max-same-issues 0 --max-issues-per-linter 0
lint_golang_fix:    # golangci-lint ... --fix
lint_actions:       # actionlint v1.6.27
lint_fix::          # 合并 golang + JSON schema fix

test_fast::         # 快速测试项目包 + codegen node
test_all::          # test_pkg + test_integration
test_pkg_rest:      # go test 排除慢速 codegen
test_performance:   # go test -timeout 1h -parallel=1 -count=1
test_lifecycle_fuzz:  # 模糊测试，LIFECYCLE_TEST_FUZZ_CHECKS 默认 10000
GO_TEST_RACE = false  # 生命周期模糊测试显式禁用 race 以优化性能
```

CI：race detection 对 Darwin 和 Linux amd64 构建二进制时启用，测试集成通过 `PULUMI_INTEGRATION_TESTS` 环境变量按 SDK（nodejs/python/go/pcl）分组。

### 2.6 lint 风格特征

**forbidigo 强制测试断言风格（require 替代 assert）+ goheader 版权头强制 + ruleguard 自定义规则引擎（`.golangci/rules.go`）+ depguard 禁止旧 protobuf 包 + gocritic 大量禁用默认检查保留 ruleguard**

---

## §3 minio/minio

### 3.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/minio/minio/master/.golangci.yml`
- CI workflow：`https://raw.githubusercontent.com/minio/minio/master/.github/workflows/go.yml`

源码位置：
- https://github.com/minio/minio/blob/master/.golangci.yml
- https://github.com/minio/minio/blob/master/.github/workflows/go.yml

### 3.2 完整 linters: enable 列表（共 13 个）

```
durationcheck
forcetypeassert
gocritic
gomodguard
govet
ineffassign
misspell
revive
staticcheck
unconvert
unused
usetesting
whitespace
```

### 3.3 完整 linters-settings 展开

**run:**
```yaml
version: "2"
linters:
  default: none
```

**misspell:**
```yaml
locale: US
```

**staticcheck:**
```yaml
checks:
  - all
  - -SA1008
  - -SA1019
  - -SA4000
  - -SA9004
  - -ST1000
  - -ST1005
  - -ST1016
  - -U1000
```

**formatters:**
```yaml
enable:
  - gofumpt
  - goimports
```

**issues:**
```yaml
max-issues-per-linter: 100
max-same-issues: 100
```

### 3.4 完整 exclude-rules

```yaml
exclusions:
  generated: lax
  rules:
    - linters: [forcetypeassert]
      path: _test\.go
    - path: (.+)\.go$
      text: 'empty-block:'
    - path: (.+)\.go$
      text: 'unused-parameter:'
    - path: (.+)\.go$
      text: 'dot-imports:'
    - path: (.+)\.go$
      text: should have a package comment
    - path: (.+)\.go$
      text: error strings should not be capitalized or end with punctuation or a newline
  paths:
    - third_party$
    - builtin$
    - examples$
```

### 3.5 CI / Makefile target 摘录

**Makefile:**
```makefile
lint:             # golangci-lint run --build-tags kqueue --timeout 10m; typos
lint-fix:         # golangci-lint run --fix --build-tags kqueue --timeout 10m

test:             # verifiers + build; CGO_ENABLED=0; MINIO_API_REQUESTS_MAX=10000; go test ./...
test-race:        # build minio; go test -race ./...
install-race:     # go build -race; GORACE=history_size=7; CGO_ENABLED=1

check-gen:        # go generate && git diff --exit-code (验证生成文件未变化)
verifiers:        # lint + check-gen
```

**CI workflow (`go.yml`):**
```yaml
env:
  CGO_ENABLED: 0
  GO111MODULE: on
  MINIO_KMS_SECRET_KEY: ...
steps:
  - make verify       # lint + check-gen
  - make test-timeout
```

Go version: 1.24.x

### 3.6 lint 风格特征

**极简 13 linter + forcetypeassert 强制类型断言安全（测试文件豁免）+ gomodguard 模块白名单 + staticcheck 全开精选关闭 8 项 + build-tags kqueue + typos 拼写检查器**

---

## §4 grafana/grafana

### 4.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/grafana/grafana/main/.golangci.yml`
- lint workflow：`https://raw.githubusercontent.com/grafana/grafana/main/.github/workflows/lint.yml`（fetch 失败）

源码位置：
- https://github.com/grafana/grafana/blob/main/.golangci.yml

### 4.2 完整 linters: enable 列表（共 20 个）

```
asciicheck
bodyclose
depguard
dogsled
errcheck
errorlint
exhaustive
gocyclo
goprintffuncname
gosec
govet
ineffassign
misspell
nakedret
prealloc
revive
staticcheck
unconvert
unused
whitespace
```

### 4.3 完整 linters-settings 展开

**run:**
```yaml
version: '2'
run:
  timeout: 15m
  concurrency: 10
  allow-parallel-runners: true
linters:
  default: none
```

**depguard（完整 rules 展开）：**

```yaml
rules:
  aggregator:
    list-mode: lax
    files: [./pkg/aggregator/*, ./pkg/aggregator/**/*]
    allow: [github.com/grafana/grafana/pkg/aggregator, .../pkg/semconv, .../pkg/apimachinery]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apimachinery is not allowed to import grafana core

  apimachinery:
    list-mode: lax
    files: ['**/pkg/apimachinery/*', '**/pkg/apimachinery/**/*']
    allow: [github.com/grafana/grafana/pkg/apimachinery]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apimachinery is not allowed to import grafana core

  apiserver:
    list-mode: lax
    files: ['**/pkg/apiserver/*', '**/pkg/apiserver/**/*']
    allow: [.../pkg/apimachinery, .../pkg/apiserver]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apiserver is not allowed to import grafana core

  apps-playlist:
    list-mode: lax
    files: [./apps/playlist/*, ./apps/playlist/**/*]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apps/playlist is not allowed to import grafana core

  apps-dashboard:
    list-mode: lax
    files: [./apps/dashboard/*, ./apps/dashboard/**/*]
    allow: [.../pkg/apimachinery]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apps/dashboard is not allowed to import grafana core

  apps-secret:
    list-mode: lax
    files: [./apps/secret/*, ./apps/secret/**/*]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: apps/secret is not allowed to import grafana core

  coreplugins:
    files:
      - '**/pkg/tsdb/grafana-postgresql-datasource/**'
      - '**/pkg/tsdb/grafana-pyroscope-datasource/**'
      - '**/pkg/tsdb/grafana-testdata-datasource/**'
      - '**/pkg/tsdb/azuremonitor/**'
      - '**/pkg/tsdb/cloud-monitoring/**'
      - '**/pkg/tsdb/graphite/**'
      - '**/pkg/tsdb/mssql/**'
      - '**/pkg/tsdb/mysql/**'
      - '**/pkg/tsdb/opentsdb/**'
      - '**/pkg/tsdb/parca/**'
      - '**/pkg/tsdb/tempo/**'
      - '**/pkg/tsdb/cloudwatch/**'
      - '**/pkg/tsdb/loki/**'
      - '**/pkg/tsdb/zipkin/**'
      - '**/pkg/tsdb/jaeger/**'
      - '**/pkg/tsdb/influxdb/**'
    deny:
      - pkg: github.com/grafana/grafana/pkg/api
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/cmd
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/cuectx
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/extensions
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/kinds
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/middleware
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/modules
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/registry
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/services
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/build
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/codegen
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/events
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/ifaces
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/kindsysreport
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/mocks
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/plugins
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/setting
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/util
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/bus
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/components
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/expr
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/infra
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/login
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/models
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/server
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/tests
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/web
        desc: Core plugins are not allowed to depend on Grafana core packages
      - pkg: github.com/grafana/grafana/pkg/tsdb/intervalv2
        desc: Core plugins are not allowed to depend on Grafana core packages

  storage-unified-apistore:
    list-mode: lax
    files: [./pkg/storage/unified/apistore/**]
    allow: [.../pkg/apimachinery, .../pkg/apiserver, .../pkg/unified/resource]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: pkg/storage/unified/apistore is not allowed to import grafana core

  storage-unified-resource:
    list-mode: lax
    files: [./pkg/storage/unified/resource/**]
    allow: [.../pkg/apimachinery]
    deny:
      - pkg: github.com/grafana/grafana/pkg
        desc: pkg/storage/unified/resource is not allowed to import grafana core

  main:
    deny:
      - pkg: io/ioutil
        desc: 'Deprecated: As of Go 1.16, use io or os instead'
      - pkg: gopkg.in/yaml.v2
        desc: use go.yaml.in/yaml/v3 instead
      - pkg: gopkg.in/yaml.v3
        desc: use go.yaml.in/yaml/v3 instead
      - pkg: github.com/pkg/errors
        desc: 'Deprecated: Go 1.13 supports this in stdlib'
      - pkg: github.com/xorcare/pointer
        desc: Use pkg/util.Pointer instead
      - pkg: github.com/gofrs/uuid
        desc: Use github.com/google/uuid instead
      - pkg: github.com/bmizerany/assert
        desc: Use github.com/stretchr/testify/assert instead
```

**exhaustive:**
```yaml
default-signifies-exhaustive: true
```

**gocritic:**
```yaml
enabled-checks:
  - ruleguard
settings:
  ruleguard:
    rules: pkg/ruleguard.rules.go
```

**misspell:**
```yaml
ignore-rules:
  - Unknwon
  - Creater
```

**nakedret:**
```yaml
max-func-lines: 60
```

**revive:**
```yaml
confidence: 3
severity: warning
```

**formatters:**
```yaml
enable:
  - goimports
exclusions:
  paths: [devenv, scripts, third_party$, builtin$, examples$, pkg/util/xorm]
```

**issues:**
```yaml
max-same-issues: 0
```

### 4.4 完整 exclude-rules

```yaml
exclusions:
  generated: lax
  rules:
    - linters: [staticcheck]
      text: ST1003
    - linters: [staticcheck]
      text: ST1001
    - linters: [staticcheck]
      text: 'SA1019: http.CloseNotifier'
    - linters: [staticcheck]
      text: 'SA1019: strings.Title'
    - linters: [staticcheck]
      text: 'SA1019: "go.opentelemetry.io/otel/exporters/jaeger"'
    - linters: [staticcheck]
      text: use fake service and real access control evaluator instead
    - linters: [gosec]
      text: G108
    - linters: [gosec]
      text: G110
    - linters: [gosec]
      text: G115
    - linters: [gosec]
      text: G201
    - linters: [gosec]
      text: G202
    - linters: [gosec]
      text: G306
    - linters: [gosec]
      text: '401'
    - linters: [gosec]
      text: '402'
    - linters: [gosec]
      text: '501'
    - linters: [gosec]
      text: '404'
    - linters: [errorlint]
      text: non-wrapping format verb for fmt.Errorf
    - linters: [staticcheck]
      text: ST1000
    - linters: [staticcheck]
      text: ST1020
    - linters: [staticcheck]
      text: ST1021
    - linters: [gosec]
      path: (.+)_test\.go
      text: G601
  paths:
    - devenv
    - scripts
    - third_party$
    - builtin$
    - examples$
    - pkg/util/xorm
```

### 4.5 CI / Makefile target 摘录

lint workflow（`backend-code-quality.yml`，fetch 失败，从 CI action output 推断）：
```
golangci-lint run --timeout 10m
govulncheck ./...
dependency-review (PR 时启用)
Go version: ~1.26
```

### 4.6 lint 风格特征

**depguard 架构边界最严格（8 个 rule 块覆盖 pkg/aggregator/apimachinery/apiserver/apps/coreplugins/storage 层 + main 全局禁用 io/ioutil + gopkg.in/yaml.v2/v3 + pkg/errors）+ 15m 超时 10 并发 + errorlint 强制 fmt.Errorf %w**

---

## §5 nats-io/nats-server

### 5.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/nats-io/nats-server/main/.golangci.yml`
- CI workflow：`https://raw.githubusercontent.com/nats-io/nats-server/main/.github/workflows/tests.yaml`（fetch failed）

源码位置：
- https://github.com/nats-io/nats-server/blob/main/.golangci.yml

### 5.2 完整 linters: enable 列表（共 6 个）

```
forbidigo
govet
ineffassign
misspell
staticcheck
unused
```

### 5.3 完整 linters-settings 展开

**run:**
```yaml
version: "2"
run:
  concurrency: 4
  modules-download-mode: readonly
  issues-exit-code: 1
  tests: true
output:
  formats:
    text:
      path: stdout
      print-linter-name: true
      print-issued-lines: true
```

**forbidigo:**
```yaml
forbid:
  - pattern: ^fmt\.Print(f|ln)?$
```

**govet:**
```yaml
settings:
  printf:
    funcs:
      - (github.com/golangci/golangci-lint/pkg/logutils.Log).Infof
      - (github.com/golangci/golangci-lint/pkg/logutils.Log).Warnf
      - (github.com/golangci/golangci-lint/pkg/logutils.Log).Errorf
      - (github.com/golangci/golangci-lint/pkg/logutils.Log).Fatalf
```

**misspell:**
```yaml
locale: US
```

**staticcheck:**
```yaml
checks:
  - all
  - -QF*
  - -ST1003
  - -ST1016
```

**formatters:**
```yaml
enable:
  - goimports
exclusions:
  generated: lax
  paths: [.github, doc, docker, logos, scripts, util, third_party$, builtin$, examples$]
```

### 5.4 完整 exclude-rules

```yaml
exclusions:
  generated: lax
  presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
  rules:
    - linters: [forbidigo]
      path: main.go
    - linters: [forbidigo]
      source: "nats-server: v%s"
    - linters: [forbidigo]
      path: server/opts.go
    - linters: [forbidigo]
      path: _test.go
  paths:
    - .github
    - doc
    - docker
    - logos
    - scripts
    - util
    - third_party$
    - builtin$
    - examples$
```

### 5.5 CI / Makefile target 摘录

Makefile：fetch failed（404）。
CI workflow `tests.yaml`：fetch failed（连接中断）。

从搜索结果确认 CI 使用 `golangci-lint` action，workflow 名称为 "NATS Server Tests"，地址：`https://github.com/nats-io/nats-server/actions/workflows/tests.yaml`。

(Makefile fetch failed; CI workflow fetch failed after 1 retry)

### 5.6 lint 风格特征

**极简 6 linter 极简原则 + forbidigo 禁止 fmt.Print 系列（服务器不允许裸打印输出）+ tests:true（扫测试文件）+ staticcheck 全开仅关闭 QF* + ST1003/ST1016**

---

## §6 open-policy-agent/opa

### 6.1 raw URL 清单

- golangci 配置：`https://raw.githubusercontent.com/open-policy-agent/opa/main/.golangci.yaml`（注意扩展名为 .yaml）
- Makefile：`https://raw.githubusercontent.com/open-policy-agent/opa/main/Makefile`

源码位置：
- https://github.com/open-policy-agent/opa/blob/main/.golangci.yaml
- https://github.com/open-policy-agent/opa/blob/main/Makefile

### 6.2 完整 linters: enable 列表（共 15 个）

```
copyloopvar
errcheck
gocritic
govet
ineffassign
intrange
mirror
misspell
perfsprint
prealloc
revive
staticcheck
unconvert
unused
usetesting
```

注：`# - gosec` 被显式注释掉，注释说明 "too many false positives"

### 6.3 完整 linters-settings 展开

**run:**
```yaml
version: "2"
run:
  timeout: 5m
linters:
  default: none
```

**gocritic:**
```yaml
enabled-checks:
  # NOTE: these are rules enabled IN ADDITION TO the default set
  - filepathJoin
  - dupImport
  - redundantSprint
  - stringConcatSimplify
disabled-checks:
  - appendAssign
  # NOTE(ae): should be enabled, too many violations to fix in one go
  - singleCaseSwitch
  # not sure what to replace with in many locations
  - exitAfterDefer
  # disabled from the performance tag enabled further down
  - hugeParam
enabled-tags:
  - performance
settings:
  ifElseChain:
    # ridiculous value set for now, should be lowered to something reasonable
    minThreshold: 10
```

**govet:**
```yaml
disable:
  # enable later and fix
  - buildtag
enable:
  - deepequalerrors
  - nilness
```

**lll:**
```yaml
line-length: 200
```

**perfsprint:**
```yaml
# only rule disabled by default, but it's a good one
err-error: true
```

**revive:**
```yaml
rules:
  - name: redefines-builtin-id
    disabled: true
  - name: unused-receiver
    disabled: false
```

**staticcheck:**
```yaml
checks:
  [
    "all",
    "-ST1000",
    "-ST1003",
    "-ST1016",
    "-ST1020",
    "-ST1021",
    "-ST1022",
    "-QF1001",
    "-QF1002",
    "-QF1003",
    "-QF1008",
  ]
```

**formatters:**
```yaml
enable:
  - gofmt
  - goimports
```

**issues:**
```yaml
max-same-issues: 0
```

### 6.4 完整 exclude-rules

```yaml
exclusions:
  warn-unused: true
  generated: lax
  presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
  rules:
    - linters: [staticcheck]
      text: This package is intended for older projects transitioning from OPA v0.x
      path-except: v1/
    - linters: [staticcheck]
      text: SA1019   # Using a deprecated function, variable, constant or field
      path: _test\.go
  paths:
    - internal/gojsonschema
```

### 6.5 CI / Makefile target 摘录

**Makefile（完整 lint/test/race 目标）：**
```makefile
GOLANGCI_LINT_VERSION = ...   # 版本变量

check:  # lint target
ifeq ($(DOCKER_RUNNING), 1)
    $(DOCKER) run --rm -v $(shell pwd):/app:ro,Z -w /app \
        golangci/golangci-lint:${GOLANGCI_LINT_VERSION} golangci-lint run -v
else
    @echo "Docker not installed or running. Skipping golangci run."
endif

fmt:    # lint-fix target
ifeq ($(DOCKER_RUNNING), 1)
    $(DOCKER) run --rm -v $(shell pwd):/app:Z -w /app \
        golangci/golangci-lint:${GOLANGCI_LINT_VERSION} golangci-lint run -v --fix
else
    @echo "Docker not installed or running. Skipping golangci run."
endif

test: go-test wasm-test

go-test: generate
    $(GO) test $(GO_TAGS),slow ./...

go-test-short: generate
    $(GO) test $(GO_TAGS) -short ./...

race-detector: generate
    $(GO) test $(GO_TAGS),slow -race -vet=off ./...

test-coverage: generate
    $(GO) test $(GO_TAGS),slow -coverprofile=coverage.txt -covermode=atomic ./...
```

### 6.6 lint 风格特征

**golangci 在 Docker 容器中运行 + gocritic performance tag 全开 + mirror/intrange/perfsprint 性能三件套 + copyloopvar/usetesting Go 现代化 + gosec 显式注释掉（false positive 太多）+ warn-unused:true 提示未使用豁免规则 + lll line-length:200**

---

## 附：fetch 失败记录

| 项目 | 文件 | 状态 |
|---|---|---|
| grafana | .golangci.toml | 404，改为 .golangci.yml 成功 |
| nats-server | .golangci.yaml | 404，改为 .golangci.yml 成功 |
| opa | .golangci.yml | 404，改为 .golangci.yaml 成功 |
| nats-server | Makefile | 404 (1 retry) |
| nats-server | .github/workflows/tests.yaml | 连接中断 (1 retry) |
| grafana | Makefile | 连接中断 (1 retry) |
| grafana | .github/workflows/backend-code-quality.yml | 404 |
| opa | .github/workflows/post-merge.yml / build.yml | 404 / 404 |
| pulumi | .github/workflows/ci-lint.yml | 连接中断 |

---

## 引用清单

- [caddyserver/caddy .golangci.yml](https://github.com/caddyserver/caddy/blob/master/.golangci.yml)
- [pulumi/pulumi .golangci.yml](https://github.com/pulumi/pulumi/blob/master/.golangci.yml)
- [minio/minio .golangci.yml](https://github.com/minio/minio/blob/master/.golangci.yml)
- [grafana/grafana .golangci.yml](https://github.com/grafana/grafana/blob/main/.golangci.yml)
- [nats-io/nats-server .golangci.yml](https://github.com/nats-io/nats-server/blob/main/.golangci.yml)
- [open-policy-agent/opa .golangci.yaml](https://github.com/open-policy-agent/opa/blob/main/.golangci.yaml)
- [open-policy-agent/opa Makefile](https://github.com/open-policy-agent/opa/blob/main/Makefile)
- [NATS Server Tests workflow](https://github.com/nats-io/nats-server/actions/workflows/tests.yaml)
