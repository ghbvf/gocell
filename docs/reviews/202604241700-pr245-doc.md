# PR #245 Doc Review — refactor/520-pr-a5c-outbox-emitter-unify

> Reviewer: doc-engineer | Date: 2026-04-24 | Branch HEAD: 3447eb9d

---

## Finding 表

| # | 文件:行号 | 级别 | 范围 | 说明 & 修复建议 |
|---|-----------|------|------|----------------|
| D1 | `kernel/outbox/emitter.go:14-16` | C2 | IN_SCOPE | `Emitter` 接口 godoc 无 "see also DurabilityReporter" 交叉引用。读者从接口入口无法发现可选的 DurabilityReporter 协议扩展点。建议在 `Emitter` 注释尾部追加一行：`// Implementations may optionally satisfy DurabilityReporter to expose durability status.` |
| D2 | `kernel/cell/mode_resolver.go:128-131` | C2 | IN_SCOPE | `DirectPublishModeForDurability` godoc 未说明 "unknown mode 默认走 demoPolicy" 的行为边界。代码 `if mode == DurabilityDurable { return durablePolicy }; return demoPolicy` 对任何非 Durable 值（包括零值、未来新增等级）都返回 demoPolicy，但注释只提到 "demo (and any unknown mode)"，未给出明确的 fallback 安全性说明或调用者预期。建议在 "demo (and any unknown mode) returns demoPolicy" 后追加：调用者不应传入 DurabilityDurable 以外的非 DurabilityDemo 值；如有新等级需显式扩展本函数。 |
| D3 | `cells/accesscore/cell.go:73-91` (`WithOutboxDeps`) | C2 | IN_SCOPE | "accumulative: nil leaves prior value" 语义缺少反例警示。当前 godoc 说明了 nil 保留旧值，但未警告 "传 nil 不会清空已注入的值（需要重置请用 WithEmitter(nil) 再 WithOutboxDeps）"。auditcore 的同名函数有相同缺失（`cells/auditcore/cell.go:59-68`）。建议补充一句负向示例说明，防止调用方误以为 `WithOutboxDeps(nil, nil)` 可清除已有依赖。 |
| D4 | `cells/auditcore/cell.go:43-49` (`WithEmitter`) | C3 | IN_SCOPE | auditcore `WithEmitter` godoc 比 accesscore 版本短：缺少 "Durability for L2 slice upgrades is derived from outbox.ReportDurable(emitter)" 的说明，且无 `ref:` 行与 accesscore 版本互文。三 Cell 的 `WithEmitter` 应保持同等信息密度，建议对齐 accesscore 版本的内容深度。 |
| D5 | `cells/accesscore/cell_providers.go:1-10` | C3 | IN_SCOPE | 文件头注释存在（符合要求），但 split 动机 reference 写的是 "A5a-R5 / PR-A5c F4"，未给出可检索的 PR 编号或 plan 文档路径（读者无法从代码跳转到决策记录）。建议改为 `// This split mirrors PR-A5a (PR#234) cell.go physical split (docs/plans/202604232330-025-architecture-pr-implementation-plan.md §V-A15); A5a-R5 / PR-A5c F4 completes the provider extraction.` |
| D6 | `kernel/outbox/emitter.go:104-115` (`DurabilityReporter`) | C3 | IN_SCOPE | ref 注释指向 `kernel/cell.EmitterOutcome.Durable`（非对标开源框架）。CLAUDE.md 要求新建/重构 kernel/ 模块时标 "ref: {framework} {file}"。PR description 已在 Design references 列出 Watermill `disabledPublisher`，但 emitter.go 内的 `DurabilityReporter` godoc 未引用 Watermill `message/router.go`。建议在 godoc 末补 `// ref: github.com/ThreeDotsLabs/watermill message/router.go — disabledPublisher sentinel (mirrored as NewNoopEmitter).` |
| D7 | `runtime/eventbus/eventbus.go:573-582` (`unmarshalInboundEntry`) | C3 | IN_SCOPE | F5 将 `outboxrt.UnmarshalEnvelope` 迁移到 `outbox.UnmarshalEnvelope` 后，`unmarshalInboundEntry` 本身的注释（line 573 起）未更新引用来源说明。当前注释仅写 "decodes a v1 wire envelope from payload into an outbox.Entry"，无任何 ref。建议补 `// Delegates to outbox.UnmarshalEnvelope; envelope format defined in kernel/outbox/envelope.go.` |
| D8 | `docs/plans/202604232330-025-architecture-pr-implementation-plan.md:47-50` | C3 | IN_SCOPE | 第八轮更新块内容正确（PR-A5c ✅，commits，净工期，Wave 2 影响），但 PR 编号 "#245" 未加超链接（其他轮次也未加，属于全文一致风格，可选）。backlog.md 核销追加 "A5a-R4/R5 ✅ PR#245 (PR-A5c)" 位置和内容均正确。**无需修复，仅标注。** |
| D9 | plan.md 依赖图 / 风险矩阵 | C3 | IN_SCOPE | 全文检索 "A5c" 在 Wave 列表、推荐执行顺序节已标 ✅，风险矩阵节 A5c 行同步。无遗漏。 |
| D10 | PR description body | C1 | IN_SCOPE | 结构完整（Summary / Scope / Design references / Test plan / Risk & rollout / Out-of-scope），Refs 字段指向 plan 文件 + A5a-R4/R5 backlog 条目，Design references 含 4 条 framework ref。**PASS**。 |
| D11 | 文件命名规范 (本报告) | C3 | IN_SCOPE | 本报告路径 `docs/reviews/202604241700-pr245-doc.md` 符合 `yyyyMMddHHmm-编号-...` 格式。**PASS**。 |

---

## 关键发现摘要

**强制修复（C1/C2）：**

- D1：`Emitter` 接口入口缺 `DurabilityReporter` 交叉引用，影响可发现性。
- D2：`DirectPublishModeForDurability` 未说明 unknown mode fallback 行为边界，可能导致未来等级扩展时静默走错策略。
- D3：`WithOutboxDeps` accumulative nil 语义缺反例警示，易引发调用方误操作。

**建议改进（C3）：**

- D4：auditcore `WithEmitter` godoc 深度不一致。
- D5：cell_providers.go 文件头 ref 不可检索。
- D6：DurabilityReporter godoc 缺 Watermill framework ref（CLAUDE.md 合规性）。
- D7：unmarshalInboundEntry 注释未反映 F5 迁移后的新引用路径。

---

## 整体结论

**REQUEST_CHANGES**

C2 级 3 项（D1/D2/D3）需修复后 approve：Emitter 接口可发现性缺口、unknown mode 行为边界未声明、accumulative nil 反例缺失，都属于 kernel/ 公开 API godoc 应覆盖的契约范围。C3 项（D4-D7）建议同批处理以保持三 Cell godoc 对等性和 CLAUDE.md ref 合规，修复成本极低（各 1-2 行注释），建议纳入本 PR。
