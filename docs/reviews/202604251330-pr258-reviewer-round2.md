# PR #258 PR-A14b Round 2 Review

**Merge commit**: `f9d0edc9` | **Branch**: `526-pr-a14b-route-group` | **Base**: post-PR-A29 develop | 2026-04-25

## 范围

post-rebase + 彻底 Policy refactor 的 review。覆盖：merge conflict resolution 质量、cell.Policy struct 设计、health fallback 删除、shutGrace plumb、PolicyVerboseToken 语义、FinalizeAuth Warn、Round-1 findings 保留状态、PR-A29 整合质量、覆盖率与 lint。

## Findings 汇总

| ID | Cx | Scope | 一句话 |
|----|----|-------|------|
| **R2-01** | Cx1 | IN_SCOPE | `WithVerboseToken` 只发 warn 不返 401，未自动装 `PolicyVerboseToken` 401 前置闸门 |
| **R2-02** | Cx2 | IN_SCOPE | ~15 个测试用主端口直接探 `/healthz`，未走 `waitForHealthy` helper，HealthListener fallback 切换会静默失效 |
| **R2-03** | Cx1 | IN_SCOPE | `shutdownAllServers` shutGrace 用 `context.Background()`，全局 shutdown 截止时间不上限—— shutGrace > shutdownTimeout 时阻塞 |
| **R2-04** | Cx1 | IN_SCOPE | `policyVerboseActive` 与 `readyzVerbose` 完全重复，跨包独立维护 |
| **R2-05** | Cx1 | IN_SCOPE | `TestNew_WithTracer` 用 `Len==2` 脆弱断言，不校验具体 option 类型 |
| **R2-06** | Cx1 | IN_SCOPE | `shutGrace=0` 继承全局超时路径无单测 |
| **R2-07** | Cx1 | IN_SCOPE | `phase5CollectRouteGroups` 的 health → primary fallback remap 无直接单测 |
| **R2-08** | Cx1 | IN_SCOPE | health stuck-checker timeout 路径无 timing 测试 |
| **R2-09** | Cx1 | IN_SCOPE | `Bootstrap` struct 30+ 字段平铺，缺分组注释 |
| **R2-10** | Cx1 | IN_SCOPE | `listener-topology.md` 漏写 health-on-primary fallback（测试便利） |
| **R2-11** | Cx1 | IN_SCOPE | `warnNoAuthVerifier` 每次启动都对 HealthListener 误报警，制造 alert fatigue |

## RESOLVED（无需修）

- **R2-12** WithTracer 自动 probe-filter 确认接通，`TestBootstrap_TracingE2E_InfraEndpoints` 通过
- **R2-13** `shutGrace` 已写入 `boundServer` 并在 `shutdownAllServers` 消费
- **R2-14** ARCH-05 双 JWT guard 用 `cfg.policy.Name == "jwt"` 字符串匹配（struct-native，无 type-assert）
- **R2-15** CORR-01 FinalizeAuth 已对所有 listener 调用（但触发 R2-11）
- **PR-A29 整合**: opaque refresh token 完整保留 — `cells/accesscore/refresh_gc.go`、`POST /api/v1/access/sessions/refresh` 路由、`cmd/corebundle/auth_integration_test.go::POST_refresh_bypasses_auth` 全部就位
- **Round-1 修复全部保留**: CORR-02 dup-ref check、OPS-01 named shutdown log、OPS-02 cell-ID error wrap、LAYER-07/08、所有 doc 更新

## 关键 P1 项

**R2-03**（运行时 bug）：`shutdownAllServers` 在 shutGrace > 0 时使用：
```go
shutCtx, cancel = context.WithTimeout(context.Background(), bs.shutGrace)
```
应为 `context.WithTimeout(ctx, bs.shutGrace)`，让全局 shutdownTimeout 作为上限。

**R2-11**（运维噪声）：`FinalizeAuth` 对每个 listener 调用，HealthListener 永远没有 JWT verifier + 永远有 `Public:true` 路由，`warnNoAuthVerifier` 每次生产启动都喷 Warn。需要把 HealthListener 从 warn 中排除。

## 总结

无 P0 blocker，但 R2-03（shutGrace 上限不强制）和 R2-11（HealthListener 误报警）是真实运维痛点。其余 9 项是质量/可维护性改进。

PR-A29 与 PR-A14b 的合并质量好，无遗漏；Round-1 全部保留。
