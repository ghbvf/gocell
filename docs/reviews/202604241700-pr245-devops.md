# PR #245 DevOps Review — refactor/520-pr-a5c-outbox-emitter-unify

审查时间: 2026-04-24  
审查人角色: DevOps  
分支: `refactor/520-pr-a5c-outbox-emitter-unify` @ HEAD `3447eb9d`

---

## Finding 表

| # | 文件:行号 | 级别 | 范围 | 说明 | 修复建议 |
|---|-----------|------|------|------|----------|
| F1 | `tools/archtest/outbox_cell_test.go:57` | C2 | IN_SCOPE | **OUTBOX-CELL-01 未在 CI 中显式启用。** `_build-lint.yml` 的 `go test ./...` 覆盖 `tools/archtest/...`（无 build tag 隔离），但没有单独步骤确认该包被包含。如果 `go test` 的包发现出错（例如 `go list` 依赖缺失），archtest 会静默跳过而 CI 不会失败。| 在 `build-test` job 增加步骤 `go test -v ./tools/archtest/...`，保证 OUTBOX-CELL-01 有单独可见的通过/失败信号。 |
| F2 | `cmd/corebundle/audit_module.go:60` | C2 | IN_SCOPE | **auditcore 生产配线永远走 memory 分支（WithOutboxDeps(pub, nil)）。** `audit_module.go` 无论 `StorageBackend` 是 postgres 还是 memory 都调用 `WithOutboxDeps(shared.EventBus, nil)`，DirectPublishModeForDurability 返回 FailOpen，生产 DurabilityDurable 模式下 auditcore 没有真实 outboxWriter。`CheckNotNoop` 对 publisher-only 路径在 durable 模式下不会报错（ResolveEmitter demo 分支），导致 audit 事件在 postgres 生产模式下仍走 DirectEmitter(FailOpen) 而非 WriterEmitter。| 在 `audit_module.go` 补充 postgres 分支，类似 `buildConfigCoreOpts`：postgres 时传入真实 `outboxWriter`；或 PR 描述中明确声明 auditcore 在生产是有意 demo-mode（需要对应 slice.yaml 一致性等级降级说明）。 |
| F3 | `cells/accesscore/cell_init.go:56-62` | C3 | IN_SCOPE | **accesscore DirectPublishModeForDurability 两侧都是 FailOpen。** durable 模式下 session revocation 事件用 DirectEmitter(FailOpen) 意味着 broker 故障时事件静默丢失，没有告警或降级记录（slog.Warn 只在 fail-open 路径触发，但该路径被设计为非报错）。可观测性规范要求 Warn 级别的结构化日志覆盖降级运行。| 确认这是有意设计（PR 描述中明确），或者在 accesscore durable 路径改用 FailClosed，与 configcore 保持对称。当前 slog.Warn 行（emitter.go:93-96）结构化 KV 仅含 `topic` + `error`，缺少 `cell` 字段，与可观测性规范不符。 |
| F4 | `cmd/corebundle/bundle.go:193` | C4 | IN_SCOPE | **buildConfigCoreOpts PG 分支 option 顺序语义等价性已验证。** `WithPostgresPool` + `WithOutboxDeps(pub, outboxWriter)` 与原 `WithPostgresDefaults` 的语义等价：options 均在 `Init()` 前 apply，`resolveEmitter` 在 `Init()` 中统一消费，无顺序依赖。无问题，仅记录。 | 无需修改，记录审查结论。 |
| F5 | `kernel/outbox/emitter.go:91-96` | C3 | IN_SCOPE | **DirectEmitter fail-open 的 slog.Warn 缺少 `cell` 字段。** 根据可观测性规范，降级日志须有关联业务字段。`DirectEmitter` 不持有 cellID，导致生产 Warn 日志无法关联到具体 Cell。| 在 `NewDirectEmitter` 增加可选 `cellID string` 字段，或通过 context key 传入，Warn 日志补充 `slog.String("cell", e.cellID)`。 |
| F6 | 回滚分析（全 PR） | C3 | IN_SCOPE | **F5（envelope.go 删除）revert 复杂度高。** `runtime/outbox/envelope.go` 已删除（本 PR 将其移入 `kernel/outbox/envelope.go`）。若需单独回滚 F5，需恢复 envelope.go 文件并修改所有 consumer 的 import 路径，涉及 adapters/postgres、examples、cmd 等多处，不可单步 `git revert`。PR 描述未说明回滚顺序。| 在 PR 描述补充回滚顺序说明：F2→F3→F7 可单步 revert；F5 需手工恢复 envelope.go + 全局 import 修复。 |
| F7 | `WithEmitter(e)` 安全注入路径 | C3 | IN_SCOPE | **WithEmitter 允许注入任意 Emitter，mock 可绕过 durable 保证。** 测试或错误配线时可注入 `NewNoopEmitter()`，其 `Durable()` 返回 false，`CheckNotNoop` 不被调用（WithEmitter 路径跳过 ResolveEmitter）。生产 DurabilityDurable 模式下 `CheckNotNoop` 对 WithEmitter 路径没有护栏。| 在 `WithEmitter` 路径补充 `cell.DurabilityMode` 检查：durable 模式下对 `ReportDurable(e)==false` 的注入发出 Warn 或 fail-fast（与 CheckNotNoop 语义对齐）。当前设计是 safe-default-false，但缺少生产告警。 |

---

## CI 流水线评估

- `go test -race ./... -count=1` 覆盖 `tools/archtest/...`，OUTBOX-CELL-01 会随 `go test ./...` 自动执行，但没有独立可见步骤（F1）。
- integration-test job 包含 `cmd/corebundle/...`，`auth_integration_test.go` 和 `outbox_e2e_integration_test.go` 均已迁移到新 `WithOutboxDeps` API，e2e 验证覆盖新配线。
- `go build -tags=integration ./...` 构建范围覆盖 `cmd/corebundle`，新 Option 配线编译期已验证。
- lint-mode: pr 使用 `--new-from-merge-base`，本 PR 改动面（cells/ + kernel/ + cmd/）均在 diff 内，行号偏移问题不影响 new-issue 过滤。

---

## 整体结论

**REQUEST_CHANGES**

关键项：F2（auditcore 生产 postgres 路径缺失真实 outboxWriter，导致 L2 事件降级为 FailOpen DirectEmitter）和 F7（WithEmitter 注入路径在 durable 模式下缺少 durability 护栏）需要在合并前明确决策或修复。F3/F5 为可观测性合规问题，可在本 PR 内低成本修复。F1 为 CI 信号增强，建议但非阻塞。
