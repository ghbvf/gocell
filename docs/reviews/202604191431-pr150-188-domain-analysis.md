# PR#150–#188 全域分析报告

**统计区间**：PR#150 – PR#188（共 39 个 PR）  
**报告日期**：2026-04-19  
**当前开放 PR**：PR#187（跨 Auth/Router 双域）

---

## 总览

| 域 | PR 数量 | 状态 |
|----|---------|------|
| Auth | 10 | 9 ✅ / 1 🔄 |
| Config-core | 5 | 5 ✅ |
| Kernel | 2 | 2 ✅ |
| Outbox/RabbitMQ | 8 | 8 ✅ |
| Observability | 2 | 2 ✅ |
| HTTP/Router | 4 | 3 ✅ / 1 🔄 |
| CI/Test | 4 | 4 ✅ |
| Pkg/Query/CMD | 4 | 4 ✅ |
| Distlock/跨层重构 | 1 | 1 ✅ |
| **合计** | **40** | **38 ✅ / 2 🔄** |

> PR#187 同时横跨 Auth 和 Router 两域，计入双域统计，PR 实体数为 39。

---

## 域 1 — Auth

**主线**：从认证加固 → JWT 信任边界 → 权限治理模型完善，沿 P0→P1 路线纵向推进。

| PR | 描述 |
|----|------|
| #151 | access-core 安全加固 + READYZ |
| #159 | Injectable slog/clock/nonce/metrics（PR-R-AUTH） |
| #162 | 移除 2-part service token 兼容 |
| #166 | P0 JWT intent enforcement（access vs refresh） |
| #170 | aud claim 强制校验（RFC 8725 §3.3） |
| #171 | #170 follow-up — fail-fast audience + HTTP route tests + S9 obs |
| #172 | AUTH-DX-01 — sso-bff curl 修正 + random seed admin |
| #183 | AUTH-SETUP-01 — first-run admin bootstrap（P1-12 核心线） |
| #185 | Batch A — JWT 信任边界 + controlplane guard |
| #187 | 🔄 Policy/Guard 抽象 + (nil,nil) 根因修复（S12+S3+S35） |

**关键里程碑**：
- #166/#170 完成 JWT 双向信任边界（intent + audience），满足 RFC 8725 §3.3
- #183 完成 first-run admin bootstrap，打通 P1-12 核心线
- #187（进行中）引入 Policy/Guard 抽象层，修复 (nil,nil) 权限漏洞根因

---

## 域 2 — Config-core

**主线**：配置写操作权限收敛 + 存储层 PG 化 + 死代码清理。

| PR | 描述 |
|----|------|
| #155 | rollback contract + publish redaction（PR-H2） |
| #168 | configwrite 写操作收归 admin role |
| #169 | PG pilot — session repo + migration 004 + tx ctx key（PR-C1） |
| #175 | 移除 configpublish 死代码 fail-open runMode |
| #181 | PR-CONFIG-POLISH — S2 contract 401/403 + S13 4xx obs |

**关键里程碑**：
- #168 完成写操作 RBAC 收敛，配合 Auth 域 JWT 信任边界形成闭环
- #169 完成 session 存储层 PG 化（migration 004），脱离内存依赖

---

## 域 3 — Kernel

**主线**：运行时诊断能力与生命周期治理加固。

| PR | 描述 |
|----|------|
| #152 | validator 诊断位置 — file:line:column |
| #154 | hook 生命周期超时 + observer + outbox metadata DX |

**关键里程碑**：
- #152 validator 错误精确到列，DX 大幅提升
- #154 hook 超时保护上线，防止生命周期死锁

---

## 域 4 — Outbox/RabbitMQ

**主线**：从 broker conformance → relay 可靠性 → Subscription 一等公民 → 生产就绪，最密集的交付域。

| PR | 描述 |
|----|------|
| #150 | rabbitmq conformance 每 subtest 独立 Connection |
| #174 | PR-OUTBOX-WIRE — relay + broker health + atomic metrics |
| #176 | S8 RBAC-OUTBOX-MIGRATION — role 变更事务性 outbox |
| #177 | S30 — Store 接口提升至 runtime/outbox + S28 envelope 共享 |
| #180 | A5 entry.ID guard + A6 polish + X6 lease renewal |
| #184 | Subscription 一等公民 + lease hard fence + explicit ready |
| #186 | P1-14 envelope fail-closed + eventbus test sleep 清理 |
| #188 | P1-15 relay failure budget + readiness probe |

**关键里程碑**：
- #177 将 Store 接口提升至 runtime/outbox，完成分层边界治理（S30）
- #184 Subscription 成为一等公民，lease hard fence 防止双重消费
- #188 relay failure budget + readiness probe，outbox 达到生产就绪门槛（P1-15）

---

## 域 5 — Observability

**主线**：测试确定性 + 指标体系 provider 中立化。

| PR | 描述 |
|----|------|
| #156 | test determinism + cursor log（CONFORMANCE-SLEEP-01 + OBS-TABLE-01） |
| #157 | provider-neutral metrics + async hook dispatcher + pool statter |

**关键里程碑**：
- #157 metrics 层 provider 中立，支持 Prometheus/OpenTelemetry 双导出

---

## 域 6 — HTTP/Router

**主线**：信任边界收敛 + RunMode 显式化 + 方法级公开端点治理。

| PR | 描述 |
|----|------|
| #158 | 信任边界收敛 + config key filter + PR#133 C3 关闭 |
| #165 | explicit RunMode + fail-closed defaults（PR#160 follow-up） |
| #182 | P1-3 PUBLIC-ENDPOINT-METHOD-MATCH-01 — 方法感知公开端点 |
| #187 | 🔄 S3 router 相关（含 Auth 域 S12+S35，跨域 PR） |

**关键里程碑**：
- #165 RunMode 显式化，消除 fail-open 默认行为
- #182 公开端点从路径级收窄到方法级，防止越权访问（P1-3）

---

## 域 7 — CI/Test

**主线**：CI 提速 + flake 清零 + schema/migration 集成验证。

| PR | 描述 |
|----|------|
| #153 | CI 并行化 + SonarCloud 拆分 + rabbitmq testcontainer 复用 |
| #161 | rabbitmq test 确定性 + hook duration 度量 + gocognit 15 |
| #173 | schema guard 加固 + online migrations + relay 集成测试 |
| #179 | watcher + outboxtest 两处 CI flake 修复 |

**关键里程碑**：
- #153 testcontainer 复用显著降低 CI 耗时
- #173 在线 migration 测试闭环，relay 集成测试上线

---

## 域 8 — Pkg/Query/CMD

**主线**：通用工具抽象 + cursor 稳定性 + CLI 工具链完善。

| PR | 描述 |
|----|------|
| #160 | generic cursor helper + log 收敛 + prod guard |
| #163 | circuitbreaker ISP 拆分 + gobreaker 类型封装（CB-IFACE-01） |
| #164 | PR-CMD — app 包提取 + fail-fast/dry-run + bin/ 输出 |
| #167 | cursor stability + key rotation 接线（PR-P-QUERY） |

**关键里程碑**：
- #163 circuitbreaker 完成 ISP 拆分，依赖方只引用所需接口
- #167 key rotation 接线完成，cursor 稳定性验证通过

---

## 域 9 — Distlock/跨层重构

| PR | 描述 |
|----|------|
| #178 | Locker contract 提升至 runtime/distlock（X8） |

**关键里程碑**：
- #178 分布式锁接口上移至 runtime 层，完成跨层依赖反转（X8）

---

## 待办

| 事项 | 关联 |
|------|------|
| PR#187 合并 | S12 + S3 + S35 三条 backlog 项收尾 |
| Policy/Guard 抽象验收 | Auth 域 P1 后续 |

---

*生成来源：PR#150–#188 合并记录人工汇总*
