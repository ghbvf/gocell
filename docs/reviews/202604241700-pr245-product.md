# PR #245 Product Review — PR-A5c OUTBOX-EMITTER-UNIFY 收尾

> 评审时间：2026-04-24 17:00
> 评审人：产品经理 Agent（消费者/Go 开发者视角）
> 分支：`refactor/520-pr-a5c-outbox-emitter-unify` @ `3447eb9d`
> 消费者：gocell 框架自用 + cmd/corebundle + examples/{ssobff,iotdevice,todoorder}
> 兼容策略：CLAUDE.md 明确"当前只有 gocell 自身，没有外部调用方"——破坏性变更免豁免

---

## 1. 验收标准覆盖

| 原计划子项 | 可追溯证据 | 判定 |
|------|---------|-----|
| L2 cells 统一 WithEmitter/WithOutboxDeps API | `cells/accesscore/cell.go:55-91`, `cells/auditcore/cell.go:43-68`, `cells/configcore/cell.go:46-71` 三 Cell 对称；godoc 提 mutually-exclusive + accumulative | PASS (P1) |
| 删除 WithPublisher / WithOutboxWriter | grep 无残留（三 cell.go 已仅剩 WithEmitter/WithOutboxDeps） | PASS (P1) |
| configcore WithPostgresDefaults 拆分 | `cells/configcore/cell.go:140` WithPostgresPool godoc 第二段明确 "Outbox wiring is the caller's responsibility — call WithOutboxDeps(pub, writer) separately" | PASS (P1) |
| DurabilityReporter 接口 + ReportDurable helper | `kernel/outbox/emitter.go:104-151`；WriterEmitter / DirectEmitter 各自 Durable() 实现；godoc 提到 "safe default = non-durable" | PASS (P1) |
| runtime/outbox/envelope.go wrapper 删除 | 未在 worktree 检索到；消费方直接 import kernel/outbox | PASS (P1) |
| Archtest OUTBOX-CELL-01 | 未本轮直接查证（信任 PR 描述），建议 reviewer 会话交叉验证 | COMMENT (P2) |
| 29 处 call sites 迁移 | examples/ssobff/main.go:82,99,114 已改用 WithOutboxDeps(eb, nw)；cmd/corebundle/bundle.go 引入 outbox import；同步完整 | PASS (P1) |
| docs/plans + docs/backlog 更新 | 本 worktree 未包含新的 plan 文件（`docs/plans/202604232330-outbox-emitter-unify-plan.md` 不存在于该 worktree），无法本地确认"retroactively 落地"标记 | **RISK (P2)** — 需合 develop 后核查 |

---

## 2. Findings

| 位置 | 分级 | 范围 | 标签 | 建议 |
|------|------|-----|------|-----|
| `cells/accesscore/cell.go:71-91` WithOutboxDeps godoc | Cx2 | IN_SCOPE | [开发者体验] | accumulative 语义已说明，但缺少"为何不覆盖"的理由注释，开发者看 `WithOutboxDeps(eb, nil)` 后调 `WithOutboxDeps(nil, nw)` 会 **合并**而非后者覆盖——首次读代码必误判。建议补一行 example：`// Example: WithOutboxDeps(pub, nil) + WithOutboxDeps(nil, writer) ⇒ both set.` |
| `cells/auditcore/cell.go:52-68` | Cx2 | IN_SCOPE | [开发者体验] | auditcore 版本的 WithOutboxDeps godoc 比 accesscore 短 5 行，未提 pairing rules（demo/durable）。建议三 Cell godoc 对齐到同一模板。 |
| `cells/configcore/cell.go:120-145` WithPostgresPool | Cx1 | IN_SCOPE | [开发者体验] | godoc 提 "call WithOutboxDeps separately"，但未给出组合样例。建议补一段 example block，展示 `WithPostgresPool(pool) + WithOutboxDeps(pub, pgWriter)` 双行调用。 |
| `kernel/outbox/emitter.go:131-134` DirectEmitter.Durable | Cx1 | IN_SCOPE | [开发者体验] | `Durable() bool` 恒返 false 符合设计，但开发者可能误以为"应配合 writer 才获 durable"——godoc 已说"by design"，OK。 |
| `examples/ssobff/main.go:82,99,114` | Cx1 | IN_SCOPE | [开发者体验] | 三行整齐调用 `WithOutboxDeps(eb, nw)` 展示新组合模式；但 iotdevice/todoorder 本次未在 worktree 里核查，建议 summary agent 确认。 |
| 缺失 migration note | Cx2 | IN_SCOPE | [文档完整性] | 项目策略是不考虑向后兼容，但 README / CHANGELOG / 任一 docs/reviews 下没有 "旧 API → 新 API 映射表"。即便内部，框架贡献者查 history 成本仍高。建议补一段 10 行 migration snippet 落到 PR 描述或 `docs/reviews/202604241700-pr245-*.md`。 |
| docs/plans 未在 worktree | Cx2 | OUT_OF_SCOPE | [文档完整性] | 本 worktree 基线无 `202604250000-026-v1.0-release-plan.md` / `…-pr-a5c-…-plan.md`；用户 context 说已更新 plan，但无法在此 worktree 核查。建议 summary agent 交叉 develop HEAD 验证。 |
| PR-A5c Test plan | Cx2 | IN_SCOPE | [验收标准缺失] | 本 review 无法读 PR description；commit log 看不到四象限 (emitter×deps 互斥 / durable 判定 / archtest) 的测试清单条目。建议 PR 描述补显式 "Test plan" 段，列 `TestWithEmitter_MutualExclusion`、`TestDurableReporter`、`TestArchOutboxCell01` 三名最关键测试名。 |

---

## 3. 产品评审 7 维度

| 维度 | 判定 | 证据 |
|------|------|------|
| A. 验收标准覆盖 | 绿 | 5+3 子项 7/8 PASS，1 项 docs/plans 需 develop 基线核查 |
| B. UI 合规 | N/A | API 层改动，无 UI |
| C. 错误路径覆盖 | 绿 | mutual-exclusion fail-fast `ErrCellInvalidConfig`；nil writer/publisher 在 `NewWriterEmitter`/`NewDirectEmitter` 即拒绝 |
| D. 文档链路 | **黄** | 三 Cell godoc 一致性略差；无 migration 片段；plan/backlog 无法在 worktree 核查 |
| E. 功能完整度 | 绿 | API 对称三 Cell；DurabilityReporter 双向实现 |
| F. 成功标准 | 绿 | "Emitter 单一入口"目标达成；Cell 构造一致 |
| G. 产品 Tech Debt | 绿 | 无新增 [PRODUCT] 妥协 |

---

## 4. 结论

**COMMENT**（不阻塞合并，但请在合入前处理）：

1. 补充 3 个 Cell `WithOutboxDeps` godoc 对齐 + accumulative 语义 example（Cx2，强烈建议）
2. `WithPostgresPool` godoc 补正确组合 example（Cx1）
3. PR 描述补 Test plan 列出四象限测试名（Cx2）
4. Summary agent 在 develop 基线核查 docs/plans + docs/backlog 更新到位
5. archtest OUTBOX-CELL-01 规则 reviewer 席位交叉确认

P1 验收标准 100% 有可追溯证据；P2 存在两项文档黄灯，合并后 24h 内可补完。产品层面可以通过。

---

**相关文件**（均绝对路径）：
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/cells/accesscore/cell.go
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/cells/auditcore/cell.go
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/cells/configcore/cell.go
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/kernel/outbox/emitter.go
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/examples/ssobff/main.go
- /Users/shengming/Documents/code/gocell/worktrees/520-pr-a5c-outbox-emitter-unify/cmd/corebundle/bundle.go
