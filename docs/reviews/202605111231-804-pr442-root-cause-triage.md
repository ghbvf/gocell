# PR #442 latest-develop root-cause triage

Baseline: `develop == origin/develop == 47cd9e018fdc56bc52296a1aa2b696f479a18034`.

Sources:

- `docs/reviews/202605111900-pr442-six-role/*.md`
- `docs/backlog.md`
- current source inspection on latest `develop`
- latest GitHub checks for `develop` and PR #453/#454/#455

Status vocabulary:

- **Resolved**: latest develop code and/or CI prove the finding is closed.
- **Backlog**: issue still exists or is intentionally deferred, and has an effective open backlog entry.
- **Gap**: not fixed and either not registered, registered only generically, or registered as done while code still contradicts it.

## Core 8 Findings

These are the eight findings from the previous consolidated answer, re-bucketed by root cause.

| Root cause | Total | Resolved | Backlog | Gap |
|---|---:|---:|---:|---:|
| Plan/write funnel not end-to-end atomic | 3 | 0 | 1 | 2 |
| Input/YAML/metadata contract not single-sourced | 3 | 0 | 2 | 1 |
| CI/enforcement follow-up | 2 | 2 | 0 | 0 |
| **Total** | **8** | **2** | **3** | **3** |

### Core Detail

| Finding | Root cause | Status | Evidence / backlog |
|---|---|---|---|
| `scaffold cell` dry-run/live mismatch and half-generated follow-up files | Plan/write funnel not end-to-end atomic | Backlog | `cmd/gocell/app/scaffold.go` still dry-runs the skeleton plan, while live later calls `autoGenerateCellBundleArtifacts`. Open backlog: `SCAFFOLD-CELL-BUNDLE-CROSS-STAGE-PLAN-MERGE-01`. |
| `pathsafe` parent-directory symlink TOCTOU | Plan/write funnel not end-to-end atomic | Gap | `WritePlannedFiles` still resolves containment before `mkdirAllTracked`/`writeFileNoFollow`; leaf is protected, parent directories are not handle-based. No effective backlog for parent symlink/openat. |
| `scaffold assembly` YAML raw scalar rendering | Input/YAML/metadata contract not single-sourced | Gap | `kernel/assembly/gentpl/scaffold-assembly-yaml.tpl` still renders `id/team/role/deployTemplate` as raw scalars. `SCAFFOLD-YAML-SAFETY-TYPED` is marked done but does not match this code path. |
| `synthesizeAssemblyMeta` omits `Build.Binary` | Input/YAML/metadata contract not single-sourced | Backlog | `kernel/assembly/generator.go` still says `Build.Binary intentionally omitted`. Open backlog: `ASSEMBLY-META-SYNTHESIS-FIELD-GUARD`. |
| PR #442 tool-shard/CI failure | CI/enforcement follow-up | Resolved | Latest develop checks are green; archtest is process-isolated and sharded. PR #453/#454/#455 checks also pass. |
| `--with-http --with-events` duplicate plan path | Plan/write funnel not end-to-end atomic | Gap | `resolveBundleVariants` enables both variants, but event artifact keeps `eventSliceID := sliceID` unless `WithBoth` is set. No duplicate-AbsPath plan guard or backlog found. |
| `SCAFFOLD-WRITE-FUNNEL-01` alias/hard-rating weakness | CI/enforcement follow-up | Resolved | Type-aware archtest and depguard hardening landed via PR558: `SCAFFOLD-WRITE-FUNNEL-DEPGUARD-01` / `SCAFFOLD-WRITE-FUNNEL-HARD-UPGRADE`. |
| Assembly ID validation does not reuse metadata assembly ID rules | Input/YAML/metadata contract not single-sourced | Backlog | Current CLI/kernel path uses path/control-char validation, not `metadata.MatchAssemblyID`. Generic backlog: `SCAFFOLD-INPUT-CONTRACT-TYPED-ID-01`; exact assembly FMT-A1 coverage should be made explicit. |

## Expanded Six-Role Triage

The six-role review included stale findings from an older tree and several follow-up items that were later fixed by PR557/PR558/PR559. After de-duplication, the expanded set is 26 items.

| Root cause | Total | Resolved | Backlog | Gap |
|---|---:|---:|---:|---:|
| Plan/write/filesystem semantics | 5 | 1 | 2 | 2 |
| Input/YAML/metadata contract drift | 6 | 2 | 2 | 2 |
| AI-rebust enforcement carrier mismatch | 5 | 3 | 2 | 0 |
| Architecture boundary / mutable state coupling | 4 | 1 | 3 | 0 |
| CI/DX/docs/backlog hygiene | 6 | 2 | 2 | 2 |
| **Total** | **26** | **9** | **11** | **6** |

### Root Cause Buckets

#### 1. Plan/write/filesystem semantics

Pattern: render/plan/execute is not a single coherent transaction everywhere, and `pathsafe` still mixes path-based prechecks with leaf-only nofollow writes.

| Item | Status |
|---|---|
| Leaf symlink + write funnel rollback basics | Resolved: `PATHSAFE-LEAF-SYMLINK-NOFOLLOW`, `SCAFFOLD-FAILURE-CLEANUP-RECOVERY`. |
| Cell bundle cross-stage plan merge | Backlog: `SCAFFOLD-CELL-BUNDLE-CROSS-STAGE-PLAN-MERGE-01`. |
| `collectMissingDirs` EACCES rollback gap | Backlog: `PATHSAFE-COLLECT-MISSING-DIRS-EACCES-01`. |
| Parent-directory symlink TOCTOU | Registered by this branch: `PATHSAFE-PARENT-SYMLINK-TOCTOU-01`. |
| `--with-http --with-events` duplicate path | Registered by this branch: `SCAFFOLD-BUNDLE-VARIANT-DUPLICATE-PATH-01`. |

#### 2. Input/YAML/metadata contract drift

Pattern: scaffold input validation, YAML scalar safety, and metadata synthesis are spread across cmd/cellgen/kernel instead of being represented as typed shared contracts.

| Item | Status |
|---|---|
| Contract YAML `codegen: true` cleanup | Resolved: `CONTRACT-YAML-CODEGEN-DEFAULT-CLEANUP` via PR559. |
| Cellgen owner team/role YAML whitelist | Resolved in latest develop with `ownerTeamPattern` / `ownerRolePattern`. |
| Assembly meta field synthesis guard | Backlog: `ASSEMBLY-META-SYNTHESIS-FIELD-GUARD`. |
| Shared scaffold typed ID validator | Backlog: `SCAFFOLD-INPUT-CONTRACT-TYPED-ID-01`. |
| Assembly YAML raw scalar rendering | Registered by this branch: `SCAFFOLD-ASSEMBLY-YAML-SCALAR-SAFETY-01`; `SCAFFOLD-YAML-SAFETY-TYPED` narrowed to cmd/cellgen. |
| Assembly ID exact metadata rule | Registered by this branch: `SCAFFOLD-ASSEMBLY-ID-METADATA-RULE-01`; generic typed-ID backlog now cross-links it. |

#### 3. AI-rebust enforcement carrier mismatch

Pattern: review found several places where a rule was described as Hard but implemented as string/AST convention. Most high-risk items have since been hardened.

| Item | Status |
|---|---|
| Write funnel depguard hardening | Resolved: `SCAFFOLD-WRITE-FUNNEL-DEPGUARD-01` / `SCAFFOLD-WRITE-FUNNEL-HARD-UPGRADE`. |
| Bundle marker/no-codegen literal ceiling | Resolved: `SCAFFOLD-BUNDLE-ARCHTEST-HARDEN`; PR558 records Medium ceiling decision. |
| Listener marker typed const cross-check | Resolved: `SCAFFOLD-LISTENER-MARKER-TYPED-CONST-01`. |
| Inline scaffold contract template regression guard | Backlog: `SCAFFOLD-INLINE-TEMPLATE-ARCHTEST`. |
| Cellgen errcode funnel hard path | Backlog: `CELLGEN-ERRCODE-FUNNEL-HARDEN`. |

#### 4. Architecture boundary / mutable state coupling

Pattern: assembly scaffold now has a better one-shot plan than before, but it does so by coupling `kernel/assembly` to `pathsafe.PlannedFile` and by temporarily mutating `ProjectMeta`.

| Item | Status |
|---|---|
| Assembly cross-stage plan merge | Resolved: `SCAFFOLD-ASSEMBLY-CROSS-STAGE-PLAN-MERGE-01`. |
| `PlanAssemblyScaffold` ownership/framing | Backlog: `ARCH-ASSEMBLY-SCAFFOLD-OWNERSHIP-01`. |
| `Generator` concurrent map mutation | Backlog: `ASSEMBLY-GENERATOR-CONCURRENCY-01`. |
| `synthesizeAssemblyMeta` field guard | Backlog: `ASSEMBLY-META-SYNTHESIS-FIELD-GUARD`. |

#### 5. CI/DX/docs/backlog hygiene

Pattern: several user-facing or verification claims were true only after later PRs, and a few docs/help/backlog entries still overclaim.

| Item | Status |
|---|---|
| Scaffold verify scripts in CI | Resolved: `_build-lint.yml` now runs bundle, assembly, and rejection gates. |
| Governance timeout cold-cache risk | Resolved: `governance.yml` is now 15 minutes and skips archtest as single-owned by `_build-lint.yml`. |
| Examples assembly cleanup | Backlog: `EXAMPLES-ASSEMBLY-MINIMAL-CLEANUP`. |
| `scaffold project` from-zero journey | Backlog: `SCAFFOLD-PROJECT-INIT-CMD`. |
| `pkg/pathsafe` missing from cross-platform `os-smoke` | Registered by this branch: `PATHSAFE-OS-SMOKE-COVERAGE-01`. |
| `gocell generate --help` still says `codegen: true` prerequisite | Registered by this branch: `GENERATE-HELP-CODEGEN-DEFAULT-DRIFT-01`. |

## Backlog Corrections Needed

These are not new feature decisions; they are bookkeeping corrections so backlog status matches latest code. This branch applies the corrections in `docs/backlog.md`.

| Backlog / gap | Correction |
|---|---|
| `SCAFFOLD-YAML-SAFETY-TYPED` | Narrowed to cmd/cellgen paths; assembly gap split to `SCAFFOLD-ASSEMBLY-YAML-SCALAR-SAFETY-01`. |
| `SCAFFOLD-DRY-RUN-COMPLETE-INVENTORY` | Narrowed to first-stage plans / assembly full plan; cell derived-file gap cross-linked to `SCAFFOLD-CELL-BUNDLE-CROSS-STAGE-PLAN-MERGE-01`. |
| `SCAFFOLD-INPUT-CONTRACT-TYPED-ID-01` | Updated to mention assembly ID metadata-rule drift and cross-link `SCAFFOLD-ASSEMBLY-ID-METADATA-RULE-01`. |
| `PATHSAFE-PARENT-SYMLINK-TOCTOU-01` | Registered. |
| `SCAFFOLD-BUNDLE-VARIANT-DUPLICATE-PATH-01` | Registered. |
| `PATHSAFE-OS-SMOKE-COVERAGE-01` | Registered. |
| `GENERATE-HELP-CODEGEN-DEFAULT-DRIFT-01` | Registered. |

## Recommended Fix Batches

1. **Batch A: input/YAML correctness**
   - Fix assembly YAML safe scalar rendering.
   - Make assembly ID validation explicit against metadata rules.
   - Tighten `SCAFFOLD-YAML-SAFETY-TYPED` and `SCAFFOLD-INPUT-CONTRACT-TYPED-ID-01` backlog wording.

2. **Batch B: filesystem atomicity**
   - Add duplicate `AbsPath` detection in `pathsafe.WritePlannedFiles`.
   - Normalize `WithHTTP+WithEvents` or give event artifacts a distinct slice ID whenever both variants are enabled.
   - Register and design parent-directory symlink/openat hardening.

3. **Batch C: backlog hygiene + low-risk DX**
   - Correct stale done claims.
   - Add `pkg/pathsafe/...` to `os-smoke`.
   - Fix `gocell generate --help` stale `codegen: true` prerequisite text.
