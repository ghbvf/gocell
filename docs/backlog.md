# GoCell Backlog

> 只含待办事项。已完成项归档至 `docs/reviews/archive/202604180035-backlog-pre-cleanup.md`。
> 更新日期: 2026-04-25（合并冲突解决：以 config 域 PR-CFG-1..7 重组版本为基线，并入 LOCAL `PR262-AUTH-POLICY-PLAN-01` typed plan 后续；反映 PR#262/#263/#264/#265 落地；**PR258-RES 残留复核**：RES-1/5/6 ✅ 已修 + RES-2 ✅ 已收口 + RES-3 ❌ won't-do（向后兼容文档） + RES-4 真实保留）。**P1 全量复核（2026-04-25，3 批 9 子 agent）**：(a) 关闭 ✅ 已修 6 条：V-A8/A10/A12/A14 + PR244-F8 + PR245-F5 + PR247-N3/N4；(b) 关闭 ❌ won't-do 3 条：V-A5（适用域错位）+ V-A9（RSA PEM 与 demo-key 黑名单不兼容）+ PR-CFG-5（rejectDemoKey 已含 real-mode 检查，real+合法 key 是设计意图）；(c) 修正描述 1 条：P1-8 `handleListRoles` 搭车项已用 `query.MapPageResult` 完成，删搭车；(d) 真根因更正 1 条：PR-CFG-1 真正缺失在 `PGResource.Checkers()` 未转发 relay probe（kernel 接口不需改），最小修 Cx1；(e) 降级 1 条：PR244-F1 核心已 fail-fast，残余仅 warn 日志降 P2。详细 PR 推进状态请看 `docs/plans/202604232330-025-architecture-pr-implementation-plan.md`。**PR#266 PR-CFG-B round-2 收口（2026-04-25）**：标 ✅ PR-CFG-2/PR-CFG-3/PR247-N5；ADV-05 语义优化为只检查 active（draft/deprecated 豁免，对标 K8s deprecation policy）；新增 governance 规则 SLICE-CONSISTENCY-01；删 domain→events type alias（F-ARCH-03）；example actor maxL 优化（external-audit-sink/example-order-platform L4→L2）；config consumer Info→Debug；CI 集成测试对齐 metadata-only。新登记 5 条 P2 OUT_OF_SCOPE：PR266-METRICS-CONFIG-EVENT / PR266-AUDITAPPEND-STRICT / PR266-NOOP-TX-RUNNER-DOC / PR266-CONTRACT-CLIENTS-ENFORCE / PR266-METADATA-ONLY-CONSUMER-BUSINESS。
> 基线: `develop @ 358878a1`（PR#258/#259/#260/#261/#262/#263/#264/#265 全部合入后；🎯 v1.0 发布硬约束已全清零，Wave 2 主线全部完工）
>
> **🔨 在飞 PR**：无（最近 8 个 PR 已合并至 develop）
>
> **最近合入主线**（按时间倒序）：PR#265 distlock TestManager_HeapOrder flaky 修复 / PR#264 fix(accesscore) harden first admin setup / PR#263 PR-A30 AUTH-TEST-COVERAGE / PR#262 PR-A14b round-3 reviewer findings (#1-#11) / PR#261 PR-A10 OUTPUT-JSON-SARIF / PR#260 PR-A20 DistLock context-derived lock / PR#259 PR-A31 AUTH-FIRSTRUN-DX / PR#258 PR-A14b three-listener + RouteGroup / PR#256 PR-A35 READYZ-POLISH / PR#251 PR-A29 AUTH-REFRESH-MAIN / PR#250 PR-A6 typed event payloads + Emit[T] / PR#252 PR-A12b command lifecycle / PR#249 PR-A12 kernel/command / PR#247 PR-A26 setup slice / PR#246 PR-A11 kernel/wrapper (+ FU1 PR#257) / PR#245 PR-A5c outbox emitter unify / PR#244 PR-A25 AUTH-PROD-HARDENING / PR#242 PR-A18 vault test split / PR#240 PR-A18 vault namespace + datakey / PR#239 PR-A9 CONTRACT-META-01 / PR#238 PR-A5b configcore split / PR#237 PR-A14a dual-listener / PR#235 PR-A13 docs clean / PR#234 PR-A5a lifecycle / PR#230 PR-A8 vault auth pluggable / PR#228 PR-A4 HTTP metrics autowire / PR#227 PR-A3 per-cell adapter / PR#225 PR-A2 pkg/validation
>
> **未导入外部 PR**：PR#129 Sentinel DSN redaction / PR#130 Bolt journey catalog
>
> 标记说明:
> 🟡 可延后 = 不卡正确性或安全；latent risk / DX / 测试覆盖 / 纯 tech debt / 供应链加固 / 架构打磨 — 可机会性纳入或 v1.0 后做
> 🟠 条件延后 = 有明确触发条件（如首次 prod migration / PG 接线），触发前可延
> 🔴 = 发布前必做硬约束（v1.0 区块已全清零；剩余 🔴 为多 pod / docs 阶段触发）

---

## P0 阻塞项

✅ **全清**（L0 / L1 已合入）

---

## P1 待办

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| ~~PR258-RES-1~~ | ~~**TEST-ENVELOPE-MIGRATION-01**~~ ✅ 验证已修：`runtime/bootstrap/bootstrap_test.go:107` 引入 `readyzPayload(t, body)` 帮助函数（被 14 处调用）统一消化 200 `{"data":...}`/503 `{"error":{"details":...}}`/401 `{"error":{...}}` 三种 envelope；全仓 grep 无任何直接读 `body["dependencies"]`/`body["cells"]` 的残留 | — | — | — |
| ~~PR258-RES-2~~ | ~~**DOCS-AUTH-MOUNT-UNIFICATION-01**~~ ✅ 已收口：`.claude/rules/gocell/runtime-api.md` + `docs/new-setting/runtime/CLAUDE.md` + `docs/ops/listener-topology.md` 已重写为 `auth.Mount`/`auth.Route` + 明确说"没有 `WithAuthDiscovery`/`WithAuthMiddleware`"；`examples/ssobff/README.md:92` 残留 `bootstrap.WithAuthDiscovery()` 已替换为 `PolicyJWTFromAssembly` 描述 | — | — | — |
| ~~PR258-RES-3~~ | ~~**DOCS-HTTPADDR-MIGRATION-01**~~ ❌ won't-do：本仓"不考虑向后兼容"（CLAUDE.md `Review 和重构时不考虑向后兼容——当前只有 gocell 自身，没有外部调用方`）。`GOCELL_HTTP_ADDR` 旧 → 新三 ADDR 的迁移块是为外部用户写的兼容文档；当前 `env-vars.md:126-128` 已登记新三变量 + `listener-topology.md` 已写完整拓扑，无需追加 breaking-change 章节 | — | — | — |
| PR258-RES-4 | **BIND-FAILURE-PORT-REBIND-ASSERT-01** (Cx2, 🟡 可延后): `runtime/bootstrap/dual_listener_test.go::TestTripleListener_MidBindFailure_RollsBackEarlierBindings` (lines 480-484) 注释自承"`We cannot inspect the exact addrs easily since :0 assigns ephemeral ports`"——只断言 Run 返回错误 + 不 hang，没有实证已绑端口被释放。**修复**：在 `WithListener(...)` 之前预绑一个 ln，把它的 `Addr().String()` 作为 primary 注入；rollback 后 `net.Listen("tcp", primaryAddr)` 必须立即成功 | 2h | `runtime/bootstrap/dual_listener_test.go` | PR #258 round-3 reviewer |
| ~~PR258-RES-5~~ | ~~**INTERNAL-PREFIX-EXEMPTION-NARROW-01**~~ ✅ PR#262 已修：`bootstrap_phases.go:39-46` 注释明示 "PR-258 RES-5: frameworkPrimaryWhitelist is gone"；引入 `router.InternalPrefixIsolationResponder()` early-responder middleware（`runtime/http/router/router.go:806-816`），原 `WithPolicyCoverageWhitelist` + `WithPublicPathPrefix` + chi-route 404 三件套已收敛为单一 predicate | — | — | — |
| ~~PR258-RES-6~~ | ~~**METRICS-TOKEN-HASHED-COMPARE-01**~~ ✅ PR#262 已修：`cmd/corebundle/metrics.go:32-36` `withMetricsTokenGuard` 已切换 `sha256.Sum256` → `subtle.ConstantTimeCompare` 固定 32 字节摘要，与 `runtime/http/health.verboseDecision` 路径一致 | — | — | — |
| ~~PR262-AUTH-POLICY-PLAN-01~~ | ~~**AUTH-POLICY-TYPED-PLAN-01**~~ ✅ PR#269 落地：sealed typed `AuthPlan` / `ListenerAuth` / `GroupAuth` 接口替代 `cell.Policy`，6 个 typed concrete plan（AuthNone/AuthJWT/AuthJWTFromAssembly/AuthMTLS/AuthServiceToken/AuthVerboseToken）+ bootstrap type-switch dispatcher（`runtime/bootstrap/auth_plan_apply.go`）+ phase0 typed validate（`auth_plan_validate.go`）+ AST archtest 兜底（`tools/archtest/auth_plan_test.go` AUTH-PLAN-01..04）；7 个 `bootstrap.Policy*` 工厂全删，`cell.Policy` 删除，调用方（cmd/corebundle、3 examples、所有 fixture）已迁移。**PR#269 round-2** 收口三 finding：F1 `/readyz?verbose` 401 envelope 统一到 `httputil.WritePublicError(ErrReadyzVerboseDenied)` 与 health handler 同契约；F2a `./tools/...` 纳入 PR CI build-test others shard；F2b segregation fixture 改为 `go build -tags=segregation_check` 真 type-check（mutation-tested，加 marker 立刻 FAIL）；F3 CHANGELOG 清理 PR-A14a 残留迁移块（`WithPrimaryListener`/`WithInternalListener`/`WithInternalMiddleware` 早在 PR-A14b/#258 已删但 changelog 未同步） | — | — | — |
| PR-CFG-1 | **READYZ-RELAY-PROBE-FORWARD-01** (P1, Cx1, 🔴 治理基线 — 范围已收窄): **2026-04-25 复核更正**：`bootstrap.expandManagedResources()` (`runtime/bootstrap/managed_resource.go:63-88`) 已实现"`ManagedResource.Checkers()` 自动聚合到 readyz"——治理约束**已存在**，不需要改 kernel 接口。真问题在 `adapters/postgres/pool_resource.go:71-83`：`PGResource.Checkers()` 仅返回 `"postgres"` pool ping，未把 relay 的 `outbox-relay-poll/reclaim/cleanup` 三探针转发出去。relay 失败时 readyz 仍绿仍是真实的运维盲区。**最小修**：`PGResource.Checkers()` 在返回 map 中合并 relay 的 `HealthCheckers()`（按 `relayHealthChecker` 接口或 `*runtimeoutbox.Relay` 类型断言）；`pool_resource_test.go` 加正向断言。**搭车（可选 Cx2）**：archtest 加"adapter 暴露 HealthCheckers 但未桥接到 ManagedResource.Checkers" 防回退；可拆独立 PR。**配套源 finding**：2026-04-25 config 域审查 P1-6 + 2026-04-25 第二轮复核（reviewer subagent）。 | 2h（最小修）+ 2h（archtest 可选） | `adapters/postgres/pool_resource.go` + `pool_resource_test.go` + 可选 `tools/archtest/managed_resource_test.go`（新） | 2026-04-25 config 域审查 P1-6 + 复核修正 |
| ~~PR-CFG-2~~ | ~~**EVENT-DEAD-SUBSCRIBER-VALIDATE-01 + FLAG-CHANGED-RETIRE**~~ ✅ PR#266 已落地：(1) 治理 ADV-05 在 `kernel/governance/rules_advisory.go::validateADV05` 实施（round-2 P1-2 fix 后语义收敛为只检查 `lifecycle: active`，draft/deprecated 豁免，对标 K8s API deprecation policy + Watermill router lifecycle，比原计划的"draft 也强校验"更优雅）；(2) `flagwrite/service.go` 删 emit + L2→L1 降级；`flag.changed.v1` contract 标 `lifecycle: deprecated`；额外新增 archtest **LAYER-09** 禁止 cells/X import cells/Y/events（治理基线扩展） | — | — | — |
| ~~PR-CFG-3~~ | ~~**EVENT-CONTRACT-BOUNDARY-RECOMPLY-01 (激进方案)**~~ ✅ PR#266 三件同 PR 全部落地：(1) **B1b** entry-upserted payload 删 value 字段改 metadata-only（subscriber 必须 GET 回源），producer/subscriber 占位符路径删除；(2) **B1c** accesscore 切断对 configcore/events 的 import，新建 `cells/accesscore/internal/dto/config_event_decoder.go` 本地 decode；configcore/events 从 export 收回到 `cells/configcore/internal/events`；(3) **G1** `tools/archtest/archtest_test.go` 新增 LAYER-09 规则禁止 cells/X import cells/Y/events。round-2 follow-up: 删 `cells/configcore/internal/domain/config_events.go` 中 EntryUpserted/EntryDeleted 的 type alias，生产侧直接 import internal/events（彻底清除反向依赖，F-ARCH-03）；CI fix: `cmd/corebundle/outbox_e2e_integration_test.go` 同步删 entryPayload.Value 字段与断言 | — | — | — |
| PR-CFG-4 | **CONFIG-READ-METADATA-ADMIN-GATE-01** (P1, Cx2, 🔴 安全数据泄漏 — batch2 业务修): `cells/configcore/cell_routes.go:67-78` 把 `GET /api/v1/config/` 与 `GET /api/v1/config/{key}` 挂在 `auth.Authenticated()` 下，任意已登录用户都能枚举 key 列表 + 读到 sensitive 元数据（值已脱敏，但"有哪些敏感配置、叫什么名字"本身就是侦察面）。同时 `contracts/http/config/{get,list}/v1/response.schema.json` 既未声明 `sensitive: bool` 字段也未在 contract test 锁形状，但真实 DTO 已经把该字段写进响应——contract-first 调用方会把线上合法响应当成非法。**根因**：把"值脱敏"等同于"资源可公开读"，没把 key + sensitive 视为敏感元数据；DTO 升级时 contract schema 没跟（流程缺工具校验 DTO ↔ schema 同步）。**修复（同 PR）**：(1) `cells/configcore/cell_routes.go` 两条 GET 改 `auth.AnyRole(auth.RoleAdmin)`；(2) `contracts/http/config/{get,list}/v1/response.schema.json` 显式声明 `sensitive: bool`；(3) `cells/configcore/slices/configread/{handler,service}.go` + DTO 校齐；(4) contract_test 加 `MustRejectResponse` 负向断言。**对标**：Vault KV v2 `data` vs `metadata` 路径分离授权；K8s RBAC ConfigMap/Secret 默认不进 `view`；Consul KV 把 keys 枚举与 metadata/value 读取分离。**配套源 finding**：2026-04-25 config 域审查 P1-1 + P1-2（安全 + 产品席位）。 | 1d | `cells/configcore/cell_routes.go` + `contracts/http/config/get/v1/response.schema.json` + `contracts/http/config/list/v1/response.schema.json` + `cells/configcore/slices/configread/handler.go` + `cells/configcore/slices/configread/service.go` + `cells/configcore/slices/configread/contract_test.go` | 2026-04-25 config 域全链路审查 P1-1 + P1-2 |
| ~~PR-CFG-5~~ | ~~**KEYPROVIDER-LOCALAES-REAL-MODE-REJECT-01**~~ ❌ won't-do（误判，2026-04-25 复核关闭）：`cmd/corebundle/demo_keys.go:58-68` `rejectDemoKey` **首行即 `if !isRealMode(adapterMode) { return nil }`**——dev 模式 no-op，real 模式才比对 `wellKnownDemoKeys`；当前实现的语义是"real 模式拒绝公开 demo 值"而非"real 模式拒绝 local-aes 整个 provider"。real 模式 + 合法随机 32 字节 hex key 启动 local-aes 是**设计意图**（项目从未承诺 real 模式硬禁 local-aes），不是漏洞。如果未来 ADR 明确"生产硬性要求 vault-transit"，再以独立 P2 加固项重新登记。**复核来源**：2026-04-25 reviewer subagent | — | — | — |
| PR-CFG-6 | **OUTBOX-EMIT-FAILOPEN-DROP-COUNTER-01** (P1, Cx2, 🟡 ops 可观测 — batch2，原 MULTI-REVIEW-RES-2 残余): PR-CFG-1 落地后 `Relay.HealthCheckers()` 已自动接入 readyz，**MULTI-REVIEW-RES-2 的 readyz 降级部分已解决**；本条剩余两件：(a) `kernel/outbox/emitter.go:101-108` `DirectPublishFailOpen` 分支仅 `slog.Warn` + `return nil`，**无任何 metrics counter 自增**（复核已确认）；(b) `tools/archtest/outbox_topic_test.go:217-237` `extractStringField` 检查 `*ast.BasicLit`，遇到 `*ast.Ident`（常量引用）/`*ast.SelectorExpr` 时直接 `ok=false` 跳过——常量/变量构造的 topic 漏检（复核已确认）。**修复**：(1) `DirectEmitter` 构造时注入 metrics provider（避免在 kernel/ 引入全局注册表），fail-open 分支自增 `gocell_outbox_emit_failopen_dropped_total{cell, topic}`；(2) archtest 同包内构建常量值表（`ast.GenDecl` + `ast.ValueSpec` 扫一遍同文件 const）做单文件级值传播，参考 PR#257 PR246-FU1 FMT-19 AST 模式。**工时调整**：原估 2h 偏低，复核建议 3-4h（含两件拆分提交）。**关联**：原 MULTI-REVIEW-RES-2 同时登记，本条与之合并实施。 | 3-4h | `kernel/outbox/emitter.go` + `runtime/observability/metrics/` + `tools/archtest/outbox_topic_test.go` | 2026-04-25 config 域审查 P1-6 衍生 + 复核确认 |
| VERIFY-JSON-OUTPUT-01 | **VERIFY-JSON-OUTPUT-01** (Cx2, 🟡 可延后, 关联 P1-4 ✅ PR#261)：`gocell verify {slice|cell|journey|targets}` 当前只输出文本 PASS/FAIL；CI 想机器消费 verify outcome 没渠道。`VerifyResult{TargetID, Passed, Results, Errors, ManualPending}` 数据形态独立于 `ValidationResult`（test-execution 而非 finding），不能挤进 PR-A10 的 `Printer`。**修复**：在 `cmd/gocell/app/printers/` 增加 `verify.go`（独立 `VerifyPrinter` interface + 私有 DTO），`gocell verify` 加 `--format text\|json`，SARIF 不适用此场景。 | 3h | `cmd/gocell/app/printers/verify.go`（新）+ `cmd/gocell/app/verify.go` | PR-A10 user-aligned scope cut（2026-04-25） |
| CHECK-DIAGNOSTIC-LOCATOR-01 | **CHECK-DIAGNOSTIC-LOCATOR-01** (Cx2-Cx3, 🟡 可延后, 关联 P1-4 ✅ PR#261)：`cmd/gocell/app/check.go::contractHealthResult` 在 app 层手工 struct-literal 构造 `ValidationResult`，绕过 `kernel/governance/locator.go::newResult`，结果只填 `File`、不填 `Line/Column`。`gocell validate` 路径能给字段级坐标（YAML node 行/列），`gocell check contract-health` 退化到文件级；后续每加一个 `check` 子命令都会复制这个反模式。**根因**：PR-A10 把"统一诊断"做到了 printer 层，没下沉到结果对象生产层。**修复方向**：(a) 把 `governance.Locator` 暴露成可在 cmd 层使用的 builder，cmd 层读取 contract.yaml 的 yaml.Node 后用 locator 拼字段位置；或 (b) 把 `validateContractHealth` 整体下沉到 `kernel/governance/`（与 `validateContractHealth` 已经只读 `ContractMeta` + `metadata.ProjectMeta` 一致），cmd 层只调用。建议走 (b) 方向：减少跨层 API 暴露，复用现有 `Validator.locator` 实例。同时检查未来 `check {slice-coverage,assembly-completeness,journey-readiness,l0-imports}` 实现时遵循同一路径。**对标**：ESLint 先稳定 `LintResult[]` canonical 对象再交 formatter，结果生产和渲染解耦。 | 1d | `kernel/governance/contracthealth.go`（新）+ `cmd/gocell/app/check.go`（瘦身）+ `kernel/governance/locator.go`（评估是否暴露） | PR#261 round 2 Finding #4（2026-04-25） |
| SARIF-URIBASEID-01 | **SARIF-URI-SEMANTICS-01** (Cx2, 🟡 可延后, 关联 P1-4 ✅ PR#261)：`cmd/gocell/app/printers/sarif.go::sarifArtifactLocation` 仅含裸 `URI` 字段，两个维度待补：(a) **uriBaseId 缺失**——SARIF 2.1.0 §3.4.4 + GitHub Code Scanning 推荐相对 URI 携带 `"uriBaseId": "%SRCROOT%"`；(b) **URI 编码缺失**——`ValidationResult.File` 只是仓库相对路径（如 `cells/x/cell.yaml`），不是 RFC 3986 URI；带空格 / 非 ASCII（如金样 `unicode_and_special_chars.sarif` 的 `cells/中文/cell.yaml`）/ 反斜杠的路径直接进 `artifactLocation.uri` 字段在不同 ingester 上行为未定义。**对标**：ESLint SARIF formatter 把路径转成 file URI；staticcheck 补 `%SRCROOT%` uriBaseId；golangci-lint 不补（最低成本但可移植性弱）。**修复**：(1) `sarifArtifactLocation` 加 `URIBaseId string\`json:"uriBaseId,omitempty"\`` 字段；(2) URI 字段经 `url.PathEscape` 处理或转成完整 `file://` URI；(3) 新增 golden case 含空格 + 非 ASCII 路径 + Windows 反斜杠路径锁住格式。**前置**：先用一次实际 GitHub Advanced Security PR 注释推送验证当前是否真的不渲染链接 / 锚点漂移，再决定修不修。 | 2h | `cmd/gocell/app/printers/sarif.go` + golden 文件 | PR#261 round 1 + round 2 Finding #3（2026-04-25） |
| P1-5 | **METADATA-PERF-BENCH-01** (Cx3, 🟡 可延后): `BenchmarkParseFS_500Files` 性能基准 + goccy/go-yaml 单次解码迁移成本评估 | 4h | `kernel/metadata/parser_test.go` | PR#152 seat-4 |
| P1-8 | **FEAT-1 DEVICE-LIST-API**: 新建 `devicelist` slice + `GET /api/v1/devices` 分页 + contract + contract_test；同步触发 CONTRACT-LIST-LINT-01 规则。**复核更正**：原描述中 rbaccheck `handleListRoles` 搭车项**已不存在**——`cells/accesscore/slices/rbaccheck/handler.go:80-95` 当前已使用 `httputil.ParsePageParamsOrWrite` + `query.MapPageResult` 实现真实游标分页（非 `map[string]any` 硬编码）；本条仅保留 devicelist 新建主线。 | 3h | `cells/devicecell/slices/devicelist/` + `contracts/http/device/list/v1/` | backend_issues.md #1 + 2026-04-25 复核 |
| S-5xx-code-mask | **5XX-ERRCODE-RESPONSE-MASK-01** (Cx2, **安全加固**, 🟡 可延后): 所有 5xx 错误响应的 `error.code` 字段直接透传 `errcode.Code` 字符串（如 `ERR_KEY_PROVIDER_AUTH_FAILED` / `ERR_AUTH_ROLE_FETCH_FAILED` / `ERR_VAULT_AUTH_FAILED` / `ERR_CONFIG_DECRYPT_FAILED` 等 20+ 个 500/503 code）。`httputil.WriteError` 已把 message 统一成 `"internal server error"`，但 code 字符串未屏蔽，攻击者可据此区分基础设施故障类型（密钥服务 / Vault / 角色仓库），构成轻度枚举泄漏。**修复**：`httputil.WriteError`/`writeErrcodeError` 在 `status>=500` 时把响应中的 `code` 统一改为 `ERR_INTERNAL`（或 503 改为 `ERR_SERVICE_UNAVAILABLE`），原 code 保留到 slog 供 operator 观测；客户端只见通用码。**影响面**：`pkg/httputil/response_test.go` 多个 `wantStatus: 5xx` + code 断言用例需要翻新；一并审计 `ErrKeyProviderTransient` / `ErrVaultAuthFailed` / `ErrCircuitOpen` 503 是否统一成 `ERR_SERVICE_UNAVAILABLE`。 | 3h | `pkg/httputil/response.go` + `pkg/httputil/response_test.go` | PR#241 六维度审查 F2+F3（OUT_OF_SCOPE） |
| L7 | **EXAMPLES-STARTUP-SMOKE-01** (Cx1, 🟡 可延后): 2026-04-22 复查确认所有 `NewCursorCodec` 字面量均 ≥ 32 字节（审计文档的"30/31 字节"系误数）。**唯一剩余 TODO**：CI 没有独立的 examples startup smoke job；`examples/ssobff` 当前仅在 integration tag 下跑 walkthrough test，`main()` 真实启动路径无单独验证（若启动依赖顺序或 option wiring 回归，CI 无法检测）。**修复**：`.github/workflows/ci.yml` 新增 `examples-smoke` job：`timeout 5s go run ./examples/ssobff 2>&1` 期望进入 ready 状态后 ctx cancel 退出（或等价 testable entrypoint）。 | 0.5h | `.github/workflows/ci.yml` | 2026-04-20 发现；2026-04-22 cursor key 误报核销，缩减范围 |
| PR220-3 | **JOURNEY-VERIFY-FAIL-CLOSED-01** (Cx3): (1) `J-sessionrefresh` / `J-ssologin` 的 `journeyID → RunPattern` 驼峰生成与真实测试函数名不一致，当前 zero-match 失败；(2) 即使命名修好，`tests/integration/journey_test.go` 28 条 `t.Skip("stub...")` 仍因 `go test` exit 0 被当 passed。**修复**：修 RunPattern 生成 + 解析 `go test -json` 识别 `--- SKIP`，skip stub 算 pending/fail 不算 verified。 | 4h | `kernel/verify/ref.go` + `gotest.go` + `runner.go` + `tests/integration/journey_test.go` + `docs/guides/integration-testing.md` | PR#220 遗留报告 P1 |
| ~~V-A5~~ | ~~**VALIDATION-HELPER-EXTRACT-01**~~ ❌ won't-do（2026-04-25 复核误判）：`pkg/validation/validation.go` `RequireNotBlank` 已存在（PR-A2 落地）；`runtime/auth/middleware.go:70` 等处的 `if token == ""` 是 HTTP token 提取分支（"该路由是否携带 token"），非字段校验场景，与 `RequireNotBlank` 适用域不同；不应强制替换 | — | — | — |
| V-A6 | **WEBSOCKET-DEPRECATED-01** (Cx1, **供应链**): `go.mod` 使用 `nhooyr.io/websocket v1.8.17` 已 archived。**修复**：切 `github.com/coder/websocket`（API 兼容）。 | 2h | `go.mod` + 全仓 import | 202604221954-verification §A6（P1-5） |
| V-A7 | **DEPRECATED-ADAPTER-METHODS-01** (Cx1): `Connection.Statter()`、`Subscriber.InitializeSubscription()` 仍被测试 + `kernel/outbox/outbox_test.go:464` 使用。**修复**：移除 `Deprecated:` 标注（承认稳定 API）或先迁测试再删。 | 1h | `adapters/*/` | 202604221954-verification §A7（P1-8） |
| ~~V-A8~~ | ~~**CMD-THICK-ENTRY-REDUCE-01**~~ ✅ 已修（2026-04-25 复核）：`cmd/corebundle/main.go` 当前 94 行（已拆为 `app.go`/`bundle.go`/`shared_deps.go`/`controlplane.go`/`secrets.go` 多文件）；`internalGuard` 已暴露 `Middleware()`/`NonceStore()` 等 exported 方法 | — | — | — |
| ~~V-A9~~ | ~~**DEMO-KEY-REJECT-LOADKEYSET-01**~~ ❌ won't-do（2026-04-25 复核误判）：`LoadKeySetFromEnv` 加载的是 RSA PEM 私钥；`rejectDemoKey` 与 `wellKnownDemoKeys` 黑名单专为 HMAC/AES 字符串密钥设计（固定字符串比对），RSA PEM 格式不可与字符串黑名单做有意义比较；cursor/HMAC/service-secret 三处的检查不能直接套用 RSA 路径。如需 RSA 弱密钥防护，应另开独立 ADR 设计 PEM 内容验证 | — | — | — |
| ~~V-A10~~ | ~~**GOVERNANCE-WINDOWS-PATH-01**~~ ✅ 已修（2026-04-25 复核）：`examples/ssobff/main.go:167` 已改为 `accesscore.ResolveBootstrapCredentialPath(os.Getenv("GOCELL_STATE_DIR"))`，内部 `initialadmin/bootstrap.go` 走 `filepath.Dir`/`filepath.Join` | — | — | — |
| V-A11 | **GOVERNANCE-EXAMPLES-COVERAGE-01** (Cx3): `kernel/governance/validate.go:138-154` rules 只扫 cells/contracts/journeys/assemblies，不扫 `examples/`。**修复**：新增 `rules_examples.go` 检查硬编码 key/path/URL。 | 3h | `kernel/governance/rules_examples.go`（新） | 202604221954-verification §A11（P1-17） |
| ~~V-A12~~ | ~~**SYMLINK-TOCTOU-CREDFILE-AUDIT-01**~~ ✅ 已修（2026-04-25 复核）：`cells/accesscore/initialadmin/credfile_io.go:37-81` `writeCredentialFile` 采用 `os.Lstat`（非 `os.Stat`）+ `platformImpl.SecureNewFile`（含 `O_EXCL\|O_CREATE`）+ `os.Rename` 原子替换，已是 TOCTOU 安全实现 | — | — | — |
| V-A13 | **GENTPL-LIFECYCLE-PATTERN-01** (Cx1+Cx2, 🟡 可延后): `kernel/assembly/gentpl/main.go.tpl` 直连 `app.Start/Stop` 跳过 `bootstrap.Run` phase3b hooks（含 initial admin bootstrap）；生成的新 assembly 默认丢失生命周期；需决定模板是"最小骨架"还是"开箱即用"，二选一并补集成测试 | 1h（设计）+ 2h（实施 + 测试） | `kernel/assembly/gentpl/main.go.tpl` | PR #243 review §E1 |
| ~~V-A14~~ | ~~**SHUTDOWN-WAIT-READY-CHANNEL-01**~~ ✅ 已修（2026-04-25 复核）：`runtime/shutdown/shutdown.go::Wait` 直接 `received := <-sig` 阻塞 signal channel；`shutdown_test.go` 走 `m.Shutdown()` 直调路径，无 50ms 硬等 | — | — | — |
| V-A15 | **INITIALADMIN-OPTION-FAILFAST-UNSUPPORTED-01** (Cx1, 🟡 可延后): `accesscore.WithInitialAdminBootstrap()` 在 `!unix && !windows` 平台仍可调用但 runtime 才报 errUnsupportedPlatform；需在 cell.Init 时 platform-check 并返回明确 errcode | 1h | `cells/accesscore/cell.go` + `cells/accesscore/initialadmin/bootstrap_unsupported.go` | PR #243 review §S7 |
| V-A16 | **SHUTDOWN-INTERRUPT-WINDOWS-TEST-01** (Cx1, 🟡 可延后): `runtime/shutdown.NotifyContext` 在 Windows 仅靠 os-smoke CI 间接覆盖；需补 `shutdown_interrupt_windows_test.go`（`//go:build windows`）单测 `os.Interrupt` 触发 ctx 取消 | 0.5h | `runtime/shutdown/` | PR #243 review §S11 |
| V-A17 | **SHUTDOWN-BUILD-TAG-MUTUAL-EXCLUSION-01** (Cx1, 🟡 可延后): `runtime/shutdown/signals_*.go` 的 `unix` / `windows` / `!unix && !windows` 互斥靠 4 OS cross-compile 验证；建议加编译期守卫测试（如检查只有 1 个 `signalsToWatch` 实现可见） | 0.5h | `runtime/shutdown/` | PR #243 review §S8 |
| V-A18 | **DOCS-MACOS-TROUBLESHOOTING-CLARIFY-01** (Cx1, 🟡 可延后): `docs/operations/first-run-setup.md:244` 故障排查表 macOS `$TMPDIR/gocell` hint 与自动 `~/Library/...` fallback 语义重叠，易误导用户 | 0.5h | `docs/operations/first-run-setup.md` | PR #243 review §S10 |
| V-A19 | **DEMO-KEY-WARN-COMMENT-01** (Cx2, 与 V-A9 同 PR): `cmd/corebundle/demo_keys.go:18-36` 可预测模板；`examples/ssobff/main.go:99,116` demo key 无警告。**修复**：加 `// DO NOT COPY TO PRODUCTION` 注释。 | 0.5h | `cmd/corebundle/demo_keys.go` + `examples/ssobff/main.go` | 202604221954-verification §A19（P2-11） |
| A26-R1 | **ADMINPROVISION-DIST-LOCK-01** (Cx3, 🟠 条件延后 — 触发条件：PG adapter for accesscore UserRepo/RoleRepo 接线): `adminprovision.Provisioner.Ensure` 当前靠 sync.Mutex 覆盖单进程原子性；多实例 PG 部署需在 PG adapter 的 RunInTx 内执行 `pg_advisory_xact_lock(<const>)` 保证跨进程唯一 admin 创建。加到 PG adapter 的 TxRunner 或新 `AcquireBootstrapLock(ctx)` port 方法。 | 3h | `adapters/postgres/accesscore/` + `cells/accesscore/internal/ports/role_repo.go` | PR#247 round-2 review P0（已本 PR 单进程闭合） |
| A26-R2 | **SETUP-ADMIN-RATE-LIMIT-01** (Cx2, 🟡 可延后 — 取决于是否面向公网): `POST /api/v1/access/setup/admin` 现在虽然 410 快速短路、bcrypt 仅在成功路径触发，但仍是 Public。加 per-IP rate-limit middleware + 失败计数 lockout（如 5min 内 5 次 400 触发 429）；或改造成一次性 bootstrap token（env 生成，第一次 POST 消费）。 | 4h | `runtime/auth/` + `cells/accesscore/cell_routes.go` | PR#247 round-2 review P1-B |
| A26-R3 | **SETUP-PATH-NAMESPACE-POLICY-01** (Cx1, 🟡 可延后): 在 `.claude/rules/gocell/api-versioning.md` 中明确「顶级 `/api/v1/setup/` 为未来跨 Cell system-level 初始化保留；per-Cell 初始化入口挂在 `/api/v1/{cell}/setup/...`」。本 PR 已把 accesscore setup 迁到 `/api/v1/access/setup/*`（Consul 模式）；规则写明后避免未来又有 PR 在顶级 setup 下开端点。 | 0.5h | `.claude/rules/gocell/api-versioning.md` | PR#247 round-2 review N-01 |
| A26-R4 | **SETUP-ORPHAN-E2E-01** (Cx2, 🟠 条件延后 — 触发条件：PG adapter for accesscore): orphan recovery 分支目前只在 provisioner 单元测 + setup service 单元测覆盖。in-memory repo 无法跨进程持久化 orphan 状态，E2E 场景要等 PG adapter 接入后，用真 DB 写 orphan 行 → 重启 → setup POST 验证 RequireReset=false 清除 + 无重复 event。 | 1.5h | `cmd/corebundle/setup_integration_test.go` (PG path) | PR#247 round-2 review N-06 |
| PR244-F1 | **MULTIPOD-INMEMORY-NONCE-WARN-01** (P2 复核降级, Cx1, 🟡 残余补 warn 日志): **2026-04-25 复核更正**：核心防御（结构化方案 b）已在 `cmd/corebundle/shared_deps.go:214-218` 落地——`validateControlPlane` 当 `InMemory && !SinglePodReplayProtection` 时已 fail-fast；finding 残余仅剩"启动成功后补一条 Info/Warn 日志"提示运维"该副本走 in-memory replay 防护"。降为 P2，工时调整 30min。 | 30min | `cmd/corebundle/controlplane.go` | PR#244 round-2 reviewer F1 + 2026-04-25 复核 |
| ~~PR244-F8~~ | ~~**NONCE-CAPACITY-CONTROL-01**~~ ✅ 核心已修（2026-04-25 复核）：`runtime/auth/nonce.go:87,100-103,153-161` 已实现 `defaultMaxNonceEntries=100000` + `WithMaxNonceEntries` option + 满时 LRU prune + fail-closed（`ErrNonceStoreFull`）；DoS 核心防护已在。残余仅 capacity gauge 未装（属 P3 可观测性，不阻塞） | — | — | — |
| PR244-F9 | **AUTH-REPLAY-ERRCODE-GRANULARITY-01** (P2, Cx1-Cx2, 🟡 可延后): `ErrAuthUnauthorized` 当前覆盖 signature/nonce/audience 等所有 401 原因，replay 攻击无法从 HTTP 响应码区分于常规凭证失败。**修复**：新增 `ErrAuthReplayDetected` 400/401 子码；需跨消费方（audit log / SIEM / monitoring dashboard）同步评估。 | 2h | `pkg/errcode/errcode.go` + `runtime/auth/nonce.go` + 消费方告警规则 | PR#244 round-2 review F9 |
| ~~PR245-F5~~ | ~~**KERNEL-OUTBOX-ENVELOPE-TEST-COVERAGE-AUDIT-01**~~ ✅ 已确认覆盖（2026-04-25 复核）：`kernel/outbox/envelope_test.go` 已覆盖 `schemaVersion` camelCase（`TestMarshalEnvelope_StampsSchemaVersionV1`）/ `aggregateId/eventType` camelCase（`TestUnmarshalEnvelope_V1Success`）/ broken JSON → `ErrEnvelopeSchema`（`TestUnmarshalEnvelope_BrokenJSONRejected`）/ empty id/eventType 被拒（`TestUnmarshalEnvelope_MissingRequiredFieldRejected`）/ `MarshalDirectEnvelope`（`TestMarshalDirectEnvelope_ProducesV1`）；wire-format 关键断言全到位 | — | — | — |
| PR245-F6 | **OUTBOX-ARCHTEST-SCAN-SCOPE-EXPAND-01** (Cx2, 🟡 可延后): `tools/archtest/outbox_cell_test.go::isCellFile` 仅匹配 `cells/<name>/cell.go`，若未来 Option 搬到 `cells/<name>/options.go` / `cell_options.go` / `cell_extra.go`，`OUTBOX-CELL-01` 守卫即失效。**修复**：`isCellFile` 改为 `cells/<name>/*.go`（排除 `*_test.go` + `internal/` + `slices/`）扫整个 cell 包顶层 decl。 | 1h | `tools/archtest/outbox_cell_test.go` | PR#245 round-1 reviewer F-6 |
| PR245-F10 | **CELL-RAW-DEPS-ARCHTEST-EXPAND-01** (Cx2, 🟠 PR-A22 Cell ISP 拆分触发): PR-A5c 收口只 ban `WithPublisher` / `WithOutboxWriter` raw Option；`AccessCore.txRunner` 等 raw dep 仍保留。PR-A22 落地时对等扩展 archtest 规则 `CELL-RAW-DEPS-01` 禁止新引入 raw-dep Option，避免重新打开口子。 | 2h | `tools/archtest/outbox_cell_test.go` 或新 `raw_deps_test.go` | PR#245 round-1 reviewer F-10（PR-A22 触发） |
| ~~PR247-N3~~ | ~~**CELL-PATTERNS-EVENT-DTO-01**~~ ✅ 已修（2026-04-25 复核）：`setup/service.go:282-285` + `identitymanage/service.go:151` 均已使用 `dto.UserCreatedEvent{UserID, Username}` typed struct；`map[string]any` 仅残留在 `Lock()` 的 `user.locked` 事件——已被 PR250-F2 涵盖，本条不重复登记 | — | — | — |
| ~~PR247-N4~~ | ~~**SETUP-ADMIN-ERROR-MESSAGE-PATH-LEAK-01**~~ ✅ PR#264 已修（2026-04-25 复核）：`setup/service.go:270-279` `setupRetiredError()` message 改为 "first-run admin already provisioned; this endpoint is retired"，login 路径挪到 `details.loginEndpoint` | — | — | — |
| ~~PR247-N5~~ | ~~**SETUP-SLICE-CONSISTENCYLEVEL-EXPLICIT-01**~~ ✅ PR#266 round-2 已收口（F-DX-01）：(1) `kernel/metadata/types.go::SliceMeta` 加 `ConsistencyLevel string` 字段（KnownFields strict 解除）；(2) 新增 governance 规则 **SLICE-CONSISTENCY-01** (`kernel/governance/rules_slice.go`)：slice 显式声明的 ConsistencyLevel 必须 ≤ 父 cell（允许降级，禁止升级）；(3) `flagwrite/slice.yaml` 注释 → `consistencyLevel: L1` 实字段；(4) `setup/slice.yaml` 显式 `consistencyLevel: L2`；(5) `parser_strict_test.go` + `rules_slice_test.go` 加 8+ 个 table case | — | — | — |
| ~~PR247-N7~~ | ~~**SETUP-ADMIN-EMAIL-CONTROL-CHAR-GUARD-01**~~ ✅ PR#264 合入：`setup/service.go::validateCreateAdminInput` 已加 `strings.ContainsAny(in.Email/Username, "\r\n\t\x00")` 控制字符拒绝；password 改为 8-72 字节 + 强制 printable ASCII（pattern `^[ -~]+$`）；`request.schema.json` 同步收紧 password `maxLength: 72` 与 bcrypt 输入上限对齐 | — | — | — |
| PR-A12-SWEEPER-WIRE | **PR-A12-KERNEL-COMMAND-SWEEPER-WIRE-01** (Cx1-Cx2, 🟠 触发条件：`InMemQueue.ListPending` 长出 multi-device scan 方法或 PG adapter 接入): PR#249 kernel/command 单设备 Sweeper 已 functional，cell 级 Sweeper 需要 multi-device 扫描能力后 wire。当前 `examples/iotdevice/cells/devicecell/cell.go` 已加 `TODO(PR-A12-SWEEPER-WIRE)` 占位。**修复**：`InMemQueue` 长出 `ListAllPending(ctx, since time.Time)` → cell-level `Sweeper` 启动后台定时回收，超时 entry 按 `TimeoutPhase` 分流（retry / fail / dead-letter）。 | 2h | `kernel/command/commandtest/inmem.go` + `examples/iotdevice/cells/devicecell/cell.go` | PR#249 out-of-scope residual |
| PR252-F1 | **COMMAND-QUEUE-REGISTRAR-BOOTSTRAP-FAILFAST-01** (P1, Cx3, 🟠 下一个 durable command adapter 前触发): PR#252 引入 `QueueRegistrar` / `ActiveScanner` / runtime command adapter 发现后，能力声明与 bootstrap 装配仍分散；若 cell 声明可注入 queue 但实际装配缺失，容易只告警或局部降级，形成"看起来接上但未生效"的启动隐患。**修复**：把 QueueRegistrar 发现、queue/scanner 注入、capability 校验纳入唯一 bootstrap phase；缺 queue 或缺 scanner 时 fail-fast；补 bootstrap phase 单测覆盖 missing queue / missing scanner / duplicate registrar / devicecell 成功路径。 | 4-6h | `kernel/cell/registrar.go` + `runtime/command/` + `runtime/bootstrap/bootstrap_phases.go` + `examples/iotdevice/cells/devicecell/cell.go` | PR#252 six-seat review P1/Cx3（Queue 注入闭环） |
| PR252-F2 | **COMMAND-SWEEPER-PRODUCTION-GOVERNANCE-01** (P1, Cx4, 🟠 多副本 command consumer 或 PG command queue 前触发): PR#252 的 sweeper 可扫描 active commands 并 AckTimeout，但生产治理层不足：多副本下缺 leader/分片 ownership，扫描/Ack 错误只回调可能被忽略，连续失败不会降级 readiness，核心路径缺指标，存在放大负载与静默退化风险。**修复**：引入单活 leader 或稳定分片 ownership；定义连续失败阈值并接入 `/readyz` 降级；记录 scan / ack timeout / ack failure / consecutive failure 指标；补 fake scanner/queue 的失败预算测试和多实例 ownership 测试。 | 8h | `kernel/command/sweeper.go` + `runtime/command/` + `runtime/bootstrap/` + `runtime/http/health/` + `kernel/observability/metrics/` | PR#252 six-seat review P1/Cx4（sweeper ownership/readiness/metrics） |
| PR250-F1 | **CONFIG-VERSION-PUBLISHED-SENSITIVE-FIELD-EVAL-01** (P1 安全, Cx2, 🟡 可延后 — 预先存在): `contracts/event/config/version-published/v1/payload.schema.json` 的 `sensitive: bool` 字段将 config 条目的敏感分类广播给所有订阅者（value 未暴露，仅暴露分类标签）。**预先存在**：PR-A6 之前此字段已在 `event.config.changed.v1` 的 "published" oneOf 分支上同样广播；PR-A6 拆 topic 时保留了语义，未新增泄漏。**修复（可选）**：从 wire 移除 `sensitive`，订阅者需要时直接查 configcore（`http.config.get.v1` 或 internal API）；或保留字段但将 consumer 白名单收敛到 auditcore。**决策 trigger**：首次引入外部 cell 订阅此 topic 时评估；当前 subscribers 均为同 repo 内三 cell，攻击面有限。 | 1-2h | `contracts/event/config/version-published/v1/payload.schema.json` + `cells/configcore/internal/domain/config_events.go` + 订阅者 | PR#250 round-1 security reviewer #1 |
| PR250-F2 | **EVENT-USER-FLAG-CAMELCASE-FOLLOWUP-01** (P1 安全/DX, Cx2, 🟡 可延后): `event.user.created.v1` / `event.user.locked.v1` / `event.flag.changed.v1` payload 仍为 snake_case (`user_id` / `username` / `event_type` 等)，PR-A6 将 session/config/audit 事件迁至 camelCase 时按 plan 明确将这些事件留作 trailing sweep。**现状**：auditappend 通过 `UserIDCamel`/`UserIDSnake` 双 tag fallback 跨协议提取 actor，临时过渡。**修复**：沿 PR-A6 套路迁移：(a) `contracts/event/user/{created,locked}/v1/payload.schema.json` + `contracts/event/flag/changed/v1/payload.schema.json` 字段改 camelCase；(b) `cells/accesscore/internal/dto/user_events.go` `UserCreatedEvent` 的 JSON tag 改 camelCase；(c) setup / identitymanage / flagwrite 的 producer 调用点迁 `outbox.Emit[T]`（同时吸收 PR247-N3 EVENT-DTO 的 typed struct 诉求）；(d) auditappend 删除 `UserIDSnake` 降级回单 tag；(e) 更新 docs/design/ capability-map + master-plan + capability-inventory 引用。 | 6-8h | `contracts/event/{user,flag}/**` + `cells/accesscore/internal/dto/user_events.go` + `cells/accesscore/slices/{setup,identitymanage}/service.go` + `cells/configcore/slices/flagwrite/service.go` + `cells/auditcore/slices/auditappend/service.go` | PR#250 fix commit + round-1 security reviewer #2（与 PR247-N3 合并批处理） |
| PR250-F3 | **EVENT-WIRE-BYTE-PINNING-TEST-EVAL-01** (P2, Cx2, 🟡 可延后 — 倾向不做): PR-A6 的 7 个 typed event struct 未做 wire-byte 级 golden-string 断言（contract test 走 JSON schema round-trip，对字段顺序 / `omitempty` 不敏感）。reviewer 建议增加 `assert.Equal(t, ``{"action":"created",...}``, string(entry.Payload))` 类断言以捕获未被 schema 捕获的 camelCase 回归。**反方观点**：schema validation + `additionalProperties: false` 已在 contract_test 覆盖字段名漂移；golden-string 额外脆弱（字段顺序 / `omitempty` 边界 / 生成器差异）；净增维护成本。**决策**：除非后续出现真实 camelCase regression 漏网，否则关闭为 won't-do。 | 2h | `cells/accesscore/slices/*/contract_test.go` + `cells/configcore/slices/*/contract_test.go` + `cells/auditcore/slices/*/contract_test.go` | PR#250 round-1 testing reviewer #4 |
| PR-CFG-A-DEFER-1 | **READYZ-VERBOSE-FAILOPEN-FIX-01** (P1 安全, Cx2, 🟡 可延后 — pre-existing): `runtime/http/health/health.go:534` 当 `HealthListener` 未配置 `PolicyVerboseToken` 且 token == "" 时，`/readyz?verbose=1` 直接渲染 cell 拓扑（dependency 名、错误链）。`ssobff/main.go:157` 已通过 `WithReadyzVerboseDisabled` 默认关闭，但默认行为不安全——任何没显式 disabled 的 assembly 即零门槛暴露内部依赖图。**修复**：(1) `health.go::renderReadyz` 中 token=="" 时 fall-back 到精简 envelope（仅 status + ts），verbose 字段需 token 才返回；或 (2) 配置层强制 HealthListener 必须设 `PolicyVerboseToken` 或 `WithReadyzVerboseDisabled` 二选一，缺省 fail-fast。**对标**：Kubernetes `/healthz/verbose` gated by `--profiling` flag + RBAC；Consul agent `?pretty` 不暴露内部拓扑。**配套源 finding**：PR-CFG-A round-2 reviewer Finding #1（OUT_OF_SCOPE）。 | 3-4h | `runtime/http/health/health.go` + `runtime/bootstrap/health.go` + `examples/ssobff/main.go` 验证 | PR-CFG-A (PR#268) round-2 reviewer Finding #1 |
| PR-CFG-A-DEFER-2 | **CONFIGCORE-L2-MEMORY-MODE-DIVERGENCE-01** (P2 治理一致性, Cx1, 🟡 可延后 — 元数据评估): `cells/configcore/cell.yaml` 声明 `consistencyLevel: L2`（OutboxFact），但当 `GOCELL_CONFIGCORE_STORAGE_BACKEND=memory` 时实际行为是 L0（无事务、无 outbox 写入路径，事件由 DirectEmitter fail-open 模式发射）。声明值与运行时行为分歧——治理工具与 readiness gate 都依赖 `consistencyLevel` 元数据决策。**修复方向**（待 architect 评估）：(a) 引入 `durabilityMode: durable\|memory` 字段，cell.yaml 声明"逻辑级别"，runtime 通过 `cell.Init()` 上报"实际级别"；(b) 拆 cell（`configcore-memory` / `configcore-pg`）；(c) 在 `gocell validate --strict` 加规则：声明 L2 的 cell 启动时必须装载 outbox.Writer（非 nil 非 noop）。**对标**：Kubernetes `Reliable / Eventual` annotation + admission webhook；Cassandra consistencyLevel 是请求级而非声明级。**配套源 finding**：PR-CFG-A round-2 reviewer Finding #3（OUT_OF_SCOPE）。 | 1d（设计 + 决策）+ 4h（实施任一方案） | `cells/configcore/cell.yaml` + `cells/configcore/cell_init.go` + `kernel/governance/rules_*.go` | PR-CFG-A (PR#268) round-2 reviewer Finding #3 |
| PR-CFG-A-DEFER-3 | **HEALTH-AGG-ARCHTEST-GOPACKAGES-UPGRADE-01** (Cx3, 🟡 可延后 — 架构精度提升): `tools/archtest/health_aggregation_test.go` 当前用 `go/parser` 做语法树扫描，遇嵌入字段（`type X struct { *PGResource }`）盲区——不能解析跨包类型继承的 Checkers() 实现。已在代码中以 known-limitation 注释标注。**升级**：迁移到 `go/packages` + `go/types`，做真类型分析，覆盖嵌入/接口实现等结构性场景。**触发条件**：业务出现真实嵌入 ManagedResource 嵌入场景（当前 0 命中），或本仓 archtest 整体升级到 type-check 时一并迁移。**对标**：staticcheck（go/packages）；kubebuilder controller-gen（go/packages + types）。**配套源 finding**：PR-CFG-A round-2 reviewer Finding #6（IN_SCOPE 已 known-limitation 注释）。 | 4-6h | `tools/archtest/health_aggregation_test.go` | PR-CFG-A (PR#268) round-2 reviewer Finding #6 |

---

## P2 待办

### kernel / runtime

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| DISTLOCK-WORKER | **DISTLOCK-WORKER-LIFECYCLE-01** (Cx2, 🟡 P3 可延后): Manager lazy-start/drain 模型与 `worker.Worker` Start/Stop ctx 驱动关闭模型不匹配；第一个接入 cell 按需选择 wiring 策略。当前 distlock 在 `cmd/corebundle` 零 caller，无紧迫性。 | 2h | `runtime/distlock/` + 第一个接入的 cell | PR-A20 尾巴清零 — 保留 defer |
| ~~DISTLOCK-LOCKER-TC3-FLAKY~~ | ~~**DISTLOCK-LOCKER-TC3-FLAKY-01**~~ ✅ 修复（PR#251）：根因不是"墙钟 budget 边缘"，而是 `Manager.nextTimer` 中 `clock.Now()` 与 `NewTimer(d)` 两次独立持锁的 read-then-act 缝隙——FakeClock 下 `Advance` 抢插会让 `ft.deadline = nextRenew + advance_d`，timer 永不 fire；ERROR log 里的 "key3a budget exhausted" 实际发生在 t.Fatal 之后（defer release2 触发 manager 重新装 timer 时 d=0 立刻 fire）。**修复**：`Clock` 接口 `NewTimer(d Duration)` 替换为 `NewTimerAt(deadline time.Time)`，FakeClock 在单次 `fc.mu` 持锁内完成 deadline 写入；`nextTimer` 直接消费 `heapItem.nextRenew`（已是绝对时间），消除中间 round-trip 与竞态窗口。比 backlog 原推荐的 (b) 测试侧屏障更彻底——race 窗口物理消失，未来任何新测试不需要"小心 fc.Advance 时机"。 | — | — | — |
| R2-FOLLOW | **HTTP-METRICS-LABEL-REALIGN-01** (Cx2, P2, 🟠 多 cell assembly 部署前触发): PR#228 auto-wire HTTP collector 的 label 名为 `cell` 但实际值来自 `runtime/bootstrap/bootstrap_phases.go:675-683` `cellID := b.assemblyID`——多 cell assembly 下所有端点请求会贴同一个 assembly-id value，`cell` 维度 dashboard/告警误归因。**修复**：两步走——(1) 最小兼容：label 名改 `assembly` 或同时暴露 `assembly`+`cell` 双标签保留向后兼容窗口；(2) 真解：在 router 注册路由时把 owning cell 写入 request context，collector middleware 从 ctx 读取，`NewProviderCollector` 配置改 `AssemblyID string, CellResolver func(*http.Request) string`。参考 Kratos `operation`/go-zero `route`/OpenTelemetry Resource vs Semantic-attr 分层。ref: `runtime/observability/metrics/provider_collector.go:60,69,89` | 4h | `runtime/bootstrap/bootstrap_phases.go` + `runtime/observability/metrics/provider_collector.go` + `runtime/http/middleware/metrics.go` + dashboards | 2026-04-24 六席位复核（架构缺陷） |
| R3 | **OB-02** (🟡 可延后): safe_observe broken logger 注入测试 | 1h | `runtime/http/middleware/safe_observe_test.go` | 历史 backlog 0-J |
| PR220-4 | **CI-LINT-EVENT-SEMANTIC-SPLIT-01** (Cx1): `.github/workflows/ci.yml` `push` + `pull_request` 都用 `--new-from-merge-base=origin/develop`；push 到 develop 时 merge-base 退化导致 post-merge lint 门禁失效。**修复**：`pull_request` 保留 diff 降噪，`push` 改全量 lint 或 `before..after` diff。 | 1h | `.github/workflows/ci.yml` | PR#220 遗留报告 P2 |
| PR-A40 | **HOUSEKEEPING-SWEEP-01** (Cx1, 🟡 可延后, 一次性卫生 PR): 打包 verification 报告中三条纯卫生项，单 PR 一次清完即关闭。**(1) A12 DEPRECATED-SHIM-CLEAR**：`kernel/outbox/outbox.go:536,543,552` 三个 deprecated 符号无外部 caller，直接删除。**(2) A13 KERNEL-GODOC-BACKFILL**：`kernel/cell/` 公开项 ~20-25% 缺 godoc，按包补齐（`Cell` / `EventRouter` / `LifecycleHook` / `LifecycleContributor` / `Prefixer` / `ResolveEmitter` / `RunnerOrNoop` 等）。**(3) A18 SLOG-ERROR-UNIFY**：`adapters/` 混用 `"err", err`（原始 kv）/`slog.String("error", err.Error())`/`slog.Any("error", err)` 三种风格，统一为 `slog.Any("error", err)`（与 `runtime/` 一致）。**理由**：原 graceful-backus 决议"不入 backlog 留 verification 报告追踪"，但 verification 报告不被自动追踪，且 v1.0 后 sweep 节点合适做一次。 | 3.5h | `kernel/outbox/outbox.go` + `kernel/cell/*.go` + `adapters/*/*.go` | graceful-backus §问题 P2 卫生（A12/A13/A18）+ verification §P5/P6 |
| A5a-R3 | **ACCESSCORE-INITIALADMIN-THIN-WRAPPER-01** (Cx3, 🟡 **评估后可能 won't-do**): 消费者需同时 import `cells/accesscore` + `cells/accesscore/initialadmin` 才能设 TTL 等配置（见 `cmd/corebundle/access_module.go` `InitialAdminOpts []initialadmin.LifecycleOption`）。**候选方案**：在 `cells/accesscore` 加 re-export thin wrapper（`WithInitialAdminTTL(d time.Duration) Option` 等），composition root 仅 import accesscore 即可配置。**反方观点**：product-manager + architect review 均倾向保持现状——thin wrapper 会在 accesscore 增加 facade 维护成本，且 `LifecycleOption` 切片语义天然暴露了底层结构；省一次 import 的 DX 收益有限。**决策 trigger**（2026-04-24 review F-7 追加）：PR-A5b 合入时回看一次；或任意第二个 cell 公开"subpackage + functional options"模式时触发复查；无 trigger 触发则视为永久 won't-do。 | 1-2h | `cells/accesscore/` | PR#234 review PM F3 / architect P2 / reviewer F-7 |
| A5a-R9 | **INITIALADMIN-CREDPATH-NAMING-CHAIN-01** (Cx1, 🟡 可延后): `cmd/corebundle` / `examples/*` 拿 credential path 要经过三跳 `accesscore.ResolveBootstrapCredentialPath(stateDir)` → `initialadmin.ResolveCredentialPath(stateDir)` → `resolveCredentialPath()`（见 `cells/accesscore/cell.go:96-98` + `cells/accesscore/initialadmin/bootstrap.go:125-143`）。外层 wrapper 只是 zero-arg convenience，转发层数超出价值。**修复**：删 `accesscore.ResolveBootstrapCredentialPath`，调用方直接用 `initialadmin.ResolveCredentialPath`；或删内层 `resolveCredentialPath()` convenience。**选方案**：删外层（accesscore wrapper 只有 `cmd/corebundle` 一处调用方）。 | 0.5h | `cells/accesscore/cell.go` + `cmd/corebundle/` | PR#234 reviewer F-4 |
| A5a-R10 | **INITIALADMIN-ERRCODE-CONSISTENCY-01** (Cx1, 🟡 可延后): `cells/accesscore/initialadmin/bootstrap.go:79-90` `newBootstrapper` 对 deps/cfg 校验用裸 `fmt.Errorf("initialadmin: bootstrapper requires UserRepo")`，违反 CLAUDE.md "禁止裸 errors.New 对外暴露"。虽然 `newBootstrapper` 已 unexported（R1 lowercase），错误不跨包，严重度低，但 `lifecycle.go:136` 对称位置用的是 `errcode.New(errcode.ErrCellInvalidConfig, ...)`，风格不统一。**修复**：三处 `fmt.Errorf` → `errcode.New(errcode.ErrCellInvalidConfig, "initialadmin: ...")`。 | 0.5h | `cells/accesscore/initialadmin/bootstrap.go` | PR#234 reviewer F-6 |
| A5a-R11 | **LIFECYCLECONTRIBUTOR-BLOCKING-SEMANTICS-WARN-01** (Cx3, 🟡 可延后): `kernel/cell.LifecycleHook` 标 `EXPERIMENTAL` 且 godoc 要求 "OnStart must return promptly"，但 `runtime/bootstrap/lifecycle.go:runHook` 只做 ctx 超时硬保护，无"快接近 StartTimeout 但未触发"的软警告。下一个接入 `LifecycleContributor` 的 Cell 若写了慢 OnStart，排查困难。**修复**：在 phase3b 或 `runHook` 添加"OnStart 耗时 ≥ defaultStartTimeout × 0.8 时 slog.Warn with cell+hook name"，作为 early回路不 fail；第二接入方出现时把 EXPERIMENTAL 标记去掉并把 "return promptly" 升级为 lint 或测试护栏。 | 1-2h | `runtime/bootstrap/lifecycle.go` + `kernel/cell/registrar.go` godoc | PR#234 detailed C3 |
| A5a-R12 | **INITIALADMIN-TESTING-ENTROPY-UNEXPORT-01** (Cx2, 🟡 **评估后可能 won't-do**): `cells/accesscore/initialadmin/lifecycle.go:91` `WithPasswordSourceForTesting` 是 public export，外部包可用其降低密码熵，生产误用没有护栏。reviewer 建议改回 unexported `withPasswordSource`。**反方观点**：保留导出的理由是 `cells/accesscore` 包的 cell-level test（`package accesscore`，非 `accesscore_test`）需要注入确定性熵源，而 `cells/accesscore/initialadmin` 是独立 package，`withPasswordSource` 外包不可见。替代方案 `accesscore.NewLifecycleForTest(...)` 更侵入（加 test-only 构造函数），收益是消除 1 个 testing-named public option。**决策 trigger**：v1.0 前 security audit 若标红则落地；或 R1 之后发现 accesscore 包内有更多 test-only entropy 注入需求时打包处理。 | 1h | `cells/accesscore/initialadmin/lifecycle.go` | PR#234 reviewer F-2 |
| PR238-FU2 | **OBS-IS-INFRA-BUCKET-COUNTER-AUDIT-01** (P2, Cx1, 🟠 条件延后，下一个引入 Category-labeled Prometheus counter 前触发): PR#238 把 `ErrConfigRepoNotFound` 的 Category 从 `CategoryUnspecified` 改为 `CategoryDomain`，`IsInfraError(err)` 对该类错误返回从 `true` 翻转到 `false`。当前 `runtime/observability/metrics/`、`adapters/*/` 代码均未用 `errcode.Category` 或 `IsInfraError` 作为 Prometheus counter label；若未来任何 PR 引入这类 counter，必须同步评估 bucket 漂移对 SLO 告警的影响。**动作**：引入第一个 Category-labeled counter 的 PR 在 PR description 中显式声明 bucket 迁移表 + dashboards 更新；review 必拦截。 | 0.5h | 新 counter 所在文件 + ops runbook | PR#238 L4 reviewer OPS-02 |
| CTXCANCEL-HTTP-499 | **CTX-CANCEL-HTTP-499-MAPPING-01** (P2, Cx2, 🟡 可延后): 客户端主动断开（context.Canceled）当前被映射到 HTTP 500 + log5xx Error 级日志，与服务端真实故障混淆（Warn+Error 双发 / SLO Error rate 污染）。**修复**：参考 nginx `499 Client Closed Request` / Envoy / Google Cloud LB 的 semantics，为 ctx-cancel 路径新增 `ErrConfigReqCanceled` code + `pkg/httputil/codeToStatus` 映射 499。这样 `log5xx` 不再触发，只保留 repo 层 Warn。 | 3h | `pkg/errcode/` + `pkg/httputil/response.go` + `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer OPS-L4-04 |
| OTEL-SPAN-CTXCANCEL | **OTEL-SPAN-CTX-CANCEL-STATUS-01** (P2, Cx2, 🟡 可延后): ctx cancel 路径下 HTTP response 可能未写（client 已断开），`runtime/http/middleware/tracing.go` 的 `state.Status()` 仍为默认 200，span 错误标记失败，追踪系统中看不到这条 error。**修复**：在 `tracing.go` 中补 `ctx.Err() != nil && !state.Committed()` 判断强制 `span.SetStatus(codes.Error, ...)`。参考 OpenTelemetry semantic conventions for HTTP spans。 | 2h | `runtime/http/middleware/tracing.go` | PR#238 L4 Round-2 reviewer OPS-L4-06 |
| FU2-GOVERNANCE-STATIC | **OBS-IS-INFRA-BUCKET-GOVERNANCE-STATIC-01** (P2, Cx3, 🟠 与 PR238-FU2 联动，触发 FU2 的同 PR 必做): PR238-FU2 当前是 backlog prose 约定（"下一个引入 Category-labeled Prometheus counter 前强制评审"），没有任何静态护栏（CI / kernel/governance / lint）。未来引入 counter 的开发者可能完全不知道 FU2。**硬化方案**：在 `kernel/governance/` 新增 `rules_obs.go` 扫描 `runtime/observability/metrics/` + `adapters/*/` 下任何 Prometheus counter 注册，若 label 值来自 `errcode.Category` / `IsInfraError(...)` 输出 Warning 要求 PR 附 bucket-migration 表。**依赖**：PR238-FU2 同时触发时必做。 | 2h | `kernel/governance/rules_obs.go`（新）+ CI | PR#238 L4 Round-2 reviewer OPS-L4-08 + KG-R2-03 |
| BOOTSTRAP-LIFECYCLE-INTEG-TEST-PACKAGE | **BOOTSTRAP-LIFECYCLE-INTEGRATION-TEST-PACKAGE-01** (P2, Cx1, 🟡 可延后): `runtime/bootstrap/lifecycle_integration_test.go` 属 `package bootstrap_test`（外部测试包）但引用 `WithInternalListener` / `newLocalListener` 没有包前缀——前者定义在 `bootstrap.go`（生产代码），后者定义在 `bootstrap_test.go`（内部 `package bootstrap`）。当前 `go test -tags=integration ./...` 扫到该文件会 build 失败："undefined: WithInternalListener / newLocalListener"。CI 的 `integration-test` job 只跑 `./adapters/... ./tests/integration/... ./cmd/corebundle/... ./examples/ssobff/... ./cells/accesscore/slices/identitymanage/...`，不覆盖 runtime/bootstrap，所以没有即时 CI failure；但未来扩展 integration job 范围或本地 full-repo integration test 会翻车。**修复**：二选一——(a) 把文件改成 `package bootstrap`（与其他 listener-binding 测试同包），或 (b) 留 `package bootstrap_test` 但加 `bootstrap.` 前缀 + 把 `newLocalListener` 导出为 `bootstrap.NewLocalListenerForTest` 或搬到独立 testhelper 子包。建议 (a)，因为该测试本身就是白盒 lifecycle 探针，不需要黑盒包隔离。 | 0.5h | `runtime/bootstrap/lifecycle_integration_test.go` | discovered via PR #246 round-6 local `go test -tags=integration ./...` |
| PR246-ADR-SYNC | **PR-A11-ADR-SYNC-AFTER-ROUND6-01** (P2, Cx1, 🟡 可延后): PR #246 round-5/6 期间 FMT-18 从 regex 重写为 go/ast 实现并扩展了以下能力，但 `docs/architecture/202604242030-adr-kernel-wrapper-contract-observability.md` §9 仍描述 "regex scan wrapper.ContractSpec literals"：(1) AST 基础实现；(2) 识别 `wrapper.EventSpec(...)` 调用形态；(3) import alias 感知；(4) 同文件 `const = "..."` 引用求值；(5) `unresolved` warning。另外新增的 `wrapper.EventSpec(id, transport)` helper 是推荐写法，`docs/guides/cell-development-guide.md:187` 示例仍用旧的 `ContractSpec{Kind:"event", ...}` 字面量形式没同步。**修复**：更新 ADR §9 描述 + guide 的 event-spec 示例改用 `EventSpec` helper。**按 feedback_discuss_before_doc_update 原则**：本条是文档补录，不涉及决策变更。 | 1h | `docs/architecture/202604242030-adr-kernel-wrapper-contract-observability.md` + `docs/guides/cell-development-guide.md` | discovered via PR #246 round-6 local sweep |
| PR266-METRICS-CONFIG-EVENT | **CONFIG-EVENT-CONSUMER-METRICS-01** (P2, Cx2, 🟡 可延后): PR#266 round-2 P2-2 把 `cells/accesscore/slices/configreceive/service.go` + `cells/configcore/slices/configsubscribe/service.go` 的 per-event Info 日志降到 Debug，但 metrics 部分 defer：现有 `runtime/observability/metrics` 只有 HTTP-request `RecordRequest`，没有 generic event-processed counter helper。**修复**：扩 metrics provider 加 `RecordEventProcessed(cell, slice, outcome)`（outcome 枚举 ack/stale/reject/permanent_error），两个 config consumer 注入并自增 `gocell_config_event_processed_total{cell, slice, outcome}`；后续 audit/order/iot consumer 复用同一接口。**对标**：Watermill `metrics.NewPrometheusMetricsBuilder().DecorateSubscriber()` event-counter 模式；OpenTelemetry messaging semantic conventions `messaging.consumer.processed_messages`。 | 2-3h | `runtime/observability/metrics/provider_collector.go` + `cells/{accesscore/slices/configreceive,configcore/slices/configsubscribe}/service.go` | PR#266 round-2 P2-2 defer |
| PR266-AUDITAPPEND-STRICT | **AUDITAPPEND-STRICT-UNMARSHAL-01** (P2, Cx2, 🟡 可延后): PR#266 round-2 review 提出 `cells/auditcore/slices/auditappend/service.go:102` 用宽松 `json.Unmarshal` 接受未知字段，与 audit 合规"严格记录"语义存在张力。当前是 best-effort by design（audit ingestion 遇到 unknown payload 走 lenient path 让事件不丢），但应明确：(a) 注释说明 lenient 选择背后的合规取舍；(b) 提供 strict 模式 toggle 给将来需要严格审计的 deployment；(c) 永久错误路径（PermanentError → DLX）的 unmarshal 失败行为与 lenient 路径区分。**修复方向**：先做 (a) 注释加强，(b)(c) 待第一个 strict-audit 客户出现时实施。 | 2h | `cells/auditcore/slices/auditappend/service.go` | PR#266 round-2 OPS reviewer F-OPS-03 |
| PR266-NOOP-TX-RUNNER-DOC | **NOOP-TX-RUNNER-DOC-EXPAND-01** (P2, Cx1, 🟡 可延后, 美化项): `kernel/persistence/tx.go:16-34` `NoopTxRunner` 注释已说明 DurabilityDemo / Nooper 接口 / durable 拒绝路径，但缺一个明确的"何时使用 NoopTxRunner vs nil TxRunner"的对比段落。**修复**：补段落说明 (a) NoopTxRunner 是显式 demo signal，(b) nil 是未配置错误（构造函数 fail-fast），(c) RunnerOrNoop helper 把 nil 兜底成 NoopTxRunner 用于可选 Option 装配。 | 30min | `kernel/persistence/tx.go` | PR#266 round-2 OPS reviewer F-OPS-02 |
| PR266-CONTRACT-CLIENTS-ENFORCE | **CONTRACT-CLIENTS-RUNTIME-ENFORCE-01** (P2, Cx3, 🟡 设计决策): `kernel/governance/helpers.go:21-55` 把 contract `Endpoints.Clients` 当治理元数据消费（depcheck/location 用），无 runtime call gating。如果将来希望"未声明 client 的调用方在运行时被拒"，需要新增：(a) `kernel/wrapper.ContractSpec` 携带 client allowlist；(b) HTTP middleware 校验 caller cell-id（X-Calling-Cell header 或 mTLS SAN）；(c) 失败路径的 audit 事件。**反方观点**：当前 contract 是声明性元数据，runtime gating 增加 fail-closed 复杂度且 cell-id 注入需要 service mesh 支持；保持 governance-only 是合理边界。**触发条件**：第一个明确"未授权 cell 调用"安全事件出现，或团队决定走 service mesh 路线。 | 1d | `kernel/wrapper/` + `runtime/http/middleware/` + 新增 audit event contract | PR#266 round-2 SEC reviewer F-SEC-04 |
| PR266-METADATA-ONLY-CONSUMER-BUSINESS | **CONFIG-METADATA-ONLY-CONSUMER-BUSINESS-01** (P2, Cx3, 🟡 业务接入): PR#266 把 `event.config.entry-upserted.v1` 改为 metadata-only（key+version），订阅者 contract 上明确"必须 GET /api/v1/config/{key} 回源取值"。`accesscore/configreceive` 当前是 ADV-05 强制的 placeholder consumer（只 log），真实业务接入（JWT TTL refresh、key rotation interval、policy reload）尚未落地；`configcore/configsubscribe` 内部维护 version cache 是正确实现（值由 caller 业务逻辑触发回源，consumer 不缓存值）。**修复方向**：(a) accesscore 接入：identifying 哪些 config key 触发 access policy reload + 注入 ConfigClient 接口（ports/configport.go）+ HandleEntryUpserted 内回源 + reload 业务；(b) 至少一个 caller demo 验证 metadata-only 闭环行为；(c) 扩 e2e test 覆盖回源行为（注入 mock ConfigClient + 断言被调用）。 | 4-6h | `cells/accesscore/internal/ports/` + `cells/accesscore/slices/configreceive/service.go` + `cmd/corebundle/outbox_e2e_integration_test.go` | PR#266 round-2 reviewer P2-1（架构/测试席位）|
| PR-A41-FU1 | **PR-A41-CLI-PRINTER-FOLLOWUP-01** (P2, Cx2, 🟡 可延后, 三子项打包单 PR): PR-A41 (PR#270) round-3 review 收尾的 3 条 P2 残项。**(a) GOVERNANCE-LOCATOR-PARENT-FALLBACK-LIFT**：当前 parent-fallback 实现在 `kernel/governance/locator.go::locate`（CH-03 missing leaf 才能拿到非零 Line/Column），逻辑层面应下沉到 `kernel/metadata.Locate` —— 其它消费者（cmd/depcheck/未来 check 子命令）也能复用。同时 `responses[N]` 整数 key 当前锚点是 value 首行（line 6），可加 `LocateAtKey` 选项返回 key 行（line 5）提升一行精度。**(b) VERIFY-FORMAT-JSON-E2E**：`gocell verify {slice\|cell\|journey} --format=json` 只有 printer 单测 + SARIF 拒收 E2E，没有"正向 JSON 路径"端到端测试（实际跑 `runVerify` + json.Unmarshal + 断言 `verifyResultJSON` schema）。补一个走 `runVerify` 入口的 E2E 测试。**(c) SARIF-SCHEMA-CI-GATE-EVAL**：PR-A41 round-2 因 `@microsoft/sarif-multitool@4.5.4` 在 GitHub runner 自身 init 即 crash（`FileVersionInfo.GetVersionInfo` empty `Assembly.Location`）删了 CI schema 校验步骤；当前依赖 golden 文件 + `printer_test.go::TestGolden_SARIF` re-parse 兜底结构层。如要恢复 SARIF spec 全量 schema 覆盖，评估换 ajv-cli + 官方 SARIF 2.1.0 schema（无 .NET 依赖），或用 Go 写轻量校验器。当前结构层 golden 兜底基本够用，列触发性低优项。 | ~5.5h（a 3h + b 0.5h + c 2h） | (a) `kernel/metadata/location.go` + `kernel/governance/locator.go`；(b) `cmd/gocell/app/scaffold_verify_test.go`；(c) `.github/workflows/_build-lint.yml` | PR-A41 (PR#270) round-3 reviewer P2 残项 |

### adapter

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| A8 | **CI-DIGEST-01** (🟡 可延后): testcontainers 镜像 tag+digest 双固定 | 1h | `adapters/*/integration_test.go` | PR#139 review |
| A9 | **CI-LINT-PIN-01** (🟡 可延后): golangci-lint patch 级固定 + dependabot | 1h | `.github/workflows/ci.yml` | PR#139 review |
| A10 | **OBS-LGTM-INTEGRATION-01** (Cx3, 🟡 可延后): `//go:build integration` 夜间 OTel collector 真实 OTLP 协议兼容性测试 | 2h | `adapters/otel/integration_test.go` | PR#157 review S6-04 |
| PR-A8-RESIDUAL | **VAULT-K8S-AUTH-E2E-01** (Cx2, 🟡 可延后): PR#230 K8s auth 仅单测覆盖（`kubernetesAuth` struct），无 e2e/集成测试演证 ServiceAccount 挂载 → JWT login → secret fetch 完整链路。**修复**：补 integration test（可用 kind/minikube 或 vault test container + mock SA token） | 4h | `adapters/vault/auth_test.go` + integration fixture | PR#230 合并后残余 |
| A21 | **HEALTH-CHECKER-CTX-BUDGET-01** (P2, Cx3, 🟡 可延后): `runtime/http/health.Checker` 签名为 `func() error`，聚合层 (`Handler.ReadyzHandler`) 顺序执行所有 checker 且无统一超时/并行策略。`TransitKeyProvider.Checkers["vault_transit_ready"]` 内部自控 3s context，但其他 checker（PGResource、broker health）预算各自为政；未来 checker 退化或新增时，`/readyz` 尾延迟会叠加放大。**方案**：将 `Checker` 升级为 `func(ctx.Context) error`，`ReadyzHandler` 为整个聚合注入统一 deadline（如 2s）并考虑并发执行；失败时日志/响应输出每个 checker 的耗时与错误名称，用于运维定位。前置：需要 `kernel/lifecycle.ManagedResource.Checkers()` 签名同步升级。排期建议 R2 之后独立 PR 做。 | 3h | `runtime/http/health/health.go` + `kernel/lifecycle/managed_resource.go` + 所有 `Checkers()` 实现 | PR #205 review 2026-04-20 P2 finding |
| LATER-AL-R | **RMQ-STATUS-DIAGNOSTIC-FIELDS-01** (P2, Cx2, 🟡 可延后): PR-A18 已完成 RabbitMQ Connection 接入 `lifecycle.ManagedResource`（Health probe 维度），但 `ConnectionStatus()` 仍返回裸 enum，缺结构化诊断字段。**修复**：重构为 `ConnectionState{state, message, lastError, lastDisconnectAt, reconnectAttempts}`，供 ops dashboard 诊断 broker 抖动 / 重连历史；与 PR240-OB1 Vault cache version metric 同属"诊断字段"族。**理由**：当前 enum 只能告知"connected/disconnected"，无法回答"为什么断开 / 上次断开多久了 / 重连了几次"。 | 3h | `adapters/rabbitmq/connection.go` | graceful-backus §架构 P2 LATER-AL-R + backlog_later_detail §3 |
| LATER-T-1 | **ADAPTER-TSKIP-BACKFILL-01** (P2, Cx3, 🟡 可延后): 6 个主干 adapter 共 15 处 `t.Skip` 黑盒盲区（缺 testcontainer 实跑），代码路径无法在 integration 层验证。**修复**：审计每处 t.Skip 来源（缺 docker / 缺 schema / 缺 fixture），分批补 Testcontainer 配置；优先补"重连 / 错误恢复 / TLS 拒收 / 死信路由"等运行时关键路径。 | 1-2d | `adapters/postgres/*_test.go` + `adapters/redis/*_test.go` + `adapters/rabbitmq/*_test.go` + `adapters/vault/*_test.go` + `adapters/otel/*_test.go` | graceful-backus §问题 P2 测试覆盖 LATER-T-1 |
| PR225-N1 | **RMQ-WAITANDCLOSE-ABANDONED-GOROUTINE-TEST-01** (Cx1, 🟡 可延后): `adapters/rabbitmq/subscription_run.go` `waitAndClose` 在 ctx 超时返回后，内部 goroutine 仍跑完 `localWg.Wait()` 再 `close(done)`；`done` 是无缓冲 channel 且无接收方，属于 intentional abandoned-on-timeout 模式（与 fx StopTimeout 语义一致），但缺一个回归测试确认 goroutine 最终随 processDelivery 完成而退出。 | 2h | `adapters/rabbitmq/subscription_run.go` + `subscription_run_test.go` | PR#225 reviewer 第二轮 N1（2026-04-24） |
| PR237-F06 | **DUAL-LISTENER-DEVMODE-WARN-TEST-01** (Cx1, 🟡 可延后): PR-A14a reviewer 留尾：`cmd/corebundle/shared_deps.go::internalGuardFromEnv` 在 dev 模式（`GOCELL_ADAPTER_MODE=""`、未设 `GOCELL_SERVICE_SECRET`）返回 nil guard 并发出 `slog.Warn`，当前无测试断言该 Warn 被触发。添加 `internal_guard_env_test.go` table-driven 覆盖 dev/real × 有/无 SERVICE_SECRET 四象限。 | 30min | `cmd/corebundle/internal_guard_env_test.go`（新） | PR #237 reviewer F-06 |
| PR237-F13 | **TESTNEW-PORT-COLLIDE-RENAME-01** (Cx1, 🟡 可延后): PR-A14a reviewer 留尾：`runtime/bootstrap/bootstrap_test.go::TestNew_WithOptions` 断言 `primaryAddr == ":9090"`，与 PR-A14a 新增的 internal-listener 默认端口 `:9090` 命名撞名（语义正确，但读起来有二义性）。重命名为 `:7070` 或类似值以避免跟默认值混淆。 | 10min | `runtime/bootstrap/bootstrap_test.go` | PR #237 reviewer F-13 |
| PR237-T1 | **DUAL-LISTENER-TEST-GAPS-01** (Cx1, 🟡 可延后): PR-A14a 第二轮六角色 review 测试维度 10 条 Cx1 gap 合并登记：(T-01) FinalizeAuth 排序不变量——duplicate 与 Delegated 不一致交叉用例；(T-02) `shutdownBothServers` 部分错误聚合未测（只有 clean path）；(T-03) httpErrCh 双关闭在 `-race` 下未覆盖；(T-04) 测试仅覆盖 `owned=false` primary-listener 路径，`owned=true` 分支零覆盖；(T-05) `WithInternalMiddleware` 多个 mw 的顺序保真；(T-06) `FinalizeAuth` 二次调用的错误文本；(T-07) 有 metas 未 FinalizeAuth 的 `ServeHTTP` panic 文本；(T-08) 确认 `auth_integration_test.go` 的 `//go:build integration` tag 存在；(T-09) `TestAuthWiring_InternalGuard_*` 对 internal listener 的 wait-for-ready；(T-10) `runtime.NumGoroutine` baseline 捕获时机偏早。 | 3h | `runtime/http/router/*_test.go` + `runtime/bootstrap/*_test.go` + `cmd/corebundle/auth_integration_test.go` | PR #237 第二轮 reviewer (testing 维度) |
| PR237-OB2 | **INTERNAL-LISTENER-METRICS-01** (Cx2, 🟡 可延后): PR-A14a 的 internal mux 未挂 `middleware.Metrics`——`http_requests_total` / `http_request_duration_seconds` 完全不记录 /internal/v1/* 流量。方案：(a) 给 collector 加 `listener` 标签，两个 mux 用同一 Provider 不同 label；或 (b) 新增 router.WithInternalMetricsCollector option + 第二个 collector 实例。运营可见度缺失非 P0，且 Provider 接口改造属于更大议题，暂延 PR-A14b 或单开独立 PR。 | 4h | `runtime/http/router/router.go` + `runtime/http/middleware/metrics.go` + `runtime/bootstrap/bootstrap_phases.go::autoWireHTTPMetricsCollector` | PR #237 第二轮 reviewer OB-F2 |
| PR237-DX1 | **LISTENER-OPTION-NAMING-UNIFY-01** (Cx2, 🟡 可延后): PR-A14a 选项前缀不对称——`WithHTTPPrimaryAddr` / `WithHTTPInternalAddr`（带 HTTP）与 `WithPrimaryListener` / `WithInternalListener`（不带 HTTP）。IDE 补全时两组选项不相邻。方案：统一为 `WithHTTP*Listener` 或去掉 addr 侧的 HTTP 前缀。当前只有 gocell 自身无外部调用方，可在任意时间一次改名。 | 2h | `runtime/bootstrap/bootstrap.go` + 所有测试 + `cmd/corebundle` + `examples/*/main.go` | PR #237 第二轮 reviewer DX F3 |
| PR237-A2 | **ROUTEDECL-DELEGATED-RENAME-01** (Cx2, 🟡 可延后): `auth.Route.Delegated` / `kernel/cell.AuthRouteMeta.Delegated` 字段名保留了 pre-PR-A14a 的"JWT 验证下放"语义，但 PR-A14a 后它只是 FinalizeAuth 对 `/internal/v1/*` 路由的 **一致性标记**，不再驱动任何 runtime predicate。rename 为 `Internal bool` 更贴合新职责。本 PR 仅更新 godoc；rename 涉及 kernel/cell 公共接口 + 16 处调用站点，作为独立 PR 一次做完。 | 2h | `kernel/cell/registrar.go` + `runtime/auth/routedecl.go` + `runtime/http/router/router.go` + `runtime/bootstrap/bootstrap_phases.go` + `cells/accesscore/slices/rbacassign/handler.go` + 所有测试 | PR #237 第二轮 reviewer architect A2 |
| PR237-A4 | **INTERNAL-PREFIX-CONSTANT-RELOCATE-01** (Cx2, 🟡 可延后): `runtime/http/router/router.go::internalPathPrefix = "/internal/v1/"` 是路由包私有常量，但 `.claude/rules/gocell/api-versioning.md` 将 `/internal/v1/` 作为跨 runtime 的控制面前缀。若后续出现 v2-internal，router 包不是正确位置。建议迁到 `pkg/httputil/prefix.go` 或类似位置，与 `PublicAPIV1Prefix` 一起作为单一来源。 | 1h | `pkg/httputil/prefix.go`（新） + `runtime/http/router/router.go` | PR #237 第二轮 reviewer architect A4 |
| PR240-A1 | **MANAGEDRESOURCE-CONTRACT-FORMALIZE-01** (Cx2, 🟡 可延后): PR-A18 把 RabbitMQ Connection 接入 `lifecycle.ManagedResource`，叠加 Vault / Postgres / Redis 已经接入，`ManagedResource` 已成事实上的 adapter 生命周期契约。建议：(a) `kernel/lifecycle/doc.go` 写实施清单（Worker() != nil 何时合理 / Checkers 命名后缀 / Close 排干约定）；(b) 加 archtest 强制 `adapters/*` 顶层 exported 类型实现 `ManagedResource` 或显式 opt out。 | 4h | `kernel/lifecycle/doc.go`（新） + `kernel/governance/rules_arch.go` | PR #240 L4 architect 跨切面 finding 1 |
| PR240-A2 | **PROBE-NAMING-CONVENTION-RULE-01** (Cx1, 🟡 可延后): PR-A18 后 probe 名出现两种风格——`vault_transit_ready` / `rabbitmq_ready`（snake_case + `_ready`）vs outbox-relay 的 `outbox-relay-poll/reclaim/cleanup`（kebab-case + role）。约定仅靠 review 维护，下一个 adapter 会再引入第三种风格。建议在 `.claude/rules/gocell/observability.md` 写入命名规则。 | 30min | `.claude/rules/gocell/observability.md` | PR #240 L4 architect 跨切面 finding 2 |
| PR240-OB1 | **VAULT-CACHE-VERSION-METRIC-01** (Cx2, 🟡 可延后): PR-A18 `cachedLatestVersion` lock-free 缓存对正确性安全（keyID 来自 Vault 响应），但多 pod 缓存陈旧时 SRE 无法诊断"为什么 Pod X 报 keyID v3 而 Pod Y 报 v4"。加 `gocell_vault_cached_key_version` gauge（每次 `Current()` cache hit 时 emit）+ `slog.Debug` 字段。仅诊断价值，无修复路径。 | 2h | `adapters/vault/transit_provider.go` | PR #240 L4 devops finding 6 |
| PR240-DX1 | **RMQ-MANAGEDRESOURCE-WIRING-EXAMPLE-01** (Cx1, 🟡 可延后): PR-A18 删了 `WithBrokerHealth` 改走 `WithManagedResource(conn)`，`adapters/rabbitmq/doc.go` 写了 migration note 但无可运行示例。当 cmd/corebundle 真正接入 RMQ 时（目前只有 InMemoryEventBus）随手补一段 5 行 wiring snippet：`conn, _ := rabbitmq.NewConnection(...)` → `bootstrap.New(WithManagedResource(conn), WithPublisher(rabbitmq.NewPublisher(conn)), WithSubscriber(...))`。 | 30min | `adapters/rabbitmq/doc.go` 或 `docs/patterns/rabbitmq-wiring.md`（新） | PR #240 L4 product-manager DX finding |
| PR240-DOC1 | **VAULT-DOC-AAD-DESCRIPTION-STALE-01** (Cx1, 🟡 可延后, 文档归档): `docs/archive/202604191800-adr-config-value-encryption.md:173-181` 描述"Passes the AAD as the `context` field"——pre-R1c bug 的描述，PR#205 后已不发送 AAD 给 Vault，PR-A18 进一步改 datakey 路径。归档 ADR 加 strikethrough 注解避免误导读者。 | 15min | `docs/archive/202604191800-adr-config-value-encryption.md` | PR #240 L4 devops finding 2 |
| PR237-A5 | **PR-A14B-SCOPE-REWRITE-01** (Cx1, 🟡 可延后): PR-A14a 彻底重构 + 吸收 PR-A32 后，PR-A14b 剩余范围（声明式 RouteGroup API + health listener + mTLS + 编译期 listener ref 校验）与原 plan block 不匹配。plan 需更新说明物理双 mux 已在 14a 完成、14b 聚焦声明式 API + 可选第三 listener。 | 30min | `docs/plans/202604232330-025-architecture-pr-implementation-plan.md:444+` | PR #237 第二轮 reviewer architect A5 |
| PR237-A6 | **BOOTSTRAP-LISTENER-SLICE-01** (Cx2, 🟡 可延后, 🟠 PR-A14b 触发): `phase7StartHTTPServer` 硬编码 2 个 server；未来 3rd listener（health 独立 / admin 独立）需要改 phase 本身。`serveAndReport` 已经泛化于 (name, srv, ln)，extensibility 半完成。方案：重构 phase7 迭代 `[]listenerBinding{name, handler, addr/ln}` slice（PR-A14b 的 RouteGroup API 自然拥有该 slice）。 | 2h | `runtime/bootstrap/bootstrap_phases.go` | PR #237 第二轮 reviewer architect A6 |
| PR237-PM5 | **DUAL-LISTENER-DEPLOYMENT-GUIDE-01** (Cx2, 🔴 发布前必做, 🟡 可延后至发布前 docs 阶段): `docs/operations/` 缺双 listener 部署章节——未描述 (a) LB/ingress 只映射 8080，(b) internal listener VPC-only 绑定（启用 mTLS 或 service-token），(c) 从单 listener 迁移的现有部署改造步骤。方案：新增 `docs/operations/dual-listener-deployment.md` 或在 first-run-setup 文档扩写一节。 | 2h | `docs/operations/dual-listener-deployment.md`（新）或 `docs/operations/first-run-setup.md` | PR #237 第二轮 reviewer product PM-05 |
| PR237-PM7 | **EXAMPLE-INTERNAL-LISTENER-COMMENT-01** (Cx1, 🟡 可延后): `examples/{ssobff,todoorder,iotdevice}/main.go` 的双 addr 配置缺注释，读者不知道为什么 todoorder/iotdevice 这类不暴露 /internal/v1/* 路由的 example 也要配第二个端口（答：当前 internal addr 无 "disabled" 默认，必填）。方案：在每个 example 双 addr 行补一行注释；或实现 `WithHTTPInternalDisable()` 允许单 listener 模式（但与 PR-A14a 物理隔离契约冲突，权衡后再定）。 | 1h | `examples/*/main.go` + `runtime/bootstrap/bootstrap.go` (optional new option) | PR #237 第二轮 reviewer product PM-07 |
| PR239-S1 | **PATH-PARAM-UUID-RUNTIME-VALIDATION-01** (Cx2, P1 安全, 🟠 多 pod 生产前触发): PR-A9 合约元数据声明 `format: uuid` 的 path param 在 runtime handler 层未 `uuid.Parse` 校验；恶意客户端可传任意字符串穿透 `r.PathValue("id")`。涉及 identitymanage/sessionlogout/rbaccheck 等多处。**修复**：在每个读取 UUID pathParam 的 handler 入口加 `uuid.Parse(id)`，非法值返回 400。Cx2（~4 个 handler 文件） | 2h | `cells/accesscore/slices/identitymanage/handler.go` + `cells/accesscore/slices/sessionlogout/handler.go` + `cells/accesscore/slices/rbaccheck/handler.go` | PR#239 六角色 review round 1（S3 安全席位） |
| PR239-S2 | **INTERNAL-GUARD-REQUIRED-01** (Cx3, P1 安全, 🔴 多 pod 生产前必做): `runtime/auth/middleware.go:51-54` `/internal/v1/*` 使用 `Delegated: true` 路径跳过 JWT 验证；如 composition root 未显式调用 `WithInternalPathPrefixGuard`，内部端点依赖 `AnyRole(RoleInternalAdmin)` 的 Principal 存在假设。**修复**：在 `FinalizeAuth` 阶段将 delegated 路径存在作为 fail-fast 校验，要求 guard 必须注册；或在 bootstrap layer 强制默认 InMemoryGuard（与 S-nonce 类似）。需 architect 决策 | 4h | `runtime/bootstrap/bootstrap.go` + `runtime/http/router/router.go` | PR#239 六角色 review round 1（S4 安全席位） |
| PR239-S3 | **CONTRACT-4XX-COMPLETENESS-01** (Cx1, P2, 🟡 可延后): `http.auth.login.v1` Public 端点缺 `responses[400]`（bad credentials / malformed request）声明；`http.auth.refresh.v1` 已在 PR#251 补齐 400/401/503。其他已声明端点仍需扫 handler 实际会返回的 4xx，补齐声明 | 3h | `contracts/http/**/contract.yaml` | PR#239 六角色 review round 2（S4 安全席位） |
| PR239-T1 | **SCAFFOLD-SMOKE-01** (Cx1, 🟡 可延后): 缺 `gocell scaffold contract --kind=http` 生成文件能通过 `gocell validate --strict` 的端到端冒烟测试。**修复**：`kernel/scaffold/scaffold_test.go` 或新建 `scaffold_smoke_test.go` 加一条 case：CreateContractHTTP → Validator 通过（预期 FMT-13 失败因模板 TODO placeholder 无 pathParams，这本身是 DX 设计；断言错误码而非通过） | 1h | `kernel/scaffold/scaffold_test.go` | PR#239 六角色 review round 2（测试席位） |
| PR239-T2 | **EXTRACTPATHPLACEHOLDERS-WHITEBOX-01** (Cx1, 🟡 可延后): `kernel/governance/rules_fmt.go::extractPathPlaceholders` 无独立白盒单元测试；去重逻辑、空串、非法命名（`{123abc}`）、chi 原生语法（`{name-with-dash}` / `{name:regex}`）的 silent miss 行为仅间接覆盖。**修复**：新增 `rules_fmt_test.go::TestExtractPathPlaceholders` table-driven | 1h | `kernel/governance/rules_fmt_test.go`（新） | PR#239 六角色 review round 2（测试席位） |
| PR239-O1 | **CI-VALIDATE-STRICT-FLAG-01** (Cx1, 🟡 可延后): `.github/workflows/_build-lint.yml` 的 `gocell validate` 未加 `--strict` flag；FMT-16 / FMT-C1 / FMT-A1 / DOC-NAME-01 等 strict-only 规则在 CI 不会 fire。**修复**：改为 `go run ./cmd/gocell validate --strict`。预存在问题，PR-A9 六角色 review round 1 发现 | 10min | `.github/workflows/_build-lint.yml` | PR#239 六角色 review round 1（运维席位） |
| ~~PR239-DX1~~ | ~~**FMT-13-HOW-TO-FIX-HINT-01**~~ ✅ PR-A10 搭车：`kernel/governance/rules_fmt.go` FMT-13 错误消息追加 YAML fix 片段（`pathParams:\n  <name>:\n    type: string`），`TestFMT13_HasHowToFixHint` 锁住格式。 | — | — | — |
| ~~PR239-OB1~~ | ~~**CONTRACT-HEALTH-HTTP-COLUMNS-01**~~ ✅ PR-A10 搭车：`cmd/gocell/app/check.go::printContractHealthTable` 增加 `METHOD / PATH` 两列；非 HTTP / 缺字段渲染 `-`。 | — | — | — |

### slice / cell 收口

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| S13-follow | **4XX-LOG-SAMPLING-01** (P3, Cx1, 🟡 可延后): 高频端点（如 GET config）4xx 日志可加采样（1%）或在 rate-limiter 触发后限速，避免大量 429 日志淹没告警通道。注：PR#181 F3 已把 log4xx 字段最小化（移除 client message，仅保留 code/status/correlation IDs/internal），采样必要性已大幅下降；仅在真实运维告警通道过载时再做 | 1h | `pkg/httputil/response.go` | PR-CONFIG-POLISH 后续 |
| S14a | **CONFIG-VALUE-KMS-AWS-PROVIDER-01** 🟠（明确部署目标云平台后触发）：基于 KeyProvider 接口加 AWS-KMS / GCP-KMS adapter；envelope encryption（DEK 本地 + KEK 在 KMS）；rotation 走 KMS API；dev fallback LocalAES。**前置 S14 CONFIG-VALUE-ENCRYPTION-01 ✅ PR#195 已完成（LocalAES + VaultTransit）**。**触发**：明确生产云平台 + 通过 KMS 安全评审 | 6h | 🟠 | `runtime/crypto/aws_kms_provider.go`（新）+ `gcp_kms_provider.go`（新）+ `cmd/corebundle/bundle.go` env 解析 | PR-CC-VALUE-ENCRYPT 后续 |
| ~~S19~~ | ~~**JWT-AUDIENCE-DRIFT-INTEG-TEST-01**~~ ✅ PR#263 (PR-A30) 合入：`cells/accesscore/auth_integration_test.go::loginAndGetPair` 重构为 options + struct return（`withIssuerAuds` / `withVerifierAuds` 注入 drift），新增 `TestAuthIntegration_LoginAccessTokenAudienceDrift` 覆盖 issuer-drift / verifier-drift / multi-aud-one-match / aligned 四象限 | — | — | — |
| ~~S21~~ | ~~**JWT-AUD-TEST-TABLE-DRIVEN-01**~~ ✅ PR#263 (PR-A30) 合入：`runtime/auth/jwt_aud_test.go` 13 个 Test\* 折叠为 3 个 table-driven（`TestJWTVerifier_VerifyIntent_AudienceTable` 9 行 + `TestJWTIssuer_DefaultAudience_Table` 3 行 + `TestNewJWTVerifier_NoAudiences_ReturnsError`） | — | — | — |
| ~~S22~~ | ~~**REFRESH-AUD-REAL-ROUTE-TEST-01**~~ ✅ PR#263 (PR-A30) 合入：新 `cells/accesscore/auth_refresh_aud_integration_test.go::TestAuthIntegration_RefreshAccessTokenAudienceDrift`，refresh token 已 opaque（PR-A29），audience 在 response access JWT 上验证 | — | — | — |
| S23 | **AUTH-WALKTHROUGH-COMPOSE-01** (P2, Cx3, 🟡 可延后): `examples/ssobff/walkthrough_test.go` 手装精简 server（无 bootstrap lifecycle），与 `main.go` 的真实组装路径、README 步骤三份语义各自独立，可掩盖 public-endpoint wiring / configcore 接线 / audit event delivery 的回归。**修法**：提取 `NewSSOBFFApp(opts...)` 组装函数被 `main.go` 和 walkthrough test 共用；test server 走同一 bootstrap + Start/Stop 路径。来源: PR#172 review F3（OUT_OF_SCOPE） | 4h | `examples/ssobff/bootstrap.go`（新）+ `main.go` + `walkthrough_test.go` | PR#172 review F3 |
| ~~S24~~ | ~~**AUTH-MIDDLEWARE-AUD-REFRESH-E2E-01**~~ ✅ PR#263 (PR-A30) 合入：新 `runtime/auth/middleware_aud_e2e_test.go` 单一 table test 覆盖 `httptest.NewServer` + `AuthMiddleware` 完整链路（right/wrong/missing aud → 200/401/401） | — | — | — |
| L11 | **GOVERNANCE-CI-MAINBRANCH-01** (Cx1, 🟡 可延后): governance strict gate CI workflow 仅挂 develop 分支，main/release 路径可绕过同等级治理门禁。**修复**：`.github/workflows/governance.yml` 扩展 `branches` 至 `[develop, main, 'release/**']`。 | 0.5h | `.github/workflows/governance.yml` | #210-review 2026-04-21 审查 |
| L7 | **FMT15-NEXTCURSOR-ENFORCE-01** (Cx1, 🟡 可延后): 列表响应治理规则（FMT-14）只强制 `hasMore`，未强制 `nextCursor`，导致接口形态漂移（部分 slice 返回 `nextCursor` 为空字符串而非 omitempty）。**修复**：`kernel/governance/rules_fmt.go` 新增 FMT-15：响应含 `hasMore` 时必须同时含 `nextCursor`（可为空但字段必须存在）；补对应 validate 测试。 | 2h | `kernel/governance/rules_fmt.go` + 对应 validate 测试 | 2026-04-20 分层审查 |
| L8 | **PAGINATION-HELPER-EXTRACT-01** (Cx2, 🟡 可延后): `ParsePageRequest` + `slog.Warn` + `WriteDomainError` 三行分页错误处理模式在 auditquery、configwrite、flagwrite 等多处 slice handler 重复，观测语义分散。**修复**：`pkg/httputil/pagination.go` 提取公共 helper；各 slice handler 统一引用，消除重复。 | 2h | `pkg/httputil/pagination.go`（新）+ 各 `cells/*/slices/*/handler.go` | 2026-04-20 分层审查 |
| L9 | **EXAMPLES-CONTEXT-NOOP-01** (Cx1, 🟡 可延后): `examples/` 下自定义 `noopTxRunner` 吞掉上游 context，破坏取消传播语义；且与 `persistence.NoopTxRunner` 形成分叉。**修复**：删除 examples 层自定义实现，改用 `persistence.NoopTxRunner`（或等价基础实现），恢复 context 传播。 | 1h | `examples/*/` 相关文件 | 2026-04-20 分层审查 |
| FEAT-1-残余 | **RBACCHECK-PAGINATION-REAL-01** (Cx2): PR#221 devicelist 主体完成，但 `cells/accesscore/slices/rbaccheck/handler.go:79` `handleListRoles` 仍硬编码 `hasMore:false`（`map[string]any` 响应，无 `nextCursor`）。**修复**：迁 `query.PageResult[RoleResponse]` + 真实游标分页（改 service+repository 层）；补 `rbaccheck/contract_test.go` 的 `MustRejectResponse` 负向断言。 | 3h | `cells/accesscore/slices/rbaccheck/handler.go` + `rbaccheck/contract_test.go` | PR#214 review + PR#221 搭车项 |
| T6 | **GOCELL-PER-CELL-ADAPTER-01** (Cx2, **PR-X-PG-REPO-ACCESS 强制前置**): 全局 env 读取拆为单 cell 级 adapter 配置，支持 cell-per-adapter；当前 `cmd/corebundle/bundle.go` env 解析共享，无法支持多 cell 不同 PG/Redis 实例。 | 2h | `cmd/corebundle/` + cell 配置 | `docs/plans/20260418-pr-c2-and-phase-x-detailed.md` T6（未登记） |
| PR220-e2 | **GENERATED-BOUNDARY-STRATEGY-01** (Cx2): `assemblies/corebundle/generated/boundary.yaml` 缺失；`gocell validate --strict` 只给 `REF-16` warning。**决策**：若 boundary 作为生成事实源 → 进入 regenerate-and-diff 门禁；若不维护 → 降低活动验证地位。 | 1h | `kernel/governance/` + `.github/workflows/` | PR#220 遗留报告 额外项 |
| PR220-e3 | **STATUS-BOARD-J-ORDERCREATE-01** (Cx1): `journeys/J-ordercreate.yaml` 存在但 `journeys/status-board.yaml` 无对应条目（ADV-01 warning）；且 J-ordercreate 有一个 auto criterion 缺 `checkRef`，verify 会失败。**修复**：补 status-board 条目 + checkRef；或标明非活动 / 移出活动目录。 | 0.5h | `journeys/status-board.yaml` + `journeys/J-ordercreate.yaml` | PR#220 遗留报告 额外项 |
| ~~PR238-FU1~~ | ~~**CONFIGCORE-DECRYPT-CATEGORY-AUTH-EVAL-01**~~ ✅ PR#271 (PR-A50+A51) 落地：`cryptoOpError` 改为 cause-aware 分类，覆盖 encryptValue/decryptValue/encryptVersionValue/decryptVersionValue 全部四条路径：(1) ctx cancel cause → `ErrClientCanceled` (HTTP 499)；(2) `ErrKeyProviderTransient` cause → 保留 `CategoryInfra`（Vault outage 仍触发 infra 告警）；(3) 其他 cause → `CategoryAuth`（KMS 拒绝 / GCM tamper）。`IsInfraError` 不再把真实 KMS 鉴权失败误归 infra bucket，但保留对 transient/ctx-cancel 信号的 infra 标记。 | — | — | — |
| PR238-FU3 | **CONFIGREPO-CTX-CANCEL-INTEG-TEST-01** (P2, Cx1, 🟡 可延后): PR#238 单测覆盖 `ctxCanceledError` 所有 4 个 call site（GetByKey / Update-SELECT / Update-RETURNING / Delete / GetVersion），但 `config_repo_integration_test.go` 对真实 PG 未验证 ctx cancel → `IsInfraError(true) + !IsDomainNotFound` 端到端路径。**修复**：新增 `TestConfigRepo_CancelledCtx_AgainstRealPG`：pre-cancel ctx，调 `GetByKey`，断言 `IsInfraError(err) && !IsDomainNotFound(err, ErrConfigRepoNotFound)`。 | 1h | `cells/configcore/internal/adapters/postgres/config_repo_integration_test.go` | PR#238 L4 reviewer T-05 |
| PR238-FU4 | **CONFIGREPO-LEGACY-NOTFOUND-TEST-DEDUP-01** (P3, Cx1, 🟡 可延后): `config_repo_test.go` 中 `TestConfigRepository_GetByKey_NotFound`（line ~172）和 `TestConfigRepository_GetVersion_NotFound` 用 `assert.AnError` 测试的是 other-error 分支，与 `TestGetByKey_OtherScanError_ReturnsErrConfigRepoQuery` / `TestGetVersion_OtherScanError_ReturnsErrConfigRepoQuery` 重复。保留会造成 mutation-test 误导。**修复**：删除两个 legacy 命名函数或把它们重构为 table 行。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo_test.go` | PR#238 L4 reviewer T-04 |
| PR238-FU5 | **CELL-SPLIT-LAYOUT-NORMALIZE-01** (P3, Cx2, 🟡 可延后): accesscore + configcore 已按 `cell.go + cell_init.go + cell_routes.go` 三文件范式拆分（PR#234 / PR#238）；但存在小不对齐：(a) `configDirectPublishMode` / `ensureCursorCodec` 放在 cell_init.go 命名偏重 init 但实际是 pure helper；(b) `RegisterSubscriptions` 放在 `cell_routes.go` 与文件名 "routes" 暗示不一致，两 cell 均如此。**修复**：auditcore（272 行，现在不拆）触发 cell 拆分时统一规范（引入 `cell_lifecycle.go` 或 `cell_external.go` 命名惯例），并反向迁移 accesscore/configcore。 | 2-3h | `cells/{accesscore,configcore,auditcore}/` | PR#238 L4 reviewer D2/D3/D6 |
| PR238-FU6 | **CONFIGREPO-CTX-CANCEL-OP-GRANULARITY-01** (P3, Cx1, 🟡 可延后): `ctxCanceledError` 的 `op` 字段目前仅区分 `GetByKey` / `Update` / `UpdateForRollback` / `Delete` / `GetVersion`，但 `Update` 包含 SELECT-FOR-UPDATE 和 UPDATE-RETURNING 两阶段，ctx-cancel 发生在哪一阶段对运维定位锁竞争问题有意义。**修复**：细化 op 值为 `Update/select-for-update` vs `Update/returning`。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer OPS-L4-02 |
| ~~STALECIPHER-OPFIELD~~ | ~~**STALE-CIPHER-LOG-OP-FIELD-01**~~ ✅ PR#271 (PR-A50+A51) 落地：`cryptoOpError` 调用站点统一 op 字段为 PascalCase（`Encrypt` / `Decrypt` / `EncryptVersion` / `DecryptVersion`），与 `wrapCtxCancel` 的 `GetVersion` / `Update` 等保持一致命名。 | — | — | — |
| ~~FLAGREPO-CTXCANCEL~~ | ~~**FLAG-REPO-CTX-CANCEL-CLASSIFY-01**~~ ✅ PR#271 (PR-A50+A51) 落地：`FlagRepository.scanFlagOrMapError` + `wrapNonScanQueryErr` 通过 `pkg/ctxcancel.Wrap` 统一处理 ctx cancel；`flag_repo_ctx_cancel_test.go` 12 个 case 覆盖 GetByKey/Update/Delete/Toggle/Create/List。 | — | — | — |
| PR238-FU7 | **CONFIGREPO-CTXCANCEL-HARDENING-POLISH-01** (P3, Cx1, 🟡 speculative 延后): `ConfigRepository` 仍保留一条 speculative 防御项：`r.logger` 在 `NewConfigRepository` 中 nil-fallback 到 `slog.Default()`，若未来有人引入 `&ConfigRepository{db: x}` 零值构造（绕过构造函数）会 panic（`r.logger.WarnContext` 用于 stale-cipher 等路径）。**修复**：方法入口加 `if r.logger == nil { r.logger = slog.Default() }` 显式防御。**触发**：首次出现 `&ConfigRepository{}` 零值构造。**已 obsolete 子项（PR#271 落地）**：(b) `slog.WarnContext` 在 canceled ctx 下 OTel log exporter 提取 IDs 失败 — `wrapCtxCancel` 已不再发 slog.Warn（HTTP 层 log4xx 唯一权威）；(c) `cause` 字段 cardinality 漂移 — 同样无 slog 调用。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer SEC-R2-3 + OPS-L4-01 + OPS-L4-07 (PR#271 收敛 b/c) |
| PR238-FU8 | **CONFIGREPO-UPDATE-ROLLBACK-OP-LABEL-TEST-01** (P3, Cx1, 🟡 可延后): `doUpdate` 通过 `op` 参数向 `scanConfigOrMapError` 传 `"Update"` 或 `"UpdateForRollback"`，`InternalMessage` 会携带该 op。但 `TestConfigRepository_UpdateForRollback_NotFound` / `TestConfigRepository_UpdateForRollback` 均未断言 InternalMessage 含 "UpdateForRollback"，若有人把 op 硬编码回 "Update"，CI 不会 FAIL。**修复**：`UpdateForRollback` 相关 NotFound 测试追加 `assert.Contains(t, ec.InternalMessage, "UpdateForRollback")`。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo_test.go` | PR#238 L4 Round-2 reviewer T-R4 |
| ~~CTXCANCEL-HELPER-PKGLEVEL~~ | ~~**CTXCANCEL-ERROR-PKG-LEVEL-EXTRACT-EVAL-01**~~ ✅ PR#271 (PR-A50+A51) 落地：抽到 `pkg/ctxcancel/`（`Detect` / `Wrap`），configcore 全部走 helper。auditcore postgres adapter 后续在 PR#539 (PR271-FU3) 接入；accesscore 无 postgres adapter（mem / sessionmint），无需推广。 | — | — | — |
| ~~PR271-FU1~~ | ~~**CLIENT-CANCEL-REASON-GRANULARITY-01**~~ ✅ PR#539 (PR271-FU1) 落地：`ctxcancel.Wrap` 在 `*errcode.Error.Details` 中写入 `"reason": "canceled"` / `"deadline_exceeded"`；`pkg/httputil` 新增请求级 `WithCancelReasonSlot` / `CancelReason` ctx 槽，`writeErrcodeError` 在 499 出口把 reason 写槽；tracing middleware 在请求开始装槽、结束时读槽生成 `client.cancel.reason` 双值（无槽时仍回退 `"context_canceled"` 以兼容历史测试）。`ref: opentelemetry-go-contrib otelhttp/internal/semconv/client.go`；`ref: kratos transport/http/status` 区分 Canceled/DeadlineExceeded。 | — | — | — |
| ~~PR271-FU2~~ | ~~**CONFIG-ENCRYPT-FAILED-ERRCODE-SYMMETRY-01**~~ ✅ PR refactor/542 (PR271-FU2) 落地：引入 `ErrConfigEncryptFailed` 常量（与 `ErrConfigDecryptFailed` 对称，HTTP 500 映射），`encryptValue` / `encryptVersionValue` 调用站点切换到新 code；`TestCodeToStatus_Exhaustive` 自动覆盖；`TestConfigRepo_CryptoOpError_CauseAwareClassification` ops 表同步更新；新增 `TestEncrypt_FailEncrypt_RoutesToErrConfigEncryptFailed` 4 子用例验证 Create/UpdateForRollback/PublishVersion/cryptoOpError 路径。 | — | — | — |
| ~~FLAGREPO-CREATE-MSG-LEAK~~ | ~~**FLAG-REPO-CREATE-PUBLIC-MSG-KEY-LEAK-01**~~ ✅ PR refactor/542 (flag_repo 自纠) 落地：`FlagRepository.wrapNonScanQueryErr` 重构为 `*errcode.Error{Message: generic, InternalMessage: op+identifier}`；Create 调用站点 public Message 改为 `"flag repo: create failed"`，`flag.Key` 仅保留在 InternalMessage；`TestFlagRepo_Create_PublicMessageDoesNotLeakKey` 断言公共 Message 不含 key、InternalMessage 含 `"key=user_secret_flag_key"`。 | — | — | — |
| ~~PR271-FU3~~ | ~~**PERSISTENCE-CTXCANCEL-PROMOTE-AUDITCORE-01**~~ ✅ PR#539 (PR271-FU3) 落地：`pkg/ctxcancel.Wrap` 在 auditcore postgres adapter 的 5 个 IO 边界（Append exec / GetRange query / Query query / scanAuditEntries Scan / scanAuditEntries rows.Err）短路。**描述修正**：原 backlog 写的 accesscore 实际**没有 postgres adapter**（`cells/accesscore/internal/` 下只有 mem / sessionmint / domain），无需推广。governance archtest 强制使用 helper 仍归属 `PR238-FU2` 单独决策。`audit_repo_ctx_cancel_test.go` 24 个 case（5 IO × 4 cause variants + non-cancel infra fallback）覆盖。 | — | — | — |
| ~~PR271-FU4~~ | ~~**PKG-PERSISTENCE-CTXCANCEL-PLACEMENT-EVAL-01**~~ ✅ PR#539 (PR271-FU4) **已迁移**：`pkg/persistence/ctxcancel/` → `pkg/ctxcancel/`，删除空 `pkg/persistence/` 父目录。**层级评估结论**：`kernel/` 与 `runtime/` 都不合适——`pkg/httputil` 已依赖 `ctxcancel.DetailsKeyReason`，`pkg/` 严禁依赖 kernel/runtime/，上移会破坏现有依赖链；`ctxcancel` 也不具备 `kernel/persistence/TxCtxKey` 那种 cells↔adapters 桥接需求。**路径修正理由**：包注释自身写"IO operations (DB scan, RPC call, message bus claim)"，原 `persistence/` 段比实际范围窄；与 `pkg/` 兄弟（`aeadutil`/`ctxkeys`/`errcode`/`httputil`/`idutil`/`query`）的 capability-typed 平铺命名一致。 | — | — | — |
| PR-CFG-7 | **CONFIG-PILOT-E2E-CONDITIONAL-SKIP-01** (P2, Cx2, 🟡 测试基建 — batch3 业务修, 🟠 触发: PR-CFG-3 合并后): `tests/e2e/config_pilot_e2e_test.go` 唯一一条覆盖 HTTP → corebundle → PG → encryption → outbox → consumer 的 e2e 用例**全部无条件 `t.Skip("requires full assembly")`**；`tests/e2e/outbox_e2e_integration_test.go` 只证明消息上了总线，没证明真实 consumer side effect。**根因**：测试覆盖停在 repo/handler/outbox 边界，没把最容易漂移的装配链路跑起来——这正是 P1-1..P1-6 一系列问题能同时留在 develop 上不被发现的原因。**修复**：(a) skip 改为条件 skip（`requireDocker(t)` / `requirePG(t)` / `requireRMQ(t)`），CI 可达时跑；(b) 补一条断言：发布 entry → accesscore configreceive 落库副作用可见（PR-CFG-3 落地后此处写真实 metadata-event 消费，**不是占位符**）；(c) 在 `.github/workflows/_build-lint.yml` 的 integration-test job 把 `tests/e2e/...` 加进 scope。**前置**：PR-CFG-3 (EVENT-CONTRACT-BOUNDARY-RECOMPLY-01) 合并后做，否则 consumer 形态会再变一次，本 PR 要二次返工。**配套源 finding**：2026-04-25 config 域审查 P2-2（测试席位）。**Cross-ref**：与 F10 (TEST-JOURNEY-ASSEMBLY-HARNESS-01) 不同——后者是 28 条 journey skip，本条是 config 域单条 e2e。 | 1d | `tests/e2e/config_pilot_e2e_test.go` + `.github/workflows/_build-lint.yml` | 2026-04-25 config 域全链路审查 P2-2 |

### 发布 + 文档

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| PR-A37 | **DEVTOOLS-METADATA-EXPORT-01** (Cx2, 🟡 可延后, Wave 3): 新 `gocell export metadata [--format=json\|yaml] [--out=<path>] [--include-deps] [--root=<dir>]` CLI 子命令，复用 `kernel/metadata.NewParser` + `kernel/governance.DependencyChecker.buildDependencyGraph()` 一次性导出全量元数据 + cell 级依赖图 JSON。**目标**：解锁 `gocell-web` 仓库自包含 docker build —— 前端 `parser.ts` 当前用 Vite `import.meta.glob('../../../../../gocell/**.yaml')` 读 sibling repo，gocell-web 单独 clone 后 DevTools 元数据不可用。本 PR 落地后 gocell-web Dockerfile build 阶段调 `gocell export metadata --include-deps --out=public/metadata.json`，前端改为 `fetch('/metadata.json')`，零 CORS / 零 live endpoint 部署耦合。`schemaVersion: "v1"` 字符串字段固定 wire 形态，meta_struct_guard_test.go 已守 camelCase JSON tag。`--include-deps` 由 PR-A38 提供（A38 落地前 fail-fast）。**搭车**：无（独立主题，按用户决策不与 PR-A10 OUTPUT-JSON-SARIF 合并） | 1d | `cmd/gocell/app/export.go`（新）+ `kernel/metadata/export.go`（新）+ `kernel/metadata/export_test.go` + `kernel/governance/depcheck.go`（暴露 `Graph()` 方法）+ `docs/guides/devtools-metadata-export.md` + gocell-web `docs/backend-integration.md` 同步 | `gocell-web/docs/backend-integration.md` + 2026-04-25 plan 扩充讨论 |
| PR-A38 | **TOOLS-DEPGRAPH-01** (Cx3, 🟡 可延后, Wave 3, 依赖 PR-A37): 新模块 `tools/depgraph/`（**严禁放 `kernel/`** — 引入 `golang.org/x/tools/go/packages` 违反 CLAUDE.md "kernel 只依赖标准库 + pkg/ + yaml.v3" 硬规则；与 `tools/archtest/` 同级生态）。API：`Load(rootDir, opts) (*Graph, error)` → `Graph{Nodes []PkgNode, Edges []PkgEdge, RootModule string}`，`PkgNode = {ImportPath, Layer (kernel\|cells\|runtime\|adapters\|pkg\|cmd\|examples\|tools\|external), CellID, Files, LinesApprox}`。Layer/CellID 从 import path 段位推断。默认只扫 `github.com/ghbvf/gocell/...`，stdlib/第三方折叠成 `external/<module>` 单节点，`--include-stdlib` / `--include-thirdparty` 显式打开。输出 JSON（被 PR-A37 `--include-deps` 消费）+ DOT（开发者本地 graphviz 可视化）。**对标**：`loov/goda` 但只交付图模型基座，不复刻 reach/cut/nodes 表达式语言（前端可视化层处理）。**搭车不强制**：`LAYER-GO-IMPORT-01` 用 depgraph 替换 `tools/archtest/` 现有文件级 string scan（OUTBOX-SERVICE-03 等），可做传递闭包级校验，留作 follow-up PR-A39。 | 1.5-2d | `tools/depgraph/depgraph.go`（新）+ `graph.go` + `layer.go` + `cmd/depgraph/main.go`（独立 CLI） + `docs/guides/depgraph.md` + PR-A37 `cmd/gocell/app/export.go` 接入 `--include-deps` | 2026-04-25 plan 扩充讨论（PR-A37 衍生） |
| F2 | **SYSTEM-TOPOLOGY-API** (🟡 可延后): `GET /internal/v1/system/topology` 返回 cell/slice/contract 拓扑 JSON；基于 `kernel/registry` | 4h | 新 slice 或 `runtime/bootstrap/` | 历史 Batch 8 |
| F3 | **P2-T-02 audit e2e 测试**: Journey 级验收 | 2h | `journeys/` + integration test | 历史 Batch 8 |
| F4 | Review cells/ | 4h | — | Wave 4 |
| F5 | Review examples/ | 2h | — | Wave 4 |
| F6 | Review 报告汇总 | 2h | — | Wave 4 |
| F7 | 发布文档 | 4h | — | Wave 4 |
| F8 | 性能基准 | 4h | — | Wave 4 |
| F9 | **v1.0 tag** | — | — | Wave 4 |
| F10 | **TEST-JOURNEY-ASSEMBLY-HARNESS-01** (Cx3, 🟡 可延后): `tests/integration/journey_test.go` 全部 28 条均 `t.Skip("stub: requires full assembly")`；需要建 full-assembly harness 后统一恢复（J-sessionrefresh / J-sessionlogout / J-accountlockout / J-auditlogintrail / J-config-* / J-ssologin / J-useronboarding），不只 refresh 一条 | 8h | `tests/integration/` + assembly fixture | PR#166 R2-P2（范围扩大） |
| ~~MULTI-REVIEW-RES-1~~ | ~~**SETUP-410-MIGRATION-DOC-CONSOLIDATE-01**~~ ✅ PR#264 合入：`docs/operations/first-run-setup.md` 概览段重写为 `interactive` vs `bootstrap` 双模式说明 + `GOCELL_ACCESSCORE_ADMIN_PROVISION_MODE` 环境变量描述（接受 `""`/`"interactive"`/`"bootstrap"`）；多副本/HA 段落补充 same-source pending provenance 防止用户接管的语义；env-vars.md 同步登记新变量 | — | — | — |
| MULTI-REVIEW-RES-2 | **OUTBOX-FAILOPEN-SECURITY-EVENT-ALERT-01** (Cx2, 🟡 可延后, **范围已收窄**): PR-A5c F9（PR#245）已落 `kernel/outbox.Entry.FailurePolicy` per-entry 策略 + 三 cell 默认 `DirectPublishFailClosed` + archtest `OUTBOX-TOPIC-FAILOPEN-01` 静态守卫。**2026-04-25 更新**：原描述的 (3) "fail-open drop 频率超阈值时通过 `HealthContributor` 反馈到 /readyz `degraded`" 由 **PR-CFG-1**（READYZ-MANAGEDRESOURCE-AUTO-AGGREGATE-01）的通用聚合机制吸收；原 (1) counter + (2) archtest AST 升级合并到 **PR-CFG-6**（OUTBOX-EMIT-FAILOPEN-DROP-COUNTER-01）实施。**本条保留为索引指针**：实际工作在 PR-CFG-1 + PR-CFG-6，两者完成后本条可关闭。 | — | （指向 PR-CFG-1 / PR-CFG-6） | 2026-04-25 多 reviewer 复盘 #29 + 同日 config 域审查重组 |
| PR267-FU-IOTDEVICE-OWNER | **IOTDEVICE-HORIZONTAL-AUTHZ-OWNER-FILTER-01** (P2, Cx3, 🟠 触发: PR#267 PR-CFG-C 合并后): PR#267 reviewer 发现 `examples/iotdevice/cells/devicecell/slices/devicestatus` 等 device-scope 路由只做了角色级 RBAC，没有 OwnerID/principal 校验——operator A 拿到合法 token 即可读 device B 状态/命令。**根因**：本 PR 收口了"是否登录 + 是否角色匹配"，但"主体能否对该资源动作"属于 domain 层 ownership 模型，需要 `domain.Device.OwnerID` + `service.go` ownership filter，超出本 PR 路由层范围。**修复方向**：(1) `examples/iotdevice/cells/devicecell/internal/domain/device.go` 加 `OwnerID string`；(2) `slices/devicestatus/service.go` + `slices/devicecommand/service.go` 拒绝 `principal.Subject != device.OwnerID && !RoleAdmin`；(3) `cell_test.go` 加 operator A 看 device B → 403 用例。**前置**：无（独立于 PR-CFG-D 的 e2e 工作）。 | 4h | `examples/iotdevice/cells/devicecell/internal/domain/` + `slices/devicestatus/service.go` + `slices/devicecommand/service.go` + `cell_test.go` | PR#267 reviewer architect-finding round-2 |
| PR267-FU-TODOORDER-OWNER | **TODOORDER-OWNER-BASED-AUTHZ-01** (P2, Cx3, 🟠 触发: PR#267 合并后): PR#267 给 todoorder 路由挂上了 `RoleCustomer` 角色级 RBAC，但 `/api/v1/orders/{id}` 路径不携带 userId，`OrderResponse` 没有 OwnerID 字段，customer A 仍可读 customer B 订单。**根因**：与 PR267-FU-IOTDEVICE-OWNER 同型——本 PR 完成"角色边界"，"资源所有者边界"是独立 domain 模型加固。**修复方向**：(1) `examples/todoorder/cells/ordercell/internal/domain/order.go` 加 `CustomerID string`；(2) `slices/orderquery/service.go` 加 `auth.SelfOr(...)` 或 service 层 `principal.Subject` 过滤；(3) contract.yaml 决定是否让路径携带 customerId（`/api/v1/customers/{cid}/orders/{oid}`）或保留无业务标识 path 加 service 层守卫。**对标**：Vault KV v2 entity-namespaced 路径；K8s subresource RBAC。 | 4h | `examples/todoorder/cells/ordercell/internal/domain/` + `slices/orderquery/service.go` + `slices/ordercreate/service.go` + contract.yaml | PR#267 reviewer round-2 |
| PR267-FU-AUTHTEST-INTERNAL | **AUTHTEST-PACKAGE-INTERNAL-PATH-01** (P3, Cx3, 🟡 架构评估): PR#267 的 archtest AUTH-AUTHTEST-B/C 守住了 `runtime/auth/authtest` 的进口面，但守卫是动态测试。改为 `runtime/auth/internal/authtest/` 后由 Go 编译器拒绝 kernel/cells/examples 的 import（compile-time），archtest 中的 B/C 规则可退化为单一 internal 路径检查。**评估点**：迁移影响面（`runtime/bootstrap/*_test.go`、`runtime/http/router/*_test.go`、`kernel/cell/celltest/*` ——PR#267 已把 kernel/celltest 解耦，剩下两处都在 runtime/ 子树，internal 规则不会拒绝）；与现有 archtest 双层防御共存还是替换。**修复**：先做 dry-run import-graph 分析（参考 PR-A38 TOOLS-DEPGRAPH-01 落地后） → ADR 决策。 | 3h | `runtime/auth/authtest/` → `runtime/auth/internal/authtest/` + 各 _test.go import 路径 + archtest B/C 规则简化 | PR#267 reviewer architect FINDING-03 |
| PR267-FU-ROLE-PREFIX-ADR | **ROLE-NAMING-PREFIX-ADR-01** (P3, Cx2, 🟡 ADR 决策): 当前命名约定隐式分两档——平台共享角色裸名 (`RoleAdmin = "admin"`)，cell/example 私有角色带 `role:` 前缀 (`RoleOperator = "role:operator"`、`RoleCustomer = "role:customer"`)。该惯例没写在 CLAUDE.md / runtime-api.md，新 cell 加角色时容易选错前缀，token verifier 又是字符串比较，错配 silently 失败。**修复方向**：写 ADR `docs/architecture/role-naming-convention.md` 固定语义 + 在 `runtime/auth/authz.go` godoc 里加一段引用；`gocell scaffold cell` 模板可生成符合约定的 `dto/authz.go`。**不修代码**：现有命名按隐式约定已正确；只补文档。 | 1.5h | `docs/architecture/role-naming-convention.md`（新）+ `runtime/auth/authz.go` godoc | PR#267 reviewer round-2「prefix inconsistency」 |
| ~~PR267-FU-AUTHTEST-BUILDTAG~~ | ~~**AUTHTEST-BUILD-TAG-DEFENCE-01**~~ ❌ 不做（PR#267 reviewer round-2，2026-04-25 关闭）：建议给 `runtime/auth/authtest/*.go` 加 `//go:build !production` 防生产构建链入；现有 archtest AUTH-AUTHTEST-B/C 已守 import 面，build tag 维护成本大于收益且会让 `go test ./...` 默认包含 production-tag-stripped 文件，导致 vet/lint scope 漂移。**保留为决议记录** | — | — | PR#267 reviewer round-2（won't-do） |

---

## P3 长期 / 大型独立

| # | 任务 | 工时 | 前置 | 来源 |
|---|------|------|------|------|
| X1 | **PG-DOMAIN-REPO**: 5 个域 Repository PostgreSQL 实现（User/Session/Role/Device/Command）。前置: ① 4 migration DDL + **CONFIG-VERSIONS-MIGRATION-01** (`004_create_config_entries_and_versions.sql`)；② `ports.RoleRepository.CreateRole`。**落地联动**（同 PR 或紧邻 PR）: **RBAC-ASSIGN-LEVEL-UPGRADE-01** L0→L1；**SEED-ROLE-IFACE-01** 去 type assertion；**ACCESS-LEVEL-AUDIT-01** slice.yaml 校正；**AUTH-CACHE-01 激活** Redis session cache + 撤销失效路径 | 3-5d | — | PR#155 review F4 + 多 PR 汇总 |
| X2 | **WM-35 BFF handler 接入 cookie session** ★ | 2d | WM-2-F1 ✅ | 长期 roadmap |
| X3 | **WM-36 SecureCookie key rotation** ★ | 1.5d | WM-35 | 长期 roadmap |
| X4 | **WM-7 泛型 BulkResult** | 1d | — | 历史 Batch 8 |
| X5 | **P3-TD-11 accesscore domain 拆分** User/Session/Role | 4h | X1 | 历史 Batch 8 |
| X9 | **LINT-MODERN-01** (Cx2, P3): 全仓库 modernization baseline 清理（rangeint / stringsseq / forvar / inline / testingcontext / any / nhooyr.io→coder/websocket）。独立 PR，不混入功能 | 6h | — | PR#163 post-review |
| DEAD-VARIABLE-01 | **DEAD-VARIABLE-DEADCODE-SCAN-01** (Cx1, P3, 🟡 可延后): `golangci-lint` 未启用 `unused` / `deadcode` linters；批量扫一遍可能存在的 dead vars / unused functions / 未引用 const，作为 v1.0 后整理性卷尾 PR。**修复**：临时启用 `unused` + `deadcode` linters → 走一遍 baseline → 删除真死代码 / 加 `//nolint:unused` 给故意保留项（如 archtest 测试样本） → commit + 关闭 lint。 | 4h | `.golangci.yml`（临时改）+ 全仓清理 commit | graceful-backus §问题 P3 P2-6 verif. |
| X12 | **REFRESH-IDLE-EXPIRE-01** (Cx2, 🟠 条件延后，PR-A29 已合可启动): `idle_expires_at` 滑动窗口列 + Policy.MaxIdle。长时间未使用的 token 提前过期 | 3h | `runtime/auth/refresh/types.go` + `adapters/postgres/` + migration | 开源对标 Zitadel 双过期列 |
| X13 | **REFRESH-PARTITION-01** (Cx2, 🟠 条件延后，生产流量达阈值后): `expires_at` range 分区，用 DROP PARTITION 替代批量 DELETE 进行 GC | 3h | migration + DBA ops runbook | 通用 PG 高吞吐模式 |
| X14 | **REFRESH-GRACE-COUNTER-01** (Cx2, 🟠 条件延后，PR-A29 已合可启动): `first_used_at` + `used_times` 列，grace 窗口内限制重用次数上限 | 2h | `adapters/postgres/refresh_store.go` + migration | 开源对标 Hydra COALESCE 模式 |

---

## 设计决策记录（历史 — 不修，避免重复审查）

| PR | Finding | 结论 | 理由 |
|---|---------|------|------|
| #137 | assembly 层不校验零值 + 非法枚举旁路 | ✅ 已修 | assembly.startInternal 加 ValidateMode 入口闸门 + CheckNotNoop 改为 allowlist |
| #137 | corebundle DurabilityDurable + in-memory | 不修 | 语义正确：Durable 拒绝 Nooper 标记类型，nil 和 `eventbus.New()` 合法通过；effectiveMode + adapterInfo + slog 日志已透明标注 |
| #137 | durability_test 非 table-driven | 不修 | 8 个测试断言模式差异大，table-driven 增加复杂度不增加覆盖率 |
| #137 | SonarCloud 5.6% duplication | 不修 | cell-per-package 固有结构相似，5 cell 的 CheckNotNoop 参数列表各不相同，不可提取 |
| #140 | Request/Trace ID 生成不做 `rand.Read` 错误分支 | ✅ 已修 | Go 1.24+ `crypto/rand.Read` always-succeed-or-fatal，`_, _ =` 是死代码 |
| #140 | JSON unknown field 用字符串匹配 + guard test | ✅ 已修 | Go 标准库 `encoding/json` 至 1.25 仍用文本错误，无 typed error |
| #143 | Seed admin password 通过环境变量传入 | 不修 | dev 模式标准做法（Casdoor/Zitadel 同模式）；生产改用 secrets manager 在 PG-DOMAIN-REPO 时处理 |
| #143 | Slice 目录命名 kebab vs no-dash | 不修 | CLAUDE.md "Cell 开发规则"约定 |
| #143 | `TestContext` 从非 `_test.go` 文件导出 | 不修 | 跨包测试需要，`_test.go` 函数 package-scoped 无法跨包调用 |
| #143 | POST /assign 返回 200 而非 201 | 不修 | 幂等操作返回 200（Casbin 模式），201 暗示每次创建新资源 |
| #146 | computeBoundaryContracts 遍历全量 contracts | 不修 | generator 必须遍历全量才能发现 imported contracts；FMT-09 validate 阶段已保证合法，generator fail-fast 是正确的 defense-in-depth |
| #155 | `slices/*/slice.yaml` allowedFiles 双路径（kebab + no-dash） | 不修 | 全项目统一惯例，FMT-14 治理规则守护，`gocell scaffold slice` 模板默认产出双路径 |
| #159 | 7 个 Option type 分散 | 不修 | 每个 Option 针对不同构造函数，合并会丧失类型安全性；对标 Kratos jwt.Option 每个中间件独立 |
| #159 | GenerateServiceToken `crypto/rand` 失败静默返回 "" | 不修 | Go 1.24+ `crypto/rand.Read` always-succeed-or-fatal，空串触发下游 401 是 fail-closed |
| #159 | ServiceTokenMiddleware nil ring 每请求 500 | 不修 | 返回 error 违背 Go 标准 middleware 签名惯例（net/http / go-chi / Kratos 均不返回 error）；构造时 panic 不符合编码规范 |
| #159 | 缺 service token duration metric | 不修 | HMAC-SHA256 <0.1ms，histogram 分桶无意义信息；等 Redis nonce store 引入网络 I/O 后再加 |
| #210-review | internal service principal `role:internal-admin` 与路由策略 `AnyRole("admin")` 不一致 | 假阳性 | `/internal/v1/*` 路由通过 `WithInternalPathPrefixGuard` 设置 `authDelegatedMatcher`，AuthMiddleware 识别 `isDelegated=true` 后跳过整个 JWT + Policy 链。`AnyRole("admin")` 对 internal 路由从未执行，service token guard 是唯一鉴权层。**但 Policy 声明应修正为 `AnyRole(auth.RoleInternalAdmin)` 作为 defense-in-depth**，见 L10 |
| #210-review | BuildApp option 冲突检测缺失 | 🟡 延后 | 当前 3 个 CellModule，option 直接 append；引入第 4 个 module 前补 key 冲突检测（至少 warning） |

---

## 触发条件项（仅在条件满足时做）

> 已触发并完成项：T8 PUBLIC-ENDPOINT-STRUCT-MIGRATE-01 ✅ PR#201（已在 PR#201 触发并完成）

| # | 任务 | 工时 | 触发条件 |
|---|------|------|----------|
| T1 | **AUTH-PROVIDER-EXPORT-01** `authProvider` 接口 unexported，需移动出 `runtime/bootstrap` | 1h | 第二个 auth provider cell |
| T2 | **AUTH-ISSUE-OPTIONS-01** `JWTIssuer.Issue()` 重构为 `IssueOptions` struct | 1h | Issue() 第 5 个参数 |
| T3 | **DEVICE-ENQUEUE-RBAC** HandleEnqueue 无设备维度鉴权 | 2h | 多租户 operator |
| T4 | **CB-RESILIENCE-PACKAGE-01** 把 `Allower` / `CircuitBreakerRetryAfter` 从 `runtime/http/middleware` 迁移到 `runtime/resilience/circuitbreaker/` 独立包 | 4h | 出现第二个非 HTTP 的 CB 消费方 |
| T5 | **AUTH-SIGNER-01** `SigningKeyProvider` 返回 `crypto.Signer` 替代 `*rsa.PrivateKey` | 2h | golang-jwt v6 发布 |
| T6 | **CONTRACT-EVENT-PAYLOAD-CODEGEN-01** 从 `contracts/event/{domain}/{name}/v{N}/payload.schema.json` 派生 Go struct + decode/validate helper 到 `generated/contracts/event/{domain}/{name}/v{N}/payload.gen.go`。schema 是单一真理源，consumer cell `import` 生成产物（语义等价 protobuf `*.pb.go`），不需要手写 typed view。当前 `gocell scaffold` / `generate` 仅覆盖 metadata yaml + assembly entrypoint，**无 schema → Go 能力**。对标：`xeipuuv/gojsonschema` codegen / `oapi-codegen` / `quicktype` schema → struct 模式。 | 1-2d | event subscriber 重复 decode/validate 扩散到 ≥5 cell consumer（当前 2 cell：`accesscore/configreceive` + `configcore/configsubscribe`） |

---

## v1.1+ 长期规划

> **详细内容请阅读: [backlog_later_detail.md](./backlog_later_detail.md)**
>
> metadata 校验规则 (G-1~G-6) / Kernel 子模块 (wrapper/command/webhook/reconcile/scheduler/replay/rollback)
> adapters 分层重整 (AL-03~AL-04, RMQ-STATUS-01) / 架构风险 (Cell 接口拆分, adapter t.Skip, ER-ARCH-01)
> 契约增强: CONTRACT-BREAKING-01 / CONTRACT-CODEGEN-01 / CONTRACT-STUB-01
> spec tech-debt (C-AC7 jti / C-L6 contract ID / C-DC9 auditarchive stub / DURABLE-TYPE-01) — ~~CONTRACT-META-01~~ ✅ PR#239 合入
> winmdm defer (WM-18/32/4/5/22/23/16) / winmdm reject (WM-3/14/21/24/25/26/30/31/34b)
> v2+ (WM-28/29, GAP-1/2/5/6/8/11/12/13/14)

---

## 工时汇总（2026-04-25 P1 全量复核后）

| 分类 | 工时 | 备注 |
|------|------|------|
| P0 阻塞 | **0** | 全清 |
| P1（复核后） | ~38h | 真实剩余 P1：P1-5/8 + S-5xx + PR220-3 + V-A6/A7/A11/A13/A15/A16/A17/A18/A19（9 条 verification，建议全降 P2） + A26-R1~R4 + PR244-F9 + PR245-F6/F10 + PR247-N5 + PR-A12-SWEEPER-WIRE + PR252-F1/F2 + PR250-F1/F2/F3 + **PR-CFG-1..4/6**（CFG-5 已 won't-do）+ PR258-RES-4。**复核关闭 12 条**（V-A5/A8/A9/A10/A12/A14 + PR244-F8 + PR245-F5 + PR247-N3/N4 + PR-CFG-5 + **PR262-AUTH-POLICY-PLAN-01 ✅ PR#269**）|
| P2 kernel/runtime | ~23h | R2-FOLLOW + R3 + A5a-R3/R9/R10/R11/R12 + PR238-FU2 + CTXCANCEL-HTTP-499 + OTEL-SPAN-CTXCANCEL + FU2-GOVERNANCE-STATIC + BOOTSTRAP-LIFECYCLE-INTEG-TEST-PACKAGE + PR246-ADR-SYNC + PR220-4 + PR-A41-FU1 + PR-A40 housekeeping sweep（A12/A13/A18 打包） — DISTLOCK-LOCKER-TC3-FLAKY ✅ PR#251 |
| P2 adapter | ~18h | A8/A9/A10 CI/lint/OTel + PR-A8-RESIDUAL Vault K8s e2e + A21 health checker ctx budget + LATER-AL-R RMQ structured diagnostic + LATER-T-1 adapter t.Skip backfill 1-2d |
| P2 PR-A14a/A18 follow-up | ~30h | PR225-N1 + PR237-F06/F13/T1/OB2/DX1/A2/A4/A5/A6/PM5/PM7 + PR240-A1/A2/OB1/DX1/DOC1 |
| P2 PR-A9 follow-up | ~9h | PR239-S1/S2/S3/T1/T2/O1/DX1/OB1 |
| P2 slice/cell | ~32h | S13-follow + S14a + S23（S19/S21/S22/S24 ✅ PR#263） + L11/L7/L8/L9 + FEAT-1-残余 + T6 + PR220-e2/e3 + PR238-FU1/FU3/FU4/FU5/FU6/FU7/FU8 + STALECIPHER-OPFIELD + FLAGREPO-CTXCANCEL + CTXCANCEL-HELPER-PKGLEVEL + **PR-CFG-7**（config e2e harness 1d） |
| P2 发布 + 文档 | ~27h + v1.0 tag | PR-A37/A38（DevTools 双 PR） + F2/F3/F4-F8/F10（MULTI-REVIEW-RES-1 ✅ PR#264） |
| **核心路径合计（不含 P3）** | **~177h（约 22 工作日）** | 多数 🟡 可延后或 🟠 等触发条件，真实开发负载 ~55-75h |
| P3 长期 | 3-5d + 若干独立项 | X1 PG-DOMAIN-REPO / X2/X3 BFF cookie / X4/X5 / X9 LINT-MODERN / X12/X13/X14 = PR-A33 / DEAD-VARIABLE-01 |
| 触发条件项 | T1-T6 占位 | 第二个 auth provider / Issue 第 5 参数 / 多租户 / 第二个 CB / golang-jwt v6 / event consumer ≥ 5 cell |
