# GoCell Backlog

> 只含待办事项。已完成项归档至 `docs/reviews/archive/202604180035-backlog-pre-cleanup.md`。
> 更新日期: 2026-04-25（追加：PR-A6 (PR#250) round-1 review 残余 PR250-F1 sensitive-flag / PR250-F2 user-flag-event-camelcase-followup / PR250-F3 wire-byte-pinning-test；PR-A12b (PR#252) review 残余 PR252-F1 command queue bootstrap fail-fast / PR252-F2 command sweeper production governance；PR-A6 主体 3 commits 合入 open PR#250；BOOTSTRAP-LIFECYCLE-INTEG-TEST-PACKAGE + PR246-ADR-SYNC discovered via PR #246 round-6 local sweep）
> 更新日期: 2026-04-24 晚间（追加：PR-A25 (PR#244) round-2 review 残余 PR244-F8/F9/F1；PR-A5c (PR#245) round-1 review 残余 PR245-F5/F6/F10；PR-A26 (PR#247) review 追加 PR247-N3/N4/N5/N7；PR-A12 (PR#249) PR-A12-SWEEPER-WIRE；S-outbox-safety 由 PR#245 F9 per-entry FailurePolicy 收口 ✅；P1-19 AUTH-SETUP-ENDPOINT ✅ PR#247）
> 基线: develop@cb447725（PR#247 合并后；open: PR #242 PR-A18 test split / PR #246 PR-A11 kernel/wrapper）
> 最近合入概览: PR#175-222 分层重构 + auth F系列 + configcore PG pilot；PR#224 outbox emitter refactor；PR#225 pkg/validation + adapterutil (PR-A2)；PR#227 per-cell adapter (PR-A3)；PR#228 HTTP metrics autowire + readyz ctx (PR-A4)；PR#230 Vault pluggable auth + self-healing renewal (PR-A8)；PR#231 watcher debounce deflake；PR#233 configcore integration vet；PR#234 PR-A5a lifecycle 自动发现；PR#237 PR-A14a dual-listener；PR#238 PR-A5b configcore 拆分；PR#239 PR-A9 CONTRACT-META-01；PR#243 cross-platform initial admin；PR#244 PR-A25 AUTH-PROD-HARDENING (S-nonce + S32)；PR#245 PR-A5c 全量收口 (WithEmitter/WithOutboxDeps/envelope kernel)；PR#247 PR-A26 setup slice + adminprovision；PR#248 DACL alias test fix；PR#249 PR-A12 kernel/command L4 + devicecell 迁移；详见 git log
> 核销追加: L10 ✅ PR#218；A14/S4b ✅ PR#230；R2 ✅ PR#228；LATER-SD-1 CONTRACT-META-01 ✅ PR#239；L7-FMT15b ✅ PR#239；S2-follow ✅ PR#239；PR220-2 DOC-NAMING-GUARD-01 ✅ PR#235 (A13)；V-A2/V-A3/V-A4 ✅ PR#243；S-nonce / S32 ✅ PR#244 (PR-A25)；S-outbox-safety ✅ PR#245 F9 per-entry FailurePolicy；P1-19 AUTH-SETUP-ENDPOINT-01 ✅ PR#247；LATER-K2 KERNEL/COMMAND ✅ PR#249；PR220-5 EVENTROUTER-IDENTITY ✅ PR#224 (PR-A5c 顺带)；S4 EVENT-PAYLOAD-TYPED / S41 MARSHAL-ERR-EXPLICIT ✅ PR#250 (PR-A6)
> 未��入外部 PR: PR#129 Sentinel DSN redaction / PR#130 Bolt journey catalog
> 标记说明:
> 🟡 可延后 = 不卡正确性或安全；latent risk / DX / 测试覆盖 / 纯 tech debt / 供应链加固 / 架构打磨 — 可机会性纳入或 v1.0 后做
> 🟠 条件延后 = 有明确触发条件（如首次 prod migration / PG 接线），触发前可延

---

## P0 阻塞项

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| ~~L0~~ | ~~**SERVICE-SECRET-REJECT-DEMO-01**~~ ✅ PR#210 合入（`internalGuardFromEnv` 对 current/previous secret 调用 `rejectDemoKey` + 测试覆盖） | — | — | — |
| ~~L1~~ | ~~**AUDIT-ROUTE-POLICY-01**~~ ✅ `handler.go:74` `RegisterRoutes` + `auth.Policy(auditQueryPolicy)` 已实现；`FinalizeAuth` 全链路覆盖（2026-04-23 核实） | — | — | — |

---

## P1 待办

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| P1-4 | **OUTPUT-JSON-SARIF-01** (Cx3, 🟡 可延后): `gocell validate` 缺机器可读输出通道（JSON/SARIF）。统一诊断模型（单一 `Issue` struct → 多 printer 映射）。对标 golangci-lint / staticcheck / ESLint / kubectl print flags | 6h | `cmd/gocell/` + `kernel/governance/` 序列化 | PR#152 round-2 review |
| P1-5 | **METADATA-PERF-BENCH-01** (Cx3, 🟡 可延后): `BenchmarkParseFS_500Files` 性能基准 + goccy/go-yaml 单次解码迁移成本评估 | 4h | `kernel/metadata/parser_test.go` | PR#152 seat-4 |
| P1-8 | **FEAT-1 DEVICE-LIST-API**: 新建 `devicelist` slice + `GET /api/v1/devices` 分页 + contract + contract_test；同步触发 CONTRACT-LIST-LINT-01 规则。**搭车**：`cells/accesscore/slices/rbaccheck/handler.go:79` `handleListRoles` 永远返回 `hasMore:false`、无 `nextCursor`、无分页能力（`map[string]any` 硬编码响应），需迁移到 `query.PageResult[RoleResponse]` + 真实游标分页（改 service+repository 层）；同时补 `rbaccheck/contract_test.go` 的 `MustRejectResponse` 负向断言。（discovered via PR#214 /fix review） | 3h→5h | `cells/devicecell/slices/devicelist/` + `contracts/http/device/list/v1/` + `cells/accesscore/slices/rbaccheck/handler.go` + `rbaccheck/contract_test.go` | backend_issues.md #1 + PR#214 review |
| ~~S2~~ | ~~**MASTER-KEY-REJECT-DEMO-01**~~ ✅ PR#207 合入 | — | — | — |
| ~~S-nonce~~ | ~~**SERVICE-TOKEN-NONCE-STORE-01**~~ ✅ PR-A25（`cmd/corebundle/controlplane.go::internalGuardFromEnv` 默认注入 `auth.InMemoryNonceStore(ttl = ServiceTokenMaxAge + 30s)`；`runtime/auth.NonceStore` 接口新增 `Kind()`；`auth.WithNonceStore` → `WithServiceTokenNonceStore`；`serviceTokenConfig` 默认 `NewNoopNonceStore()` 消除 nil 路径；`cmd/corebundle.SharedDeps.InternalGuard` 升级为 `*internalGuard` 类型以暴露 nonce store kind；`SharedDeps.validateControlPlane` real 模式拒绝 `NonceStoreKindNoop`，新错误码 `ErrControlplaneServiceSecretMissing` / `ErrControlplaneNonceStoreMissing`；集成测试 `TestAuthWiring_InternalGuard_RequiresServiceToken/internal_replay_same_nonce_401` 验证端到端 401）| — | — | — |
| S4b | **VAULT-TOKEN-STATIC-REAL-GUARD-01** (Cx1, **P1 安全**, 🟠 条件延后，多 pod 生产部署前触发): `adapters/vault/transit_provider.go` `adapterMode=real` 时仍允许静态 VAULT_TOKEN 运行，生产应要求 AppRole/K8s auth（自带续期 + 最小权限）。**修复**：新增 `authMode` option；real 模式 + static token → `errcode.ErrVaultAuthFailed`；与 A14（AppRole/K8s auth）+ **VAULT-RENEWAL-DEGRADATION-GAUGE**（静默降级不可观测）合并实施。来源：R1c(PR#204) 审查 §10 | 1h | `adapters/vault/transit_provider.go` | R1c(PR#204) 追加审查 |
| S-5xx-code-mask | **5XX-ERRCODE-RESPONSE-MASK-01** (Cx2, **安全加固**, 🟡 可延后): 所有 5xx 错误响应的 `error.code` 字段直接透传 `errcode.Code` 字符串（如 `ERR_KEY_PROVIDER_AUTH_FAILED` / `ERR_AUTH_ROLE_FETCH_FAILED` / `ERR_VAULT_AUTH_FAILED` / `ERR_CONFIG_DECRYPT_FAILED` 等 20+ 个 500/503 code）。`httputil.WriteError` 已把 message 统一成 `"internal server error"`，但 code 字符串未屏蔽，攻击者可据此区分基础设施故障类型（密钥服务 / Vault / 角色仓库），构成轻度枚举泄漏。**修复**：`httputil.WriteError`/`writeErrcodeError` 在 `status>=500` 时把响应中的 `code` 统一改为 `ERR_INTERNAL`（或 503 改为 `ERR_SERVICE_UNAVAILABLE`），原 code 保留到 slog 供 operator 观测；客户端只见通用码。**影响面**：`pkg/httputil/response_test.go` 多个 `wantStatus: 5xx` + code 断言用例需要翻新；一并审计 `ErrKeyProviderTransient` / `ErrVaultAuthFailed` / `ErrCircuitOpen` 503 是否统一成 `ERR_SERVICE_UNAVAILABLE`。 | 3h | `pkg/httputil/response.go` + `pkg/httputil/response_test.go` | PR#241 六维度审查 F2+F3（OUT_OF_SCOPE） |
| ~~S-nonce~~ | ~~**SERVICE-TOKEN-NONCE-STORE-01** / **S32 CONTROLPLANE-TOKEN-PROD-GATE**~~ ✅ PR-A25（同上行：real 模式 fail-fast + in-memory 默认兜底；S32 invariant 由 `TestInternalGuardFromEnv_RealMode_GuardInstalledWithSecret` + `internalGuardFromEnv` real-mode 分支共同保证）| — | — | — |
| ~~S-outbox-safety~~ | ~~**OUTBOX-DIRECT-SAFETY-GATING-01**~~ ✅ PR#245 F9 per-entry FailurePolicy（更优方案）收口：`kernel/outbox.Entry.FailurePolicy` 下沉到 entry 层；三 cell 默认改 `DirectPublishFailClosed`（k8s apiserver audit 模型）；archtest `OUTBOX-TOPIC-FAILOPEN-01` 禁止 `session.*`/`user.*`/`role.*`/`audit.*` topic 设 `FailurePolicyFailOpen` | — | — | — |
| ~~S4b~~ | ~~**VAULT-TOKEN-STATIC-REAL-GUARD-01**~~ ✅ PR#230 合入（`adapters/vault/auth.go` `AssertForRealMode()` + `transit_provider.go:774` 在 Login I/O 之前 fail-fast 拒绝静态 token；A14/S4b/VAULT-RENEWAL 同批完成） | — | — | — |
| ~~L2~~ | ~~**ROUTE-POLICY-REGISTRY-01**~~ ✅ `runtime/http/router/policy_coverage.go` 实现路由-策略覆盖校验；`router.go:FinalizeAuth()` 调用 `verifyPolicyCoverage()`；bootstrap phase 注册时即强制覆盖（2026-04-23 核实） | — | — | — |
| ~~L4~~ | ~~**ID-VALIDATION-SINGLE-SOURCE-01**~~ ✅ `pkg/idutil/id.go` 已建（`MaxHTTPIDLen`、`IsSafeID`），统一事实源（2026-04-23 核实） | — | — | — |
| ~~L6~~ | ~~**CONTRACTTEST-MODEL-ALIGN-01**~~ ✅ `pkg/contracts/schema_types.go` 已建（`SchemaRef`/`SchemaRefs` 共享结构体），一致性测试已覆盖（2026-04-23 核实） | — | — | — |
| ~~L7-FMT15b~~ | ~~**CONFIG-GET-DUAL-MODE-SPLIT-01**~~ ✅ PR#239 合入（config/get response.schema.json 去 oneOf 简化为单条；新建 `http.config.list.v1` 合约 + response.schema.json 分页形态；configread/slice.yaml serve 双 contract；contract_test 双向 reject 验证） | — | — | — |
| ~~L7-FMT15~~ | ~~**FMT15-NEXTCURSOR-ENFORCE-01**~~ ✅ PR#214 合入（`hasNextCursorProperty` + `parseListSchemaInfo` + `checkFMT15Contract`；tests: missingBoth/missingNextCursor/null-property；contracts/http/auth/role/list/v1 补 nextCursor 声明）| — | — | — |
| L7 | **EXAMPLES-STARTUP-SMOKE-01** (Cx1, 🟡 可延后): 2026-04-22 复查确认所有 `NewCursorCodec` 字面量均 ≥ 32 字节（审计文档的"30/31 字节"系误数）。**唯一剩余 TODO**：CI 没有独立的 examples startup smoke job；`examples/ssobff` 当前仅在 integration tag 下跑 walkthrough test，`main()` 真实启动路径无单独验证（若启动依赖顺序或 option wiring 回归，CI 无法检测）。**修复**：`.github/workflows/ci.yml` 新增 `examples-smoke` job：`timeout 5s go run ./examples/ssobff 2>&1` 期望进入 ready 状态后 ctx cancel 退出（或等价 testable entrypoint）。 | 0.5h | `.github/workflows/ci.yml` | 2026-04-20 发现；2026-04-22 cursor key 误报核销，缩减范围 |
| ~~P1-A~~ | ~~**PRINCIPAL-UNIFIED-CONTRACT-01**~~ ✅ pre-existing（PR-A7 探索实测确认）：`runtime/auth.Principal` + `WithPrincipal/FromContext/MustFromContext` + `UnionAuthenticator` + JWT/Service `Authenticator` 早已落地；`cmd/corebundle/auth_integration_test.go:321-348` 验证 `/internal/v1` delegated → ServiceToken → `RoleInternalAdmin` 全链路绿；所有 handler 消费 `auth.FromContext`。PR-A7 仅做尾部清理（删 `sessionvalidate.Service.Verify()` dead shim）。 | — | — | — |
| ~~P1-19~~ | ~~**AUTH-SETUP-ENDPOINT-01**~~ ✅ PR#247 (PR-A26) 合入：新 `cells/accesscore/slices/setup/`（handler + service + contract_test） + 新 `cells/accesscore/internal/adminprovision/`（共享 user+role 原子供给）+ 两个 Public contract（`/api/v1/access/setup/{status,admin}`，Consul 风格 per-cell 命名）；`POST /access/setup/admin` 成功 201 / 已初始化 409（`ErrSetupAlreadyInitialized`）；orphan recovery 语义；sync.Mutex 单进程原子性；round-2 登记 A26-R1~R4 + N3/N4/N5/N7 四条 follow-up | — | — | — |
| ~~PR220-1~~ | ~~**DOC-CAPABILITY-INVENTORY-REWRITE-01**~~ ✅ A13 落地：活动文档事实源按当前 metadata/contract/route 重写；`sessionvalidate` / `authorizationdecide` 明确为内部 service 能力，非公开 HTTP route。 | — | — | — |
| ~~PR220-1b~~ | ~~**DOC-IOTDEVICE-README-ENVELOPE-01**~~ ✅ A13 落地：register / enqueue / ack 示例均改为真实 `data` envelope。 | — | — | — |
| PR220-3 | **JOURNEY-VERIFY-FAIL-CLOSED-01** (Cx3): (1) `J-sessionrefresh` / `J-ssologin` 的 `journeyID → RunPattern` 驼峰生成与真实测试函数名不一致，当前 zero-match 失败；(2) 即使命名修好，`tests/integration/journey_test.go` 28 条 `t.Skip("stub...")` 仍因 `go test` exit 0 被当 passed。**修复**：修 RunPattern 生成 + 解析 `go test -json` 识别 `--- SKIP`，skip stub 算 pending/fail 不算 verified。 | 4h | `kernel/verify/ref.go` + `gotest.go` + `runner.go` + `tests/integration/journey_test.go` + `docs/guides/integration-testing.md` | PR#220 遗留报告 P1 |
| ~~PR220-5~~ | ~~**EVENTROUTER-SUBSCRIPTION-IDENTITY-SPLIT-01** (Cx2)~~ ✅ 由 PR #224 (PR-A5c OUTBOX-EMITTER-UNIFY) 顺带落地：`kernel/outbox/subscription.go` 拆 `ConsumerGroup` + `CellID` + `TraceIdentity()` helper；PR-A6 (PR#250) plan 已核实并登记主线完成。 | — | — | — |
| ~~PR220-e1~~ | ~~**NAMING-BASELINE-CONTRADICTION-01**~~ ✅ A13 落地：`docs/architecture/naming-baseline.md` 重写为 no-dash 单一规则，并指向 DOC-NAME-01。 | — | — | — |
| ~~V-A2~~ | ~~**HARDCODED-UNIX-PATH-01**~~ ✅ PR#243 合入（`examples/ssobff/main.go` `os.UserConfigDir()` fallback + `filepath.Join` 拼接；`stateDir` 硬编码路径已替换） | — | — | — |
| ~~V-A3~~ | ~~**WINDOWS-SIGNAL-01**~~ ✅ PR#243 合入（`runtime/shutdown/` `signalsToWatch()` 平台分离：unix=SIGINT+SIGTERM，windows=os.Interrupt；`lifecycle_test.go` 改为 `unix \|\| windows` tag；`shutdown_test.go` 信号测试 Unix-only tag）| — | — | — |
| ~~V-A4~~ | ~~**SYMLINK-TEST-PRIVILEGE-01**~~ ✅ PR#243 合入（`runtime/config/watcher_test.go` + `kernel/governance/validate_test.go` symlink 测试均加 `//go:build !windows` tag 或 `t.Skip` 守护） | — | — | — |
| V-A5 | **VALIDATION-HELPER-EXTRACT-01** (Cx2): `runtime/auth/*.go` 6+ 处 `if token == ""` 无 helper，`pkg/validation.RequireNotBlank` 缺失。**修复**：抽 `pkg/validation/` 通用 helper，消除重复。 | 2h | `pkg/validation/`（新） | 202604221954-verification §A5（P1-4） |
| V-A6 | **WEBSOCKET-DEPRECATED-01** (Cx1, **供应链**): `go.mod` 使用 `nhooyr.io/websocket v1.8.17` 已 archived。**修复**：切 `github.com/coder/websocket`（API 兼容）。 | 2h | `go.mod` + 全仓 import | 202604221954-verification §A6（P1-5） |
| V-A7 | **DEPRECATED-ADAPTER-METHODS-01** (Cx1): `Connection.Statter()`、`Subscriber.InitializeSubscription()` 仍被测试 + `kernel/outbox/outbox_test.go:464` 使用。**修复**：移除 `Deprecated:` 标注（承认稳定 API）或先迁测试再删。 | 1h | `adapters/*/` | 202604221954-verification §A7（P1-8） |
| V-A8 | **CMD-THICK-ENTRY-REDUCE-01** (Cx2, PARTIALLY): `cmd/corebundle/` 已拆 `app.go`+`bundle.go`（PR#220），但 `main.go` 仍偏大。**修复**：继续按需缩减，move wiring 到 `bundle.go`。**搭车**（PR-A25 F7）：迁移时将 `cmd/corebundle.internalGuard` unexported 类型提升为 exported（如 `corebundle.InternalGuard`）或抽取为接口 `InternalGuardProvider`，避免 `SharedDeps.InternalGuard *internalGuard` 字段在跨包迁移时被 package-visibility 阻塞。 | 2h → 3h | `cmd/corebundle/` | 202604221954-verification §A8（P1-13）+ PR-A25 reviewer F7 (2026-04-24) |
| V-A9 | **DEMO-KEY-REJECT-LOADKEYSET-01** (Cx2, **安全**): `runtime/auth/keys.go:328 LoadKeySetFromEnv()` 缺 `rejectDemoKey` 检查；cursor/HMAC/service-secret 都有。**修复**：加 `rejectDemoKey` + demo 模板值黑名单。 | 1h | `runtime/auth/keys.go` | 202604221954-verification §A9（P1-14） |
| V-A10 | **GOVERNANCE-WINDOWS-PATH-01** (Cx2): cmd/gocell/ 已用 `filepath.Join`；`examples/ssobff/main.go:150,168` 用 `"/"` 拼接。**修复**：改 `filepath.Join`。 | 0.5h | `examples/ssobff/main.go` | 202604221954-verification §A10（P1-15） |
| V-A11 | **GOVERNANCE-EXAMPLES-COVERAGE-01** (Cx3): `kernel/governance/validate.go:138-154` rules 只扫 cells/contracts/journeys/assemblies，不扫 `examples/`。**修复**：新增 `rules_examples.go` 检查硬编码 key/path/URL。 | 3h | `kernel/governance/rules_examples.go`（新） | 202604221954-verification §A11（P1-17） |
| V-A12 | **SYMLINK-TOCTOU-CREDFILE-AUDIT-01** (Cx2, 🟡 可延后): PR#243 在 `cells/accesscore/initialadmin` 加了 `O_NOFOLLOW` + `Lstat`（B2），但 `cells/accesscore/initialadmin` 之外其他凭据/状态写入路径未审计；需扫一遍全仓 `os.OpenFile(...O_CREATE...)` 确认无遗漏 TOCTOU 路径 | 2h | 全仓 `os.OpenFile` call sites | PR #243 §B2 follow-up |
| V-A13 | **GENTPL-LIFECYCLE-PATTERN-01** (Cx1+Cx2, 🟡 可延后): `kernel/assembly/gentpl/main.go.tpl` 直连 `app.Start/Stop` 跳过 `bootstrap.Run` phase3b hooks（含 initial admin bootstrap）；生成的新 assembly 默认丢失生命周期；需决定模板是"最小骨架"还是"开箱即用"，二选一并补集成测试 | 1h（设计）+ 2h（实施 + 测试） | `kernel/assembly/gentpl/main.go.tpl` | PR #243 review §E1 |
| V-A14 | **SHUTDOWN-WAIT-READY-CHANNEL-01** (Cx1, 🟡 可延后): `runtime/shutdown.Manager.Wait` 注册 signal handler 与单测 `syscall.Kill` 之间用 50ms 硬等；需引入 ready-channel 模式（如 `WaitWithReady(readyCh chan struct{})`）消除 flake 风险 | 1h | `runtime/shutdown/shutdown.go` + `shutdown_test.go` | PR #243 review §S3 |
| V-A15 | **INITIALADMIN-OPTION-FAILFAST-UNSUPPORTED-01** (Cx1, 🟡 可延后): `accesscore.WithInitialAdminBootstrap()` 在 `!unix && !windows` 平台仍可调用但 runtime 才报 errUnsupportedPlatform；需在 cell.Init 时 platform-check 并返回明确 errcode | 1h | `cells/accesscore/cell.go` + `cells/accesscore/initialadmin/bootstrap_unsupported.go` | PR #243 review §S7 |
| V-A16 | **SHUTDOWN-INTERRUPT-WINDOWS-TEST-01** (Cx1, 🟡 可延后): `runtime/shutdown.NotifyContext` 在 Windows 仅靠 os-smoke CI 间接覆盖；需补 `shutdown_interrupt_windows_test.go`（`//go:build windows`）单测 `os.Interrupt` 触发 ctx 取消 | 0.5h | `runtime/shutdown/` | PR #243 review §S11 |
| V-A17 | **SHUTDOWN-BUILD-TAG-MUTUAL-EXCLUSION-01** (Cx1, 🟡 可延后): `runtime/shutdown/signals_*.go` 的 `unix` / `windows` / `!unix && !windows` 互斥靠 4 OS cross-compile 验证；建议加编译期守卫测试（如检查只有 1 个 `signalsToWatch` 实现可见） | 0.5h | `runtime/shutdown/` | PR #243 review §S8 |
| V-A18 | **DOCS-MACOS-TROUBLESHOOTING-CLARIFY-01** (Cx1, 🟡 可延后): `docs/operations/first-run-setup.md:244` 故障排查表 macOS `$TMPDIR/gocell` hint 与自动 `~/Library/...` fallback 语义重叠，易误导用户 | 0.5h | `docs/operations/first-run-setup.md` | PR #243 review §S10 |
| V-A19 | **DEMO-KEY-WARN-COMMENT-01** (Cx2, 与 V-A9 同 PR): `cmd/corebundle/demo_keys.go:18-36` 可预测模板；`examples/ssobff/main.go:99,116` demo key 无警告。**修复**：加 `// DO NOT COPY TO PRODUCTION` 注释。 | 0.5h | `cmd/corebundle/demo_keys.go` + `examples/ssobff/main.go` | 202604221954-verification §A19（P2-11） |
| A26-R1 | **ADMINPROVISION-DIST-LOCK-01** (Cx3, 🟠 条件延后 — 触发条件：PG adapter for accesscore UserRepo/RoleRepo 接线): `adminprovision.Provisioner.Ensure` 当前靠 sync.Mutex 覆盖单进程原子性；多实例 PG 部署需在 PG adapter 的 RunInTx 内执行 `pg_advisory_xact_lock(<const>)` 保证跨进程唯一 admin 创建。加到 PG adapter 的 TxRunner 或新 `AcquireBootstrapLock(ctx)` port 方法。 | 3h | `adapters/postgres/accesscore/` + `cells/accesscore/internal/ports/role_repo.go` | PR#247 round-2 review P0（已本 PR 单进程闭合） |
| A26-R2 | **SETUP-ADMIN-RATE-LIMIT-01** (Cx2, 🟡 可延后 — 取决于是否面向公网): `POST /api/v1/access/setup/admin` 现在虽然 410 快速短路、bcrypt 仅在成功路径触发，但仍是 Public。加 per-IP rate-limit middleware + 失败计数 lockout（如 5min 内 5 次 400 触发 429）；或改造成一次性 bootstrap token（env 生成，第一次 POST 消费）。 | 4h | `runtime/auth/` + `cells/accesscore/cell_routes.go` | PR#247 round-2 review P1-B |
| A26-R3 | **SETUP-PATH-NAMESPACE-POLICY-01** (Cx1, 🟡 可延后): 在 `.claude/rules/gocell/api-versioning.md` 中明确「顶级 `/api/v1/setup/` 为未来跨 Cell system-level 初始化保留；per-Cell 初始化入口挂在 `/api/v1/{cell}/setup/...`」。本 PR 已把 accesscore setup 迁到 `/api/v1/access/setup/*`（Consul 模式）；规则写明后避免未来又有 PR 在顶级 setup 下开端点。 | 0.5h | `.claude/rules/gocell/api-versioning.md` | PR#247 round-2 review N-01 |
| A26-R4 | **SETUP-ORPHAN-E2E-01** (Cx2, 🟠 条件延后 — 触发条件：PG adapter for accesscore): orphan recovery 分支目前只在 provisioner 单元测 + setup service 单元测覆盖。in-memory repo 无法跨进程持久化 orphan 状态，E2E 场景要等 PG adapter 接入后，用真 DB 写 orphan 行 → 重启 → setup POST 验证 RequireReset=false 清除 + 无重复 event。 | 1.5h | `cmd/corebundle/setup_integration_test.go` (PG path) | PR#247 round-2 review N-06 |
| PR244-F1 | **MULTIPOD-INMEMORY-NONCE-WARN-01** (P1, Cx2, 🟠 多 pod 拓扑前触发): `cmd/corebundle/shared_deps.go:137` `validateControlPlane` 仅拦截 `NonceStoreKindNoop`，`NonceStoreKindInMemory` 在多 pod 横向扩展时产生跨 pod replay 窗口无运行时拦截。**修复（二选一）**：(a) 轻量 — `internalGuardFromEnv` 装配成功后若 kind==InMemory 追加 `slog.Warn("replay protection is single-pod only — inject a distributed store before horizontal scaling")`；(b) 结构化 — 引入 `runtime/bootstrap.Topology.SinglePodReplayProtection` opt-in 标志，多 pod 部署显式声明后可启用 InMemory，否则 startup fail-fast。**决策点**：需架构选型（warn-only vs Topology 门禁）。 | 2-3h | `cmd/corebundle/controlplane.go` + `cmd/corebundle/shared_deps.go` + `runtime/bootstrap/topology.go` + `docs/ops/env-vars.md` | PR#244 round-2 reviewer P1（F1 分歧待决） |
| PR244-F8 | **NONCE-CAPACITY-CONTROL-01** (P2, Cx2, 🟡 可延后): `runtime/auth/nonce.go` `InMemoryNonceStore` 无硬上限 + 无 emit capacity 指标，理论上 DoS 窗口内攻击者可以持续填充 nonce map 放大内存。当前 1h TTL 下 map 增长受限，但无容量治理机制。**修复**：加 `WithMaxEntries(n int)` option（默认 100k）+ `gocell_auth_nonce_store_size` gauge；满时 LRU evict 或 fail-closed（选前者）。 | 2h | `runtime/auth/nonce.go` + `runtime/auth/nonce_test.go` | PR#244 round-2 review F8 |
| PR244-F9 | **AUTH-REPLAY-ERRCODE-GRANULARITY-01** (P2, Cx1-Cx2, 🟡 可延后): `ErrAuthUnauthorized` 当前覆盖 signature/nonce/audience 等所有 401 原因，replay 攻击无法从 HTTP 响应码区分于常规凭证失败。**修复**：新增 `ErrAuthReplayDetected` 400/401 子码；需跨消费方（audit log / SIEM / monitoring dashboard）同步评估。 | 2h | `pkg/errcode/errcode.go` + `runtime/auth/nonce.go` + 消费方告警规则 | PR#244 round-2 review F9 |
| PR245-F5 | **KERNEL-OUTBOX-ENVELOPE-TEST-COVERAGE-AUDIT-01** (Cx1-Cx2, 🟡 可延后): PR#245 删除 `runtime/outbox/envelope_test.go`（394 行），其中 wire-format 关键断言（`schemaVersion` camelCase、`aggregateId/aggregateType/eventType/createdAt` camelCase、broken JSON → `ErrEnvelopeSchema`、empty id/eventType 被拒）需确认已由 `kernel/outbox/envelope_test.go` 覆盖；缺失项迁入 kernel 侧。 | 1h | `kernel/outbox/envelope_test.go` | PR#245 round-1 reviewer F-5 |
| PR245-F6 | **OUTBOX-ARCHTEST-SCAN-SCOPE-EXPAND-01** (Cx2, 🟡 可延后): `tools/archtest/outbox_cell_test.go::isCellFile` 仅匹配 `cells/<name>/cell.go`，若未来 Option 搬到 `cells/<name>/options.go` / `cell_options.go` / `cell_extra.go`，`OUTBOX-CELL-01` 守卫即失效。**修复**：`isCellFile` 改为 `cells/<name>/*.go`（排除 `*_test.go` + `internal/` + `slices/`）扫整个 cell 包顶层 decl。 | 1h | `tools/archtest/outbox_cell_test.go` | PR#245 round-1 reviewer F-6 |
| PR245-F10 | **CELL-RAW-DEPS-ARCHTEST-EXPAND-01** (Cx2, 🟠 PR-A22 Cell ISP 拆分触发): PR-A5c 收口只 ban `WithPublisher` / `WithOutboxWriter` raw Option；`AccessCore.txRunner` 等 raw dep 仍保留。PR-A22 落地时对等扩展 archtest 规则 `CELL-RAW-DEPS-01` 禁止新引入 raw-dep Option，避免重新打开口子。 | 2h | `tools/archtest/outbox_cell_test.go` 或新 `raw_deps_test.go` | PR#245 round-1 reviewer F-10（PR-A22 触发） |
| PR247-N3 | **CELL-PATTERNS-EVENT-DTO-01** (Cx1, 🟡 可延后): `cells/accesscore/slices/setup/service.go:205` `publishUserCreated` 和 `cells/accesscore/slices/identitymanage/service.go:128` 都用 `map[string]any` 序列化 `event.user.created.v1` payload，违背 `cell-patterns.md` "typed struct + converter" 序列化边界约定。**修复**：抽 `cells/accesscore/internal/dto/UserCreatedEvent` struct，setup + identitymanage 共用（PR-A6 S4 EVENT-PAYLOAD-TYPED-01 可合并批处理）。 | 1h | `cells/accesscore/internal/dto/user_events.go` + `setup/service.go` + `identitymanage/service.go` | PR#247 round-1 reviewer N-03（与 S4 合并） |
| PR247-N4 | **SETUP-ADMIN-ERROR-MESSAGE-PATH-LEAK-01** (Cx1, 🟡 可延后): `setup/service.go:174` `ErrSetupAlreadyInitialized` message 包含 `"use /api/v1/access/sessions/login"`，将内部路径耦合到错误消息体。**修复**：message 保留通用 "first-run admin already provisioned"，login 路径挪到 `details.next_action`。 | 15min | `cells/accesscore/slices/setup/service.go` | PR#247 round-1 reviewer N-04 |
| PR247-N5 | **SETUP-SLICE-CONSISTENCYLEVEL-EXPLICIT-01** (Cx1, 🟡 可延后): `cells/accesscore/slices/setup/slice.yaml` 未显式声明 `consistencyLevel: L2`（依赖继承 cell.yaml）。作者在 fix commit 中以 "SliceMeta does not accept the field" 为由 defer，需先 `grep -r consistencyLevel cells/*/slices/*/slice.yaml` 验证其他 slice 是否显式声明；若有则 setup slice.yaml 补写并加 `TestSliceMetaAcceptsConsistencyLevel` 单测，避免后来人反复翻案。 | 30min | `cells/accesscore/slices/setup/slice.yaml` + `kernel/metadata/parser_test.go` | PR#247 round-1 reviewer N-05 |
| PR247-N7 | **SETUP-ADMIN-EMAIL-CONTROL-CHAR-GUARD-01** (Cx1, 🟡 可延后): `contracts/http/auth/setup/admin/v1/request.schema.json:7` email 字段仅 `minLength/maxLength`，无 `format: email`（合理，setup 场景常是 `root@local`）。但缺"不含控制字符"的轻量 service 层校验，存在 log injection 风险。**修复**：`setup/service.go` CreateAdmin 加 `if strings.ContainsAny(in.Email, "\r\n\x00")` 返回 400；或明确 doc 中写 email 仅作标识、不校验 RFC 5321。 | 30min | `cells/accesscore/slices/setup/service.go` | PR#247 round-1 reviewer N-07 |
| PR-A12-SWEEPER-WIRE | **PR-A12-KERNEL-COMMAND-SWEEPER-WIRE-01** (Cx1-Cx2, 🟠 触发条件：`InMemQueue.ListPending` 长出 multi-device scan 方法或 PG adapter 接入): PR#249 kernel/command 单设备 Sweeper 已 functional，cell 级 Sweeper 需要 multi-device 扫描能力后 wire。当前 `examples/iotdevice/cells/devicecell/cell.go` 已加 `TODO(PR-A12-SWEEPER-WIRE)` 占位。**修复**：`InMemQueue` 长出 `ListAllPending(ctx, since time.Time)` → cell-level `Sweeper` 启动后台定时回收，超时 entry 按 `TimeoutPhase` 分流（retry / fail / dead-letter）。 | 2h | `kernel/command/commandtest/inmem.go` + `examples/iotdevice/cells/devicecell/cell.go` | PR#249 out-of-scope residual |
| PR252-F1 | **COMMAND-QUEUE-REGISTRAR-BOOTSTRAP-FAILFAST-01** (P1, Cx3, 🟠 下一个 durable command adapter 前触发): PR#252 引入 `QueueRegistrar` / `ActiveScanner` / runtime command adapter 发现后，能力声明与 bootstrap 装配仍分散；若 cell 声明可注入 queue 但实际装配缺失，容易只告警或局部降级，形成"看起来接上但未生效"的启动隐患。**修复**：把 QueueRegistrar 发现、queue/scanner 注入、capability 校验纳入唯一 bootstrap phase；缺 queue 或缺 scanner 时 fail-fast；补 bootstrap phase 单测覆盖 missing queue / missing scanner / duplicate registrar / devicecell 成功路径。 | 4-6h | `kernel/cell/registrar.go` + `runtime/command/` + `runtime/bootstrap/bootstrap_phases.go` + `examples/iotdevice/cells/devicecell/cell.go` | PR#252 six-seat review P1/Cx3（Queue 注入闭环） |
| PR252-F2 | **COMMAND-SWEEPER-PRODUCTION-GOVERNANCE-01** (P1, Cx4, 🟠 多副本 command consumer 或 PG command queue 前触发): PR#252 的 sweeper 可扫描 active commands 并 AckTimeout，但生产治理层不足：多副本下缺 leader/分片 ownership，扫描/Ack 错误只回调可能被忽略，连续失败不会降级 readiness，核心路径缺指标，存在放大负载与静默退化风险。**修复**：引入单活 leader 或稳定分片 ownership；定义连续失败阈值并接入 `/readyz` 降级；记录 scan / ack timeout / ack failure / consecutive failure 指标；补 fake scanner/queue 的失败预算测试和多实例 ownership 测试。 | 8h | `kernel/command/sweeper.go` + `runtime/command/` + `runtime/bootstrap/` + `runtime/http/health/` + `kernel/observability/metrics/` | PR#252 six-seat review P1/Cx4（sweeper ownership/readiness/metrics） |
| PR250-F1 | **CONFIG-VERSION-PUBLISHED-SENSITIVE-FIELD-EVAL-01** (P1 安全, Cx2, 🟡 可延后 — 预先存在): `contracts/event/config/version-published/v1/payload.schema.json` 的 `sensitive: bool` 字段将 config 条目的敏感分类广播给所有订阅者（value 未暴露，仅暴露分类标签）。**预先存在**：PR-A6 之前此字段已在 `event.config.changed.v1` 的 "published" oneOf 分支上同样广播；PR-A6 拆 topic 时保留了语义，未新增泄漏。**修复（可选）**：从 wire 移除 `sensitive`，订阅者需要时直接查 configcore（`http.config.get.v1` 或 internal API）；或保留字段但将 consumer 白名单收敛到 auditcore。**决策 trigger**：首次引入外部 cell 订阅此 topic 时评估；当前 subscribers 均为同 repo 内三 cell，攻击面有限。 | 1-2h | `contracts/event/config/version-published/v1/payload.schema.json` + `cells/configcore/internal/domain/config_events.go` + 订阅者 | PR#250 round-1 security reviewer #1 |
| PR250-F2 | **EVENT-USER-FLAG-CAMELCASE-FOLLOWUP-01** (P1 安全/DX, Cx2, 🟡 可延后): `event.user.created.v1` / `event.user.locked.v1` / `event.flag.changed.v1` payload 仍为 snake_case (`user_id` / `username` / `event_type` 等)，PR-A6 将 session/config/audit 事件迁至 camelCase 时按 plan 明确将这些事件留作 trailing sweep。**现状**：auditappend 通过 `UserIDCamel`/`UserIDSnake` 双 tag fallback 跨协议提取 actor，临时过渡。**修复**：沿 PR-A6 套路迁移：(a) `contracts/event/user/{created,locked}/v1/payload.schema.json` + `contracts/event/flag/changed/v1/payload.schema.json` 字段改 camelCase；(b) `cells/accesscore/internal/dto/user_events.go` `UserCreatedEvent` 的 JSON tag 改 camelCase；(c) setup / identitymanage / flagwrite 的 producer 调用点迁 `outbox.Emit[T]`（同时吸收 PR247-N3 EVENT-DTO 的 typed struct 诉求）；(d) auditappend 删除 `UserIDSnake` 降级回单 tag；(e) 更新 docs/design/ capability-map + master-plan + capability-inventory 引用。 | 6-8h | `contracts/event/{user,flag}/**` + `cells/accesscore/internal/dto/user_events.go` + `cells/accesscore/slices/{setup,identitymanage}/service.go` + `cells/configcore/slices/flagwrite/service.go` + `cells/auditcore/slices/auditappend/service.go` | PR#250 fix commit + round-1 security reviewer #2（与 PR247-N3 合并批处理） |
| PR250-F3 | **EVENT-WIRE-BYTE-PINNING-TEST-EVAL-01** (P2, Cx2, 🟡 可延后 — 倾向不做): PR-A6 的 7 个 typed event struct 未做 wire-byte 级 golden-string 断言（contract test 走 JSON schema round-trip，对字段顺序 / `omitempty` 不敏感）。reviewer 建议增加 `assert.Equal(t, ``{"action":"created",...}``, string(entry.Payload))` 类断言以捕获未被 schema 捕获的 camelCase 回归。**反方观点**：schema validation + `additionalProperties: false` 已在 contract_test 覆盖字段名漂移；golden-string 额外脆弱（字段顺序 / `omitempty` 边界 / 生成器差异）；净增维护成本。**决策**：除非后续出现真实 camelCase regression 漏网，否则关闭为 won't-do。 | 2h | `cells/accesscore/slices/*/contract_test.go` + `cells/configcore/slices/*/contract_test.go` + `cells/auditcore/slices/*/contract_test.go` | PR#250 round-1 testing reviewer #4 |

---

## P2 待办

### kernel / runtime

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| ~~K1~~ | ~~**METADATA-PROJECTLOC-IFACE-01**~~ ❌ **won't do**：原始问题（yaml.v3 AST 对外泄漏）已由 PR#212 通过私有化 `fileNodes` + 公开 `Locate()`/`PrepareFileNode()`/`HasFileNodes()` 方法解决，接口方案被更优雅的实现替代。提取接口无增量价值：`governance` 同时依赖 `ProjectMeta` 的 5 个数据字段（`Cells/Slices/Contracts/Assemblies/Actors`）和位置方法，接口只能覆盖位置部分，会导致持有两个依赖反而更复杂；且 `governance` 是 module 内部消费者，无 mock 隔离需求。 | ~~3h~~ | ~~`kernel/metadata/`~~ | PR#152 seat-1 → 关闭 |
| ~~K2~~ | ~~**OBS-RELAY-REGISTER-ATOMIC-01**~~ ✅ `relay_collector.go:81-91` 含 LIFO unregister 回滚循环，部分失败可原子回退（2026-04-23 核实） | — | — | — |
| ~~R1~~ | ~~**BOOTSTRAP-RUN-COGNIT-01**~~ ✅ PR#200/#202/#203/#204（R1a ManagedResource+Worker→kernel + R1b KeyProvider→kernel/crypto + R1c Vault envelope encryption+adapters/vault 迁移 + R1d CellModule+BuildApp 四步完成）: ~~`bootstrap.go::Run()` 认知复杂度 225（`//nolint:gocognit` 抑制），拆 `validateOptions()` / `buildRouter()` / `startServers()` 三段式~~ | ~~4h~~ | ~~`runtime/bootstrap/bootstrap.go`~~ | PR#163 agent 报告 |
| ~~R2~~ | ~~**OBS-HTTP-COLLECTOR-AUTOWIRE-01**~~ ✅ PR#228 (PR-A4) 合入 | — | — | — |
| R2-FOLLOW | **HTTP-METRICS-LABEL-REALIGN-01** (Cx2, P2, 🟠 多 cell assembly 部署前触发): PR#228 auto-wire HTTP collector 的 label 名为 `cell` 但实际值来自 `runtime/bootstrap/bootstrap_phases.go:675-683` `cellID := b.assemblyID`——多 cell assembly 下所有端点请求会贴同一个 assembly-id value，`cell` 维度 dashboard/告警误归因。**修复**：两步走——(1) 最小兼容：label 名改 `assembly` 或同时暴露 `assembly`+`cell` 双标签保留向后兼容窗口；(2) 真解：在 router 注册路由时把 owning cell 写入 request context，collector middleware 从 ctx 读取，`NewProviderCollector` 配置改 `AssemblyID string, CellResolver func(*http.Request) string`。参考 Kratos `operation`/go-zero `route`/OpenTelemetry Resource vs Semantic-attr 分层。ref: `runtime/observability/metrics/provider_collector.go:60,69,89` | 4h | `runtime/bootstrap/bootstrap_phases.go` + `runtime/observability/metrics/provider_collector.go` + `runtime/http/middleware/metrics.go` + dashboards | 2026-04-24 六席位复核（架构缺陷） |
| R3 | **OB-02** (🟡 可延后): safe_observe broken logger 注入测试 | 1h | `runtime/http/middleware/safe_observe_test.go` | 历史 backlog 0-J |
| R4 | **INTERNAL-LISTENER-01** (Cx4, 🟡 可延后): `/internal/v1/` 与公网共用 listener + Bearer JWT；独立 listener 或 service-token/mTLS 策略 | 4-8h | `runtime/bootstrap/bootstrap.go` + cell 路由注册拆分 | PR#143 review F1 |
| READYZ-VERBOSE | **READYZ-VERBOSE-TOKEN-DENY-01** (Cx1, P2, 🟡 可延后): `runtime/http/health/health.go:397-419` 当 `ReadyzVerboseToken` 已配置但请求 header 不匹配时返回 false，handler 静默降级为普通 200 无 verbose（slog.Warn 但对客户端无指示）。运维排障可能把"被拒绝 verbose"误判为"系统正常但无详情"。**修复**：token 已配置但请求 missing/mismatched 时返回 `401 Unauthorized`（或至少在响应 JSON 中打 `verboseSuppressed: "unauthorized"` 标记）；区分"未配置 verbose"与"token 错"两种路径并文档化 | 1h | `runtime/http/health/health.go` + `docs/ops/` | 2026-04-24 六席位复核 |
| READYZ-LEAK-GUARDRAILS | **READYZ-UNCOOPERATIVE-CHECKER-GUARDRAILS-01** (Cx2, P2, 🟡 可延后): `runtime/http/health/health.go:296-322` 自己注释承认 "An uncooperative probe (one that ignores ctx) will leak its goroutine past this function's return"。Go 层面无法强杀 goroutine 是语言限制，不是架构缺陷；但缺治理：高频探针 + 慢 probe 下会放大资源占用。**修复**：(1) `Readiness.maxConcurrentProbes`（默认 8）并发上限；(2) `gocell_readyz_ongoing_probes` gauge + `gocell_readyz_probe_leaked_total{name}` counter；(3) checker 合约测试：所有内置 probe 必须在 ctx 取消后 100ms 内退出 | 3h | `runtime/http/health/health.go` + `runtime/http/health/health_test.go` | 2026-04-24 六席位复核（A21 延伸） |
| ~~PR220-2~~ | ~~**DOC-NAMING-GUARD-01**~~ ✅ A13 落地：`docs/architecture/naming-guard.yaml` + strict-only `DOC-NAME-01` 接入 `gocell validate --strict` / fail-fast；扫描 active docs/templates/example README/backlog。 | — | — | — |
| PR220-4 | **CI-LINT-EVENT-SEMANTIC-SPLIT-01** (Cx1): `.github/workflows/ci.yml` `push` + `pull_request` 都用 `--new-from-merge-base=origin/develop`；push 到 develop 时 merge-base 退化导致 post-merge lint 门禁失效。**修复**：`pull_request` 保留 diff 降噪，`push` 改全量 lint 或 `before..after` diff。 | 1h | `.github/workflows/ci.yml` | PR#220 遗留报告 P2 |
| ~~PR220-5~~ | ~~见 P1 段 **EVENTROUTER-SUBSCRIPTION-IDENTITY-SPLIT-01**~~ ✅ PR #224 合入 | — | — | — |
| A5a-R3 | **ACCESSCORE-INITIALADMIN-THIN-WRAPPER-01** (Cx3, 🟡 **评估后可能 won't-do**): 消费者需同时 import `cells/accesscore` + `cells/accesscore/initialadmin` 才能设 TTL 等配置（见 `cmd/corebundle/access_module.go` `InitialAdminOpts []initialadmin.LifecycleOption`）。**候选方案**：在 `cells/accesscore` 加 re-export thin wrapper（`WithInitialAdminTTL(d time.Duration) Option` 等），composition root 仅 import accesscore 即可配置。**反方观点**：product-manager + architect review 均倾向保持现状——thin wrapper 会在 accesscore 增加 facade 维护成本，且 `LifecycleOption` 切片语义天然暴露了底层结构；省一次 import 的 DX 收益有限。**决策 trigger**（2026-04-24 review F-7 追加）：PR-A5b 合入时回看一次；或任意第二个 cell 公开"subpackage + functional options"模式时触发复查；无 trigger 触发则视为永久 won't-do。 | 1-2h | `cells/accesscore/` | PR#234 review PM F3 / architect P2 / reviewer F-7 |
| A5a-R9 | **INITIALADMIN-CREDPATH-NAMING-CHAIN-01** (Cx1, 🟡 可延后): `cmd/corebundle` / `examples/*` 拿 credential path 要经过三跳 `accesscore.ResolveBootstrapCredentialPath(stateDir)` → `initialadmin.ResolveCredentialPath(stateDir)` → `resolveCredentialPath()`（见 `cells/accesscore/cell.go:96-98` + `cells/accesscore/initialadmin/bootstrap.go:125-143`）。外层 wrapper 只是 zero-arg convenience，转发层数超出价值。**修复**：删 `accesscore.ResolveBootstrapCredentialPath`，调用方直接用 `initialadmin.ResolveCredentialPath`；或删内层 `resolveCredentialPath()` convenience。**选方案**：删外层（accesscore wrapper 只有 `cmd/corebundle` 一处调用方）。 | 0.5h | `cells/accesscore/cell.go` + `cmd/corebundle/` | PR#234 reviewer F-4 |
| A5a-R10 | **INITIALADMIN-ERRCODE-CONSISTENCY-01** (Cx1, 🟡 可延后): `cells/accesscore/initialadmin/bootstrap.go:79-90` `newBootstrapper` 对 deps/cfg 校验用裸 `fmt.Errorf("initialadmin: bootstrapper requires UserRepo")`，违反 CLAUDE.md "禁止裸 errors.New 对外暴露"。虽然 `newBootstrapper` 已 unexported（R1 lowercase），错误不跨包，严重度低，但 `lifecycle.go:136` 对称位置用的是 `errcode.New(errcode.ErrCellInvalidConfig, ...)`，风格不统一。**修复**：三处 `fmt.Errorf` → `errcode.New(errcode.ErrCellInvalidConfig, "initialadmin: ...")`。 | 0.5h | `cells/accesscore/initialadmin/bootstrap.go` | PR#234 reviewer F-6 |
| A5a-R11 | **LIFECYCLECONTRIBUTOR-BLOCKING-SEMANTICS-WARN-01** (Cx3, 🟡 可延后): `kernel/cell.LifecycleHook` 标 `EXPERIMENTAL` 且 godoc 要求 "OnStart must return promptly"，但 `runtime/bootstrap/lifecycle.go:runHook` 只做 ctx 超时硬保护，无"快接近 StartTimeout 但未触发"的软警告。下一个接入 `LifecycleContributor` 的 Cell 若写了慢 OnStart，排查困难。**修复**：在 phase3b 或 `runHook` 添加"OnStart 耗时 ≥ defaultStartTimeout × 0.8 时 slog.Warn with cell+hook name"，作为 early回路不 fail；第二接入方出现时把 EXPERIMENTAL 标记去掉并把 "return promptly" 升级为 lint 或测试护栏。 | 1-2h | `runtime/bootstrap/lifecycle.go` + `kernel/cell/registrar.go` godoc | PR#234 detailed C3 |
| A5a-R12 | **INITIALADMIN-TESTING-ENTROPY-UNEXPORT-01** (Cx2, 🟡 **评估后可能 won't-do**): `cells/accesscore/initialadmin/lifecycle.go:91` `WithPasswordSourceForTesting` 是 public export，外部包可用其降低密码熵，生产误用没有护栏。reviewer 建议改回 unexported `withPasswordSource`。**反方观点**：保留导出的理由是 `cells/accesscore` 包的 cell-level test（`package accesscore`，非 `accesscore_test`）需要注入确定性熵源，而 `cells/accesscore/initialadmin` 是独立 package，`withPasswordSource` 外包不可见。替代方案 `accesscore.NewLifecycleForTest(...)` 更侵入（加 test-only 构造函数），收益是消除 1 个 testing-named public option。**决策 trigger**：v1.0 前 security audit 若标红则落地；或 R1 之后发现 accesscore 包内有更多 test-only entropy 注入需求时打包处理。 | 1h | `cells/accesscore/initialadmin/lifecycle.go` | PR#234 reviewer F-2 |
| PR238-FU2 | **OBS-IS-INFRA-BUCKET-COUNTER-AUDIT-01** (P2, Cx1, 🟠 条件延后，下一个引入 Category-labeled Prometheus counter 前触发): PR#238 把 `ErrConfigRepoNotFound` 的 Category 从 `CategoryUnspecified` 改为 `CategoryDomain`，`IsInfraError(err)` 对该类错误返回从 `true` 翻转到 `false`。当前 `runtime/observability/metrics/`、`adapters/*/` 代码均未用 `errcode.Category` 或 `IsInfraError` 作为 Prometheus counter label；若未来任何 PR 引入这类 counter，必须同步评估 bucket 漂移对 SLO 告警的影响。**动作**：引入第一个 Category-labeled counter 的 PR 在 PR description 中显式声明 bucket 迁移表 + dashboards 更新；review 必拦截。 | 0.5h | 新 counter 所在文件 + ops runbook | PR#238 L4 reviewer OPS-02 |
| CTXCANCEL-HTTP-499 | **CTX-CANCEL-HTTP-499-MAPPING-01** (P2, Cx2, 🟡 可延后): 客户端主动断开（context.Canceled）当前被映射到 HTTP 500 + log5xx Error 级日志，与服务端真实故障混淆（Warn+Error 双发 / SLO Error rate 污染）。**修复**：参考 nginx `499 Client Closed Request` / Envoy / Google Cloud LB 的 semantics，为 ctx-cancel 路径新增 `ErrConfigReqCanceled` code + `pkg/httputil/codeToStatus` 映射 499。这样 `log5xx` 不再触发，只保留 repo 层 Warn。 | 3h | `pkg/errcode/` + `pkg/httputil/response.go` + `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer OPS-L4-04 |
| OTEL-SPAN-CTXCANCEL | **OTEL-SPAN-CTX-CANCEL-STATUS-01** (P2, Cx2, 🟡 可延后): ctx cancel 路径下 HTTP response 可能未写（client 已断开），`runtime/http/middleware/tracing.go` 的 `state.Status()` 仍为默认 200，span 错误标记失败，追踪系统中看不到这条 error。**修复**：在 `tracing.go` 中补 `ctx.Err() != nil && !state.Committed()` 判断强制 `span.SetStatus(codes.Error, ...)`。参考 OpenTelemetry semantic conventions for HTTP spans。 | 2h | `runtime/http/middleware/tracing.go` | PR#238 L4 Round-2 reviewer OPS-L4-06 |
| FU2-GOVERNANCE-STATIC | **OBS-IS-INFRA-BUCKET-GOVERNANCE-STATIC-01** (P2, Cx3, 🟠 与 PR238-FU2 联动，触发 FU2 的同 PR 必做): PR238-FU2 当前是 backlog prose 约定（"下一个引入 Category-labeled Prometheus counter 前强制评审"），没有任何静态护栏（CI / kernel/governance / lint）。未来引入 counter 的开发者可能完全不知道 FU2。**硬化方案**：在 `kernel/governance/` 新增 `rules_obs.go` 扫描 `runtime/observability/metrics/` + `adapters/*/` 下任何 Prometheus counter 注册，若 label 值来自 `errcode.Category` / `IsInfraError(...)` 输出 Warning 要求 PR 附 bucket-migration 表。**依赖**：PR238-FU2 同时触发时必做。 | 2h | `kernel/governance/rules_obs.go`（新）+ CI | PR#238 L4 Round-2 reviewer OPS-L4-08 + KG-R2-03 |
| BOOTSTRAP-LIFECYCLE-INTEG-TEST-PACKAGE | **BOOTSTRAP-LIFECYCLE-INTEGRATION-TEST-PACKAGE-01** (P2, Cx1, 🟡 可延后): `runtime/bootstrap/lifecycle_integration_test.go` 属 `package bootstrap_test`（外部测试包）但引用 `WithInternalListener` / `newLocalListener` 没有包前缀——前者定义在 `bootstrap.go`（生产代码），后者定义在 `bootstrap_test.go`（内部 `package bootstrap`）。当前 `go test -tags=integration ./...` 扫到该文件会 build 失败："undefined: WithInternalListener / newLocalListener"。CI 的 `integration-test` job 只跑 `./adapters/... ./tests/integration/... ./cmd/corebundle/... ./examples/ssobff/... ./cells/accesscore/slices/identitymanage/...`，不覆盖 runtime/bootstrap，所以没有即时 CI failure；但未来扩展 integration job 范围或本地 full-repo integration test 会翻车。**修复**：二选一——(a) 把文件改成 `package bootstrap`（与其他 listener-binding 测试同包），或 (b) 留 `package bootstrap_test` 但加 `bootstrap.` 前缀 + 把 `newLocalListener` 导出为 `bootstrap.NewLocalListenerForTest` 或搬到独立 testhelper 子包。建议 (a)，因为该测试本身就是白盒 lifecycle 探针，不需要黑盒包隔离。 | 0.5h | `runtime/bootstrap/lifecycle_integration_test.go` | discovered via PR #246 round-6 local `go test -tags=integration ./...` |
| PR246-ADR-SYNC | **PR-A11-ADR-SYNC-AFTER-ROUND6-01** (P2, Cx1, 🟡 可延后): PR #246 round-5/6 期间 FMT-18 从 regex 重写为 go/ast 实现并扩展了以下能力，但 `docs/architecture/202604242030-adr-kernel-wrapper-contract-observability.md` §9 仍描述 "regex scan wrapper.ContractSpec literals"：(1) AST 基础实现；(2) 识别 `wrapper.EventSpec(...)` 调用形态；(3) import alias 感知；(4) 同文件 `const = "..."` 引用求值；(5) `unresolved` warning。另外新增的 `wrapper.EventSpec(id, transport)` helper 是推荐写法，`docs/guides/cell-development-guide.md:187` 示例仍用旧的 `ContractSpec{Kind:"event", ...}` 字面量形式没同步。**修复**：更新 ADR §9 描述 + guide 的 event-spec 示例改用 `EventSpec` helper。**按 feedback_discuss_before_doc_update 原则**：本条是文档补录，不涉及决策变更。 | 1h | `docs/architecture/202604242030-adr-kernel-wrapper-contract-observability.md` + `docs/guides/cell-development-guide.md` | discovered via PR #246 round-6 local sweep |

### adapter

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| ~~A1~~ | ~~**READYZ-BROKER-HEALTH-01**~~ ✅ `connection.go::Health(ctx) error` 已实现；bootstrap 通过 `WithManagedResource(conn)` 接入（PR-A18 起；前身 `WithBrokerHealth` / `BrokerHealthChecker` 已删除，probe 名 `"rabbitmq"` → `"rabbitmq_ready"`） | — | — | — |
| ~~A2~~ | ~~**P4-TD-05**~~ ✅ `tests/integration/outbox_fullchain_test.go` 已建（PG+RMQ+Redis 3-container）（2026-04-23 核实） | — | — | — |
| A7 | **POOLSTATS-IFACE-01** (🟡 可延后): 三个 adapter PoolStats 公共接口（OTel collector 消费） | 1h | `adapters/postgres/pool.go` + `redis/client.go` + `rabbitmq/connection.go` | PR#134 review |
| A8 | **CI-DIGEST-01** (🟡 可延后): testcontainers 镜像 tag+digest 双固定 | 1h | `adapters/*/integration_test.go` | PR#139 review |
| A9 | **CI-LINT-PIN-01** (🟡 可延后): golangci-lint patch 级固定 + dependabot | 1h | `.github/workflows/ci.yml` | PR#139 review |
| A10 | **OBS-LGTM-INTEGRATION-01** (Cx3, 🟡 可延后): `//go:build integration` 夜间 OTel collector 真实 OTLP 协议兼容性测试 | 2h | `adapters/otel/integration_test.go` | PR#157 review S6-04 |
| ~~A3~~ | ~~**SHARED-DEPS-KEYPROVIDER-EXIST-01**~~ ✅ PR#207 合入（`shared_deps.go:117-121`）| — | — | — |
| ~~M3~~ | ~~**CONFIG-STALE-KEY-OBSERVE-01**~~ ✅ PR#207 slog.Warn + callback hook；PR#209 生产 Prometheus counter `gocell_config_stale_cipher_total` 注册 | — | — | — |
| ~~A13~~ | ~~**VAULT-TOKEN-RENEW-01**~~ ✅ PR#207 LifetimeWatcher + tokenRenewalWorker；PR#209 Prometheus counters `gocell_vault_token_renew_{success,failure}_total` + registry 注册 | — | — | — |
| ~~A19~~ | ~~**VAULT-READINESS-HEALTH-01**~~ ✅ PR#205 合入（Vault 启动自检错误分类 + ManagedResource.Checkers 接入 /readyz） | — | — | — |
| ~~A20~~ | ~~**VAULT-KEYID-CONTRACT-STRONG-01**~~ ✅ PR#209：`kernel/crypto/verifykeyid.go` ParseKeyID/MatchKeyID helpers + Vault Decrypt 迁移 + godoc "verifiable metadata" 升级 | — | — | — |
| ~~A14~~ | ~~**VAULT-AUTH-PLUGGABLE-01**~~ ✅ PR#230 合入（`adapters/vault/auth.go` `AuthMethod` 接口 + `MethodToken`/`MethodAppRole`/`MethodKubernetes` 三实现 + `AssertForRealMode()` real-mode guard + `tokenRenewalWorker.doReauth()` 续租失败自愈无限退避重试。`VAULT-RENEWAL-DEGRADATION-GAUGE` 被更优方案替代——续租结束直接走重认证不再降级，metric 无需增加。残余：K8s auth e2e 测试补充见 PR-A8-RESIDUAL） | — | — | — |
| PR-A8-RESIDUAL | **VAULT-K8S-AUTH-E2E-01** (Cx2, 🟡 可延后): PR#230 K8s auth 仅单测覆盖（`kubernetesAuth` struct），无 e2e/集成测试演证 ServiceAccount 挂载 → JWT login → secret fetch 完整链路。**修复**：补 integration test（可用 kind/minikube 或 vault test container + mock SA token） | 4h | `adapters/vault/auth_test.go` + integration fixture | PR#230 合并后残余 |
| ~~PR-A11-M~~ | ~~**CELL-ROUTES-MOUNT-MIGRATION-01**~~ ✅ PR-A11 round-4：生产 HTTP 路由已迁移到 `auth.Mount(Route{Contract: ...})`，生产 consumer 订阅已迁移到 `AddContractHandler(spec, handler, cg)`；最终清理删除了 legacy test shim，当前只保留 contract-first API。 | — | — | — |
| ~~PR-A11-TESTMIGRATE~~ | ~~**TEST-ROUTEDECL-SUNSET-01**~~ ✅ PR-A11 final cleanup：剩余测试已迁移到 `auth.Mount(Route{Contract: ...})` / `AddContractHandler(spec, handler, consumerGroup)`；`runtime/auth/routedecl.go`、`cell.EventRouter.AddHandler`、`eventrouter.Router.AddHandler` 与对应 compat 测试已删除。 | — | — | — |
| ~~PR-A11-B~~ | ~~**OUTBOX-CONSUMER-WRAPPER-01**~~ ✅ PR-A11 一并落地：`AddContractHandler(spec, handler, cg)` 将 contract identity 写入 subscription；bootstrap 将 `ContractTracingMiddleware(tracer, redactor)` 放在 `ObservabilityContextMiddleware` 之后、`ConsumerBase` 之前，consumer span 覆盖 duplicate/claim/retry/final disposition；outbox metadata 传播 `traceparent`，OTel consumer span 继续 publish trace。 | — | — | — |
| ~~PR-A11-V~~ | ~~**FMT-17-SPEC-CONTRACT-SYNC-01**~~ ✅ PR-A11 round-4 以 FMT-18 落地：`gocell validate --strict` 扫描 `cells/**/*.go` 的 `wrapper.ContractSpec{}` 字面量并和 `contracts/**/contract.yaml` 交叉校验 ID / Kind / HTTP Method / Path；`examples/**` 因 demo contract 可无 YAML backing 暂豁免。 | — | — | — |
| ~~PR-A11-S~~ | ~~**LOGGING-SLOG-CONTRACT-ID-01**~~ ✅ PR-A11 round-4：`runtime/observability/logging.contextHandler` 追加 `contract_id`，从 `kernel/ctxkeys.ContractIDFrom` 读取，空值跳过。 | — | — | — |
| ~~PR-A11-R1~~ | ~~**WRAPPER-NO-PACKAGE-STATE-LINT-01**~~ ✅ PR-A11 round-4 以 FMT-19 落地：strict validate 拒绝 `kernel/wrapper/*.go` 中接口/指针类型的可变 package-level var；允许 compile-time interface check 与零值 sentinel。 | — | — | — |
| ~~PR-A11-R4~~ | ~~**CONTRACT-OBS-ROUND4-REVIEW-FIXES-01**~~ ✅ PR-A11 round-4 review finding 一次性收口：HTTP 改为单 span owner + AttrCarrier（修复 double span / panic 200 / ResponseWriter optional interface），consumer contract span 移到 subscription middleware 外层（覆盖 ConsumerBase short-circuit），async `traceparent` 继续 OTel trace，OTel `[]byte` attr 脱敏为 len+sha256，`simpleSpan` 加锁满足并发契约。 | — | — | — |
| ~~PR-A11-SEC~~ | ~~**HTTP-SPAN-ERROR-REDACT-01**~~ ✅ PR-A11 final cleanup：`runtime/http/middleware.Tracing` 增加 `WithErrorRedactor`；`Recovery` 在 active HTTP span 上记录 redacted panic error；`bootstrap.WithErrorRedactor` 同时传给 HTTP tracing options 与 consumer `ContractTracingMiddleware`。 | — | — | — |
| ~~A15~~ | ~~**VAULT-NAMESPACE-MULTITENANT-01**~~ ✅ PR-A18：`NewTransitKeyProviderFromEnv` 读 `VAULT_NAMESPACE` → `client.SetNamespace`（HashiCorp 官方 env，未引入 GOCELL 别名）。 | — | — | — |
| ~~A16~~ | ~~**VAULT-DATAKEY-ENDPOINT-01**~~ ✅ PR-A18：envelope encrypt 主路径替换为 `transit/datakey/plaintext`（server-side HSM-backed DEK 生成；删 `wrapDEKWithVault` + 客户端 crypto/rand DEK）。Decrypt 路径不变，老 EDK 继续可解。S14a / S3 大 blob 后续需求直接复用本路径。 | — | — | — |
| ~~A17~~ | ~~**VAULT-AEAD-UTIL-EXTRACT-01**~~ ✅ PR#205 合入（`pkg/aeadutil/gcm.go` EncryptSplit/Decrypt 纯函数，双拷贝消除） | — | — | — |
| ~~A18~~ | ~~**VAULT-ROTATE-OPTIMISTIC-LOCK-01**~~ ✅ PR-A18：`TransitKeyProvider` 删 `sync.RWMutex` 字段，`atomic.Int64 cachedLatestVersion` lock-free version cache；`Current()` 命中缓存零 Vault 调用；`Rotate()` 无锁（Vault 服务端原子，本地仅缓存失效+刷新）。多 pod 弱一致性对正确性无影响（keyID 从 Vault 响应解，不从缓存读）。 | — | — | — |
| A21 | **HEALTH-CHECKER-CTX-BUDGET-01** (P2, Cx3, 🟡 可延后): `runtime/http/health.Checker` 签名为 `func() error`，聚合层 (`Handler.ReadyzHandler`) 顺序执行所有 checker 且无统一超时/并行策略。`TransitKeyProvider.Checkers["vault_transit_ready"]` 内部自控 3s context，但其他 checker（PGResource、broker health）预算各自为政；未来 checker 退化或新增时，`/readyz` 尾延迟会叠加放大。**方案**：将 `Checker` 升级为 `func(ctx.Context) error`，`ReadyzHandler` 为整个聚合注入统一 deadline（如 2s）并考虑并发执行；失败时日志/响应输出每个 checker 的耗时与错误名称，用于运维定位。前置：需要 `kernel/lifecycle.ManagedResource.Checkers()` 签名同步升级。排期建议 R2 之后独立 PR 做。 | 3h | `runtime/http/health/health.go` + `kernel/lifecycle/managed_resource.go` + 所有 `Checkers()` 实现 | PR #205 review 2026-04-20 P2 finding |
| V-A14 | **ADAPTER-CLOSE-HELPER-01** (Cx2): Redis `client.go:243-265` / Postgres `pool.go:160-179` 的 close with deadline 模式完全一致（RabbitMQ 有锁+cleanup 差异）。**修复**：抽 `adapters/adapterutil.CloseWithDeadline`，给 Redis + Postgres 用。 | 2h | `adapters/adapterutil/`（新） + `adapters/redis/` + `adapters/postgres/` | 202604221954-verification §A14（P2-5） |
| PR225-N1 | **RMQ-WAITANDCLOSE-ABANDONED-GOROUTINE-TEST-01** (Cx1, 🟡 可延后): `adapters/rabbitmq/subscription_run.go` `waitAndClose` 在 ctx 超时返回后，内部 goroutine 仍跑完 `localWg.Wait()` 再 `close(done)`；`done` 是无缓冲 channel 且无接收方，属于 intentional abandoned-on-timeout 模式（与 fx StopTimeout 语义一致），但缺一个回归测试确认 goroutine 最终随 processDelivery 完成而退出。 | 2h | `adapters/rabbitmq/subscription_run.go` + `subscription_run_test.go` | PR#225 reviewer 第二轮 N1（2026-04-24） |
| PR237-F06 | **DUAL-LISTENER-DEVMODE-WARN-TEST-01** (Cx1, 🟡 可延后): PR-A14a reviewer 留尾：`cmd/corebundle/shared_deps.go::internalGuardFromEnv` 在 dev 模式（`GOCELL_ADAPTER_MODE=""`、未设 `GOCELL_SERVICE_SECRET`）返回 nil guard 并发出 `slog.Warn`，当前无测试断言该 Warn 被触发。添加 `internal_guard_env_test.go` table-driven 覆盖 dev/real × 有/无 SERVICE_SECRET 四象限。 | 30min | `cmd/corebundle/internal_guard_env_test.go`（新） | PR #237 reviewer F-06 |
| PR237-F13 | **TESTNEW-PORT-COLLIDE-RENAME-01** (Cx1, 🟡 可延后): PR-A14a reviewer 留尾：`runtime/bootstrap/bootstrap_test.go::TestNew_WithOptions` 断言 `primaryAddr == ":9090"`，与 PR-A14a 新增的 internal-listener 默认端口 `:9090` 命名撞名（语义正确，但读起来有二义性）。重命名为 `:7070` 或类似值以避免跟默认值混淆。 | 10min | `runtime/bootstrap/bootstrap_test.go` | PR #237 reviewer F-13 |
| PR237-T1 | **DUAL-LISTENER-TEST-GAPS-01** (Cx1, 🟡 可延后): PR-A14a 第二轮六角色 review 测试维度 10 条 Cx1 gap 合并登记：(T-01) FinalizeAuth 排序不变量——duplicate 与 Delegated 不一致交叉用例；(T-02) `shutdownBothServers` 部分错误聚合未测（只有 clean path）；(T-03) httpErrCh 双关闭在 `-race` 下未覆盖；(T-04) 测试仅覆盖 `owned=false` primary-listener 路径，`owned=true` 分支零覆盖；(T-05) `WithInternalMiddleware` 多个 mw 的顺序保真；(T-06) `FinalizeAuth` 二次调用的错误文本；(T-07) 有 metas 未 FinalizeAuth 的 `ServeHTTP` panic 文本；(T-08) 确认 `auth_integration_test.go` 的 `//go:build integration` tag 存在；(T-09) `TestAuthWiring_InternalGuard_*` 对 internal listener 的 wait-for-ready；(T-10) `runtime.NumGoroutine` baseline 捕获时机偏早。 | 3h | `runtime/http/router/*_test.go` + `runtime/bootstrap/*_test.go` + `cmd/corebundle/auth_integration_test.go` | PR #237 第二轮 reviewer (testing 维度) |
| PR237-OB2 | **INTERNAL-LISTENER-METRICS-01** (Cx2, 🟡 可延后): PR-A14a 的 internal mux 未挂 `middleware.Metrics`——`http_requests_total` / `http_request_duration_seconds` 完全不记录 /internal/v1/* 流量。方案：(a) 给 collector 加 `listener` 标签，两个 mux 用同一 Provider 不同 label；或 (b) 新增 router.WithInternalMetricsCollector option + 第二个 collector 实例。运营可见度缺失非 P0，且 Provider 接口改造属于更大议题，暂延 PR-A14b 或单开独立 PR。 | 4h | `runtime/http/router/router.go` + `runtime/http/middleware/metrics.go` + `runtime/bootstrap/bootstrap_phases.go::autoWireHTTPMetricsCollector` | PR #237 第二轮 reviewer OB-F2 |
| PR237-DX1 | **LISTENER-OPTION-NAMING-UNIFY-01** (Cx2, 🟡 可延后): PR-A14a 选项前缀不对称——`WithHTTPPrimaryAddr` / `WithHTTPInternalAddr`（带 HTTP）与 `WithPrimaryListener` / `WithInternalListener`（不带 HTTP）。IDE 补全时两组选项不相邻。方案：统一为 `WithHTTP*Listener` 或去掉 addr 侧的 HTTP 前缀。当前只有 gocell 自身无外部调用方，可在任意时间一次改名。 | 2h | `runtime/bootstrap/bootstrap.go` + 所有测试 + `cmd/corebundle` + `examples/*/main.go` | PR #237 第二轮 reviewer DX F3 |
| PR237-A2 | **ROUTEDECL-DELEGATED-RENAME-01** (Cx2, 🟡 可延后): `auth.Route.Delegated` / `kernel/cell.AuthRouteMeta.Delegated` 字段名保留了 pre-PR-A14a 的"JWT 验证下放"语义，但 PR-A14a 后它只是 FinalizeAuth 对 `/internal/v1/*` 路由的 **一致性标记**，不再驱动任何 runtime predicate。rename 为 `Internal bool` 更贴合新职责。本 PR 仅更新 godoc；rename 涉及 kernel/cell 公共接口 + 16 处调用站点，作为独立 PR 一次做完。 | 2h | `kernel/cell/registrar.go` + `runtime/auth/routedecl.go` + `runtime/http/router/router.go` + `runtime/bootstrap/bootstrap_phases.go` + `cells/accesscore/slices/rbacassign/handler.go` + 所有测试 | PR #237 第二轮 reviewer architect A2 |
| PR237-A4 | **INTERNAL-PREFIX-CONSTANT-RELOCATE-01** (Cx2, 🟡 可延后): `runtime/http/router/router.go::internalPathPrefix = "/internal/v1/"` 是路由包私有常量，但 `.claude/rules/gocell/api-versioning.md` 将 `/internal/v1/` 作为跨 runtime 的控制面前缀。若后续出现 v2-internal，router 包不是正确位置。建议迁到 `pkg/httputil/prefix.go` 或类似位置，与 `PublicAPIV1Prefix` 一起作为单一来源。 | 1h | `pkg/httputil/prefix.go`（新） + `runtime/http/router/router.go` | PR #237 第二轮 reviewer architect A4 |
| PR240-A1 | **MANAGEDRESOURCE-CONTRACT-FORMALIZE-01** (Cx2, 🟡 可延后): PR-A18 把 RabbitMQ Connection 接入 `lifecycle.ManagedResource`，叠加 Vault / Postgres / Redis 已经接入，`ManagedResource` 已成事实上的 adapter 生命周期契约。建议：(a) `kernel/lifecycle/doc.go` 写实施清单（Worker() != nil 何时合理 / Checkers 命名后缀 / Close 排干约定）；(b) 加 archtest 强制 `adapters/*` 顶层 exported 类型实现 `ManagedResource` 或显式 opt out。 | 4h | `kernel/lifecycle/doc.go`（新） + `kernel/governance/rules_arch.go` | PR #240 L4 architect 跨切面 finding 1 |
| PR240-A2 | **PROBE-NAMING-CONVENTION-RULE-01** (Cx1, 🟡 可延后): PR-A18 后 probe 名出现两种风格——`vault_transit_ready` / `rabbitmq_ready`（snake_case + `_ready`）vs outbox-relay 的 `outbox-relay-poll/reclaim/cleanup`（kebab-case + role）。约定仅靠 review 维护，下一个 adapter 会再引入第三种风格。建议在 `.claude/rules/gocell/observability.md` 写入命名规则。 | 30min | `.claude/rules/gocell/observability.md` | PR #240 L4 architect 跨切面 finding 2 |
| PR240-OB1 | **VAULT-CACHE-VERSION-METRIC-01** (Cx2, 🟡 可延后): PR-A18 `cachedLatestVersion` lock-free 缓存对正确性安全（keyID 来自 Vault 响应），但多 pod 缓存陈旧时 SRE 无法诊断"为什么 Pod X 报 keyID v3 而 Pod Y 报 v4"。加 `gocell_vault_cached_key_version` gauge（每次 `Current()` cache hit 时 emit）+ `slog.Debug` 字段。仅诊断价值，无修复路径。 | 2h | `adapters/vault/transit_provider.go` | PR #240 L4 devops finding 6 |
| ~~PR240-T1~~ | ~~**VAULT-ENCRYPT-MALFORMED-RESPONSE-COVERAGE-01**~~ ✅ PR#242：`adapters/vault/transit_datakey_test.go::TestEncrypt_MalformedDatakeyResponse` 覆盖 datakey 响应 `plaintext` 缺失/非 string、DEK base64 decode 失败、`ciphertext` 缺失/非 string；所有路径断言 `ErrKeyProviderEncryptFailed` 且 permanent。 | — | — | — |
| PR240-DX1 | **RMQ-MANAGEDRESOURCE-WIRING-EXAMPLE-01** (Cx1, 🟡 可延后): PR-A18 删了 `WithBrokerHealth` 改走 `WithManagedResource(conn)`，`adapters/rabbitmq/doc.go` 写了 migration note 但无可运行示例。当 cmd/corebundle 真正接入 RMQ 时（目前只有 InMemoryEventBus）随手补一段 5 行 wiring snippet：`conn, _ := rabbitmq.NewConnection(...)` → `bootstrap.New(WithManagedResource(conn), WithPublisher(rabbitmq.NewPublisher(conn)), WithSubscriber(...))`。 | 30min | `adapters/rabbitmq/doc.go` 或 `docs/patterns/rabbitmq-wiring.md`（新） | PR #240 L4 product-manager DX finding |
| PR240-DOC1 | **VAULT-DOC-AAD-DESCRIPTION-STALE-01** (Cx1, 🟡 可延后, 文档归档): `docs/archive/202604191800-adr-config-value-encryption.md:173-181` 描述"Passes the AAD as the `context` field"——pre-R1c bug 的描述，PR#205 后已不发送 AAD 给 Vault，PR-A18 进一步改 datakey 路径。归档 ADR 加 strikethrough 注解避免误导读者。 | 15min | `docs/archive/202604191800-adr-config-value-encryption.md` | PR #240 L4 devops finding 2 |
| PR237-A5 | **PR-A14B-SCOPE-REWRITE-01** (Cx1, 🟡 可延后): PR-A14a 彻底重构 + 吸收 PR-A32 后，PR-A14b 剩余范围（声明式 RouteGroup API + health listener + mTLS + 编译期 listener ref 校验）与原 plan block 不匹配。plan 需更新说明物理双 mux 已在 14a 完成、14b 聚焦声明式 API + 可选第三 listener。 | 30min | `docs/plans/202604232330-025-architecture-pr-implementation-plan.md:444+` | PR #237 第二轮 reviewer architect A5 |
| PR237-A6 | **BOOTSTRAP-LISTENER-SLICE-01** (Cx2, 🟡 可延后, 🟠 PR-A14b 触发): `phase7StartHTTPServer` 硬编码 2 个 server；未来 3rd listener（health 独立 / admin 独立）需要改 phase 本身。`serveAndReport` 已经泛化于 (name, srv, ln)，extensibility 半完成。方案：重构 phase7 迭代 `[]listenerBinding{name, handler, addr/ln}` slice（PR-A14b 的 RouteGroup API 自然拥有该 slice）。 | 2h | `runtime/bootstrap/bootstrap_phases.go` | PR #237 第二轮 reviewer architect A6 |
| PR237-PM5 | **DUAL-LISTENER-DEPLOYMENT-GUIDE-01** (Cx2, 🔴 发布前必做, 🟡 可延后至发布前 docs 阶段): `docs/operations/` 缺双 listener 部署章节——未描述 (a) LB/ingress 只映射 8080，(b) internal listener VPC-only 绑定（启用 mTLS 或 service-token），(c) 从单 listener 迁移的现有部署改造步骤。方案：新增 `docs/operations/dual-listener-deployment.md` 或在 first-run-setup 文档扩写一节。 | 2h | `docs/operations/dual-listener-deployment.md`（新）或 `docs/operations/first-run-setup.md` | PR #237 第二轮 reviewer product PM-05 |
| PR237-PM7 | **EXAMPLE-INTERNAL-LISTENER-COMMENT-01** (Cx1, 🟡 可延后): `examples/{ssobff,todoorder,iotdevice}/main.go` 的双 addr 配置缺注释，读者不知道为什么 todoorder/iotdevice 这类不暴露 /internal/v1/* 路由的 example 也要配第二个端口（答：当前 internal addr 无 "disabled" 默认，必填）。方案：在每个 example 双 addr 行补一行注释；或实现 `WithHTTPInternalDisable()` 允许单 listener 模式（但与 PR-A14a 物理隔离契约冲突，权衡后再定）。 | 1h | `examples/*/main.go` + `runtime/bootstrap/bootstrap.go` (optional new option) | PR #237 第二轮 reviewer product PM-07 |
| PR239-S1 | **PATH-PARAM-UUID-RUNTIME-VALIDATION-01** (Cx2, P1 安全, 🟠 多 pod 生产前触发): PR-A9 合约元数据声明 `format: uuid` 的 path param 在 runtime handler 层未 `uuid.Parse` 校验；恶意客户端可传任意字符串穿透 `r.PathValue("id")`。涉及 identitymanage/sessionlogout/rbaccheck 等多处。**修复**：在每个读取 UUID pathParam 的 handler 入口加 `uuid.Parse(id)`，非法值返回 400。Cx2（~4 个 handler 文件） | 2h | `cells/accesscore/slices/identitymanage/handler.go` + `cells/accesscore/slices/sessionlogout/handler.go` + `cells/accesscore/slices/rbaccheck/handler.go` | PR#239 六角色 review round 1（S3 安全席位） |
| PR239-S2 | **INTERNAL-GUARD-REQUIRED-01** (Cx3, P1 安全, 🔴 多 pod 生产前必做): `runtime/auth/middleware.go:51-54` `/internal/v1/*` 使用 `Delegated: true` 路径跳过 JWT 验证；如 composition root 未显式调用 `WithInternalPathPrefixGuard`，内部端点依赖 `AnyRole(RoleInternalAdmin)` 的 Principal 存在假设。**修复**：在 `FinalizeAuth` 阶段将 delegated 路径存在作为 fail-fast 校验，要求 guard 必须注册；或在 bootstrap layer 强制默认 InMemoryGuard（与 S-nonce 类似）。需 architect 决策 | 4h | `runtime/bootstrap/bootstrap.go` + `runtime/http/router/router.go` | PR#239 六角色 review round 1（S4 安全席位） |
| PR239-S3 | **CONTRACT-4XX-COMPLETENESS-01** (Cx1, P2, 🟡 可延后): `http.auth.login.v1` / `http.auth.refresh.v1` 两个 Public 端点缺 `responses[400]`（bad credentials / malformed token）声明；其他已声明端点缺 `responses[400]` 的 bad-request 情况。**修复**：全仓 HTTP contract 扫一遍 handler 实际会返回的 4xx，补齐声明 | 3h | `contracts/http/**/contract.yaml` | PR#239 六角色 review round 2（S4 安全席位） |
| PR239-T1 | **SCAFFOLD-SMOKE-01** (Cx1, 🟡 可延后): 缺 `gocell scaffold contract --kind=http` 生成文件能通过 `gocell validate --strict` 的端到端冒烟测试。**修复**：`kernel/scaffold/scaffold_test.go` 或新建 `scaffold_smoke_test.go` 加一条 case：CreateContractHTTP → Validator 通过（预期 FMT-13 失败因模板 TODO placeholder 无 pathParams，这本身是 DX 设计；断言错误码而非通过） | 1h | `kernel/scaffold/scaffold_test.go` | PR#239 六角色 review round 2（测试席位） |
| PR239-T2 | **EXTRACTPATHPLACEHOLDERS-WHITEBOX-01** (Cx1, 🟡 可延后): `kernel/governance/rules_fmt.go::extractPathPlaceholders` 无独立白盒单元测试；去重逻辑、空串、非法命名（`{123abc}`）、chi 原生语法（`{name-with-dash}` / `{name:regex}`）的 silent miss 行为仅间接覆盖。**修复**：新增 `rules_fmt_test.go::TestExtractPathPlaceholders` table-driven | 1h | `kernel/governance/rules_fmt_test.go`（新） | PR#239 六角色 review round 2（测试席位） |
| PR239-O1 | **CI-VALIDATE-STRICT-FLAG-01** (Cx1, 🟡 可延后): `.github/workflows/_build-lint.yml` 的 `gocell validate` 未加 `--strict` flag；FMT-16 / FMT-C1 / FMT-A1 / DOC-NAME-01 等 strict-only 规则在 CI 不会 fire。**修复**：改为 `go run ./cmd/gocell validate --strict`。预存在问题，PR-A9 六角色 review round 1 发现 | 10min | `.github/workflows/_build-lint.yml` | PR#239 六角色 review round 1（运维席位） |
| PR239-DX1 | **FMT-13-HOW-TO-FIX-HINT-01** (Cx1, 🟡 可延后): FMT-13 错误消息 `"http contract %q path placeholder %q has no pathParams declaration"` 只说明"是什么"不告知"怎么修"。**修复**：追加修复 YAML 片段提示，形如 `"; add: pathParams:\n  {name}:\n    type: string"`。建议并入 PR-A10 OUTPUT-JSON-SARIF 诊断模型一并做 | 1h | `kernel/governance/rules_fmt.go` | PR#239 六角色 review round 1（DX 席位） |
| PR239-OB1 | **CONTRACT-HEALTH-HTTP-COLUMNS-01** (Cx1, 🟡 可延后): `cmd/gocell/app/check.go::checkContractHealth` 输出表列仅 `ID / KIND / LIFECYCLE / OWNER`，不显示 HTTP transport 字段（method / path / successStatus / responses 数量）；监控 dashboard 无法从 `gocell check contract-health` 输出判断合约 transport 元数据完整性。**修复**：加 `METHOD / PATH` 列或独立 sub-section。建议并入 PR-A10 诊断模型统一优化 | 1h | `cmd/gocell/app/check.go` | PR#239 六角色 review round 1（运维席位） |

### slice / cell 收口

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| ~~S2-follow~~ | ~~**CONTRACT-ERROR-SCHEMA-EXTEND-01**~~ ✅ PR#239 合入（27 个平台 HTTP contract + 5 个 example contract 补 `responses[401]`，admin-guarded 再补 `responses[403]`；Public 端点 auth/login/refresh 保持无 401/403） | — | — | — |
| ~~S4~~ | ~~**EVENT-PAYLOAD-TYPED-01** (Cx2): sessionlogin/sessionlogout/configwrite/configpublish/auditappend/auditverify 事件 payload `map[string]any` → typed event struct~~ ✅ PR-A6 (PR#250) 合入：7 个 producer 全量迁 `outbox.Emit[T]` + typed struct (accesscore `dto.Session{Created,Revoked}Event` / configcore `domain.Config{EntryWritten,VersionPublished,Rollback}Event` / auditcore `dto.Audit{Appended,IntegrityVerified}Event`)；同时拆 `event.config.changed.v1` 为 `entry-written` + `version-published` 双 topic；payload 字段全量 camelCase。event.user.* / event.flag.* 迁 camelCase 留在 PR250-F2 trailing sweep。 | — | — | — |
| S41 | ~~**MARSHAL-ERR-EXPLICIT-01**~~ ✅ PR-A6 (PR#250) 合入：`sessionlogin/service.go:149` + `sessionlogout/service.go:94` 的 `_, _ = json.Marshal(...)` 通过迁 `outbox.Emit[T]` 在签名层面消除；helper 返回 `error` 强制 caller 处理，`_ = outbox.Emit(...)` 会被 errcheck lint 拦截。 | — | — | — |
| ~~S5~~ | ~~**RBAC-REVOKE-POST-01**~~ ✅ `handler.go:62` revoke 路由已声明为 `POST`（2026-04-23 核实） | — | — | — |
| ~~S6~~ | ~~**RBAC-LAST-ADMIN-GUARD**~~ ✅ `ports/role_repo.go:21-28` `RemoveFromUserIfNotLast` 原子计数保证；`service.go:200` 使用该方法（2026-04-23 核实） | — | — | — |
| ~~S7~~ | ~~**VALIDATE-EVIDENCE-CI-01**~~ ✅ `ci.yml:44,47` 含 `gocell validate` + `check contract-health`，失败阻断（2026-04-23 核实） | — | — | — |
| ~~S10~~ | ~~**MODE-SEMANTIC-SPLIT-01**~~ ✅ `mode.go` 中写路径独立类型已落地，Init 单点映射（2026-04-23 agent 核实） | — | — | — |
| ~~S11~~ | ~~**CONFIG-CORE-INIT-COGNIT-01**~~ ✅ `cell.go` Init() 已拆分，nolint 抑制解除（2026-04-23 agent 核实） | — | — | — |
| S13-follow | **4XX-LOG-SAMPLING-01** (P3, Cx1, 🟡 可延后): 高频端点（如 GET config）4xx 日志可加采样（1%）或在 rate-limiter 触发后限速，避免大量 429 日志淹没告警通道。注：PR#181 F3 已把 log4xx 字段最小化（移除 client message，仅保留 code/status/correlation IDs/internal），采样必要性已大幅下降；仅在真实运维告警通道过载时再做 | 1h | `pkg/httputil/response.go` | PR-CONFIG-POLISH 后续 |
| S14a | **CONFIG-VALUE-KMS-AWS-PROVIDER-01** 🟠（明确部署目标云平台后触发）：基于 KeyProvider 接口加 AWS-KMS / GCP-KMS adapter；envelope encryption（DEK 本地 + KEK 在 KMS）；rotation 走 KMS API；dev fallback LocalAES。**前置 S14 CONFIG-VALUE-ENCRYPTION-01 ✅ PR#195 已完成（LocalAES + VaultTransit）**。**触发**：明确生产云平台 + 通过 KMS 安全评审 | 6h | 🟠 | `runtime/crypto/aws_kms_provider.go`（新）+ `gcp_kms_provider.go`（新）+ `cmd/corebundle/bundle.go` env 解析 | PR-CC-VALUE-ENCRYPT 后续 |
| ~~S15~~ | ~~**ERROR-CTX-CANCELLED-CLASSIFY**~~ ✅ PR#238 合入（`config_repo.go` ctxCanceledError + `errcode.Category` 显式化 + dual-channel 测试覆盖，从 PR-A5b 搭车落地） |
| ~~S18~~ | ~~**JWT-AUDIENCE-ENV-VAR-01**~~ ✅ `GOCELL_JWT_AUDIENCE` env var 已接入；`main.go:222` 所有模式强制要求；`main.go:257` 启动时 `slog.Any("audiences", ...)` 打印（2026-04-23 核实） | — | — | — |
| S19 | **JWT-AUDIENCE-DRIFT-INTEG-TEST-01** (P2, Cx1, 🟡 可延后): `cmd/corebundle/jwt_aud_integration_test.go` 直接调 `deps.issuer.Issue(…, []string{jwtAudience}, …)` 而非真实 sessionlogin 路径，无法检测 sessionlogin/sessionrefresh 硬编码与 `DefaultJWTAudience` 的 drift。**修法**：集成测试调用 sessionlogin.Service.Login → 解析响应 token → VerifyIntent，使 drift 编译失败而非运行时静默放行。**前置**：S18（accesscore WithTokenAudience 注入）落地后更易实现。来源: PR#170 review F-T-3 | 2h | `cmd/corebundle/` + `cells/accesscore/slices/sessionlogin/` | PR#170 review F-T-3 |
| ~~S20~~ | ~~**JWT-AUDIENCE-STARTUP-LOG-01**~~ ✅ `main.go:257` `slog.Any("audiences", reg.Audiences())` 已存在，随 S18 一并完成（2026-04-23 核实） | — | — | — |
| S21 | **JWT-AUD-TEST-TABLE-DRIVEN-01** (P3, Cx1, 🟡 可延后): `runtime/auth/jwt_aud_test.go` 9 个场景为独立函数，违反 CLAUDE.md table-driven 规范。**修法**：重构为 `struct { name; expectedAuds []string; tokenAud []string; wantErr bool; errContains string }` 结构。来源: PR#170 review F-T-5 | 1h | `runtime/auth/jwt_aud_test.go` | PR#170 review F-T-5 |
| S22 | **REFRESH-AUD-REAL-ROUTE-TEST-01** (P2, Cx2, 🟡 可延后): `runtime/auth/middleware_aud_test.go` 的 refresh-path audience 回归测试手造 fake handler 直接调 `VerifyIntent`，不经过真实 route registration / body binding / `Service.Refresh`，无法检测 public-route bypass 或 body decode 回归。**修法**：在 `cells/accesscore/auth_integration_test.go` 补一条真实 HTTP 测试：POST `{"refreshToken": wrong-aud-token}` 到 `/api/v1/access/sessions/refresh`，断言返回 401；再补 missing-aud token 场景。**前置**：可独立于 S18 实施。来源: PR#171 外部审查 F2 | 2h | `cells/accesscore/auth_integration_test.go` + `runtime/auth/middleware_aud_test.go` | PR#171 外部审查 |
| S23 | **AUTH-WALKTHROUGH-COMPOSE-01** (P2, Cx3, 🟡 可延后): `examples/ssobff/walkthrough_test.go` 手装精简 server（无 bootstrap lifecycle），与 `main.go` 的真实组装路径、README 步骤三份语义各自独立，可掩盖 public-endpoint wiring / configcore 接线 / audit event delivery 的回归。**修法**：提取 `NewSSOBFFApp(opts...)` 组装函数被 `main.go` 和 walkthrough test 共用；test server 走同一 bootstrap + Start/Stop 路径。来源: PR#172 review F3（OUT_OF_SCOPE） | 4h | `examples/ssobff/bootstrap.go`（新）+ `main.go` + `walkthrough_test.go` | PR#172 review F3 |
| S24 | **AUTH-MIDDLEWARE-AUD-REFRESH-E2E-01** (P3, Cx2, 🟡 可延后): `TestAuthMiddleware_WrongAudience_RefreshPath_Returns401` 直接调 `verifier.VerifyIntent()`，未经过 `AuthMiddleware` 真实中间件链（parse req → call verifier → write error）。本质是在测 verifier 行为而非 middleware 集成。生产 refresh 路径确实也是 `VerifyIntent` 直调，无实际安全风险；但若未来 middleware 有 early-return/short-circuit 改动，该测试无法检测 regression。**修法**：用 `httptest.NewServer` + 真实 `AuthMiddleware` + `makeTokenWithAud` 覆盖 refresh-path wrong-audience 场景，使测试打到完整链路。来源: PR#293 round-2 /fix 分析 | 1h | `runtime/auth/middleware_aud_test.go` | PR#293 round-2 |
| ~~L10~~ | ~~**INTERNAL-ROUTE-POLICY-ALIGN-01**~~ ✅ PR#218 合入（`cells/accesscore/cell.go` internal 路由 Policy 改为 `AnyRole(auth.RoleInternalAdmin)`，defense-in-depth 到位） | — | — | — |
| L11 | **GOVERNANCE-CI-MAINBRANCH-01** (Cx1, 🟡 可延后): governance strict gate CI workflow 仅挂 develop 分支，main/release 路径可绕过同等级治理门禁。**修复**：`.github/workflows/governance.yml` 扩展 `branches` 至 `[develop, main, 'release/**']`。 | 0.5h | `.github/workflows/governance.yml` | #210-review 2026-04-21 审查 |
| L7 | **FMT15-NEXTCURSOR-ENFORCE-01** (Cx1, 🟡 可延后): 列表响应治理规则（FMT-14）只强制 `hasMore`，未强制 `nextCursor`，导致接口形态漂移（部分 slice 返回 `nextCursor` 为空字符串而非 omitempty）。**修复**：`kernel/governance/rules_fmt.go` 新增 FMT-15：响应含 `hasMore` 时必须同时含 `nextCursor`（可为空但字段必须存在）；补对应 validate 测试。 | 2h | `kernel/governance/rules_fmt.go` + 对应 validate 测试 | 2026-04-20 分层审查 |
| L8 | **PAGINATION-HELPER-EXTRACT-01** (Cx2, 🟡 可延后): `ParsePageRequest` + `slog.Warn` + `WriteDomainError` 三行分页错误处理模式在 auditquery、configwrite、flagwrite 等多处 slice handler 重复，观测语义分散。**修复**：`pkg/httputil/pagination.go` 提取公共 helper；各 slice handler 统一引用，消除重复。 | 2h | `pkg/httputil/pagination.go`（新）+ 各 `cells/*/slices/*/handler.go` | 2026-04-20 分层审查 |
| L9 | **EXAMPLES-CONTEXT-NOOP-01** (Cx1, 🟡 可延后): `examples/` 下自定义 `noopTxRunner` 吞掉上游 context，破坏取消传播语义；且与 `persistence.NoopTxRunner` 形成分叉。**修复**：删除 examples 层自定义实现，改用 `persistence.NoopTxRunner`（或等价基础实现），恢复 context 传播。 | 1h | `examples/*/` 相关文件 | 2026-04-20 分层审查 |
| FEAT-1-残余 | **RBACCHECK-PAGINATION-REAL-01** (Cx2): PR#221 devicelist 主体完成，但 `cells/accesscore/slices/rbaccheck/handler.go:79` `handleListRoles` 仍硬编码 `hasMore:false`（`map[string]any` 响应，无 `nextCursor`）。**修复**：迁 `query.PageResult[RoleResponse]` + 真实游标分页（改 service+repository 层）；补 `rbaccheck/contract_test.go` 的 `MustRejectResponse` 负向断言。 | 3h | `cells/accesscore/slices/rbaccheck/handler.go` + `rbaccheck/contract_test.go` | PR#214 review + PR#221 搭车项 |
| T6 | **GOCELL-PER-CELL-ADAPTER-01** (Cx2, **PR-X-PG-REPO-ACCESS 强制前置**): 全局 env 读取拆为单 cell 级 adapter 配置，支持 cell-per-adapter；当前 `cmd/corebundle/bundle.go` env 解析共享，无法支持多 cell 不同 PG/Redis 实例。 | 2h | `cmd/corebundle/` + cell 配置 | `docs/plans/20260418-pr-c2-and-phase-x-detailed.md` T6（未登记） |
| ~~V-A15~~ | ~~**CELL-GO-SPLIT-01**~~ ✅ accesscore: PR#234（PR-A5a）; configcore: PR#238（PR-A5b）。auditcore 272 行暂不拆分，留作未来机会性清理。 |
| ~~V-A16~~ | ~~**RUN-IN-TX-HELPER-01** (Cx2): `kernel/persistence.TxRunner` 加 helper~~ **✅ 完结**：`kernel/persistence.RunnerOrNoop` 由 PR #224 落地；accesscore slice 层残留 `runInTx` 死层由 PR #234 清理。auditverify / ordercreate 若有残留由各自 cell PR 独立清理。 | 2h | `kernel/persistence/` | 202604221954-verification §A16（P2-8） |
| ~~V-A17~~ | ~~**FETCH-ROLE-NAMES-DEDUP-01**~~ ✅ PR-A7 升级版落地：抽 `cells/accesscore/internal/sessionmint.Mint`（非 rolefetch 单函数）——覆盖 3 调用点（Login / IssueForUser / rotateAndIssue），删 6 个重复私有 wrapper（`fetchRoleNames` × 2 + `issueAccessToken` × 2 + `issueRefreshToken` × 2）；源码实测 sessionlogin:120 与 sessionrefresh:163 原先同样 fail-open，统一改为 fail-closed（新 `errcode.ErrAuthRoleFetchFailed`，HTTP 500），消除 silent empty-roles 签名风险。 | — | — | — |
| PR220-e2 | **GENERATED-BOUNDARY-STRATEGY-01** (Cx2): `assemblies/corebundle/generated/boundary.yaml` 缺失；`gocell validate --strict` 只给 `REF-16` warning。**决策**：若 boundary 作为生成事实源 → 进入 regenerate-and-diff 门禁；若不维护 → 降低活动验证地位。 | 1h | `kernel/governance/` + `.github/workflows/` | PR#220 遗留报告 额外项 |
| PR220-e3 | **STATUS-BOARD-J-ORDERCREATE-01** (Cx1): `journeys/J-ordercreate.yaml` 存在但 `journeys/status-board.yaml` 无对应条目（ADV-01 warning）；且 J-ordercreate 有一个 auto criterion 缺 `checkRef`，verify 会失败。**修复**：补 status-board 条目 + checkRef；或标明非活动 / 移出活动目录。 | 0.5h | `journeys/status-board.yaml` + `journeys/J-ordercreate.yaml` | PR#220 遗留报告 额外项 |
| PR238-FU1 | **CONFIGCORE-DECRYPT-CATEGORY-AUTH-EVAL-01** (P2, Cx2, 🟡 可延后): `cells/configcore/internal/adapters/postgres/config_repo.go:162,196` `decryptValue` / `decryptVersionValue` 现统一 `Category: CategoryInfra`（PR#238）。对标 k8s apimachinery 将 AES-GCM auth failure 视为 `CategoryAuth` 的传统，当前方案让 crypto tamper-evidence 与 DB 故障混入同一 infra bucket，告警时无法区分。**修复**：增加 `errcode.WrapAuth(ErrConfigDecryptFailed, ...)` 路径或用结构体字面量设 `Category: CategoryAuth`；HTTP 状态仍为 500（由 codeToStatus 决定）。**为什么可延后**：当前无 EventBus 消费 config entries；影响仅限告警精度与未来 consumer routing。 | 1h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 reviewer SEC-2 |
| PR238-FU3 | **CONFIGREPO-CTX-CANCEL-INTEG-TEST-01** (P2, Cx1, 🟡 可延后): PR#238 单测覆盖 `ctxCanceledError` 所有 4 个 call site（GetByKey / Update-SELECT / Update-RETURNING / Delete / GetVersion），但 `config_repo_integration_test.go` 对真实 PG 未验证 ctx cancel → `IsInfraError(true) + !IsDomainNotFound` 端到端路径。**修复**：新增 `TestConfigRepo_CancelledCtx_AgainstRealPG`：pre-cancel ctx，调 `GetByKey`，断言 `IsInfraError(err) && !IsDomainNotFound(err, ErrConfigRepoNotFound)`。 | 1h | `cells/configcore/internal/adapters/postgres/config_repo_integration_test.go` | PR#238 L4 reviewer T-05 |
| PR238-FU4 | **CONFIGREPO-LEGACY-NOTFOUND-TEST-DEDUP-01** (P3, Cx1, 🟡 可延后): `config_repo_test.go` 中 `TestConfigRepository_GetByKey_NotFound`（line ~172）和 `TestConfigRepository_GetVersion_NotFound` 用 `assert.AnError` 测试的是 other-error 分支，与 `TestGetByKey_OtherScanError_ReturnsErrConfigRepoQuery` / `TestGetVersion_OtherScanError_ReturnsErrConfigRepoQuery` 重复。保留会造成 mutation-test 误导。**修复**：删除两个 legacy 命名函数或把它们重构为 table 行。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo_test.go` | PR#238 L4 reviewer T-04 |
| PR238-FU5 | **CELL-SPLIT-LAYOUT-NORMALIZE-01** (P3, Cx2, 🟡 可延后): accesscore + configcore 已按 `cell.go + cell_init.go + cell_routes.go` 三文件范式拆分（PR#234 / PR#238）；但存在小不对齐：(a) `configDirectPublishMode` / `ensureCursorCodec` 放在 cell_init.go 命名偏重 init 但实际是 pure helper；(b) `RegisterSubscriptions` 放在 `cell_routes.go` 与文件名 "routes" 暗示不一致，两 cell 均如此。**修复**：auditcore（272 行，现在不拆）触发 cell 拆分时统一规范（引入 `cell_lifecycle.go` 或 `cell_external.go` 命名惯例），并反向迁移 accesscore/configcore。 | 2-3h | `cells/{accesscore,configcore,auditcore}/` | PR#238 L4 reviewer D2/D3/D6 |
| PR238-FU6 | **CONFIGREPO-CTX-CANCEL-OP-GRANULARITY-01** (P3, Cx1, 🟡 可延后): `ctxCanceledError` 的 `op` 字段目前仅区分 `GetByKey` / `Update` / `UpdateForRollback` / `Delete` / `GetVersion`，但 `Update` 包含 SELECT-FOR-UPDATE 和 UPDATE-RETURNING 两阶段，ctx-cancel 发生在哪一阶段对运维定位锁竞争问题有意义。**修复**：细化 op 值为 `Update/select-for-update` vs `Update/returning`。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer OPS-L4-02 |
| STALECIPHER-OPFIELD | **STALE-CIPHER-LOG-OP-FIELD-01** (P3, Cx1, 🟡 可延后): `observeStaleCipher`（GetByKey/Update 路径）和 `applySensitiveListSentinel`（List 路径）的 stale-cipher Warn 日志字段集合不对称，后者缺 `key` 且两者都缺 `op` 字段，运维无法区分 read 和 list 路径的 stale cipher。**修复**：两处补 `slog.String("op", ...)`。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer OPS-L4-09 |
| FLAGREPO-CTXCANCEL | **FLAG-REPO-CTX-CANCEL-CLASSIFY-01** (P2, Cx1, 🟡 可延后): `cells/configcore/internal/adapters/postgres/flag_repo.go::scanFlagOrMapError` 与 `config_repo.go::scanConfigOrMapError` 是同包平行实现，后者已通过 PR#238 加入 ctx-cancel 优先分类，前者未同步。**修复**：为 `FlagRepository` 补 `ctxCanceledError` 方法 + scanFlagOrMapError 加 ctx cancel 短路；或把 ctxCanceledError 抽到 package 级自由函数复用。 | 1h | `cells/configcore/internal/adapters/postgres/flag_repo.go` | PR#238 L4 Round-2 reviewer F2 |
| PR238-FU7 | **CONFIGREPO-CTXCANCEL-HARDENING-POLISH-01** (P3, Cx1, 🟡 speculative 延后): 三条 speculative 健壮性/可观测细节合并登记：(a) `ctxCanceledError` / `scanConfigOrMapError` 方法化后 `r.logger` 依赖构造函数 fallback，若未来有人引入 `&ConfigRepository{db: x}` 零值构造会 panic，建议加 `if r.logger == nil { r.logger = slog.Default() }` 显式防御；(b) `slog.WarnContext(ctx, ...)` 传入已 canceled ctx，stdlib handler 安全，但 OTel log exporter 可能在 canceled ctx 下提取 trace_id/span_id 失败；建议调用前用 `appendCorrelationAttrs(ctx, nil)` 提取 IDs 后改用 `context.Background()`；(c) `slog.String("cause", err.Error())` 当前 cardinality=2（context.Canceled / DeadlineExceeded 固定字符串），未来若 pgx 用 `%w` 包装 ctx 错误，err.Error() 会变冗长，可预先改为枚举值 `"canceled"` / `"deadline_exceeded"` 固化 cardinality。**触发**：接入 OTel log exporter / 首次出现 `&ConfigRepository{}` 零值构造 / pgx 升级到 ctx 包装版本。 | 1.5h | `cells/configcore/internal/adapters/postgres/config_repo.go` | PR#238 L4 Round-2 reviewer SEC-R2-3 + OPS-L4-01 + OPS-L4-07 |
| PR238-FU8 | **CONFIGREPO-UPDATE-ROLLBACK-OP-LABEL-TEST-01** (P3, Cx1, 🟡 可延后): `doUpdate` 通过 `op` 参数向 `scanConfigOrMapError` 传 `"Update"` 或 `"UpdateForRollback"`，`InternalMessage` 会携带该 op。但 `TestConfigRepository_UpdateForRollback_NotFound` / `TestConfigRepository_UpdateForRollback` 均未断言 InternalMessage 含 "UpdateForRollback"，若有人把 op 硬编码回 "Update"，CI 不会 FAIL。**修复**：`UpdateForRollback` 相关 NotFound 测试追加 `assert.Contains(t, ec.InternalMessage, "UpdateForRollback")`。 | 0.5h | `cells/configcore/internal/adapters/postgres/config_repo_test.go` | PR#238 L4 Round-2 reviewer T-R4 |
| CTXCANCEL-HELPER-PKGLEVEL | **CTXCANCEL-ERROR-PKG-LEVEL-EXTRACT-EVAL-01** (P3, Cx3, 🟡 架构评估): `ctxCanceledError` 目前仅存在于 `configcore/internal/adapters/postgres`，accesscore (pre-existing)、auditcore、flag_repo 均无对等实现。`FLAG-REPO-CTX-CANCEL-CLASSIFY-01` 若选择"抽 package 级自由函数复用"路径，可进一步评估是否提到 `pkg/errcode/ctxcancel.go` 或 `adapters/adapterutil/` 供全仓 PG repo 共享。**决策点**：在 FLAGREPO-CTXCANCEL 落地时一并决定抽取层级；现在登记避免漂移。 | 2h | `pkg/errcode/` 或 `adapters/adapterutil/`（新） | PR#238 L4 Round-2 reviewer F-R2-2 |

### 发布 + 文档

| # | 任务 | 工时 | 文件 | 来源 |
|---|------|------|------|------|
| F2 | **SYSTEM-TOPOLOGY-API** (🟡 可延后): `GET /internal/v1/system/topology` 返回 cell/slice/contract 拓扑 JSON；基于 `kernel/registry` | 4h | 新 slice 或 `runtime/bootstrap/` | 历史 Batch 8 |
| F3 | **P2-T-02 audit e2e 测试**: Journey 级验收 | 2h | `journeys/` + integration test | 历史 Batch 8 |
| F4 | Review cells/ | 4h | — | Wave 4 |
| F5 | Review examples/ | 2h | — | Wave 4 |
| F6 | Review 报告汇总 | 2h | — | Wave 4 |
| F7 | 发布文档 | 4h | — | Wave 4 |
| F8 | 性能基准 | 4h | — | Wave 4 |
| F9 | **v1.0 tag** | — | — | Wave 4 |
| F10 | **TEST-JOURNEY-ASSEMBLY-HARNESS-01** (Cx3, 🟡 可延后): `tests/integration/journey_test.go` 全部 28 条均 `t.Skip("stub: requires full assembly")`；需要建 full-assembly harness 后统一恢复（J-sessionrefresh / J-sessionlogout / J-accountlockout / J-auditlogintrail / J-config-* / J-ssologin / J-useronboarding），不只 refresh 一条 | 8h | `tests/integration/` + assembly fixture | PR#166 R2-P2（范围扩大） |

---

## P3 长期 / 大型独立

| # | 任务 | 工时 | 前置 | 来源 |
|---|------|------|------|------|
| X1 | **PG-DOMAIN-REPO**: 5 个域 Repository PostgreSQL 实现（User/Session/Role/Device/Command）。前置: ① 4 migration DDL + **CONFIG-VERSIONS-MIGRATION-01** (`004_create_config_entries_and_versions.sql`)；② `ports.RoleRepository.CreateRole`。**落地联动**（同 PR 或紧邻 PR）: **RBAC-ASSIGN-LEVEL-UPGRADE-01** L0→L1；**SEED-ROLE-IFACE-01** 去 type assertion；**ACCESS-LEVEL-AUDIT-01** slice.yaml 校正；**AUTH-CACHE-01 激活** Redis session cache + 撤销失效路径 | 3-5d | — | PR#155 review F4 + 多 PR 汇总 |
| X2 | **WM-35 BFF handler 接入 cookie session** ★ | 2d | WM-2-F1 ✅ | 长期 roadmap |
| X3 | **WM-36 SecureCookie key rotation** ★ | 1.5d | WM-35 | 长期 roadmap |
| X4 | **WM-7 泛型 BulkResult** | 1d | — | 历史 Batch 8 |
| X5 | **P3-TD-11 accesscore domain 拆分** User/Session/Role | 4h | X1 | 历史 Batch 8 |
| X9 | **LINT-MODERN-01** (Cx2, P3): 全仓库 modernization baseline 清理（rangeint / stringsseq / forvar / inline / testingcontext / any / nhooyr.io→coder/websocket）。独立 PR，不混入功能 | 6h | — | PR#163 post-review |
| X10 | **AUTH-REFRESH-OPAQUE-01** (Cx3, 🟠 条件延后，X1 PG-DOMAIN-REPO 上线后触发): refresh token 由 JWT 改为 opaque string + server-side rotation store（RFC 6819 §5.2.2.2）；减小 JWT 承载、允许即时撤销 | 1-2d | `runtime/auth/` + `adapters/postgres/` 新 refresh_token_store | PR#166 R1-F2-7 |
| X11 | **REFRESH-HMAC-SPLIT-01** (Cx3, 🟠 条件延后，**X15 之前必须完成**): HMAC-split token 格式 (selector\|verifier)，DB 存 selector 明文 + SHA-256(verifier)。需 migration ALTER + Store 接口变更。防 DB 泄漏后 token 被直接使用。**必须在 X15 上线前完成**——明文 token 入库后改格式需数据迁移 | 4h | `runtime/auth/refresh/` + `adapters/postgres/refresh_store.go` + migration | 开源对标 Hydra 双层防护 |
| X12 | **REFRESH-IDLE-EXPIRE-01** (Cx2, 🟠 条件延后，X15 集成后): `idle_expires_at` 滑动窗口列 + Policy.MaxIdle。长时间未使用的 token 提前过期 | 3h | `runtime/auth/refresh/types.go` + `adapters/postgres/` + migration | 开源对标 Zitadel 双过期列 |
| X13 | **REFRESH-PARTITION-01** (Cx2, 🟠 条件延后，生产流量达阈值后): `expires_at` range 分区，用 DROP PARTITION 替代批量 DELETE 进行 GC | 3h | migration + DBA ops runbook | 通用 PG 高吞吐模式 |
| X14 | **REFRESH-GRACE-COUNTER-01** (Cx2, 🟠 条件延后，X15 集成后): `first_used_at` + `used_times` 列，grace 窗口内限制重用次数上限 | 2h | `adapters/postgres/refresh_store.go` + migration | 开源对标 Hydra COALESCE 模式 |
| X15 | **REFRESH-OPAQUE-INTEGRATION-01** (Cx3, 🟠 条件延后，**X11 完成后**): sessionrefresh/sessionlogin 切换 opaque token + 接线 access_module.go。替换当前 JWT refresh token 为 opaque + refresh.Store。前置: X11 HMAC-split | 6h | `cells/accesscore/slices/sessionrefresh/` + `cmd/corebundle/access_module.go` | F2 plan 后续集成 |

---

## 设计决策记录（历史 — 不修，避免重复审查）

| PR | Finding | 结论 | 理由 |
|---|---------|------|------|
| #137 | assembly 层不校验零值 + 非法枚举旁路 | ✅ 已修 | assembly.startInternal 加 ValidateMode 入口闸门 + CheckNotNoop 改为 allowlist |
| #137 | corebundle DurabilityDurable + in-memory | 不修 | 语义正确：Durable 拒绝 Nooper 标记类型，nil 和 `eventbus.New()` 合法通过；effectiveMode + adapterInfo + slog 日志已透明标注 |
| #137 | durability_test 非 table-driven | 不修 | 8 个测试断言模式差异大，table-driven 增加复杂度不增加覆盖率 |
| #137 | SonarCloud 5.6% duplication | 不修 | cell-per-package 固有结构相似，5 cell 的 CheckNotNoop 参数列表各不相同，不可提取 |
| #140 | Request/Trace ID 生成不做 `rand.Read` 错误分支 | ✅ 已修 | Go 1.24+ `crypto/rand.Read` always-succeed-or-fatal，`_, _ =` 是死代码 |
| #140 | JSON unknown field 用字符串匹配 + guard test | ✅ 已修 | Go 标准库 `encoding/json` 至 1.25 仍用文本错误，无 typed error |
| #143 | Seed admin password 通过环境变量传入 | 不修 | dev 模式标准做法（Casdoor/Zitadel 同模式）；生产改用 secrets manager 在 PG-DOMAIN-REPO 时处理 |
| #143 | Slice 目录命名 kebab vs no-dash | 不修 | CLAUDE.md "Cell 开发规则"约定 |
| #143 | `TestContext` 从非 `_test.go` 文件导出 | 不修 | 跨包测试需要，`_test.go` 函数 package-scoped 无法跨包调用 |
| #143 | POST /assign 返回 200 而非 201 | 不修 | 幂等操作返回 200（Casbin 模式），201 暗示每次创建新资源 |
| #146 | computeBoundaryContracts 遍历全量 contracts | 不修 | generator 必须遍历全量才能发现 imported contracts；FMT-09 validate 阶段已保证合法，generator fail-fast 是正确的 defense-in-depth |
| #155 | `slices/*/slice.yaml` allowedFiles 双路径（kebab + no-dash） | 不修 | 全项目统一惯例，FMT-14 治理规则守护，`gocell scaffold slice` 模板默认产出双路径 |
| #159 | 7 个 Option type 分散 | 不修 | 每个 Option 针对不同构造函数，合并会丧失类型安全性；对标 Kratos jwt.Option 每个中间件独立 |
| #159 | GenerateServiceToken `crypto/rand` 失败静默返回 "" | 不修 | Go 1.24+ `crypto/rand.Read` always-succeed-or-fatal，空串触发下游 401 是 fail-closed |
| #159 | ServiceTokenMiddleware nil ring 每请求 500 | 不修 | 返回 error 违背 Go 标准 middleware 签名惯例（net/http / go-chi / Kratos 均不返回 error）；构造时 panic 不符合编码规范 |
| #159 | 缺 service token duration metric | 不修 | HMAC-SHA256 <0.1ms，histogram 分桶无意义信息；等 Redis nonce store 引入网络 I/O 后再加 |
| #210-review | internal service principal `role:internal-admin` 与路由策略 `AnyRole("admin")` 不一致 | 假阳性 | `/internal/v1/*` 路由通过 `WithInternalPathPrefixGuard` 设置 `authDelegatedMatcher`，AuthMiddleware 识别 `isDelegated=true` 后跳过整个 JWT + Policy 链。`AnyRole("admin")` 对 internal 路由从未执行，service token guard 是唯一鉴权层。**但 Policy 声明应修正为 `AnyRole(auth.RoleInternalAdmin)` 作为 defense-in-depth**，见 L10 |
| #210-review | BuildApp option 冲突检测缺失 | 🟡 延后 | 当前 3 个 CellModule，option 直接 append；引入第 4 个 module 前补 key 冲突检测（至少 warning） |

---

## 触发条件项（仅在条件满足时做）

> 已触发并完成项：T8 PUBLIC-ENDPOINT-STRUCT-MIGRATE-01 ✅ PR#201（已在 PR#201 触发并完成）

| # | 任务 | 工时 | 触发条件 |
|---|------|------|----------|
| T1 | **AUTH-PROVIDER-EXPORT-01** `authProvider` 接口 unexported，需移动出 `runtime/bootstrap` | 1h | 第二个 auth provider cell |
| T2 | **AUTH-ISSUE-OPTIONS-01** `JWTIssuer.Issue()` 重构为 `IssueOptions` struct | 1h | Issue() 第 5 个参数 |
| T3 | **DEVICE-ENQUEUE-RBAC** HandleEnqueue 无设备维度鉴权 | 2h | 多租户 operator |
| T4 | **CB-RESILIENCE-PACKAGE-01** 把 `Allower` / `CircuitBreakerRetryAfter` 从 `runtime/http/middleware` 迁移到 `runtime/resilience/circuitbreaker/` 独立包 | 4h | 出现第二个非 HTTP 的 CB 消费方 |
| T5 | **AUTH-SIGNER-01** `SigningKeyProvider` 返回 `crypto.Signer` 替代 `*rsa.PrivateKey` | 2h | golang-jwt v6 发布 |

---

## v1.1+ 长期规划

> **详细内容请阅读: [backlog_later_detail.md](./backlog_later_detail.md)**
>
> metadata 校验规则 (G-1~G-6) / Kernel 子模块 (wrapper/command/webhook/reconcile/scheduler/replay/rollback)
> adapters 分层重整 (AL-03~AL-04, RMQ-STATUS-01) / 架构风险 (Cell 接口拆分, adapter t.Skip, ER-ARCH-01)
> 契约增强: CONTRACT-BREAKING-01 / CONTRACT-CODEGEN-01 / CONTRACT-STUB-01
> spec tech-debt (C-AC7 jti / C-L6 contract ID / C-DC9 auditarchive stub / DURABLE-TYPE-01) — ~~CONTRACT-META-01~~ ✅ PR#239 合入
> winmdm defer (WM-18/32/4/5/22/23/16) / winmdm reject (WM-3/14/21/24/25/26/30/31/34b)
> v2+ (WM-28/29, GAP-1/2/5/6/8/11/12/13/14)

---

## 工时汇总

| 分类 | 工时 |
|------|------|
| P0 阻塞 | ~~L0 ✅~~ ~~L1 ✅~~；**全清** |
| P1 | ~46h（前值 32h + PR252-F1/F2 command follow-ups ~14h；PR244-F1 2.5h 多 pod nonce warn 待决）|
| P2 kernel/runtime | ~19h |
| P2 adapter | ~9h |
| P2 slice/cell | ~34h |
| 安全加固（可延后） | +3h S-5xx-code-mask + 4h PR244-F8/F9 + PR247-N3/N4/N5/N7 ~2.5h |
| P2 PR#245/#249 follow-ups | +5h（PR245-F5/F6/F10 共 4h + PR-A12-SWEEPER-WIRE 2h）|
| P2 发布 + 文档 | ~25h + v1.0 tag |
| **核心路径合计（不含 P3）** | **~128.5h（约 16 工作日）** |
| P3 长期 | 3-5d + 若干独立项 |
| 备注 | verification 3 条 housekeeping（A12/A13/A18）未入 backlog，建议一次性 PR 清理 |
