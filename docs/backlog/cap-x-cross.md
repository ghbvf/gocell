# GoCell Backlog — cap-x-cross: 横切

> 跨多个 capability、无明确 primary owner 的条目（跨 ≥ 4 cap 才进本表，次要 capability 在描述里写 `(also: cap-XX)`）
> 父表：[`docs/backlog.md`](../backlog.md)（schema / cap 主轴）
> 归档：[`docs/backlog/archive/`](archive/)

---

## x.1 adapter / 外部系统

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| ADAPTER-ERROR-CLASSIFICATION-TRANSIENT-01 | **ADAPTER-ERROR-CLASSIFICATION-TRANSIENT** — 现状: postgres/redis/s3 错误码不分 transient/permanent，consumer 无法做退避决策；修复: 复用 `errcode.WrapInfra` + `errcode.IsTransient`，PG `40001/40P01/08*`、Redis `i/o timeout`、S3 5xx/429 标 transient，其余永久 | arch-opt | P1/Cx3 | 🟠 | 第 1 个 handler disposition 收口 | `adapters/{postgres,redis,s3}/errors.go` + `pkg/errcode/` | systems layer review + 030 §2 A-03 |
| ADAPTER-FAKE-EXPORT-01 | **Adapter fake export 一致性** — 现状: adapter fake 仅 `_test.go` white-box，cells 测试只能自写 fake 或 import adapter（破 LAYER-04）；修复: 每个对外接口 adapter 开 `adapters/<name>/<name>fake/` 子包导出 `NewFakeClient/NewMemKeyProvider`（参考 `runtime/eventbus` in-mem 范式）| arch-opt | P1/Cx3 | 🟠 | cell mock 扩展 | `adapters/*/fake/` (新子包) | systems layer review + 030 §2 A-04 |
| DEVOPS-INTEGRATION-CLEANUP-WAIT-TIMEOUT-01 | **Devops integration cleanup wait timeout** — 现状: e2e cleanup 超时；修复: 加 wait helper | arch-opt | Cx1 | 🟡 | — | `tests/e2e/` | GitHub #19 |
| ADAPTER-CONNECT-BUDGET-01 | **adapter 级 ConnectTimeout 强制** — 现状: 各 adapter 依赖上层 ctx；修复: adapter 级 ConnectTimeout（默认 5s）写 Config + Validate + `ERR_ADAPTER_CONNECT_TIMEOUT` (also: cap-08, cap-10；PG 部分由 PR#401 已部分覆盖) | bug | P1/Cx2 | 🟡 | v1.0 GA 前 | `adapters/rabbitmq/connection.go` + `adapters/postgres/pool.go` | backlog1 §2.4 |
| S3-FAILURE-INJECTION-01 | **S3 故障注入测试** — 现状: 缺 MinIO testcontainer 集成测；修复: 上传 403/5xx/timeout/recovery 路径覆盖 (also: cap-13) | test | P1/Cx2 | 🟡 | v1.0 GA 前 | `adapters/s3/s3_test.go` | backlog1 §2.5 |

## x.2 PR-specific 跨域 FU

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| PR-BATCH2-RETRO-FU | **Batch2 retrospective 收口（2026-05-12 代码实测）** — 现状: 原 retrospective 13 条 finding (5 P1 + 8 P2) 2026-05-12 代码核实后，**5 条 ✅ 实测已修**：BR-P1-01 `cfg.metrics.recordServiceVerify` 已有 `if m == nil { return }` nil guard / BR-P1-02 `ChangePassword` `GetByID+Compare+Generate` 全在 `changePasswordInTx` tx 闭包内 + S6 password_version CAS / BR-P1-03 archtest scanner walker `skipDirCheck` 覆盖 worktrees / BR-P1-04 `TestListenerDXA52Guard` 已 `listenerDXDocExcluded` + scanner.ModuleScope 过滤 bak/docs / BR-P2-04 backlog 链接 grep 0 命中 (已修)；BR-P2-05 工时虚高（PR-CFG-4/V-A6/V-A7）grep 0 命中（已修）。**真未修 5 条 + 待核 2 条**：<ul><li>**BR-P1-05** `runtime/bootstrap/doc.go:10` package-level 示例 `[]cell.ListenerAuth{cell.AuthNone{}}` 与 PrimaryListener 应用 JWT 的安全规范矛盾，误导消费者；改为 `cell.MustNewAuthJWT(...)` 或显式注释「示例 only，生产必须 JWT」(Cx1)</li><li>**BR-P2-01** `tests/e2e/scripts/bootstrap-admin.sh` `BASE_URL` / `E2E_ADMIN_USERNAME` / `E2E_ADMIN_EMAIL` / `E2E_ADMIN_PASSWORD` 4 处 `${VAR:-default}` 软回退，**违反 memory `feedback_no_soft_fallback`**（CI 内部基建禁 default）；改 `set -u` 强制必需，CI workflow step 显式 export (Cx1)</li><li>**BR-P2-03** `tools/archtest/storage_backend_test.go` line 36 仍 import `go/parser` + line 179 `buildLocalVarValues` 手写 resolver，未复用 `tools/archtest/internal/typeseval` 共享 helper（与 PR408-FU-SCANNER-FRAMEWORK Batch 2 同主题，迁移留尾）(Cx2)</li><li>**BR-P2-06** `journeys/status-board.yaml:41` 引用 `journeyId: J-ordercreate` 但 `journeys/J-ordercreate.yaml` 不存在；删 entry 或补 yaml（与 cap-14 JOURNEY-STATUS-BOARD-LIFECYCLE-CONSISTENCY-01 同根）(Cx1)</li><li>**BR-P2-08** `runtime/auth/servicetoken.go:402-430` `classifyServiceTokenVerifyError` 用 `strings.Contains(msg, ...)` 决定 metric reason label；godoc 标"by design"（bracket-prefixed code 措辞），但 error message 改动会静默断告警链；改用 `errors.As(err, &ec); switch ec.Code` typed dispatch（**by-design vs 改 design 争议**）(Cx1)</li><li>**BR-P2-02 待核** — `_build-lint.yml:404` integration-test job 用 `comm -13 bare tagged` 动态发现 `//go:build integration` 文件得 pkgs，覆盖范围依赖 build tag 分布；retrospective 声称缺 `runtime/auth/` + `cells/accesscore/slices/sessionlogin/` scope，需 spot check 实际 pkgs 列表</li><li>**BR-P2-07 待核** — `cells/configcore/slices/configwrite/handler.go` 已切 generated codegen typed `req.Key`（不再 `r.PathValue("key")`），但 `contracts/http/config/write/v1/contract.yaml` 是否在 `pathParams.key` 加 pattern 约束未核（identitymanage UUID 路径强校验通过 contract `format: uuid` + `httputil.ParseUUIDPathParam` 链路，config key 是 dotted-path 不是 UUID）</li></ul> | fu | Cx1-Cx2 | 🟠 | — | `runtime/bootstrap/doc.go` + `tests/e2e/scripts/bootstrap-admin.sh` + `tools/archtest/storage_backend_test.go` + `journeys/status-board.yaml` + `runtime/auth/servicetoken.go` | `docs/reviews/202605011700-batch2-end-retrospective-six-seat.md` BR-P1-01..05 + BR-P2-01..08 + 2026-05-12 代码实测 |
| PR-A41-FU1 | **PR-A41 advisory rules follow-up** — 现状: governance advisory 规则余项；修复: 跟进 | arch-opt | Cx2 | 🟡 | — | `kernel/governance/` | PR-A41 |
| PR237-F06 | **DUAL-LISTENER-DEVMODE-WARN-TEST-01** — 现状: PR-A14a 留尾，`cmd/corebundle/shared_deps.go::internalGuardFromEnv` 在 dev 模式（`GOCELL_ADAPTER_MODE=""`、未设 `GOCELL_SERVICE_SECRET`）返回 nil guard 并 `slog.Warn`，无测试断言 Warn 触发；修复: 加 `internal_guard_env_test.go` table-driven 覆盖 dev/real × 有/无 SERVICE_SECRET 四象限 | test | Cx1 | 🟡 | — | `cmd/corebundle/internal_guard_env_test.go` (新) | PR#237 reviewer F-06 |
| PR237-DX1 | **LISTENER-OPTION-NAMING-UNIFY-01** — 现状: PR-A14a 历史 listener 选项在 HTTP 地址配置与 listener 配置之间前缀不对称，IDE 补全时相关选项不相邻；修复: 统一到当前 canonical listener/address option naming，并由 LISTENER-DX-A52 archtest 防回归；旧符号 provenance 仅保留在归档 backlog / review / plan 文档 | refactor | Cx2 | 🟡 | — | `runtime/bootstrap/bootstrap.go` + 测试 + `cmd/corebundle` + `examples/*/main.go` | PR#237 第二轮 reviewer DX F3 |
| PR237-A4 | **Listener architecture** — 现状: 双 listener 架构 doc 缺；修复: 写架构说明 | arch-opt | Cx2 | 🟡 | — | `runtime/http/` | PR#237 |
| PR238-FU4 | **CONFIGREPO-LEGACY-NOTFOUND-TEST-DEDUP-01** — 现状: `config_repo_test.go` 中 `TestConfigRepository_GetByKey_NotFound` (line ~172) 与 `TestConfigRepository_GetVersion_NotFound` 用 `assert.AnError` 测的是 other-error 分支，与 `TestGetByKey_OtherScanError_ReturnsErrConfigRepoQuery` / `TestGetVersion_OtherScanError_ReturnsErrConfigRepoQuery` 重复，造成 mutation-test 误导；修复: 删除两个 legacy 命名函数或重构为 table 行 | test | P3/Cx1 | 🟡 | — | `cells/configcore/internal/adapters/postgres/config_repo_test.go` | PR#238 L4 reviewer T-04 |
| PR464-FU-CHANGEPASSWORD-BCRYPT-TX-CONCURRENCY-LIMIT-01 | **bcrypt-in-tx 并发限制保护** — 现状: ChangePassword 把 bcrypt 置于 TX 内（B-class 决策）；高并发场景下多个 goroutine 并发 bcrypt 100ms/次可耗尽 PG pool；修复: cell-level / slice-level 并发限制（如 worker pool / rate-limiter）保护 bcrypt-in-tx 路径。 | arch-opt | P2/Cx2 | 🟠 | 压测中 ChangePassword 出现 PG pool 耗尽 | `cells/accesscore/slices/identitymanage/` + 可能的 rate-limiter | PR #464 review F3 |
| PR464-FU-CAS-PROTOCOL-ARCHTEST-TYPESEVAL-UPGRADE-01 | **CAS-PROTOCOL-COMPOSITION-ROOT-01 archtest typeseval 升 Hard** — 现状: 当前 archtest 用 string anchor 识别 `MustNewProtocol` 调用点（Soft）；修复: 改用 typeseval 按 receiver type 识别（Medium→Hard），对标 session Protocol archtest 形态；需与 audit_ledger / session composition-root archtest 同步升级，避免单点改造不一致。 | arch-opt | P2/Cx2 | 🟠 | session/audit_ledger composition-root archtest 同步升级时 | `tools/archtest/` + 可能 `tools/archtest/internal/typeseval/` | PR #464 review F9 |
| PR464-FU-CAS-PROTOCOL-CONSUMPTION-BINDING-01 | **CAS Protocol 声明与 repo 落地脱耦** — 现状: `cas.Protocol` 仅声明 `VersionField` + `ConflictPolicy`，但 repo 层（PG/mem）直接调 `cas.CheckVersionMatch(0, "config_entry", key)` 硬编码 entity 名，不读 `Protocol.ConflictPolicy`；引入第二种 ConflictPolicy（LastWriteWins / RetryWithMerge）时会出现"看起来可配，实际未生效"长期漂移；修复: 让 repo 在构造期接 `*cas.Protocol` 依赖（`NewConfigRepository(..., proto)`），`CheckVersionMatch` 改为 Protocol method 形态（`p.CheckMatch(rowsAffected, entity, key)`）按 ConflictPolicy 派生不同语义；同时把 entity 名 + version 列名读自 Protocol 字段，消除硬编码 | arch-opt | P2/Cx3 | 🟠 | 第二种 ConflictPolicy 引入或多 entity field 出现 | `runtime/state/cas/protocol.go` + `cells/configcore/internal/adapters/postgres/{config,flag}_repo.go` + `cells/accesscore/internal/adapters/postgres/user_repo.go` + mem 对等实现 | PR #464 round-3 P2.3 review |
| PR467-FU-PANIC-ASSERTION-HELPER-CONSOLIDATE-01 | **panic-with-Assertion 测试辅助跨包重复** — 现状: `runtime/websocket/hub_test.go::assertNewHubPanicsWithErrcodeMessage` 与 `cells/auditcore/internal/appender/spec_test.go::assertSpecPanicsWithErrcodeMessage` 两个 helper 逻辑完全相同（recover → assert `*errcode.Error` → Message/Kind/Category）。修复: 第 3 个同形态 helper 出现 / 跨包测试基础设施收口需求触发时，抽到 `pkg/testutil/errcodetest/` 统一供给；当前 2 处保持现状不增 churn | refactor+test | Cx2 | 🟡 | 第 3 个同形态 helper 出现 / `pkg/testutil/errcodetest` 建包需求 | `pkg/testutil/errcodetest/` (新) + `runtime/websocket/hub_test.go` + `cells/auditcore/internal/appender/spec_test.go` | PR #467 review P2-09 |

## x.3 B-floor findings (B-FLOOR-FOLLOWUP + F-* 系列)

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| B-FLOOR-FOLLOWUP | **TYPED-ENVELOPE-ADAPTER-FLOOR-UPGRADE** — 现状: PR#403 段 1 是 Ceiling 守（`return ⊆ declared`，零 typed return 合法），25+ adapter 全部走 framework fallback，contract.yaml 删 status 不会有 build 信号；修复: 段 2.5 升 Success-Floor（每 adapter 至少一处 typed `XxxNNNJSONResponse{...}`，~16h dev）+ 段 4 升 Full-Floor（每个声明 status 至少一处 typed return，~24h dev）；对标: goa strict response set / connect-go typed error returns | refactor | 段 2.5 Cx3 / 段 4 Cx3 | 🟠 | contract.yaml status 声明 ⇔ adapter typed return 出现首次实际漂移事故，或 cells 数量增长到 Floor 升级 ROI > 16h dev（依赖已删除的 invariant Registry 路径已退役） | `cells/*/slices/*/handler.go` (~20) + archtest + ADR D7 演进锚点 | PR #403 第三轮 review §R1 |
| F-01 | **CODEOWNERS-PR-TEMPLATE** — 缺 `.github/CODEOWNERS` + `pull_request_template.md`，reviewer 路由全靠手动，PR 描述无强制 ref/一致性级别/journey 影响面；修复: 新建 CODEOWNERS（`/kernel/ @owner-kernel` 等）+ pull_request_template.md（4 项 checklist）+ branch protection 配置文件 | doc | P1/Cx1 | 🟡 | — | `.github/CODEOWNERS` (新) + `.github/pull_request_template.md` (新) | 030 §3 F-01 |
| F-02 | **MAKEFILE-LINT-RACE-ARCHTEST** — Makefile 13 target 缺 `lint`/`race`/`archtest` 独立 target，CI 与本地命令漂移，lint exclusions 13 条无周期复盘；修复: `make lint`/`race`/`archtest` + CI yaml 改调 Makefile + `hack/verify-lint-exclusions.sh` 校验时间戳 | feat | P1/Cx2 | 🟡 | — | `Makefile` + CI workflows + `hack/` | 030 §3 F-02 |
| F-04 | **CMD-GOCELL-VS-COREBUNDLE-DOC** — cmd/CLAUDE.md 主题是 corebundle 三层组装，对 cmd/gocell 在 Composition Root 中地位完全没着墨；修复: 文首加对照段：cmd/gocell = 治理/元数据/生成器 CLI（dev+CI）；cmd/corebundle = assemblies/corebundle/ 运行时组装产物 | doc | P2/Cx1 | 🟡 | — | `cmd/CLAUDE.md` | 030 §3 F-04 |
| F-05 | **QODANA-WORKFLOW-AUDIT** — Qodana 与 CodeQL/Semgrep 双重覆盖、增量价值未在 yaml 注释说明；`pr-mode: false` 不阻断 PR；修复: 二选一 (a) 补 yaml 头部注释明确差异化覆盖；(b) retire workflow + 删 `QODANA_TOKEN` secret | doc | P2/Cx1 | 🟡 | — | `.github/workflows/qodana_code_quality.yml` | 030 §3 F-05 |
| F-06 | **REQUIREMENTS-TRACEABILITY-CHAIN** — 无 `docs/requirements/` 目录；ADR/Roadmap/journey goal 三处隐含需求；contract.yaml/journey 无 `requirementID` 反向链；V 模型左侧追溯断点；修复: 引入 `docs/requirements/REQ-*.yaml` (id/text/category/priority/satisfiedBy/verify) + contract.yaml + journey schema 加 `requirementID: []` + archtest `REQ-TRACE-01` 双向校验 + 1-2 ADR | feat | P2/Cx3 | 🟡 | V 模型左侧补全启动 | `docs/requirements/` (新) + `kernel/metadata/` + `tools/archtest/` + ADR | 030 §3 F-06 |

## x.4 tech-debt P3/P4 系列

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| P3-TD-04 | **websocket/oidc/s3 sandbox httptest panic** — 现状: sandbox 限 net.Listen，单测 panic；guard 已加；修复: 评估 CI sandbox 替代方案或维持 guard | test | Cx1 | 🟡 | — | `adapters/{websocket,oidc,s3}/` + CI | tech-debt-registry P3-TD-04 |
| P3-TD-05 | **示例 docker-compose start_period** — 现状: 3 个示例 compose 缺 start_period（rabbitmq healthcheck）+ 用废弃的 `version: "3.9"`；修复: 补 start_period + 删 version 键（合并 P4-TD-07） | arch-opt | Cx1 | 🟡 | v1.1 启动 | `examples/*/docker-compose.yml` | tech-debt-registry P3-TD-05 + P4-TD-07 |
| P4-TD-01 | **noop outbox/Claimer 共享包** — 现状: 各处 ad-hoc noop 实现，KG-02 建议提取；修复: 抽到共享 `runtime/testutil/outbox/` + 测试 helper 收口 | refactor | Cx2 | 🟡 | — | `runtime/testutil/` (扩) + 各 cell 测试 | tech-debt-registry P4-TD-01 |
| P4-TD-06 | **CI example validation `\|\| true` 形式化** — 现状: 验证错误被静默吞咽；修复: 删 `\|\| true` 让 CI 阻断 | bug | Cx1 | 🟡 | v1.1 启动 | `.github/workflows/` | tech-debt-registry P4-TD-06 |

## x.5 kernel/runtime cross-cut + 其它

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| USER-REPO-UPDATE-USE-CASE-SPLIT-01 | **user_repo.Update generic → use-case 专用方法拆分（AI Hard 升级）** — 现状: S6 PR 加 narrow `UpdatePassword(ctx, userID, newHash, resetRequired, expectedPV) (newVersion, error)` 已 ship；但 `UserRepository.Update(ctx, *domain.User)` generic 方法仍保留给 `Lock` / `Unlock` / `UpdateProfile` 三个 caller，caller 传整 User struct，依赖文档约定"该路径不动 password 字段"——AI 视角是 Soft（依赖 caller 知识 + 字段忽略约定）；修复: 把 generic Update 拆为 `UpdateProfile(ctx, userID, email, username, ...) error` + `UpdateLockState(ctx, userID, status, lockedAt) error`，每个方法接收原始字段而非整 struct，type system 让"误写无关字段"不可表达；对标: Auth0 / Ory Kratos 同范式（identity 写按 use case 拆方法）| refactor | P2/Cx3 | 🟡 | S6 PR 合入后 | `cells/accesscore/internal/ports/user_repo.go` + PG / mem 实现 + identitymanage Lock/Unlock/Update service | S6 PR + 激进自审 L2（PR 整体决策组合） |
| CONTRACT-SHARED-MIXIN-FUNNEL-01 | **contracts/shared schema mixin codegen funnel（AI Hard 升级）** — 现状: S6 PR 把 `expectedVersion: integer, minimum:1, required` 字段写入 6 份 `contracts/http/config/{update,delete,rollback,flags/update,flags/toggle,flags/delete}/v1/request.schema.json`（hand-crafted），靠 archtest `CAS-CONTRACT-EXPECTED-VERSION-SCHEMA-01` cross-validate 阻止漂移（Medium）；修复: 新建 `contracts/shared/cas/v1/expected_version.schema.json` 单源 schema mixin，6 个 request.schema.json 改用 `$ref` 引用（codegen funnel #1 Hard）；验证 cmd/contractgen 是否支持 `$ref` 解析，必要时扩 codegen；对标: OpenAPI spec components 单源 + JSON Schema `$ref` | refactor | P2/Cx3 | 🟡 | S6 PR 合入后 + 验证 codegen `$ref` 支持 | `contracts/shared/cas/v1/expected_version.schema.json` (新) + 6 份 `contracts/http/config/.../request.schema.json` + cmd/contractgen | S6 PR + 激进自审（hand-crafted Medium 升 Hard） |
| X4 | **WM-7 泛型 BulkResult** — 现状: 各 cell 各写 BulkResult；修复: 抽泛型 | feat | P3/— | 🟡 | — | `pkg/` | 历史 Batch 8 |
| KERNEL-WEBHOOK-01 | **kernel/webhook 出站请求** — 现状: 缺 Webhook Receiver/Dispatcher 抽象；修复: 新建 webhook 包 + HMAC 认证 + SSRF 黑白名单（依赖 Outbox Relay 稳定）(also: cap-04, cap-08) | feat | P2/Cx3 | 🟡 | Outbox Relay 稳定后 | `kernel/webhook/` (新) | backlog_later §2 + WM-4 |
| RUNTIME-SCHEDULER-01 | **runtime/scheduler Cron 调度** — 现状: PeriodicWorker 仅固定间隔；修复: 新建 scheduler 包 + Cron 表达式 + 分布式防重 (also: cap-11, cap-12) | feat | P2/Cx3 | 🟡 | 业务出现 Cron 需求 | `runtime/scheduler/` (新) | backlog_later §2 |
| KERNEL-ROLLBACK-01 | **kernel/rollback 元数据模型** — 现状: 缺跨事件撤回原语；修复: 新建 rollback 包 + 元数据模型 (also: cap-07, cap-08) | feat | P3/Cx3 | 🟡 | V1.1+ 启动 | `kernel/rollback/` (新) | backlog_later §2 |
| L3-EXAMPLE-PROJECTION-01 | **examples L3 投影 reference** — 现状: 无完全 L3 一致性级别官方 reference cell，业务可能错误塞入 L2；修复: examples/ 补 L3 Projection 样板 (also: cap-08) | doc | P2/Cx2 | 🟡 | v1.1 启动 | `examples/` | backlog_later §4 |
| C-DC9 | **auditarchive 死代码靶子打通** — 现状: handler 返 `ErrNotImplemented`，S3 adapter 已就绪但中间业务层漏接；修复: 打通导出链路 (also: cap-08) | bug | P2/Cx2 | 🟡 | — | `cells/auditcore/slices/auditarchive/` + S3 adapter | backlog_later §6 C-DC9 |
| B2-C-13 | **L2 跨层 e2e 回归不足** — 现状: setup → audit → config 跨 cell e2e 不全；修复: 加跨 cell integration test | test | P2/Cx3 | ✅ | closed by T4 (accesscore scope only) (`tests/integration/l2atomicity/`): 7 e2e 测试覆盖 login → refresh reuse cascade → rbacassign revoke → validate fail-closed + 三态 401 归一 + ChangePassword/epoch 比对，接入 race-pg-integration lane。audit/configcore 跨 cell e2e 仍未覆盖；如有需要新立 `B2-C-13-AUDIT` / `B2-C-13-CONFIG` 触发条件 = S7 / config L2 完成后 | `tests/integration/l2atomicity/` | backlog2 §4 B2-C-13 |
| B2-T-07-FU-4 | **SVCTOKEN 跨信任域限制** — 现状: 跨 trust domain 时 SVCTOKEN 无额外限制；修复: 加 trust domain claim + 验证（A5 follow-up） | arch-opt | Cx4 | 🟠 | 多租户/跨信任域需求 | `contracts/` + `runtime/auth/` | backlog2 §8 A5 follow-up |
| INTERFACE-EXPORT-SURFACE-CLOSURE-BUNDLE | **Interface 出口表面 + 消费者文档闭环束（PR #441/#450 review 聚合，3 子条）** — 现状: 新协议/接口 ship 时"出口表面收口 + 消费者文档闭环"两件事漂移：PR #441 ISP 子接口在 README/guides 0 命中；PR #450 ledger.Store 接口 5 条架构演进 + 14 条文档同步漏洞。修复: 三大束合并排期，体现"interface ship 必带消费者闭环"原则。子条：<ul><li>**PR441-DOC-INTERFACE-ISP-CONSUMER-DOCS-01** (P1/Cx3, 🟠 立即排期独立 doc PR) — README + `docs/guides/cell-development-guide` + `codegen-new-endpoint` 三处主文档对 4 子接口 / sealed marker / `Wrap*ForCell` 0 命中；新增 `docs/guides/why-sealed-marker.md` FAQ + README §"30-Minute Tutorial" 改 4 段式 (Files: `README.md` + `docs/guides/{cell-development-guide,codegen-new-endpoint}.md` + 新 `why-sealed-marker.md`，PR441 review PM-F2 + PM-F7)</li><li>**S7-FU-ARCH-EVO** (P1/Cx2, 🟡 5 条 A-04/A-05/A-08/F-04/F-14) — ~~A-04 `LedgerStore.Probes()` duck-typing 协议外溢~~ ✅ resolved by PR-REPO-READYZ: `LedgerStore.Probes()` deleted, all three cells unified on `cell.RegisterRepoReadiness` typed funnel (ADR `docs/architecture/202605161030-adr-cell-repo-readyz-probe.md` §D4)；A-05 `MustTamper*` 暴露生产 export 表面；A-08 `Protocol.HMACKey()` getter PII 出口；F-04 `strictTailVerifyOnStartup` 全量 HMAC 启动超时（k8s probe）；F-14 PERMISSION-BASED-AUTHZ-01 注册 (Files: `adapters/postgres/audit_ledger_store.go` + `runtime/audit/ledger/{mem_store,protocol}.go` + `cells/auditcore/cell.go` + `cells/auditcore/slices/auditquery/handler.go`，PR #450 review 2026-05-11)</li><li>**S7-FU-DOCS** (P2/Cx1, 🟡 14 条 DOC-02~07 + PM-01~05 + K-03/K-06 + A-09) — observability.md key 列表/payload redaction 描述同步、ADR 数字编号规范澄清、202605101800 ADR §1.2 实施状态修订、godoc + Example block 补充、journey passCriteria checkRef 切换确认、ADR 增"读侧接口契约 + 三方等价关系"章节、`event/audit/appended` contract.yaml 加 PII 风险声明 (Files: `.claude/rules/gocell/observability.md` + ADR 202605101800 + `runtime/audit/ledger/doc.go` + `cells/auditcore/internal/appender/doc.go` + `contracts/event/audit/appended/v1/contract.yaml`，PR #450 review 2026-05-11)</li></ul> | doc+arch-opt | P1/Cx3 | 🟠 | E.1 立即排期；E.2/E.3 与 S7 接口/PG 演进同窗口 | `README.md` + `docs/guides/` + ADR + `runtime/audit/ledger/` + `cells/auditcore/` + `adapters/postgres/audit_ledger_store.go` + contracts | PR #441 + #450 六角色 review 聚合 |

