# GoCell Backlog — cap-13: 可观测性

> 主要包：`runtime/observability` + `runtime/audit` + `runtime/bootstrap`（health/readyz）+ `adapters/*`（managed resource）
> 父表：[`docs/backlog.md`](../backlog.md)（schema / cap 主轴）
> 归档：[`docs/backlog/archive/`](archive/)

---

## 13.1 health / readyz / probe

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| R3 | **safe_observe DI** — 现状: observe DI 路径未统一；修复: 抽象统一 | arch-opt | — | 🟡 | — | `runtime/observability/` | R3 |
| PR-CI-5-FU-HEALTH-LATE-WATCHER | **Health late watcher** — 现状: late watcher 路径未覆盖；修复: 补 | arch-opt | Cx2 | 🟡 | — | `runtime/http/health/` | PR-CI-5 |
| PR284-FU-COMPOSE-HEALTH | **Compose health** — 现状: docker-compose health 不全；修复: 补 healthcheck | arch-opt | Cx2 | 🟡 | — | `examples/*/docker-compose.yml` | PR#284 |
| B2-R-05 | **OTel metric provider ctx 固定 Background** — ↩ 转 METRICS-CTX-FUNNEL-01：在 PR-1 (PR-OTEL-HARDEN-4, 807-otel-adapter-harden) 内核实，原 backlog 标的「透传 caller ctx」无法在 adapters/otel 内部独立实现——kernel `metrics.Counter/Histogram` 接口故意省略 ctx（Prom 形态 label-binding 理由覆盖 label 维度，ctx 缺席仅靠 `metric_provider.go` 行内意见支撑），对齐 OTel 原生 ctx-bearing 形态需要 Cx4 跨 adapters/prometheus + kernel/outbox + runtime/http + kernel/wrapper 的接口重构，与 OTel adapter hardening 不同 scope。`metric_provider.go` Inc/Add 注释与 `kernel/observability/metrics/metrics.go` 包 godoc 均已加 METRICS-CTX-FUNNEL-01 引用 | bug | P1/Cx4 | ↩ METRICS-CTX-FUNNEL-01 | — | `adapters/otel/metric_provider.go` | backlog2 §3 B2-R-05 → METRICS-CTX-FUNNEL-01 |
| METRICS-CTX-FUNNEL-01 | **kernel metrics 接口 ctx funnel** — 现状: `kernel/observability/metrics.Counter.Inc/Add` 与 `Histogram.Observe` 不带 ctx，adapters/otel 用 `context.Background()` 兜底，丢失 OTel exemplar / baggage 关联；修复: 升 ctx-bearing 形态对齐 OTel 原生 — 所有发射点需要同步改。可定位发射点用 `grep -rn "\.Inc()\|\.Add(\|\.Observe(" --include="*.go" -- kernel/ runtime/ adapters/ cells/` 然后过 type info 确认 receiver 是 `metrics.Counter` / `metrics.Histogram`。具体已知点：`kernel/outbox/emitter.go::DirectEmitter.Emit`（ctx 在 scope）、`kernel/outbox/relay_collector.go::RecordPollResult / RecordBatchSize`（无 ctx，caller `runtime/outbox` 需改）、`kernel/outbox/outbox.go::DiscardPublisher.Publish`（ctx 在 scope）、`runtime/http/middleware/metrics.go::Metrics`（r.Context 在 scope）、`kernel/wrapper/*` 的 metric 发射点。adapter 端：`adapters/prometheus/{metric_provider.go,hook_observer.go}` + `adapters/otel/metric_provider.go` 改 `inner.Add(ctx, ...)`；不向后兼容直接替换；同步在 `metric_provider.go` Inc/Add 注释 + `kernel/observability/metrics/metrics.go` 包 godoc 引用 METRICS-CTX-FUNNEL-01；trigger: B2-R-05 转此条 (PR-807) | bug | P1/Cx4 | 🟡 | B2-R-05 转此条 (PR-807) | `kernel/observability/metrics/{metrics.go,counter.go,histogram.go}` + `adapters/{prometheus,otel}/metric_provider.go` + `kernel/outbox/{emitter.go,relay_collector.go,outbox.go}` + `runtime/http/middleware/metrics.go` + `runtime/outbox/*` (relay caller) + `kernel/wrapper/` | B2-R-05 转条目 (PR-807) |
| B2-A-19 | **OTEL-SPAN-SETATTR-INSECURE-PLAINTEXT** — 现状: `adapters/otel/span.go:46-49` `attrToKeyValue` 对 string 直 `attribute.String` 透传，无 redaction / 无值长度上限 / 无 `Insecure` 构造守卫；`Insecure=true`（明文 collector）时 token/PII 经 `SetAttributes` 明文出站。与仅覆盖 `span.RecordError` 文本的 `SPAN-RECORD-ERROR-REDACT-01` 不同域（后者不拦任意 attribute 值）；修复: 构造期校验 `Insecure=false` 或显式 `WithUnsafePlaintext()` + attribute 值长度上限 + redaction hook | bug | P1/Cx2 | 🟡 | — | `adapters/otel/span.go` | backlog2 §5.3 B2-A-19（030 §6.1 删除回灌 2026-05-19）|
| REPO-READYZ-UPSTREAM-FUNNEL-HARD-01 | **RepoHealthProber conformance-wiring 升 Hard** — 现状: PR-REPO-READYZ funnel 双向锁 = Hard 下游（form-uniqueness archtest `CELL-REPO-READYZ-PROBE-01` N1/N2 锁注册形态）+ Medium 上游（同 archtest 守"新 `cell.RepoHealthProber` 实现必须出现在 `RunRepoReadinessConformance` 调用点"，archtest-bound 非 compile-time——Go 类型系统无法强制"测试必须存在"）。charter §"Funnel 双向锁评级" 要求登记显式 Hard 化任务且 funnel godoc 点名本条。修复方向: sealed registration / codegen marker 让"实现 RepoHealthProber 但未接 conformance"在编译期不可表达 | arch-opt | P1/Cx3 | 🟡 | PR-REPO-READYZ 派生 | `kernel/cell/repo_readiness.go` + `kernel/cell/celltest/` + `tools/archtest/cell_repo_readyz_probe_test.go` | ADR `docs/architecture/202605161030-adr-cell-repo-readyz-probe.md` §Funnel 双向锁评级 |

## 13.2 audit chain observability

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| PR392-FU-AUDIT-CHAIN-WIRING | **BOOTSTRAP-AUDIT-CHAIN-WIRING-01** ✅ shipped (637-bootstrap-audit-chain-wiring, plan 039 W1-2) — onAuthFail observer 收口到 `runtime/audit.NewBootstrapAuthFailObserver` typed funnel：slog（SRE 告警）+ audit hash-chain（合规链）双写；assembly module 顺序 `configcore → auditcore → accesscore` 落地，funnel 双向锁 `BOOTSTRAP-AUDIT-OBSERVER-FUNNEL-{DOWNSTREAM-HARD,UPSTREAM-MEDIUM}-01` + `MODULE-ORDER-AUDITCORE-BEFORE-ACCESSCORE-01` archtest 守 | arch-opt | P2/Cx2 | ✅ | — (shipped) | `runtime/audit/{bootstrap_append,bootstrap_observer}.go` + `cmd/corebundle/{access,audit,shared_deps}_module*.go` + `assemblies/corebundle/assembly.yaml` | PR #392 ADR §D10 + PR #450 unblock; plan 039 W1-2 |
| SSOBFF-BOOTSTRAP-AUDIT-CHAIN-WIRING-01 | **ssobff 同步 bootstrap audit observer** — 现状: `examples/ssobff/app.go::ssobffBootstrapAuthFailLogger` 仍是 slog-only，未走 `runtime/audit.NewBootstrapAuthFailObserver` funnel；ssobff 已自建 auditcore + `buildSSOBFFAuditCore` 持有 `ledger.Store`，可直接复用 corebundle 同款 funnel。修复: 改 ssobff observer 构造为 `audit.NewBootstrapAuthFailObserver(slog.Default(), ledgerStore, clock.Real())`；同步把 `examples/ssobff/app.go` 也纳入 `BOOTSTRAP-AUDIT-OBSERVER-FUNNEL-UPSTREAM-MEDIUM-01` archtest 扫描范围（当前仅 `cmd/corebundle/` 非 _test.go） | arch-opt | P3/Cx1 | 🟠 | corebundle PR (637) ship 后立即触发 — F2 carve-out 解锁 | `examples/ssobff/app.go` + `tools/archtest/bootstrap_audit_observer_funnel_test.go` | plan 039 W1-2 §F2 carve-out |
| BOOTSTRAP-AUDIT-CLIENTIP-PII-EVAL-01 | **bootstrap auth-fail clientIp PII 合规评估** — 现状: `runtime/audit.AppendBootstrapAuthFail` 把客户端 IP 明文持久化到不可篡改 hash chain（ledger.Entry.Payload `clientIp` 字段）+ slog `client_ip` 字段；`auditcore` `auditquery` 出口 `pkg/redaction.RedactPayload` 的 `sensitiveKeyPattern` 不含 `client[_-]?ip`，IP 原样下发给 query 消费者。修复方向: GoCell 引入显式 PII 合规框架（GDPR 或同等）后评估：(A) IP 用 HMAC pseudonymize 入链；(B) 在 `RedactPayload` 加 `client_ip` 到 sensitive key；(C) 接受现状（与 hashicorp/vault audit 同型）。触发: 用户/法务提出 GDPR 合规要求 / 部署地区涉欧盟 | compliance | P3/Cx2 | 🟢 | 用户/法务提出 GDPR 合规要求 / 部署地区涉欧盟 | `runtime/audit/bootstrap_append.go` + `runtime/audit/bootstrap_observer.go` + `pkg/redaction/redaction.go` + `cells/auditcore/slices/auditquery/handler.go` | PR #603 review B1+B2 finding |
| BOOTSTRAP-AUDIT-OBSERVER-OPS-FOLLOWUP-01 | **bootstrap audit observer 实战 baseline followup（3 子项 trigger 独立）** — 共同根因: 新 audit observer 路径缺实战数据，按各自 trigger 独立解锁，**不强制簇内一起改**。**(a) append 失败 Prometheus counter** — `audit.NewBootstrapAuthFailObserver` 失败时仅记 `bootstrap_audit_append_failed` slog 行，无 counter，SRE 无法基于阈值告警；对标 `outbox_emit_failopen_dropped_total`（`kernel/outbox/emitter.go`）。修复: 在 observer 增加 metrics 注入路径（或对接 `obmetrics.ConfigEventCollector` 类似 collector），失败时 counter++。trigger (a): SRE 提出 audit chain 告警 dashboard 需求 / 首次实战告警遗漏事故。**(b) 同步 Append 阻塞 setup 端点** — observer 返回的 closure 在 detached ctx 下同步调用 `ledger.Store.Append`，PG ledger 慢/挂时 setup/admin 端点 worst-case HTTP 响应延迟 = `bootstrapAppendDetachedTimeout`（PR #603 二轮 review H1 已 ship，当前 = 2s）。这是 setup 端点 P99 的明确下限，而非"等观察才发现"。当前 fail-closed 是有意选择（async/timeout 会引入合规丢记录窗口）。修复方向（任一）: 缩短 detached timeout (≤ 1s) / bounded goroutine pool + 背压 / 接受现状（合规高于可用性）/ async 化并接受合规窗口。trigger (b): SRE 提出 setup 端点 P99 < detached-timeout SLA / 合规对"丢记录窗口"容忍度评估完成 / PG ledger 实战故障案例。**(c) rate_limited 高频 burst 聚合** — `rate_limited` 每请求写一条独立 ledger entry (EventID = uuid.NewString())，理论爆破产生噪音淹没合规通道；当前 bootstrap rate limiter `5 req/min + burst 10` 限制实际 burst 远低于阈值。修复方向: 5 分钟窗口聚合（"N 次 rate_limited from IP X"）。trigger (c): 实战观察 audit ledger entry 中 rate_limited 比例 ≥ 70% 持续 1 周。 | arch-opt | P2/Cx2 | 🟠 | (a) SRE 告警需求 / (b) 生产 P99 异常 / (c) rate_limited ≥ 70% 持续 1 周 — **任一子项 trigger 命中即单点修复对应子项** | `runtime/audit/bootstrap_observer.go` + `runtime/audit/bootstrap_append.go` + `cmd/corebundle/access_module.go` (metric 注入路径) + `runtime/observability/metrics/` | PR #603 review B3+B6+C2 合并 BOOTSTRAP-AUDIT-APPEND-FAIL-METRIC-01 + BOOTSTRAP-AUDIT-APPEND-SYNC-BLOCK-TRIGGER-01 + BOOTSTRAP-AUDIT-RATE-LIMITED-BURST-AGGREGATE-01 |

## 13.3 metrics / collector

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| OBS-SSA-ANALYZER-01 | **OBS SSA analyzer** — 现状: 缺静态分析；修复: 加 SSA-based analyzer | arch-opt | Cx3 | 🟡 | — | `tools/archtest/` + `runtime/observability/` | OBS-SSA |
| M1-OBSERVED | **HEALTHZ-INTERFACE-PACKAGE-01** — 现状: 38 处 Health 实现分散无统一接口；修复: 新建 `kernel/healthz` 接口包 (Aggregator/Probe/Snapshot) + codegen 从 cell.yaml 派生状态 schema + 默认 `runtime/observability/healthz/inmemory` 实现 + 可选 postgres/otel adapter + `HEALTHZ-WRITE-01` archtest + 38 处分散 Health 收口（不持久化 yaml，持久化交宿主） (also: cap-14, cap-10) | feat | P2/Cx3 | 🟡 | — | `kernel/healthz/` (新) + `runtime/observability/healthz/` + `tools/codegen/` | ADR-202605041430 M1 |
| CONFIGPUBLISH-FAILOPEN-METRIC-ASSERT-HARD-01 | **fail-open metric counter 断言 (Hard 升级 PR320-FU)** — 现状: PR#553 用 `outbox.WarnDirectPublishFailOpen` exported const 把 fail-open 信号锁到 Medium；`outbox_emit_failopen_dropped_total` counter 已 fire（emitter.go:206-209）但无业务测试断言，需要 metricstest.RecordingProvider testutil 才能 assert counter +1。修复方向: 新建 `kernel/observability/metrics/metricstest/recording.go` (~80 行) + 测试 + ADR；改 fail-open service test 用 RecordingProvider 取代或补充 WARN const 断言（达 Hard：counter 没增 = 没走 fail-open，违反不可表达）。(also: cap-07) | arch-opt | P3/Cx3 | 🟠 | 第 2 个 cell 需要断言相同 counter 模式 | `kernel/observability/metrics/metricstest/`（新）+ `cells/*/slices/*/service_test.go` | PR#553 plan §AI-rebust evaluation |
| S7-PG-OBSERVABILITY-ALIGN-BUNDLE | **S7 PG / observability / migration 对齐束（PR #450 review 聚合，4 子条）** — 现状: S7 落地的 PG/migrations/readyz/observability 与现有规范的对齐尚未补齐，加上跨 outbox/audit 共通的运维收口 3 条。子条：<ul><li>**S7-FU-OPS** (P2/Cx1, 🟡 13 条 D-02~D-09 + F-06/F-08/F-09/F-11/F-13) — 020 migration `prev_hash/hash` TEXT 加 `CHECK(length=64)`；`strictTailVerify` 加 30s timeout；021 Up/Down 加 `SET LOCAL lock_timeout='5s'`；`audit_ledger_ready` 注册到 readyz.md；`auditQueryFetchCap=5000` 注释；`audit_ledger_store` Append/Verify/Query 加 OTel span（S8）；020 Down 路径加 lock_timeout；actor_id 合规决策；$1 双引用注释；`clear(hmacKey)` 双重冗余；migration table 名硬编码 (Files: `adapters/postgres/migrations/{020_audit_ledger,021_audit_entries_event_id_unique}.sql` + `audit_ledger_store.go` + `cells/auditcore/` + `cmd/corebundle/audit_module.go`，PR #450 review)</li><li>**KERNEL-OUTBOX-PROBE-NAME-RENAME-01** (P3/Cx1, 🟠 下次 outbox 主题 PR) — `outbox-failopen-rate.auditcore` 含连字符违反 observability.md snake_case；改 `outbox_failopen_rate_<cellid>` + archtest 锁命名 (Files: `kernel/outbox/emitter.go`，PR #450 F-CR-OUT-OF-SCOPE-1)</li><li>**AUDIT-LEDGER-CROSS-POOL-TEST-IMPROVE-01** (P3/Cx1, 🟠 `setupPostgres` 暴露 DSN API) — `TestAuditLedgerStore_RestartRecovery_AcrossPool` 共享同一 `pgxpool.Pool`（非真物理重启）；`setupPostgres` 暴露 DSN 后用 `pgxpool.New(ctx, dsn)` 替代 pA (Files: `adapters/postgres/audit_ledger_store_test.go` + `integration_test.go`，PR #450 F-CR-OUT-OF-SCOPE-2)</li><li>**S8-AUDIT-QUERY-KEYSET-PUSH-DOWN-01** (P1/Cx3, 🟠 S8 audit query PG adapter 落地) — ❌ 未实现（2026-05-18 核实）：`runtime/audit/ledger/store.go:40-50` `QueryListParams` 仍仅 `Limit int`（注释自述 "no cursor ... PG store will extend with keyset in S8+"）；`adapters/postgres/audit_ledger_store.go:386-398` 仅 `AppendParam("LIMIT ",...)` 无 `pgquery.AppendKeyset`；硬 limit `cells/auditcore/slices/auditquery/service.go:29` 现为 **`auditQueryFetchCap=500`（非原描述 5000，描述订正）**。修复: 扩 keyset cursor（mirror configcore List）+ PG `pgquery.AppendKeyset` + 去硬 limit (Files: `runtime/audit/ledger/store.go` + `adapters/postgres/audit_ledger_store.go` + `cells/auditcore/slices/auditquery/service.go`，PR #450 F-CR-4 follow-up)</li></ul> | feat+arch-opt+test | P1/Cx3 | 🟠 | F.4 S8 落地一并；F.1 PR450 follow-up batch；F.2/F.3 触发型 | `adapters/postgres/migrations/` + `audit_ledger_store.go` + `runtime/audit/ledger/` + `cells/auditcore/` + `cmd/corebundle/audit_module.go` + `kernel/outbox/emitter.go` | PR #450 六角色 review 聚合 (2026-05-11) |

## 13.4 slog / logging / OTel

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| PR237-OB2 | **Listener observability** — 现状: per-listener 观测 metric 不全；修复: 补 | arch-opt | Cx2 | 🟡 | — | `runtime/observability/` | PR#237 |
| PR283-OTEL-SLOG-ERROR-ATTR | **OTEL-SLOG-ERROR-ATTR-NORMALISE-01** — 现状: `slog.Any("error", err)` 在 OTEL bridge 会展开 struct；修复: ReplaceAttr hook 序列化 err.Error() | arch-opt | P2/Cx2 | 🟠 | 首次 OTEL slog bridge 接入 | `adapters/otel/` + `runtime/observability/logging/` | PR#283 round-2 I3 |

## 13.5 adapter managed resource / 杂项

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| A5a-R3 | **Observability ctx 透传** — 现状: 部分路径丢 ctx；修复: thread ctx | arch-opt | — | 🟡 | — | `runtime/observability/` | A5a |
| A5a-R12 | **Observability 集成补全** — 现状: integration test gap；修复: 加测 | test | — | 🟡 | — | `runtime/observability/` | A5a |
| P4-TD-10 | **Metrics path label cardinality** — 现状: `r.URL.Path` 直接作 label，参数化路由展开成高基数序列（`/users/123` `/orders/42`...）；修复: 改用 chi route template 或 `_` 占位 | bug | P2/Cx2 | 🟡 | — | `runtime/observability/metrics.go` | tech-debt-registry P4-TD-10 |
| WS-DX-01 | **WS per-conn context tracing** — 现状: per-conn ctx 基于 Background()，无 tracing/correlation 传到 MessageHandler；修复: 透传 tracing ctx | arch-opt | Cx2 | 🟡 | observability 接入时 | `runtime/websocket/` | tech-debt-registry WS-DX-01 |
| USER-REPO-READYZ-PROBE-01 | **UserRepository.RepoReady probe 探测 users 表 schema** — 现状: accesscore 已有 `session_store_ready` probe（探测 `sessions` 表），不覆盖 `users` 表 schema 漂移；migration 032 新增 `failed_login_count` / `last_failed_at` / `locked_until` 三列，`UpdateLockoutFields` 若列缺失会在运行期 fail，但 readyz 不感知。修复方向: 新增 `UserRepository.RepoReady` 实现，执行代表性查询（如 `SELECT failed_login_count FROM users LIMIT 1`）验证三列存在；通过 `cell.RegisterRepoReadiness(reg, "user_repo_ready", prober)` 注册（遵循 CELL-REPO-READYZ-PROBE-01 funnel）；`celltest.RunRepoReadinessConformance` 覆盖 healthy / 列删除 / mem 三路。**触发条件**: migration 漂移导致 UpdateLockoutFields 运行期失败 | feat | P3/Cx2 | 🟢 | migration 漂移导致 UpdateLockoutFields 运行期失败 | `cells/accesscore/internal/adapters/postgres/user_repo.go` + `cells/accesscore/internal/mem/user_repo.go` + `cmd/corebundle/access_module.go` | PR-618 review F7 |
| SAFEID-UPSTREAM-FUNNEL-HARD-01 | **SafeID wire-decode 上游 Hard 升级** — 现状: PR #582 引入 `pkg/idutil.SafeID` typed funnel；下游 Hard（`SAFEID-WIREMESSAGE-USAGE-01` archtest 锁字段类型）成立，但上游"`UnmarshalEnvelope` 是 wire envelope 唯一 decode 入口"目前仅 reasoning-based（无 archtest 锁定）；修复方向: archtest caller-allowlist — 用 typeseval 检查所有"将 bytes unmarshal 到 `*outbox.WireMessage`"的调用点，要求 callee 是 `outbox.UnmarshalEnvelope`。AI-rebust 升级路径 Medium-by-archtest → Hard。触发条件: 出现第三处直接 `json.Unmarshal(bytes, &outbox.WireMessage{})` / 跨 transport 增量调用点 / archtest 周期复盘。 | arch-hard | P2/Cx2 | 🟡 | 同 触发条件 | `tools/archtest/safeid_funnel_test.go` (新增 caller archtest) + 可能 `tools/archtest/internal/typeseval/` helper | PR #582 review F2 |
| METRICS-GAUGEVEC-UPSTREAM-HARD-01 | **GaugeVec funnel 上游 Hard 升级** — 现状: D3a-1 added `metrics.Provider.GaugeVec` interface and archtest `METRICS-GAUGEVEC-FUNNEL-01` to ban direct `prom.NewGaugeVec` / OTel `Float64UpDownCounter` calls in business packages. The downstream form-uniqueness is Hard (callee resolved via `*types.Info`), but the upstream is Medium: `prom.NewGaugeVec` is a public exported API and the Go type system cannot prevent business packages from importing prometheus directly and calling it. The archtest scope filter (excluding `adapters/prometheus/` and `adapters/otel/`) is the only caller-allowlist mechanism today. **Upgrade path**: Wrap `prom.NewGaugeVec` (and Counter / Histogram peers, for consistency) inside `adapters/prometheus/internal/promwrap/` — a Go-internal package. The `internal` mechanism prevents external imports at compile time, making upstream Hard. Same approach for the OTel side. **Risk**: blocked on refactor cost (~6h) and potentially churn across adapter-internal callsites. D3a-1 ship made the scope-filter trade-off deliberately; this entry tracks the principled upgrade. **Refs**: `tools/archtest/observability_metrics_test.go` TestGaugeVecFunnel godoc; `.claude/rules/gocell/ai-collab.md` §Funnel 双向锁评级. | arch-hard | P2/Cx2 | 🟡 | 第二个直接 NewGaugeVec 调用点出现时 | `adapters/prometheus/internal/promwrap/` (new) + `adapters/otel/internal/otelwrap/` (new) + `tools/archtest/observability_metrics_test.go` | D3a-1 ship |
| BOOTSTRAP-SHUTDOWN-OUTCOME-LABEL-ALIGNMENT-01 | **shutdown outcome label + metric name 三源不一致** — `runtime/bootstrap/phases_shutdown.go` emits `outcome="success"` to `bootstrap_shutdown_total`, but `docs/ops/alerting-rules.md` ~line 200 documents `outcome="clean"`, and `docs/ops/graceful-shutdown-k8s.md` ~line 71 references the metric name `bootstrap_shutdown_outcome_total` (actual name is `bootstrap_shutdown_total`). Pre-existing tech debt surfaced by D3a-1 W4 shutdown_metrics MOVE review. **Upgrade path**: unify code + alerting-rules.md + graceful-shutdown-k8s.md on a single label value (`"success"` — cheaper to update 2 docs than 1 emitter + every operator dashboard). Pick during the next shutdown-related PR. **Risk**: any dashboard/alert using `outcome="clean"` or the old metric name is silently empty today; operators would have noticed if live. Self-resolving when shutdown work next occurs. | tech-debt | P2/Cx1 | 🟡 | 下次触碰 shutdown 相关代码时 | `runtime/bootstrap/phases_shutdown.go` + `docs/ops/alerting-rules.md` + `docs/ops/graceful-shutdown-k8s.md` | D3a-1 PR #589 review F17 |
| CONSUMER-BASE-SLOG-DEFAULT-INJECTION-01 | **ConsumerBase test global slog 注入** — `kernel/outbox/consumer_base_test.go` uses `captureDefaultSlogForConsumerBase` which calls `slog.SetDefault()` globally. Pattern marks itself "Do NOT call t.Parallel()", but other tests in the same package use `t.Parallel()`. If the parallel scheduler colocates them in the same process generation, the global slog handler gets stomped — undefined log capture behavior. D3a-1 added one more test (G-05 retry-exhausted log level) that uses the same capture helper without amplifying the risk. **Upgrade path**: inject `*slog.Logger` via `ConsumerBaseConfig` instead of capturing global, allowing per-test slog without `SetDefault` mutation. **Risk**: low — pattern works today, no observed flake. Cleanup for a future kernel/outbox refactor. | tech-debt | P3/Cx1 | 🟡 | kernel/outbox 下次重构时 | `kernel/outbox/consumer_base_test.go` | D3a-1 PR #589 review F16 |
| ROADMAP-D3A2-UNBLOCK-ANNOTATION-01 | **D3a-2 roadmap row 依赖注释过时** — After D3a-1 ship, the roadmap's D3a-2 row (line ~72 of `docs/plans/202605101839-029-master-roadmap.md`) still says "与 D3a-1 可并行（无依赖）". Post-ship it should say "D3a-1 已 ship；D3a-2 unblocked". Cosmetic — self-resolving when D3a-2 PR includes its own roadmap update. **Risk**: none (informational). | doc | P3/Cx1 | 🟡 | D3a-2 PR 自行修复 | `docs/plans/202605101839-029-master-roadmap.md` | D3a-1 PR #589 review F33 |

