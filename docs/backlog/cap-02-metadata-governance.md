# GoCell Backlog — cap-02: 元数据解析与治理

> 主要包：`kernel/governance` + `kernel/contractspec` + `kernel/depgraph` + `tools/archtest/internal/typeseval`
> 父表：[`docs/backlog.md`](../backlog.md)（schema / cap 主轴）
> 归档：[`docs/backlog/archive/`](archive/)

---

## 02.1 kernel spec / contractspec / depgraph

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| P1-5 | **METADATA-PERF-BENCH-01** — 现状: 缺 `BenchmarkParseFS_500Files` 性能基准；修复: 加 bench + 评估 goccy/go-yaml 单次解码迁移成本 | test | P1/Cx3 | 🟡 | — | `kernel/metadata/parser_test.go` | PR#152 seat-4 |
| KERNEL-CONTRACTSPEC-CONTRACTMETA-DUAL-DEF-01 | **Contract 双源定义** — 现状: `kernel/wrapper.ContractSpec` 与 `kernel/metadata.ContractMeta` 双源；修复: K#04 PR-4 codegen 落地时合一 | arch-opt | Cx3 | 🟠 | K#04 PR-4 codegen 迁移 | `kernel/wrapper/` + `kernel/metadata/` | systems layer review |
| KERNEL-INTERNAL-DAG-GUARD-01 | **kernel 反向 import 守卫** — 现状: 缺 archtest 守 kernel 反向 import；修复: 引入新依赖时一并加 DAG 守卫 | arch-opt | Cx2 | 🟠 | kernel 出现新反向引用 | `tools/archtest/` | systems layer review |
| SHARED-ERROR-SCHEMA-GENERATION-01 | **共享 error schema 单源** — 现状: 4 份 mirror 人工同步；修复: canonical → make generate 派生 examples/testdata | arch-opt | P2/Cx2-Cx3 | 🟡 | 下次 envelope schema 变更 | `contracts/shared/errors/` + `tests/contracttest/testdata/` | PR#396 review |
| KERNEL-DEPGRAPH-OUT-EVAL-01 | **Depgraph out evaluation** — 现状: depgraph 只 in-eval；修复: 加 out-eval 路径 | arch-opt | Cx3 | 🟠 | 第 3 个 depgraph 消费方 | `kernel/depgraph/` + `runtime/` | PR#357 |
| DURABLE-TYPE-01 | **L2/L3 持久化级别静态保护** — 现状: 类型抹除让 L2/L3 检测退化为启动期 panic；修复: 探索类型系统层面静态编译保护（仓储级能力推断） | arch-opt | P2/Cx3 | 🟡 | v1.1 启动 | `kernel/metadata/` + `kernel/persistence/` | backlog_later §6 |
| KERNEL-TEST-RUNTIME-IMPORT-BAN-01 | **守 kernel/**/*_test.go 禁止 import runtime/*** — 现状: depguard kernel-isolation 排除了 _test.go（4 处历史 import: governance/rules_http_test.go, metadata/meta_struct_guard_test.go, cell/celltest/mux_test.go）；修复: 加 depguard kernel-test-isolation rule（allowlist runtime/auth + runtime/devtools/catalog 因测试 wiring 必需）或评估 auth/catalog 抽出 | arch | P3/Cx2 | 🟠 | 下次 touch 这些文件时一并处理 OR 独立守卫 PR | `.golangci.yml` + 4 处 kernel test | PR441 review reviewer-F1（探索后确认全部历史遗留，PR441 未引入）|
| PR-FIXTURE-CELLID-TYPED-BUILDER-01 | **fixture cell-id typed builder Hard 升级** — 现状: governance / archtest 测试 fixture 用裸 string literal 构造 cell id（如 `kernel/governance/targets_test.go::l0Project()`、`rules_misc_strict_test.go::TestValidator_FMTC1_CellIDPattern` 的 RED case），PR #484 review v2 sed 修了既有 kebab 残留（shared-validate → sharedvalidate / billing-core → billingcore）但**仍是 Soft**（hand-crafted fixture by string literal，AI 下次写测试可重新引入非合规 id）；修复: 在 `kernel/metadata/metadatatest`（或就近 testhelper 包）暴露 `NewCellID(s string) string` typed builder，构造时 `MatchCellID` 不合规则 panic（fixture 用错时 fail-fast），archtest 用 typeseval callee 解析 ban 任何 cell-id-as-string-literal 的 fixture map key（与 PR #484 CELL-ID-PATTERN-SINGLE-SOURCE-01 同款 typed-info funnel 形态） | arch-opt | P2/Cx2 | 🟠 | 下次 governance fixture 大改 / 引入 metadatatest helper 时一并落地 | `kernel/governance/*_test.go` + `kernel/metadata/metadatatest/` + `tools/archtest/` | PR #484 review v2（Soft 既有补丁 silent carryover 禁止；章程 ai-collab.md "Soft → Hard 改造方向 - hand-crafted fixture → real source AST capture"）|

## 02.2 typeseval / archtest helper

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| PR-TS1-FU-VALIDATIONRESULT-EMITTER-SEALED-MARKER-01 | **ValidationResult emitter Hard 升级（owner allowlist via types.Identical）** — ✅ closed by R2-P1 (2026-05-16): `kernel/governance/emitter_invariant.go` 新增 BFS owner invariant 声明 + 3 行 compile-time witnesses (`var _ *locator` / `var _ RuleCode` / `var _ ValidationResult`)；`signatureMatchesValidationResultEmitter` predicate body 零字符串锚点，三处全 `types.Identical`——owner gate 直接 `types.Identical(recvNamed, locatorType)` 限定 *locator 是唯一 emitter holder；fixture loader fail-fast on missing scope identifier；新增 2 个 RED fixture（`embedded_locator_outer_receiver_ignored_RED` 覆盖 embedding promotion 路径，`unrelated_receiver_with_emitter_shape_ignored_RED` 覆盖完全无关类型）。AI-rebust **Hard**（predicate body 违反不可表达 + types.Identical 结构性免疫 method promotion；rename 在 compile-time witness 即 build-fail）。注意：方案首版（PR #521 commit baba2a6f2）用 sealed marker iface + types.Implements，被 reviewer F-1 发现因 Go embedding promotion 导致 *Validator/*DependencyChecker 也实现 marker → owner gate 失效。types.Identical 形态结构性免疫此问题。OSS 对标：staticcheck/govet/gopls 同类 loader miss 均 Soft 静默 skip——本方案严两档。 | arch-opt | P3/Cx2 | ✅ R2-P1 | — | `kernel/governance/emitter_invariant.go` + `kernel/governance/rule_inventory_test.go` + `kernel/governance/rule_inventory_bfs_test.go` + `kernel/governance/signature_match_predicate_test.go` | PR-TS1 review P2-2（2026-05-11）→ R2-P1 (2026-05-16) |
| TYPESEVAL-IFACE-IMPL-HELPER-CONSOLIDATE-01 | **typeseval ImplementsInterface helper 收编** — 现状（R2-P2 实施期全仓 `types.Implements` 扫描修正：非「实测 2-3 处」，**实际 6 处调用点、两种语义**）: value-or-ptr 5 处（`managed_resource_contract:152` / `rmq_invariants:142,264` / `http_contract_visibility:213,219` / `cell_repo_readyz_probe:426` / `user_repo_conformance_enrollment:334`，形态不一致 site1 无条件双包裹 vs site2 防护）+ value-only 1 处（`cell_public_option_param:229`，`t` 常为 `*types.Interface`，ptr fallback 无意义）；charter §5 L4 承认"interface impl 枚举"是 typed helper 真实需求。修复: 在 `tools/typesutil/`（非 internal）抽**两个 typed helper** `ImplementsInterface(t types.Type, iface *types.Interface) bool`（value-or-ptr）+ `ImplementsInterfaceExact`（value-only，charter typed-choice-for-semantics 范本）——签名镜像 stdlib `go/types.Implements(V,T)` + 对标 x/tools copylock.go，**有意偏离** backlog 文字签名 `(typesInfo, expr, ifaceObj)`（6 站点无一持 `ast.Expr`）。12-case 表驱动单测；6 站点全委托。PR-a（framework+迁移，#540 merged）+ **PR-b**（archtest funnel `TYPESUTIL-IMPLEMENTS-FUNNEL-01`，info.Uses 全量 sweep 禁裸 `go/types.Implements` 任意引用形态出 helper 文件外，PANIC-REGISTERED-01 同构 **Hard**，结构性消除复发面）—— **PR-b 关闭本行（R2-P2 PR-b，#547）** | arch-opt | P3/Cx2 | ✅ | ~~第 4 处 implements* 重复~~ → PR-b funnel 后**结构性消除**（archtest-RED：call/dot-import/alias/func-value 全覆盖，trigger 不可能再触发） | `tools/typesutil/implements_interface{,_test}.go` + 6 `tools/archtest/*_test.go`（PR-a）+ `tools/archtest/implements_funnel_test.go`（PR-b） | 036 plan §2 helper 命名修订 v3；charter §5 L4 + §Funnel 双向锁 |
| TYPESEVAL-EVAL-PREDICATE-CENTRALIZED-01 | **TYPESEVAL-EVAL-PREDICATE-CENTRALIZED-01** — ✅ closed by PR #475 (Phase 3.5 PR-EP1, 2026-05-12, Hard funnel)：`eval_predicate_centralization_test.go` AST walk `tools/archtest/` 所有 `constraint.Expr.Eval(...)` callsite，断言 predicate 属两种 canonical shape 之一（`typeseval.BuildContextPredicate(...)` / 全 false sentinel）；其他形式 fail-closed。**AI-rebust Hard**：手写含 hard-coded toolchain default tags 的 predicate 在 archtest 即报错。Form-uniqueness pattern 对齐 PANIC-REGISTERED-01 inline-literal Hard surface | arch-opt | P2/Cx2 | ✅ PR #475 | — | `tools/archtest/eval_predicate_centralization_test.go` + `tools/archtest/internal/typeseval/buildtag_predicate.go` | PR #472 review P1+P2 → PR #475 |
| CLOCK-INJECTION-STRUCT-FIELD-CTOR-01 | **clock injection archtest 仅覆盖 WithClock-option 构造函数，不覆盖 struct-field Clock 构造函数（oidc.Adapter）** — 现状: `CLOCK-INJECTION-TEST-CALLSITE-01` archtest 扫描 `WithClock` option 调用点，oidc.New 用 `Config.Clock` struct 字段注入，被 archtest 静默跳过；enforcement 依赖 runtime `clock.MustHaveClock` panic。修复: 扩展 archtest 支持 struct-literal 初始化路径中 `Clock` 字段为 nil 的检测，或引入 typed constructor marker 把两种注入形态统一到同一 funnel | arch-opt | P2/Cx3 | 🟠 | 下次新增 struct-field Clock adapter 时 | `tools/archtest/` + `adapters/oidc/oidc.go` | PR #504 review F-5 |
| ARCHTEST-LAYER10-PASS-MIGRATION-01 | **archtest_test.go LAYER-10 永久豁免跟踪** — 现状: `archtest_test.go` 中 `checkCellPublicAPIAdapterTypes`（LAYER-10 规则）经 `depgraph.FromPackages([]*packages.Package)` 使用 `*packages.Package` 类型化输入，Pass funnel 故意隐藏 `.Syntax` 字段，使该 depgraph 输入形态结构性不可通过 Pass funnel 表达，故 `archtest_test.go` 加入 `passFunnelPermanentExempt`（不在 LegacyAllowlist，stage-4 不会被删除，而是作为框架永久豁免保留）。修复方向: 评估 `depgraph.FromPackages` 是否可改为接受 Pass 派生输入（如 `[]*archtest.Pass` 或 Pass 内置 depgraph 视图），消除该 governance-consumer 永久豁免；或确认 LAYER-10 规则的 `*packages.Package` 依赖属于 depgraph 构造器语义（非 archtest 消费 archtest），won't-fix 关闭并归档。即使最终 won't-fix 也防止 silent carryover，ai-collab.md §Funnel 双向锁评级要求此类条目登记 backlog | arch-opt | P3/Cx2 | 🟠 | (a) 出现第二个 archtest_test.go 级别的永久豁免需求 / (b) depgraph 接口升级窗口 | `tools/archtest/archtest_test.go` + `tools/depgraph/depgraph.go` + `tools/archtest/pass_funnel_test.go` | PR #522 review F3（2026-05-16）；ADR 202605141519 §PR-522-amendment passFunnelPermanentExempt 说明 |
| ARCHTEST-ACCESSCORE-FACADE-CALLER-TYPEAWARE-01 | **accesscore_facade_test.go caller-side scan 升级 type-aware（Soft → Medium）** — 现状: `accesscore_facade_test.go` 的 `scanResolveBootstrapCredentialPathCalls` 函数用 AST 字符串名匹配识别调用方（无 receiver type 解析 / 无 package resolve），ai-collab.md §"既有 Soft 的补丁" 要求登记升级条目，防止 silent carryover。修复方向: 升级为 type-aware — 迁入 `RunTyped` + `ResolveMethodCall` 验 receiver 来自 `cells/accesscore`（Medium 形态）；若方法名足够唯一可接受 Soft，则登记 won't-fix 并归档。注意: 该文件本身不在本迁移批次，本 PR #522 不改其代码，仅登记 backlog。 | arch-opt | P3/Cx2 | 🟠 | 下次触及 `accesscore_facade_test.go` caller-scan 路径，或 reviewer 因 Soft 形态指出 finding | `tools/archtest/accesscore_facade_test.go` | PR #522 review F6（2026-05-16）；ai-collab.md §既有 Soft 补丁 silent carryover 禁止 |
| ARCHTEST-SHARDCOUNT-SYNC-GUARD-01 | **`SHARD_COUNT=16` 跨文件耦合静态守卫（SYNC 注释 → 静态/回归守卫升级）** — 现状: `hack/verify-archtest.sh` 默认 `SHARD_COUNT=16` 与 `.github/workflows/_build-lint.yml` archtest job env `SHARD_COUNT: 16` 是两处独立真理源，PR #522 仅加双向 `# SYNC:` 注释（最弱守卫）。耦合**非 PR #522 引入**（pre-existing），`ARCHTEST-VERIFY-COVERAGE-01` 已实测兜底分片覆盖完整性（447 exactly-once 分区，无空洞），故 PR #522 不强加静态守卫（避免范围蔓延）。修复方向: 加静态守卫——脚本启动时 assert env 值与 workflow yaml 解析值一致，或 archtest 解析两处断言相等（类比 `TestPassFunnelGuardListSync` 解析 `.golangci.yml` 三向同步范式）；或确认 `ARCHTEST-VERIFY-COVERAGE-01` 的 exactly-once 分区检测已构成充分上游守卫，SYNC 注释足够，won't-fix 归档。登记防 silent carryover（ai-collab.md §既有 Soft 补丁 + feedback_constraint_self_close）。 | arch-opt | P3/Cx2 | 🟠 | 下次改 `SHARD_COUNT` 值，或 reviewer 因双源漂移指出 finding | `hack/verify-archtest.sh` + `.github/workflows/_build-lint.yml` + `tools/archtest/archtest_verify_coverage_test.go` | PR #522 review 代码审查F2 vs DevOps（2026-05-16）；用户裁定本 PR 仅 SYNC 注释 + backlog |

## 02.3 governance rule (G-series + PR-FU)

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| M2-LIFECYCLE | **CELL-SLICE-LIFECYCLE-FIELD-01 + STATE-MACHINE-EXPLICIT** — 现状: (a) cell/slice 缺生命周期相位声明；(b) `cell.lifecycle` 与 `outbox.entry.state` 隐含状态机，无 enum + transition 表；修复: cell.yaml/slice.yaml 加 `lifecycle` 字段 (experimental/candidate/asset/maintenance/retired) + `kernel/cell/lifecycle.go` 显式 `state enum + transition map` + `kernel/outbox/state.go` 同款 + governance 校验状态转移合法性 + archtest 校验状态转移完备性 + 运行时通过 Aggregator 接口暴露当前相位（差距由消费方计算）+ 1 ADR (also: cap-13) | feat | P2/Cx3 | 🟠 | M1 落地 | `kernel/metadata/types.go` + `kernel/cell/lifecycle.go` + `kernel/outbox/state.go` + `kernel/governance/` + `kernel/healthz/` + ADR | ADR-202605041430 M2 + 030 §3 F-08 |
| M3-RULE-ENGINE | **GOVERNANCE-RULE-ENGINE-DATA-DRIVEN-01** — 现状: governance 64 规则散在 Go 代码；修复: `kernel/governance/engine.go` 唯一执行体 + `kernel/governance/rules/*.yaml` 数据化（5 槽位 detect/evidence/next/level/harvest）+ `next-action` 五级 (autofix/suggest/advisory/block/escalate) + 规则带 `metric` 距离函数 + 修 ADV-05 SeverityError 错分 | refactor | P2/Cx3 | 🟡 | — | `kernel/governance/engine.go` (新) + `kernel/governance/rules/*.yaml` (新) | ADR-202605041430 M3 |
| G-1 | **FMT-11 dynamic-status-field 隔离** — 现状: 动态状态字段（readiness/risk/blocker）漏入非 status-board 文件，元数据被污染；修复: governance 加 FMT-11 严格隔离 | doc | P2/Cx2 | 🟡 | 出现元数据污染或非法 contract 引用 | `kernel/governance/` | backlog_later §1 |
| GOVERNANCE-AUTH-PUBLIC-INTERNAL-FORBIDDEN | **static governance 禁 auth.public 在 /internal/v1/** — 现状: runtime 守存在，元数据 governance 阶段无闸门；修复: 加 FMT-XX 规则 + codegen fail-fast，contract 出现 `auth.public:true` + `/internal/v1/*` 路径模式即报错 | bug | P1/Cx2 | 🔴 | 发布前安全收口 | `kernel/governance/rules_fmt.go` + `tools/codegen/contractgen/builder.go` | PR#376 F-SEC-002 |
| PR408-FU-GOVERNANCE-OWNER-AST-EXTRACTION-01 | **Inventory governance section AST owner 提取** — ❌ subsumed by PR #435 (`ab4b93a70` `INVENTORY-ANCHOR-SINGLE-SOURCE 彻底版 — drop persistent inventory + drift gate`，2026-05-16 核销): 两个代码前提结构性消除——(1) `docs/audit/archtest-inventory.md` 已**删除**（非改造），「inventory 加 referenced_by 列」无载体；(2) `list-archtests.sh` 不再抽 ID，现为纯 stdout raw 视图（godoc: "intentionally does NOT re-implement parsing — there is no second grammar to drift"），「引用关系被算成归属」根因消失；(3) canonical owner 改由文件 header `// INVARIANT: <ID>` 源码锚点承载，两条 Hard archtest 守卫（`INVENTORY-ANCHOR-REQUIRED-01` + `INVENTORY-ANCHOR-VALID-ID-01`），比原 go/ast Medium 提案档位更高（co-located + single source）。**不新建任何 tool**：重建 referenced_by 索引 = 回归 PR #435 刻意铲除的持久化 artifact + drift gate，违反 single-source。详见 037r2 plan R2-P4 段。 | arch-opt | P1/Cx2 | ❌ subsumed | — | `tools/archtest/inventory_anchor_required_test.go`（gate）+ `scripts/audit/list-archtests.sh`（已降为 raw 视图）；~~`docs/audit/archtest-inventory.md`~~ 已删除 | 2026-05-07 review → PR #435 (2026-05-16) |
| PR411-AUTH-SCHEMA-GOVERNANCE-BOOL-SEMANTICS-01 | **auth mode schema/governance boolean semantics alignment** — 现状: `contract.schema.json` 用 `required` 判断 auth mode 互斥，显式 `false` 字段也会触发 schema 冲突；governance FMT-27/FMT-28 按布尔真值判断，二者语义不完全一致；修复: 统一为真值语义（schema 用 const/if-then 或明确禁止显式 false），并补 `auth.public:false + auth.bootstrap:false` 等显式 false 回归测试 | bug+test | P2/Cx2 | 🟡 | PR#411 follow-up batch 或 schema/govalidate 语义收口 | `kernel/metadata/schemas/contract.schema.json` + `kernel/metadata/schemas/contract_schema_test.go` + `kernel/governance/rules_fmt_test.go` | PR#411 review |
| G-11 | **SCAFFOLD-FREETEXT-YAML-INJECTION** — `Goal`/`OwnerTeam` 自由文本写 YAML 无字符过滤，`\n` 注入产生额外键绕过 VERIFY/FMT；修复: `validateFreeText()` 拒 `\n\r":#[]{}` + 模板裸 scalar 改单引号包裹 + `TestCreateJourney_YAMLInjection` 对抗测试 | bug | P1/Cx2 | 🟡 | 发布前安全收口 | `kernel/scaffold/` + `kernel/governance/` | 030 §3 G-11 |
| G-13 | **GOVERNANCE-RULES-REGISTRATION-GUARD** — (a) `Validator.rules()` 手工 slice，漏注册零反馈；(b) `ValidateStrict`/`ValidateStrictFailFast` 双列表漂移；(c) error 规则无修复指导；(d) rule code 字面量散落；修复: archtest 反射枚举 + 统一 `ValidateStrict(strict, failFast bool)` 单入口 + error 规则参照 ADV-06 追加 `; fix: ...` + 提取 `rulecodes.go` | arch-opt | P1/Cx2 | ✅ | PR#487 (2026-05-13) | `kernel/governance/` + `tools/archtest/` | 030 §3 G-13 |
| G-13-FU-INV1-SAMESOURCE | **PR#487 follow-up: archtest 同源遍历入口 + INV-1/2/3 form completeness** — PR#487 review 发现 INV-1 把 scanner.EachFile (重 parse) 与 pkg.TypesInfo 混用 → info.Types[sel.X] 恒 miss → 受体类型校验静默 fail-open（非 *Validator 同名 validate* 方法可绕过）；INV-2/INV-3 ValidationResult{} 检查只看已存在字段 → 漏 Code: / 漏 Message: / 位置参数三种形态绕过。修复: 新增 `typeseval.EachFileInPackage(root, pkg, skipTestFiles, fn)` 单源遍历 helper（消除 scanner.EachFile + pkg.info 混用入口，对标 go/analysis Pass）+ INV-1 receiver fail-closed + INV-2/3 positional ban + Code/Message 完整性 + 5 RED fixtures（shadow_receiver_red / composite_lit_no_code_red / composite_lit_positional_red ×2 / composite_lit_no_message_red） | bug+arch-opt | P2/Cx2 | ✅ | PR#487 (2026-05-13) | `tools/archtest/internal/typeseval/eachfile.go` + `tools/archtest/governance_rules_invariants_test.go` + `tools/archtest/testdata/governance_*_fixtures/` | PR#487 review (2026-05-13) |
| G-13-FU-H2-VALIDATIONRESULT-SEALED | **INV-3 Soft → Hard: ValidationResult.Fix typed 字段或拆 sealed Result types** — 现状: INV-3 仍依赖字符串子串 "; fix:" 在 Message 内识别 SeverityError 修复指导（即便 INV-3-FU 已加位置参数 + 字段完整性，字符串锚点本质仍是 Soft）；修复: (a) 加 `ValidationResult.Fix string` 必填字段 + `newResult/newScopedResult/newResultAt` 在 `sev == SeverityError && fix == ""` 时 runtime fail-fast，把 INV-3 字符串扫描降级为辅助；或 (b) 拆 sealed `Result` interface（ErrorResult{Fix string} / WarningResult / AdvisoryResult），让 SeverityError 无 Fix 字段不可表达。预估影响 ~60 个 SeverityError 调用点 | refactor | P2/Cx3 | 🟡 | INV-3 字符串扫描出现误判或字符串锚点演化（i18n / template 化）| `kernel/governance/validate.go` + `kernel/governance/locator.go` + `kernel/governance/rules_*.go` + ADR | PR#487 review (2026-05-13) follow-up |
| G-13-FU-H3-RULES-AUTOREGISTER | **INV-1 Hard 路径: rules() 自动派生（reflect 或 codegen）** — 现状: INV-1-FU 修后 receiver type 校验真生效（Medium 形态），但"手工 slice + archtest 反射对照"范式天花板就是 Medium —— "忘记注册" 仍可能发生，archtest 只能事后 catch；修复: 二选一 (a) NewValidator 时 `reflect.MethodByName` 自动枚举所有签名匹配 `func() []ValidationResult` 的 *Validator 方法，删手工 rules()/strictRules()；(b) `//gocell:rule` directive + codegen 派生 `rules.gen.go`，开发者只写 validateXX 方法。前者副作用: 失去显式注册可读性 + 顺序不可控；后者副作用: 引入 codegen pipeline 依赖。需要 ADR 取舍 | arch-opt | P3/Cx3 | 🟠 | INV-1 类型回归 / 大规模新增 rule 时手工 slice 维护成本爆炸 | `kernel/governance/validate.go` + `kernel/governance/rules_*.go` + (codegen 路径还有 `tools/codegen/`) + ADR | PR#487 review (2026-05-13) follow-up |
| G-13-FU-H1-REVERSE-SELFCHECK-REJECTED | **archtest 反向自检方案评估（不立 / 已被 040 阶段 1 结构性消除）** — PR#487 review 期间曾考虑加 archtest `TYPESINFO-AST-SAME-SOURCE-01`：grep `scanner.EachFile` 调用点 + 邻近代码扫 `pkg.info`/`pkg.TypesInfo` 引用，强制两者不混用。architect 决策（2026-05-13）: **不立**——反向自检本质是字符串锚点 + 邻近识别（AI 把 info 重命名 / 抽到包级变量 / 放独立 helper 任一即绕过）→ Soft 形态守 Soft 形态，违反 ai-collab.md "既有 Soft 的补丁优先升级到 Hard/Medium，而非在 Soft 层打补丁"。已采用方案 (2026-05-13): 在 `typeseval` 加 `EachFileInPackage` 单源遍历入口（Medium）。**2026-05-14 更新**: plan 040 阶段 1 ✅ shipped (PR #492) 把局部收口升级到三重 Hard 防线 — (1) `Pass.Pkg *types.Package` 编译期 `.Syntax` 不可达；(2) depguard ban archtest `*_test.go` 直接 import `golang.org/x/tools/go/packages`；(3) 元治理 archtest PASS-FUNNEL type-aware 拦截 scanner.EachFile / typeseval.LoadPackages / typeseval.SharedResolver 旧入口；旧触发条件 (a) "出现 *新* archtest 同时 import scanner.EachFile + pkg.TypesInfo" 结构性消除——新 archtest 必走 `archtest.Run` / `archtest.RunTyped` 入口，写不出双视图捕获。本条入档案 — 不需要再 reopen | arch-opt | P4/Cx0 | ✅ | — | `tools/archtest/{pass,walk,scope,content,module_root}.go` + `tools/archtest/pass_funnel_test.go` + `tools/archtest/internal/archtestmeta/legacy_allowlist.go` + `.golangci.yml` | PR#487 review (2026-05-13) decision → PR #492 (2026-05-14) 结构性消除 |
| G-15 | **KERNEL-METADATA-CODEGEN-OVERLAY** — kernel/metadata 既是被动数据又承载 `goStructName` 等 codegen-only 字段；修复: 二选一 (a) 把 codegen-only 字段挪到 `tools/codegen` schema overlay；(b) `metadata/doc.go` 注明"YAML schema 总账本"故意承载多消费方所需字段 | refactor | P2/Cx2 | 🟡 | — | `kernel/metadata/` + `tools/codegen/` | 030 §3 G-15 |
| PR432-FU-AUTH-COMBO-ARCHTEST-DOUBLE-DEFENSE-01 | **auth combo archtest 双重防线评估** — 现状: PR-C(#432) 实施 single oracle (`metadata.AuthComboLegal`) + 5 层 mirror（type system / reflect 字段数 / governance 委托 / schema if-then mirror / 双侧 32-combo matrix），reviewer 提议是否再立 `tools/archtest/auth_combo_invariants_test.go` 做安全语义 archtest 双重防线；architect 决策（2026-05-10）: **不立**——现有 5 层防线已 exhaustive，archtest scanner 边际收益接近 0（所有静态守护都已被现有动态测试 + reflect 断言覆盖）；CLAUDE.md 双重防线"推荐而非强制"，prior art (`MESSAGE-CONST-LITERAL-01`/`DETAILS-SLOG-ATTR-01`) 是因 type system 拦截力弱才必要，本 case 不同。修复: 仅在触发条件出现时立 follow-up | arch-opt | P3/Cx2 | 🟠 | (a) `governance.hasFMT27AuthModeConflict` 被重新 inline 化绕过 oracle / (b) schema.json if-then 与 `AuthComboLegal` 漂移事故首现 / (c) auth bool 字段数突破 6 个使 32-combo matrix 测试时间不可接受 | `kernel/metadata/auth_combo.go` + `kernel/governance/rules_fmt.go` + `tools/archtest/`（新文件，仅触发后）| PR#432 review finding #1（2026-05-10） |

## 02.4 杂项

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| CELLS-SLICE-MULTI-VERB-DECOMPOSE-01 | **Slice 多 verb 拆分** — 现状: auditappend 14 contractUsages、configread 双 listener；修复: 拆 `auditappend-{session,user,config,role}` 共享 dispatch + `configread-internal` 单独，不留兼容包装 | refactor | P1/Cx3 | 🟡 | — | `cells/auditcore/slices/auditappend/` + `cells/configcore/slices/configread/` | systems layer review + 030 §2 K-07 |
| B2-K-05 | **Metadata parser error 路径泄漏** — 现状: parse error 含 fs 内部路径，低强度信息泄露；修复: error 双通道 (public 仅 cell/slice ID + 字段路径，internal slog 保留 fs path) | bug | P2/Cx2 | 🟡 | — | `kernel/metadata/parser.go:190,202` | backlog2 §2 B2-K-05 |
| B2-K-07 | **Contracttest undeclared ref no-op** — 现状: `MustValidateRequest("not-declared", ...)` 静默 return，key 写错时假通过；修复: 未声明 key 改 `t.Fatalf` | bug | P1/Cx1 | 🟡 | — | `pkg/contracttest/contracttest.go:170,189` | backlog2 §2 B2-K-07 |
| B2-T-07-FU-3 | **K04-CELLGEN-CONTRACTSPEC-CLIENTS** — 现状: cellgen 不派生 contract.clients；修复: 加派生（A5 follow-up） | arch-opt | Cx2 | 🟡 | cellgen 升级窗口 | `tools/codegen/cellgen/` | backlog2 §8 A5 follow-up |
| J-03 | **CONTRACT-V1V2-DRY-RUN** — api-versioning.md 写 v2 规则但 0 实例、0 deprecation 模板、无 v1/v2 共存示例；修复: 选 contract（如 audit list）走一遍 v1→v2 演练（目录 + ContractMeta.id + ownerCell 双挂 + lifecycle + outbox triggers + journey checkRef）+ ADR；或写 ADR 明确"1.0 之前不做 v2 升级"删 v2 段落（与 029 F4 同根，可合并） | feat | P1/Cx2 | 🟡 | v1.1 启动 | `contracts/` + `.claude/rules/gocell/api-versioning.md` + ADR | 030 §3 J-03 |
| F-09 | **CONSTRAINTS-PARAMETRIC-FIELD** — cell.yaml 无 `constraints` 字段；SLO/性能/容量约束写在 PR 描述而非模型；修复: 加 `constraints: { latency: {p99_ms, p999_ms}, throughput: {publish_per_second}, capacity: {queue_depth_max} }` + verify 钩子跑 micro-benchmark 校验 | feat | P3/Cx3 | 🟡 | F-06 落地后 | `kernel/metadata/types.go` + `kernel/verify/` + cell.yaml schema | 030 §3 F-09 |

