# GoCell Backlog — cap-14: 代码生成与治理工具链

> 主要包：`tools/{archtest,codegen,depgraph,e2egate,metricschema,generatedverify}` + `cmd/gocell` 8 子命令 + `kernel/governance` 规则
> 父表：[`docs/backlog.md`](../backlog.md)（schema / cap 主轴）
> 归档：[`docs/backlog/archive/`](archive/)

---

## 14.1 archtest / typed funnel / scanner

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| K05-ARCHTEST-PACKAGES-LOAD-UPGRADE | **K05-ARCHTEST-PACKAGES-LOAD-UPGRADE** — 现状: archtest AST 仅按 `reg` 字面 receiver 匹配，rename 可绕过；修复: 升 packages.Load + 按 cell.Registry 类型判断 | arch-opt | Cx3 | 🟠 | K#06 contractgen 类型分析 | `tools/archtest/codegen_unified_test.go` | K#05 PR #365 review K05-07 |
| PR411-HANDLER-POLICY-TYPEAWARE-SCANNER-01 | **HANDLER-POLICY-REQUIRED-01 type-aware scanner** — 现状: PR#411 后残留 scanner 只按 AST 字面匹配 `<pkg>.NewHandler(..., nil)`，可能误报非 generated handler，也无法用 import/type 信息锁定目标；修复: 升级为 `go/packages` / type-aware 分析，解析 selector 目标到 `generated/contracts/http/...` handler package，并把 parse/load error 与真实命中分开断言 | arch-opt+test | P2/Cx3 | 🟡 | HANDLER-POLICY scanner 出现误报/漏报，或 PR#411 follow-up batch | `tools/archtest/handler_policy_required_test.go` | PR#411 review |
| SERVICEOWNED-HANDLER-OWNER-CHECK-01-HARD-UPGRADE | **SERVICEOWNED-HANDLER-OWNER-CHECK-01 Medium → Hard 升级（触发型）** — 现状: `SERVICEOWNED-HANDLER-OWNER-CHECK-01` archtest 为 Medium（contract-decl ↔ service-guard AST ↔ `errcode.KindNotFound` type-resolved 三因子跨函数绑定；跨函数 helper 封装形态存在理论逃逸空间）。触发升级 Hard 的条件：(a) serviceOwned endpoint ≥ 3 且 owner-guard 形态收敛同一形状；或 (b) 出现一次 owner-guard 被 helper 跨函数封装致 AST 结构匹配逃逸的真实事件。升级路线：owner 比对收敛 `auth.OwnerGuard[T]` typed funnel（service 层调用，返回固定 KindNotFound envelope），archtest 改扫 funnel 唯一出口（Hard = 违反不可表达）。**不提前**：endpoint = 1 时 funnel 抽象无法从单实例正确 factor，over-engineering；endpoint ≥ 3 且形态稳定后一次到位。本条目被 `tools/archtest/serviceowned_handler_owner_check_test.go` 文件头 godoc 点名。| arch-opt | P2/Cx3 | 🟠 | (a) serviceOwned endpoint ≥ 3 且 guard 形态收敛 / (b) helper 封装逃逸真实事件首现 | `tools/archtest/serviceowned_handler_owner_check_test.go` + `runtime/auth/` (OwnerGuard typed funnel 新) | PR-A3 batch-3 follow-up |
| IDUTIL-UUID-RAND-FAILURE-TEST-01 | **UUID rand failure test** — 现状: rand.Read 失败路径无回归；修复: fault injection test | test | Cx1 | 🟡 | — | `pkg/idutil/` | GitHub #23 |
| B2-K-08 | **Assembly race test 认知复杂度超限** — 现状: `TestAssembly_StartConcurrentSnapshots_RaceDetector` SonarCloud `brain-overload` 32/15；修复: 拆 setupRaceFixture/spawnReaders/awaitReady 三 helper（保持 race window 确定性） | refactor | P2/Cx2 | 🟡 | — | `kernel/assembly/snapshots_race_test.go:36-120` | backlog2 §2 B2-K-08 |
| FINDFIRSTINSUBTREE-API-01 | **FindFirstInSubtree typed API（subtree 版 find-first）** — 现状: `FindFirstChild`（PR-A1 / 037 §1.1）只覆盖 depth-1；`EachInSubtree` + 早返 sentinel 形态（递归 find-first）仍散落约 50+ 站点（many 实为子树存在性断言，部分为真 subtree find-first），SCANNER-FRAMEWORK-USAGE-02 allowlist=0 仅锁 EachInChildren 形态；修复: 立 `scanner.FindFirstInSubtree[N]` typed API + 扩 USAGE-02 覆盖 EachInSubtree 早返形态（subtree find-first 子集），子树存在性断言可另立 `Exists/Any` helper。**正交轴**：不是 FindFirstChild funnel 的 upstream Hard 化路径（upstream Medium 已是 Go 天花板，见 USAGE-02 godoc） | arch-opt | P2/Cx2 | 🟡 | 第 N 处 EachInSubtree 早返 sentinel 触发（独立触发型，037 §1.1 范围外） | `tools/archtest/internal/scanner/` + `tools/archtest/scanner_framework_usage_test.go` | PR-A1 split (037 §1.1) |
| PASS-PRODUCTION-UPSTREAM-HARD-01 | **RunTypedProduction upstream funnel Hard 升级** — 现状: `RunTypedProduction` 下游 Hard（`PASS-FUNNEL-LOADPACKAGES-01` type-aware 拦截 `typeseval.LoadProductionPackages` 直调，caller 必须改 allowlist），但上游 Medium：rule 作者仍可选择 `RunTyped("./...")` + 手动 `pass.IsGenerated` 跳过代替 `RunTypedProduction`，软逃逸面存在（ai-collab.md "Soft 上游 + Hard 下游不算闭环 funnel" 原则）。修复方向: 上游 Hard 化——候选方案 (a) 新增 archtest meta-rule `PASS-PRODUCTION-UPSTREAM-01`，type-aware 检测 production-only scope 的规则（`TypedOpts{Tests: false}` 组合调用点）必须经过 `RunTypedProduction` 而非 `RunTyped`；(b) sealed 选项 type——`ProductionOpts` 独立 type，`RunTypedProduction` 唯一接受者，`RunTyped` 拒绝 `ProductionOpts`（需 API break）。本条目在 `RunTypedProduction` godoc 中点名引用。 | arch-opt | P2/Cx3 | 🟠 | 第 2 次 production-only rule 作者遗漏 `RunTypedProduction` 改用 `RunTyped+IsGenerated` 被 review 抓到 | `tools/archtest/pass.go` + `tools/archtest/pass_funnel_test.go` | plan 040 Stage 1.7 (PR #507) AI-rebust 设计决策 D2 |
| EXAMPLES-DEMO-CELLGEN-UNIFY-01 | **examples/demo cellgen 流程统一** — 现状: `examples/demo/cells/demo/` 有 `cell.yaml` 但无 `cell_gen.go`（其他 cell 都有 codegen 派生产物），是个历史漏洞。`CELL-MUSTNEWBASECELL-FUNNEL-01` archtest 扫不到 demo cell 的 cellMeta literal，构造路径未受同级守护。修复方向: 跑 `gocell generate cell` 派生 cell_gen.go 接入 codegen funnel；或在 archtest scope 显式排除 + 注释说明 demo cell 仅为占位。 | arch-opt | P3/Cx3 | 🟢 | 下次 cellgen 治理收紧 / demo cell 加 slice / subscription | `examples/demo/cells/demo/cell.yaml` + cellgen scaffolder | PR-CFG-L2-DIVERGENCE review (架构 #4) |
| BASECELL-DURABILITYMODE-RUNTIME-ALIGNMENT-DEFERRED | **BaseCell.Init 期对账 cell.yaml.durabilityMode 与 runtime DurabilityMode（推迟实施）** — 现状: PR-CFG-L2-DIVERGENCE 原本要把"cell.yaml 声明的 durabilityMode" 与"assembly DurabilityMode" 在 BaseCell.Init 期强制对账（mismatch → fail-fast）。落地过程中发现破坏面失控: platform cells 全部声明 `durable`，但仓库内 cells/* unit test fixture / examples / tests/integration 大量用 `DurabilityDemo` runtime + noop deps（轻量测试），逐处 migrate 需要几十~上百处改动。PR-A 撤回该对账机制，回到 advisory 层治理（OUTGUARD-01 governance L2+ 必填、L0/L1 可省略 + mode_resolver Warn）。**重新启用前置条件**: (a) 仓库内 cells/* 全部测试 fixture 迁到与 cell.yaml.durabilityMode 一致的 runtime（DurabilityDurable + 真 PG fixture，或拆 demo cell variant 用 DurabilityDemo）；(b) 评估"cell.yaml 是否应该硬编码 runtime mode"——业界（K8s/fx/Watermill/Kratos）都不在元数据声明 runtime mode；可能更好的设计是 cell.yaml 声明 capability、runtime 决策 mode。(c) **机制与测试迁移是连体的**：重启对账时必须同期重做测试迁移——平台 cell 的 demo 单测改为假 demo cell 或真实 PG integration（即 #556 拆分时被刻意丢弃的 "C"：corebundle/kernel.assembly 夹具补 `DurabilityMode` + `buildBootstrapFromShared` 改 modules 注入 + `TestBuildBootstrap_AssemblyHasAllCells` 重定位到 integration tag）。该批改动因机制撤回已成孤立 churn 且会把快速单测推进 PG/testcontainers 导致 integration-test 红灯，故未随 PR-A 进主干；缺这步即 CI 红，禁止单独"抢救"这批改动。AI-rebust: 重新启用时按 §"载体决策原则"重选 Hard 形态。对标反例: 当前设计违反 GoCell 自身"cell.yaml 是声明、runtime 是决策"分层。 **关联 Medium 防御（PR 014, 2026-05-18）**：`CELL-L2-INIT-CHECKNOTNOOP-CALLED-01` archtest（`tools/archtest/cell_init_checknotnoop_test.go`）已守 L2+ cell Init 必调 `cell.CheckNotNoop`；重启 runtime alignment 时与此 archtest 共同构成 declared-vs-runtime 对账双层防线（archtest 静态层 + Init 运行时层）。**升级到 Hard 候选**：(1) codegen funnel——cellgen Init template 对 `ConsistencyLevel>=L2` 自动 emit `cell.CheckNotNoop(...)` 调用 + 删 hand-written `initInternal` 中重复调用；(2) `CheckNotNoop` nil-reject 语义变更（DurabilityDurable + nil dep → reject）+ 修复 cells/* 测试 fixture 破坏面（即本条目本体）；(3) 删除 `GOCELL_CONFIGCORE_STORAGE_BACKEND=memory` 模式。三者均与本条目耦合，升级需同期决策。详见 PR #576 body §Tradeoff Accepted 段及独立 backlog 条目 `CELL-L2-CHECKNOTNOOP-CODEGEN-HARD-UPGRADE-01` / `ARCHTEST-WALKER-BOUNDARY-CONTROL-01`。 | arch-opt | P3/Cx3 | 🟢 | cells/* 测试 fixture 完全收敛到与 cell.yaml.durabilityMode 一致 / GoCell 重新定义 cell.yaml 元数据范围（不再硬编码 runtime mode）| `kernel/cell/base.go` + `kernel/cell/durability.go` + 全仓 cells/*_test.go + tests/integration | PR-CFG-L2-DIVERGENCE 二轮 review (P1.1/P1.2/P1.3) → 方案 D 决策 + PR #556 拆分 review + PR 014 (CELL-L2-INIT-CHECKNOTNOOP-CALLED-01) |
| FIXTURE-DIAGNOSTIC-GOLDEN-MIGRATION | **fixture 诊断断言 golden-file 迁移（scope A/B/C 全部完成 ✅）** — 决策见 ADR `docs/architecture/202605181200-adr-archtest-fixture-diagnostic-golden.md`：fixture 诊断断言从硬编码 `wantLines []int{N}` Soft 反模式改为 golden-file 单源（`AssertGolden` + `-update` 重生 + `scanner.Canonical` 与 `Report` 共用排序去重），golden 绑 **位置 + 消息**，消解行漂移。**已落地**：scope A `#562`（`golden.go`/`golden_test.go` GOLDEN-HELPER-UNIT-01 + panic/eval 2 范例）；scope B `#563`（prod_clock_injection/prod_duration/test_time_literal/exported_error_new）`#564`（implements_funnel + notfound，notfound 同时恢复弱断言退化的精确断言）；scope C `#565`（errcode_invariants 多段 + `assert.NotEmpty/Empty` 弱断言移除）`#566`（clock_invariants/errcode_message_const/span_record_error 的 `wantViolCount int` 计数盲区消解 + review 闭环删迁移遗留死代码）。全部合并 develop @ acbe58a，`#557` 已 superseded 关闭。**残留开放子项（仅 Medium→Hard）**：可选补 meta-archtest 锁 "`*.golden` 变更必伴随规则/fixture 同 commit 变更"（regenerate-only 纪律 Soft → Hard，形态 stub 见 ADR §meta-archtest 形态 stub）；本条目在 `tools/archtest/golden.go` godoc 中点名引用。 | arch-opt | P3/Cx3 | 🟢 | regenerate-only Soft 被绕过首例 / golden 文件数足够多值得机器检测 | `tools/archtest/golden.go` + ADR 202605181200 §meta-archtest 形态 stub | PR #557 三-agent review + ADR 202605181200（scope A/B/C `#562`/`#563`/`#564`/`#565`/`#566` 全部 ship） |
| CELL-L2-CHECKNOTNOOP-CODEGEN-HARD-UPGRADE-01 | **CELL-L2-INIT-CHECKNOTNOOP-CALLED-01 Hard 化路径：codegen funnel** — 现状: 当前 archtest 为 Medium（`*types.Info` 跨包 callee 解析 + cell.yaml 元数据 cross-validate），可被 AI 通过新增同包 helper 函数或重命名 receiver 绕过（虽然 samePkgOnly BFS 收紧了部分逃生路径）。Hard 化路径：让 `tools/codegen/cellgen` 的 Init template 在 `cellMeta.ConsistencyLevel >= "L2"` 时自动 emit `cell.CheckNotNoop(durabilityMode, cellID, <sealed deps>...)` 调用，同时删除 cells/*/cell_init.go 中 hand-written `initInternal` 中的 CheckNotNoop 重复调用。漏调 = codegen 不 emit = Hard 不可表达（codegen funnel 范本）。**前置条件**: 需评估 codegen template 内"all sealed marker fields as deps"的扫描精度（不同 cell sealed 字段不同），以及与 BASECELL-DURABILITYMODE-RUNTIME-ALIGNMENT-DEFERRED 的 cells/* 测试 fixture 破坏面是否一并清理。 | arch-opt | P3/Cx3 | 🟢 | BASECELL-DURABILITYMODE-RUNTIME-ALIGNMENT-DEFERRED 破坏面收敛 / 新增 L2+ cell 触发重新评估 | `tools/codegen/cellgen/` + `cells/*/cell_init.go` 删除手写 CheckNotNoop | PR 014 review (B/C 维度 Cx2 finding) |
| ARCHTEST-WALKER-BOUNDARY-CONTROL-01 | **archtest walker boundary control API** — 现状: `tools/archtest/walk.go` 的 `EachInSubtree[T]` 是无条件深度遍历，不提供 boundary 控制（对标 `golang.org/x/tools/go/ast/inspector.Nodes(types, func(n, push) proceed bool)` 的 `proceed=false` 跳子树语义）。PR #576 在 `cell_init_checknotnoop_test.go` 的 `initReachesCheckNotNoop` / `argSubtreeReferencesCheckNotNoop` 内用 "pre-collect FuncLit ranges + post-filter CallExprs/Idents in range" 模式等价实现 FuncLit boundary（详见 godoc + collectFuncLitRanges / posInsideAnyRange helper），但实现更间接，且要求每个 boundary 类型重复一遍 ranges 收集。**升级路径**: 在 walk.go 加 `EachInSubtreeStopAt[S, Stop interface{*S2; ast.Node}, N interface{*S; ast.Node}](root, fn)` 或同形态 helper，把 boundary 控制内建到 walker API，对齐 inspector.Nodes 的 push/proceed 语义。**触发条件**: archtest 内出现 ≥ 2 处 FuncLit / nested-scope boundary 需求（当前仅 1 处，cell_init_checknotnoop_test.go 两个 callsite 共用同一 helper）。 | arch-opt | P3/Cx2 | 🟢 | 第 2 次 archtest 出现 boundary 需求 / 现 helper 拆出独立 walker 包 | `tools/archtest/walk.go` + `tools/archtest/internal/scanner/walk.go` | PR #576 round-2 review F1 finding + `golang.org/x/tools/go/ast/inspector.Nodes` 对标 |
| ARCHTEST-FIXTURE-NAMING-CONVENTION-DRIFT-01 | **archtest fixture 子目录命名风格漂移** — 现状: PR 014 引入 `red_l2_missing` / `red_cross_pkg` / `boundary_l1` / `boundary_transitive` fixture 命名，与既有 `serviceowned_handler_owner_check/{green,red_missing_guard,red_wrong_kind}` 等 `{state}_{descriptor}` 风格不完全一致（boundary_ 前缀是新增分类）。功能正确性不受影响，但跨 fixture 集合阅读体验存在轻微不一致。**统一方向**: 选定一种命名约定（green/red/boundary 三态，或 green/red 二态）+ archtest meta rule 守 testdata/ 子目录命名 prefix。 | arch-opt | P3/Cx2 | 🟢 | fixture 集合数量 ≥ 30 / reviewer 反复指出命名风格不一 | `tools/archtest/testdata/` + 命名 archtest meta rule（如增） | PR 014 review (C 维度 P2/Cx1 finding) |
| SPAN-ARCHTEST-ANCHOR-ORPHAN-01 | **`SPAN-RECORD-ERROR-REDACT-ARCHTEST-01` 孤儿 INVARIANT anchor** — `tools/archtest/span_record_error_redact_test.go` 文件头 CommentGroup 声明 `INVARIANT: SPAN-RECORD-ERROR-REDACT-ARCHTEST-01`，但无任何 Test 以该 ID 调 `Report`（实际为 `SPAN-RECORD-ERROR-REDACT-01` 与 `SPAN-RECORD-ERROR-REDACT-01-COVERAGE`）。`INVENTORY-ANCHOR-REQUIRED-01` 只校验 header 存在有效 ID，不验证 Test 实际报告该 ID（Soft 锚点-实现脱节）。**既存非本次引入**，golden 迁移高强度审查暴露。修复方向：删该 anchor（若 `*-01-COVERAGE` 已覆盖意图）或补 `TestSpanRecordErrorRedactedArchtest` 报告该 ID；根治需 anchor↔Report 对应的机器校验（Soft→Medium）。 | arch-opt | P2/Cx2 | 🟠 | 下次接触 span_record_error_redact_test.go / anchor↔Report 对应校验立项 | `tools/archtest/span_record_error_redact_test.go` + `INVENTORY-ANCHOR-REQUIRED-01` | PR #566 三-reviewer（ababfa）OUT_OF_SCOPE finding |
| INVARIANT-HEADER-CASING-DRIFT-01 | **`// invariants:` 小写列表式 vs ai-collab.md 规定 `// INVARIANT: <ID>`** — ai-collab.md §archtest 文件命名 文本写单行 `// INVARIANT: <ID>`，但仓库内约 30 个 `*_test.go` 已用 `// invariants:` + `//   - INVARIANT: <ID>` 列表续行式（`INVENTORY-ANCHOR-REQUIRED-01` 的 `hasValidInventoryAnchorInHeader` 实际两种都过，故无 CI 失败、静默漂移）。文档与实践脱节，是 Soft 注释约定复利的典型。修复方向：更新 ai-collab.md 显式认可列表式（多规则文件头），或统一收敛到单行式（涉 ~30 文件 sweep）；二选一，关键是 doc 与实践一致。 | arch-opt | P3/Cx2 | 🟠 | ai-collab.md §archtest 文件命名 下次修订 / 第 31 个文件再引入第三种格式 | `.claude/rules/gocell/ai-collab.md` + ~30 `tools/archtest/*_test.go` 文件头 | PR #566 三-reviewer（ababfa）OUT_OF_SCOPE finding |
| WALK-DEPTH-API-EACHCHILDREN-01 | **`collectRecordErrorImplRanges` 用 `EachInSubtree[ast.FuncDecl]` 应为 `EachInChildren`** — 顶层 FuncDecl 是 `*ast.File` 直接子节点（depth=1），按 ai-collab.md §"typed function choice for walk depth" 应用 `EachInChildren`（深度选择语义自检）。当前结果正确（Go 无嵌套方法声明故无额外节点），但违反 API 语义约定，损害"选错语义=选错 API 名"的可检测性。**既存非本次引入**。修复方向：改 `EachInChildren[ast.FuncDecl]` + godoc 说明 depth=1 充分（与 `clockControlPlaneAllowedFuncs` 写法一致）；可顺带扫全 archtest 同类误用。 | arch-opt | P3/Cx1 | 🟢 | 下次接触 span_record_error_redact_test.go / archtest walk-depth API 合规 sweep | `tools/archtest/span_record_error_redact_test.go` + 潜在同类点 | PR #566 三-reviewer（ababfa）OUT_OF_SCOPE finding |
| FIXTURE-SCANNER-REL-STRATEGY-UNIFY-01 | **fixture scanner Rel 计算策略不一致（span vs errcode）** — `runSpanRecordErrorFixtureDiags` 用 `filepath.Base(p.Abs(file))` 取 basename；`runErrcodeMessageConstFixtureScan` 用 `DirsScope(fixtureDir,..)` + `p.Rel(file)`（fixtureDir 相对）。单层 fixture 下二者侥幸一致，存在子包目录（如 stub `errcode/`）时 `p.Rel` 产生 `errcode/x.go` 前缀而 filter 依赖 `filepath.Base(filepath.Dir(abs))`，Rel 计算与 filter 路径脱节。**既存非本次引入**。修复方向：统一两 scanner 的 Rel 策略（或都 basename、或都 fixture-relative module path）并加 test 断言锁 Rel 格式。 | arch-opt | P3/Cx2 | 🟠 | 出现含子包目录的 fixture / golden Rel 格式回归首例 | `tools/archtest/span_record_error_redact_test.go` + `tools/archtest/errcode_message_const_fixtures_test.go` | PR #566 三-reviewer（ababfa）+ PR #566 R1 OUT_OF_SCOPE finding |
| ARCHTEST-FUNNEL-CALLSITE-LEVEL-01 | **domain-authz funnel allowlist file→callsite Hard 升级（触发型）** — 现状: domain-authz funnel allowlist 已从 package → file（`tools/archtest/domain_authz_mutation_funnel_invariants_test.go`），但仍是文件级；同文件未来新增 live direct setter 仍可漏过。修复方向: callsite-level（用 `typeseval.ResolveCallSite` 按调用点身份而非文件归属判定），把上游升到 Hard，与 `DOMAIN-AUTHZ-FIELD-PRIVATE-01` 下游 Hard 形成闭环 funnel | arch-opt | P2/Cx3 | 🟠 | 第 N 次同文件内出现新 live setter 漏过 / domain-authz funnel 主题 PR | `tools/archtest/domain_authz_mutation_funnel_invariants_test.go` + 可能 `tools/archtest/internal/typeseval/` | 计划 034 §"Backlog only（U-8）" + S4-FU PR #501 review |
| GOLANGCI-GOPACKAGES-EXEMPTION-CLEANUP-01 | **.golangci.yml archtest import go/packages 例外清理（触发型）** — 现状: `.golangci.yml` 对 archtest 直接 import `go/packages` 的 depguard 例外，治理债未清完（typeseval 尚未完全收口 go/packages 调用）。修复方向: typeseval 完全收口 `go/packages` 后删除 `.golangci.yml` 该例外，由 typed façade 单源 | arch-opt | P3/Cx2 | 🟠 | archtest typed 化批 / typeseval 完全收口 go/packages 调用 | `.golangci.yml` + `tools/archtest/internal/typeseval/` | 计划 034 §"Backlog only（U-9）" + S4-FU PR #501 review |
| PASS-FUNNEL-NONTESTGO-EXEMPT-UPGRADE-01 | **PASS-FUNNEL-LOADPACKAGES-01 non-test .go scope upgrade** — 现状: PASS-FUNNEL-LOADPACKAGES-01 scope `HasSuffix("_test.go")` 过滤掉 `tools/archtest/warm.go` 等 non-test .go 文件，warm.go 直调 `typeseval.LoadProductionPackages` 不受 archtest 拦截。这是 ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01 落地的 designed exempted bridge（避免 testmain_test.go 直 import typeseval 撞 funnel）。该旁路是 PASS-FUNNEL "Half-Hard"（下游 Hard / 上游 non-test .go 路径 Soft）。修复方向: (a) 扩 scope 到 non-test .go + warm.go 加 allowlist; (b) seal warm.go 至 typed wrapper API（如新增 `archtest.WarmProductionPackagesAPI` 显式 marker）；任一路径都需保留 warm.go 唯一桥接职能。**触发**: 第 2 个 non-test .go 文件在 archtest 包内直调 typeseval loader 符号 / 或 archtest 治理收紧。 | arch-opt | P2/Cx2 | 🟠 | 第 2 处 non-test .go 直调 typeseval loader / archtest funnel 治理升级 batch | `tools/archtest/warm.go` + `tools/archtest/pass_funnel_test.go` | PR #584 review F1 + ADR 202605190000 §威胁矩阵 |
| TAGGROUP-LOOP-HELPER-WRAP-UPGRADE-01 | **TAGGROUP-LOOP-FORBIDS-RUNTYPED-01 BS-5 helper-wrap 升级（触发型）** — 现状: 规则 `bodyContainsRunTyped` 仅解析 loop body 内**直接** `archtest.RunTyped` CallExpr；body 调用一个包内 local helper 而该 helper 自身调 RunTyped 的形态（BS-5）不被捕获——不做 intra-package call-graph 可达性分析。理由: (a) 关闭需一层(或递归) call-graph 分析，对一个"防复抄单一历史范本"规则不成比例（fresh Claude 复抄的是 *直接* RunTyped-in-body 形态）；(b) 精确反向自检不可行——`func foo(){ RunTyped(...) }` local helper 是 archtest suite 普遍合法 idiom，任何 blanket 断言会 false-positive（同 staticcheck SA4000 对 fn()==fn() 副作用盲区的 accepted trade-off）。BS-4 var-indirection 已在 PR #584 review 闭合（red_var_bound_range / green_var_bound_parity fixture）。升级方向: bodyContainsRunTyped 扩为一层 intra-package call-graph（loop body → local FuncDecl → RunTyped）+ 补 red/green fixture；保持 (callee, body) form-uniqueness Hard 形态。**触发**: 首次出现 helper-wrapped tagGroup loop 到达 CI / 或 archtest funnel 治理收紧 batch。本条目被 `tools/archtest/taggroup_loop_no_runtyped_test.go` 文件头 godoc BS-5 点名。 | arch-opt | P3/Cx3 | 🟠 | helper-wrapped tagGroup loop 首现 CI / archtest funnel 治理升级 batch | `tools/archtest/taggroup_loop_no_runtyped_test.go` + `tools/archtest/internal/taggrouploopfixtures/` | PR #584 review F1 (BS-5 accepted blind spot) |
| ARCHTEST-SLOWGATE-ALLOWLIST-CLEANUP-01 | **slowgate allowlist 缩减（基于 PR #584 TestMain 预热实测）** — 现状: `tools/slowgate/allowlist.txt` 包含 TestPanicRegistered / TestUserRepoConformanceEnrollment / TestCellRepoReadyzProbe / TestCellgenErrcodeFunnelNoBuildTagFiles 等 cache-miss-related borderline test 条目（PR #584 TestMain 预热 LoadProductionPackages 主路径后预期可缩减）。**注意分类**: subprocess-overhead 类（如 TestArchtestVerifyCoverage01 跑 5×`go test -list` 子进程）不在缩减范围 — 子进程 cache 与 TestMain 预热无关，需独立优化（见 `ARCHTEST-VERIFY-COVERAGE-DISPATCH-OPTIMIZE-01`）。**触发**: PR #584 合并后 CI 16-shard 矩阵连续 2-3 次稳定运行，对照每条 cache-miss-related borderline test 实测 wall < 20s。修复: 从 allowlist 移除已不跨 budget 的 cache-miss 类条目，wave 增量缩减。 | refactor | P3/Cx2 | 🟢 | PR #584 合并后 CI 矩阵连续 2-3 次稳定 + 实测 wall < 20s | `tools/slowgate/allowlist.txt` | PR #584 review F13 + ADR 202605190000 §slowgate allowlist follow-up |
| ARCHTEST-VERIFY-COVERAGE-DISPATCH-OPTIMIZE-01 | **TestArchtestVerifyCoverage01 子进程开销优化** — 现状: 该 test 跑 1 次 `verify-archtest.sh DRY_RUN` + K=4 次 `LIST_SHARD_TESTS` 子进程，每次子进程 `go test -list` 加载 archtest 包 ~5-6s on GHA → 累积 27.14s 跨 20s slowgate budget（PR #584 modulo 重排到 shard 12 暴露）。已加 slowgate allowlist 应急。修复方向: (a) K=4 → K=2（godoc 已说 K-independent），3 子进程 ≈ 16s 进 budget；(b) bash 脚本侧合并 DRY_RUN + LIST_SHARD_TESTS 为单次调用，去重 `go test -list`；(c) 用 `go list -test` 一次性拿全集。任一路径都需保留 modulo 分配验证强度。**触发**: ARCHTEST-SLOWGATE-ALLOWLIST-CLEANUP-01 启动或 TestArchtestVerifyCoverage01 wall 进一步上升 | refactor | P3/Cx3 | 🟢 | allowlist cleanup wave 启动 / wall 进一步上升 | `tools/archtest/archtest_verify_coverage_test.go` + `hack/verify-archtest.sh` | PR #584 CI shard 12 slowgate fail + ADR 202605190000 §slowgate allowlist follow-up |
| PG-REPO-AMBIENT-TX-UPSTREAM-HARD-01 | **PG-REPO-AMBIENT-TX-01 上游 Hard 升级（触发型）** — R3 callsite caller-allowlist 已落地（PR #578 review round-2）→ 上游 Medium 由 archtest 强制（非 doc-only）：R1+R2 守 holding+construction，R3 守 usage-point（`pool` 直接访问 + `ExecDirect` 非 allowlist 调用），唯一现存合法 bypass `revokeSessionDetachedAt` 静态 pin 在 `r3ExecDirectAllowlist`，任何新 bypass 立即 CI 红。**Hard 终态** = sealed Executor 接口 + 私有构造（`pgExecutor.pool` 包外不可达），仍为升级目标。现状: 上游 Medium（archtest-bound form-uniqueness，ai-collab.md §Funnel 双向锁评级）——intra-package compile Hard 不可达（同一 visibility scope），Go 编译器无法阻止 sibling file 访问。升级路径: (a) 将 `pgExecutor` 改为 exported interface（如 `type PGExecutor interface { Exec/QueryRow/Query/ExecDirect }`）且 struct 重命名为 unexported 实现体；(b) 将 `newPGExecutor` 构造路径私有化到单一工厂函数，包外无法直接 construct；(c) 包外 repo/store struct 只能持有 `PGExecutor` interface 字段，使"持有 *pgxpool.Pool"在包外成为 compile-time 不可表达——等级升至 Go type-system Hard，与 `SlogDependencyEntry` unexported fields 上游 Hard（`HEALTH-REDACTED-ERROR-MSG-FUNNEL-01`）同级。**不提前触发理由**：当前 adapters/postgres 包内无已知绕过实例；该升级引入 interface 边界，需要同时更新 storetest conformance suite 与 pgExecutor 调用端，工时 ~3h；触发条件一出现即立项。本条目在 `tools/archtest/pg_repo_ambient_tx_test.go` 文件头 godoc 与 ADR `docs/architecture/202605101200-adr-typed-go-heavy-protocol-primitives.md` §4.5.1 中点名引用。 | arch-opt | P2/Cx3 | 🟠 | (a) 出现新 *_repo.go/*_store.go 绕过 R1/R2/R3（sibling file 直接加字段或添加非 allowlist ExecDirect 调用被 archtest 触发时）；(b) adapters/postgres 包暴露面扩展（新增 cell adapter package 需要同等保护） | `adapters/postgres/pg_executor.go` + `cells/accesscore/internal/adapters/postgres/pg_executor.go` + `tools/archtest/pg_repo_ambient_tx_test.go` + ADR `202605101200` §4.5.1 | DX4 PR #578 review A3 finding — Funnel 双向锁评级；round-2 P2-1 R3 落地 |
| TEST-EVENTUALLY-FUNNEL-01 | **upstream funnel closure — ban bare `require.Eventually` / `assert.Eventually`** — 现状: `testwait.External` 是 test-side polling 的已批准入口，但上游未封闭：业务代码仍可直接调 `require.Eventually` / `assert.Eventually` 绕过 const-literal reason 要求。实现效果: Hard downstream（`TEST-POLLING-EXTERNAL-REASON-LITERAL-01` archtest 锁 callee+arg 形态）+ Soft upstream（无 ban），per ai-collab.md §Funnel 双向锁评级整体分类为 Soft transitional。目标: archtest 禁止 production 和 test 代码直接调用 `require.Eventually` / `assert.Eventually`，使"绕过 testwait.External"在 archtest 意义上不可表达，将 funnel 从 Soft transitional 晋升为 Hard。本条目被 `pkg/testutil/testwait/testwait.go` 包 godoc 与 `tools/archtest/test_polling_external_reason_literal_test.go` 文件头注释引用。 | arch-opt | P2/Cx3 | 🟠 | PR2（TEST-POLLING-DETERMINISM bulk migration）全部合并且 race CI 绿 24h | `tools/archtest/test_polling_external_reason_literal_test.go` + `pkg/testutil/testwait/testwait.go` | PR #594 review + ai-collab.md §Funnel 双向锁评级 + plan `docs/plans/202605181600-042-archtest.md` §1.1 PR3 |
| LASTADMIN-SENTINEL-MIGRATION-COUPLING-01 | **`lastAdminTriggerSentinel` 与 migration 024 trigger 消息耦合无机器守卫（触发型）** — 现状: `isLastAdminProtected` 以冒号分隔前缀匹配 `lastAdminTriggerSentinel = "effective_admin_invariant"` 识别 last-admin 保护异常；该字符串必须与 `024_effective_admin_invariant.sql` 触发函数 `RAISE EXCEPTION` 消息保持一致。P2-2/P2-3 (PR #578 round-2) 已将 sentinel 迁移到 accesscore-internal 包（`cells/accesscore/internal/adapters/postgres/lastadmin.go`），unexported；colon-delimited 匹配消除 sibling-prefix 误报（P2-3 fix），但 Go 常量 ↔ SQL migration 消息之间**无静态守卫**：未来 migration 重命名 trigger/消息时 classifier 静默 false-negative。修复方向: archtest `EachContentFile` 断言 sentinel 字符串恰出现在 1 个 migration 文件且与 `lastAdminTriggerSentinel` const 值一致（Go const ↔ SQL 双向锁）；或迁移期 governance 校验。 | arch-opt | P2/Cx2 | 🟠 | 下次新增/重命名 last-admin trigger 的 migration / 该 false-negative 首次实证 | `cells/accesscore/internal/adapters/postgres/lastadmin.go` + `adapters/postgres/migrations/024_effective_admin_invariant.sql` + 潜在 `tools/archtest/` | PR #578 DX4 L4 review 安全维度 S5 OUT_OF_SCOPE finding; P2-2/P2-3 round-2 fix 迁移到 cell-internal |
| BOOTSTRAP-AUDIT-OBSERVER-RUNTIME-AUTH-WINDOW-01 | **`BootstrapAuthFailObserver` runtime/auth 接口改造窗口（sealed marker + reason typed 一并）** — 共享改动面: `runtime/auth.BootstrapAuthFailObserver` type alias → sealed/typed 改造 + 8+ integration test 调用点 sweep。两件事必须同 PR 推（独立做任一件都触发同一批 test 改造）。**(a) funnel 上游升 Hard** — 现状: 上游 Medium 已推到形态可达上限（archtest `observerObjectsInFile` 按 `*types.Object` 注册 + `reassignViolations` 扫 `token.ASSIGN` 重赋值，覆盖同名 shadow / reassign 绕过；scope=`cmd/corebundle/` 非 _test.go）。**剩余 Medium 盲点**: wrapper chain 形态（`obs2 := wrap(obs1)` 其中 `wrap` 返回非 funnel 实现），archtest 无法跨 CallExpr dataflow 跟踪。瓶颈: type alias `type ... = func(ctx, string)` 无 sealed marker，type system 无法阻止 free-form func 字面量 / wrapper chain。升级: 把 type alias 改为 sealed marker（`type BootstrapAuthFailObserver struct { ... }` + unexported field + `runtime/audit` 唯一构造），外部包 composite literal 不可表达；提供测试用 `audit.NoopBootstrapAuthFailObserver()` constructor。**(b) reason typed funnel** — 现状: reason 三值（`missing_header`/`wrong_credentials`/`rate_limited`）在 `runtime/auth/bootstrap.go` 字面声明 + `runtime/audit.AppendBootstrapAuthFail` 内 whitelist map 二次校验，是 string-typed concept 的 Soft 形态（ai-collab.md §Hard 范本"string-typed concept funnel"）。升级: `type BootstrapAuthFailReason string` + 三常量 + observer 签名 `func(ctx, BootstrapAuthFailReason)`，未知 reason 编译期不可表达；whitelist map 同步删除。本条目在 `tools/archtest/bootstrap_audit_observer_funnel_test.go` + `runtime/audit/bootstrap_append.go` godoc 点名引用。 | arch-opt | P3/Cx3 | 🟠 | wrapper-chain 形态首次出现 / 第 4 个 reason 出现 / runtime/auth 接口重构主题 PR — **任一 trigger 命中时簇内 (a)+(b) 一起做** | `runtime/auth/bootstrap.go` (type alias → sealed marker + reason typed) + `runtime/audit/bootstrap_observer.go` + `runtime/audit/bootstrap_append.go` + 全仓 `auth.NewBootstrapMiddleware` 调用点（8+ test 文件）+ `tools/archtest/bootstrap_audit_observer_funnel_test.go` | PR #603 review 合并 BOOTSTRAP-AUDIT-OBSERVER-FUNNEL-HARD-UPGRADE-01 + BOOTSTRAP-AUTHFAIL-REASON-TYPED-FUNNEL-01；plan 039 W1-2 §D + §G；PR #603 二轮 review M2（types.Object + reassign 推满 Medium 上限） |
| BOOTSTRAP-AUDIT-OBSERVER-DOWNSTREAM-SSA-REACHABILITY-HARD-UPGRADE-01 | **`BOOTSTRAP-AUDIT-OBSERVER-FUNNEL-DOWNSTREAM-HARD-01` 升级到真 CFG 可达性（SSA-based）** — 现状: PR #603 三轮 review (PR #603 二轮 review F3 followup) 把 downstream Hard 从"返回 closure 子树包含调用"加固到三联词「typed callee + returned-closure scope + unconditional-position」——`collectClosureConditionalScopes` 把 IfStmt.Body/Else / ForStmt.Body / RangeStmt.Body / SwitchStmt.Body / SelectStmt.Body / 嵌套 FuncLit.Body 全部排除，`if false { AppendBootstrapAuthFail(...) }` / `for false { ... }` / 嵌套 closure 隐藏 三类历史绕过形态全部 fail。**剩余 AST-pattern 盲点**: (i) helper-function indirection — `return func(ctx, reason){ helperThatAppends(ctx) }` 且 `helperThatAppends` 在同包 FuncDecl 里调 AppendBootstrapAuthFail，当前 walker 不做 intra-package call-graph 可达性追踪（cell_init_checknotnoop_test.go 的 BFS pattern 是 precedent，可复用）；(ii) 真 CFG reachability — 闭包前面有无条件 `return` / `panic("...")` 把调用变成 dead-but-syntactically-top-level；(iii) multi-return-path 构造函数 — 当前只看第一个返回 FuncLit 的 ReturnStmt，多返回路径下其他 closure 不被检查。升级路径: SSA-based reachability，借 `golang.org/x/tools/go/ssa` BuildPackage + BasicBlock walk（对标 honnef.co/go/tools SA9003 empty-branch / dominikh/go-tools 同源 CFG 检测）；或更轻量 intra-package call-graph + 简单 unreachable detection（对标 `golang.org/x/tools/go/analysis/passes/unreachable`）。**不提前触发理由**: 引入 SSA 加载到 archtest 是较大改造（之前 cap-14 探讨过），当前形态已挡住所有 PR #603 二轮 review 提到的具体绕过形态 + 同形态 `BOOTSTRAP-AUDIT-OBSERVER-RUNTIME-AUTH-WINDOW-01` arm a sealed-marker 上游 Hard 化也部分补偿（包外不能写 wrapper closure）。本条目在 `tools/archtest/bootstrap_audit_observer_funnel_test.go` 文件头 godoc 与 `TestBootstrapAuditObserverFunnelDownstreamHard01` 函数 godoc 点名引用。 | arch-opt | P3/Cx3 | 🟢 | (a) helper-indirection 形态首现 production AST / (b) SSA-based archtest 升级主题 PR (覆盖该类规则 ≥ 2 处) / (c) `BOOTSTRAP-AUDIT-OBSERVER-RUNTIME-AUTH-WINDOW-01` arm a 落地（sealed marker 让上游绕过收敛，downstream SSA 升级 ROI 上升） | `tools/archtest/bootstrap_audit_observer_funnel_test.go` (downstream 测试体) + 可能 `tools/archtest/internal/typeseval/` (SSA loader 新) + 对标 `golang.org/x/tools/go/ssa` / `honnef.co/go/tools` SA9003 | PR #603 三轮 review F3 follow-up（用户明确指出 if-false dead-code 绕过 + 治理强度声明 inflate） |

## 14.2 codegen / scaffold / verify

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| SCAFFOLD-PROJECT-INIT-CMD | **SCAFFOLD-PROJECT-INIT-CMD** — 现状: `gocell scaffold cell/assembly` 假设项目已有 go.mod；缺 kubebuilder init / goctl new 等价的 "从零建项目"；修复: `gocell scaffold project --id=... --module=...` 产 go.mod + 顶层目录骨架 + 一个示例 cell + 一个示例 assembly | feat | P3/Cx3 | 🟢 | 首个外部使用者要求 / 教程化推广 | `cmd/gocell/app/scaffold_project.go` (新) | K#09 carveout |
| PR332-VERIFY-GENERATED-REMEDIATION-DRIFT-01 | **Verify codegen drift remediation 提示** — 现状: drift 报错不提示修复命令；修复: 补 hint | arch-opt | Cx2 | 🟡 | — | `cmd/gocell/` | PR#332 |
| VERIFY-CODEGEN-SANDBOX-INTEGRATION | **VERIFY-CODEGEN-SANDBOX-INTEGRATION** — 现状: --local=false sandbox 路径无端到端回归；修复: 补 1-2 条 git worktree integration test | test | Cx2 | 🟠 | 修改 verify-codegen-*.sh 或 runVerifyCodegen* | `cmd/gocell/app/codegen_*_drift_test.go` + tools/codegen helper | PR #404 K#10 review P2 |
| TEST-CHDIR-PARALLEL-CLI-01 | **TEST-CHDIR-PARALLEL-CLI-01** — 现状: 4 个 CLI test 用 os.Chdir 阻碍 t.Parallel()；修复: 抽 RootResolver helper | test | P3/Cx2 | 🟡 | CLI 测试 > 30s 或新 generate sub-cmd | `cmd/gocell/app/generate_*_test.go` + `verify_codegen_*_test.go` | PR #361 round-2 #3 |
| CLI-TOPLEVEL-HELP-REGISTRY-01 | **顶层 PrintUsage⟷commands 单源化** — 现状: CLI-UNIMPL-HIDE-01 把四棵子命令树做成 registry 单源，但顶层 `dispatch.go` `commands` map ↔ `PrintUsage` prose 仍双源（属同一漂移类的第 5 个面，declared blind spot）；当前由 archtest `TestCLIUnimplHide01_PrintUsageNoStaleToken` 反向断言「无 indexes / not implemented stale token」补偿；修复: 顶层命令也纳入 typed registry（命令名+help+handler 单源），`PrintUsage` 由 registry 派生，archtest 闭环 | arch-opt | P2/Cx2 | 🟠 | 下一次顶层命令增删 / 出现 PrintUsage↔commands 漂移 | `cmd/gocell/app/dispatch.go` + `tools/archtest/cli_unimpl_hide_test.go` | PR #502 (038 Wave 1) review ledger §1 |
| CELL-WIRING-ASSEMBLY-REGRESSION-TEMPLATE-01 | **Cell 装配完整性最小回归断言模板** — 现状: cell-level 占位/重构清理（如 PR-CFG-CELL-ROUTES-CLEAN 删 cell_routes.go）依赖三层：(a) `cell_gen.go` codegen funnel + (b) archtest `findAllCellInitFiles` 枚举 + (c) `cmd/corebundle/subscription_validator_wiring_test.go` + `tools/archtest/event_subscription_contractgen_coverage_test.go`；缺一条「per-cell 启动后实际挂载的路由/订阅集 ↔ slice.yaml contractUsages × contract.yaml endpoints 声明集」cross-validate 模板，未来同类清理理论上可能出现「编译通过但装配缺口」；修复: 评估并落地最小装配回归模板，候选载体二选一：(i) 扩展 D2-FU-01 cmd/corebundle/bundle_test.go 加 cell-level listener/route/subscription 集合 typed 断言；(ii) 新建 archtest cross-validate `slice.yaml contractUsages × cell 启动后实际挂载 routes/subscribers`。AI-rebust 评级必须 ≥ Medium（hand-crafted len() 数量约定 / 名字 convention 是 Soft 严禁立项）；不立项理由（PR #447）: 当前 PR 删除真·零代码占位，删除前后 `cell_gen.go` 拥有的 routes/subscriptions 集合不变，无具体回归风险 | test+arch | P2/Cx3 | 🟠 | 下一次 cell-level cleanup 出现真实 wiring 回归 / D2-FU-01 落地后评估扩展 | `cmd/corebundle/bundle_test.go` + `tools/archtest/` | PR #447 reviewer P2 follow-up + cross-ref D2-FU-01 |
| CATALOG-DTO-CODEGEN-DERIVE-01 | **catalog wire DTO codegen 派生（Hard 升级路径）** — 现状: PR-MD1 任务二仅升 Medium-偏-Hard（reflect + excludelist 显式登记）；catalog/wire.go 中 5 个 DTO（CellSpec/SliceSpec/ContractSpec/JourneySpec/AssemblySpec/ActorSpec）+ 对应 buildXxxEntity 函数手写映射；修复: codegen 从 `kernel/metadata` 反射派生 5 个 DTO + buildEntity 函数（同 cellgen literal_printer.go 单源思路），字段集偏差变 compile-time 不可表达 = Hard。本 PR-MD1 因工时 15-25h 超本 PR 4 倍而 defer | arch-opt | P3/Cx3 | 🟢 | 第 2 次 AssemblyMeta/CellMeta/SliceMeta yaml 字段未同步至 catalog wire DTO / catalog DTO 总数 ≥ 6 / wire 序列化漂移事故首现 | `runtime/devtools/catalog/wire.go` + `build.go` + `tools/codegen/catalogdtgen/` (new) | PR-MD1 follow-up |
| CELLGEN-ERRCODE-FUNNEL-HARDEN-PATH-C-FULL | **Path C-full — typed return + errcode.Error 字段私有化 (compile-time Hard 上限)** — R2-P5.1 落地 Path A Ident-scan (OSS 业界上限) 后剩余的真盲区是 (i) 复合字面量 `&myErr{}` 构造非-errcode 类型实现 error（含 `&errcode.Error{Code:"x"}` 跨包字段构造）；(ii) `reflect.MakeFunc` 反射构造；(iii) 跨包 function-valued var imports 的间接调用。其中 (i) 由 `TestCellgenErrcodeFunnelBlindSpotsAbsent` runtime 守 CI 即红，是 trigger 主路径。compile-time Hard 升级需把 cellgen 30 个 error-returning 函数返回类型改 `*errcode.Error`（~30 一行编辑 + 2 处 raw stdlib err forward 加 errcode.Wrap）**且**把 `pkg/errcode.Error` 7 个 exported 字段 (`Kind / Code / Message / InternalMessage / Details / Cause / Category`) 改 unexported + 提供 accessor 方法 + 迁移所有读取这些字段的 caller（runtime/http middleware / observability / redaction layer 等）。Scope ~25-40h 跨包重构；archtest 简化为 FuncDecl-level 单规则 ~50 行。**Trigger condition**（任一即触发，触发=同 PR 评估升级决策，不是直接立项）：(a) `TestCellgenErrcodeFunnelBlindSpotsAbsent` 在 cellgen 出现首例复合字面量实现 error 的命中——触发同 PR 决策树：扩 Path A blind-spot 检测面 vs 升 Path C-full；(b) 同一版本周期（≤ 1 Sprint）内出现 ≥ 2 次第三方 error lib escape 需要扩 depguard `cellgen-error-libs` deny + archtest blacklist + RED fixture 的 trigger-based mitigation，per-entry 维护负担超过 Path C-full 一次性成本；(c) `pkg/errcode.Error` 字段私有化由其他 ADR/PR 推动（高优 PII / unstable API 治理） — 顺势整合 Path C-full | arch-opt | P3/Cx4 | 🟢 | trigger (a)(b)(c) 之一 | `pkg/errcode/errcode.go` (字段私有化 + accessor 方法) + `tools/codegen/cellgen/*.go` (30 个 func 签名) + `tools/archtest/cellgen_errcode_funnel_test.go` (改 FuncDecl-level) + `runtime/http/middleware/` + `pkg/redaction/` + `runtime/observability/` (所有读 errcode.Error 字段的 caller) | ADR `docs/architecture/202605171200-adr-cellgen-errcode-funnel-mechanism.md` §D7 |
| SCAFFOLD-FUNNEL-RESIDUAL-DRIFT-BUNDLE | **Scaffold 漏斗化残余漂移束（PR #442 review 聚合，8 子条）** — 现状: scaffold/assembly 漏斗主线已 ship（K#09 SCAFFOLD-ONE-CMD），残余漂移面（输入/输出/边界/并发/模板退化）8 处待收口。子条：<ul><li>**ARCH-ASSEMBLY-SCAFFOLD-OWNERSHIP-01** (Cx3, 🟡 assemblygen 重构窗口 / ADR 修订) — `kernel/assembly.PlanAssemblyScaffold` 持有 scaffold 写入计划与 `kernel/scaffold/` 整层删除 framing 概念裂缝；下移 `tools/codegen/assemblygen/` 或修 ADR 202605101430 (Files: `kernel/assembly/generator.go` + `tools/codegen/assemblygen/`(可能新) + ADR 202605101430，PR442 architect F1 + kernel-guardian F6)</li><li>**ASSEMBLY-GENERATOR-CONCURRENCY-01** (P1/Cx1, 🟠 Generator 并发调用进产线) — Generator map mutation + defer revert 在并发下污染风险；sync.Mutex 或 immutable Generator pattern (Files: `kernel/assembly/generator{,_test}.go`，PR442 kernel-guardian F3)</li><li>**SCAFFOLD-INLINE-TEMPLATE-ARCHTEST** (P2/Cx1, 🟡 模板退化事故 / Hard 化 input contract) — `inlineContractYAMLTpl` 含 `codegen:false` 字面但无 archtest 守，模板退化会让 draft contract 走 funnel 默认 true (Files: `tools/archtest/` + `cmd/gocell/app/scaffold.go`，K#09 PR#442 round-2 F15)</li><li>**SCAFFOLD-CELL-BUNDLE-CROSS-STAGE-PLAN-MERGE-01** (P2/Cx3, 🟢 scaffold cell 跨阶段 rollback 事故 / pathsafe API 扩展) — 扩 `pathsafe.PlannedFile` 加 `ForceOverwrite`，把 contractgen/cellgen 派生合并入单 plan funnel（与 round-6 assembly 对称）(Files: `pkg/pathsafe/pathsafe.go` + `tools/codegen/{cellgen,contractgen}/` + `cmd/gocell/app/scaffold.go`，K#09 PR#442 round-6 carryover)</li><li>**EXAMPLES-ASSEMBLY-MINIMAL-CLEANUP** (P2/Cx2, 🟡 K#09 ship 后立即) — `examples/{ssobff,todoorder,iotdevice}/` 当前**根本没有 assembly.yaml**（find 验证）；旧描述误以为已存在需简化。修复: 创建 minimal `id+cells+owner` 三字段 assembly.yaml（与 corebundle 对齐，build/deploy 派生）；同时验证 examples cell-package 是否符合 `metadata.MatchCellID` pattern（PR #527 Lane B 引入 metadata-rule enforcement 后这是消费者侧 ID 合规边界，可能需 rename） (Files: `examples/{ssobff,todoorder,iotdevice}/assembly.yaml` (新)，K#09 carveout + PR #527 review)</li><li>**PATHSAFE-COLLECT-MISSING-DIRS-EACCES-01** (P3/Cx2, 🟡 中间目录权限受限场景 / pathsafe API 扩展) — `collectMissingDirs` 用 `os.IsNotExist` 漏 EACCES；区分 `IsNotExist` vs other error，配 EACCES 测试 (Files: `pkg/pathsafe/pathsafe.go`，K#09 PR#442 round-7 wave-2 P2#3)</li><li>**VALIDATE-SCAFFOLD-ID-TAB-CHAR-01** (P3/Cx1, 🟡 新 scaffold field 字符注入向量) — `\t` YAML 合法字符不构成注入向量，纯 defense-in-depth；按需扩展检查集 (Files: `cmd/gocell/app/scaffold.go`，K#09 PR#442 round-7 wave-2 P2#2)</li><li>**ASSEMBLY-RUN-RUNTIME-SMOKE** (P2/Cx2, 🟢 scaffold 用户尝试 `go run` 时报告) — `cmd/{id}/run.go` stub 加最小可运行 stub（noop bootstrap + graceful shutdown），smoke gate 加 `go run ./cmd/{id} -timeout=5s` 验证退出码 (Files: `kernel/assembly/gentpl/scaffold-run-go.tpl` + `hack/verify-scaffold-assembly.sh`，K#09 PR#442 round-2 F17)</li></ul> | refactor+arch-opt | P1/Cx3 | 🟠 | D.2 立即排期（并发 race）；D.5 K#09 ship 后批；其余触发型 | `kernel/assembly/` + `pkg/pathsafe/` + `tools/codegen/{cellgen,contractgen}/` + `cmd/gocell/app/scaffold*.go` + examples assembly.yaml | PR #442 round-6/round-7 review + 六角色 review (architect F1 + kernel-guardian F2/F3/F6) |
| OUTBOX-SERVICE-OPTION-CODEGEN-01 | **outbox-bound service Option boilerplate codegen** — 现状: 11 处 slice service 的 WithTxManager / WithEmitter + Service struct + NewXxx fail-fast 共 ~55 行 mandated boilerplate（由 OUTBOX-SERVICE-01 archtest + Option 范式分层联合强制），AI 写新 service 偶发漂移，SonarCloud 噪音 6%+；修复: cell.yaml / slice.yaml 加 `outboxBound: true` 元数据，cellgen 生成标准 Option set + Service base struct + fail-fast NewXxx，hand-written 文件仅写自定义 Option；触发条件: ANY of (a) 第 2 次 OUTBOX-SERVICE-01 / Option 范式分层 invariant 变更需要批量改 11+ 处 (PR-S7 是第 1 次，下一次触发立即启动)、(b) outbox-bound service 总数 ≥ 14、(c) 1 个季度内出现 ≥ 2 处 AI 写错的 boilerplate 被 archtest 抓 | feat | P3/Cx3 | 🟢 | 触发条件之一满足 | `tools/codegen/cellgen/` + `cell.yaml` schema | PR #481 (PR-S7) duplication analysis 2026-05-12 |
| SCAFFOLD-DERIVED-FORCEOVERWRITE-GATE-SPLIT-BACKLOG | **SCAFFOLD-DERIVED-FORCEOVERWRITE-GATE-SPLIT-BACKLOG — planDerivedArtifact 闸函数与 DerivedOverwrite 构造拆分后的 archtest 重评估** — `planDerivedArtifact` 当前同时承担 governance 闸（`IsGoCellGenerated`）+ `pathsafe.DerivedOverwrite` 调用，是受信任看门人。未来若把 gate 与 constructor 拆分，必须重新校验 `SCAFFOLD-DERIVED-FORCEOVERWRITE-01` archtest 假设是否仍成立。来源：PR #555 reviewer F4 | arch-opt | P3/Cx2 | 🟠 | planDerivedArtifact 发生拆分重构 | `tools/archtest/scaffold_derived_forceoverwrite_test.go` + `tools/codegen/cellgen/stage_render.go` | PR #555 Lane E reviewer F4 |
| SCAFFOLD-INPUT-CONTRACT-TYPED-ID-CONTRACT-JOURNEY-REMAINING | **SCAFFOLD-INPUT-CONTRACT-TYPED-ID-CONTRACT-JOURNEY-REMAINING — contract --id / journey --id 仍未走 typed funnel** — PR #555 round-2 把 cell / slice / assembly / **contract --owner / journey --cells** 全部 `--id` / cell-ID 形态参数收编到 `scaffoldid.Parse`。剩余 contract --id（多段 dot 分隔，如 `http.cell.example.v1`）+ journey --id（带 `J-` 前缀 + 内部 dash，如 `J-foo-bar`）形态不同，仍走 legacy validateScaffoldID。升级方案：在 pkg/scaffoldid 加 ContractID + JourneyID 两个独立 struct typed 变体（各自 pattern），替换 validateContractFlags --id + validateJourneyFlags --id。来源：PR #555 reviewer F4（产品维度）+ [[feedback_pr_scope_carveouts_must_backlog]] | refactor | P3/Cx3 | 🟠 | 后续 ID typed funnel 主题 PR | `cmd/gocell/app/scaffold.go` validateContractFlags + validateJourneyFlags + `pkg/scaffoldid/*.go` | PR #555 Lane E reviewer F4 |
| SCAFFOLD-ARCHTEST-FILE-RENAME-INVARIANT-MERGE | **SCAFFOLD-ARCHTEST-FILE-RENAME-INVARIANT-MERGE — scaffold_* archtest 文件主题合并** — ai-collab.md「同主题规则 ≥ 3 → `{theme}_invariants_test.go`」要求 scaffold 主题归入 `scaffold_invariants_test.go`。当前 `tools/archtest/` 仍有 `scaffold_write_funnel_test.go`（1 条规则）+ `scaffold_derived_forceoverwrite_test.go`（2 条规则）两个单规则文件 + `scaffold_bundle_invariants_test.go` 主题文件。重组需要同步更新 `ARCHTEST-VERIFY-COVERAGE-01` shard discovery 一致性。建议独立 PR 处理避免 review 风险。来源：PR #555 reviewer F2（DX 维度） | refactor | P3/Cx3 | 🟠 | 独立 archtest 重命名 PR | `tools/archtest/scaffold_*_test.go` + `tools/archtest/archtest_verify_coverage_test.go` 联动 | PR #555 Lane E reviewer F2 (DX) |
| PATHSAFE-FORCEOVERWRITE-PLAN-TIME-AUTH-TOCTOU | **PATHSAFE-FORCEOVERWRITE-PLAN-TIME-AUTH-TOCTOU — generated-file overwrite 授权窗口 TOCTOU** — `tools/codegen/cellgen/stage_render.go::planDerivedArtifact` 在 plan 时通过 `os.ReadFile` 读取目标文件并校验 `governance.IsGoCellGenerated` 头部；write 时 `pathsafe.WritePlannedFiles` → `writePass` → `secureMkdirAllAndWrite(forceOverwrite=true)` 走 path-based `os.Remove` + `O_EXCL|O_NOFOLLOW` recreate，两阶段之间文件可能被替换为非 generated 文件而绕过 plan-time 闸。**升级方案**: 在 plan-time 同时记录文件 inode（unix）+ content-hash，写时在 fd-anchored open 之后重新校验 inode 与 hash 一致，否则中止 + rollback。**为何 backlog 而非本 PR 修**: Cx4 — fd-anchored revalidation 涉及 syscall (openat/fstat) + 跨阶段 state passing (record → secureMkdirAllAndWrite) + Windows fallback 重构，估 ~300+ 行；属独立的 pathsafe syscall 改造窗口（与 Lane E typed-scaffold-id funnel 主题正交）；与 PATHSAFE-FORCEOVERWRITE-STAGING-CRASHSAFE-01 同源（staging+rename + inode-bind 应一次性引入）。AI-rebust: Hard（fd-anchored bind + revalidation）。来源: PR #555 reviewer P2-4 | arch-opt | P3/Cx4 | 🟢 | 与 PATHSAFE-FORCEOVERWRITE-STAGING-CRASHSAFE-01 同批 | `pkg/pathsafe/pathsafe.go` + `tools/codegen/cellgen/stage_render.go` | PR #555 reviewer P2-4 |
| PATHSAFE-FORCEOVERWRITE-STAGING-CRASHSAFE-01 | **PATHSAFE-FORCEOVERWRITE-STAGING-CRASHSAFE-01 — Crash-safe ForceOverwrite via staging + atomic rename** — 来源: PR #526 (Lane A) commit `5724fe9af` 实施**方案 A**（in-memory `writeRecord` capture + path-based restore），关闭了 user-space mid-plan failure 的 rollback gap (用户复现的 "F0 永久消失" bug)。**方案 A 局限**: (1) 原文件内容 buffer 全量驻留内存（scaffold 小文件 ~KB 级可接受，若 pathsafe 扩到大文件则压力显著）；(2) 进程崩溃 / 信号杀死之间无法恢复（in-memory state 丢失，原文件已 unlinkat，磁盘既无新文件也无旧文件）；(3) rollback 路径用 path-based `os.WriteFile`/`os.Symlink`，与 fd-anchored write 路径不对称。**方案 B 升级目标**: 每个 ForceOverwrite entry 先写到 `{path}.staging-{nonce}`（同父目录，fd-anchored），全部 staging 成功后 phase-2 用 `renameat` atomic 替换原文件。原文件在 rename 那一刻才被覆盖（消除 "unlinkat + write 之间无任何 inode" 的窗口）。配合 fsync(parentFd) 可达 crash-safe (commit point = rename + fsync; crash → 看到原文件或新文件之一)。**触发条件**: 任一成立 → 评估升级：(a) pathsafe 用例扩到非 scaffold（大文件 codegen 产物 / migration 备份）；(b) 出现 process crash 期间 ForceOverwrite-driven 数据丢失事故；(c) `renameat` 跨平台抽象稳定可用。对标: etcd-io/etcd `wal/file.go` (tmp+rename safe-write pattern); git `safe_write_file`. **预估改动**: `pkg/pathsafe/pathsafe.go` + `nofollow_unix.go` 新增 ~200 行；windows 需新 `renameat` 等价。AI-rebust 评级: Hard (crash boundary by file system commit point). | arch-opt | P3/Cx4 | 🟢 | 上述触发条件之一成立 | `pkg/pathsafe/pathsafe.go` + platform helpers | PR #526 commit `5724fe9af` 方案 A 局限注释 |

## 14.3 contract codegen + 兼容

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| CONTRACT-BREAKING-01 | **`gocell check contract-breaking`** — 现状: 缺 API schema 历史破坏性变更比对；修复: 借鉴 buf.build 引入 40+ 条规则（字段删除/必填放宽阻断） | feat | P2/Cx3 | 🟡 | V1.1 启动 | `cmd/gocell/` + `kernel/governance/` | backlog_later §5 |
| CONTRACT-CODEGEN-01 | **Go DTO ↔ JSON Schema 双向推断** — 现状: 代码与契约 YAML 分裂；修复: Struct Tags 实时双写到 JSON Schema（对齐 oapi-codegen） | feat | P2/Cx3 | 🟡 | V1.1 启动 | `tools/codegen/` + DTO 模板 | backlog_later §5 |
| CONTRACT-STUB-01 | **Consumer-Driven Contract Stub** — 现状: 缺消费方 stub 校验；修复: 提供 Stub 桩代码套件（对标 Spring Cloud Contract / Pact） | feat | P2/Cx3 | 🟡 | V1.1 启动 | `tools/contracttest/` | backlog_later §5 |
| C-L6 | **Contract ID 解析标准统一** — 现状: CLI 用点分（http.auth）、Generator 退化为斜杠分割，开发者上下文脱节；修复: 全局检索 + 统一内部 Contract ID 解析 | bug | P2/Cx2 | 🟡 | — | `cmd/gocell/` + `kernel/scaffold/` + `tools/codegen/` | backlog_later §6 C-L6 |

## 14.4 journey / status-board

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| JOURNEY-METADATA-STATE-LIFECYCLE-TYPED-CONST-01 | **board.state / lifecycle typed const Hard 升级** — 现状: `metadata.StatusBoardEntry.State` / `JourneyMeta.Lifecycle` 是 `string`，包外可写任意字符串值；当前防线 FMT-22（state enum）+ JOURNEY-STATUS-LIFECYCLE-01（state×lifecycle 矩阵）联合拦截为 Medium archtest-bound。Hard 升级路径: `type BoardState string` + `type Lifecycle string` 私有 const set + `kernel/metadata.parseStatusBoard` 解码即 reject — 违反不可表达。`rules_journey.go` 文件头 godoc 点名引用本条目 | arch-opt | P2/Cx3 | 🟠 | 第 2 次非法 state/lifecycle 字符串绕过 archtest / `kernel/metadata` 重构窗口 | `kernel/metadata/types.go` + `kernel/metadata/parser.go` + 调用点扇出 | PR-4 (fix/208) AI-rebust Hard 升级路径 A |
| JOURNEY-CONTRACT-EXISTENCE-CODEGEN-DERIVE-01 | **JOURNEY-CONTRACT-EXISTENCE-01 codegen 派生 Hard 升级** — 现状: archtest-bound governance rule（Medium），包外可手写 contract.yaml + 不引用 → 靠 `gocell validate` 拦截。Hard 升级路径: codegen 从 journey/contract 双源派生映射 — 违反不可表达。`rules_journey.go` 文件头 godoc 点名引用本条目 | arch-opt | P3/Cx4 | 🟢 | 第 2 次 active platform contract 漏引用 / codegen pipeline 扩展窗口 | `tools/codegen/` + `kernel/governance/rules_journey.go` | PR-4 (fix/208) AI-rebust Hard 升级路径 B |
| JOURNEY-USERONBOARDING-AUTO-EXPANSION-01 | **J-useronboarding 3 manual criteria → auto 升级** — 现状（PR-W3-1 落地后）: J-useronboarding 4 passCriteria 中仅 `login-verify` 是 `mode: auto`（session.MemStore round-trip 真实持久化）；其余 3 criteria (user-create / role-assign / event-publish) 是 `mode: manual` — 因 `tests/integration/` 受 Go internal-package 屏障禁止 import `cells/accesscore/internal/credentialinvalidate`，致 `identitymanage.Service.Create` / `rbacassign.Service.Assign` production code path 无 Docker-free load-bearing seam（PR-W3-1 激进自审拒绝 schema-only tautology 测试占位）。修复方向（二选一 / 双轨）: (a) `tests/testutil/corebundle/` 共享 harness 提取 — `cmd/corebundle` package main → library 改造 + 共享 newSessionPGHarness，触发型 Cx4，影响面 ≥ 5 个 e2e 测试包；(b) `tests/integration/l2atomicity/` 扩 J-useronboarding e2e 子套件 — 复用既有 l2Harness（testcontainers PG + corebundle），保持 J-useronboarding `lifecycle: active` 但 user-create / role-assign / event-publish 仍 manual（l2 是 docker-依赖、`make verify` 跳过），单独 `gocell verify journey --id=J-useronboarding` Docker 模式下补 auto。**注意**: 在选项 (a) / (b) 实现前不可将 3 criteria mode: manual 改回 auto；journey YAML inline 注释已引用本 backlog ID 守护，governance VERIFY-06 + journey.schema.json mode enum 已 Hard 拦截非法值。触发条件 = J-useronboarding 业务密度提升 / 出现真实 user-create 回归 / 040 archtest Pass-Driver 工作完成后 corebundle 重构窗口。**当前 `tests/integration/l2atomicity/` 覆盖核查**：仅覆盖 `identitymanage.changePassword` (change_password_e2e_test.go)，**未覆盖** user-create POST /api/v1/access/users / event.user.created.v1 publish / role-assign 路径——选项 (b) 需新增 e2e 子套件，不是「复用既有覆盖」。**Warning 关闭条件**：本 backlog ship 后（3 manual → auto），J-useronboarding `status-board.state: doing → done`，JOURNEY-STATUS-LIFECYCLE-01 active+doing in-transit Warning 同步消失 | test | P2/Cx3 | 🟠 | 触发型 — 见描述 | `tests/integration/` + 可能 `cmd/corebundle/` + `journeys/J-useronboarding.yaml`（manual→auto + checkRef 回填）+ `journeys/status-board.yaml`（state: doing → done） | PR-W3-1 (615-journey-useronboarding-harness) AI-rebust 升级路径 |
| JOURNEY-ACCOUNTLOCKOUT-AUTO-EXPANSION-01 | **J-accountlockout 3 manual criteria → auto 升级**（**partially closed by PR #585 review-fix** — `tests/integration/l2atomicity/journey_accountlockout_e2e_test.go::TestJAccountlockoutAutoLockCycle` covers Threshold-failed-attempts → auto-lock + `event.user.locked.v1` audit-ledger observation + admin-unlock → re-login 正向闭环；the 3 previously-manual criteria are now `mode: auto` routed to `journey.J-accountlockout.auto-lock-cycle`. 剩余 open work：lazy-unlock-by-TTL — 见下方 JOURNEY-ACCOUNTLOCKOUT-AUTO-EXPANSION-01-LAZYUNLOCK 子条目。本条目作为 historical record 保留，便于未来回溯升级路径决策。） — 历史现状（PR-618 落地后）: J-accountlockout 4 passCriteria 中仅 `login-reject` 是 `mode: auto`（errcode 信封 byte-equal 防枚举断言，Docker-free）；其余 3 criteria (auto-lock / event-publish / admin-unlock) 是 `mode: manual` — 因 `tests/integration/` 受 Go internal-package 屏障禁止 import `cells/accesscore/internal/{accountlockout,authzmutate,credentialinvalidate}`，致 `sessionlogin.Login` 失败计数 → 阈值锁定 → outbox emit + admin unlock 路径无 Docker-free load-bearing seam。修复方向（二选一 / 双轨）: (a) `tests/testutil/corebundle/` 共享 harness 提取 — 与 J-useronboarding 同 Cx4 重构；(b) `tests/integration/l2atomicity/` 扩 J-accountlockout e2e 子套件 — **采用 (b) 路线**：复用 l2Harness（testcontainers PG + corebundle），新增 N 次连续失败 → auto-lock + outbox 含 event.user.locked.v1{actorId="system"} + 后续 admin unlock → re-login 成功的完整链路。 | test | P2/Cx3 | 🟢 partially closed by PR #585 review-fix | (b) shipped；only lazy-unlock 子条目余项触发型 | `tests/integration/l2atomicity/journey_accountlockout_e2e_test.go` + `journeys/J-accountlockout.yaml`（manual→auto + checkRef 回填） | PR-618 (ACCESSCORE-ACCOUNT-LOCKOUT-AUTO-LOCK-01) → PR #585 review-fix |
| JOURNEY-ACCOUNTLOCKOUT-AUTO-EXPANSION-01-LAZYUNLOCK | **J-accountlockout 第 4 条 manual criterion (TTL 懒解锁 → user.unlocked 事件) → auto 升级** — 现状（PR #585 review-fix 落地后）: `TestJAccountlockoutAutoLockCycle` 覆盖了 admin-unlock 路径，但 TTL 自然过期触发的 `accountlockout.TryLazyUnlock` → `ActivateUser` + `publishUnlocked(event.user.unlocked.v1)` 路径没有 e2e 覆盖，因为 `LockoutTTL = 15 * time.Minute` 与 `clock.Real()`-bound l2Harness 不兼容（CI 测试时间预算 < 15min）。Journey YAML inline 注释已点名本 backlog ID 守护，不可在解决前 silent revert 任何 criterion 到 auto。修复方向: (a) `l2Harness` 暴露 clock-advance seam — `cells/accesscore` Init 路径接受 `clock.Clock` 注入（已支持），harness 改用 `kernel/clock/clockmock` 并提供 `harness.AdvanceClock(d time.Duration)` 用于驱动 TTL 触发；(b) 测试用 `httpLogin(victim)` 在 TTL 边界外触发 lazy-unlock + 断言 `event.user.unlocked.v1` 出现在 auditcore ledger（与 auto-lock 对称）。**触发条件**: (i) 项目内首个需要 clock-advance e2e 的其他 journey（共享 harness 改造经济性阈值）；(ii) 产品要求 lazy-unlock 路径必须有 wire-level 自动化覆盖。**AI-rebust**: clock-advance harness 是 Hard 形态（typed clock injection 已存在），仅需 e2e 测试补全。 | test | P3/Cx3 | 🟢 | 见描述 | `tests/integration/l2atomicity/harness_test.go` + `tests/integration/l2atomicity/journey_accountlockout_e2e_test.go` + `journeys/J-accountlockout.yaml` (lazy-unlock criterion manual→auto) | PR #585 review-fix split of JOURNEY-ACCOUNTLOCKOUT-AUTO-EXPANSION-01 |
| ACCOUNTLOCKOUT-THRESHOLD-DRIFT-DETECT-01 | **`jAccountlockoutThreshold` 漂移检测 (Soft → Hard 升级)** — 现状: `tests/integration/l2atomicity/journey_accountlockout_e2e_test.go::jAccountlockoutThreshold = 5` 镜像 `cells/accesscore/internal/accountlockout.Threshold = 5`；前者不能 import internal/ 包，所以两个常量平行存在，**无任何机器检测漂移**（Soft 命名 convention + godoc 引用 backlog ID）。如果未来调阈值至 7 但只改 internal 常量，e2e 仍按 5 次循环，threshold 未达，测试无症状通过——但生产行为已变。修复方向: (a) **archtest typed**: 用 `archtest.RunTypedProduction` + `EvaluateConstString` 在 production AST 解析 `accountlockout.Threshold` 字面值，对比 e2e 测试包内 `jAccountlockoutThreshold` BasicLit 值（Hard form-uniqueness：值不匹配 = CI red）；(b) **policy contract**: 把 lockout policy 移出 internal，在 `cells/accesscore/policy/` 导出 const，e2e 直接 import（type-system level 单源，但 cells/policy/ 子目录需 governance VERIFY-06 + cell.yaml allowedFiles 协议化）；(c) **HTTP exposure**: 内部端点暴露 policy 值，e2e 通过 wire 读取（最重，但语义最强）。**触发条件**: (i) Threshold 实际调整事件首现；(ii) 出现第 2 个 e2e 测试需要 internal/ 常量镜像（经济性阈值）。AI-rebust: 当前 Soft（命名 convention + godoc），升级路径 Hard（option a）。 | arch-opt | P3/Cx2 | 🟢 | Threshold 实际调整 / 第 2 个 internal-mirror 常量出现 | `tools/archtest/` (新 test) + `tests/integration/l2atomicity/journey_accountlockout_e2e_test.go` + `cells/accesscore/internal/accountlockout/policy.go` | PR #585 review-fix F6 |
| CONTRACT-ENDPOINT-TEST-MAPPING-HARD-CODEGEN-01 | **CONTRACT-ENDPOINT-TEST-MAPPING-01 codegen 派生 Hard 升级** — 现状: archtest/governance-bound rule（Medium），slice.yaml `verify.contract` `.serve` 是开发者手写字符串，可漏可错；规则双向校验在 `gocell validate` 拦截。Hard 升级路径: codegen 从 contract.yaml `endpoints.server` + cell ownership 派生 slice 的 `.serve` 声明 — slice.yaml 不再 SoR，违反不可表达。`rules_contract_test_mapping.go` 文件头 godoc 点名引用本条目 | arch-opt | P3/Cx4 | 🟢 | 第 2 次 active HTTP contract serve 漏声明 / codegen pipeline 扩展窗口 | `tools/codegen/` + `kernel/governance/rules_contract_test_mapping.go` | PR-W6-1 (fix/218) AI-rebust Hard 升级路径 |
| CONTRACT-PATH-QUERY-COVERAGE-HARD-CODEGEN-01 | **CONTRACT-PATH-QUERY-COVERAGE-01 codegen 派生 Hard 升级** — 现状: per-param archtest GATE（Medium）+ 配套 `CONTRACT-PATH-QUERY-PARAM-NAME-LITERAL-01` 锁实参字面量（Medium）；cells/**/contract_test.go 中每个 `MustReject{Path,Query}Param` stub 仍是开发者手写，漏一个 param 由 archtest 拦截而非编译期拦截。Hard 升级路径: `gocell generate contract-test` 从 contract.yaml `endpoints.http.{pathParams,queryParams}` 派生 reject case skeleton（每个 param 至少 1 个 invalid value 占位），业务只填具体值——漏 param 在生成阶段即不可表达。`contract_path_query_coverage_test.go` 文件头 godoc 点名引用本条目 | arch-opt | P3/Cx4 | 🟢 | 第 2 次 active HTTP contract param 漏 reject case / codegen pipeline 扩展窗口 | `tools/codegen/` + `tools/archtest/contract_path_query_coverage_test.go` | PR-W6-1 fix/218 round-3 review F2 AI-rebust Hard 升级路径 |
| J-02 | **JOURNEYS-FIXTURES-DECISION** — `fixtures/` 仅 `.gitkeep`，CLAUDE.md 声明"供 run-journey 使用"但 schema 缺 fixtures 字段；修复: 二选一 (a) 删除 `fixtures/` + 撤回 CLAUDE.md 引用；(b) 引入 `fixtures: [fixture-id]` 字段 + runner 注入机制 | doc | P1/Cx1 | 🟡 | — | `fixtures/` + `kernel/metadata/` + CLAUDE.md | 030 §3 J-02 |

## 14.5 doc / ADR / NoLint / governance rules

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| V-A11c | **GOVERNANCE-RULES-EXAMPLES-EXEMPTION-AUDIT-01** — 现状: V-A11 主条目 ❌ subsumed（parser fs.WalkDir 已覆盖 examples/），但 `TestProjectWalksExamples (validate_test.go:4911)` 只断言 parser 装载，未真跑 `ValidateStrict` 在 examples/ contract 上；现存 governance 规则中部分有 `strings.HasPrefix(c.File, "examples/")` examples 豁免（如 ADV-01 / JOURNEY-CONTRACT-EXISTENCE-01），每条规则对 examples 的豁免态度不统一；修复: 为每个 governance 规则审视 examples 豁免（应/不应豁免、为什么）+ 加一个 e2e 测试构造 `examples/badcell/contract.yaml` 含已知违反，证明 ValidateStrict 真在 examples 上跑并产生 finding；输出豁免决策表纳入 CLAUDE.md | arch-opt | P3/Cx3 | 🟡 | examples cell 出现治理盲区 / 第二条 examples 豁免规则被加上 | `kernel/governance/rules_*.go` + `kernel/governance/validate_test.go` + `kernel/governance/CLAUDE.md` | PR #573 review A-8（reviewer A 产品 P1 Cx1）|
| V-A11b | **EXAMPLES-HARDCODE-STRING-SCAN-01** — 现状: examples/ 源码中 cell id 以裸字符串字面量形式散在 cmd / test / handler 多处（具体清单见 `docs/architecture/naming-guard.yaml` replacements 列表），重命名 cell 时漂移风险；修复: archtest 扫 `examples/**/*.go` 中出现 cell id 字符串字面量的位置，要求都引用 `examples/<x>/cells/<cell>/id.go` 单源 const；V-A11 (parser walk) 仅覆盖 metadata 维度，不覆盖 Go 源码字符串维度 | arch-opt | P3/Cx3 | 🟢 | examples cell 重命名 / 新增 example 时漂移暴露 | `tools/archtest/` (新) + `examples/**/*.go` | V-A11 archive backlog 分支; PR #573 review A-9 升 active backlog |
| V-A13 | **GENTPL-LIFECYCLE-PATTERN-01** — 现状: gentpl/main.go.tpl 直连 app.Start/Stop 跳过 phase3b（PR#392 已删 phase3b admin provision）；修复: 决定模板"最小骨架 vs 开箱即用" + 集成测试 | doc+arch-opt | Cx1+Cx2 | 🟡 | — | `kernel/assembly/gentpl/main.go.tpl` | PR #243 review §E1 |
| SLICE-ISP-DEFERRED | **SLICE-ISP-SPLIT-01** (deferred) — 现状: `kernel/cell.Slice` 7 方法形态相似 Cell 但本 PR (PR-A22) 默认不拆，因 cells/* 全嵌 BaseSlice 0 fatigue + 无 metadata.SliceMeta 单源化数据层驱动；触发条件: 首次出现需替换 BaseSlice 7 方法之一的第三方 Slice 实现，按 PR-A22 同精神拆 SliceIdentity / SliceLifecycle / SliceMetadata；修复: 拆分 + ADR amend | arch-opt | Cx3 | 🔴 | 第三方 Slice 实现需替换 BaseSlice 7 方法之一 | `kernel/cell/interfaces.go` (Slice) + `kernel/cell/base.go` | ADR 202605101800 §D4 |
| FU2-GOVERNANCE-STATIC | **Governance static analysis** — 现状: typed gate (→ PR#321) 已落，static 后续；修复: 跟进 | arch-opt | Cx3 | 🟢 | — | `tools/archtest/` | — |
| F2 | **SYSTEM-TOPOLOGY-API** — 现状: 缺 `GET /internal/v1/system/topology`；修复: 基于 `kernel/registry` 返回 cell/slice/contract 拓扑 JSON | feat | P3/Cx2 | 🟡 | — | 新 slice 或 `runtime/bootstrap/` | 历史 Batch 8 |
| NOLINT-AUDIT-01 | **Nolint audit** — 现状: 全仓 101 处 nolint 含 errcheck 类豁免；修复: 审查 | arch-opt | Cx2 | 🟡 | — | 全仓 *.go | NOLINT-AUDIT-01 |
| ADR-INDEX-01 | **ADR index** — 现状: 缺 ADR 索引；修复: 生成 docs/architecture/INDEX.md | doc | Cx1 | 🟡 | — | `docs/architecture/` | ADR-INDEX-01 |
| ADR-DATE-CONSISTENCY-CHECK | **ADR 文件名日期 vs 内容 Date 一致性** — 现状: PR#404 ADR `202605061800-...md` 文件内 Date: 2026-05-07（1 天误差）；修复: archtest 校验 `docs/architecture/yyyymmddHHmm-*.md` 文件名前缀日期 = 内容 `Date:` 字段日期 | test | P3/Cx1 | 🟡 | — | `tools/archtest/` + ADR 命名约定 | PR#404 F6 |
| F-07 | **SYSML-VIEW-CODEGEN** — 5 张 SysML 图（BDD/IBD/用例/活动/状态机）有元数据天然映射但无生成器；修复: 新建 `tools/sysmlgen/` → `generated/sysml/<view>.{puml,mermaid}` + CI step `make sysml-verify` | feat | P3/Cx3 | 🟡 | F-06 落地后 | `tools/sysmlgen/` (新) + `generated/sysml/` (新) | 030 §3 F-07 |

## 14.6 杂项 / PR FU / T-*

| ID | 描述 | Type | P/Cx | Flag | Trigger | Files | Source |
|---|---|---|---|---|---|---|---|
| PR250-F3 | **Event wire byte pinning** — 现状: 缺 byte 级回归；修复: 加 pinning test | test | Cx2 | 🟡 | — | `cells/accesscore/` | PR#250 |
| PR266-AUDITAPPEND-STRICT | **AUDITAPPEND-STRICT-UNMARSHAL-01** — 现状: `cells/auditcore/slices/auditappend/service.go:102` 用宽松 json.Unmarshal 接受未知字段，与 audit 合规"严格记录"语义存在张力；当前是 best-effort by design（lenient path 让事件不丢）；修复方向: (a) 注释加强说明 lenient 取舍；(b) strict 模式 toggle 给将来需要严格审计的 deployment；(c) 永久错误路径（PermanentError → DLX）的 unmarshal 失败行为与 lenient 区分；(b)(c) 待第一个 strict-audit 客户出现实施 | arch-opt | P2/Cx2 | 🟡 | strict-audit 客户出现 | `cells/auditcore/slices/auditappend/service.go` | PR#266 round-2 OPS reviewer F-OPS-03 |
| T6 | **CONTRACT-EVENT-PAYLOAD-CODEGEN-01** — 现状: scaffold/generate 无 schema → Go 能力；修复: 派生 payload.gen.go + decode/validate helper | feat | — | 🟠 | event subscriber decode 扩散 ≥5 cell | `tools/codegen/eventgen/` (新) + `generated/contracts/event/` | T6 |
| T7 | **CH-05 alias eval** — 现状: import alias / const eval 漂移；修复: governance 加 | arch-opt | — | 🟡 | import alias / const drift | `kernel/governance/` | T7 / PR-A45 |
| T10 | **Devtools cell promotion** — 现状: catalog 内置；修复: 升级为外部 cell | arch-opt | — | 🟠 | catalog customization | `cells/devtools/` + `runtime/` | T10 / PR-A37 |
| M4-COVERAGE | **REVERSE-COVERAGE-ARCHTESTS-01** — 现状: 缺 5 条反向追溯规则；修复: 加 `IMPL-DECL-COVER-01` (cell 间 Go import 必须经 contract，非 slice 间) + `HANDLER-DECL-COVER-01` (http handler 必须出现在某 contract.yaml) + `EMIT-DECL-COVER-01` (outbox emit 必须出现在 contract.triggers) + `DEAD-CONTRACT-01` (active contract 必须有 handler 入口) + `DEAD-CODE-01` (deprecated contract 引用代码不能在 main 分支)；不含 SLICE-DECOUPLE | arch-opt | P2/Cx3 | 🟠 | M3 落地 | `tools/archtest/` | ADR-202605041430 M4 |
| B2-A-13 | **PG pool tx rollback 日志泄漏** — 现状: rollback 日志输出 SQL 片段；修复: 走 `pkg/redaction` | bug | P2/Cx2 | 🟡 | — | `adapters/postgres/pool.go:87,113` | backlog2 §5.1 B2-A-13 |
| B2-A-25 | **Prometheus lookup vec 99% 重复** — 现状: 多处 lookup vec 模板代码 ~99% 重复；修复: 抽 helper 收敛 | refactor | P2/Cx2 | 🟡 | — | `adapters/prometheus/metric_provider.go:201-227` | backlog2 §5.3 B2-A-25 |
| B2-A-34 | **Redis cluster CI live gate 缺** — 现状: integration_cluster build tag 已加但 CI 未启用 live job；修复: 加 GH Actions cluster job | test | P2/Cx3 | 🟡 | — | `.github/workflows/_build-lint.yml` + `adapters/redis/cluster_real_test.go` | backlog2 §5.3 B2-A-34 |
| L2-ATOMICITY-HARNESS-FOLLOWUPS | **T4 (l2atomicity) e2e harness follow-ups（聚合 4 项）** — 现状: PR #523 ship 后留下四件已审视但未在 PR 内修的 finding。<ul><li>**F24（PR review）** `tests/integration/l2atomicity/` ~250 行 harness 与 `cmd/corebundle/setup_pg_integration_test.go::newSessionPGHarness` 形态重复，因后者位于 `package main`（Go 不可外部 import）而无法直接复用；触发条件 = `cmd/corebundle` 拆 library（package corebundle + 薄 main shim）后将 harness 提取到 `tests/testutil/corebundle/`</li><li>**F22（PR review）** `TestL2_LoginRefreshValidate_HappyPath` 使用 sessions/refresh_tokens/outbox_entries 三表 count+1 作为同 tx 原子提交的代理断言；触发条件 = 后续若出现"三表都加但不在同 tx"的真实漂移，升级为 `SELECT xmin FROM ...` 同事务比对</li><li>**F25（PR review）** `cmd/corebundle/setup_pg_integration_test.go:610-611` `TestSessionLogin_PGGoldenPath` 注释仍写"authz_epoch_at_issue column was dropped in S4b migration 025"，与 S4d migration 026 RESTORE 现状矛盾；触发条件 = 下次接触 `setup_pg_integration_test.go` 时顺手改注释，或独立小 PR 处理</li><li>**L2-ATOMICITY-BROKER-E2E**（PR #523 P1.1 review FU）当前 harness 用 in-process eventbus（runtime/eventbus.InMemoryEventBus）做 publisher/subscriber transport；真实 broker 路径（rabbitmq publisher + rabbitmq subscriber + relay write-back fencing 在 broker 失败下的行为）未覆盖；触发条件 = rabbitmq adapter 出现 transport-level 行为回归 / `OUTBOX-LEASE-ID-CAS-01` 边界扩展时立项；新建 `tests/integration/l2atomicity_broker/` + rabbitmq testcontainer</li></ul> ~~AUDIT-ROLE-EVENT-ACTOR-PROPAGATION-01~~ 已在 PR #523 内闭环（rbacassign 发 RoleChangedEvent 时填充 `ActorID` from service-token caller cell；schema 加可选 actorId）。 | fu | Cx1-Cx2 | 🟡 | 各项独立触发条件 | `tests/integration/l2atomicity/` + `tests/testutil/corebundle/` (新) + `cmd/corebundle/setup_pg_integration_test.go` + `tests/integration/l2atomicity_broker/` (新) | PR #523 L4 review F22 / F24 / F25 + /fix P1.1 |
| MEM-STORE-RWMUTEX-READ-CONCURRENCY | **mem.Store.mu sync.Mutex → sync.RWMutex（outside-tx 读并发）** — 现状: PR #238 (238-mem-tx-lock-ownership) 把 tx sentinel 真值化（bool→`*memTxToken`，holdsLock 锁所有权）根治 concurrent-map-write flake，但 `cells/accesscore/internal/mem.Store.mu` 仍是 `sync.Mutex`：outside-tx 读方法（GetByID/GetByUsername/GetByUserID/CountByRole/CountEffectiveAdmins/EffectiveAdminExists/ListByUserID 等）与写方法互斥串行。对标 client-go `tools/cache/thread_safe_store.go` 用 `sync.RWMutex` 让读并发。修复方向: `store.mu` → `sync.RWMutex`，per-call 读方法 `RLock`、写方法 + RunInTx `Lock`（写锁贯通，holdsLock=true 跳锁逻辑不变）；逐一论证 18 方法读/写分类（"读方法不写任何 store map"，含 cloneUser 纯读校验）；archtest MEM-TX-LOCK-OWNERSHIP-01 不受影响。**defer 理由（非 silent carryover）**: 正交于 #238 flake 根因，独立可验证，混入会扩大该 P0 修复 review 面与回归风险；store.go 包 godoc 已点名本条目。AI-rebust: Cx2（多方法 + 读写分类正确性逐一论证），不引入新 Hard 守卫（RWMutex 是性能优化，锁所有权 Hard 由 #238 memTxToken 提供）。 | arch-opt | Cx2 | 🟢 | mem 读路径成为单测/demo 并发瓶颈，或 client-go 对标一致性收口时 | `cells/accesscore/internal/mem/store.go` (Store.mu + txHoldsLock) + `user_repo.go` + `role_repo.go` | ADR 202605171846-adr-mem-tx-lock-ownership §D5 |
| MEM-TX-R2B-RECEIVER-HARD-UPGRADE | **MEM-TX-LOCK-OWNERSHIP-01 R2b receiver 识别 Soft → Hard 升级** — 现状: `TestMemTxLockOwnership01_R2bFindsHoldsLockAccessor` 中 R2b accessor 以 `fd.Name.Name == "txHoldsLock" && receiverTypeName(fd) == "Store"` 两处字符串锚点识别（`receiverTypeName` 是字符串比对 helper，Soft 字符串锚点），评级为 Medium（两部分匹配 + companion-index 防空集）。按 ai-collab.md §"Funnel 双向锁评级"，允许 Medium 上游过渡形态，但必须同步登记本 backlog 条目并在 funnel godoc 中点名。修复方向: 把 `receiverTypeName(fd) == "Store"` 替换为 `*types.Info` 类型化 receiver 识别——`pass.TypesInfo.TypeOf(fd.Recv.List[0].Type)` resolve 到 `cells/accesscore/internal/mem.Store`（或其指针）named type，以 canonical import path + type name 双重匹配（对标 `AUDIT-LEDGER-COMPOSITION-ROOT-01` alias-immune 模式）；archtest 须升级为 `RunTyped` pass 并移除 `receiverTypeName` 字符串比对分支。评级目标: Hard（形态选错 = compile-fail 或 type-mismatch，不可表达）。**触发条件**: R2b receiver 字符串锚点绕过真实事件首现（如 `mem` 包内新增同名 `txHoldsLock` 在不同 receiver 上），或 `MEM-TX-LOCK-OWNERSHIP-01` archtest 迁移 `RunTyped` 批次。**Cx**: Cx2（单文件 archtest 升级 + typed Pass 迁移，不跨 3 包）。本条目在 `tools/archtest/mem_tx_lock_ownership_test.go` `TestMemTxLockOwnership01_R2bFindsHoldsLockAccessor` 函数 godoc 中点名。 | arch-opt | Cx2 | 🟠 | R2b receiver 字符串锚点绕过真实事件首现，或 MEM-TX-LOCK-OWNERSHIP-01 迁移 RunTyped 批次 | `tools/archtest/mem_tx_lock_ownership_test.go` | ADR 202605171846-adr-mem-tx-lock-ownership §D3 |
| B2-X-01 | **Outbox E2E 固定 sleep** — 现状: integration test 含固定 `time.Sleep`；修复: 改 condition wait | test | P2/Cx1 | 🟡 | — | `cmd/corebundle/outbox_e2e_integration_test.go:169` | backlog2 §7 B2-X-01 |
| D2-FU-01 | **defaultRuntimeOptions wiring 语义断言加强** — 现状: `TestDefaultRuntimeOptions_IncludesRedisHealthAndCloser` 只断言 `assert.Len(withRedis, len(base)+2)`，listener 组合 / internal auth chain / verbose token 触发等语义未被这个测试锁定（其他 wiring 语义在 subscription_validator_wiring_test / vault_readiness_wiring_test 已有覆盖）；修复: 在 bundle_test.go 增加 listener type 集合 / verbose 开关行为的 typed 断言 | test | P2/Cx2 | 🟡 | — | `cmd/corebundle/bundle_test.go:133-147` | PR #443 6-seat review F1（OUT_OF_SCOPE 登记）|
| B2-X-08 | **cmdrun Windows 进程组杀不完** — 现状: Windows 平台进程组不彻底；修复: JobObject 或 taskkill /T | bug | P2/Cx2 | 🟡 | Windows 平台用例 | `pkg/cmdrun/cmdrun_windows.go` | backlog2 §7 B2-X-08 |
| JOURNEY-AUDITLOGINTRAIL-CRITERIA-EXPANSION-01 | **J-auditlogintrail passCriteria 扩展** — 现状（PR #588 落地后）: J-auditlogintrail lifecycle: active + 2 个 mode: auto criteria (`event-consume` / `hash-chain`) 全部 Docker-free 覆盖；但两处 silent gap 按 ai-collab.md「PR 范围切割必须显式 backlog」规则同 PR 登记追踪：(a) **`http.audit.list.v1` query criterion 缺失** — yaml `contracts:` 含 `http.audit.list.v1`（PR-4 fix/208 JOURNEY-CONTRACT-EXISTENCE-01 反向引用补齐），但 passCriteria 无对应 criterion 测「审计 hash chain 写入后可被 /api/v1/audit/entries 查询到」端到端可见性；auditquery 路径 `cells/auditcore/cell_test.go::TestAuditCore_RouteQueryEntries` 已证 Docker-free 可测，扩 yaml 一条 mode: auto criterion + tests/integration 新文件即可闭环。(b) **roadmap 029 C6 描述「accesscore 投影」第 3 段未落地** — roadmap 原文「断言审计链写入 → 登录事件发布 → accesscore 投影三段完整性」中第 3 段在代码里不存在：grep `audit.appended` 在 `cells/accesscore/**` 无 consumer，event.audit.appended.v1 当前仅在 auditcore 4 个 auditappend* slice 中 publish；本 PR 按 yaml 真值源仅实现 2 段。触发条件 = (a) 任何 auditquery 端到端可见性回归需求 / `make verify` 扩 docker-free e2e 范围；(b) accesscore 新增 audit.appended consumer / projection use-case，或 SIEM 等 actor 通过 actors.yaml 注册成为 audit.appended subscriber。**实施前不可静默关闭本条目**：未来扩 J-auditlogintrail.yaml criterion 时 inline 注释需引用本 backlog ID；若 audit 投影方向变更（如改由 SIEM actor 消费）按 ADV-06 actor 路径更新 contracts + 同 PR 关闭本条目 | test | P3/Cx3 | 🟠 | 触发型 — 见描述 | `journeys/J-auditlogintrail.yaml`（criteria 扩展）+ `tests/integration/`（新 criterion test）+ 可能 `cells/accesscore/`（audit consumer projection） | PR #588 (620-c6-journey-auditlogintrail-e2e) AI-rebust trigger backlog |
| REQUIRED-DEP-NIL-GUARD-01 | **Service required-dep nil-guard archtest（Soft→Hard 升级）** — 现状: 每个 `cells/*/slices/*/service.go` `NewXxx` 手动写 `validation.IsNilInterface` 检查，OUTBOX-SERVICE-01 archtest 只守 `txRunner` 一个字段；rbacassign / authorizationdecide / rbaccheck 历史漏 nil guard，PR #490 第五轮 review 手动补齐五处 accesscore service。修复: 用 `typeseval` 扫每个 `func NewXxx(...) (*Xxx, error)` 入参签名，对照 body 前 30 行 nil guard 调用集合 + 同步把 `authorizationdecide.NewService` 改 error-return 形态；新增 RED fixture 拒新 service 漏 guard。AI-rebust: Soft（命名 convention + 个人自觉）→ Hard（archtest type-aware）| archtest | P2/Cx3 | 🟡 | 下次新增 service 触发 | `tools/archtest/` + `pkg/validation/IsNilInterface` + ai-collab.md §"Soft → Hard 改造方向" | PR #490 第五轮 review 重评估 |
| ENFORCESESSIONSTATE-HOTPATH-OPT-01 | **sessionvalidate enforceSessionState 两次串行 PG 读优化** — 现状: 每次 access token 校验走 `sessionStore.Get(sid)` + `userRepo.GetByID(sub)` 两次串行 PG 读 (~2 round-trip 每请求)；plan #034 §HIGH-4 决策为不包 read-only tx，无快照保证。无 QPS 数据时不是优化目标。修复方向: (a) 单 SQL JOIN sessions + users (cell-private SQL，避免跨 repo 协议化), (b) Redis epoch snapshot cache (TTL ≤ JWT 短 exp), (c) 单 access token 校验请求合并 cache。触发条件: 生产 QPS 超过观测阈值（暂未设定）或 P99 校验延迟受 DB 影响。设计 ADR 待写 | refactor | P3/Cx3 | 🟡 | QPS / 延迟阈值触发 | `cells/accesscore/slices/sessionvalidate/service.go:118-188` | PR #490 第五轮 review 重评估 |
| AUTH-CACHE-SUBJECT-REVERSE-INDEX-01 | **session cache RevokeForSubject 反向集合** — 现状: T5 (AUTH-CACHE-01) 落地的 `adapters/redis.CachingSessionStore.RevokeForSubject` 不 touch cache，依赖 `credentialinvalidate.Apply` 同 tx bump `users.authz_epoch` + sessionvalidate.go:202 `user.AuthzEpoch != view.AuthzEpochAtIssue` 兜底（user 状态永不进 cache）；缺一个反向索引 `accesscore:subject:<id>:sessions`（Spring Session `PRINCIPAL_NAME` 风格）让 cache 层批量 Delete subject 的全部 session entries。修复方向: Create 时 `SADD accesscore:subject:<id>:sessions <sid>`；Revoke 时 SREM；RevokeForSubject 时 SMEMBERS + 批量 Del。触发条件: 任何需要给 `userrepo.Repository` 加 cache 的优化（一旦 user.AuthzEpoch 进缓存，epoch fallback 不再成立，必须先有 cache 层一致性兜底）。AI-rebust: 当前 Soft（epoch invariant + godoc），升级路径 Medium（archtest 守 `*Caching*` 不能 wrap `userrepo.Repository`）→ Hard（反向集合 + 类型层禁止 user cache 绕过反向索引）。 | feat | P3/Cx3 | 🟡 | user.AuthzEpoch 进 cache 优化需求 | `adapters/redis/session_cache_store.go` + `cmd/corebundle/access_module.go` | PR T5 AUTH-CACHE-01 |
| AUTH-CACHE-METRICS-01 | **session cache hit/miss/error 指标** — 现状: `CachingSessionStore` 仅通过 slog 暴露 cache 状态（Warn for cache miss / corrupt entry / invalid entry / Delete cleanup failure），无 Prometheus 维度上的 hit_total / miss_total / error_total 指标，运维侧无法直接区分"cache 命中率下降"与"Redis 局部故障"。**PR #533 第三轮 review 后单 session logout 的 stale-cache 窗口无任何 slog 信号**（pre-existing active entry satisfies validate；revoked 状态仅靠 TTL 自然过期），运维需通过 Prometheus counters + dashboards (`cache_hit AND pg.revoked_at != nil` join) 才能观测 stale 量级。所有现有 `adapters/redis` primitive（Cache / IdempotencyClaimer / NonceStore）同样不发指标，是 PR T5 之前的既有现状。触发条件: 生产部署需要 SLO 或调优 cache TTL 时启动。修复方向: 通过 `runtime/observability/metric.Provider`（参照 outbox / http 指标模式）注册 `session_cache_{hits,misses,errors}_total` counter，cell label = "accesscore"。 | feat | P3/Cx2 | 🟡 | SLO 接入 / TTL 调优 | `adapters/redis/session_cache_store.go` | PR T5 review B4 |
| AUTH-CACHE-HAPPY-PATH-WIRING-TEST-01 | **wrapSessionStoreWithCache 成功路径测试** — 现状: `cmd/corebundle/access_module_session_cache_test.go` 覆盖了 wrap helper 的 disable-decision 全枝（env 未设 / 解析失败 / TTL 非正 / nil RedisClient / 构造失败），但未直接断言"env 有效 + 真实 Redis 客户端 → 返回 `*CachingSessionStore`"路径，因为 `adapters/redis.NewClient` 在构造期做 Ping 真实连接，无法 in-process 桩化。修复方向: 给 `adapters/redis` 增加一个 `internal/testapi` 子包暴露 `NewClientForTest(cmdable)` helper，或者将 wiring 决策 helper 改造为接受 `*redis.Cache` 而非 `*redis.Client`（让 cmd/corebundle 测试可注入 mock cache）。 | test | P3/Cx2 | 🟡 | 拓展点：需要 in-process 测真实 wrap 时 | `cmd/corebundle/access_module.go`+`cmd/corebundle/access_module_session_cache_test.go` | PR T5 review A8 |
| AUTH-CACHE-AFTER-COMMIT-INVALIDATION-01 | **session cache 单 session logout AfterCommit 零延迟失效** — 现状: PR #524 第三轮 review Q1=B 决策接受 stale ≤ TTL（max 30s）作为单 session logout 的安全 floor（与 RevokeForSubject epoch-fallback 对称）；in-transaction Redis DEL 因与并发 re-population 竞争（PG uncommitted row → cache-miss → lazy-populate → 2×TTL 窗口）被删除。触发条件: 产品 SLO 要求单 session logout 的缓存失效延迟 < TTL（如 <1s）。修复方向: (a) 新增 `kernel/persistence.Finalizer` 接口（Spring `TransactionSynchronization.afterCommit` 范式）；(b) `adapters/postgres.TxManager` 在 commit 成功后 fire Finalizer hooks；(c) `adapters/redis.CachingSessionStore.Revoke` 注册 Finalizer 回调，在 AfterCommit hook 内通过 `ctxutil.WithDetachedTimeout` 发起 detached DEL（避免 tx 内竞态，且 DEL 不阻塞 Revoke 返回路径）。AI-rebust: Hard 候选（sealed Finalizer interface + ctx-based funnel 限制 hook 注册点）。 | feat | P3/Cx3 | 🟡 | 产品 SLO 要求单 session logout sub-TTL 失效延迟 | `kernel/persistence/` (新) + `adapters/postgres/tx_manager.go` + `adapters/redis/session_cache_store.go` | PR #524 第三轮 review Q1=B 决策 |
| TYPED-NIL-ARCHTEST-ADAPTERS-EXTENSION-01 | **adapters/ 层 typed-nil nil-guard archtest 扩展** — 现状: `ERROR-FIRST-TYPED-NIL-01` archtest 当前仅锁 `kernel/` + `runtime/` 层强制使用 `pkg/validation.IsNilInterface`（见 `.claude/rules/gocell/runtime-api.md`）；PR #524 第三轮 review 在 `adapters/redis/session_cache_store.go:125` 修补一个具体 `inner == nil` → `IsNilInterface(inner)` 实例，但 `adapters/` 层新增 constructor 无 Hard 守卫。触发条件: adapters/ 层出现第二个 typed-nil 漏检 finding，或新 adapter constructor 大规模引入（≥3 个 `inner == nil` 实例）。修复方向: 扩展 `ERROR-FIRST-TYPED-NIL-01` archtest 范围涵盖 `adapters/` 层，或建立等价的 adapter-level archtest，强制接口类型入参使用 `pkg/validation.IsNilInterface`。AI-rebust: Hard（扩已有 archtest pattern 范围，无新机制）。 | refactor | P3/Cx3 | 🟡 | adapters/ 层出现第二个 typed-nil 漏检 / 新 adapter constructor 大规模引入 | `tools/archtest/` + `adapters/` (所有 constructor) | PR #524 第三轮 review finding #6 |
| SCAFFOLD-TEST-FIXTURE-DEDUP-01 | **scaffold test 项目 fixture helper 跨包重复** — 现状: `kernel/assembly/generator_scaffold_test.go::scaffoldTestProject` 与 `cmd/gocell/app/scaffold_assembly_test.go::setupAssemblyTestProject` 两份独立实现生成同一 tempdir+go.mod+cell.yaml 骨架（仅返回签名差异：前者返回 `(root, *ProjectMeta)`，后者仅 `root`）。cell.yaml schema 字段未来变动需双处同步，已知漂移面但非本 PR 引入；修复: 二选一 (a) 在 `kernel/assembly/assemblytest/`（新）暴露 `NewTestProject(t, cellID) (root string, pm *metadata.ProjectMeta)` 供 cmd 测试 import，cmd 包内 wrapper 丢弃 pm；(b) 全仓 grep 确认仅这 2 个调用点后做最小 internal helper 共享。AI-rebust: Cx2 跨包重构（不引入 Hard 守卫，仅消除手动同步债） | refactor | P3/Cx2 | 🟢 | cell.yaml schema 字段新增 / 第 3 个调用点出现 | `kernel/assembly/generator_scaffold_test.go` + `cmd/gocell/app/scaffold_assembly_test.go` + `kernel/assembly/assemblytest/` (可能新) | PR #527 L4 review (测试 / DX) |
| SESSIONLOGIN-LOCKOUT-ARCHTEST-BLINDSPOT-01 | **sessionlogin-lockout-funnel depguard 反向自检补全** — 现状: `.golangci.yml` depguard rule `sessionlogin-lockout-funnel`（PR-618 落地）是路径级 import ban（Hard，charter §3 合规载体），但 `//nolint:depguard` 注解可绕过且无 archtest 兜底，符合 charter §"Soft → Hard 改造方向" 场景。修复方向: 新增 archtest 扫描 `cells/accesscore/slices/sessionlogin/` 下所有 `//nolint:depguard` 注解，发现即 FAIL（反向自检：断言该注解在 production AST 不出现）；或通过 `golangci-lint` `nolintlint` 配置拒绝 `//nolint:depguard` 而无 `issues-exit-code` 豁免。**触发条件**: 观察到 lint bypass 尝试（PR review 时出现 `//nolint:depguard` in sessionlogin package） | arch-opt | P3/Cx3 | 🟢 | 观察到 lint bypass 尝试（sessionlogin 包出现 //nolint:depguard） | `tools/archtest/` (新 test) + `.golangci.yml` (`nolintlint` 配置) | PR-618 review F7 |
| CONFIG-ROLLBACK-CAS-HARD-UPGRADE-01 | **configpublish.Rollback CAS Medium → Hard 升级路径** — 现状: D5 PR-V11-CONFIG-ROLLBACK-OPTLOCK (PR #583) 测试侧落地的是 **Medium runtime integration regression guard**——`cells/configcore/slices/configpublish/service_integration_test.go::TestConcurrentRollback_PG_ExactlyOneWins` + `_Sensitive_ExactlyOneWins` 双分支 testcontainers + 双 goroutine 并发 + assertion (`successes==1 / versionConflicts==1 / version+=1`)。CAS WHERE 谓词 (`cells/configcore/internal/adapters/postgres/config_repo.go:553-565` `WHERE key=$X AND version=$expectedVersion`) 是普通 Go SQL 字符串，删除 `AND version=$N` 编译通过、单元测试不触发，靠 CI 集成测试 RED 拦截——按 ai-collab.md AI-rebust 三档定义属 Medium（runtime invariant guard），非 Hard（违反不可表达）。修复方向（任一 / 组合达 Hard）: (a) **query funnel**: SQL 通过 `kernel/state/cas` typed query builder 派生，CAS 谓词作为必填字段；删 builder 字段编译失败；(b) **codegen-derived predicate**: contract.yaml `concurrencyControl: optimistic` 字段 → codegen 派生 repo CAS SQL，generated/ 严禁手工编辑；(c) **typed CAS marker**: `WHERE` 子句通过 `pkg/state.OptLockGuard[T]` typed wrapper 注入，repo 方法签名携带 `cas.VersionMatch` typed arg，类型系统拒绝构造无 version 比较的 OptLock SQL。Trigger（任一达成才起 PR）: (i) 第二个 cell repo 需要 OptLock CAS（≥ 2 实例触发抽 funnel 经济性阈值）；(ii) `WHERE version=$N` 字符串模式在 grep 全仓出现 ≥ 3 个独立 SQL 中（需 Hard 防泛化漂移）；(iii) D5 P0 安全事故（CAS 静默丢失致并发双写真实发生）。AI-rebust: Hard（升级路径，本条目登记为 Medium → Hard 转化跟踪）。 | arch-opt | P3/Cx3 | 🟠 | 第二个 cell OptLock 需求 / `WHERE version=$N` 字符串模式 ≥ 3 处 / D5 真实并发双写事故 | `cells/configcore/internal/adapters/postgres/config_repo.go` + `kernel/state/cas/` (可能新) + `tools/codegen/contractgen/` (可能扩 concurrencyControl) | PR #583 review P3 (AI-rebust Medium 而非 Hard 评级澄清) |
| YAMLSAFE-IMPLICIT-TYPED-SCALAR-DEFENSE-01 | **yamlsafe.needsQuoting 不防 YAML implicit-typed scalar coercion** — 现状: PR #527 commit `27e98469f` 让 `pkg/yamlsafe.needsQuoting` 覆盖结构破坏面（leading `-/?/:`+space、trailing whitespace、`---`/`...`），但 YAML 1.2 §10.2.1 implicit-typed tagged strings（boolean: `true`/`false`/`yes`/`no`/`on`/`off` 全大小写；int: 纯数字串、八进制 `0o*`、十六进制 `0x*`；float: `1.0`/`.inf`/`.nan` 等；null: `null`/`Null`/`NULL`/`~`/空）会被通用 YAML 工具（yq / kubectl / 外部 codegen）按 native 类型解析。GoCell 内当前消费链（kernel/metadata parser → typed struct + governance + catalog wire DTO）全部 typed-string 字段，yaml.v3 coerce 为字符串 → 无 production bug；外部生态消费 GoCell scaffold 产物时（外部 YAML→JSON 工具 / 跨 framework codegen）OwnerTeam=`"true"` 读到 `true(bool)` 是语义跨工具不一致。PR #527 用户决定保守不修（仅覆盖结构破坏面），避免所有 plain scalar 被引号包围导致 yaml 文件可读性下降；非 silent carveout。修复方向: 扩 `needsQuoting` 加 YAML 1.2 §10.2.1.1-4 boolean/int/float/null tagged-string 全集；fixture 加 round-trip 测试 `team=true` / `role=null` / `cells=[123,foo]` 渲染产物经 yaml.v3 + yq 双向 unmarshal 字段值类型保持 string；archtest YAML-QUOTE-FUNNEL-01 + needsQuoting 覆盖范围 godoc 同步更新。AI-rebust: Cx1 单文件扩展（pkg/yamlsafe/yamlsafe.go::needsQuoting）；不引入 Hard 守卫（needsQuoting 已是 yamlsafe.Quote 单漏斗内部决策点，扩展只是 predicate 精度提升）。 | arch-opt | P3/Cx1 | 🟢 | 外部 YAML→JSON 工具消费 GoCell scaffold 产物 / metadata 字段值实际出现 `"true"`/`"123"`/`"null"` 真实事故 / GoCell 引入跨 framework codegen（如 helm/kustomize 派生） | `pkg/yamlsafe/yamlsafe.go` + `pkg/yamlsafe/yamlsafe_test.go` | PR #527 round-2 review (用户保守决策 carveout) |

