// Code generated by gocell generate contract. DO NOT EDIT.
// source: contracts/http/sample/responseprojection/v1/contract.yaml

package responseprojection

import (
	"bytes"
	"context"
	"encoding/json"
	"log/slog"
	"net/http"

	"github.com/ghbvf/gocell/framework/pkg/errcode"
	"github.com/ghbvf/gocell/framework/pkg/httputil"
	"github.com/ghbvf/gocell/framework/pkg/projection"
)

// Request — http.sample.responseprojection.v1.request
type Request struct{}

// Response — http.sample.responseprojection.v1.response
type Response struct {
	Data projection.ResourceProjection `json:"data"`
}

// ResponseData is a generated DTO for contract http.sample.responseprojection.v1.
type ResponseData struct {
	ID    string `json:"id"`
	Label string `json:"label,omitempty"`
}

// ToMap projects ResponseData into the column map the masking funnel
// (projection.NewProjection / NewProjectionList) consumes. It is exported because
// the read handler lives in the cell package, not this generated package, and must
// feed the map to the funnel — the only way to populate the projection-typed
// Response.Data. ToMap itself returns a plain map and grants no bypass: a
// []projection.ResourceProjection is obtainable solely via the sealed constructors.
// Keys mirror the wire JSON field names so the masked view's field set equals this
// DTO's field set. Optional fields (omitempty) are only included when non-zero,
// matching the struct json.Marshal serialization path.
func (i ResponseData) ToMap() map[string]any {
	m := map[string]any{
		"id": i.ID,
	}
	if i.Label != "" {
		m["label"] = i.Label
	}
	return m
}

// ResponseprojectionResponseObject is the typed response envelope for
// http.sample.responseprojection.v1. Service.Responseprojection must return one of the
// Responseprojection{Status}{Suffix} structs declared below; the
// generated handler dispatches via the unexported method, which keeps the
// implementation set closed to types declared in this package.
//
// ref: oapi-codegen pkg/codegen/templates/strict/strict-responses.tmpl@main
//
//	(buffer-then-commit pattern: encode to bytes.Buffer first so encode
//	 failures don't commit a half-written response status to wire)
type ResponseprojectionResponseObject interface {
	visitResponseprojectionResponse(ctx context.Context, w http.ResponseWriter) error
}

// Responseprojection200JSONResponse renders an HTTP 200 success response.
// Marshals the underlying Response DTO as a JSON body.
type Responseprojection200JSONResponse Response

func (r Responseprojection200JSONResponse) visitResponseprojectionResponse(ctx context.Context, w http.ResponseWriter) error {
	var buf bytes.Buffer
	if err := json.NewEncoder(&buf).Encode(Response(r)); err != nil {
		// encode 失败时 header 尚未提交，handler 收到 err 后可走 WriteError 兜底 5xx
		attrs := httputil.AppendCorrelationAttrs(ctx, []any{slog.Any("error", err)})
		slog.ErrorContext(ctx, "http.sample.responseprojection.v1: encode Responseprojection200JSONResponse body", attrs...)
		return err
	}
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(200)
	_, _ = buf.WriteTo(w)
	return nil
}

// Responseprojection404ErrorResponse renders an HTTP 404 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Responseprojection404ErrorResponse struct {
	Body errcode.Error
}

func (r Responseprojection404ErrorResponse) visitResponseprojectionResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 404, &r.Body)
	return nil
}
