// Code generated by gocell generate contract. DO NOT EDIT.
// source: contracts/http/sample/idempotencyexempt/v1/contract.yaml

package idempotencyexempt

import (
	"bytes"
	"context"
	"encoding/json"
	"log/slog"
	"net/http"

	"github.com/ghbvf/gocell/pkg/errcode"
	"github.com/ghbvf/gocell/pkg/httputil"
)

// Request — http.sample.idempotencyexempt.v1.request
type Request struct {
	Message string `json:"message"`
}

// Response — http.sample.idempotencyexempt.v1.response
type Response struct {
	Ok bool `json:"ok"`
}

// IdempotencyexemptResponseObject is the typed response envelope for
// http.sample.idempotencyexempt.v1. Service.Idempotencyexempt must return one of the
// Idempotencyexempt{Status}{Suffix} structs declared below; the
// generated handler dispatches via the unexported method, which keeps the
// implementation set closed to types declared in this package.
//
// ref: oapi-codegen pkg/codegen/templates/strict/strict-responses.tmpl@main
//
//	(buffer-then-commit pattern: encode to bytes.Buffer first so encode
//	 failures don't commit a half-written response status to wire)
type IdempotencyexemptResponseObject interface {
	visitIdempotencyexemptResponse(ctx context.Context, w http.ResponseWriter) error
}

// Idempotencyexempt201JSONResponse renders an HTTP 201 success response.
// Marshals the underlying Response DTO as a JSON body.
type Idempotencyexempt201JSONResponse Response

func (r Idempotencyexempt201JSONResponse) visitIdempotencyexemptResponse(ctx context.Context, w http.ResponseWriter) error {
	var buf bytes.Buffer
	if err := json.NewEncoder(&buf).Encode(Response(r)); err != nil {
		// encode 失败时 header 尚未提交，handler 收到 err 后可走 WriteError 兜底 5xx
		attrs := httputil.AppendCorrelationAttrs(ctx, []any{slog.Any("error", err)})
		slog.ErrorContext(ctx, "http.sample.idempotencyexempt.v1: encode Idempotencyexempt201JSONResponse body", attrs...)
		return err
	}
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(201)
	_, _ = buf.WriteTo(w)
	return nil
}

// Idempotencyexempt400ErrorResponse renders an HTTP 400 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Idempotencyexempt400ErrorResponse struct {
	Body errcode.Error
}

func (r Idempotencyexempt400ErrorResponse) visitIdempotencyexemptResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 400, &r.Body)
	return nil
}

// Idempotencyexempt500ErrorResponse renders an HTTP 500 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Idempotencyexempt500ErrorResponse struct {
	Body errcode.Error
}

func (r Idempotencyexempt500ErrorResponse) visitIdempotencyexemptResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 500, &r.Body)
	return nil
}
