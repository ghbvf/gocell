// Code generated by gocell generate contract. DO NOT EDIT.
// source: contracts/http/sample/permission/v1/contract.yaml

package permission

import (
	"context"
	"net/http"

	"github.com/ghbvf/gocell/framework/pkg/errcode"
	"github.com/ghbvf/gocell/framework/pkg/httputil"
)

// Request — http.sample.permission.v1.request
type Request struct {
	// format: uuid
	ID string `json:"id"`
}

// Response — http.sample.permission.v1.response
type Response struct{}

// PermissionResponseObject is the typed response envelope for
// http.sample.permission.v1. Service.Permission must return one of the
// Permission{Status}{Suffix} structs declared below; the
// generated handler dispatches via the unexported method, which keeps the
// implementation set closed to types declared in this package.
//
// ref: oapi-codegen pkg/codegen/templates/strict/strict-responses.tmpl@main
//
//	(buffer-then-commit pattern: encode to bytes.Buffer first so encode
//	 failures don't commit a half-written response status to wire)
type PermissionResponseObject interface {
	visitPermissionResponse(ctx context.Context, w http.ResponseWriter) error
}

// Permission204NoContentResponse renders an HTTP 204 No Content success response.
// The success status carries no body; this struct is a marker so the
// typed-envelope set covers the success case uniformly with errors.
type Permission204NoContentResponse struct{}

func (r Permission204NoContentResponse) visitPermissionResponse(ctx context.Context, w http.ResponseWriter) error {
	w.WriteHeader(204)
	return nil
}

// Permission400ErrorResponse renders an HTTP 400 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Permission400ErrorResponse struct {
	Body errcode.Error
}

func (r Permission400ErrorResponse) visitPermissionResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 400, &r.Body)
	return nil
}

// Permission500ErrorResponse renders an HTTP 500 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Permission500ErrorResponse struct {
	Body errcode.Error
}

func (r Permission500ErrorResponse) visitPermissionResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 500, &r.Body)
	return nil
}
