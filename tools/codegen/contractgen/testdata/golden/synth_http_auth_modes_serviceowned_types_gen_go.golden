// Code generated by gocell generate contract. DO NOT EDIT.
// source: contracts/http/sample/serviceowned/v1/contract.yaml

package serviceowned

import (
	"context"
	"net/http"

	"github.com/ghbvf/gocell/pkg/errcode"
	"github.com/ghbvf/gocell/pkg/httputil"
)

// Request — http.sample.serviceowned.v1.request
type Request struct {
	// format: uuid
	ID string `json:"id"`
}

// Response — http.sample.serviceowned.v1.response
type Response struct{}

// ServiceownedResponseObject is the typed response envelope for
// http.sample.serviceowned.v1. Service.Serviceowned must return one of the
// Serviceowned{Status}{Suffix} structs declared below; the
// generated handler dispatches via the unexported method, which keeps the
// implementation set closed to types declared in this package.
//
// ref: oapi-codegen pkg/codegen/templates/strict/strict-responses.tmpl@main
//
//	(buffer-then-commit pattern: encode to bytes.Buffer first so encode
//	 failures don't commit a half-written response status to wire)
type ServiceownedResponseObject interface {
	visitServiceownedResponse(ctx context.Context, w http.ResponseWriter) error
}

// Serviceowned204NoContentResponse renders an HTTP 204 No Content success response.
// The success status carries no body; this struct is a marker so the
// typed-envelope set covers the success case uniformly with errors.
type Serviceowned204NoContentResponse struct{}

func (r Serviceowned204NoContentResponse) visitServiceownedResponse(ctx context.Context, w http.ResponseWriter) error {
	w.WriteHeader(204)
	return nil
}

// Serviceowned400ErrorResponse renders an HTTP 400 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Serviceowned400ErrorResponse struct {
	Body errcode.Error
}

func (r Serviceowned400ErrorResponse) visitServiceownedResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 400, &r.Body)
	return nil
}

// Serviceowned500ErrorResponse renders an HTTP 500 error response.
// Body carries an errcode.Error whose Kind/Code/Message/Details follow the
// canonical wire schema in contracts/shared/errors/error-response-v1.schema.json
// (5xx Details are stripped by Error.MarshalJSON; Internal never serializes).
type Serviceowned500ErrorResponse struct {
	Body errcode.Error
}

func (r Serviceowned500ErrorResponse) visitServiceownedResponse(ctx context.Context, w http.ResponseWriter) error {
	httputil.WriteErrorWithStatus(ctx, w, 500, &r.Body)
	return nil
}
