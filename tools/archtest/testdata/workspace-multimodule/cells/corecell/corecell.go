// Package corecell is the core module's cell package — proves a core cell is
// scanned and classified as LayerCells.
package corecell

// Greeting is referenced by the satellite module to create a real
// satellite -> core import edge (the ALLOWED workspace dependency direction).
const Greeting = "hello from core"
