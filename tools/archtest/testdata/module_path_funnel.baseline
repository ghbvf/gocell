# ARCHTEST-MODULE-PATH-FUNNEL-01 frozen migration baseline (the backlog).
# One repo-relative path per line: tools/archtest files that STILL contain a
# bare "github.com/ghbvf/gocell" literal instead of deriving from
# PlatformModulePath. This file is a MONOTONE CEILING — never regenerated by
# -update. A new offender not listed here fails CI (no -update escape); a
# migrated file must be DELETED from this list (ratchet-down). EMPTY (the state
# reached now, #1302 complete) = Medium pure-ban terminal state: the exception
# escape is gone, but enforcement stays at CI test time, so upstream is Medium —
# type-system Hard ("do not write this literal in source") is a permanent Go
# ceiling, not reached by an empty baseline (#1304). The downstream detection
# Hard-ization (typed const-eval closing const-of-const / cross-package
# reconstruction) lives in module_path_funnel_test.go. Tracked by #1302; #1304.
# blank lines and # comments are ignored.
