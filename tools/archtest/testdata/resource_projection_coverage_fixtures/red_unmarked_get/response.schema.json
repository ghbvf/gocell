{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "fixture.red.unmarked.get.response",
  "description": "RED fixture: a GET read response that wraps a resource in a top-level `data` object but whose contract is NOT responseProjection-marked. The coverage predicate MUST flag this — a resource-bearing GET that skips the masking funnel.",
  "type": "object",
  "required": ["data"],
  "properties": {
    "data": {
      "type": "object",
      "required": ["id", "secret"],
      "properties": {
        "id": { "type": "string" },
        "secret": { "type": "string" }
      }
    }
  }
}
