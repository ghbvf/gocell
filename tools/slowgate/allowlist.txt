# slowgate allowlist — tests permitted to exceed the default 20s budget.
#
# Format: each data line is `Package<TAB>TestName`. Every data line MUST
# be preceded by a `# <reason>` comment explaining why the wall-clock cost
# is unavoidable. The companion archtest SLOWGATE-ALLOWLIST-01 enforces
# both the comment rule and that every (Package, TestName) maps to a real
# top-level test func. Independent table-driven negative cases for the
# parser live in TestLoadSlowgateAllowlist_ParseRules (same file).
#
# Threshold rationale (20s, see tools/slowgate/main.go defaultThreshold):
# packages.Load-bound and subprocess-go-toolchain tests run 5–15× slower
# on GHA ubuntu-latest 2-CPU runners than on developer laptops. 2s and 5s
# thresholds both produced perpetual allowlist churn driven by hardware
# mismatch, not real regression. 20s lets the slow-by-design population
# pass while still catching meaningful regressions (a 100ms sleep drifting
# to 21s+ still fires).
#
# Adding entries: only when CI has empirically failed the gate. The
# failing slowgate step prints `SLOW <Package> <TestName> <Elapsed>` lines
# directly to stderr in the GHA step log — append the matching pair here
# under the appropriate `# <category>:` comment with a non-empty reason.

# ----------------------------------------------------------------------
# type-graph-load: tests use packages.Load over the entire module type
# graph. Cost is dominated by Go's package loader and `go list` walk,
# scales with module size; ubuntu-latest 2-CPU runners hit 16–46s on
# the larger scans.
# ----------------------------------------------------------------------

# type-graph-load: clock injection callsite scan over module
github.com/ghbvf/gocell/tools/archtest	TestClockInjectionCallsite
# type-graph-load: clock injection prod callsite scan over module
github.com/ghbvf/gocell/tools/archtest	TestClockInjectionProdCallsite
# type-graph-load: errcode message-const-literal scan over module
github.com/ghbvf/gocell/tools/archtest	TestErrcodeMessageConstLiteral
# type-graph-load: exported error-new scan over module
github.com/ghbvf/gocell/tools/archtest	TestExportedErrorNew
# subprocess-overhead: ARCHTEST-VERIFY-COVERAGE-01 spawns 1 verify-archtest.sh
# DRY_RUN + K=4 LIST_SHARD_TESTS subprocesses, each running `go test -list`
# over tools/archtest. ~5×subprocess go-test-list ≈ 25-30s on GHA. Not a
# cache-miss bottleneck (subprocess cache is independent of TestMain warmup),
# so PR #584 in-process warmup does not help. Cleanup path (K=4 → K=2 or
# bash-script-side dispatch consolidation) tracked by
# ARCHTEST-VERIFY-COVERAGE-DISPATCH-OPTIMIZE-01 in cap-14-tooling.md.
github.com/ghbvf/gocell/tools/archtest	TestArchtestVerifyCoverage01
# type-graph-load: PANIC-REGISTERED-01 scan over module — iterates 7 build-tag
# sets (typeseval.KnownNonDefaultTags), each driving its own packages.Load
github.com/ghbvf/gocell/tools/archtest	TestPanicRegistered
# type-graph-load: kernel/clock leaf-fallback scan over module
github.com/ghbvf/gocell/tools/archtest	TestKernelClockLeafFallback
# type-graph-load: kernel/clock reset-relative scan over module
github.com/ghbvf/gocell/tools/archtest	TestKernelClockResetRelativeProd
# type-graph-load: layering rules walk over module dependency graph
github.com/ghbvf/gocell/tools/archtest	TestLayeringRules
# type-graph-load: lint-gate smoke (subprocess golangci-lint warmup)
github.com/ghbvf/gocell/tools/archtest	TestLintGateSmoke
# type-graph-load: metric-label errcode classifiers Ack scan over module
github.com/ghbvf/gocell/tools/archtest	TestMetricLabelErrcodeClassifiersRequireAck
# type-graph-load: prod clock injection scan over module
github.com/ghbvf/gocell/tools/archtest	TestProdClockInjection
# type-graph-load: prod duration const scan over module
github.com/ghbvf/gocell/tools/archtest	TestProdDurationConst
# type-graph-load: slowgate allowlist drift guard (this test) loads referenced packages
github.com/ghbvf/gocell/tools/archtest	TestSlowgateAllowlist
# type-graph-load: test-sleep discipline scan over module
github.com/ghbvf/gocell/tools/archtest	TestTestSleepDiscipline
# type-graph-load: test-time literal const scan over module
github.com/ghbvf/gocell/tools/archtest	TestTestTimeLiteralConst
# type-graph-load: test-time literal fixtures cross-validation
github.com/ghbvf/gocell/tools/archtest	TestTestTimeLiteralFixtures

# ----------------------------------------------------------------------
# subprocess-go-toolchain: tests invoke `go build` / `go test` /
# `go list` as subprocess. CI hardware adds ~10s of toolchain startup +
# module resolution per invocation; multi-call tests amplify.
# ----------------------------------------------------------------------

# subprocess-go-toolchain: kernel/verify subprocess go with caller-selected GOROOT/PATH
github.com/ghbvf/gocell/kernel/verify	TestRunGoTest_UsesCallerSelectedGoAndAdditivePATH
# subprocess-go-toolchain: kernel/verify subprocess go test against temp module
github.com/ghbvf/gocell/kernel/verify	TestRunGoTest_WithTempModule
# subprocess-go-toolchain: kernel/verify integration test runs slice with metadata refs
github.com/ghbvf/gocell/kernel/verify	TestVerifySlice_WithMetadataRefs

# ----------------------------------------------------------------------
# verify-large-fixture: cmd/gocell verify-* commands replay full
# assembly/journey/cell YAML fixtures through validators. Dominant cost
# is YAML parsing + cross-file reference resolution; scales with the
# fixture set size.
# ----------------------------------------------------------------------

# verify-large-fixture: validate strict mode against full assembly fixtures
github.com/ghbvf/gocell/cmd/gocell/app	TestRunValidate_Strict_DetectsStaleActiveJourneyCheckRef
# verify-large-fixture: verify-cell command end-to-end
github.com/ghbvf/gocell/cmd/gocell/app	TestRunVerifyCell_ValidID
# verify-large-fixture: verify-journey command end-to-end
github.com/ghbvf/gocell/cmd/gocell/app	TestRunVerifyJourney_ValidID
# verify-large-fixture: verify-slice command end-to-end (largest fixture set)
github.com/ghbvf/gocell/cmd/gocell/app	TestRunVerifySlice_ValidID

# ----------------------------------------------------------------------
# external-network-timeout: tests assert the error path of dialing an
# unreachable endpoint. The dial backoff IS the test subject — the
# wall-clock cost is the configured retry/timeout budget.
# ----------------------------------------------------------------------

# external-network-timeout: OTLP exporter dial unreachable collector
github.com/ghbvf/gocell/adapters/otel	TestNewTracer_Insecure

# type-graph-load: credential-invalidate funnel RevokeForSubject scan — SharedResolver
# over ./cells/accesscore/... ./runtime/auth/... ./adapters/... ./cmd/...; Wave 2
# added domain_authz_mutation funnel tests, shifting the 16-shard modulo split so
# this test no longer co-locates with its sibling funnel tests'cached resolver.
github.com/ghbvf/gocell/tools/archtest	TestCredentialInvalidateFunnel_RevokeForSubject_01

# type-graph-load: credential-invalidate funnel BumpAuthzEpoch scan — same
# SharedResolver whole-repo profile as the RevokeForSubject sibling above;
# 16-shard modulo split places it on a shard without the cached funnel
# resolver. Sibling already allowlisted above; this variant was omitted when
# the S4d epoch funnel family grew (PR #494) — pre-existing gap, surfaced on
# PR #504 verify-archtest shard 14 (15.94s > 15s budget).
github.com/ghbvf/gocell/tools/archtest	TestCredentialInvalidateFunnel_BumpAuthzEpoch_01

# type-graph-load: cell-id-pattern single-source literal scan — whole-repo
# typeseval load over cells/ + runtime/ + adapters/; same 16-shard modulo
# resolver-cache-loss class as the credential funnel entries above. Borderline
# pre-existing flake (~15.3s), surfaced on PR #504 verify-archtest CI.
github.com/ghbvf/gocell/tools/archtest	TestCellIDPatternSingleSourceLiterals

# type-graph-load: cell-id-pattern single-source struct-field scan — sibling
# of TestCellIDPatternSingleSourceLiterals above, same whole-repo typeseval
# load over cells/ + runtime/ + adapters/. Pre-existing borderline flake.
# Surfaced on PR #850 fixup commit verify-archtest shard 13 (28.62s > 20s)
# after this PR's 3 new fixture packages shifted the 16-shard modulo
# distribution off the cached SharedResolver — same modulo-shift SLOW-flake
# class as the entries above (cumulative trigger evidence for
# ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestCellIDPatternSingleSource

# type-graph-load: user-repo conformance enrollment scan — type-aware
# types.Implements walk over the whole module to enumerate ports.UserRepository
# implementers plus per-test conformance.RunUserRepoConformance callee
# resolution. Same 16-shard modulo resolver-cache-loss class as the entries
# above; R2-P3 PR-b (#530) added TestNotFoundTestStrict + 5 EachInSubtreeStopAt
# tests + 4 RED fixtures, shifting the modulo distribution so this test
# landed on a shard without the cached funnel resolver (22.3s > 20s budget
# on shard 2). Pre-existing borderline test, not introduced by PR-b.
github.com/ghbvf/gocell/tools/archtest	TestUserRepoConformanceEnrollment

# type-graph-load: cell repo readyz probe scan — type-aware whole-module walk
# enumerating cell.RegisterRepoReadiness call sites plus cross-package
# *types.Func resolution against the readiness probe funnel. Same 16-shard
# modulo resolver-cache-loss class as entries above; second CI shift triggered
# by R2-P3 PR-b modulo redistribution (20.43s > 20s budget on shard 1, CI run
# 25970175361). Pre-existing borderline test, not introduced by PR-b. The
# repeating "single PR triggers ≥ 2 modulo-shift SLOW" pattern is the trigger
# evidence for backlog ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01 (cap-14:14)
# Hard upgrade — see that entry's Trigger column for cumulative count.
github.com/ghbvf/gocell/tools/archtest	TestCellRepoReadyzProbe

# type-graph-load: TYPESUTIL-IMPLEMENTS-FUNNEL-01 scan — single
# RunTypedProduction with Tests:true over the whole module (test variants
# included; Tests:true is mandatory because the consolidated callsites are
# tools/archtest/*_test.go). info.Uses sweep + *types.Func identity
# resolution against go/types.Implements. Same type-graph-load /
# 16-shard resolver-cache-loss class as the entries above (whole-module
# packages.Load with type info ≈ 20–46s on ubuntu-latest 2-CPU); a single
# load already exceeds the 20s budget, same as the sibling whole-module
# type-aware scans (TestUserRepoConformanceEnrollment 22.3s,
# TestCellRepoReadyzProbe 20.43s). R2-P2 PR-b.
github.com/ghbvf/gocell/tools/archtest	TestTypesutilImplementsFunnel01

# type-graph-load: PG-REPO-AMBIENT-TX-01 R1/R2/R3 scan — type-aware
# whole-module SharedResolver over the 3 pgRepoPackagePatterns
# (adapters/postgres + 2 cell-private PG adapters). PR #586 added iotdevice
# to the production-scope predicate (was previously a silent literal-list
# blind spot), bringing TestPGRepoAmbientTx's scan to all 3 patterns and
# pushing it 0.28s over the 20s slowgate budget on a cold-resolver shard.
# Same modulo-shift SLOW-flake class as the entries above (cumulative
# trigger evidence for ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestPGRepoAmbientTx

# type-graph-load: AUTHZ-MUTATION-APPLY-FUNNEL Rule (a) SetStatus scan — same
# whole-module SharedResolver profile as the credential funnel siblings above.
# Pre-existing borderline test (~17-19s on cached resolver). Surfaced on PR
# #586 verify-archtest shard 6 (21.17s > 20s) after #586's three new
# SQLSTATE-SINGLE-SOURCE tests shifted the 16-shard modulo distribution off
# the cached SharedResolver — same modulo-shift SLOW-flake class as the
# entries above (cumulative trigger evidence for
# ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestAuthzMutationApplyFunnel_SetStatus_01

# type-graph-load: CELLGEN-ERRCODE-FUNNEL-01 NoBuildTagFiles scan — fans
# out over KnownNonDefaultTags() (multiple RunTyped invocations, one per
# tag group; same pattern as TestPanicRegistered allowlisted above). Each
# RunTyped triggers an independent packages.Load over
# ./tools/codegen/cellgen/... with type info; cumulative cost on
# ubuntu-latest 2-CPU runners exceeds 20s. Introduced by develop@44b8803f5
# (PR #574, CELLGEN-ERRCODE-FUNNEL-01 Hard upgrade); surfaced on PR #576
# verify-archtest shard 12 (24.53s > 20s) after PR #576's new fixture
# packages shifted the 16-shard modulo distribution off the cached
# SharedResolver — same modulo-shift SLOW-flake class as the entries
# above (cumulative trigger evidence for ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestCellgenErrcodeFunnelNoBuildTagFiles

# type-graph-load: TEST-POLLING-EXTERNAL-REASON-LITERAL-01 blind-spot reverse
# self-test scans all types.Info.Uses entries module-wide to assert
# testwait.External never appears outside *ast.CallExpr.Fun position (closes
# 4 indirect-reference blind spots — function-variable, function-pointer
# pass-through, method-value binding, reflect). Two RunTyped passes (default
# tags + ProductionFlatTags) over ./... with Tests:true; surfaced at 23.23s
# on PR #594 verify-archtest shard 11 — same type-graph-load class as the
# entries above (whole-module SharedResolver + Tests:true ≈ 20–46s on
# ubuntu-latest 2-CPU). Pairs with TestExternalReasonLiteral (sibling main
# rule, also whole-module scan; round-2 fixture additions shifted modulo
# off cached resolver, surfaced at 23.53s on shard 9 — allowlisted below).
github.com/ghbvf/gocell/tools/archtest	TestExternalReasonLiteral_NoIndirectReferences

# type-graph-load: TEST-POLLING-EXTERNAL-REASON-LITERAL-01 main rule scans
# every testwait.External callsite module-wide. Two RunTyped passes
# (default tags + ProductionFlatTags) over ./... with Tests:true; same
# whole-module SharedResolver + Tests:true profile as the sibling reverse
# self-test above. The earlier comment predicted this rule would stay
# under 20s on the cached resolver path; PR #594 round-2 added 4 indirect
# RED fixture packages + the *_NoIndirectReferences_Fixtures sub-test,
# shifting the 16-shard modulo distribution off the cached resolver and
# surfacing the main rule at 23.53s on shard 9 — same modulo-shift
# SLOW-flake class as the entries above (cumulative trigger evidence for
# ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestExternalReasonLiteral

# type-graph-load: POSTGRES-NOTFOUND-TEST-OTHER-ERROR-MIXUP-ARCHTEST-01
# (PR #530) scans every fake-PG ErrNotFound mixup module-wide via the
# Pass-Driver funnel. Single RunTyped over ./... with Tests:true; same
# whole-module SharedResolver + Tests:true profile as the sibling
# TestEventuallyFunnel / TestExternalReasonLiteral entries above.
# Pre-existing borderline (PR #530 introduced it). Surfaced on PR #850
# fixup commit verify-archtest shard 3 (24.69s > 20s) after this PR's
# 3 new fixture packages shifted the 16-shard modulo distribution off
# the cached SharedResolver — same modulo-shift SLOW-flake class as the
# entries above (cumulative trigger evidence for
# ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestNotFoundTestStrict

# type-graph-load: TEST-EVENTUALLY-FUNNEL-01 main rule scans every banned
# testify (require|assert).Eventually* callsite module-wide. Walks both
# *types.Info.Uses (qualified-ident / *Assertions method-selector callsites)
# and the dual blind-spot probe through CallExpr.Fun resolution; combines
# with PRODUCTION-FLAT-TAGS scan for cgo / non-default-tag files. Whole-
# module SharedResolver with Tests:true; same modulo-shift SLOW-flake class
# as TestExternalReasonLiteral above (sibling DOWNSTREAM rule; this is the
# UPSTREAM half of the testwait Hard funnel landed by PR #847). Surfaced
# at 33.76s on PR #845 verify-archtest shard 3; the same shard was already
# failing on develop d4cf5d37 before this PR, so this entry retires a
# pre-existing develop-side gap rather than introduces a new SLOW.
github.com/ghbvf/gocell/tools/archtest	TestEventuallyFunnel

# type-graph-load: TEST-EVENTUALLY-FUNNEL-01 blind-spot reverse self-test
# walks all *types.Info.Uses + *types.Info.Selections entries module-wide
# to assert no testify Eventually* symbol appears outside CallExpr.Fun
# position (closes 5 indirect-reference shapes — function-variable,
# function-pointer pass, struct-field binding, method-value / method-
# expression, reflect). Two RunTyped passes (default + ProductionFlatTags)
# over ./... with Tests:true. Same whole-module SharedResolver profile as
# TestEventuallyFunnel above (sibling pair, mirrors TestExternalReasonLiteral
# vs TestExternalReasonLiteral_NoIndirectReferences). Surfaced at 21.86s
# on PR #845 verify-archtest shard 5 after this PR's new 8-file
# accesscoretest/ package + obmetrics import shifted the 16-shard modulo
# distribution off the cached resolver — same modulo-shift SLOW-flake
# class (cumulative trigger evidence for ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestEventuallyFunnel_NoIndirectReferences

# type-graph-load: CELL-ID-PATTERN-SINGLE-SOURCE main rule scans every
# regexp.MustCompile / regexp.Compile call module-wide for banned cell-id
# family literals (the sibling-allowlisted TestCellIDPatternSingleSourceLiterals
# entry above covers the literal-only variant; this rule is the type-aware
# callsite scan). Single RunTypedProduction with Tests:true + FlatNonDefaultTags
# over ./... — same whole-module SharedResolver profile as the entries above.
# Surfaced at 23.67s on PR #845 verify-archtest shard 13 after this PR's
# new 8-file accesscoretest/ package + obmetrics import shifted the
# 16-shard modulo distribution off the cached resolver — same modulo-shift
# SLOW-flake class (cumulative trigger evidence for ARCHTEST-SHAREDRESOLVER-AMORTIZATION-01).
github.com/ghbvf/gocell/tools/archtest	TestCellIDPatternSingleSource
